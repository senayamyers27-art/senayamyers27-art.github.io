/* AWS Certified Solutions Architect – Associate SAA-C03 — generated plan (no hand-written weeks). */
CertHub.register({
  id: "aws-saa",
  vendor: "Amazon Web Services",
  name: "AWS Certified Solutions Architect – Associate",
  short: "Solutions Architect Associate",
  exam: "SAA-C03",
  blurb: "For people who design systems on AWS: choosing and combining services into architectures that are secure, resilient, high-performing and cost-optimized, from IAM and VPC design to storage, databases, decoupling, disaster recovery and pricing models.",
  status: "check",
  statusNote: "Domains and weights (Design Secure Architectures 30%, Design Resilient Architectures 26%, Design High-Performing Architectures 24%, Design Cost-Optimized Architectures 20%) are taken from the published SAA-C03 exam guide as best known; the AWS certification pages could not be opened from this environment on Sept 25, 2026. Confirm the domains, weights, exam length and passing score on the official exam page and exam guide before booking, and check there whether AWS has announced a replacement exam version.",
  lastVerified: "2026-09-25",
  notices: [],
  examInfo: { questions: "65 (50 scored, 15 unscored; multiple choice and multiple response)", minutes: 130, pass: "720 on a 100–1000 scale", extra: "AWS recommends at least one year of hands-on experience designing cloud solutions on AWS. The certification is valid for three years." },
  examSim: { questions: 65, minutes: 130 },
  sources: [
    { label: "AWS Certified Solutions Architect – Associate exam page", url: "https://aws.amazon.com/certification/certified-solutions-architect-associate/" },
    { label: "SAA-C03 exam guide (AWS documentation)", url: "https://docs.aws.amazon.com/aws-certification/latest/solutions-architect-associate-03/solutions-architect-associate-03.html" }
  ],
  planWeeks: 12,
  hoursPerWeek: "6–8",

  domains: [
    {
      id: 1,
      name: "Design Secure Architectures",
      w: 30,
      topics: [
        "IAM users, groups, roles and policies: least privilege, identity-based vs resource-based policies and policy evaluation logic",
        "Multi-account security: AWS Organizations, service control policies, IAM Identity Center and cross-account roles",
        "Federation and temporary credentials: STS AssumeRole, SAML and OIDC federation, Cognito user pools vs identity pools",
        "VPC security layers: security groups vs network ACLs, public and private subnets, NAT gateways, bastion hosts vs Session Manager",
        "Private access to AWS services: gateway endpoints, interface endpoints (PrivateLink) and endpoint policies",
        "Protecting the edge: AWS WAF, Shield Standard vs Shield Advanced, and CloudFront with origin access control",
        "Encryption at rest with AWS KMS: AWS managed vs customer managed keys, key policies, envelope encryption and S3 SSE-S3, SSE-KMS and SSE-C",
        "Encryption in transit: ACM certificates, TLS on ALB and CloudFront, and enforcing HTTPS with aws:SecureTransport",
        "Secrets management: Secrets Manager rotation vs Systems Manager Parameter Store SecureString",
        "S3 data protection: Block Public Access, bucket policies, presigned URLs, versioning, MFA Delete and Object Lock modes",
        "Detection and compliance services: CloudTrail, AWS Config rules, GuardDuty, Inspector, Macie and Security Hub"
      ],
      notes: ["Exam guide task statements 1.1 Design secure access to AWS resources; 1.2 Design secure workloads and applications; 1.3 Determine appropriate data security controls"],
      labs: [
        "Free Tier: create an IAM role that EC2 can assume with read-only access to one S3 bucket, launch a t2.micro or t3.micro instance with that instance profile, and run `aws s3 ls s3://your-bucket` from it with no access keys configured. Then try to list a different bucket and read the AccessDenied error. Terminate the instance and delete the bucket afterwards.",
        "Free Tier: build a VPC with one public and one private subnet (no NAT gateway, which bills by the hour), put a security group that only allows HTTP from your IP on a public instance, then add a network ACL deny rule for your IP and watch it override the security group. Connect with Session Manager instead of SSH. Terminate the instance and delete the VPC when done.",
        "Free Tier: create a customer managed KMS key, upload an object to S3 with SSE-KMS using it, and find the matching Decrypt event in CloudTrail event history when you download it. Add a bucket policy that denies requests where aws:SecureTransport is false. Cost warning: each customer managed key costs about $1 a month, so schedule it for deletion (7-day minimum) when you finish."
      ]
    },
    {
      id: 2,
      name: "Design Resilient Architectures",
      w: 26,
      topics: [
        "Multi-AZ web tiers: Elastic Load Balancing (ALB vs NLB), Auto Scaling groups and ELB health checks",
        "Decoupling with Amazon SQS (standard vs FIFO, visibility timeout, dead-letter queues) and SNS fan-out",
        "Event-driven and serverless patterns: EventBridge, Lambda, Step Functions and API Gateway",
        "Containers on AWS: ECS vs EKS, and the Fargate vs EC2 launch types",
        "Relational database resilience: RDS Multi-AZ, read replicas, Aurora replicas and Aurora Global Database",
        "DynamoDB resilience: global tables, point-in-time recovery and on-demand backups",
        "Route 53 routing policies and health checks: failover, weighted, latency, geolocation and multivalue",
        "Disaster recovery strategies: backup and restore, pilot light, warm standby and multi-site active-active, matched to RPO and RTO",
        "Backup and replication: AWS Backup plans, S3 Cross-Region Replication, EBS snapshot and AMI copies, AWS Elastic Disaster Recovery",
        "Resilient hybrid networking: Site-to-Site VPN, Direct Connect with VPN backup, and Transit Gateway",
        "Infrastructure as code and service quotas: CloudFormation, StackSets and planning for limits and throttling"
      ],
      notes: ["Exam guide task statements 2.1 Design scalable and loosely coupled architectures; 2.2 Design highly available and/or fault-tolerant architectures"],
      labs: [
        "Free Tier: create a launch template for a t2.micro or t3.micro that serves a simple page with user data, put it in an Auto Scaling group across two AZs (min 2) behind an ALB with ELB health checks, then terminate one instance and watch the group replace it. Cost warning: an ALB bills by the hour and may not be covered by your Free Tier plan, so delete the ALB and the Auto Scaling group within the hour.",
        "Free Tier: build an SNS topic that fans out to two SQS queues, one of which has a dead-letter queue with maxReceiveCount 2. Publish messages, receive one without deleting it until it lands in the DLQ, and read the queue metrics in CloudWatch. SQS and SNS stay free at this scale; delete the topic and queues afterwards.",
        "Free Tier: create a DynamoDB table in on-demand mode, turn on point-in-time recovery, add and then change some items, and restore the table to a time before the change as a new table. Also create an AWS Backup on-demand job for it. Cost warning: PITR and backup storage bill per GB after any Free Tier allowance, so delete both tables and the backup when done."
      ]
    },
    {
      id: 3,
      name: "Design High-Performing Architectures",
      w: 24,
      topics: [
        "EC2 instance families and placement groups (cluster, spread, partition), and enhanced networking with ENA and EFA",
        "EBS volume types and instance store: gp3 vs io2 Block Express vs st1 and sc1",
        "Shared file systems: Amazon EFS vs FSx for Windows File Server, FSx for Lustre and FSx for NetApp ONTAP",
        "S3 performance: prefixes, multipart upload, byte-range fetches and S3 Transfer Acceleration",
        "Caching: ElastiCache for Redis vs Memcached, DynamoDB Accelerator (DAX), lazy loading vs write-through",
        "Content delivery and global networking: CloudFront caching and TTLs vs AWS Global Accelerator",
        "Choosing a database: RDS, Aurora, DynamoDB, Redshift, DocumentDB, Neptune, and RDS Proxy for connection pooling",
        "DynamoDB performance: partition key design, provisioned vs on-demand capacity, auto scaling and secondary indexes",
        "Streaming ingestion: Kinesis Data Streams vs Amazon Data Firehose vs Amazon MSK",
        "Analytics services: Athena, AWS Glue, Lake Formation, EMR, Redshift Spectrum and QuickSight",
        "EC2 Auto Scaling policies: target tracking, step, simple, scheduled and predictive scaling",
        "Data migration and hybrid storage: DataSync, Snow Family, Storage Gateway, Transfer Family, DMS and SCT"
      ],
      notes: ["Exam guide task statements 3.1 Determine high-performing and/or scalable storage solutions; 3.2 Design high-performing and elastic compute solutions; 3.3 Determine high-performing database solutions; 3.4 Determine high-performing and/or scalable network architectures; 3.5 Determine high-performing data ingestion and transformation solutions"],
      labs: [
        "Free Tier: create an S3 bucket in a Region far from you, upload a 200 MB file once with a normal `aws s3 cp` and once with the accelerate endpoint turned on (`aws configure set default.s3.use_accelerate_endpoint true`), and compare times; the CLI uses multipart upload automatically for large files. Cost warning: Transfer Acceleration is charged per GB, so use a single small test file, then delete the bucket.",
        "Free Tier: upload a few CSV or JSON log files to S3, create a table in the Glue Data Catalog with an Athena CREATE TABLE statement, and run queries that filter and count rows; note the data scanned per query. Athena bills per TB scanned, so keep the data to a few MB and delete the bucket and query results afterwards.",
        "Free Tier: in an Auto Scaling group of t2.micro or t3.micro instances (min 1, max 3), add a target tracking policy for 50% average CPU, run a CPU stress command on the instance, and watch the alarms and scaling activity history. Then add a scheduled action. Delete the group and launch template when done so no instances keep running."
      ]
    },
    {
      id: 4,
      name: "Design Cost-Optimized Architectures",
      w: 20,
      topics: [
        "EC2 purchase options: On-Demand, Reserved Instances, Compute vs EC2 Instance Savings Plans, Spot, Dedicated Instances and Dedicated Hosts",
        "Spot Instances in practice: interruption notices, mixed-instances Auto Scaling groups and allocation strategies",
        "Right-sizing compute: AWS Compute Optimizer, Graviton instances, and serverless vs always-on cost models",
        "S3 storage classes: Standard, Intelligent-Tiering, Standard-IA, One Zone-IA and the three Glacier classes",
        "S3 Lifecycle rules, S3 Storage Lens and Requester Pays",
        "Cutting EBS and backup costs: gp2 to gp3, Data Lifecycle Manager, snapshot archive and unattached volumes",
        "Database cost choices: DynamoDB on-demand vs provisioned, Aurora Serverless v2, reserved DB instances and stopping idle databases",
        "Data transfer costs: inter-AZ, inter-Region and internet egress, NAT gateway charges vs gateway endpoints, and CloudFront",
        "Cost visibility tools: Cost Explorer, AWS Budgets, Cost and Usage Reports, cost allocation tags and Trusted Advisor",
        "Consolidated billing in AWS Organizations: volume discounts and sharing Reserved Instance and Savings Plans benefits"
      ],
      notes: ["Exam guide task statements 4.1 Design cost-optimized storage solutions; 4.2 Design cost-optimized compute solutions; 4.3 Design cost-optimized database solutions; 4.4 Design cost-optimized network architectures"],
      labs: [
        "Free Tier: in the Billing console create a monthly cost budget of $5 with email alerts at 50% of actual spend and 100% of forecasted spend, activate a cost allocation tag such as Project, and open Cost Explorer grouped by service. Budgets and Cost Explorer's console views are free; do this first before any other lab.",
        "Free Tier: create an S3 bucket with a lifecycle rule that moves objects to Standard-IA after 30 days, to Glacier Flexible Retrieval after 90 days and expires them after 365 days, and another rule that aborts incomplete multipart uploads after 7 days. Upload a few test files and review the rule in the console. Delete the bucket when done.",
        "Free Tier: open the EC2 console's Spot placement score and Spot pricing history for two instance types, then use the AWS Pricing Calculator to compare On-Demand, a one-year Compute Savings Plan and Spot for a small three-instance web tier. No resources are launched, so this costs nothing."
      ]
    }
  ],

  study: {
    1: [
      ["How does IAM decide whether a request is allowed?", "Everything starts as an implicit deny. An explicit deny in any applicable policy (SCP, permissions boundary, session policy, identity or resource policy) wins. Otherwise the request needs an allow from an identity or resource policy, and the SCPs and any boundary must also allow it."],
      ["When should you use an IAM role instead of an IAM user?", "Whenever a workload or person can get temporary credentials: EC2 instance profiles, Lambda execution roles, cross-account access, federation and IAM Identity Center. IAM users with long-term access keys are for rare cases that cannot use roles."],
      ["Compare security groups and network ACLs.", "Security groups attach to network interfaces, are stateful and only allow traffic; rules are all evaluated together. Network ACLs attach to subnets, are stateless so return traffic needs rules, and evaluate numbered allow and deny rules in order."],
      ["What are the three S3 server-side encryption options, and when is each chosen?", "SSE-S3 uses keys S3 manages and is the default. SSE-KMS uses a KMS key, which adds key policies, CloudTrail logging and the option to disable the key. SSE-C uses a key the client supplies on every request, which AWS does not store."],
      ["Gateway endpoint or interface endpoint?", "Gateway endpoints exist only for S3 and DynamoDB, work through route tables and have no charge. Interface endpoints (PrivateLink) are ENIs with private IPs in your subnets, work for most AWS services and partner services, and bill hourly plus per GB."],
      ["Which detection service answers which question?", "CloudTrail: who called which API. Config: what a resource looked like and whether it complies with rules. GuardDuty: is something malicious happening. Inspector: which workloads have vulnerabilities. Macie: where sensitive data lives in S3. Security Hub: one view of findings and standards checks."]
    ],
    2: [
      ["What makes a web tier highly available on AWS?", "Instances in at least two AZs, a load balancer that health-checks them, and an Auto Scaling group using ELB health checks to replace failed instances. Keep instances stateless by storing sessions in ElastiCache or DynamoDB and files in S3 or EFS."],
      ["Compare SQS standard and FIFO queues.", "Standard queues give nearly unlimited throughput, at-least-once delivery and best-effort ordering. FIFO queues give ordering within each message group and exactly-once processing via deduplication, with lower throughput limits."],
      ["Contrast RDS Multi-AZ and read replicas.", "Multi-AZ is for availability: synchronous replication to a standby in another AZ with automatic failover on the same endpoint. Read replicas are for read scaling: asynchronous copies, in-Region or cross-Region, that serve reads and can be promoted manually."],
      ["List the four DR strategies from cheapest to fastest.", "Backup and restore (hours, lowest cost), pilot light (core data live, rest launched on demand, tens of minutes), warm standby (scaled-down full copy running, minutes), and multi-site active-active (near zero downtime, highest cost)."],
      ["What is the difference between RPO and RTO?", "RPO is how much data loss is acceptable, measured back from the failure (drives backup and replication frequency). RTO is how long the service can be down before it is restored (drives how much must already be running)."],
      ["When would you choose Route 53 failover, weighted, latency or geolocation routing?", "Failover for active-passive DR with health checks; weighted for splitting traffic such as canary releases; latency to send users to the Region with the lowest latency for them; geolocation to route by the user's country or continent, for example for content rules."]
    ],
    3: [
      ["Compare the three placement group types.", "Cluster packs instances close together in one AZ for low latency and high throughput. Spread puts each instance on separate hardware (up to seven per AZ per group) for critical instances. Partition groups instances into partitions on separate racks, for large distributed systems such as Hadoop or Cassandra."],
      ["How do you choose between EFS, FSx for Windows, FSx for Lustre and FSx for NetApp ONTAP?", "EFS for shared NFS storage for Linux. FSx for Windows for SMB shares with Active Directory and NTFS. FSx for Lustre for HPC and ML throughput, often linked to S3. FSx for NetApp ONTAP for multi-protocol (NFS, SMB, iSCSI) and NetApp features such as SnapMirror."],
      ["Compare lazy loading and write-through caching.", "Lazy loading fills the cache only on a miss, so it caches only what is read but can serve stale data until the TTL expires. Write-through updates the cache on every write, so data is fresh but the cache holds items that may never be read. Many designs combine both with a TTL."],
      ["CloudFront or Global Accelerator?", "CloudFront caches HTTP and HTTPS content at edge locations and can accelerate dynamic content. Global Accelerator gives two static anycast IPs, routes TCP and UDP traffic over the AWS network to regional endpoints, and fails over quickly without DNS caching delays; it does not cache."],
      ["Kinesis Data Streams, Data Firehose or SQS?", "Data Streams for real-time streams with several consumers and replay within the retention period. Firehose for managed delivery to S3, Redshift, OpenSearch or HTTP endpoints with buffering and format conversion. SQS for decoupled work items that one consumer processes and then deletes."],
      ["How do you migrate a large data set when the network is slow?", "Work out the transfer time first: 10 TB over 100 Mbps takes about 9 days at full use. If it is too slow, use a Snowball Edge device offline; for ongoing or online transfers use DataSync, and for databases use DMS (with SCT when the engine changes)."]
    ],
    4: [
      ["Compare Reserved Instances and Savings Plans.", "Both give up to about 72% off On-Demand for a one- or three-year commitment. Standard RIs are tied to an instance family, Region and OS (Convertible RIs can be exchanged). Compute Savings Plans commit to $/hour and apply across families, Regions, Fargate and Lambda; EC2 Instance Savings Plans are tied to a family in a Region for a bigger discount."],
      ["When are Spot Instances a good fit, and how do you make them safe?", "For stateless, fault-tolerant or flexible work such as batch, CI, rendering and big data. Handle the two-minute interruption notice, checkpoint work, diversify across instance types and AZs, and use the price-capacity-optimized allocation strategy in Auto Scaling groups or EC2 Fleet."],
      ["Walk through the S3 storage classes from most to least expensive to store.", "Standard; Intelligent-Tiering (automatic, small monitoring fee); Standard-IA and One Zone-IA (retrieval fees, 30-day minimum); Glacier Instant Retrieval (milliseconds, quarterly access); Glacier Flexible Retrieval (minutes to hours); Glacier Deep Archive (12 to 48 hours, cheapest)."],
      ["Which data transfer charges matter most in designs?", "Inbound from the internet is free. Outbound to the internet, between Regions and between AZs is charged per GB. NAT gateways add per-GB processing charges, so gateway endpoints for S3 and DynamoDB and CloudFront for internet delivery are common savings."],
      ["What tools help find and control AWS costs?", "Cost Explorer for analysis and forecasts, AWS Budgets for alerts and actions, the Cost and Usage Report for detailed line items, cost allocation tags for grouping, Compute Optimizer and Trusted Advisor for right-sizing and idle resources, and consolidated billing for shared discounts."]
    ]
  },

  questions: [
    ["saa1",0,1,"An application on EC2 instances must read objects from one S3 bucket. The security team bans long-term access keys on servers. What should the architect do?",["Attach an IAM role with read access to that bucket through an instance profile","Store an IAM user's access keys in the application's configuration file","Add the instances' private IP addresses as grantees in the bucket's ACL","Pass an IAM user's access keys to the instances through EC2 user data at every launch"],0,"An instance profile delivers a role's temporary credentials through the instance metadata service and rotates them automatically. Keys in a config file or user data are long-term secrets that can leak, and bucket ACLs cannot grant access by IP address.","1.1 Secure access to AWS resources"],
    ["saa2",0,1,"An IAM policy attached to a developer allows s3:DeleteObject on a bucket, but a service control policy on the developer's account denies s3:DeleteObject. What happens when the developer tries to delete an object?",["The request is allowed, because identity policies are evaluated before SCPs","The request is denied, because an SCP deny is an explicit deny that overrides any allow","The request is allowed only if the bucket policy also grants the action","The request is denied only when it is made by the account's root user"],1,"In IAM policy evaluation an explicit deny anywhere wins, and SCPs set the maximum permissions for every principal in a member account, including its root user. A bucket policy allow cannot override an SCP deny.","1.1 Secure access to AWS resources"],
    ["saa3",0,1,"A company runs 40 AWS accounts in AWS Organizations. Nobody in the member accounts, including account administrators, may stop or delete the CloudTrail trail. What is the most effective control?",["An IAM permissions boundary attached to every administrator in each account","A CloudTrail setting that turns on log file integrity validation for the organization trail","An AWS Config rule that reports when a trail stops logging in any account","An SCP on the organization's OUs that denies the CloudTrail stop and delete actions"],3,"An SCP applies to every principal in the affected accounts and cannot be bypassed from inside them. Permissions boundaries must be set on each principal and admins could remove them, log validation only detects tampering with log files, and a Config rule only reports after the fact.","1.1 Secure access to AWS resources"],
    ["saa4",0,1,"Employees already sign in to a corporate SAML 2.0 identity provider. They need single sign-on to the console and CLI of 25 AWS accounts with role-based permissions. What should the architect set up?",["An IAM user with the same name and password in each of the 25 accounts","A shared IAM user per team, with its password kept in a password manager","IAM Identity Center connected to the corporate IdP, with permission sets assigned to accounts","An Amazon Cognito user pool that holds a copy of every employee's password"],2,"IAM Identity Center federates with an external IdP and provisions permission sets as roles in each account, giving short-term credentials for the console and CLI. Per-account IAM users multiply credentials, shared users break accountability, and Cognito user pools are for application end users.","1.1 Secure access to AWS resources"],
    ["saa5",0,1,"A mobile app lets users sign in with a social identity provider and then upload photos straight to S3, each user only under their own prefix. Which design is most secure?",["An IAM user's access keys compiled into the mobile app so every upload is signed","A bucket policy that allows public writes, with a Lambda function that renames each object","A separate IAM user created for every person who signs up in the mobile app","A Cognito identity pool that exchanges the login token for temporary credentials scoped by a policy variable"],3,"Cognito identity pools trade an identity provider token for temporary, limited AWS credentials, and a policy variable such as the Cognito identity ID restricts each user to their own prefix. Embedded keys can be extracted, public write is unsafe, and IAM users do not scale to consumers.","1.1 Secure access to AWS resources"],
    ["saa6",0,1,"Which statement correctly compares security groups and network ACLs in a VPC?",["Security groups are stateless and evaluate rules in number order from the lowest","Network ACLs are stateful, so return traffic is always allowed back automatically","Security groups are stateful and allow-only; network ACLs are stateless and can allow or deny","Security groups work at the subnet boundary and can contain explicit deny rules"],2,"Security groups attach to network interfaces, are stateful and only have allow rules. Network ACLs attach to subnets, are stateless (return traffic needs its own rule) and evaluate numbered allow and deny rules in order.","1.2 Secure workloads and applications"],
    ["saa7",0,1,"Security analysts find that a specific /24 address range is scanning web servers in a public subnet. The range must be blocked immediately while all other internet users keep access. What should the architect change?",["Add an inbound deny rule for the range to the security group attached to the web servers","Remove the internet gateway route from the public subnet's route table","Add a deny rule for the range, with a low rule number, to the subnet's network ACL","Move the web servers into a spread placement group in the same subnet"],2,"Network ACLs support explicit deny rules evaluated in number order, so a low-numbered deny blocks the range for the whole subnet. Security groups have no deny rules, and removing the internet gateway route cuts off every user.","1.2 Secure workloads and applications"],
    ["saa8",0,1,"Application servers in private subnets must download operating system patches from the internet, but nothing on the internet may start a connection to them. What should the architect add?",["An Elastic IP address attached directly to each application server in the subnets","A default route to the internet gateway in each private subnet's route table","A NAT gateway in a public subnet, with a default route to it from the private route tables","An interface VPC endpoint that points to the operating system vendor's repository"],2,"A NAT gateway lets private instances start outbound connections while blocking inbound ones from the internet. Elastic IPs or an internet gateway route would make the subnets public, and interface endpoints only reach AWS and PrivateLink services, not arbitrary internet sites.","1.2 Secure workloads and applications"],
    ["saa9",0,1,"Administrators need shell access to Linux instances in private subnets. The company wants no open inbound ports, no bastion host to patch, and every session logged. What should the architect use?",["A bastion host in a public subnet with TCP port 22 open to 0.0.0.0/0","An internet-facing Network Load Balancer that forwards TCP port 22 to the instances","A Site-to-Site VPN from each administrator's laptop to the virtual private gateway","AWS Systems Manager Session Manager, with the SSM Agent and an instance profile on each instance"],3,"Session Manager connects through the SSM Agent's outbound connection, needs no inbound ports or bastion, and can log sessions to CloudWatch Logs or S3. An open bastion or NLB exposes SSH to the internet, and per-laptop Site-to-Site VPNs are not how remote users connect.","1.2 Secure workloads and applications"],
    ["saa10",0,1,"Instances in private subnets write sensitive files to S3. Policy says this traffic must not cross the public internet or pass through the NAT gateway. What should the architect add?",["A second NAT gateway placed in another Availability Zone of the VPC","S3 Transfer Acceleration turned on for the destination bucket","An internet gateway with a route for the S3 managed prefix list in each private subnet","An S3 gateway VPC endpoint associated with the private subnets' route tables"],3,"A gateway endpoint adds a route for the S3 prefix list so traffic stays on the AWS network without a NAT gateway, and an endpoint policy or aws:sourceVpce condition can restrict it further. Transfer Acceleration uses public edge locations, and another NAT gateway still sends traffic through NAT.","1.2 Secure workloads and applications"],
    ["saa11",0,1,"Which pair of AWS services can be reached from a VPC through gateway VPC endpoints?",["Amazon SQS and Amazon SNS","Amazon S3 and Amazon DynamoDB","AWS KMS and AWS Secrets Manager","Amazon ECR and AWS STS"],1,"Only S3 and DynamoDB offer gateway endpoints, which are route table entries with no hourly charge. SQS, SNS, KMS, Secrets Manager, ECR and STS are reached privately with interface endpoints (PrivateLink), which have hourly and per-GB charges.","1.2 Secure workloads and applications"],
    ["saa12",0,1,"A static website is served from an S3 bucket through a CloudFront distribution. Users must not be able to bypass CloudFront and fetch objects from S3 directly. What should the architect do?",["Use CloudFront origin access control and a bucket policy that only allows that distribution","Turn on S3 static website hosting and publish the bucket's website endpoint","Keep the bucket public but give every object a long random key name","Enable S3 Transfer Acceleration and require the accelerate endpoint for reads"],0,"Origin access control signs CloudFront's requests to S3, and the bucket policy allows only that distribution, so the bucket can stay private with Block Public Access on. Website hosting and public objects expose the bucket directly, and Transfer Acceleration is about upload speed.","1.2 Secure workloads and applications"],
    ["saa13",0,1,"A web application behind an Application Load Balancer is hit by SQL injection attempts, and one client IP sometimes sends thousands of requests per minute. What should the architect deploy?",["AWS Shield Standard, which already protects every ALB at no extra charge","Amazon GuardDuty with automatic blocking of the offending client IP addresses at the ALB","Security group rules that match SQL keywords in the incoming HTTP requests","An AWS WAF web ACL on the ALB with managed rule groups and a rate-based rule"],3,"AWS WAF inspects HTTP requests at layer 7: managed rules catch SQL injection and a rate-based rule throttles noisy IPs. Shield Standard covers network and transport layer DDoS, GuardDuty detects threats but does not filter HTTP, and security groups cannot inspect request contents.","1.2 Secure workloads and applications"],
    ["saa14",0,1,"A company that sells tickets online is worried about large DDoS attacks during launches. It wants 24/7 access to the AWS Shield Response Team and protection against scaling charges caused by an attack. Which service meets this?",["AWS Shield Standard","AWS Firewall Manager","Amazon Inspector","AWS Shield Advanced"],3,"Shield Advanced adds enhanced detection, the Shield Response Team, cost protection for attack-driven scaling, and AWS WAF at no extra charge for protected resources. Shield Standard is free and automatic but has none of those, Firewall Manager centrally manages rules, and Inspector scans for vulnerabilities.","1.2 Secure workloads and applications"],
    ["saa15",0,1,"Auditors require that S3 data be encrypted with a key whose use is logged in CloudTrail, whose key policy the company controls, and that can be disabled to cut off access. Which option meets all three needs?",["SSE-S3 with keys managed entirely by Amazon S3","SSE-C with a key the client sends on every request","SSE-KMS with the AWS managed aws/s3 key","SSE-KMS with a customer managed key in AWS KMS"],3,"A customer managed KMS key gives the company its own key policy, CloudTrail records of each use, and the ability to disable or schedule deletion of the key. SSE-S3 offers no key control, the AWS managed key's policy cannot be edited, and SSE-C leaves key management and logging entirely to the client.","1.3 Data security controls"],
    ["saa16",0,1,"What does envelope encryption mean when AWS services use AWS KMS?",["Every object is sent to KMS, which encrypts the full object and returns it","Data is encrypted twice, first with an AES-256 key and then again with an RSA key pair","The KMS key is stored alongside the data inside an encrypted archive file","Data is encrypted with a data key, and that data key is encrypted under a KMS key"],3,"KMS generates a data key; the service encrypts the data locally with the plaintext data key, then stores only the encrypted copy of the data key next to the data. KMS keys never leave KMS unencrypted, and sending whole objects to KMS would hit the 4 KB direct-encryption limit.","1.3 Data security controls"],
    ["saa17",0,1,"A bucket stores medical reports. Compliance says any request that does not use TLS must be rejected. What should the architect add?",["Default bucket encryption set to SSE-KMS with a customer managed key and a bucket key","A bucket policy that denies all actions when aws:SecureTransport is false","S3 Block Public Access turned on at the account level","S3 Object Lock in compliance mode with a one-year retention period"],1,"The aws:SecureTransport condition key is false for plain HTTP requests, so a deny on that condition enforces encryption in transit. Default encryption protects data at rest, Block Public Access controls public grants, and Object Lock prevents deletion; none of them block HTTP.","1.3 Data security controls"],
    ["saa18",0,1,"An application stores its RDS database password in plain text in a configuration file. Security wants the password rotated every 30 days automatically and never stored in code. What should the architect use?",["AWS Secrets Manager with automatic rotation, with the application fetching the secret at runtime","A Systems Manager Parameter Store String parameter that an engineer updates by hand at the start of each month","An environment variable set in EC2 user data and changed whenever instances are replaced","Automatic yearly rotation turned on for the KMS key that encrypts the RDS storage volume"],0,"Secrets Manager has built-in rotation for RDS credentials using a rotation Lambda function, and applications read the current value by API. A plain String parameter is neither encrypted nor rotated, user data is visible to anyone who can describe the instance, and KMS key rotation does not change the database password.","1.3 Data security controls"],
    ["saa19",0,1,"Which statement about AWS Secrets Manager and Systems Manager Parameter Store is correct?",["Parameter Store charges per secret per month, while Secrets Manager standard parameters are free","Secrets Manager has managed rotation for supported databases; Parameter Store has no built-in rotation","Only Parameter Store can encrypt values with AWS KMS; Secrets Manager stores them as plain text","Secrets Manager values can be read only from Lambda functions, not from EC2 or containers"],1,"Secrets Manager encrypts with KMS, bills per secret, and offers managed rotation. Parameter Store standard parameters are free, SecureString parameters are encrypted with KMS, but rotation needs your own automation. Both are read through the API from any compute service.","1.3 Data security controls"],
    ["saa20",0,1,"Regulations require that trade records in S3 cannot be overwritten or deleted by anyone, including the root user, for seven years. What should the architect configure?",["S3 Object Lock in compliance mode with a seven-year retention period","S3 Object Lock in governance mode with a seven-year retention period","S3 Versioning with a lifecycle rule that expires versions after seven years","A bucket policy that denies s3:DeleteObject to every IAM principal"],0,"Compliance mode is write-once-read-many: no user, including root, can shorten retention or delete a locked version. Governance mode can be bypassed by users with special permission, versioning alone allows version deletion, and a bucket policy can be edited by an administrator.","1.3 Data security controls"],
    ["saa21",0,1,"A partner must download one private report from S3 for the next hour without receiving AWS credentials. What is the simplest secure option?",["Generate a presigned URL for the object that expires in one hour","Make the object public and remove the public ACL one hour later","Create an IAM user for the partner and delete it the next day","Copy the object to a new bucket with S3 Block Public Access turned off"],0,"A presigned URL carries the signer's permission for one object and expires at a set time, and the bucket stays private. Public objects, extra buckets or throwaway IAM users all widen access and depend on someone remembering to clean up.","1.3 Data security controls"],
    ["saa22",0,1,"A company has hundreds of S3 buckets and must find which ones contain personal data such as passport numbers and credit card numbers. Which service fits best?",["AWS Audit Manager","Amazon Inspector","Amazon Macie","Amazon Detective"],2,"Macie uses managed and custom data identifiers to discover sensitive data in S3 and reports findings per bucket. Audit Manager collects evidence for compliance audits, Inspector scans workloads for software vulnerabilities, and Detective helps investigate security findings.","1.3 Data security controls"],
    ["saa23",0,1,"Which service continuously analyzes CloudTrail events, VPC Flow Logs and DNS query logs to detect threats such as an instance communicating with a known crypto-mining server?",["Amazon GuardDuty","AWS Security Hub","AWS Config","AWS Trusted Advisor"],0,"GuardDuty is a managed threat detection service that reads these log sources without agents and produces findings. Security Hub collects and scores findings from GuardDuty and other services but does not analyze the logs itself, Config tracks resource configuration and compliance, and Trusted Advisor checks accounts against best practices.","1.2 Secure workloads and applications"],
    ["saa24",0,1,"The security team must be told whenever any security group in the account allows SSH from 0.0.0.0/0, and needs a history of how each security group has changed. What should the architect set up?",["Amazon Inspector network reachability scans run once a quarter against each EC2 instance","AWS Config with a managed rule for unrestricted SSH and an EventBridge notification","Amazon Macie discovery jobs that run against every bucket in the account","CloudWatch Logs Insights queries run by hand against the application logs"],1,"AWS Config records configuration history for each resource and evaluates managed rules such as restricted-ssh continuously; a noncompliant result can trigger an EventBridge rule. Quarterly scans are not continuous, Macie looks at S3 data, and application logs do not show security group changes.","1.2 Secure workloads and applications"],
    ["saa25",0,1,"An application in account B must read items from a DynamoDB table in account A. What is the recommended way to grant access?",["Create an IAM user in account A and copy its access keys into account B","Share the root user credentials of account A with the team in account B","Create a role in account A that trusts account B, and have the application assume it with STS","Peer the VPCs of both accounts so the application can reach the table directly"],2,"Cross-account roles give temporary credentials: account A's role has a trust policy for account B and a permissions policy for the table, and account B's principal needs sts:AssumeRole. Copied keys and shared root credentials are long-term secrets, and VPC peering provides network paths, not authorization.","1.1 Secure access to AWS resources"],
    ["saa26",0,1,"Developers must be allowed to create IAM roles for their Lambda functions, but no role they create may ever have more permissions than a set the security team defines. What should the architect use?",["An S3 bucket policy on the bucket that stores the Lambda deployment packages","A resource-based policy on each Lambda function that lists the allowed actions","A permissions boundary that developers are required to attach to every role they create","An AWS Config rule with remediation that deletes new roles once a week if they look too powerful"],2,"A permissions boundary caps the maximum permissions of a role, and an IAM condition can force developers to attach it when creating roles. Bucket and function policies do not limit what roles can do, and a weekly clean-up leaves a window of excess access.","1.1 Secure access to AWS resources"],
    ["saa27",0,1,"A public web application behind an Application Load Balancer needs an HTTPS certificate for www.example.com that renews automatically. What should the architect do?",["Request a public certificate in AWS Certificate Manager and attach it to an HTTPS listener","Create a self-signed certificate and import it into IAM for the load balancer","Install a certificate on every instance and renew each one by hand yearly","Store the certificate's private key in an S3 bucket that the ALB can read"],0,"ACM issues free public certificates, validates them by DNS or email, and renews them automatically when they are in use on integrated services such as ALB and CloudFront. Self-signed certificates are not trusted by browsers, and manual per-instance renewal is error-prone.","1.3 Data security controls"],
    ["saa28",0,2,"A web application runs on a single large EC2 instance in one Availability Zone. The business wants it to survive the loss of an instance or an entire AZ. What should the architect do?",["Move the application to a larger instance type with more CPU and memory","Put the instance in a cluster placement group and take hourly EBS snapshots","Run instances in an Auto Scaling group across two or more AZs behind an Application Load Balancer","Attach a second EBS volume and mirror the data between the two volumes"],2,"Spreading instances across AZs behind a load balancer removes the single point of failure, and the Auto Scaling group replaces unhealthy instances. A bigger instance or extra volume is still one instance in one AZ, and a cluster placement group packs instances close together, which is the opposite of spreading risk.","2.2 Highly available and fault-tolerant architectures"],
    ["saa29",0,2,"Instances in an Auto Scaling group behind an ALB sometimes return HTTP 500 errors while their EC2 status checks still pass, and the group never replaces them. What should the architect change?",["Increase the group's desired capacity by two instances","Switch the load balancer to a Network Load Balancer","Lower the Auto Scaling cooldown period to 60 seconds","Turn on ELB health checks for the Auto Scaling group"],3,"By default an Auto Scaling group only uses EC2 status checks. With ELB health checks on, instances that fail the load balancer's application health check are marked unhealthy and replaced. More capacity or a different load balancer leaves broken instances in service.","2.2 Highly available and fault-tolerant architectures"],
    ["saa30",0,2,"An order website receives bursts of traffic during sales. The back-end processing tier cannot keep up, and some orders are lost when it is overloaded. What design change fixes this?",["Increase the front-end instance count so it retries orders more often","Put an SQS queue between the tiers and scale the workers on queue depth","Have the front end write every order straight to the database synchronously","Move the processing tier into a single larger EC2 instance type"],1,"A queue decouples the tiers: the front end writes orders quickly, messages stay durable until processed, and workers scale on ApproximateNumberOfMessagesVisible. More retries add load, synchronous writes keep the tight coupling, and one bigger instance is still a bottleneck and single point of failure.","2.1 Scalable and loosely coupled architectures"],
    ["saa31",0,2,"A payments system must process each customer's transactions exactly once and in the order they were sent, while different customers can be processed in parallel. What should the architect use?",["An SQS standard queue with long polling turned on for the consumers","An SNS standard topic with one subscription for each customer","An SQS FIFO queue with the customer ID as the message group ID","A Kinesis data stream with a single shard for every customer"],2,"FIFO queues give ordering within a message group and deduplication, and different group IDs can be processed in parallel. Standard queues give at-least-once delivery with best-effort ordering, SNS standard topics do not guarantee order, and a single shard serializes all customers.","2.1 Scalable and loosely coupled architectures"],
    ["saa32",0,2,"A few malformed messages in an SQS queue fail processing again and again, blocking workers. How should the architect handle them?",["Set the queue's visibility timeout to zero so messages come back sooner","Configure a dead-letter queue with a redrive policy and a maxReceiveCount","Turn on long polling with a 20-second wait time on the source queue","Change the source queue's message retention period to the minimum of 60 seconds"],1,"After a message has been received maxReceiveCount times without being deleted, SQS moves it to the dead-letter queue for inspection. A zero visibility timeout causes duplicate processing, long polling only reduces empty receives, and short retention silently drops good messages too.","2.1 Scalable and loosely coupled architectures"],
    ["saa33",0,2,"When an order is placed, three independent systems must react: billing, shipping and analytics. Each must get every order even if one of them is down for an hour. What is the best design?",["Send each order to a single SQS queue that all three systems poll","Have the web tier call the three systems' APIs one after another","Write each order to an S3 bucket that each system lists every hour","Publish to an SNS topic that fans out to a separate SQS queue for each system"],3,"SNS fan-out delivers a copy to each subscribed queue, and each queue buffers messages while its consumer is down. With one shared queue each message goes to only one consumer, and chained API calls fail when any system is unavailable.","2.1 Scalable and loosely coupled architectures"],
    ["saa34",0,2,"An insurance claim workflow has several steps, retries on failure, and waits up to five days for a human approval before continuing. Which service should coordinate it?",["A single Lambda function that runs the whole process","An SQS delay queue that holds the claim for five days","AWS Step Functions with a Standard workflow","A cron job on an EC2 instance that polls a database"],2,"Standard workflows can run for up to a year, have built-in retry and catch, and support callback task tokens for human approval. Lambda stops after 15 minutes, SQS delay queues cap at 15 minutes, and a cron job on one instance is a single point of failure you must build and maintain.","2.1 Scalable and loosely coupled architectures"],
    ["saa35",0,2,"A team wants to run containerized microservices on AWS without provisioning, patching or scaling any EC2 instances. What should the architect recommend?",["Amazon ECS tasks on the EC2 launch type with an Auto Scaling group","Amazon EKS with self-managed node groups on EC2 instances","Docker installed on EC2 instances managed with Systems Manager","Amazon ECS tasks that use the AWS Fargate launch type"],3,"Fargate runs containers on serverless capacity, so there are no hosts to patch or scale. The EC2 launch type and self-managed EKS nodes both leave the team responsible for the instances, as does running Docker on EC2.","2.1 Scalable and loosely coupled architectures"],
    ["saa36",0,2,"An application uses Amazon RDS for MySQL in one Availability Zone. It must fail over automatically if that AZ fails, without changing the connection string. What should the architect do?",["Create a read replica in the same Region and point writes to it","Take automated snapshots every hour and restore on failure","Change to a larger instance class with Provisioned IOPS storage","Convert the database to a Multi-AZ deployment"],3,"Multi-AZ keeps a synchronous standby in another AZ and fails over automatically by updating the DNS endpoint the application already uses. A read replica needs manual promotion and a new endpoint, snapshots mean downtime and data loss, and a bigger instance is still in one AZ.","2.2 Highly available and fault-tolerant architectures"],
    ["saa37",0,2,"What is the main purpose of an Amazon RDS read replica, compared with the standby of a Multi-AZ DB instance?",["To provide synchronous replication for automatic failover","To store automated backups outside the primary Region","To serve read traffic from an asynchronously updated copy","To encrypt the primary database with a separate key"],2,"Read replicas use asynchronous replication and accept read queries, which offloads reporting and read-heavy traffic; they can be promoted manually. The standby in a classic Multi-AZ DB instance deployment is synchronous, used for failover and does not serve reads.","2.2 Highly available and fault-tolerant architectures"],
    ["saa38",0,2,"A MySQL-compatible application has users on three continents. It needs low-latency local reads in each Region, an RPO of about one second, and failover to another Region in about a minute. What should the architect use?",["Amazon Aurora Global Database","An RDS for MySQL Multi-AZ DB cluster in one Region","Nightly RDS snapshots copied to each of the Regions","DynamoDB global tables in three of the Regions"],0,"Aurora Global Database replicates storage to secondary Regions with typical lag under a second, serves local reads there, and a secondary can be promoted in about a minute. A Multi-AZ cluster stays in one Region, snapshot copies mean hours of data loss, and DynamoDB is not MySQL-compatible.","2.2 Highly available and fault-tolerant architectures"],
    ["saa39",0,2,"A gaming company needs a NoSQL table that accepts writes in both us-east-1 and eu-west-1 at the same time, so players in each Region get low-latency writes. What should the architect use?",["DynamoDB global tables with replicas in both Regions","A DynamoDB table with point-in-time recovery turned on","A DynamoDB table and a DAX cluster in each of the two Regions","DynamoDB Streams that copy items to an S3 bucket in the other Region"],0,"Global tables are multi-Region, multi-active: each replica accepts reads and writes and changes replicate to the others. Point-in-time recovery protects against mistakes, DAX only caches reads, and streaming into S3 does not give a writable second table.","2.2 Highly available and fault-tolerant architectures"],
    ["saa40",0,2,"A script accidentally overwrote thousands of items in a DynamoDB table 20 minutes ago. The team wants the table as it was 25 minutes ago. Which feature allows this if it was already enabled?",["DynamoDB Streams","Time to Live","Point-in-time recovery","Auto scaling"],2,"Point-in-time recovery lets you restore a table to any second in the last 35 days (the window is configurable), into a new table. Streams record 24 hours of changes but do not restore anything, TTL deletes expired items, and auto scaling adjusts capacity.","2.2 Highly available and fault-tolerant architectures"],
    ["saa41",0,2,"A company runs identical stacks in two Regions. All users should go to the primary Region and be sent to the secondary only when the primary's health check fails. Which Route 53 configuration fits?",["Weighted routing with 50% of traffic going to each Region","Latency-based routing between the two Regions' load balancers","Geolocation routing based on the country of each user","Failover routing with a health check on the primary record"],3,"Failover routing answers with the primary record while its health check is healthy and switches to the secondary when it fails. Weighted and latency routing send traffic to both Regions all the time, and geolocation routes by user location, not by health.","2.2 Highly available and fault-tolerant architectures"],
    ["saa42",0,2,"Which Route 53 routing policy should an architect use to send 10% of users to a new version of an application and 90% to the current one?",["Failover routing","Multivalue answer routing","Weighted routing","Geoproximity routing"],2,"Weighted routing splits DNS answers by the relative weights on each record, which is how canary releases are done at the DNS level. Failover is active-passive, multivalue returns several healthy records at random, and geoproximity routes by location and bias.","2.2 Highly available and fault-tolerant architectures"],
    ["saa43",0,2,"An internal reporting system can tolerate losing up to 24 hours of data and being down for a day. The company wants the lowest-cost disaster recovery strategy. Which strategy fits?",["Pilot light, with the database replicating to another Region","Backup and restore, with backups copied to another Region","Warm standby, with a smaller copy running in another Region","Multi-site active-active across two AWS Regions"],1,"With an RPO and RTO measured in hours to a day, backup and restore is enough and costs the least, because nothing runs in the recovery Region until needed. Pilot light, warm standby and active-active give faster recovery at progressively higher running cost.","2.2 Highly available and fault-tolerant architectures"],
    ["saa44",0,2,"An e-commerce platform needs an RTO of under 15 minutes after a Regional outage. The company accepts paying for a reduced-size copy of the full stack running all the time, but not for a full-size second Region. Which DR strategy matches?",["Backup and restore","Pilot light","Warm standby","Multi-site active-active"],2,"Warm standby keeps a scaled-down but fully working environment running, so recovery is mainly scaling it up. Pilot light keeps only core components such as the database replicating, so servers must be launched first. Backup and restore is slower still, and active-active runs full capacity in both Regions.","2.2 Highly available and fault-tolerant architectures"],
    ["saa45",0,2,"A company uses a pilot light DR design: an Aurora replica runs in the recovery Region, but no application servers run there. During a Regional outage, what must happen before the recovery Region can serve production traffic?",["Restore the database from the most recent nightly snapshot in that Region","Launch the application tier from AMIs, promote the database and switch DNS","Nothing, because full-size application servers already take traffic there","Copy every EBS snapshot from the failed Region into the recovery Region"],1,"In pilot light only the core data layer is live, so recovery means starting and scaling the application tier, promoting the replica to a writer, and pointing DNS at the new Region. Restoring from snapshots describes backup and restore, full-size servers already serving traffic describes active-active, and snapshots cannot be copied out of a failed Region on demand.","2.2 Highly available and fault-tolerant architectures"],
    ["saa46",0,2,"A company must back up EBS volumes, RDS databases, DynamoDB tables and EFS file systems across 12 accounts on one schedule, keep them for 90 days, and copy them to a second Region. What should the architect use?",["An EventBridge rule that calls a snapshot Lambda in each account","Amazon Data Lifecycle Manager policies for all of the resources","AWS Backup plans managed through AWS Organizations","S3 Cross-Region Replication rules for each of the data stores"],2,"AWS Backup centralizes schedules, retention and cross-Region and cross-account copies for many services, and backup policies can be applied across the organization. Custom Lambda code must be built and maintained, Data Lifecycle Manager covers only EBS snapshots and AMIs, and S3 replication only applies to S3.","2.2 Highly available and fault-tolerant architectures"],
    ["saa47",0,2,"An architect wants new objects in an S3 bucket copied automatically to a bucket in another Region for disaster recovery. What must be enabled before S3 Cross-Region Replication can be configured?",["S3 Transfer Acceleration on the source bucket","S3 Object Lock on the destination bucket","S3 Versioning on both the source and destination buckets","Requester Pays on both of the buckets"],2,"Replication requires versioning on the source and destination buckets, plus an IAM role that S3 can use to replicate. Transfer Acceleration, Object Lock and Requester Pays are unrelated to whether replication can be set up.","2.2 Highly available and fault-tolerant architectures"],
    ["saa48",0,2,"A company's data center connects to AWS over a single AWS Direct Connect link. It needs a backup path if that link fails, at the lowest cost. What should the architect add?",["A second Direct Connect link at the same Direct Connect location","An AWS Site-to-Site VPN over the internet as a backup path","A VPC peering connection between the data center and the VPC","A CloudFront distribution that points at the on-premises servers"],1,"A Site-to-Site VPN is quick to set up and inexpensive, and BGP can prefer Direct Connect while falling back to the VPN. A second Direct Connect link gives higher resilience but costs much more, VPC peering only connects VPCs, and CloudFront is for content delivery.","2.2 Highly available and fault-tolerant architectures"],
    ["saa49",0,2,"A company has 30 VPCs across several accounts and two data centers connected by VPN. Every network must reach every other one, and managing a full mesh of peering connections has become unworkable. What should the architect use?",["More VPC peering connections, with transitive routing turned on","A single NAT gateway shared by all 30 VPCs and both data centers","An Application Load Balancer in each VPC in front of all traffic","AWS Transit Gateway as a regional hub for the VPCs and VPNs"],3,"Transit Gateway is a hub-and-spoke router: each VPC and VPN attaches once and route tables control what can talk to what. VPC peering is never transitive, a NAT gateway only translates outbound traffic, and an ALB handles HTTP requests, not routing between networks.","2.1 Scalable and loosely coupled architectures"],
    ["saa50",0,2,"After a Regional outage drill, a team found that rebuilding the network, load balancers and Auto Scaling groups by hand in the recovery Region took two days. How can they cut this time most reliably?",["Write a detailed runbook of console steps and practice it every year","Take EBS snapshots of every instance and copy them to the recovery Region","Define the environment in AWS CloudFormation templates and deploy them to the recovery Region","Buy a larger AWS Support plan so engineers can open cases faster"],2,"Infrastructure as code builds the same environment repeatably and quickly in any Region, and StackSets can deploy to many accounts and Regions. Runbooks still rely on manual steps, snapshots only cover disks, not the network or load balancers, and a support plan does not rebuild anything.","2.2 Highly available and fault-tolerant architectures"],
    ["saa51",0,3,"A tightly coupled HPC simulation runs on 16 EC2 instances that exchange data constantly and need the lowest possible network latency between nodes. How should the instances be placed?",["In a spread placement group across three Availability Zones","In a partition placement group with one partition per node","In a cluster placement group in a single Availability Zone","In separate Auto Scaling groups in two different Regions"],2,"A cluster placement group packs instances close together in one AZ for low latency and high throughput, and supports Elastic Fabric Adapter on supported types. Spread and partition groups place instances on separate hardware for resilience, which adds distance, and crossing Regions adds much more latency.","3.2 High-performing compute solutions"],
    ["saa52",0,3,"Five instances run a critical quorum-based service. The company wants to minimize the chance that one hardware failure takes out more than one instance. Which placement strategy fits?",["A cluster placement group","A spread placement group","A dedicated Auto Scaling warm pool","One larger bare metal instance"],1,"A spread placement group puts each instance on distinct racks with separate power and network, up to seven per AZ per group. A cluster group packs instances close together, a warm pool only speeds up scaling, and one bare metal instance is a single point of failure.","3.2 High-performing compute solutions"],
    ["saa53",0,3,"A self-managed database on EC2 needs 150,000 sustained IOPS with consistent sub-millisecond latency on a single volume. Which EBS volume type should the architect choose?",["Provisioned IOPS SSD (io2 Block Express)","General Purpose SSD (gp3)","General Purpose SSD (gp2)","Cold HDD (sc1)"],0,"io2 Block Express supports up to 256,000 IOPS per volume with sub-millisecond latency and 99.999% durability. gp3 and gp2 cannot reach 150,000 IOPS on one volume, and sc1 is a low-cost HDD built for infrequent sequential access, not random IOPS.","3.1 High-performing and scalable storage"],
    ["saa54",0,3,"A log-processing job reads multi-terabyte files sequentially from EBS every night. Throughput matters, IOPS do not, and cost should be low. Which volume type fits best?",["Throughput Optimized HDD (st1)","Provisioned IOPS SSD (io1)","Amazon FSx for NetApp ONTAP","EC2 instance store NVMe"],0,"st1 is a low-cost HDD volume designed for large sequential reads and writes such as big data and log processing. io1 costs far more per GB for IOPS the job does not need, FSx for NetApp ONTAP is a managed multi-protocol file system rather than a cheap block volume, and instance store data is lost when the instance stops.","3.1 High-performing and scalable storage"],
    ["saa55",0,3,"Linux web servers in an Auto Scaling group across three Availability Zones must share the same set of uploaded files through a POSIX file system that grows automatically. What should the architect use?",["Amazon EFS mounted on every instance","An EBS volume attached with Multi-Attach","The instance store on each web server","An FSx for Lustre scratch file system"],0,"EFS is a managed NFS file system that many Linux instances across AZs can mount at once, and it grows and shrinks automatically. EBS Multi-Attach is limited to io1 and io2 volumes in one AZ and needs a cluster-aware file system, instance store is not shared or durable, and a Lustre scratch file system is a fixed-size, single-AZ file system for short-lived HPC data.","3.1 High-performing and scalable storage"],
    ["saa56",0,3,"A company is moving Windows applications that use SMB file shares, NTFS permissions and Active Directory authentication. What is the best managed storage choice?",["Amazon EFS with the Standard storage class","Amazon FSx for Windows File Server","Amazon FSx for Lustre linked to S3","Amazon S3 with S3 Access Points"],1,"FSx for Windows File Server provides native SMB shares, NTFS ACLs and Active Directory integration. EFS is NFS for Linux, FSx for Lustre is for high-performance compute, and S3 is object storage, not an SMB file system.","3.1 High-performing and scalable storage"],
    ["saa57",0,3,"A machine learning team trains models on a 200 TB data set stored in S3 and needs a file system with very high throughput and low latency for the compute cluster during each training run. What should the architect choose?",["Amazon EFS in the One Zone storage class","An S3 bucket mounted as a shared drive","Amazon FSx for Lustre linked to the S3 bucket","EBS gp3 volumes on each training instance"],2,"FSx for Lustre is built for HPC and ML, delivers hundreds of GB/s, and can lazy-load and write back to a linked S3 bucket. EFS is general-purpose NFS with lower peak throughput, S3 is not a POSIX file system, and separate gp3 volumes are not shared.","3.1 High-performing and scalable storage"],
    ["saa58",0,3,"Users around the world upload video files of several gigabytes to one S3 bucket in us-east-1. Uploads from Asia and Europe are slow. What should the architect do to improve upload speed?",["Turn on S3 Transfer Acceleration and use multipart uploads","Create a CloudFront distribution with the bucket as its origin","Move the bucket to the S3 One Zone-Infrequent Access class","Put a Network Load Balancer in front of the S3 bucket"],0,"Transfer Acceleration sends uploads to the nearest edge location and over the AWS backbone to the bucket, and multipart upload sends parts in parallel and retries only failed parts. CloudFront mainly speeds up downloads, a storage class does not change network speed, and an NLB cannot front S3.","3.1 High-performing and scalable storage"],
    ["saa59",0,3,"Which statement about S3 multipart upload is correct?",["Parts upload in parallel, and it is required for objects larger than 5 GB","It is required for all objects larger than 100 MB","It only works when Transfer Acceleration is turned on","Each part must be at least 1 GB, except the last part"],0,"A single PUT can upload up to 5 GB, so larger objects need multipart upload; AWS recommends it from about 100 MB. Parts can be 5 MB to 5 GB, can upload in parallel, and work with or without Transfer Acceleration.","3.1 High-performing and scalable storage"],
    ["saa60",0,3,"An application on RDS for PostgreSQL runs the same product catalog queries thousands of times a minute, and database CPU is near 90%. The data changes a few times a day. What is the most effective way to reduce latency and load?",["Convert the database to a Multi-AZ instance so the standby handles half the queries","Increase the allocated storage so the volume gets more baseline IOPS","Move the database to a larger instance and turn on storage autoscaling","Add Amazon ElastiCache for Redis and cache query results with lazy loading"],3,"A cache serves repeated reads in microseconds and removes them from the database; lazy loading fills the cache on a miss, and a TTL keeps data fresh. A classic Multi-AZ standby does not serve reads, more storage IOPS does not fix CPU, and a larger instance costs more without removing repeated work.","3.3 High-performing database solutions"],
    ["saa61",0,3,"A DynamoDB table serves a read-heavy leaderboard. The team needs microsecond read latency with minimal code changes. What should the architect add?",["DynamoDB Accelerator (DAX)","DynamoDB Streams","A global secondary index","ElastiCache for Memcached"],0,"DAX is an in-memory cache built for DynamoDB and is API-compatible, so the application mainly switches to the DAX client. Streams capture changes, a GSI gives another query pattern but not microsecond reads, and Memcached would need custom caching logic in the application.","3.3 High-performing database solutions"],
    ["saa62",0,3,"A news website hosted in one Region serves images, videos and HTML to readers worldwide, and readers far from the Region see slow page loads. What should the architect do first?",["Add more EC2 instances to the website's Auto Scaling group","Move the website's origin to a larger instance type in the same Region","Put an Amazon CloudFront distribution in front of the website","Create a Route 53 weighted record for the existing load balancer"],2,"CloudFront caches content at edge locations close to readers, cutting latency and load on the origin, and it also speeds up dynamic content over the AWS network. More or larger origin instances do not shorten the distance to readers, and weighted routing does not add locations.","3.4 High-performing network architectures"],
    ["saa63",0,3,"A multiplayer game uses UDP and needs two static IP addresses that clients can hard-code, plus fast failover between game servers in two Regions. What should the architect use?",["Amazon CloudFront with the game servers in both Regions as custom origins","Route 53 latency routing with a TTL of 60 seconds","An Application Load Balancer in each of the two Regions","AWS Global Accelerator with endpoint groups in both Regions"],3,"Global Accelerator provides two static anycast IPs, supports TCP and UDP, routes over the AWS network and fails over between Regions in seconds without waiting for DNS caches. CloudFront handles HTTP and HTTPS, DNS failover depends on client caching, and ALBs are regional HTTP load balancers.","3.4 High-performing network architectures"],
    ["saa64",0,3,"Hundreds of concurrent Lambda functions connect to an RDS for MySQL database, and the database runs out of connections during traffic spikes. What should the architect add?",["A read replica for every 100 concurrent Lambda functions","An SQS queue that stores the database connection strings","A larger EBS volume attached to the database instance","Amazon RDS Proxy between the functions and the database"],3,"RDS Proxy pools and shares connections, so many short-lived functions reuse a small number of database connections, and it also speeds up failover. Read replicas do not help write connections, a queue of connection strings does nothing, and storage size does not change connection limits.","3.3 High-performing database solutions"],
    ["saa65",0,3,"A retailer wants to run complex SQL analytics with large joins across several petabytes of sales history and feed BI dashboards. Which service is designed for this?",["Amazon RDS for MySQL","Amazon DynamoDB","Amazon ElastiCache","Amazon Redshift"],3,"Redshift is a columnar, massively parallel data warehouse for analytic SQL at petabyte scale. RDS is an OLTP database that struggles at this size, DynamoDB is a key-value store without joins, and ElastiCache is an in-memory cache.","3.3 High-performing database solutions"],
    ["saa66",0,3,"Analysts want to run occasional SQL queries on application logs already stored in S3 as Parquet files, without loading them into a database or managing servers. What should the architect recommend?",["Amazon RDS for PostgreSQL with a nightly import of the log files","Amazon Athena with tables defined in the AWS Glue Data Catalog","Amazon EMR on a cluster of EC2 instances that runs all the time","Amazon Redshift provisioned clusters loaded with the COPY command"],1,"Athena is serverless, queries data in place in S3 with standard SQL and charges per data scanned, which columnar Parquet keeps small. Nightly imports and always-on clusters add servers and cost for occasional queries.","3.5 Data ingestion and transformation"],
    ["saa67",0,3,"A website must capture clickstream events in real time. Three separate applications need to read the same events, and one of them must be able to reprocess the last 24 hours after a bug fix. Which service fits?",["Amazon Kinesis Data Streams","Amazon SQS standard queue","Amazon Data Firehose","Amazon SNS standard topic"],0,"Kinesis Data Streams keeps records for 24 hours by default (extendable) so multiple consumers can read independently and replay from a point in time. An SQS message is deleted once processed, Firehose delivers to destinations without replay, and SNS pushes messages without storing them.","3.5 Data ingestion and transformation"],
    ["saa68",0,3,"A company wants streaming application logs delivered to S3 in near real time, converted to Parquet, with no servers or consumer code to manage. What should the architect use?",["Amazon Kinesis Data Streams with a custom consumer on EC2","Amazon Data Firehose with record format conversion to S3","Amazon SQS with a Lambda function that writes files","AWS DataSync scheduled to run every 15 minutes"],1,"Firehose is a fully managed delivery service that buffers, optionally transforms and converts to Parquet or ORC, and writes to S3. Data Streams and SQS need consumer code, and DataSync copies files between storage systems rather than ingesting streams.","3.5 Data ingestion and transformation"],
    ["saa69",0,3,"An internal application gets a sharp rise in traffic every weekday at 8:45 a.m. when staff sign in. Instances take several minutes to boot, so users see slow responses first thing. What should the architect add?",["A simple scaling policy that adds one instance when CPU passes 90%","A scheduled scaling action that raises capacity shortly before 8:45 a.m.","A longer health check grace period on the Auto Scaling group","A larger minimum group size set by hand every morning by the operations team"],1,"A scheduled action adds capacity before the known peak, so instances are warm when users arrive (predictive scaling can also learn the pattern). A reactive CPU policy starts adding instances only after users are already affected, and manual changes are error-prone.","3.2 High-performing compute solutions"],
    ["saa70",0,3,"Which EC2 Auto Scaling policy type adds or removes instances to keep a chosen metric, such as average CPU utilization, close to a set value like 50%?",["Scheduled scaling","Simple scaling","Manual scaling","Target tracking scaling"],3,"Target tracking works like a thermostat: it creates and manages CloudWatch alarms to hold the metric near the target. Scheduled scaling changes capacity at set times, simple scaling applies one adjustment per alarm, and manual scaling needs a person to change capacity.","3.2 High-performing compute solutions"],
    ["saa71",0,3,"A company must move 80 TB of archive files to S3 within two weeks. Its internet link is 100 Mbps and already busy during business hours. What should the architect recommend?",["Copy the files over the internet with AWS DataSync","Upload the files with S3 Transfer Acceleration","Order AWS Snowball Edge devices and ship the data to AWS","Set up a new Site-to-Site VPN and use the AWS CLI"],2,"At 100 Mbps, 80 TB takes more than 70 days even with the link fully used, so an offline Snowball transfer is the only way to meet two weeks. DataSync, Transfer Acceleration and a VPN all still depend on the same slow link.","3.5 Data ingestion and transformation"],
    ["saa72",0,3,"A company is migrating an on-premises Oracle database to Amazon Aurora PostgreSQL and can accept only a few minutes of downtime at cutover. What should the architect use?",["AWS DMS alone, with a full load during a weekend outage window","AWS SCT to convert the schema, then AWS DMS with change data capture","Oracle Data Pump export files copied to S3 and then imported into Aurora","AWS DataSync to copy the Oracle data files to an EFS file system"],1,"A heterogeneous migration needs the Schema Conversion Tool for schema and code, and DMS with ongoing replication keeps the target in sync until a short cutover. DMS alone does not convert Oracle-specific schema objects, Data Pump files cannot be imported into PostgreSQL, and copying data files does not convert anything.","3.5 Data ingestion and transformation"],
    ["saa73",0,4,"A company runs a steady 24/7 workload and expects to keep that level of compute for three years, but it plans to move parts of it from EC2 to Fargate and Lambda and may change Regions. Which purchase option saves the most while allowing this?",["EC2 Instance Savings Plans for the current family","Standard Reserved Instances for the current type","On-Demand Capacity Reservations in each AZ","A Compute Savings Plan with a three-year term"],3,"Compute Savings Plans apply across instance families, sizes, Regions, operating systems, Fargate and Lambda. EC2 Instance Savings Plans and Standard RIs are tied to a family or type in one Region, and Capacity Reservations guarantee capacity without any discount on their own.","4.2 Cost-optimized compute solutions"],
    ["saa74",0,4,"A media company renders video frames in batch jobs that can stop and resume at any time, and deadlines are flexible. Which EC2 purchasing option is cheapest?",["On-Demand Instances","Dedicated Instances","Spot Instances","Standard Reserved Instances"],2,"Spot offers spare capacity at large discounts, and interruptions are acceptable because the jobs checkpoint and resume. On-Demand costs more, Dedicated Instances add a premium for isolated hardware, and RIs need a one- or three-year commitment to steady use.","4.2 Cost-optimized compute solutions"],
    ["saa75",0,4,"How much warning does Amazon EC2 give before it reclaims a Spot Instance?",["A two-minute interruption notice","A 30-second interruption notice","A 15-minute rebalance window","A one-hour termination notice"],0,"EC2 sends a two-minute Spot interruption notice through instance metadata and EventBridge. A separate rebalance recommendation can arrive earlier, but it is a signal, not a guaranteed time window.","4.2 Cost-optimized compute solutions"],
    ["saa76",0,4,"A company is moving licensed software to EC2. The license is priced per physical CPU socket and core, so the company must see and control the underlying server. Which option meets this?",["EC2 Dedicated Hosts","EC2 Dedicated Instances","Spot Instances in a cluster placement group","Compute Savings Plans"],0,"Dedicated Hosts give a whole physical server with visibility of sockets and cores, supporting bring-your-own-license terms. Dedicated Instances run on single-tenant hardware but hide the host details, and Spot or Savings Plans are pricing models, not host visibility.","4.2 Cost-optimized compute solutions"],
    ["saa77",0,4,"A development environment of 20 EC2 instances is used only from 8 a.m. to 6 p.m. on weekdays. What is the most cost-effective approach?",["Stop the On-Demand instances outside working hours with a schedule","Buy three-year All Upfront Reserved Instances for every one of the instances","Move each instance onto its own EC2 Dedicated Host","Buy a Compute Savings Plan sized for 24 hours of use a day"],0,"The instances are needed about 50 hours out of 168 a week, so stopping them the rest of the time (for example with AWS Instance Scheduler) cuts compute cost by around 70%. Reservations and Savings Plans bill for every hour of the term whether used or not, and Dedicated Hosts add cost.","4.2 Cost-optimized compute solutions"],
    ["saa78",0,4,"A data lake bucket holds objects whose access patterns are unknown and change over time. The company wants automatic savings without retrieval fees or lifecycle rules to maintain. Which storage class fits?",["S3 Standard-Infrequent Access","S3 Intelligent-Tiering","S3 One Zone-Infrequent Access","S3 Glacier Flexible Retrieval"],1,"Intelligent-Tiering moves each object between access tiers based on its own access, with a small monitoring fee and no retrieval fees. Standard-IA and One Zone-IA charge retrieval fees and suit data you know is rarely read, and Glacier Flexible Retrieval takes minutes to hours to restore.","4.1 Cost-optimized storage solutions"],
    ["saa79",0,4,"Application logs in S3 are read often for 30 days, occasionally for the next 60 days, and never after one year, when they can be deleted. How should the architect keep storage costs down with the least effort?",["Run a nightly Lambda function that copies logs into a new bucket","Add an S3 Lifecycle configuration that transitions and then expires the objects","Turn on S3 Versioning so older logs move to cheaper storage","Enable S3 Transfer Acceleration to compress the log objects"],1,"A lifecycle rule can move objects to Standard-IA after 30 days, to a Glacier class after 90 days, and delete them after 365 days, with no code. Custom Lambda copies cost more to build and run, versioning keeps more data rather than less, and Transfer Acceleration does not compress anything.","4.1 Cost-optimized storage solutions"],
    ["saa80",0,4,"A company keeps thumbnail images that can be regenerated from the originals at any time. They are read rarely but must load in milliseconds when requested. Which storage class is the cheapest suitable choice?",["S3 Standard","S3 One Zone-Infrequent Access","S3 Glacier Deep Archive","S3 Standard-Infrequent Access"],1,"One Zone-IA costs less than Standard-IA because it stores data in one AZ; losing that AZ is acceptable when the data can be recreated, and access is still in milliseconds. Standard costs more for rarely read data, and Deep Archive takes hours to restore.","4.1 Cost-optimized storage solutions"],
    ["saa81",0,4,"A company has hundreds of gp2 EBS volumes that are larger than needed only to get more IOPS. How can the architect lower cost without downtime?",["Change the volumes to sc1 Cold HDD to cut the price per GB","Change the volumes to gp3 with Elastic Volumes and set IOPS separately","Take snapshots, delete the volumes and restore them from the snapshots only at night","Move the data to instance store volumes on larger instances"],1,"gp3 is cheaper per GB than gp2, includes 3,000 IOPS and 125 MB/s baseline at any size, and lets you buy extra IOPS without buying capacity; Elastic Volumes changes type online. Cold HDD cannot deliver the IOPS, deleting volumes breaks the workload, and instance store is not persistent.","4.1 Cost-optimized storage solutions"],
    ["saa82",0,4,"Regulators require bank records to be kept for ten years. They are almost never read, and a retrieval time of up to 48 hours is acceptable. Which S3 storage class is the lowest cost?",["S3 Glacier Deep Archive","S3 Glacier Instant Retrieval","S3 Glacier Flexible Retrieval","S3 Intelligent-Tiering"],0,"Deep Archive is the lowest-cost S3 class, with standard retrieval within 12 hours and bulk within 48 hours. Instant Retrieval and Flexible Retrieval cost more to store, and Intelligent-Tiering is aimed at changing access patterns.","4.1 Cost-optimized storage solutions"],
    ["saa83",0,4,"A new DynamoDB table backs an event ticketing app. Traffic is near zero most days and spikes unpredictably to thousands of requests per second when tickets go on sale. Which capacity setup is most cost-effective?",["On-demand capacity mode","Provisioned capacity sized for the peak","Provisioned capacity with reserved capacity","A DAX cluster in front of a small table"],0,"On-demand bills per request and scales instantly, so idle days cost almost nothing and spikes need no planning. Provisioning for the peak pays for unused capacity most of the time, reserved capacity needs steady use, and DAX only caches reads, not writes during sales.","4.3 Cost-optimized database solutions"],
    ["saa84",0,4,"A hospital archives medical images that are opened about once a quarter, but when a doctor opens one it must load within milliseconds. Which S3 storage class is most cost-effective?",["S3 Glacier Flexible Retrieval with expedited retrievals","S3 Glacier Deep Archive with standard retrievals","S3 Standard with S3 Storage Lens monitoring","S3 Glacier Instant Retrieval"],3,"Glacier Instant Retrieval is for long-lived data accessed about once a quarter, with millisecond access and lower storage cost than Standard-IA. Expedited Flexible Retrieval still takes 1 to 5 minutes and Deep Archive takes hours, while Standard costs more for rarely read data and Storage Lens only reports on usage.","4.1 Cost-optimized storage solutions"],
    ["saa85",0,4,"A test team uses an Aurora MySQL database for a few hours at a time on irregular days, with load that varies widely. The company wants to stop paying for a fixed instance size. What should the architect choose?",["An Aurora provisioned cluster with a three-year reserved instance","Aurora Serverless v2, which scales capacity with the load","An RDS for MySQL Multi-AZ DB cluster with Provisioned IOPS","A self-managed MySQL server running on a Dedicated Host"],1,"Aurora Serverless v2 adjusts capacity in fine steps (ACUs) as load changes, and you pay for the capacity used. A three-year reservation commits to a fixed size, and Multi-AZ or Dedicated Host options add cost for an intermittent test workload.","4.3 Cost-optimized database solutions"],
    ["saa86",0,4,"EC2 instances in private subnets read and write large volumes of DynamoDB data through a NAT gateway, and NAT data processing charges are high. What is the most cost-effective change?",["Replace the NAT gateway with a self-managed NAT instance on a larger instance type","Add a DynamoDB gateway VPC endpoint to the private route tables","Add a second NAT gateway in each Availability Zone","Turn on DynamoDB Accelerator in front of the table"],1,"Gateway endpoints for DynamoDB and S3 have no hourly or data processing charge and take that traffic off the NAT gateway. A NAT instance still processes the traffic and must be managed, more NAT gateways add cost, and DAX does not change the network path for writes.","4.4 Cost-optimized network architectures"],
    ["saa87",0,4,"Which type of data transfer is generally free of charge on AWS?",["Data transferred in to AWS from the internet","Data transferred out from EC2 to the internet","Data transferred between two AWS Regions","Data sent between AZs over private IP addresses"],0,"Inbound data transfer from the internet is free. Data out to the internet, between Regions and between Availability Zones is charged per GB, which is why architects keep chatty traffic in one AZ where it is safe and use CloudFront to reduce egress.","4.4 Cost-optimized network architectures"],
    ["saa88",0,4,"The finance team wants an email as soon as the forecasted monthly AWS spend for a project goes above $10,000, before the month ends. What should the architect set up?",["A monthly Cost Explorer report that is exported and emailed to the finance team","A CloudWatch alarm on each EC2 instance's CPU utilization","A Cost and Usage Report that is delivered to an S3 bucket","An AWS Budgets cost budget with an alert on forecasted spend"],3,"AWS Budgets can alert on actual or forecasted cost and filter by tags or accounts. Cost Explorer is for analysis rather than alerts, CPU alarms do not measure spend, and the Cost and Usage Report is detailed billing data with no alerting of its own.","Domain 4: cost management tools (AWS Budgets)"],
    ["saa89",0,4,"Several projects share the same AWS accounts, and finance needs monthly costs broken down by project. What should the architect do?",["Tag resources with a project key and activate it as a cost allocation tag","Create a separate VPC for each project in the same account","Enable detailed CloudWatch monitoring on all the resources","Turn on AWS CloudTrail data events for every service"],0,"Once a user-defined tag is activated as a cost allocation tag, Cost Explorer and the Cost and Usage Report can group costs by it; tag policies help keep tagging consistent. VPCs do not split billing, and CloudWatch and CloudTrail track metrics and API calls, not cost.","Domain 4: cost allocation tags"],
    ["saa90",0,4,"A company suspects many EC2 instances are over-provisioned. It wants recommendations for smaller or different instance types based on actual CPU and memory usage. Which service provides this?",["AWS Trusted Advisor security checks","AWS Compute Optimizer","Amazon Inspector","AWS Config conformance packs"],1,"Compute Optimizer analyzes CloudWatch metrics with machine learning and recommends right-sized EC2 instances, Auto Scaling groups, EBS volumes and Lambda memory. Trusted Advisor security checks, Inspector and Config do not size instances.","4.2 Cost-optimized compute solutions"]
  ]
});
