/* Generated by tools/build.js from data/terraform.js. Edit that file, not this one. */
CertHub.register({"id":"terraform","vendor":"HashiCorp","name":"HashiCorp Certified: Terraform Associate","short":"Terraform Associate","exam":"Terraform Associate (004)","blurb":"Entry-level infrastructure-as-code certification for cloud engineers, DevOps and security staff who write, plan, apply and manage Terraform configuration and use HCP Terraform.","status":"check","statusNote":"Exam version 004 (tests Terraform 1.12, replaced 003 on Jan 8, 2026) and its eight objectives were confirmed from HashiCorp's 004 exam content list and learning path as indexed on developer.hashicorp.com on Sept 24, 2026; the pages could not be opened directly from this network, so re-check the objective wording there. HashiCorp publishes no domain weights: weights here are estimated from the number of sub-objectives in each objective.","lastVerified":"2026-09-24","notices":[{"from":"2026-01-08","until":"2026-12-31","text":"Terraform Associate 004 replaced 003 on January 8, 2026. It tests Terraform 1.12 and adds lifecycle rules, custom conditions, ephemeral values and write-only arguments, and HCP Terraform projects. Make sure your study materials say 004."}],"examInfo":{"questions":"About 57 (multiple choice, multiple answer and true/false)","minutes":60,"pass":"Not published","extra":"Online proctored, English, US$70.50 plus tax. Tests Terraform 1.12. Certification is valid for two years."},"examSim":{"questions":57,"minutes":60},"sources":[{"label":"HashiCorp infrastructure automation certifications (Terraform Associate)","url":"https://developer.hashicorp.com/certifications/infrastructure-automation"},{"label":"Terraform Associate 004 exam content list","url":"https://developer.hashicorp.com/terraform/tutorials/certification-004/associate-review-004"},{"label":"Terraform Associate 004 learning path","url":"https://developer.hashicorp.com/terraform/tutorials/certification-004/associate-study-004"}],"planWeeks":6,"hoursPerWeek":"5–7","domains":[{"id":1,"name":"Infrastructure as Code (IaC) with Terraform","w":8,"topics":["What IaC is: infrastructure defined in version-controlled, machine-readable files instead of manual console changes","Advantages of IaC: repeatability, consistency, code review, audit history, automation, fewer configuration errors","Declarative (describe the end state) vs imperative (list the steps) approaches","Idempotence: applying the same configuration twice makes no further changes","Terraform's plugin model: one workflow and language (HCL) for many providers","Multi-cloud and hybrid-cloud deployments from a single configuration","Service-agnostic workflows: managing SaaS, DNS, Git, Kubernetes and monitoring tools with providers","Terraform vs configuration management tools: provisioning infrastructure vs configuring software inside servers","Immutable infrastructure: replace rather than patch in place"],"notes":["Objectives 1a–1c"],"labs":["Install the Terraform CLI (free) and run `terraform version`; write a one-page note comparing a manual setup you have done with how IaC would change it.","Write a configuration that uses the `local` and `random` providers to create a file with a random pet name, apply it twice and confirm the second apply makes no changes (idempotence).","Add the `kreuzwerker/docker` provider next to `local` in the same configuration to show one workflow managing two unrelated platforms."]},{"id":2,"name":"Terraform fundamentals","w":11,"topics":["Installing Terraform and pinning its version with `required_version`","The `required_providers` block: `source` addresses (hostname/namespace/type) and `version` constraints","Version constraint operators: `=`, `!=`, `>=`, `<=`, and the pessimistic `~>`","The dependency lock file `.terraform.lock.hcl`: what it records and why it is committed","How providers work: plugins that call APIs, downloaded by `terraform init` from the Terraform Registry","Provider tiers in the registry: official, partner and community","Provider configuration blocks, multiple configurations with `alias`, and the `provider` meta-argument","Using several different providers in one configuration","What state is for: mapping configuration to real objects, tracking metadata and dependencies, speeding up plans","State contents: resource attributes, including sensitive values, in plain JSON"],"notes":["Objectives 2a–2d"],"labs":["Pin the `hashicorp/random` provider with `~> 3.5`, run `terraform init`, read `.terraform.lock.hcl`, then run `terraform init -upgrade` and note what changes.","Configure two Docker provider blocks (one aliased) pointing at the same local Docker host and create a container with each, using `provider = docker.second`.","Apply a small config, open `terraform.tfstate` in an editor and find each resource's type, name, provider and attributes; note any secret values stored there."]},{"id":3,"name":"Core Terraform workflow","w":19,"topics":["The Write → Plan → Apply workflow for individuals and teams","`terraform init`: backend setup, provider and module download, `-upgrade`, `-migrate-state`, `-reconfigure`, `-backend-config`","`terraform validate`: syntax and internal consistency checks without contacting provider APIs","`terraform plan`: refresh, diff and symbols (`+`, `-`, `~`, `-/+`), saved plans with `-out`","Planning options: `-var`, `-var-file`, `-target`, `-refresh=false`, `-refresh-only`, `-replace`","`terraform apply`: interactive approval, `-auto-approve`, applying a saved plan file","`terraform destroy` and `terraform plan -destroy`","`terraform fmt`: canonical style, `-check`, `-diff` and `-recursive`","Resource replacement with `-replace` instead of the deprecated `terraform taint`","Parallelism and the dependency graph during apply"],"notes":["Objectives 3a–3g"],"labs":["Use the Docker provider to run an nginx container on port 8080: run `init`, `fmt`, `validate`, `plan -out=tfplan`, `apply tfplan`, check http://localhost:8080, then `destroy`.","Change the container's external port and read the plan symbols; then run `terraform apply -replace=docker_container.web` and compare the plan output.","Break the configuration on purpose (a missing brace, then a reference to an undeclared variable) and compare what `terraform fmt`, `terraform validate` and `terraform plan` report."]},{"id":4,"name":"Terraform configuration","w":22,"topics":["Resource blocks vs data blocks, and addressing them (`TYPE.NAME`, `data.TYPE.NAME`)","Cross-resource references and implicit dependencies","Input variables: `type`, `default`, `description`, `sensitive`, `nullable`, and value precedence (TF_VAR_, tfvars, auto.tfvars, -var)","Output values and local values","Complex types: list, map, set, object, tuple; type conversion","Expressions: conditionals, `for` expressions, splat `[*]`, string templates, `dynamic` blocks","Built-in functions and testing them in `terraform console`","`count` vs `for_each`, `count.index`, `each.key` and `each.value`","Explicit dependencies with `depends_on`; `lifecycle` rules: `create_before_destroy`, `prevent_destroy`, `ignore_changes`, `replace_triggered_by`","Custom conditions: variable `validation`, `precondition` and `postcondition`, and `check` blocks","Sensitive data: `sensitive` values, ephemeral variables and resources, write-only arguments, secrets in state","Secrets management with HashiCorp Vault and the Vault provider"],"notes":["Objectives 4a–4h"],"labs":["Create three `local_file` resources from a map with `for_each`, then rewrite with `count` and remove the middle item in each version to see which approach causes fewer changes.","Add a variable `validation` that only allows the environment names dev, test and prod, and a `postcondition` on a `local_file`; trigger each failure and read the error messages.","Run a Vault dev server (`vault server -dev`), store a secret, read it with the Vault provider's `vault_kv_secret_v2` data source and mark the output `sensitive`; then look for the value in the state file."]},{"id":5,"name":"Terraform modules","w":11,"topics":["Root module vs child modules; a module is any directory of .tf files","Module sources: local paths (`./` or `../`), the public Terraform Registry, private registries, Git and HTTP URLs","Calling a module with a `module` block and passing input variables","Variable scope: child modules only see values passed in; outputs are read as `module.NAME.OUTPUT`","Passing provider configurations to modules with the `providers` argument","The `version` argument (registry sources only) and `ref` for Git sources","`terraform init` or `terraform get` to install modules into `.terraform/modules`","Using `count` and `for_each` on module blocks","Standard module structure: main.tf, variables.tf, outputs.tf, README"],"notes":["Objectives 5a–5d"],"labs":["Build a local module `./modules/web` that runs a Docker container from `image` and `external_port` inputs and outputs the container name; call it twice from the root module.","Call a public registry module pinned with `version = \"~> x.y\"` (for example a small utility module), run `terraform init` and inspect `.terraform/modules/modules.json`.","Try to read a child module's variable directly from the root module, read the error, then fix it by adding a module output."]},{"id":6,"name":"Terraform state management","w":11,"topics":["The default local backend and the `terraform.tfstate` file, `terraform.tfstate.backup`","State locking: why it prevents corruption, which backends support it, `-lock-timeout` and `terraform force-unlock`","The `backend` block inside `terraform {}`: remote backends such as S3, azurerm, gcs, consul and pg","Backend blocks cannot use variables; partial configuration with `-backend-config`","Migrating state between backends with `terraform init -migrate-state`","The `cloud` block for HCP Terraform","Sensitive data in state and protecting remote state (encryption, access control)","Resource drift: detecting it with `plan` and `apply -refresh-only`, reconciling configuration","CLI workspaces: `terraform workspace new/select/list`, `terraform.workspace`"],"notes":["Objectives 6a–6d"],"labs":["Run a free PostgreSQL container in Docker and configure the `pg` backend; migrate an existing local state into it with `terraform init -migrate-state`.","Start a long apply (a `time_sleep` resource), run a second `terraform apply` in another terminal and read the lock error; practise `-lock-timeout`.","Create a Docker container with Terraform, change it outside Terraform (`docker rename` or stop it), then use `terraform plan -refresh-only` to see the drift and decide how to reconcile it."]},{"id":7,"name":"Maintain infrastructure with Terraform","w":8,"topics":["Importing existing infrastructure with `import` blocks and generating configuration with `terraform plan -generate-config-out`","The older `terraform import` CLI command","Inspecting state: `terraform state list`, `terraform state show`, `terraform show`, `terraform output`","Refactoring: `moved` blocks and `terraform state mv`","Removing resources from state without destroying them: `removed` blocks and `terraform state rm`","Verbose logging with `TF_LOG` (TRACE, DEBUG, INFO, WARN, ERROR, JSON), `TF_LOG_PATH`, `TF_LOG_CORE` and `TF_LOG_PROVIDER`","When to use logs: provider errors, crashes and bug reports","Reviewing outputs and dependencies with `terraform output -json` and `terraform graph`"],"notes":["Objectives 7a–7c"],"labs":["Start a container by hand with `docker run -d --name legacy nginx`, write an `import` block for it, run `terraform plan -generate-config-out=generated.tf` and clean up the generated config.","Rename a resource in code, add a `moved` block, and confirm the plan shows a move rather than destroy-and-create; then list and show it with `terraform state` commands.","Run `TF_LOG=DEBUG TF_LOG_PATH=./tf.log terraform plan`, then search the log for the provider's API calls and the plugin handshake."]},{"id":8,"name":"HCP Terraform","w":10,"topics":["HCP Terraform (formerly Terraform Cloud): remote state, remote runs, a free tier and paid tiers","Connecting the CLI: `terraform login` and the `cloud` block (organization, workspaces by name or tags)","Workflows: VCS-driven, CLI-driven and API-driven runs","Workspaces: each holds its own state, variables, run history and permissions","Projects: grouping workspaces and assigning team access at the project level","Variables and variable sets; Terraform vs environment variables; sensitive variables","Collaboration and governance: teams and permissions, run approvals, policy as code (Sentinel and OPA), private registry","Health assessments: drift detection and continuous validation","Integrations: VCS providers, run triggers, run tasks, notifications, dynamic provider credentials"],"notes":["Objectives 8a–8d"],"labs":["Create a free HCP Terraform account and organization, run `terraform login`, add a `cloud` block to a config that uses only the `random` provider and `terraform_data`, and run a CLI-driven plan that executes remotely.","Create a project with two workspaces (dev and prod), attach a variable set to the project and confirm both workspaces inherit it.","Connect a GitHub repository to a VCS-driven workspace, open a pull request and read the speculative plan posted back to it."]}],"study":{"1":[["What is infrastructure as code, and name three benefits over manual changes.","Defining infrastructure in machine-readable, version-controlled files. Benefits: repeatable and consistent builds, peer review and audit history through version control, faster automated provisioning, and fewer human errors or snowflake servers."],["Explain declarative vs imperative IaC with a Terraform example.","Declarative code describes the desired end state (\"three containers exist\"); Terraform works out the steps. Imperative code lists the steps (\"create container, then another\"). With Terraform, changing count from 3 to 5 creates only two more."],["How does Terraform support multi-cloud and service-agnostic workflows?","It uses one language and one plan/apply workflow, and providers translate that into API calls for any platform: AWS and Azure in the same configuration, plus DNS, GitHub, Kubernetes or monitoring tools."],["What does idempotent mean for Terraform?","Running apply again with unchanged configuration and unchanged real infrastructure makes no changes, because Terraform compares desired state with current state and only acts on differences."],["How does Terraform differ from a configuration management tool like Ansible?","Terraform focuses on provisioning and managing the lifecycle of infrastructure (networks, VMs, services) and tracks it in state. Configuration management tools mainly install and configure software inside existing servers."]],"2":[["What does `version = \"~> 3.5\"` allow, and how is that different from `~> 3.5.0`?","`~> 3.5` allows 3.5 and later 3.x releases but not 4.0. `~> 3.5.0` allows only 3.5.x patch releases."],["What is `.terraform.lock.hcl` and should it be in version control?","The dependency lock file records the exact provider versions and checksums chosen by `terraform init`. Commit it so every run and teammate uses the same provider builds; update it with `terraform init -upgrade`."],["How do you use two regions of the same provider in one configuration?","Declare two provider blocks, give one an `alias`, and set `provider = aws.west` (for example) on resources that should use the aliased configuration."],["Give three reasons Terraform needs state.","To map resources in configuration to real-world object IDs, to track metadata such as dependencies for correct ordering on destroy, and to improve performance by caching attributes. It also enables collaboration when stored remotely."],["Why must the state file be protected?","It stores every resource attribute in plain text, including passwords and keys marked sensitive. Use a remote backend with encryption and access control, and never commit state to Git."]],"3":[["Describe the core Terraform workflow and what each step does.","Write configuration; run `terraform plan` to preview the changes against current state; run `terraform apply` to make them. In teams, plans are reviewed in pull requests before apply."],["What does `terraform init` do?","Initializes the working directory: configures the backend, downloads providers (and writes the lock file) and installs modules. It is safe to run many times and is required after adding providers, modules or changing the backend."],["How do `terraform validate` and `terraform fmt` differ?","`validate` checks that the configuration is syntactically valid and internally consistent (types, references). `fmt` only rewrites files into canonical style; with `-check` it reports files that need formatting."],["Why save a plan with `-out`?","Applying a saved plan (`terraform apply tfplan`) applies exactly the reviewed changes without prompting, which is how automation separates review from execution."],["How do you force Terraform to recreate one healthy resource?","`terraform apply -replace=ADDRESS`. It replaces the older `terraform taint` command and shows the replacement in the plan first."],["What plan symbols show create, destroy, update in place and replace?","`+` create, `-` destroy, `~` update in place, `-/+` destroy then create (or `+/-` create then destroy when `create_before_destroy` is set)."]],"4":[["When would you use `for_each` instead of `count`?","When instances are identified by a meaningful key (a map or set of strings). Removing an item only affects that instance, while with `count` removing a middle element shifts indexes and can change or recreate later instances."],["List the order in which Terraform applies variable values, lowest to highest precedence.","Environment variables (TF_VAR_name), terraform.tfvars, terraform.tfvars.json, *.auto.tfvars files in lexical order, then -var and -var-file on the command line in the order given (the last one wins)."],["What is the difference between a precondition, a postcondition and a check block?","Preconditions are checked before a resource is planned or applied and postconditions after; both block the run with an error. A check block's assertions report warnings without stopping the run."],["What does `sensitive = true` do, and what does it not do?","It redacts the value in plan, apply and output display. It does not remove the value from the state file."],["What do ephemeral values and write-only arguments add?","Ephemeral variables and resources exist only during a run and are never written to plan or state files. Write-only arguments (for example `password_wo`) accept ephemeral values that are sent to the provider but not stored."],["When is `depends_on` needed?","Only for hidden dependencies Terraform cannot see from references, such as an app that needs an IAM policy that nothing in its arguments references. Prefer implicit references where possible."]],"5":[["What module sources can Terraform use?","Local paths starting with ./ or ../, the public Terraform Registry (namespace/name/provider), private registries such as HCP Terraform's, Git, GitHub, Bitbucket, HTTP archives, and cloud storage buckets."],["How does data flow between a root module and a child module?","The root passes values in through arguments that match the child's input variables. The child exposes values through outputs, read as `module.NAME.OUTPUT`. Nothing else crosses the boundary."],["How do you pin a module version?","Registry modules use the `version` argument with a constraint. Git sources pin with `?ref=` a tag or commit in the source URL; local paths have no version."],["Do child modules inherit provider configurations?","Default (unaliased) provider configurations are inherited automatically. Aliased configurations must be passed explicitly with the `providers` map in the module block."],["Which command installs or updates modules?","`terraform init` installs them into .terraform/modules; `terraform get -update` (or `init -upgrade`) fetches newer versions allowed by constraints."]],"6":[["What is the local backend and where does it keep state?","The default backend, used when no backend is configured. It stores state in terraform.tfstate in the working directory (per-workspace files under terraform.tfstate.d) and locks using the local file system."],["What is state locking and what do you do if a lock gets stuck?","Locking stops two runs writing state at once. If a crashed run leaves a lock behind, confirm nothing is running and then use `terraform force-unlock LOCK_ID`."],["Why can't you use variables in a backend block, and what is the workaround?","The backend is set up during init before variables are evaluated. Use partial configuration: leave values out and supply them with `-backend-config` files or key=value pairs."],["How do you move state from local to a remote backend?","Add the backend (or cloud) block and run `terraform init -migrate-state`; Terraform copies the existing state to the new backend after confirmation."],["What is drift and how do you handle it?","Drift is a difference between real infrastructure and state caused by changes outside Terraform. `terraform plan -refresh-only` shows it; either accept it into state with `apply -refresh-only` (and update the code) or run a normal apply to put the infrastructure back."]],"7":[["How do you bring an existing resource under Terraform management today?","Add an `import` block with `to` (the resource address) and `id`, run `terraform plan -generate-config-out=generated.tf` to draft configuration if needed, review it, then apply."],["How do you rename a resource without destroying it?","Add a `moved` block from the old address to the new one (or run `terraform state mv`). The plan then shows a move and no replacement."],["How do you stop managing a resource without deleting it?","Use a `removed` block with `lifecycle { destroy = false }`, or `terraform state rm`. The object stays in the real world but leaves state."],["Which environment variables control logging?","`TF_LOG` sets the level (TRACE, DEBUG, INFO, WARN, ERROR or JSON), `TF_LOG_PATH` writes to a file, and `TF_LOG_CORE`/`TF_LOG_PROVIDER` set levels for Terraform core or providers separately."]],"8":[["Name the three HCP Terraform run workflows.","VCS-driven (runs start from commits and pull requests), CLI-driven (you run plan/apply locally and they execute remotely) and API-driven (automation calls the API)."],["How do projects and workspaces relate?","A workspace holds one state, its variables, runs and settings. Projects group workspaces so teams can be given access to all of them at once and organize them by app or team."],["What are variable sets used for?","Sharing the same variables (such as cloud credentials) across many workspaces or whole projects instead of repeating them in each workspace."],["Which governance features does HCP Terraform add?","Team permissions, run approvals, policy as code with Sentinel or OPA, a private registry for modules and providers, audit trails, and health assessments that find drift."],["How do you connect a local configuration to HCP Terraform?","Run `terraform login` to store an API token, add a `cloud` block with the organization and workspace, then run `terraform init`."]]},"qCount":90});
