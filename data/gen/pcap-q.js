/* Generated by tools/build.js from data/pcap.js. Edit that file, not this one. */
CertHub.addQuestions("pcap", [["pc1",0,1,"What is the output of the following code?\nimport math\nprint(math.ceil(-2.5), math.floor(-2.5), math.trunc(-2.5))",["-3 -2 -2","-3 -3 -2","-2 -3 -3","-2 -3 -2"],3,"ceil rounds toward positive infinity (-2), floor toward negative infinity (-3), and trunc toward zero (-2). Mixing up trunc and floor for negative numbers is the usual trap.","1.2 math module",["This swaps ceil and floor: ceil(-2.5) is -2 (rounding toward +infinity), not -3, and floor is -3, not -2.","It gets ceil wrong: ceil rounds toward positive infinity, so ceil(-2.5) is -2, not -3.","ceil and floor are right here, but trunc(-2.5) drops the fraction toward zero, giving -2, not -3.",null],null],["pc2",0,1,"A script contains `from math import sqrt as root` and no other imports. Which name can the script use to call the square-root function?",["math.sqrt","sqrt","root","math.root"],2,"The as clause binds only the alias root. Neither math nor sqrt is bound, so math.sqrt and sqrt raise NameError.","1.1 import variants",["The module name math is never bound by a `from math import ...` statement, so math.sqrt raises NameError.","The original name sqrt is not bound because the `as root` alias replaces it; only root is available.",null,"Neither math nor an attribute named root on it exists, so math.root raises NameError."],null],["pc3",0,1,"A team wants a folder of modules to behave as a regular package so that `import shop.cart` works on every Python 3 version they support. Which file should the shop folder contain?",["__init__.py","__main__.py","setup.py","__pycache__"],0,"__init__.py marks a directory as a regular package and runs when it is imported. __main__.py is used when running a package with -m, and __pycache__ holds compiled bytecode.","1.4 packages",[null,"__main__.py is executed when a package is run with `python -m package`; it does not mark a directory as an importable package.","setup.py is a packaging/build script for distributing a project, not a file Python looks for to treat a folder as a package.","__pycache__ is an auto-generated folder holding compiled .pyc bytecode, not something you add to define a package."],null],["pc4",0,1,"The file greet.py contains only `print(__name__)`. Another script in the same folder runs `import greet`. What does the import print?",["greet","__main__","greet.py","__greet__"],0,"When a module is imported, __name__ is the module name without the .py extension. It is \"__main__\" only when the file itself is run.","1.4 __name__",[null,"__name__ equals '__main__' only for the file run directly, not for a module that is imported.","__name__ never includes the .py extension; it is just the bare module name.","Imported modules use their plain file name; Python does not wrap module names in dunder underscores like this."],null],["pc5",0,1,"A developer keeps shared modules in /opt/pylibs and wants one script to import them without changing environment variables. Which statement, run before the import, achieves this?",["sys.path.append('/opt/pylibs')","os.chdir('/opt/pylibs/..')","sys.modules.add('/opt/pylibs')","import path('/opt/pylibs')"],0,"sys.path is the list of directories searched for modules, so appending to it works at runtime. sys.modules is a dict of loaded modules, not search locations.","1.4 sys.path",[null,"os.chdir changes the current working directory, which does not affect where Python searches for importable modules.","sys.modules is a dict of already-loaded modules and has no add method; it does not control search locations.","This is not valid Python; there is no such import form or `path(...)` call."],null],["pc6",0,1,"What is the output of the following code?\nimport random\nrandom.seed(5)\na = random.random()\nrandom.seed(5)\nb = random.random()\nprint(a == b)",["False","None","True","0.5"],2,"Seeding the generator with the same value restarts the same pseudo-random sequence, so both calls return the same float. Without reseeding, the values would differ.","1.2 random module",["Seeding with the same value replays the identical sequence, so the two floats are equal and the result is True.","random.random() returns a float and the == comparison yields a bool, so the printed value is never None.",null,"The code prints the boolean result of a == b, not the underlying random float value."],null],["pc7",0,1,"Which function returns the Python version as a tuple of strings, such as ('3', '12', '4')?",["platform.python_version()","platform.version()","sys.version_info.tuple()","platform.python_version_tuple()"],3,"python_version_tuple() returns a tuple of strings. python_version() returns a single string, and platform.version() describes the operating system version.","1.2 platform module",["python_version() returns the version as a single string like '3.12.4', not a tuple of strings.","platform.version() returns the operating system's version string, unrelated to the Python version.","version_info has no tuple() method; it is a named tuple of integers, not strings.",null],null],["pc8",0,1,"After importing a module for the first time, a developer notices a new __pycache__ folder beside it. What does the folder contain?",["Compiled bytecode (.pyc) files","A backup of the source file","Cached results of function calls","Log files of each import"],0,"Python saves compiled bytecode in __pycache__ so later imports can skip compiling unchanged source. It does not cache function results or logs.","1.4 __pycache__",[null,"__pycache__ does not store copies of your source file; it holds compiled bytecode only.","Bytecode caching is unrelated to memoizing function return values.","No import logs are written there; the folder only contains .pyc files."],null],["pc9",0,1,"What is the output of the following code?\nimport math\nprint(math.hypot(3, 4))",["5","25.0","7.0","5.0"],3,"hypot returns the Euclidean distance sqrt(3*3 + 4*4) as a float, 5.0. It never returns an int, and it does not return the sum of squares.","1.2 math module",["hypot returns a float, so the result prints as 5.0, not the integer 5.","25 is 3*3 + 4*4 without the square root; hypot takes the square root of that sum of squares.","7 is 3 + 4, but hypot computes the Euclidean distance, not the sum of the two legs.",null],null],["pc10",0,1,"Module cfg.py defines `_secret = 1` and `level = 2` and has no __all__. A script runs `from cfg import *`. Which names does the script now have?",["_secret only","cfg, level and _secret","level and _secret","level only"],3,"from … import * skips names that start with an underscore when __all__ is not defined, and it never binds the module name cfg itself.","1.1 import variants",["Names beginning with an underscore are exactly what `import *` skips, so _secret is not imported.","The module name cfg is never bound by `import *`, and _secret is skipped because of its leading underscore.","_secret starts with an underscore, so it is excluded when the module has no __all__.",null],null],["pc11",0,1,"What is the output of the following code?\nimport math\nprint(math.factorial(5) // math.sqrt(16))",["30.0","30","30.5","7.5"],0,"factorial(5) is 120 and sqrt always returns a float (4.0). Floor division with a float operand gives a float, so the result is 30.0, not the int 30.","1.2 math module",[null,"sqrt returns a float, so 120 // 4.0 is the float 30.0, not the integer 30.","Floor division discards the remainder, so no fractional .5 can appear in the result.","This is not a division of 120 by 16; factorial(5) is 120 and 120 // 4.0 is 30.0, not 7.5."],null],["pc12",0,2,"What is the output of the following code?\ntry:\n    x = int('3.5')\nexcept ValueError:\n    print('V', end='')\nexcept Exception:\n    print('E', end='')\nelse:\n    print('OK', end='')\nfinally:\n    print('F', end='')",["EF","OKF","V","VF"],3,"int('3.5') raises ValueError, so the first matching branch prints V; else is skipped because an exception occurred, and finally always prints F.","2.1 try/except/else/finally",["int('3.5') raises ValueError, which the first branch catches and prints V; the Exception branch that would print E is never reached.","else runs only when no exception occurs, but an exception did occur, so 'OK' is not printed.","This omits the finally branch, which always runs and prints F.",null],null],["pc13",0,2,"What is the output of the following code?\ntry:\n    print(1 / 0)\nexcept (ZeroDivisionError, TypeError) as e:\n    print(type(e).__name__)",["ArithmeticError","Exception","TypeError","ZeroDivisionError"],3,"The tuple branch matches either class, and e is bound to the actual exception object, a ZeroDivisionError. Its parent ArithmeticError is not what type(e) reports.","2.1 handling multiple exceptions",["ArithmeticError is the parent class; type(e).__name__ reports the actual class, which is ZeroDivisionError.","Exception is a distant ancestor, not the exact type of the raised object, which is ZeroDivisionError.","No TypeError occurs; 1/0 raises ZeroDivisionError, which is the tuple member that matches.",null],null],["pc14",0,2,"Which built-in exception is the direct parent class of ZeroDivisionError?",["ValueError","LookupError","ArithmeticError","RuntimeError"],2,"ZeroDivisionError, OverflowError and FloatingPointError derive from ArithmeticError. LookupError is the parent of IndexError and KeyError.","2.1 exception hierarchy",["ValueError is unrelated to arithmetic and is not in ZeroDivisionError's ancestry.","LookupError is the parent of IndexError and KeyError, not of ZeroDivisionError.",null,"RuntimeError is a separate branch of the hierarchy and is not a parent of ZeroDivisionError."],null],["pc15",0,2,"A function reads both list positions and dictionary keys supplied by a user. The developer wants a single except branch that catches IndexError and KeyError but not other errors. Which class should the branch name?",["Exception","AttributeError","ValueError","LookupError"],3,"IndexError and KeyError both derive from LookupError. Exception would also catch unrelated errors such as TypeError, which the developer wants to let through.","2.1 exception hierarchy",["Catching Exception would also trap unrelated errors such as TypeError, which the developer wants to let through.","AttributeError is not a parent of IndexError or KeyError, so it would catch neither.","ValueError is unrelated to indexing or key lookups and would not catch either error.",null],null],["pc16",0,2,"What is the output of the following code?\ntry:\n    d = {}\n    d['k']\nexcept LookupError:\n    print('L')\nexcept KeyError:\n    print('K')",["K","LK","L","Nothing is printed"],2,"KeyError is a subclass of LookupError, and Python takes the first matching branch, so L is printed and the KeyError branch is never reached.","2.1 order of except branches",["KeyError is a subclass of LookupError, and the LookupError branch comes first, so L prints, not K.","Only the first matching branch runs; once LookupError matches, the KeyError branch is skipped.",null,"The missing key does raise KeyError, which matches the LookupError branch, so something is printed."],null],["pc17",0,2,"What is the output of the following code?\nx = -1\ntry:\n    assert x > 0, 'neg'\nexcept AssertionError as e:\n    print(e.args)",["('neg',)","neg","()","['neg']"],0,"The assert message becomes the single argument of AssertionError, and args is always a tuple, so the output is ('neg',). Printing e itself would show neg.","2.1 assert and args",[null,"Printing e.args shows the tuple, not the bare string; str(e) would give 'neg'.","The assert has a message, so args holds one item rather than being empty.","args is a tuple, not a list, so it prints with parentheses, not square brackets."],null],["pc18",0,2,"A library author is writing a custom exception for invalid configuration. Which base class is the best choice?",["Exception","BaseException","object","SystemExit"],0,"User-defined exceptions should inherit from Exception so that `except Exception` handlers catch them. BaseException is reserved for system-exiting exceptions such as KeyboardInterrupt, and a plain object cannot be raised.","2.2 self-defined exceptions",[null,"Inheriting from BaseException means `except Exception` handlers miss it; BaseException is reserved for system-exiting exceptions.","A plain object is not raisable as an exception, so this cannot be raised.","SystemExit is meant to terminate the interpreter, not to signal a configuration error."],null],["pc19",0,2,"What is the output of the following code?\nclass AppError(Exception):\n    def __init__(self, msg, code):\n        super().__init__(msg)\n        self.code = code\ntry:\n    raise AppError('bad', 42)\nexcept AppError as e:\n    print(e, e.code)",["('bad', 42) 42","AppError 42","bad 42","bad None"],2,"Only msg is passed to Exception.__init__, so str(e) is 'bad', and code is stored as an attribute set to 42.","2.2 self-defined exceptions",["Only msg is passed to super().__init__, so str(e) is 'bad', not a two-item tuple.","Printing the exception shows its message 'bad', not the class name.",null,"code is explicitly set to 42 in __init__, so e.code is 42, not None."],null],["pc20",0,2,"Inside an except branch, a developer logs the error and then wants the same exception to keep propagating to the caller with its original traceback. What should the next statement be?",["return e","raise Exception","pass","raise"],3,"A bare raise inside an except branch re-raises the exception currently being handled. raise Exception would replace it with a new, less specific exception.","2.1 raise",["Returning the exception object does not propagate it; the caller just receives a value.","raise Exception raises a new, generic exception and discards the original type and traceback.","pass silently swallows the exception, so nothing propagates to the caller.",null],null],["pc21",0,2,"What is the output of the following code?\ndef f():\n    try:\n        return 1\n    finally:\n        print('F', end=' ')\nprint(f())",["1","1 F","F","F 1"],3,"finally runs before the function actually returns, so F is printed first; then the returned value 1 is printed by the outer print call.","2.1 finally",["The finally block prints F before the function returns, so F is printed as well as 1.","finally runs before the return value reaches the caller, so F is printed before 1, not after.","The returned value 1 is still printed by the outer print, so 1 also appears.",null],null],["pc22",0,2,"A long-running loop is wrapped in `try: … except Exception: print('error')`. The user presses Ctrl+C. What happens?",["The branch prints error and the loop continues","The handler ignores it but the loop restarts","KeyboardInterrupt is not caught and ends the program","Python converts it into a ValueError first"],2,"KeyboardInterrupt derives from BaseException, not Exception, so `except Exception` does not catch it and the program stops. A bare except: would catch it, which is why bare excepts are discouraged.","2.1 exception hierarchy",["KeyboardInterrupt derives from BaseException, not Exception, so `except Exception` does not catch it and the loop cannot continue.","The handler never runs for KeyboardInterrupt, so the loop does not restart; the program stops instead.",null,"Python does not transform KeyboardInterrupt into any other exception type."],null],["pc23",0,2,"In a try statement with except and else branches, when does the else branch run?",["Only when the try block raised no exception","Only when an exception was handled","Always, after the except branch","Only when an exception was not handled"],0,"else runs only after the try block completes without raising. Code that must always run belongs in finally instead.","2.1 else branch",[null,"else runs precisely when no exception was raised, which is the opposite of a handled exception.","else is skipped whenever an except branch runs; use finally for code that must always run.","An unhandled exception propagates out of the try statement and skips else entirely."],null],["pc24",0,2,"What is the output of the following code?\ntry:\n    raise ValueError\nexcept ValueError as e:\n    print(len(e.args))",["1","None","0","An error is raised"],2,"Raising the class without arguments creates an instance with an empty args tuple, so len is 0. Passing a message would make args hold one item.","2.1 args",["Raising ValueError with no argument gives an empty args tuple, so its length is 0, not 1.","args is always a tuple, so len() returns an integer, never None.",null,"Raising the exception class without arguments is valid and does not itself error here."],null],["pc25",0,3,"What is the output of the following code?\nprint(ord('a') - ord('A'))",["26","-32","1","32"],3,"In ASCII and Unicode, 'A' is 65 and 'a' is 97, so the difference is 32. 26 is the number of letters, a tempting but wrong guess.","3.1 ord()",["26 is the number of letters in the alphabet, not the code-point difference, which is 32.","The order is ord('a') - ord('A') = 97 - 65 = 32, a positive value, not -32.","'a' and 'A' differ by 32 in code points, not by 1.",null],null],["pc26",0,3,"What is the output of the following code?\nprint(chr(ord('C') + 2))",["E","D","67","C2"],0,"ord('C') is 67; adding 2 gives 69, and chr(69) is 'E'. chr returns a character, not a number.","3.1 chr()",[null,"Adding 2 to ord('C')=67 gives 69, which is 'E'; 'D' would be only +1.","chr() converts the number back to a character, so the result is a letter, not the integer 67.","The expression does arithmetic on code points, not string concatenation, so 'C2' is not produced."],null],["pc27",0,3,"What is the output of the following code?\ns = 'Python'\nprint(s[-4:-1])",["thon","ytho","tho","hon"],2,"Index -4 is 't' and the slice stops before -1 ('n'), giving 'tho'. The stop index is always excluded.","3.2 slicing",["The slice stops before index -1, excluding the final 'n', so the result is 'tho', not 'thon'.","Index -4 is 't', not 'y'; 'ytho' would start one position earlier.",null,"This reads the last three characters, but s[-4:-1] starts at -4 ('t') and stops before -1."],null],["pc28",0,3,"A script runs `s = 'java'` and then `s[0] = 'J'`. What happens?",["s becomes 'Java'","An IndexError is raised","A new string is bound to s[0]","A TypeError is raised"],3,"Strings are immutable and do not support item assignment, so Python raises TypeError. To change it, build a new string such as 'J' + s[1:].","3.2 immutability",["Strings are immutable, so item assignment cannot modify them in place; s does not become 'Java'.","Index 0 is valid; the failure is about immutability, which raises TypeError, not IndexError.","You cannot assign to an index of a string at all, so no new binding to s[0] occurs.",null],null],["pc29",0,3,"What is the output of the following code?\nprint('10' < '9', 10 < 9)",["True False","False False","False True","True True"],0,"Strings compare character by character: '1' has a lower code point than '9', so '10' < '9' is True. The integers compare numerically, so 10 < 9 is False.","3.2 comparing strings",[null,"'10' < '9' compares '1' (49) to '9' (57), so it is True, not False.","String comparison makes the first result True, and 10 < 9 is False, so both values are reversed here.","The integer comparison 10 < 9 is False, so the second value cannot be True."],null],["pc30",0,3,"What is the output of the following code?\nprint('-'.join(['a', 'b', 'c']))",["-a-b-c","a-b-c-","a-b-c","abc-"],2,"join places the separator only between items, never before the first or after the last.","3.3 join()",["join puts the separator only between items, never before the first one, so no leading '-' appears.","join adds no trailing separator after the last item.",null,"The separator appears between each pair of items, not once at the end."],null],["pc31",0,3,"What is the output of the following code?\nprint('a,b,,c'.split(','))",["['a', 'b', 'c']","['a,b,,c']","['a', 'b', ',', 'c']","['a', 'b', '', 'c']"],3,"With an explicit separator, split keeps empty strings between adjacent separators. Only split() with no argument collapses runs of whitespace.","3.3 split()",["With an explicit separator, the empty field between the two commas is preserved, not dropped.","split divides the string on each comma, so it returns multiple items, not the whole string unchanged.","The separator characters are removed, so a lone ',' element never appears in the result.",null],null],["pc32",0,3,"What is the output of the following code?\nprint(' a  b '.split())",["['a', 'b']","['', 'a', '', 'b', '']","[' a', ' b ']","['a', '', 'b']"],0,"split() without arguments splits on runs of whitespace and ignores leading and trailing whitespace, so no empty strings appear.","3.3 split()",[null,"split() with no argument discards leading and trailing whitespace and collapses runs, so no empty strings appear.","Argument-less split strips surrounding spaces from each token rather than keeping them.","Consecutive spaces are treated as one separator, so no empty element sits between the words."],null],["pc33",0,3,"What is the output of the following code?\ns = 'banana'\nprint(s.find('na'), s.rfind('na'), s.find('x'))",["2 4 -1","2 4 None","3 5 -1","2 2 -1"],0,"find returns the lowest index (2), rfind the highest (4), and find returns -1 when the substring is missing instead of raising an error.","3.3 find() and rfind()",[null,"find returns -1 (not None) when the substring is absent.","The first 'na' begins at index 2, not 3, and the last at 4, not 5.","rfind searches from the right and finds the later 'na' at index 4, not 2."],null],["pc34",0,3,"A parser calls `line.index(':')` on user input that sometimes has no colon, and the program crashes. Which change avoids the crash while still showing when the colon is missing?",["Use line.find(':') and check for -1","Use line.rindex(':') instead","Use line.count(':') as the position","Wrap the string with str() first"],0,"find returns -1 instead of raising ValueError. rindex behaves like index and still raises, and count returns how many times the colon appears, not where.","3.3 find() vs index()",[null,"rindex, like index, raises ValueError when the colon is missing, so it does not avoid the crash.","count returns how many colons exist, not their position, so it cannot locate the colon.","The value is already a string, so wrapping it with str() changes nothing about the missing colon."],null],["pc35",0,3,"What is the output of the following code?\nprint(sorted('bca'))",["abc","None","('a', 'b', 'c')","['a', 'b', 'c']"],3,"sorted accepts any iterable and always returns a new list, so it returns a list of characters, not a string. Use ''.join(sorted(s)) to get 'abc'.","3.3 sorted()",["sorted returns a list of characters, not a joined string; use ''.join(sorted(s)) to get 'abc'.","sorted returns a new list; it is list.sort() that returns None.","sorted returns a list, not a tuple, so parentheses are wrong here.",null],null],["pc36",0,3,"What is the output of the following code?\nprint('3'.isdigit(), 'a1'.isalnum(), 'Ab'.isupper())",["True False False","True True True","False True False","True True False"],3,"'3' is a digit, 'a1' contains only letters and digits, and 'Ab' has a lowercase letter, so isupper() is False.","3.3 isxxx() methods",["'a1'.isalnum() is True because it contains only letters and digits, so the middle value is True, not False.","'Ab'.isupper() is False because it contains a lowercase letter, so all three cannot be True.","'3'.isdigit() is True, so the first value is True, not False.",null],null],["pc37",0,3,"What is the output of the following code?\nprint(len('\\n\\t\\\\'))",["3","4","5","6"],0,"Each escape sequence is a single character: newline, tab and one backslash, so the length is 3.","3.1 escape sequences",[null,"Each escape (\\n, \\t, \\\\) is a single character, so the length is 3, not 4.","The backslash escapes each represent one character, so the length is 3, not 5.","Counting each backslash-letter pair as two characters is the mistake; they are single characters."],null],["pc38",0,3,"Which statement about Unicode and UTF-8 is correct?",["Every UTF-8 character takes exactly two bytes","UTF-8 can only encode the first 256 code points","A code point is a number and UTF-8 stores it in 1 to 4 bytes","ASCII and UTF-8 give different bytes for 'A'"],2,"Unicode assigns each character a code point, and UTF-8 is a variable-width encoding using 1 to 4 bytes. ASCII characters, such as 'A', are encoded identically in both.","3.1 encoding",["UTF-8 is variable-width, using 1 to 4 bytes, so characters are not all exactly two bytes.","UTF-8 can encode the entire Unicode range, far beyond the first 256 code points.",null,"UTF-8 encodes ASCII characters identically to ASCII, so 'A' is the same byte in both."],null],["pc39",0,3,"What is the output of the following code?\nprint('ell' in 'Hello', 'H' not in 'hello')",["True False","False True","True True","False False"],2,"'ell' is a substring of 'Hello'. The comparison is case-sensitive, so uppercase 'H' is not in 'hello', which makes `not in` True.","3.2 in and not in",["'H' not in 'hello' is True because the check is case-sensitive, so the second value is True, not False.","'ell' is a substring of 'Hello', so the first value is True, not False.",null,"Both parts are actually True, so neither value is False."],null],["pc40",0,3,"Which expression raises a TypeError?",["'1' * 1","'1' == 1","'1' != 1","'1' < 1"],3,"Equality tests between a str and an int simply return False or True, and str * int repeats the string. Ordering comparisons such as < between str and int are not supported and raise TypeError.","3.2 comparing strings with numbers",["Multiplying a string by an int is valid repetition and returns '1', so it raises no error.","Equality between a str and an int simply returns False without raising.","Inequality across types returns True and does not raise.",null],null],["pc41",0,4,"What is the output of the following code?\nclass A:\n    count = 0\n    def __init__(self):\n        A.count += 1\na = A()\nb = A()\nprint(A.count, a.count)",["2 2","2 0","1 1","2 1"],0,"count is a class variable shared by every instance; each constructor call increments A.count, and a.count finds the class variable because the instance has none of its own.","4.2 class variables",[null,"a.count reads the shared class variable through the instance, so it is 2, not 0.","Each constructor call increments A.count and two instances were created, so it is 2, not 1.","a has no instance variable count, so a.count falls back to the class value 2, not 1."],null],["pc42",0,4,"What is the output of the following code?\nclass A:\n    x = 1\na = A()\na.x = 5\nprint(A.x, a.x)",["5 5","1 1","1 5","5 1"],2,"Assigning a.x creates an instance variable that shadows the class variable; A.x is unchanged at 1.","4.2 instance vs class variables",["Assigning a.x creates an instance variable and leaves the class variable A.x unchanged at 1, not 5.","a.x was set to 5, so the instance value is 5, not 1.",null,"The values are reversed; A.x stays 1 and a.x becomes 5."],null],["pc43",0,4,"What is the output of the following code?\nclass A:\n    def __init__(self):\n        self.v = 1\na = A()\nprint(a.__dict__)",["{}","{'__init__': 1}","{'self.v': 1}","{'v': 1}"],3,"An instance's __dict__ holds only its instance variables. Methods such as __init__ live in the class's __dict__.","4.3 __dict__",["__init__ set self.v, so the instance dict is not empty.","Methods such as __init__ live in the class __dict__, not the instance __dict__, and would not map to 1.","The stored key is just the attribute name 'v'; the 'self.' prefix is not part of the key.",null],null],["pc44",0,4,"What is the output of the following code?\nclass A:\n    def __init__(self):\n        self.__s = 7\na = A()\nprint(a._A__s)",["An AttributeError is raised","None","7","__s"],2,"Name mangling stores self.__s as _A__s, so it can still be reached with the mangled name. Accessing a.__s from outside the class would raise AttributeError.","4.3 private attributes",["The mangled name _A__s does exist, so accessing a._A__s succeeds rather than raising.","The attribute was set to 7, so its value is 7, not None.",null,"Printing a._A__s prints its value 7, not the literal string '__s'."],null],["pc45",0,4,"What is the output of the following code?\nclass A:\n    def __init__(self):\n        self.__p = 1\nprint(hasattr(A(), '__p'))",["True","1","None","False"],3,"Because of name mangling the attribute is stored as _A__p, so no attribute called __p exists and hasattr returns False.","4.5 introspection",["Name mangling stores the attribute as _A__p, so no attribute literally named __p exists and hasattr is False.","hasattr returns a boolean, not the attribute's value.","hasattr always returns True or False, never None.",null],null],["pc46",0,4,"What is the output of the following code?\nclass A:\n    pass\nclass B(A):\n    pass\nprint(B.__bases__[0].__name__)",["A","B","object","__main__"],0,"__bases__ is a tuple of direct superclasses, here (A,), and __name__ gives the class name. object is A's base, not B's.","4.5 __bases__",[null,"__bases__ lists B's parents, not B itself, so the first base is A.","object is A's base, but B.__bases__[0] is A, whose name is 'A'.","__main__ is a module name, not the name of B's base class."],null],["pc47",0,4,"What is the output of the following code?\nclass A:\n    def who(self):\n        return 'A'\nclass B(A):\n    def who(self):\n        return 'B'\nclass C(A):\n    def who(self):\n        return 'C'\nclass D(B, C):\n    pass\nprint(D().who())",["A","D","C","B"],3,"D's MRO is D, B, C, A, object; B is searched first because it is listed first, so its who() is used.","4.6 multiple inheritance",["D's MRO reaches B before A, and B defines who(), so A's version is never used.","D does not define who(), so it inherits one; the MRO selects B's implementation.","B is listed before C in D(B, C), so B's who() wins over C's.",null],null],["pc48",0,4,"Classes are defined as A, B(A), C(A) and D(B, C). What is the output of `print([k.__name__ for k in D.__mro__])`?",["['D', 'B', 'A', 'C', 'object']","['D', 'C', 'B', 'A', 'object']","['D', 'B', 'C', 'object', 'A']","['D', 'B', 'C', 'A', 'object']"],3,"C3 linearization visits the shared base A only after both B and C, giving D, B, C, A, object. A depth-first order that visits A before C is the classic wrong guess.","4.6 MRO and diamonds",["C3 never visits the shared base A before C, so A cannot appear before C in the MRO.","B is listed first in D(B, C), so B precedes C in the MRO.","object is always last, after A, not before it.",null],null],["pc49",0,4,"What is the output of the following code?\nclass A:\n    def __init__(self):\n        self.x = 1\nclass B(A):\n    def __init__(self):\n        super().__init__()\n        self.x += 1\nprint(B().x)",["1","2","3","An AttributeError is raised"],1,"super().__init__() runs A's constructor, setting x to 1, and B then adds 1, so x is 2.","4.6 super()",["super().__init__() sets x to 1 and then B adds 1, so the final value is 2, not 1.",null,"Only one increment happens after the parent sets x to 1, giving 2, not 3.","super().__init__() creates x, so accessing it does not raise an AttributeError."],null],["pc50",0,4,"What is the output of the following code?\nclass A:\n    def __init__(self):\n        self.x = 1\nclass B(A):\n    def __init__(self):\n        self.y = 2\nb = B()\nprint(hasattr(b, 'x'))",["True","False","1","None"],1,"B overrides __init__ and never calls super().__init__(), so A's constructor never runs and x is never set.","4.6 overriding constructors",["B's __init__ never calls super().__init__(), so A's constructor does not run and x is never set.",null,"hasattr returns a boolean, not the value 1.","hasattr returns True or False, never None."],null],["pc51",0,4,"What is the output of the following code?\nclass P:\n    def __str__(self):\n        return 'P!'\nprint(P())",["P","P!","<__main__.P object>","None"],1,"print calls str() on its argument, which uses the class's __str__ method. Without __str__, a default representation such as <__main__.P object at 0x…> would appear.","4.6 __str__()",["__str__ returns 'P!', including the exclamation mark, so 'P' alone is incomplete.",null,"Because __str__ is defined, print uses it instead of the default object representation.","print outputs the string returned by __str__; it does not print None."],null],["pc52",0,4,"What is the output of the following code?\nclass A:\n    pass\nclass B(A):\n    pass\nb = B()\nprint(isinstance(b, A), issubclass(A, B))",["True True","False False","False True","True False"],3,"An instance of a subclass is also an instance of its superclass, so isinstance is True. A is the parent of B, not a subclass of it, so issubclass(A, B) is False.","4.6 isinstance() and issubclass()",["issubclass(A, B) is False because A is the parent of B, not its subclass.","isinstance(b, A) is True since b is a B, which is a subclass of A.","Both values are reversed; isinstance is True and issubclass(A, B) is False.",null],null],["pc53",0,4,"What is the output of the following code?\na = [1]\nb = a\nc = [1]\nprint(a is b, a is c, a == c)",["True True True","True False True","True False False","False False True"],1,"b refers to the same list object as a, so `a is b` is True. c is a separate list with equal contents, so `is` is False but == is True.","4.6 is operator",["a and c are separate list objects, so `a is c` is False, not True.",null,"a and c have equal contents, so a == c is True, not False.","b is bound to the same object as a, so `a is b` is True, not False."],null],["pc54",0,4,"What is the output of the following code?\nclass Shape:\n    def area(self):\n        return 0\nclass Sq(Shape):\n    def __init__(self, s):\n        self.s = s\n    def area(self):\n        return self.s ** 2\nfor sh in [Shape(), Sq(3)]:\n    print(sh.area(), end=' ')",["0 0","0 9","9 9","0 6"],1,"Polymorphism: each object uses its own class's area(). Shape returns 0, and Sq overrides it to return 3 ** 2, which is 9.","4.6 polymorphism",["Sq overrides area() to return s**2, so the second value is 9, not 0.",null,"The base Shape instance uses its own area(), which returns 0, so the first value is 0, not 9.","Sq(3).area() is 3**2 = 9, not 3*2 = 6."],null],["pc55",0,4,"A new developer asks why every method in a class has `self` as its first parameter. Which explanation is correct?",["self is a reserved keyword that Python adds itself","self receives the object the method was called on","self holds the class, so class variables can be changed","self is optional and only needed for private methods"],1,"When you call obj.m(), Python passes obj as the first argument, conventionally named self. It is a convention, not a keyword, and it refers to the instance, not the class.","4.4 methods and self",["self is only a naming convention, not a reserved keyword, and Python does not add it automatically.",null,"self refers to the instance, not the class; the class is reached via type(self) or the class name.","Every instance method needs a first parameter to receive the instance, regardless of visibility."],null],["pc56",0,4,"A BankAccount class must stop outside code from accidentally overwriting its balance with `acct.balance = 0`, while methods inside the class still use it. Which design follows PCAP-style encapsulation?",["Store it as a class variable called balance","Store it as self.__balance and expose methods","Store it as a global variable in the module","Store it in a local variable inside __init__"],1,"A double-underscore instance attribute is name-mangled, so outside code cannot reach it by its plain name, and deposit/withdraw methods control changes. A local variable in __init__ disappears when the constructor ends.","4.1 encapsulation",["A class variable would be shared by all accounts and still openly writable, so it does not protect a per-object balance.",null,"A module global is shared program-wide and freely writable, offering no encapsulation for an account.","A local variable in __init__ vanishes when the constructor returns, so the balance would not persist on the object."],null],["pc57",0,4,"What is the output of the following code when it is run directly as a script?\nclass A:\n    pass\nprint(A.__module__)",["A","__main__","main","None"],1,"__module__ holds the name of the module where the class was defined; for the script being run, that module is __main__.","4.5 __module__",["__module__ holds the module name, not the class name 'A'.",null,"The run script's module is named '__main__' with double underscores, not 'main'.","__module__ is always a string naming the defining module, never None."],null],["pc58",0,4,"What is the output of the following code?\nclass A:\n    x = 1\nprint('x' in A().__dict__, 'x' in A.__dict__)",["True True","False True","True False","False False"],1,"x is a class variable, so it appears in the class's __dict__. A new instance has no instance variables yet, so its __dict__ is empty.","4.3 __dict__",["A fresh instance has no instance variables, so 'x' is not in its __dict__; the first value is False.",null,"x is a class variable, so it is in the class __dict__ (True), but the instance __dict__ is empty.","x does appear in the class __dict__, so the second value is True, not False."],null],["pc59",0,4,"A class defines `def f():` with no parameters, and a script calls `A().f()`. Which exception is raised?",["AttributeError","NameError","TypeError","ValueError"],2,"Python passes the instance as the first argument, but f accepts none, so the call fails with TypeError (takes 0 positional arguments but 1 was given). The method exists, so it is not an AttributeError.","4.4 methods and self",["The method f exists on the instance, so it is found; the error is about arguments, not a missing attribute.","f is defined and reachable, so no name lookup fails.",null,"The mismatch is in the number of positional arguments, which raises TypeError, not ValueError."],null],["pc60",0,4,"What is the output of the following code?\nclass A:\n    x = 'A'\nclass B:\n    x = 'B'\nclass C(A, B):\n    pass\nprint(C.x)",["A","B","AB","An error is raised"],0,"Attribute lookup follows the MRO C, A, B, object, so A.x is found first. Class variables use the same lookup order as methods.","4.6 multiple inheritance",[null,"The MRO is C, A, B, so A.x is found first and B.x is never reached.","Attribute lookup returns a single value from the first class that defines it, not a concatenation.","C(A, B) has a valid MRO, so the lookup succeeds and returns A.x rather than raising."],null],["pc61",0,4,"A developer writes `class A: pass`, `class B(A): pass` and then `class C(A, B): pass`. What happens when the last class statement runs?",["C is created with MRO C, A, B, object","C is created with MRO C, B, A, object","A TypeError is raised: no consistent MRO","C is created but calls to its methods fail"],2,"Listing A before its own subclass B makes C3 linearization impossible, so Python raises TypeError when the class is defined. Swapping the order to C(B, A) works.","4.6 MRO",["Listing A before its subclass B makes this order impossible, so no such MRO can be built.","C3 cannot produce any consistent order here, so the class is not created at all.",null,"The failure happens at class-definition time with a TypeError, so C is never created."],null],["pc62",0,4,"What is the output of the following code?\nclass A:\n    def hi(self):\n        return 'A'\nclass B(A):\n    def hi(self):\n        return 'B' + super().hi()\nprint(B().hi())",["B","A","AB","BA"],3,"B's override builds its result from 'B' followed by the parent's result 'A', giving BA.","4.6 overriding and super()",["The method concatenates 'B' with super().hi(), which returns 'A', so the result includes A too.","B overrides hi() to prepend 'B' before calling the parent, so 'B' comes first.","The order is 'B' then the parent's 'A', giving 'BA', not 'AB'.",null],null],["pc63",0,4,"What is the output of the following code?\nclass A:\n    items = []\n    def add(self, v):\n        self.items.append(v)\na, b = A(), A()\na.add(1)\nprint(b.items)",["[]","[1]","None","An AttributeError is raised"],1,"items is a mutable class variable; self.items.append mutates the single shared list rather than creating an instance copy, so b sees the change.","4.2 class variables",["items is a shared class variable, so appending through a mutates the one list that b also sees, giving [1].",null,"b.items refers to the shared list, which now contains [1], not None.","items exists as a class variable, so accessing b.items succeeds without an AttributeError."],null],["pc64",0,4,"A Car needs an Engine. The developer writes `class Car(Engine)`, and a reviewer objects. What is the reviewer's likely point?",["Inheritance cannot be used with constructors","A car has an engine, so composition fits better","Car must inherit from object explicitly first","Only one level of inheritance is allowed"],1,"Inheritance models an is-a relationship. A car has an engine, so storing an Engine object as an attribute (composition) models it correctly. Python allows many levels of inheritance.","4.1 OOP concepts",["Inheritance works fine with constructors; that is not the reviewer's concern.",null,"Classes implicitly inherit from object already, so this is not the issue.","Python supports arbitrarily deep inheritance, so depth is not the objection."],null],["pc65",0,4,"What is the output of the following code?\nclass Dog:\n    pass\nd = Dog()\nprint(type(d).__name__, d.__class__ is Dog)",["Dog True","d True","Dog False","object True"],0,"type(d) and d.__class__ both return the class Dog, whose __name__ is 'Dog'. The variable name d is not stored in the object.","4.5 introspection",[null,"type(d).__name__ is the class name 'Dog'; the variable name d is not stored on the object.","d.__class__ is Dog, so the identity check is True, not False.","type(d) is Dog, not object, so the name is 'Dog'."],null],["pc66",0,4,"What is the output of the following code?\nclass A:\n    def __init__(self, v=0):\n        self.v = v\nprint(A().v + A(5).v)",["0","5","10","An error is raised"],1,"The first object uses the default v=0 and the second receives 5, so the sum is 5.","4.4 constructors",["A() uses the default 0 but A(5) supplies 5, so the sum is 5, not 0.",null,"Only one object receives 5; the other uses the default 0, so the total is 5, not 10.","The default value makes both calls valid, so no error occurs."],null],["pc67",0,4,"What is the output of the following code?\nclass A:\n    def set(self):\n        self.z = 3\na = A()\nprint(hasattr(a, 'z'), end=' ')\na.set()\nprint(hasattr(a, 'z'))",["False True","True True","False False","True False"],0,"Instance variables can be created in any method, not only __init__; z does not exist until set() runs.","4.2 instance variables",[null,"Before set() runs, z does not exist, so the first hasattr is False, not True.","After set() creates self.z, the second hasattr becomes True, not False.","The values are reversed; z is absent first and present after set()."],null],["pc68",0,4,"What is the output of the following code?\nclass A:\n    pass\nprint(A.__bases__)",["()","(<class 'object'>,)","None","(<class 'type'>,)"],1,"In Python 3 every class without an explicit base inherits from object, so __bases__ is a one-item tuple containing object. type is the metaclass, not a base.","4.5 __bases__",["Every class inherits from object, so __bases__ is not an empty tuple.",null,"__bases__ is always a tuple of base classes, never None.","type is the metaclass, not a base class; the single base here is object."],null],["pc69",0,4,"Class Manager inherits from class Employee. Which statement uses the terms correctly?",["Employee is the subclass of Manager","Manager is the superclass of Employee","Employee is the superclass of Manager","Manager and Employee are sibling classes"],2,"The class being inherited from is the superclass (Employee); the class that inherits is the subclass (Manager).","4.1 OOP concepts",["It is the reverse: Manager inherits from Employee, so Manager is the subclass, not Employee.","Manager inherits from Employee, so Manager is the subclass, not the superclass.",null,"One class inherits from the other, so they are in a parent-child relationship, not siblings."],null],["pc70",0,4,"What is the output of the following code?\nclass A:\n    n = 0\na = A()\nb = A()\na.n += 1\nA.n = 10\nprint(a.n, b.n)",["10 10","1 1","1 10","11 10"],2,"a.n += 1 reads the class value 0 and creates an instance variable a.n = 1. Changing A.n later affects only instances without their own n, so b.n is 10.","4.2 instance vs class variables",["a.n += 1 gives a its own instance n of 1, so a.n is 1, not 10.","b never gets its own n, so b.n reads the updated class value 10, not 1.",null,"a.n was set to 1 before A.n changed, and instance values do not track later class changes, so a.n is 1, not 11."],null],["pc71",0,5,"What is the output of the following code?\nprint([x * x for x in range(5) if x % 2])",["[0, 4, 16]","[1, 9]","[1, 4, 9, 16]","[1, 3]"],1,"x % 2 is truthy only for odd x (1 and 3), whose squares are 1 and 9. [0, 4, 16] would be the result for even numbers.","5.1 list comprehensions",["x % 2 keeps odd numbers, not even ones, so 0, 2 and 4 are excluded.",null,"The filter drops even x, so 4 and 16 (squares of 2 and 4) are not included.","The expression squares each kept value, so it yields 1 and 9, not the raw 1 and 3."],null],["pc72",0,5,"What is the output of the following code?\nprint([i for i in range(3) for j in range(2)])",["[0, 1, 2, 0, 1, 2]","[0, 0, 1, 1, 2, 2]","[0, 1, 0, 1, 0, 1]","[[0, 0], [1, 1], [2, 2]]"],1,"The for clauses nest left to right, so for each i the inner loop runs twice, repeating i. The result is a flat list, not a list of lists.","5.1 nested comprehensions",["The outer loop is i and the inner is j, so each i repeats before advancing, giving 0,0,1,1,2,2.",null,"The emitted value is i, not j, so the output does not cycle 0,1.","The comprehension produces a flat list of i values, not nested lists."],null],["pc73",0,5,"What is the output of the following code?\nf = lambda x, y=2: x ** y\nprint(f(3), f(2, 3))",["9 8","6 6","9 9","8 9"],0,"f(3) uses the default y=2, giving 9; f(2, 3) is 2 cubed, giving 8.","5.2 lambdas",[null,"f(3) is 3**2 = 9, not 3*2 = 6; the lambda exponentiates rather than multiplies.","f(2, 3) overrides y with 3, giving 2**3 = 8, not 9.","The results are swapped; f(3) is 9 and f(2, 3) is 8."],null],["pc74",0,5,"What is the output of the following code?\nprint(list(map(lambda x: x * 2, [1, 2, 3])))",["[1, 2, 3, 1, 2, 3]","[2, 4, 6]","[2, 3, 4]","<map object>"],1,"map applies the lambda to each element and list() collects the results. Multiplying the list itself by 2 would give the repeated list.","5.2 map()",["This repeats the list; map instead applies the lambda to each element, doubling it.",null,"The lambda multiplies by 2, not adds 1, so the values double to 2, 4, 6.","list() consumes the map iterator and produces an actual list, so the object repr is not shown."],null],["pc75",0,5,"What is the output of the following code?\nprint(list(filter(lambda s: len(s) > 2, ['a', 'abc', 'de', 'xyz'])))",["['a', 'de']","[False, True, False, True]","['abc', 'xyz']","[3, 3]"],2,"filter keeps the items for which the function returns a true value; it does not return the booleans themselves.","5.2 filter()",["filter keeps items where len > 2 is True, which excludes 'a' and 'de' rather than keeping them.","filter returns the matching items, not the boolean results of the predicate.",null,"filter returns the strings themselves, not their lengths."],null],["pc76",0,5,"What is the output of the following code?\ndef outer(n):\n    def inner(x):\n        return x + n\n    return inner\nadd5 = outer(5)\nprint(add5(10))",["5","10","15","An error is raised"],2,"inner is a closure that remembers n = 5 after outer returns, so add5(10) is 15.","5.3 closures",["inner adds n (5) to its argument 10, giving 15, not just n.","The closure adds the remembered n=5 to 10, producing 15, not 10 alone.",null,"inner is a valid closure that remembers n after outer returns, so no error occurs."],null],["pc77",0,5,"What is the output of the following code?\nfs = [lambda: i for i in range(3)]\nprint([f() for f in fs])",["[0, 1, 2]","[2, 2, 2]","[0, 0, 0]","[3, 3, 3]"],1,"Each lambda looks up i when it is called, not when it is created. By then the comprehension has finished with i = 2, so all three return 2.","5.3 closures and late binding",["Each lambda reads i when called, not when created, so they all see the final i rather than distinct values.",null,"i ends at 2 after the loop, so the shared lookup returns 2, not 0.","range(3) stops at i = 2, so the final value is 2, not 3."],null],["pc78",0,5,"What is the output of the following code?\ndef gen():\n    yield 1\n    yield 2\ng = gen()\nprint(next(g), next(g))",["1 1","1 2","2 2","[1, 2]"],1,"A generator pauses at each yield and resumes from there on the next call to next(), so the calls return 1 and then 2.","5.3 generators",["The generator advances past the first yield on the second next(), so it returns 2, not 1 again.",null,"The first next() returns 1, since the generator starts at the first yield.","next() returns individual values one at a time, not a list."],null],["pc79",0,5,"A generator function yields exactly two values. A script calls next() on the generator object a third time without a default. What happens?",["It returns None","It restarts and returns the first value","StopIteration is raised","IndexError is raised"],2,"An exhausted generator raises StopIteration on next(). for loops catch this automatically, but a direct next() call without a default does not.","5.3 generators",["A bare next() on an exhausted generator raises rather than returning None; None comes only with a default argument.","Generators do not restart; once exhausted they stay exhausted.",null,"Exhausted iterators signal completion with StopIteration, not IndexError."],null],["pc80",0,5,"What is the output of the following code?\nm = map(str, [1, 2])\nprint(list(m), list(m))",["['1', '2'] []","['1', '2'] ['1', '2']","[1, 2] [1, 2]","[] []"],0,"map returns a one-shot iterator. The first list() consumes it, so the second list() gets nothing.","5.2 map()",[null,"map is a one-shot iterator, so the second list() finds it already consumed and empty.","map(str, ...) produces strings, so the elements are '1' and '2', not integers.","The first list() does consume the values, so it is not empty; only the second one is."],null],["pc81",0,5,"A script must add one line to the end of an existing log file each time it runs, keeping all earlier lines. Which open() mode should it use?",["'w'","'x'","'r+'","'a'"],3,"'a' opens for appending and creates the file if needed. 'w' truncates the file, 'x' fails if the file exists, and 'r+' writes from the start unless you seek.","5.4 open() modes",["Write mode truncates the file to empty, destroying the earlier log lines.","Exclusive-create mode fails with FileExistsError once the log file already exists.","Read/write mode writes from the start and can overwrite existing lines unless you seek to the end.",null],null],["pc82",0,5,"A program reads a PNG image with open('logo.png', 'rb').read(). What type is the returned value?",["str","bytes","bytearray","list"],1,"Binary mode returns bytes objects instead of decoded str. A bytearray is only filled when you pass one to readinto().","5.4 binary mode",["Binary mode does not decode text, so read() returns bytes, not a str.",null,"read() in binary mode returns an immutable bytes object; a bytearray is only produced when you supply one to readinto().","read() returns a single bytes object, not a list of values."],null],["pc83",0,5,"A command-line tool prints results to standard output, and error messages must stay separate so they are not redirected into the results file. Where should error messages be written?",["sys.stdin","sys.stdout","sys.stderr","sys.argv"],2,"sys.stderr is the predefined stream for diagnostics and is not captured by a plain `>` redirect. sys.stdin is for input and sys.argv is a list of arguments.","5.4 predefined streams",["stdin is the input stream, not a place to write error messages.","stdout carries the normal results and is captured by a `>` redirect, so errors would mix into the results file.",null,"argv is a list of command-line arguments, not an output stream."],null],["pc84",0,5,"A backup script wants to print a friendly message only when a file is missing, and re-raise every other I/O error. Which test inside `except OSError as e:` does this?",["if e.errno == errno.ENOENT:","if e.errno == errno.EACCES:","if e.args == 'missing':","if e.strerror is None:"],0,"errno.ENOENT means no such file or directory. EACCES is permission denied, and args is a tuple, never equal to a plain string.","5.4 errno",[null,"EACCES means permission denied, not a missing file, so it would not detect the file-not-found case.","args is a tuple, never equal to a plain string, so this test is always False.","strerror holds a descriptive message for OSErrors, so it is not None and does not identify a missing file."],null],["pc85",0,5,"A loop calls f.readline() on a text file opened in 'r' mode. How can the loop tell that the end of the file has been reached?",["readline() returns None","readline() returns an empty string ''","readline() raises EOFError","readline() returns '\\n'"],1,"readline returns '' only at end of file; a blank line in the middle of the file comes back as '\\n'. EOFError is raised by input(), not by file reads.","5.4 readline()",["readline returns '' at end of file, never None.",null,"File reads do not raise EOFError; that comes from input(). readline just returns ''.","A '\\n' indicates a blank line within the file, not the end of the file."],null],["pc86",0,5,"A program opens a binary file and runs `buf = bytearray(8)` and then `n = f.readinto(buf)`. What is stored in n?",["The bytes that were read","The number of bytes read into buf","The new file position in characters","True if buf was filled completely"],1,"readinto fills the existing bytearray in place and returns how many bytes it read, which may be less than 8 near the end of the file.","5.4 readinto() and bytearray",["readinto stores the bytes in the provided buffer and returns a count instead of the data.",null,"readinto returns the number of bytes read, not the file offset or position.","It returns an integer count, which may be less than the buffer size, not a boolean."],null],["pc87",0,5,"What is the output of the following code?\nb = bytearray(3)\nb[0] = 65\nprint(list(b))",["[65, 0, 0]","['A', 0, 0]","[65]","[0, 0, 65]"],0,"bytearray(3) creates three zero bytes, and bytearrays are mutable, so assigning 65 to index 0 changes the first byte. Items are integers, not characters.","5.4 bytearray",[null,"bytearray items are integers, so the first element is 65, not the character 'A'.","bytearray(3) has three elements; assigning to index 0 does not shrink it to one.","The assignment targets index 0, so 65 goes to the front, not the end."],null],["pc88",0,5,"What is the output of the following code?\nm = [[r * c for c in range(3)] for r in range(2)]\nprint(m[1])",["[0, 0, 0]","[0, 1, 2]","[1, 2, 3]","[0, 2, 4]"],1,"Row r = 1 multiplies 1 by each column 0, 1 and 2. Row 0 would be all zeros.","5.1 nested comprehensions",["That is row 0 (r=0), where every product is 0; row 1 multiplies 1 by each column.",null,"Columns run 0, 1, 2, so row 1 is 0, 1, 2, not 1, 2, 3.","That would be row 2 (r=2), but the grid only has rows 0 and 1."],null],["pc89",0,5,"What is the output of the following code?\nprint(sorted(['bb', 'a', 'ccc'], key=lambda s: -len(s)))",["['a', 'bb', 'ccc']","['bb', 'a', 'ccc']","['ccc', 'bb', 'a']","['a', 'ccc', 'bb']"],2,"The key function sorts by negative length, so the longest string comes first. Without the key, alphabetical order would give ['a', 'bb', 'ccc'].","5.2 lambdas as arguments",["That is ascending length order; the negative-length key sorts the longest first instead.","Sorting by -len groups strictly by descending length, so 'ccc' must come first.",null,"The key sorts purely by descending length, producing 'ccc', 'bb', 'a', not this mix."],null],["pc90",0,5,"A function writes data to a file and may raise an exception halfway through. Which approach guarantees the file is closed either way?",["Call f.close() at the end of the function","Open the file in a with statement","Open the file in 'x' mode","Call f.flush() after every write"],1,"A with block closes the file when it exits, even if an exception is raised. A close() at the end is skipped when an exception jumps out first, and flush() does not close.","5.4 closing files",["If an exception jumps out before that line, the close() call is skipped and the file stays open.",null,"Exclusive-creation mode controls how the file is opened, not whether it is closed on error.","flush pushes buffered data to disk but does not close the file."],null]]);
