/* More ckad practice questions with wrong-answer notes, and a difficulty level for every question. Merged into the free bank by tools/build.js. */
CertHub.addExtra("ckad", {
  questions: [
    ["x1",0,1,"A data-import Job keeps creating new Pods after each failure, and the team wants Kubernetes to give up after four failed retries. Which Job field should they set?",["backoffLimit: 4","activeDeadlineSeconds: 4","completions: 4","parallelism: 4 with restartPolicy: Never"],0,"backoffLimit is the number of retries before the Job is marked failed. activeDeadlineSeconds is tempting because it also stops a Job, but it measures elapsed seconds rather than failed attempts.","Domain 1: Jobs and backoffLimit"],
    ["x2",0,1,"A reporting Job must be stopped if it is still running 10 minutes after it starts, no matter how many retries remain. Which setting in the Job spec does this?",["backoffLimit: 600","activeDeadlineSeconds: 600","ttlSecondsAfterFinished: 600","terminationGracePeriodSeconds: 600"],1,"activeDeadlineSeconds caps the total run time of the Job; once exceeded, its Pods are terminated and the Job fails with DeadlineExceeded. ttlSecondsAfterFinished is tempting but only deletes finished Jobs.","Domain 1: Jobs and activeDeadlineSeconds"],
    ["x3",0,1,"An app always connects to localhost:6379. A second container in the same Pod accepts that connection and forwards it to the correct Redis shard elsewhere in the cluster. Which multi-container pattern is this?",["Adapter","Init container","Ambassador","Ephemeral container"],2,"An ambassador container proxies the main container's connections to outside services, so the app only ever talks to localhost. Adapter is tempting because it is also a helper container, but it reshapes output rather than brokering connections.","Domain 1: Multi-container patterns (ambassador)"],
    ["x4",0,1,"A legacy app writes metrics in its own text format, but the monitoring system only scrapes Prometheus-format metrics. You cannot change the app. Which design fits best?",["An init container that rewrites the metrics once","An ambassador container that forwards the metrics unchanged","A readiness probe that exports the metrics","An adapter container that converts the metrics"],3,"The adapter pattern places a helper container next to the app that presents the app's output in a standard interface, here Prometheus metrics. An ambassador is tempting but it only relays traffic without transforming it.","Domain 1: Multi-container patterns (adapter)"],
    ["x5",0,1,"An image already ships several files in /etc/app. You must add config.yaml from ConfigMap app-cfg into that directory without hiding the existing files. Which volumeMount works?",["mountPath: /etc/app with readOnly: true","mountPath: /etc/app with items listing only config.yaml","mountPath: /etc/app/config.yaml with subPath: config.yaml","envFrom with configMapRef app-cfg and no volume"],2,"subPath mounts a single key of the volume as one file at the given path, leaving the rest of the directory intact. Using items is tempting, but the volume is still mounted over the whole directory.","Domain 1: volumeMounts and subPath"],
    ["x6",0,1,"A log-shipping sidecar must read files that the main container writes to a shared emptyDir, and it must not be able to change them. What is the simplest way to enforce this?",["Set readOnly: true on the sidecar's volumeMount","Set readOnlyRootFilesystem: true on the sidecar","Set medium: ReadOnly on the emptyDir","Mount the emptyDir only in the main container"],0,"readOnly is set per volumeMount, so the main container can keep writing while the sidecar's mount is read-only. readOnlyRootFilesystem is tempting but it does not affect mounted volumes.","Domain 1: volumeMounts and readOnly"],
    ["x7",0,1,"An app expects its settings file (from a ConfigMap), its TLS key (from a Secret) and its Pod labels (from the downward API) all in one directory, /etc/app. Which volume type provides this in a single mount?",["emptyDir","configMap with a Secret listed under items","hostPath pointing at /etc/app on the node","projected"],3,"A projected volume combines sources such as configMap, secret, downwardAPI and serviceAccountToken into one directory. A configMap volume is tempting but it can only take keys from a single ConfigMap.","Domain 1: Ephemeral volumes (projected)"],
    ["x8",0,1,"A PVC omits storageClassName, and the cluster has a StorageClass marked as default with a dynamic provisioner. What happens when the PVC is created?",["The PVC stays Pending until an admin creates a PV by hand","The default StorageClass provisions a matching PV","The PVC binds to any existing PV regardless of size","The Pod falls back to an emptyDir volume"],1,"When storageClassName is omitted, the default StorageClass is applied to the claim and its provisioner creates a PV dynamically. Staying Pending is tempting, but that is the behavior when no default class exists.","Domain 1: StorageClass and dynamic provisioning"],
    ["x9",0,1,"Three replicas of a Deployment, spread across different nodes, must all write to the same shared directory. Which PVC access mode is required, assuming the storage backend supports it?",["ReadWriteOnce","ReadWriteMany","ReadOnlyMany","ReadWriteOncePod"],1,"ReadWriteMany allows the volume to be mounted read-write by many nodes at once, which shared-file backends such as NFS provide. ReadWriteOnce is tempting because it is the common default, but it is limited to one node.","Domain 1: PersistentVolume access modes"],
    ["x10",0,1,"A Python Dockerfile runs `COPY . .` and then `RUN pip install -r requirements.txt`. Every small code change makes the build reinstall all dependencies. What is the best fix?",["Add --no-cache-dir to pip install","Chain all RUN lines into one instruction","Build with --no-cache every time","Copy requirements.txt and install it before copying the rest"],3,"Each instruction's cache is invalidated when its inputs change. Copying only requirements.txt first means the install layer is reused until dependencies change. Chaining RUN lines is tempting but the early COPY . . still busts the cache.","Domain 1: Dockerfile layers and build cache"],
    ["x11",0,1,"An image has `ENTRYPOINT [\"/app/server\"]` and `CMD [\"--port=8080\"]`. A Pod sets `command: [\"/bin/sh\", \"-c\"]` and no args. What does the container run?",["/bin/sh -c with no arguments, ignoring the image CMD","/bin/sh -c /app/server --port=8080","/bin/sh -c --port=8080","/app/server --port=8080"],0,"If a Pod sets command but not args, Kubernetes uses only that command and ignores both the image ENTRYPOINT and CMD. Expecting the CMD to be kept as arguments is the tempting mistake; you must set args explicitly.","Domain 1: command and args vs ENTRYPOINT and CMD"],
    ["x12",0,1,"A colleague gives you /tmp/app.tar, created with docker save from image app:1.0. You must load it and tag it as registry.local/app:1.0. Which commands do this?",["docker import /tmp/app.tar registry.local/app:1.0","docker pull /tmp/app.tar, then docker tag app:1.0 registry.local/app:1.0","docker load -i /tmp/app.tar, then docker tag app:1.0 registry.local/app:1.0","docker run -i /tmp/app.tar, then docker commit registry.local/app:1.0"],2,"docker load restores images saved with docker save, including tags and metadata, and docker tag adds the new name. docker import is tempting because it also reads tar files, but it is the counterpart of docker export and loses image config.","Domain 1: Saving, loading and tagging images"],
    ["x13",0,2,"New Pods of Deployment web pass their readiness probe and then often crash about 20 seconds later, yet the rollout moves on to the next batch immediately. Which field makes the rollout wait before treating new Pods as available?",["progressDeadlineSeconds: 30","revisionHistoryLimit: 30","terminationGracePeriodSeconds: 30","minReadySeconds: 30"],3,"minReadySeconds requires a new Pod to stay Ready for that many seconds before it counts as available, so a Pod that crashes after 20 seconds holds the rollout back. progressDeadlineSeconds is tempting but only reports a stuck rollout.","Domain 2: Rolling updates and minReadySeconds"],
    ["x14",0,2,"A Deployment has 10 replicas with `maxSurge: 0` and `maxUnavailable: 20%`. What is the minimum number of available Pods during a rolling update?",["10","12","8","5"],2,"20% of 10 is 2, so at most two Pods may be unavailable, leaving at least 8 available. 12 is tempting if you confuse maxUnavailable with maxSurge, but maxSurge is 0 here.","Domain 2: maxSurge and maxUnavailable"],
    ["x15",0,2,"Traffic has doubled and Deployment web needs 5 replicas right now. Which command does this?",["kubectl set replicas deployment/web 5","kubectl scale deployment web --replicas=5","kubectl rollout scale deployment/web --count=5","kubectl autoscale deployment web --replicas=5"],1,"kubectl scale changes the replica count directly and does not create a new revision. kubectl autoscale is tempting but it creates a HorizontalPodAutoscaler rather than setting a fixed number.","Domain 2: kubectl scale"],
    ["x16",0,2,"Before rolling back, you want to see the Pod template that Deployment web used in revision 2. Which command shows it?",["kubectl rollout history deployment/web --revision=2","kubectl rollout undo deployment/web --to-revision=2","kubectl get replicaset web --revision=2","kubectl describe deployment web --history=2"],0,"kubectl rollout history with --revision prints the Pod template recorded for that revision without changing anything. rollout undo is tempting but it performs the rollback.","Domain 2: kubectl rollout history"],
    ["x17",0,2,"You added a Helm repo several weeks ago. Its site now lists a newer chart version, but helm install still picks the old version. What should you run first?",["helm repo update","helm repo add again with the same name","helm upgrade --install with --force","helm dependency update"],0,"Helm uses a locally cached index of each repo; helm repo update downloads the latest index so new chart versions become visible. helm dependency update is tempting because of its name, but it works on a chart's own dependencies.","Domain 2: Helm repositories"],
    ["x18",0,2,"You want to find charts named redis in the Helm repositories you have already added. Which command?",["helm search hub redis","helm search repo redis","helm list redis","helm show chart redis"],1,"helm search repo searches the charts in the repos you added with helm repo add. helm search hub is tempting but it searches the public hub, not your configured repos.","Domain 2: Helm search"],
    ["x19",0,2,"You run `helm upgrade web ./chart -f prod.yaml --set replicaCount=4`, and prod.yaml contains replicaCount: 2. How many replicas does the release get?",["2, because values files override --set","The chart default, because the two values conflict","4, because --set overrides values files","None, because Helm rejects conflicting values"],2,"Helm merges chart defaults, then -f files in order, then --set values, so --set wins. Thinking the file wins is the tempting mistake because it was listed first on the command line.","Domain 2: Helm values precedence"],
    ["x20",0,2,"A CI pipeline deploys release api with the same command every time. The release may or may not exist yet. Which single command installs it the first time and upgrades it afterwards?",["helm install api ./chart --replace","helm rollback api 0","helm template api ./chart","helm upgrade --install api ./chart"],3,"helm upgrade --install upgrades the release if it exists and installs it if not, which makes it safe to repeat. helm install --replace is tempting but it fails when the release is still deployed.","Domain 2: Helm install and upgrade"],
    ["x21",0,2,"Release web in namespace frontend is no longer needed. Which command removes it and the resources it created?",["helm delete-release web --namespace frontend","kubectl delete namespace frontend","helm uninstall web -n frontend","helm rollback web 0 -n frontend"],2,"helm uninstall removes the release's resources and its release record in that namespace. Deleting the namespace is tempting because it also removes the release, but it wipes unrelated workloads too.","Domain 2: Helm uninstall"],
    ["x22",0,2,"In a Kustomize overlay, every resource must be deployed into namespace staging, even though the base manifests have no namespace. Which kustomization.yaml field does this?",["namespace: staging","namePrefix: staging-","labels with namespace: staging","commonAnnotations with namespace: staging"],0,"The namespace field sets metadata.namespace on every namespaced resource Kustomize outputs. namePrefix is tempting because overlays often use it too, but it only changes names.","Domain 2: Kustomize namespace"],
    ["x23",0,2,"An overlay uses configMapGenerator to create app-cfg, and a Deployment references app-cfg. When you change a value and run kubectl apply -k, the Deployment rolls out new Pods. Why?",["kubectl apply -k always restarts every Deployment","The kubelet restarts Pods whenever a ConfigMap changes","configMapGenerator adds a rollout restart annotation","The ConfigMap name gets a new content hash, and Kustomize updates the reference"],3,"By default, generated ConfigMaps get a name suffix computed from their content, and Kustomize rewrites references to it. The Pod template therefore changes and triggers a rollout. Blaming the kubelet is tempting, but it does not restart Pods on ConfigMap changes.","Domain 2: Kustomize configMapGenerator"],
    ["x24",0,2,"A base Deployment web has 2 replicas. Only the prod overlay should run 5 replicas, and other overlays must keep 2. What is the right way to do this?",["Edit replicas in the base deployment.yaml","Add a patch in the prod overlay that sets spec.replicas: 5 for web","Set namePrefix: prod- and replicas in labels","Run kubectl scale after every apply -k"],1,"Overlays customize a shared base through patches (or the replicas field), so only the prod output changes. Editing the base is tempting because it is quick, but it changes all environments.","Domain 2: Kustomize overlays and patches"],
    ["x25",0,3,"You are writing a Deployment by hand and need to see which fields are valid under spec.strategy on this cluster. Which command shows them?",["kubectl api-resources deployment.spec.strategy","kubectl explain deployment.spec.strategy","kubectl describe deployment.spec.strategy","kubectl get deployment --schema=strategy"],1,"kubectl explain reads the cluster's API schema and documents any field path, which is handy offline during the exam. api-resources is tempting because it also reads API info, but only at the resource-kind level.","Domain 3: kubectl explain"],
    ["x26",0,3,"An old manifest defines a HorizontalPodAutoscaler with `apiVersion: autoscaling/v2beta2` and is rejected by a current cluster. Which apiVersion should it use?",["autoscaling/v1beta1","apps/v1","metrics.k8s.io/v1","autoscaling/v2"],3,"The beta HPA APIs were removed and replaced by the stable autoscaling/v2, which keeps multi-metric support. metrics.k8s.io is tempting because HPAs use metrics, but it is a different API.","Domain 3: API deprecations"],
    ["x27",0,3,"A new Pod stays Pending. kubectl describe shows `0/3 nodes are available: 3 Insufficient cpu`. What is the most likely fix?",["Lower the Pod's CPU request or add node capacity","Raise the Pod's CPU limit","Add a liveness probe","Restart the kubelet on each node"],0,"The scheduler places Pods only where the sum of requests fits a node's allocatable CPU, so the request must fit or capacity must grow. Raising the limit is tempting, but limits play no part in scheduling.","Domain 3: Reading Pod status (Pending)"],
    ["x28",0,3,"You need the last 50 log lines from the past hour of Deployment api, without looking up a Pod name. Which command?",["kubectl logs api --lines=50 --age=1h","kubectl get logs deployment api --tail=50","kubectl logs deploy/api --tail=50 --since=1h","kubectl describe deploy/api --tail=50 --since=1h"],2,"kubectl logs accepts deploy/<name> and picks one of its Pods, with --tail limiting lines and --since limiting time. Using a bare name is tempting, but it is looked up as a Pod.","Domain 3: Container logs"],
    ["x29",0,3,"For a quick report you need only the IP address of every Pod in the namespace, on one line. Which command prints that?",["kubectl get pods -o wide --no-headers","kubectl get pods -o jsonpath='{.status.podIP}'","kubectl get pods --show-labels=podIP","kubectl get pods -o jsonpath='{.items[*].status.podIP}'"],3,"A list is returned as an object with items, so the jsonpath must iterate .items[*] to reach each status.podIP. -o wide is tempting because it shows IPs, but it prints other columns too.","Domain 3: Output formatting with jsonpath"],
    ["x30",0,3,"Pod app crashes at startup, and you want a copy of it with the app container's command replaced by a shell so you can look around, without changing the original Pod. Which command?",["kubectl exec -it app -c app -- sh","kubectl edit pod app and change the command","kubectl debug app -it --copy-to=app-dbg --container=app -- sh","kubectl debug app -it --image=busybox --target=app"],2,"kubectl debug --copy-to creates a copy of the Pod, and naming --container with a command after -- replaces that container's command in the copy. The --target variant is tempting, but it attaches to the original Pod rather than making a modified copy.","Domain 3: Debugging with kubectl debug"],
    ["x31",0,3,"A service implements the standard gRPC health checking protocol on port 9090. Which liveness probe handler checks it most directly?",["httpGet on port 9090 at path /health","grpc with port: 9090","tcpSocket on port 9090","exec running curl localhost:9090"],1,"The grpc probe handler calls the gRPC Health Checking Protocol on the given port and uses the service's own health status. tcpSocket is tempting because it works on any port, but it only tests that something is listening.","Domain 3: Probe handlers"],
    ["x32",0,3,"A Deployment's Pods briefly show Completed and then CrashLoopBackOff. kubectl logs shows the expected output and the exit code is 0. What is the most likely cause?",["The container's main process finishes and exits","The memory limit is too low","The image tag does not exist","The readiness probe is failing"],0,"Deployment Pods use restartPolicy Always, so a command that runs and exits 0 is restarted again and again, eventually backing off. That task belongs in a Job, or the command must stay in the foreground. A readiness failure is tempting, but readiness never restarts containers.","Domain 3: Pod status and exit codes"],
    ["x33",0,3,"You need to find which Pod in a namespace has restarted most often, listing Pods ordered by restart count. Which command?",["kubectl get pods --sort-by='.status.containerStatuses[0].restartCount'","kubectl top pod --sort-by=restarts","kubectl get pods -o wide --sort-by=RESTARTS","kubectl get events --sort-by=restartCount"],0,"--sort-by takes a JSONPath expression, and restartCount lives in each container status. Using the RESTARTS column name is tempting because it is shown in the output, but sort keys must be field paths.","Domain 3: Output tricks (--sort-by)"],
    ["x34",0,4,"Pod A mounts ConfigMap app-cfg as a whole directory; Pod B mounts one key of it with subPath. After the ConfigMap is updated, only Pod A sees the new value. Why?",["Pod B needs a readiness probe to reload the file","Volumes mounted with subPath do not receive ConfigMap updates","ConfigMap updates reach only the first Pod that mounts them","Pod B must set readOnly: false on the mount"],1,"ConfigMap volumes are refreshed by swapping a symlink, but a subPath mount binds one file directly and never sees the swap, so the container must be restarted. Blaming readOnly is tempting, but it only restricts the container's writes.","Domain 4: ConfigMaps as volumes (subPath)"],
    ["x35",0,4,"A ClusterRole pod-reader already exists. ServiceAccount ci in namespace dev should get those permissions only inside dev. What is the best approach?",["A ClusterRoleBinding that references pod-reader","Copy the ClusterRole into a namespaced Role in every namespace","A RoleBinding in dev that references ClusterRole pod-reader","Add the namespace dev to the ClusterRole's metadata"],2,"A RoleBinding may reference a ClusterRole, granting its rules only in the RoleBinding's namespace. Copying the rules into a Role is tempting and would work, but reusing the ClusterRole is simpler and avoids drift.","Domain 4: RBAC bindings"],
    ["x36",0,4,"You want to see every action your current credentials are allowed to perform in namespace dev. Which command?",["kubectl auth whoami -n dev","kubectl get rolebindings -n dev -o wide","kubectl describe clusterrole --mine","kubectl auth can-i --list -n dev"],3,"kubectl auth can-i --list prints the resources and verbs allowed for the current user in the namespace. auth whoami is tempting because it is in the same command group, but it only reports identity.","Domain 4: kubectl auth can-i"],
    ["x37",0,4,"A mutating admission webhook injects a proxy sidecar, and a validating webhook rejects Pods whose containers lack resource limits. The injected sidecar has no limits. What happens to a new Pod whose own app container has limits?",["It is admitted, because validation runs before mutation","It is admitted, because injected containers are exempt","It is rejected, because validation sees the mutated Pod","The sidecar is removed so the Pod passes"],2,"Mutating admission runs before validating admission, so validating webhooks judge the Pod after the sidecar is added, including its missing limits. Assuming validation comes first is the tempting error.","Domain 4: Admission control"],
    ["x38",0,4,"Namespace team-b has a LimitRange with `max.memory: 2Gi` for containers. A Pod whose container sets `limits.memory: 4Gi` is submitted. What happens?",["It is rejected at admission","It is admitted and capped at 2Gi","It is scheduled but OOMKilled at 2Gi","It is admitted because only ResourceQuota is enforced"],0,"The LimitRanger admission plugin rejects containers whose requests or limits fall outside min/max. Expecting the value to be capped is tempting because LimitRange also sets defaults, but it only fills in missing values.","Domain 4: LimitRange"],
    ["x39",0,4,"Namespace ops has a ResourceQuota with `pods: \"10\"`. Deployment worker is scaled from 8 to 12 replicas. What do you observe?",["12 Pods run and two are marked Pending","The scale command is rejected and 8 Pods keep running","All 12 Pods are deleted until replicas are set to 10","10 Pods run and the ReplicaSet reports exceeded quota"],3,"Quota counts Pods at creation, so the ReplicaSet creates Pods until the quota is used up and then logs forbidden: exceeded quota events. Expecting Pending Pods is tempting, but rejected Pods never exist to be Pending.","Domain 4: ResourceQuota"],
    ["x40",0,4,"A node runs low on memory. It hosts one Pod with no requests or limits, one with requests below limits, and one with requests equal to limits. Which is evicted first?",["The Pod with requests equal to limits","The Pod with no requests or limits","The Pod with requests below limits","The Pod that started most recently"],1,"A Pod with no requests or limits is BestEffort, and under memory pressure the kubelet evicts those before Burstable and Guaranteed Pods. Burstable is tempting because it may use more than requested, but BestEffort goes first.","Domain 4: QoS classes"],
    ["x41",0,4,"A container spec sets `cpu: \"0.25\"`. Which value is equivalent?",["25m","250m","0.25Mi","2500m"],1,"One CPU equals 1000m (millicores), so 0.25 CPU is 250m. 25m is tempting if you slip a decimal place.","Domain 4: Resource units"],
    ["x42",0,4,"File app.env contains three lines: LOG=debug, PORT=8080, MODE=dev. You want a ConfigMap with three keys, one per line. Which command?",["kubectl create configmap app --from-file=app.env","kubectl create configmap app --from-literal=app.env","kubectl create secret generic app --from-env-file=app.env","kubectl create configmap app --from-env-file=app.env"],3,"--from-env-file parses each KEY=value line into its own key. --from-file is tempting because it also reads a file, but it stores the entire file under one key.","Domain 4: Creating ConfigMaps"],
    ["x43",0,4,"Pods fail with ImagePullBackOff because the image is in a private registry that needs a login. What is the standard fix?",["Create a docker-registry Secret and list it in imagePullSecrets","Create a generic Secret with the password and mount it as a volume","Set imagePullPolicy: Never","Add the registry password as an env var in the container"],0,"A kubernetes.io/dockerconfigjson Secret (kubectl create secret docker-registry) referenced by imagePullSecrets on the Pod or its ServiceAccount lets the kubelet authenticate. Mounting the credentials is tempting, but pulls happen before any volume is used.","Domain 4: Secrets for registry credentials"],
    ["x44",0,4,"You have tls.crt and tls.key and need a Secret named web-tls for an Ingress. Which command?",["kubectl create secret generic web-tls --cert=tls.crt --key=tls.key","kubectl create configmap web-tls --from-file=tls.crt --from-file=tls.key","kubectl create secret tls web-tls --cert=tls.crt --key=tls.key","kubectl create certificate web-tls --cert=tls.crt --key=tls.key"],2,"kubectl create secret tls creates a kubernetes.io/tls Secret with tls.crt and tls.key keys, which Ingress expects. The generic variant is tempting but does not take those flags or set the right type.","Domain 4: TLS Secrets"],
    ["x45",0,4,"A script needs a short-lived API token for ServiceAccount build in namespace ci. Which command issues one?",["kubectl get secret build-token -n ci","kubectl describe sa build -n ci --token","kubectl auth whoami --as=build -n ci","kubectl create token build -n ci"],3,"kubectl create token uses the TokenRequest API to issue a time-limited token for a ServiceAccount. Reading a token Secret is tempting because older clusters auto-created them, but that is no longer the default.","Domain 4: ServiceAccount tokens"],
    ["x46",0,4,"A container runs as UID 1000 and cannot write to a newly provisioned volume owned by root. Which Pod setting usually fixes this for volumes that support ownership changes?",["securityContext.runAsUser: 0","allowPrivilegeEscalation: true","securityContext.fsGroup: 1000","readOnlyRootFilesystem: false"],2,"With fsGroup set, supported volumes are made group-owned by that GID and writable by the group, and the container's processes get it as a supplementary group. Running as root is tempting because it works, but it drops a key security control.","Domain 4: SecurityContext (fsGroup)"],
    ["x47",0,4,"A hardened Pod uses readOnlyRootFilesystem: true, but the app crashes because it must write temporary files to /tmp. What is the best fix?",["Set readOnlyRootFilesystem: false","Mount an emptyDir at /tmp","Set runAsUser: 0 so root can write","Add the SYS_ADMIN capability"],1,"An emptyDir at /tmp gives the app a writable scratch area while the rest of the filesystem stays read-only. Turning off readOnlyRootFilesystem is tempting because it is quick, but it removes the hardening entirely.","Domain 4: SecurityContext (readOnlyRootFilesystem)"],
    ["x48",0,4,"Namespace dev is labeled `pod-security.kubernetes.io/warn=restricted` with no enforce label. A Pod that breaks the restricted profile is created. What happens?",["The Pod is created and the user sees a warning","The Pod is rejected","The Pod is mutated to meet the restricted profile","The Pod is created with no feedback"],0,"Pod Security Admission has enforce, audit and warn modes; warn only returns warnings to the user. Rejection is tempting, but that needs the enforce label.","Domain 4: Pod Security Admission"],
    ["x49",0,5,"A NetworkPolicy on backend Pods has two separate `from` entries: one with `namespaceSelector: {matchLabels: {team: web}}` and one with `podSelector: {matchLabels: {role: fe}}`. Who may connect?",["Any Pod in team=web namespaces, or role=fe Pods in the policy's namespace","Only role=fe Pods in team=web namespaces","Only role=fe Pods in any namespace","Every Pod in every namespace"],0,"Separate items in a from list are ORed, and a podSelector on its own applies to the policy's namespace. Reading them as AND is the tempting mistake; that only happens when both selectors are in the same entry.","Domain 5: NetworkPolicy peers (OR semantics)"],
    ["x50",0,5,"A policy must allow ingress from all Pods in namespace monitoring, which only has the labels Kubernetes adds automatically. Which peer selects it?",["namespaceSelector matching name: monitoring","namespaceSelector matching kubernetes.io/metadata.name: monitoring","podSelector matching namespace: monitoring","ipBlock with the cidr of namespace monitoring"],1,"Kubernetes adds the immutable label kubernetes.io/metadata.name to every namespace, so it can be selected without adding labels. Using name: monitoring is tempting, but that label does not exist unless someone sets it.","Domain 5: NetworkPolicy namespaceSelector"],
    ["x51",0,5,"Pods labeled app=api are selected by two NetworkPolicies: one allows ingress from app=web, the other allows ingress from app=batch. What traffic reaches the api Pods?",["Only traffic from app=web, because it was created first","No traffic, because the policies conflict","Traffic from both app=web and app=batch","Only traffic allowed by both policies at once"],2,"NetworkPolicies are additive: once a Pod is selected, the allowed traffic is the union of every policy's rules. Treating them as an intersection is the tempting mistake because it sounds more secure.","Domain 5: Combining NetworkPolicies"],
    ["x52",0,5,"Pods may send egress traffic to 10.0.0.0/16, except to the management subnet 10.0.5.0/24. How is this written in one egress rule?",["Two ipBlocks: allow 10.0.0.0/16 and deny 10.0.5.0/24","namespaceSelector with cidr 10.0.0.0/16","podSelector with except: 10.0.5.0/24","ipBlock with cidr 10.0.0.0/16 and except [10.0.5.0/24]"],3,"ipBlock supports an except list to carve ranges out of the allowed CIDR. A separate deny block is tempting, but NetworkPolicy only expresses allows.","Domain 5: NetworkPolicy ipBlock"],
    ["x53",0,5,"An Ingress rule has `path: /login` with `pathType: Exact`. Which request matches it?",["/login/","/login/reset","/login","/Login"],2,"pathType Exact matches the URL path exactly and case-sensitively. /login/ is tempting because Prefix would accept it, but Exact does not.","Domain 5: Ingress pathType"],
    ["x54",0,5,"Requests to an Ingress that match none of its host or path rules should go to Service fallback on port 80. Which Ingress field sets this?",["spec.defaultBackend","spec.rules[].http.paths with path /*","spec.tls.defaultService","metadata.annotations.default-backend"],0,"spec.defaultBackend handles any request that no rule matches. A catch-all path is tempting, but it still only applies inside a matching host rule and /* is not a wildcard.","Domain 5: Ingress default backend"],
    ["x55",0,5,"An Ingress routes host shop.example.com, but there is no DNS record for it yet. How do you test it from a shell that can reach the controller's IP?",["curl shop.example.com --resolve-ingress","kubectl port-forward ingress/shop 80:80","curl <controller-ip>/shop.example.com","curl -H 'Host: shop.example.com' <controller-ip>/"],3,"Ingress controllers choose a rule by the HTTP Host header, so setting it by hand lets you test before DNS exists. Putting the host in the URL path is tempting but the controller sees it as a path, not a host.","Domain 5: Testing Ingress"],
    ["x56",0,5,"In a local kind cluster, a Service of type LoadBalancer shows EXTERNAL-IP <pending> indefinitely. What is the most likely reason?",["The Service selector does not match any Pods","No load balancer implementation exists in the cluster","LoadBalancer Services need a NetworkPolicy","The Pods have not passed their readiness probes"],1,"LoadBalancer Services depend on a cloud controller or an add-on such as MetalLB to hand out addresses; without one, the IP stays pending. The selector is tempting to blame, but it affects endpoints, not address assignment.","Domain 5: Service types (LoadBalancer)"],
    ["x57",0,5,"A shopping-cart app keeps session state in memory, so each client should keep reaching the same Pod behind a ClusterIP Service. Which setting provides this?",["externalTrafficPolicy: Local","sessionAffinity: ClientIP","clusterIP: None","publishNotReadyAddresses: true"],1,"sessionAffinity: ClientIP sends requests from the same client IP to the same Pod. externalTrafficPolicy: Local is tempting because it also keeps traffic local, but it is about nodes, not a client-to-Pod mapping.","Domain 5: Service session affinity"],
    ["x58",0,5,"Which command creates a NodePort Service named web with port 80 forwarding to target port 8080?",["kubectl create service web --type=NodePort --port=80:8080","kubectl expose service web --node-port=8080","kubectl port-forward service/web 80:8080 --nodeport","kubectl create service nodeport web --tcp=80:8080"],3,"kubectl create service nodeport <name> --tcp=<port>:<targetPort> creates the Service with selector app=<name>. The --type variant is tempting because expose uses --type, but create service uses a subcommand.","Domain 5: kubectl create service"],
    ["x59",0,5,"Service db has `port: 80` and `targetPort: 5432`. App Pods have a default-deny egress policy and need a rule allowing traffic to the db Pods. Which port must the egress rule allow?",["5432, the Pod's port","80, the Service port","Both 80 and 5432 must be listed","No port, because Services bypass NetworkPolicy"],0,"Service traffic is translated to a Pod IP and targetPort before NetworkPolicy rules are checked, so the rule must allow 5432 (and DNS if names are used). Allowing the Service port 80 is the tempting mistake.","Domain 5: NetworkPolicy and Service ports"],
    ["x60",0,5,"Two Ingress controllers run in the cluster, and a new Ingress must be handled only by the one whose IngressClass is named nginx. Which field sets this?",["metadata.labels.controller: nginx","spec.defaultBackend.class: nginx","spec.ingressClassName: nginx","spec.rules[].host: nginx"],2,"spec.ingressClassName names the IngressClass, and only the controller behind it handles the Ingress. Using a label is tempting because labels select many things, but controllers use the IngressClass.","Domain 5: IngressClass"]
  ],
  whys: {
    "x1": [null,"activeDeadlineSeconds is a wall-clock time limit in seconds for the whole Job, not a count of retries.","completions sets how many successful Pods the Job needs; it does not cap how many failures are tolerated.","parallelism controls how many Pods run at once; with restartPolicy Never failures still count against backoffLimit, which is left at its default."],
    "x2": ["backoffLimit counts failed retries; a value of 600 would allow many more attempts instead of enforcing a time limit.",null,"ttlSecondsAfterFinished only cleans up a Job after it has already finished; it never stops a running one.","This is how long a Pod gets to shut down after being asked to stop, not a limit on how long the Job may run."],
    "x3": ["An adapter transforms the main container's output (for example, metrics or logs) into a standard format; it does not proxy outbound connections.","An init container runs to completion before the app starts, so it cannot proxy traffic for the app's lifetime.",null,"Ephemeral containers are added temporarily for debugging with kubectl debug, not declared as a permanent proxy."],
    "x4": ["An init container finishes before the app starts, so it cannot convert metrics the app keeps producing while it runs.","An ambassador proxies connections; forwarding the data unchanged leaves it in the format the monitoring system cannot read.","Probes only report health to the kubelet; they cannot publish or translate application metrics.",null],
    "x5": ["Mounting the volume at /etc/app replaces the whole directory with the volume contents, readOnly or not, so the image's files disappear.","items limits which keys are projected, but the volume still covers /etc/app and hides the existing files.",null,"envFrom turns keys into environment variables; it never creates a file in /etc/app."],
    "x6": [null,"readOnlyRootFilesystem applies to the container's own root filesystem, not to mounted volumes, so the sidecar could still write to the emptyDir.","emptyDir supports medium Memory or the default disk; there is no ReadOnly medium.","Then the sidecar would not see the log files at all, so it could not ship them."],
    "x7": ["emptyDir starts empty; it cannot pull in ConfigMap, Secret or downward API data by itself.","A configMap volume can only project keys from one ConfigMap; items cannot reference a Secret.","hostPath exposes a node directory and has nothing to do with ConfigMaps or Secrets; it also weakens isolation.",null],
    "x8": ["That only happens when no default StorageClass exists or dynamic provisioning is not available.",null,"Binding always requires a PV that satisfies the requested size and access mode.","Kubernetes never silently swaps a PVC for an emptyDir; the Pod would wait for the claim."],
    "x9": ["ReadWriteOnce allows read-write mounting by a single node, so replicas on other nodes could not mount it.",null,"ReadOnlyMany lets many nodes mount the volume, but only for reading, so the replicas could not write.","ReadWriteOncePod restricts the volume to one Pod, which is even stricter than ReadWriteOnce."],
    "x10": ["That only stops pip keeping its own download cache; the layer is still rebuilt whenever any source file changes.","Merging RUN lines reduces layer count but does nothing about the COPY that invalidates the cache first.","--no-cache disables layer caching completely, which makes every build slower, not faster.",null],
    "x11": [null,"Setting command replaces the ENTRYPOINT; it does not append the old ENTRYPOINT as an argument.","When command is set and args is not, the image's CMD is ignored too, so --port=8080 is not passed.","The Pod's command overrides the image ENTRYPOINT, so /app/server is not what starts."],
    "x12": ["docker import builds an image from a filesystem tarball and drops metadata such as ENTRYPOINT; it is meant for docker export output.","docker pull fetches from a registry by name; it cannot read a local tar file.",null,"docker run needs an image, not a tar file, and commit would snapshot a container rather than restore the saved image."],
    "x13": ["progressDeadlineSeconds sets when a stalled rollout is reported as failed; it does not delay counting Pods as available.","revisionHistoryLimit controls how many old ReplicaSets are kept for rollback, not rollout pacing.","This is the shutdown grace period for Pods being stopped; it does not affect when new Pods are trusted.",null],
    "x14": ["That would require maxUnavailable 0; here up to two Pods may be unavailable at once.","12 would be the maximum with a surge of 2; with maxSurge 0 the total never goes above 10.",null,"5 would mean 50% unavailable, far more than the 20% allowed."],
    "x15": ["kubectl set supports subcommands such as image, env and resources, but not replicas.",null,"kubectl rollout has status, history, undo, pause, resume and restart; there is no scale subcommand.","kubectl autoscale creates an HPA with --min and --max; it has no --replicas flag and does not set a fixed count."],
    "x16": [null,"This actually rolls back to revision 2 instead of just showing it.","kubectl get has no --revision flag, and ReplicaSet names include a hash suffix.","kubectl describe has no --history flag; it only shows the current spec and events."],
    "x17": [null,"Re-adding an existing repo name does not refresh anything; helm repo update is the command that fetches the latest index.","--force changes how resources are replaced; it does not refresh the local chart index.","This refreshes the dependencies listed in a local chart's Chart.yaml, not the index of your configured repos."],
    "x18": ["search hub queries Artifact Hub online rather than the repositories configured on your machine.",null,"helm list shows installed releases, not charts available in repositories.","helm show chart prints metadata for one chart you name exactly; it does not search."],
    "x19": ["The order is the reverse: values from --set take precedence over those in -f files.","Helm merges values by precedence; a conflict does not fall back to the chart default.",null,"Helm does not reject overlapping values; it resolves them by precedence."],
    "x20": ["--replace only reuses the name of a deleted release; it fails if the release is already deployed.","rollback returns an existing release to an earlier revision; it cannot install a new one.","helm template only renders manifests locally; nothing is installed or upgraded.",null],
    "x21": ["There is no delete-release command; helm uninstall (with the alias helm delete) is the correct one.","This deletes everything in the namespace, not just this release, and is far broader than asked.",null,"rollback moves a release to another revision; it does not remove it."],
    "x22": [null,"namePrefix changes resource names, not the namespace they are created in.","Adding a label called namespace only tags resources; it does not place them in that namespace.","Annotations are metadata only and do not change where a resource is created."],
    "x23": ["apply -k only changes what differs; Deployments with an unchanged Pod template are not rolled.","The kubelet never restarts Pods because a ConfigMap changed; mounted files update in place and env vars stay as they were.","Kustomize does not add restart annotations; the rollout comes from the changed ConfigMap name in the Pod template.",null],
    "x24": ["Changing the base affects every overlay built from it, not just prod.",null,"Labels are metadata; putting a replicas label does not change the replica count.","A manual scale is lost the next time the overlay is applied and is not declared in the overlay."],
    "x25": ["api-resources lists resource kinds and their groups; it does not describe fields.",null,"describe shows the state of existing objects; it does not document the schema.","kubectl get has no --schema flag."],
    "x26": ["There is no v1beta1 for autoscaling; the stable older version is autoscaling/v1.","apps/v1 is for Deployments, StatefulSets and similar workloads, not HPAs.","metrics.k8s.io is the metrics API read by the HPA, not the API group of the HPA object itself.",null],
    "x27": [null,"Scheduling uses requests, not limits, so raising the limit does not help the Pod fit.","Probes only run after a Pod is scheduled and started; they do not affect placement.","The nodes are not failing; they simply do not have enough unrequested CPU for this Pod."],
    "x28": ["kubectl logs has no --lines or --age flags, and api alone would be treated as a Pod name.","There is no get logs resource; logs are read with kubectl logs.",null,"describe shows object details and events, not container logs."],
    "x29": ["-o wide prints many columns, not just the IPs, so you would still need to trim the output.","A list response has an items array; without .items[*] this path finds nothing.","--show-labels is a boolean that adds a labels column; it cannot select the IP field.",null],
    "x30": ["exec needs a running container, and this one keeps crashing at startup.","A running Pod's container command cannot be edited, and it would also change the original Pod.",null,"This adds an ephemeral container to the original crashing Pod; it does not create a copy with a changed command."],
    "x31": ["gRPC health checks are gRPC calls, not plain HTTP GET requests to a path.",null,"tcpSocket only proves the port accepts connections, not that the service reports itself healthy.","curl sends HTTP, not the gRPC health check; it also needs curl inside the image."],
    "x32": [null,"Running out of memory gives OOMKilled with exit code 137, not a clean exit 0.","A missing image causes ImagePullBackOff before the container ever runs or logs anything.","A failing readiness probe removes the Pod from endpoints but never restarts the container."],
    "x33": [null,"kubectl top shows CPU and memory usage and only sorts by those; it has no restart data.","--sort-by takes a JSONPath field, not a column heading.","Events have no restartCount field; they describe what happened, not per-Pod counters."],
    "x34": ["Probes do not affect how the kubelet refreshes volume contents.",null,"Every Pod with a normal ConfigMap volume gets the update; the difference here is the subPath.","Updates are written by the kubelet regardless of readOnly; readOnly only limits the container."],
    "x35": ["A ClusterRoleBinding grants the permissions in every namespace, which is broader than needed.","It would work in dev, but it duplicates rules and still needs a RoleBinding; a RoleBinding can already reference the ClusterRole.",null,"ClusterRoles are cluster-scoped and have no namespace; scope is set by the binding."],
    "x36": ["whoami shows who you are authenticated as, not what you are allowed to do.","This lists bindings in the namespace, but you would still have to resolve each role and subject by hand.","There is no --mine flag, and describing roles does not show which ones apply to you.",null],
    "x37": ["The order is mutating then validating, so validation checks the final object.","Validating webhooks see the whole mutated object; injected containers are not exempt by default.",null,"Admission webhooks do not undo each other's changes; the validating webhook can only admit or reject."],
    "x38": [null,"LimitRange does not lower values that exceed max; it rejects the Pod.","The Pod is never created, so it cannot be OOMKilled.","LimitRange is enforced at admission just like ResourceQuota."],
    "x39": ["Quota is checked when Pods are created, so the extra two are never created at all.","Scaling only changes the Deployment spec, which quota does not block; Pod creation is what fails.","Quota never deletes running Pods; it only rejects new ones.",null],
    "x40": ["That Pod is Guaranteed QoS and is the last to be evicted.",null,"That Pod is Burstable; it is only considered before Guaranteed Pods, after BestEffort ones.","Start time is not the eviction criterion; QoS and usage relative to requests are."],
    "x41": ["25m is 0.025 CPU, ten times less than 0.25.",null,"Mi is a binary memory unit and is not valid for CPU.","2500m is 2.5 CPUs, ten times more than 0.25."],
    "x42": ["--from-file creates one key named app.env whose value is the whole file.","--from-literal expects key=value pairs typed on the command line, not a file name.","This creates a Secret, not a ConfigMap, for non-sensitive settings.",null],
    "x43": [null,"The kubelet does not read mounted volumes to pull images; the credentials must be referenced in imagePullSecrets.","That would stop the kubelet from pulling at all, so the Pod fails unless the image is already on the node.","Env vars are given to the running container; the image must be pulled before the container exists."],
    "x44": ["generic does not accept --cert and --key; it would need --from-file and would not set the tls type.","A private key belongs in a Secret, and an Ingress tls section references a Secret.",null,"kubectl has no create certificate command."],
    "x45": ["Current clusters do not create long-lived token Secrets for ServiceAccounts automatically, so this Secret usually does not exist.","describe has no --token flag and does not issue tokens.","whoami only reports identity; it never issues credentials.",null],
    "x46": ["Running as root would bypass the problem by removing the non-root protection, which weakens security.","This controls whether a process can gain more privileges than its parent; it does not change volume ownership.",null,"This affects the container's root filesystem, not the permissions of a mounted volume."],
    "x47": ["This works but gives up the protection for the whole filesystem to solve a problem in one directory.",null,"Root cannot write to a read-only filesystem either, and running as root weakens security.","SYS_ADMIN is very broad and does not make a read-only root filesystem writable for the app."],
    "x48": [null,"Rejection happens only in enforce mode; warn mode lets the Pod through.","Pod Security Admission never changes Pods; it only admits, warns, audits or rejects.","warn mode returns a warning to the client, so the user is told about the violations."],
    "x49": [null,"That would be one entry containing both selectors; separate entries are combined with OR.","A podSelector alone matches Pods in the policy's own namespace, not in all namespaces.","Only Pods matching at least one of the two entries are allowed."],
    "x50": ["A name label is not added automatically, so this selector matches nothing unless someone adds it.",null,"podSelector matches Pod labels in the policy's own namespace, not other namespaces.","Namespaces do not have their own CIDR, and Pod IPs change, so ipBlock is not meant for in-cluster Pods."],
    "x51": ["Creation order does not matter; policies are not ranked.","NetworkPolicies do not conflict; they only add allowed connections.",null,"Policies are combined as a union, not an intersection."],
    "x52": ["NetworkPolicies have no deny rules; a second ipBlock can only allow more traffic.","namespaceSelector selects namespaces by label and has no cidr field.","podSelector matches labels and has no except field.",null],
    "x53": ["Exact matching is case-sensitive and the trailing slash makes it a different path.","Exact does not match sub-paths; that would need pathType Prefix.",null,"Paths are matched case-sensitively, so /Login is different from /login."],
    "x54": [null,"Ingress paths are not globs; /* is not a catch-all and rules still depend on host matching.","The tls section only lists hosts and certificate Secrets; it has no defaultService.","Annotations are controller-specific extras; the standard field is spec.defaultBackend."],
    "x55": ["curl has no --resolve-ingress option, and without DNS the name will not resolve.","port-forward works with Pods, Services and workloads, not Ingress objects.","This puts the host in the path; the Ingress matches the Host header, so the rule is not used.",null],
    "x56": ["A wrong selector gives no endpoints but does not stop an external IP from being assigned.",null,"NetworkPolicies filter traffic; they are never needed to assign an address.","Readiness affects endpoints, not whether the load balancer is provisioned."],
    "x57": ["This keeps external traffic on the receiving node's Pods; it does not pin a client to one Pod.",null,"A headless Service removes the virtual IP and load balancing instead of adding stickiness.","This only publishes Pods that are not Ready; it has nothing to do with stickiness."],
    "x58": ["kubectl create service needs a type subcommand such as nodeport; it has no --type flag.","expose creates a Service from an existing resource and has no --node-port flag.","port-forward creates a local tunnel; it never creates a Service.",null],
    "x59": [null,"NetworkPolicies are enforced on Pod IPs and ports after the Service address is translated, so 80 is not what the packet uses.","Listing 80 adds nothing; only the destination Pod port is checked.","Service traffic is still subject to NetworkPolicy once it is translated to Pod addresses."],
    "x60": ["Controllers do not choose Ingresses by an arbitrary label.","defaultBackend points to a Service or resource and has no class field.",null,"host is the hostname clients use, not the controller that handles the Ingress."]
  },
  levels: {
    "ck1": 2, "ck2": 1, "ck3": 3, "ck4": 1, "ck5": 3, "ck6": 2, "ck7": 2, "ck8": 2, "ck9": 2, "ck10": 3,
    "ck11": 1, "ck12": 3, "ck13": 2, "ck14": 2, "ck15": 2, "ck16": 2, "ck17": 2, "ck18": 2, "ck19": 1, "ck20": 1,
    "ck21": 3, "ck22": 2, "ck23": 2, "ck24": 2, "ck25": 3, "ck26": 3, "ck27": 1, "ck28": 1, "ck29": 1, "ck30": 1,
    "ck31": 1, "ck32": 1, "ck33": 2, "ck34": 1, "ck35": 1, "ck36": 3, "ck37": 1, "ck38": 1, "ck39": 1, "ck40": 2,
    "ck41": 3, "ck42": 2, "ck43": 2, "ck44": 1, "ck45": 1, "ck46": 1, "ck47": 2, "ck48": 2, "ck49": 3, "ck50": 1,
    "ck51": 1, "ck52": 1, "ck53": 2, "ck54": 3, "ck55": 2, "ck56": 1, "ck57": 1, "ck58": 3, "ck59": 2, "ck60": 2,
    "ck61": 3, "ck62": 1, "ck63": 1, "ck64": 3, "ck65": 3, "ck66": 1, "ck67": 3, "ck68": 1, "ck69": 2, "ck70": 2,
    "ck71": 3, "ck72": 2, "ck73": 1, "ck74": 3, "ck75": 1, "ck76": 2, "ck77": 2, "ck78": 2, "ck79": 2, "ck80": 2,
    "ck81": 3, "ck82": 3, "ck83": 2, "ck84": 2, "ck85": 3, "ck86": 1, "ck87": 2, "ck88": 1, "ck89": 2, "ck90": 3,
    "x1": 2, "x2": 2, "x3": 2, "x4": 2, "x5": 3, "x6": 2, "x7": 2, "x8": 2, "x9": 2, "x10": 3,
    "x11": 3, "x12": 1, "x13": 2, "x14": 2, "x15": 1, "x16": 1, "x17": 2, "x18": 1, "x19": 2, "x20": 2,
    "x21": 1, "x22": 1, "x23": 3, "x24": 2, "x25": 1, "x26": 1, "x27": 2, "x28": 1, "x29": 2, "x30": 3,
    "x31": 2, "x32": 3, "x33": 2, "x34": 3, "x35": 3, "x36": 1, "x37": 3, "x38": 2, "x39": 2, "x40": 2,
    "x41": 1, "x42": 2, "x43": 2, "x44": 1, "x45": 1, "x46": 2, "x47": 3, "x48": 2, "x49": 3, "x50": 2,
    "x51": 3, "x52": 2, "x53": 2, "x54": 1, "x55": 2, "x56": 2, "x57": 1, "x58": 1, "x59": 3, "x60": 1
  }
});
