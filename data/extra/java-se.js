/* More java-se practice questions with wrong-answer notes, and a difficulty level for every question. Merged into the free bank by tools/build.js. */
CertHub.addExtra("java-se", {
  questions: [
    ["x1",0,1,"What is the output?\nint a = 0b101 + 0x1F + 010;\nSystem.out.println(a);",["44","46","36","It does not compile"],0,"0b101 is binary 5, 0x1F is hex 31 and 010 is octal 8, so the sum is 44. 46 is the tempting answer if you read 010 as decimal ten, but a leading zero marks an octal literal.","Domain 1: Integer literals in binary, hex and octal"],
    ["x2",0,1,"Which declaration fails to compile?",["int a = 1_000_000;","double d = 3_.14;","int b = 0b1010_1010;","long c = 9_999L;"],1,"An underscore may appear only between digits, so placing one next to the decimal point is a compile error. 0b1010_1010 can look suspicious, but underscores are allowed between binary digits too.","Domain 1: Underscores in numeric literals"],
    ["x3",0,1,"What is the output?\nLocalDate d = LocalDate.of(2025, 1, 31);\nSystem.out.println(Period.between(d, LocalDate.of(2025, 3, 1)));",["P29D","P1M","P1M1D","P30D"],2,"Period.between() counts whole months, where 31 January plus one month is 28 February 2025, and then the remaining days, one, giving P1M1D. P29D is tempting because it is the total day count, but Period keeps months separate.","Domain 1: Period calculations"],
    ["x4",0,1,"A subscription page must show how many years, months and days remain between two LocalDate values. Which expression fits?",["Duration.between(start, end).toDays()","Instant.between(start, end)","start.compareTo(end)","Period.between(start, end)"],3,"Period models a date-based amount in years, months and days, which suits LocalDate values. Duration is tempting but measures seconds and nanoseconds, so it fails with dates that have no time part.","Domain 1: Period versus Duration"],
    ["x5",0,1,"What is the output?\nString s = \"\\u2003 hi \\u2003\";\nSystem.out.println(s.trim().length() + \" \" + s.strip().length());",["2 2","6 6","6 2","2 6"],2,"trim() strips only characters whose code is at most U+0020, so the em spaces at both ends block it and the length stays 6, while strip() recognises Unicode whitespace and leaves hi. 2 2 is tempting if you treat the two methods as identical.","Domain 1: strip() versus trim()"],
    ["x6",0,1,"What is the output?\nStringBuilder sb = new StringBuilder(\"0123456\");\nsb.delete(1, 3).replace(2, 4, \"X\");\nSystem.out.println(sb);",["0X56","03X56","0X3456","03X6"],3,"delete(1, 3) removes indexes 1 and 2 to give 03456, then replace(2, 4, \"X\") swaps indexes 2 and 3 (45) for X, giving 03X6. 03X56 is tempting if you treat the end index as inclusive of only one character.","Domain 1: StringBuilder delete and replace"],
    ["x7",0,1,"What is the output?\nint i = 130;\nbyte b = (byte) i;\nSystem.out.println(b + \" \" + (char) 66);",["-126 B","130 B","-126 66","It does not compile"],0,"Narrowing keeps only the low 8 bits, so 130 wraps to -126, and (char) 66 is the character B. 130 B is tempting, but an explicit cast silently overflows rather than failing.","Domain 1: Narrowing conversions and casting"],
    ["x8",0,2,"What is the output?\nint n = 15;\nString r = n % 3 == 0 ? n % 5 == 0 ? \"FB\" : \"F\" : \"N\";\nSystem.out.println(r);",["F","FB","N","It does not compile"],1,"The conditional operator groups to the right, so the expression reads n % 3 == 0 ? (n % 5 == 0 ? \"FB\" : \"F\") : \"N\", and 15 passes both tests. F is tempting if you stop after the first test.","Domain 2: The ternary operator"],
    ["x9",0,2,"Which statement, placed inside a method, fails to compile?",["if (false) { System.out.println(); }","while (false) { System.out.println(); }","do { } while (false);","for (int i = 0; i < 0; i++) { }"],1,"A while loop whose condition is the constant false makes its body unreachable, which is a compile error. if (false) looks equivalent, but the language makes a special exception for if so that it can be used for conditional compilation.","Domain 2: Unreachable code"],
    ["x10",0,2,"A method declares `int x;`, then runs `if (flag) x = 1;` where flag is a boolean parameter, and then `System.out.println(x);`. What happens?",["It does not compile: x might not be initialized","It prints 0 when flag is false","It prints 1 in every case","A NullPointerException when flag is false"],0,"Local variables must be definitely assigned before use, and the compiler sees a path where x is never set. Printing 0 is tempting because fields default to 0, but local variables have no default.","Domain 2: Definite assignment"],
    ["x11",0,2,"What is the output?\nrecord P(int x, int y) {}\n// in main:\nObject o = new P(2, 3);\nString s = switch (o) {\n  case P(int x, int y) when x > y -> \"A\";\n  case P(var x, var y) -> \"B\" + (x + y);\n  default -> \"C\";\n};\nSystem.out.println(s);",["A","B23","C","B5"],3,"The guarded case fails because 2 is not greater than 3, and the second record pattern binds x and y, adding them in parentheses before concatenation. B23 is tempting, but the parentheses force numeric addition.","Domain 2: Record patterns and when guards"],
    ["x12",0,2,"What is the output?\nint[] a = {1, 2, 3};\nfor (int v : a) v *= 2;\nSystem.out.println(Arrays.toString(a));",["[2, 4, 6]","[2, 2, 3]","[1, 2, 3]","It does not compile"],2,"The enhanced for loop copies each element into v, so changing v leaves the array untouched. [2, 4, 6] is tempting, but updating an array needs an indexed loop such as a[i] *= 2.","Domain 2: Enhanced for loops"],
    ["x13",0,3,"What does `new B();` print?\nclass A {\n  static { System.out.print(\"s\"); }\n  { System.out.print(\"a\"); }\n  A() { System.out.print(\"A\"); }\n}\nclass B extends A {\n  static { System.out.print(\"t\"); }\n  { System.out.print(\"b\"); }\n  B() { System.out.print(\"B\"); }\n}",["stAaBb","saAtbB","tsaAbB","staAbB"],3,"Static initializers run first, parent then child (s, t); then for the object, A's instance initializer and constructor run (a, A) before B's (b, B). saAtbB is tempting, but both classes are initialized before the object is created.","Domain 3: Initialization order with inheritance"],
    ["x14",0,3,"What is the output?\nrecord Temp(double c) {\n  Temp {\n    if (c < -273.15) throw new IllegalArgumentException();\n    c = Math.round(c);\n  }\n}\n// in main:\nSystem.out.println(new Temp(21.6).c());",["21.6","22","22.0","It does not compile: c cannot be reassigned"],2,"A compact constructor works on its implicit parameters, and the fields are assigned from them at the end, so the rounded value is stored as the double 22.0. The compile error is tempting because record fields are final, but it is the parameter being changed.","Domain 3: Records and compact constructors"],
    ["x15",0,3,"Given `interface Util { static String tag() { return \"U\"; } }` and `class Impl implements Util {}`, which call compiles?",["Impl.tag()","Util.tag()","new Impl().tag()","Util.super.tag()"],1,"Static methods in interfaces are not inherited, so they must be called through the interface name. Impl.tag() is tempting because static methods of superclasses are inherited, but interfaces follow a different rule.","Domain 3: Static interface methods"],
    ["x16",0,3,"Given `class Animal {}`, `class Dog extends Animal {}` and `class Cat extends Animal {}`, what happens with:\nAnimal a = new Dog();\nCat c = (Cat) a;",["It compiles, then throws ClassCastException","It fails to compile: Dog cannot be cast to Cat","It compiles, and c refers to a Dog object","It compiles, and c is set to null"],0,"Downcasting from Animal to Cat compiles because an Animal variable could hold a Cat, but at run time the object is a Dog, so ClassCastException is thrown. The compile error is tempting, but the compiler does not know the object's real class.","Domain 3: Polymorphism and casting"],
    ["x17",0,3,"What happens?\nint base = 10;\nRunnable r = new Runnable() {\n  public void run() { System.out.println(base + 1); }\n};\nbase = 20;\nr.run();",["It prints 11","It does not compile: base is not effectively final","It prints 21","It throws IllegalStateException"],1,"Local and anonymous classes, like lambdas, may use local variables only if they are final or effectively final, and base = 20 breaks that. Printing 11 is tempting if you think the value is copied at creation, but the compiler rejects the code.","Domain 3: Anonymous classes and captured variables"],
    ["x18",0,3,"`class A { Number get() { return 0; } }`. Which method in `class B extends A` is a valid override?",["Object get() { return 1; }","int get() { return 1; }","long get() { return 1L; }","Integer get() { return 1; }"],3,"Overriding allows covariant return types, so a subclass may return Integer, a subclass of Number. int is tempting because of autoboxing, but return types in overriding must be reference subtypes.","Domain 3: Overriding and covariant returns"],
    ["x19",0,3,"What is the output?\nenum Op {\n  ADD { int apply(int a, int b) { return a + b; } },\n  MUL { int apply(int a, int b) { return a * b; } };\n  abstract int apply(int a, int b);\n}\n// in main:\nint r = 0;\nfor (Op op : Op.values()) r += op.apply(2, 3);\nSystem.out.println(r);",["11","5","6","It does not compile: enums cannot be abstract"],0,"Each constant has its own class body implementing apply(), and values() visits ADD then MUL, giving 5 + 6 = 11. The compile error is tempting, but abstract methods are allowed when every constant implements them.","Domain 3: Enums with methods"],
    ["x20",0,3,"`sealed class Vehicle permits Car, Truck {}`. Third-party code must be able to extend Truck freely. How should Truck be declared?",["final class Truck extends Vehicle {}","sealed class Truck extends Vehicle {}","non-sealed class Truck extends Vehicle {}","class Truck extends Vehicle {}"],2,"non-sealed reopens the hierarchy below Truck, letting any class extend it while Vehicle stays sealed. A plain class is tempting, but a permitted subclass must choose final, sealed or non-sealed.","Domain 3: Sealed classes and non-sealed"],
    ["x21",0,3,"Given `Object o`, which condition fails to compile?",["o instanceof String s && s.isEmpty()","!(o instanceof String s) || s.isEmpty()","o instanceof String s || s.isEmpty()","o instanceof String s ? s.isEmpty() : false"],2,"With ||, the right side runs only when the instanceof test failed, so the pattern variable s is not definitely matched there and is out of scope. The negated form is tempting to reject, but there s is in scope because the right side runs only after a match.","Domain 3: instanceof patterns and flow scoping"],
    ["x22",0,3,"What is the output?\nclass A {}\nclass B extends A {}\nstatic String m(A a) { return \"A\"; }\nstatic String m(B b) { return \"B\"; }\n// in main:\nA x = new B();\nSystem.out.println(m(x));",["A","B","AB","It does not compile: the call is ambiguous"],0,"Overload resolution happens at compile time using the static type of the argument, which is A, so m(A) is chosen. B is tempting because the object is a B, but only overriding uses the run-time type.","Domain 3: Overloading versus overriding"],
    ["x23",0,3,"What is the output?\ninterface I { default String who() { return \"I\"; } }\nclass P { public String who() { return \"P\"; } }\nclass C extends P implements I {}\n// in main:\nSystem.out.println(new C().who());",["I","PI","It does not compile: who() is inherited twice","P"],3,"When a class inherits a concrete method from its superclass and a default method with the same signature from an interface, the class method wins. The compile error is tempting because two defaults would conflict, but a superclass method is not a default.","Domain 3: Default methods and the class-wins rule"],
    ["x24",0,3,"What is the output?\nrecord R(int[] a) {}\n// in main:\nint[] arr = {1};\nSystem.out.println(new R(arr).equals(new R(arr)) + \" \"\n  + new R(new int[]{1}).equals(new R(new int[]{1})));",["true true","true false","false false","false true"],1,"A record's generated equals() compares reference components with their equals(), and arrays use identity, so the shared array is equal but two separate arrays are not. true true is tempting if you expect records to compare array contents.","Domain 3: Records and equals()"],
    ["x25",0,4,"What does `System.out.println(f());` print?\nstatic int f() {\n  try { throw new RuntimeException(); }\n  finally { return 2; }\n}",["A RuntimeException reaches the caller","Nothing: it does not compile (missing return)","2, followed by a stack trace","2"],3,"When finally completes abruptly with return, the exception thrown in try is discarded and the method returns 2. Expecting the RuntimeException to propagate is tempting, which is exactly why returning from finally is considered bad practice.","Domain 4: try/finally flow"],
    ["x26",0,4,"A recursive method recurses too deeply inside `try { ... } catch (Exception e) { log(e); }`. What happens?",["The catch block logs it as an Exception","The StackOverflowError is not caught and propagates","The JVM retries the call with a larger stack","The error is converted to a RuntimeException"],1,"StackOverflowError is a subclass of Error, which sits beside Exception under Throwable, so catch (Exception e) does not catch it. Expecting the catch block to log it is tempting because it looks like a catch-all.","Domain 4: The Throwable hierarchy"],
    ["x27",0,4,"Why does this fail to compile?\ncatch (IOException | SQLException e) {\n  e = new IOException(\"retry\");\n}",["IOException and SQLException are related types","A multi-catch block must rethrow the exception","A multi-catch parameter is implicitly final","SQLException is unchecked, so it cannot be caught"],2,"The parameter of a multi-catch clause is implicitly final, so assigning a new value to e is a compile error. The related-types rule is tempting, but it only applies when one type is a subclass of the other, which is not the case here.","Domain 4: Multi-catch rules"],
    ["x28",0,4,"`BufferedReader br = Files.newBufferedReader(p);` is declared earlier and never reassigned. Which try-with-resources header compiles?",["try (br) { ... }","try (BufferedReader copy) { ... }","try (Files.newBufferedReader(p)) { ... }","try (var copy) { ... }"],0,"Since Java 9, a try-with-resources header can name an existing final or effectively final variable, so try (br) is valid. try (Files.newBufferedReader(p)) is tempting, but only variable names or declarations with initializers are allowed.","Domain 4: try-with-resources"],
    ["x29",0,4,"Files.readString(Path) declares IOException. What happens with this code in a method that has no throws clause?\npaths.forEach(p -> System.out.println(Files.readString(p)));",["It does not compile: the lambda cannot throw IOException","It compiles; forEach wraps it in UncheckedIOException","It compiles; the exception is silently ignored","It compiles, but only as a parallel stream"],0,"Consumer.accept() does not declare checked exceptions, so a lambda that may throw IOException must catch it itself. Expecting forEach to wrap it in UncheckedIOException is tempting, but that wrapping must be written by hand.","Domain 4: Checked exceptions and lambdas"],
    ["x30",0,5,"What is the output?\nint[] a = {2, 4, 6, 8};\nSystem.out.println(Arrays.binarySearch(a, 6) + \" \" + Arrays.binarySearch(a, 5));",["2 -1","3 -3","2 -3","2 -2"],2,"binarySearch returns the index of a found key (6 at index 2), and for a missing key returns -(insertion point) - 1; 5 would go at index 2, so the result is -3. -2 is tempting if you forget the extra -1.","Domain 5: Arrays.binarySearch"],
    ["x31",0,5,"What is the output?\nint[] x = {1, 2, 3}, y = {1, 2, 4};\nSystem.out.println(Arrays.mismatch(x, y) + \" \" + Arrays.compare(x, y));",["3 -1","2 -1","2 1","-1 -1"],1,"mismatch() returns the first index where the arrays differ, 2, and compare() compares that pair, 3 and 4, returning a negative value. -1 for mismatch is tempting, but that value means no mismatch at all.","Domain 5: Arrays.mismatch and Arrays.compare"],
    ["x32",0,5,"What is the output?\nMap<String, List<Integer>> m = new HashMap<>();\nm.computeIfAbsent(\"a\", k -> new ArrayList<>()).add(1);\nm.computeIfAbsent(\"a\", k -> new ArrayList<>()).add(2);\nSystem.out.println(m);",["{a=[2]}","{a=[1]}","{}","{a=[1, 2]}"],3,"computeIfAbsent() creates and stores a list only when the key is missing, and it returns the stored list both times, so both values end up in it. {a=[2]} is tempting if you think the list is replaced on each call.","Domain 5: Map.computeIfAbsent"],
    ["x33",0,5,"What is the output?\nMap<String, Integer> m = new HashMap<>();\nm.put(\"x\", null);\nm.putIfAbsent(\"x\", 5);\nSystem.out.println(m.get(\"x\") + \" \" + m.getOrDefault(\"y\", 0));",["5 0","null 0","5 null","null null"],0,"putIfAbsent() replaces a null value as if the key were missing, so x maps to 5, and getOrDefault() returns 0 for the missing key y. null 0 is tempting because the key x already existed.","Domain 5: putIfAbsent and getOrDefault"],
    ["x34",0,5,"Inside `static void fill(List<? super Integer> list)`, which statement compiles?",["Integer i = list.get(0);","list.add(42);","Number n = list.get(0);","list.add(4.2);"],1,"With ? super Integer, the list accepts Integer values, but reading gives only Object because the element type could be Number or Object. Number n = list.get(0) is tempting, but the lower bound says nothing about what the list holds.","Domain 5: Lower-bounded wildcards"],
    ["x35",0,5,"What happens when this line runs?\nSet<String> s = Set.of(\"a\", \"b\", \"a\");",["s contains a and b","It does not compile","IllegalArgumentException is thrown","s contains a, b and a"],2,"Set.of() throws IllegalArgumentException when given duplicate elements. Ending up with a and b is tempting because that is what adding to a HashSet does, but the factory method is stricter.","Domain 5: Unmodifiable collections"],
    ["x36",0,6,"Which functional interface matches the lambda `() -> LocalDate.now()`, which takes no arguments and returns a value?",["Consumer<LocalDate>","Function<Void, LocalDate>","Predicate<LocalDate>","Supplier<LocalDate>"],3,"Supplier<T> has get(), which takes no arguments and returns a T, exactly the shape of this lambda. Function<Void, LocalDate> is tempting, but its apply() still requires one parameter.","Domain 6: Functional interfaces"],
    ["x37",0,6,"Which declaration compiles with the method reference String::length?",["Supplier<Integer> f = String::length;","Predicate<String> f = String::length;","Function<String, Integer> f = String::length;","UnaryOperator<String> f = String::length;"],2,"String::length used without a receiver takes a String and returns its length, which fits Function<String, Integer> through autoboxing. Supplier<Integer> is tempting, but it would need a bound receiver such as \"abc\"::length.","Domain 6: Method references"],
    ["x38",0,6,"What is the output?\nSystem.out.println(Stream.iterate(1, n -> n < 20, n -> n * 3).toList());",["[1, 3, 9, 27]","[3, 9]","[3, 9, 27]","[1, 3, 9]"],3,"The three-argument iterate() starts with the seed, stops as soon as the predicate fails, and applies the operator between elements, giving 1, 3 and 9. Including 27 is tempting, but it is tested before being emitted.","Domain 6: Stream.iterate"],
    ["x39",0,6,"What is the output?\nSystem.out.println(Stream.of(5, 3, 5, 1, 3, 9)\n  .distinct().sorted().limit(3).toList());",["[1, 3, 5]","[1, 3, 3]","[5, 3, 1]","[1, 1, 3]"],0,"distinct() leaves 5, 3, 1, 9, sorted() orders them as 1, 3, 5, 9, and limit(3) keeps the first three. [5, 3, 1] is tempting if you forget that sorted() comes before limit().","Domain 6: Intermediate operations"],
    ["x40",0,6,"What is the output?\nstatic String fb() { System.out.print(\"F\"); return \"d\"; }\n// in main:\nString r = Optional.of(\"v\").orElse(fb())\n  + Optional.of(\"w\").orElseGet(() -> fb());\nSystem.out.println(r);",["vw","Fvw","FFvw","Fvd"],1,"An argument to orElse() is evaluated before the call even when a value is present, so F is printed once, while orElseGet() runs its supplier only for an empty Optional. vw is tempting if you assume orElse() is lazy.","Domain 6: Optional orElse versus orElseGet"],
    ["x41",0,6,"What is the output?\nIntSummaryStatistics st = IntStream.of(4, 8, 6).summaryStatistics();\nSystem.out.println(st.getMin() + \" \" + st.getAverage() + \" \" + st.getCount());",["4 6 3","4 6.0 3","4 6.0 18","8 6.0 3"],1,"IntSummaryStatistics reports min as an int, average as a double and count as a long, so the line is 4 6.0 3. 4 6 3 is tempting, but the average is always a double.","Domain 6: Primitive streams and summary statistics"],
    ["x42",0,6,"What is the output?\nMap<Character, Integer> m = Stream.of(\"apple\", \"avocado\", \"banana\")\n  .collect(Collectors.toMap(s -> s.charAt(0), String::length, Integer::sum));\nSystem.out.println(m.get('a'));",["12","7","5","An IllegalStateException is thrown"],0,"The third argument to toMap() resolves duplicate keys, so apple (5) and avocado (7) both map to 'a' and are summed to 12. The exception is tempting because it happens without a merge function.","Domain 6: Collectors.toMap with a merge function"],
    ["x43",0,6,"What is the output?\nSystem.out.println(Stream.of(1, 2, 3, 4)\n  .gather(Gatherers.windowSliding(3)).toList());",["[[1, 2, 3], [4]]","[[1, 2], [2, 3], [3, 4]]","[[1, 2, 3]]","[[1, 2, 3], [2, 3, 4]]"],3,"windowSliding(3) emits overlapping windows of three elements, moving one step each time, so four elements give two windows. [[1, 2, 3], [4]] is tempting, but that is what windowFixed(3) produces.","Domain 6: Stream gatherers"],
    ["x44",0,6,"What is the output?\nSystem.out.println(Stream.of(1, 2, 3)\n  .gather(Gatherers.scan(() -> 0, Integer::sum)).toList());",["[0, 1, 3, 6]","[6]","[1, 3, 6]","[1, 2, 3]"],2,"Gatherers.scan() emits a running result after each element, starting from the initial value 0, giving 1, 3 and 6. [6] is tempting, but that is the single result produced by Gatherers.fold().","Domain 6: Stream gatherers"],
    ["x45",0,6,"`List<Integer> out = new ArrayList<>(); IntStream.range(0, 10_000).parallel().forEach(out::add);` sometimes leaves fewer than 10,000 elements or throws. What is the BEST fix?",["Declare out as final","Declare out as a volatile field","Replace the ArrayList with a LinkedList","Collect instead: IntStream.range(0, 10_000).parallel().boxed().toList()"],3,"Adding to a shared non-thread-safe list from a parallel stream is a stateful side effect; letting the stream collect its own result avoids the race. volatile is tempting, but it only affects the reference, not the list's internal state.","Domain 6: Parallel streams and side effects"],
    ["x46",0,7,"A plain JAR named commons-text-1.10.jar, with no module-info and no Automatic-Module-Name manifest entry, is placed on the module path. What module name does it get?",["commons-text","commons.text.1.10","commons.text","It joins the unnamed module"],2,"An automatic module's name comes from the JAR file name with the version and .jar removed and non-alphanumeric characters replaced by dots, giving commons.text. The unnamed module is tempting, but that applies only to the class path.","Domain 7: Automatic modules"],
    ["x47",0,7,"Code in a named module needs types from a JAR that is on the class path, not the module path. What happens?",["It works: the unnamed module exports all its packages","Compilation fails: named modules cannot read the unnamed module","It works once the module adds requires unnamed;","It works only after the app is packaged with jlink"],1,"Class-path code lives in the unnamed module, which named modules cannot declare a dependency on, so the types are not accessible. The first distractor is tempting because the unnamed module does export all its packages, but only other unnamed and automatic modules read it.","Domain 7: Module path versus class path"],
    ["x48",0,7,"Before modularizing a legacy JAR, a team wants a report of which packages and JDK modules it depends on, including JDK internal APIs. Which tool is designed for this?",["jdeps","jlink","jshell","jcmd"],0,"jdeps analyses class files and JARs and lists their package and module dependencies, and can flag uses of JDK internals. jlink is tempting because it also works with modules, but it assembles runtime images.","Domain 7: JDK tools"],
    ["x49",0,7,"Modules are compiled into the mods folder. Which command runs main class com.shop.App in module shop?",["java --module-path mods --module com.shop.App/shop","java --module-path mods --module shop/com.shop.App","java -cp mods shop/com.shop.App","java --module-path mods com.shop.App"],1,"The --module (or -m) option takes module/mainclass, and --module-path (or -p) says where the modules are. Swapping the order is the tempting slip, but the module name always comes first.","Domain 7: Running modules"],
    ["x50",0,8,"What happens?\nThread t = new Thread(() -> {});\nt.start();\nt.start();",["The task runs twice on the same thread","The second call is silently ignored","It does not compile","The second start() throws IllegalThreadStateException"],3,"A thread can be started only once, and calling start() again throws IllegalThreadStateException. Expecting the task to run twice is tempting, but a new Thread object is needed for each run.","Domain 8: Thread lifecycle"],
    ["x51",0,8,"Which statement starts a new virtual thread running task?",["Thread.ofVirtual().start(task);","new VirtualThread(task).start();","Thread.ofPlatform().virtual().start(task);","Executors.newFixedThreadPool(1).submit(task);"],0,"Thread.ofVirtual() returns a builder for virtual threads, and start(task) creates and starts one. The fixed thread pool is tempting because it runs the task, but its workers are platform threads.","Domain 8: Virtual threads and Thread.Builder"],
    ["x52",0,8,"Class Counter has `synchronized void inc()` and `static synchronized void reset()`. Thread 1 calls c.inc() while Thread 2 calls Counter.reset(). What is true?",["They block each other because both are synchronized","reset() waits until every Counter object is unlocked","They can run at the same time because they lock different objects","It does not compile: a class cannot mix both kinds"],2,"An instance synchronized method locks this, while a static synchronized method locks Counter.class, so the two can run concurrently. Expecting them to block each other is tempting, which is why shared data touched by both needs a common lock.","Domain 8: synchronized methods"],
    ["x53",0,8,"A ConcurrentHashMap<String, String> named cache receives `cache.put(\"k\", null);`. What happens?",["The key is stored with a null value","Any existing mapping for k is removed","A NullPointerException is thrown","It does not compile"],2,"ConcurrentHashMap rejects null keys and values, so put() throws NullPointerException. Storing a null value is tempting because HashMap allows it, but a null would be ambiguous with a missing key in concurrent code.","Domain 8: Concurrent collections"],
    ["x54",0,8,"Two threads keep reacting to each other by releasing and retrying locks, so both stay busy but neither completes its work. What is this called?",["Livelock","Deadlock","Starvation","Race condition"],0,"In a livelock threads stay active and keep changing state in response to each other but make no progress. Deadlock is tempting, but deadlocked threads are blocked, not busy.","Domain 8: Deadlock, starvation and livelock"],
    ["x55",0,9,"What is the output?\nPath p = Path.of(\"/home/ann/docs/a.txt\");\nSystem.out.println(p.getFileName() + \" \" + p.getParent().getFileName() + \" \" + p.getNameCount());",["a.txt docs 5","a.txt /home/ann/docs 4","a.txt ann 4","a.txt docs 4"],3,"getFileName() returns the last element, the parent is /home/ann/docs whose file name is docs, and the root is not counted, so there are 4 names. 5 is tempting if you count the root as an element.","Domain 9: Path operations"],
    ["x56",0,9,"A program calls Files.delete(Path.of(\"reports\")) on a directory that still contains files. What happens?",["The directory and its contents are deleted","DirectoryNotEmptyException is thrown","The call returns false","Only the files inside are deleted"],1,"Files.delete() removes only empty directories and throws DirectoryNotEmptyException otherwise. Returning false is tempting because the older File.delete() works that way, but the NIO method throws.","Domain 9: Files methods"],
    ["x57",0,9,"A program calls Files.move(src, dst) with no options, and dst already exists. What happens, and how can it overwrite dst instead?",["dst is overwritten silently; no option is needed","It returns false; pass StandardCopyOption.ATOMIC_MOVE","src is renamed to dst(1); pass LinkOption.NOFOLLOW_LINKS","FileAlreadyExistsException; pass StandardCopyOption.REPLACE_EXISTING"],3,"Files.move() throws FileAlreadyExistsException if the target exists, unless REPLACE_EXISTING is given. ATOMIC_MOVE is tempting, but it only requires the move to happen as one atomic file system operation.","Domain 9: Files.copy and Files.move"],
    ["x58",0,9,"A loop reads a text file with BufferedReader.readLine(). How does it detect the end of the file?",["readLine() returns an empty string","readLine() returns null","readLine() throws EOFException","readLine() returns -1"],1,"readLine() returns null when no more lines are available, so the usual loop is while ((line = br.readLine()) != null). -1 is tempting, but that is what the single-character read() method returns.","Domain 9: BufferedReader"],
    ["x59",0,10,"What is the output?\nSystem.out.println(MessageFormat.format(\"{1} owes {0}\", \"Ann\", \"Bob\"));",["Ann owes Bob","{1} owes Ann","Bob owes Ann","Bob owes {0}"],2,"MessageFormat placeholders are zero-based argument indexes, so {1} is Bob and {0} is Ann. Ann owes Bob is tempting if you assume arguments fill placeholders left to right.","Domain 10: MessageFormat"],
    ["x60",0,10,"What is the output?\nSystem.out.println(NumberFormat.getInstance(Locale.GERMANY).format(1234.5));",["1.234,5","1,234.5","1 234,5","1234,5"],0,"The German locale uses a period for grouping and a comma as the decimal separator, giving 1.234,5. 1 234,5 is tempting because some European locales, such as French, group with a space.","Domain 10: Formatting numbers with NumberFormat"]
  ],
  whys: {
    "x1": [null,"46 treats 010 as decimal ten, but a leading zero makes it an octal literal worth 8.","The octal literal 010 is 8, not 0, so it must be added to the total.","Binary (0b), hex (0x) and octal (leading 0) literals are all valid int literals."],
    "x2": ["Underscores between digits are allowed, so 1_000_000 is a valid int literal.",null,"Underscores may separate binary digits, so 0b1010_1010 is valid.","An underscore between digits followed by the L suffix is valid."],
    "x3": ["Period.between() reports whole months first and then leftover days, not only a day count.","After adding one month (to 28 February) one day remains, so the period is not exactly one month.",null,"There are 29 days between these dates, and the period is expressed in months and days anyway."],
    "x4": ["Duration works with time-based units; with two LocalDate values it throws UnsupportedTemporalTypeException.","Instant has no between() method, and LocalDate values are not instants.","compareTo() only says which date is earlier; it does not give years, months and days.",null],
    "x5": ["trim() removes only characters up to U+0020, so the em space (U+2003) at each end stops it.","strip() uses Unicode whitespace rules and does remove the em spaces, leaving hi.",null,"The results are reversed: trim() is the one that keeps the Unicode spaces."],
    "x6": ["replace() works on the already shortened text 03456, so positions 2 to 4 hold 45, not 12.","The end index of replace() is exclusive, so both 4 and 5 are replaced by X.","delete(1, 3) removes 1 and 2 first, so 3 moves to index 1.",null],
    "x7": [null,"A byte holds only -128 to 127, so 130 cannot survive the narrowing cast.","Casting 66 to char produces the character B, and string concatenation prints it as B.","An explicit cast allows narrowing, so the code compiles."],
    "x8": ["15 is also divisible by 5, so the inner ternary picks FB, not F.",null,"15 is divisible by 3, so the outer condition is true and N is not chosen.","Nested ternary operators without parentheses are legal and group to the right."],
    "x9": ["The language deliberately allows if (false) so code can be switched off, so it compiles.",null,"A do-while body runs at least once, so it is reachable and compiles.","i < 0 is not a constant expression, so the compiler treats the body as reachable."],
    "x10": [null,"Local variables get no default value, so there is no 0 to print.","x is assigned only when flag is true, and the compiler does not assume that.","An int is a primitive and cannot be null; the problem is caught at compile time."],
    "x11": ["The guard x > y is false for 2 and 3, so the first case does not match.","The parentheses make x + y an int addition before concatenation, giving 5.","The second record pattern matches any P, so default is not reached.",null],
    "x12": ["v is a copy of each element, so doubling it does not change the array.","No element of the array is changed, including the first.",null,"Assigning to the enhanced for variable is legal; it just has no effect on the array."],
    "x13": ["Instance initializer blocks run before the constructor body of the same class.","Both classes are initialized, parent first, before any instance code runs.","A superclass is initialized before its subclass, so s comes before t.",null],
    "x14": ["In a compact constructor the parameter can be reassigned, and its final value is stored in the field.","c is a double component, so the rounded long is widened back and prints as 22.0.",null,"Compact constructor parameters are not final; reassigning them is the intended way to normalize values."],
    "x15": ["Static interface methods are not inherited by implementing classes, so Impl.tag() is not found.",null,"An object of an implementing class cannot call the interface's static method.","Interface.super syntax calls default methods from an implementing class, not static methods."],
    "x16": [null,"The compiler checks only the declared type Animal, which could hold a Cat, so the cast is allowed.","The JVM checks the real object's class at run time and refuses to treat a Dog as a Cat.","A failed cast throws an exception; it never produces null."],
    "x17": ["base is reassigned, so it is not effectively final and the anonymous class cannot capture it.",null,"The value captured would never be the later 20, and in any case the code does not compile.","This is a compile-time rule, not a run-time check."],
    "x18": ["The return type may be narrowed, not widened; Object is a supertype of Number.","A primitive int is not a subtype of Number, so this is an incompatible return type.","long is a primitive, and a primitive return type cannot override a reference type.",null],
    "x19": [null,"values() returns every constant, so MUL's result is added too.","ADD also runs, contributing 5 before MUL adds 6.","An enum may declare an abstract method as long as every constant supplies a body."],
    "x20": ["final stops every subclass, which is the opposite of what is needed.","A sealed Truck would itself restrict its subclasses to a permitted list.",null,"A permitted subclass must say final, sealed or non-sealed, so this does not compile."],
    "x21": ["With &&, the right side runs only when the match succeeded, so s is in scope.","The right side of || runs only when the left is false, which means the match succeeded, so s is in scope.",null,"The true branch of the conditional runs only after a successful match, so s is in scope."],
    "x22": [null,"Overloads are chosen at compile time from the declared type A, not the object's run-time class.","Only one overload runs for a single call.","Only m(A) accepts an expression of type A, so there is no ambiguity."],
    "x23": ["A method inherited from a class takes priority over an interface default method.","Only one who() runs; nothing combines the two results.","The class-wins rule resolves the conflict, so C needs no override.",null],
    "x24": ["Record equals() compares an array component with the array's own equals(), which is identity, so equal contents do not count.",null,"Two records holding the very same array reference are equal, so the first result is true.","The first pair shares one array, so it is equal; only the second pair differs."],
    "x25": ["A return in finally discards the pending exception, so it never reaches the caller.","The finally block always returns, so the method does complete with a value and compiles.","The exception is discarded completely, so no stack trace is printed.",null],
    "x26": ["StackOverflowError extends Error, not Exception, so catch (Exception e) does not match it.",null,"The JVM never retries a call when the stack overflows.","Errors are not wrapped automatically; the StackOverflowError itself propagates."],
    "x27": ["SQLException extends Exception, not IOException, so the two types are unrelated and may share a catch.","Rethrowing is optional; the block can handle the exception and continue.",null,"SQLException is a checked exception, and checked exceptions can be caught normally."],
    "x28": [null,"A resource declaration must include an initializer, so a bare declaration is rejected.","A resource must be a declaration or a variable name; an arbitrary method call is not allowed.","var needs an initializer to infer the type, and a resource needs one anyway."],
    "x29": [null,"forEach does no wrapping; UncheckedIOException must be thrown by your own code.","Checked exceptions are never silently ignored; the compiler requires them to be handled.","Parallel execution does not change the compile-time rules for checked exceptions."],
    "x30": ["-1 means the key would go at index 0, but 5 belongs at index 2.","Index 2 holds 6, so the first search returns 2, not 3.",null,"A missing key gives -(insertion point) - 1, so index 2 yields -3, not -2."],
    "x31": ["mismatch() returns a zero-based index, so the third element is index 2.",null,"x has the smaller element (3 against 4) at the first difference, so compare() is negative.","-1 from mismatch() means the arrays are equal, which they are not."],
    "x32": ["The second call finds the existing list and returns it, so no new list replaces it.","The second call adds 2 to the same list that already holds 1.","computeIfAbsent() stores the new list in the map before returning it.",null],
    "x33": [null,"putIfAbsent() treats a key mapped to null as absent, so it stores 5.","getOrDefault() returns the default 0 because y has no mapping.","Both parts differ: x now maps to 5 and y falls back to 0."],
    "x34": ["The list might be a List<Object>, so get() returns Object, which cannot be assigned to Integer.",null,"The list might be a List<Object>, so its elements are not guaranteed to be Numbers.","A Double is not an Integer, and the list might be a List<Integer>."],
    "x35": ["Set.of() rejects duplicate elements instead of silently dropping them.","The duplicate is detected only at run time, so the code compiles.",null,"No Set implementation keeps duplicates, and Set.of() fails anyway."],
    "x36": ["A Consumer takes an argument and returns nothing, which is the opposite shape.","Function.apply() takes one argument, but this lambda takes none.","A Predicate takes an argument and returns a boolean.",null],
    "x37": ["An unbound instance method reference needs the String as an argument, and Supplier.get() takes none.","length() returns an int, not a boolean, so it cannot implement test().",null,"UnaryOperator<String> must return a String, but length() returns an int."],
    "x38": ["27 fails the test n < 20, so the stream ends before it.","The seed 1 is the first element; iterate() does not skip it.","The seed is included and 27 is excluded by the hasNext test.",null],
    "x39": [null,"distinct() removes the repeated 3, so it cannot appear twice.","sorted() runs before limit(), so the result is in ascending order.","There is only one 1 in the source, and duplicates are removed anyway."],
    "x40": ["orElse() takes an already evaluated argument, so fb() runs and prints F even though v is present.",null,"orElseGet() calls its supplier only when the Optional is empty, so fb() runs only once.","The second Optional holds w, so its value is used rather than the fallback d."],
    "x41": ["getAverage() returns a double, so it prints as 6.0.",null,"getCount() returns the number of elements, 3, not their sum.","getMin() returns the smallest value, 4; 8 is the maximum."],
    "x42": [null,"The merge function sums the values; it does not keep the last one.","The merge function combines both a-words rather than keeping the first.","With a merge function, duplicate keys are combined instead of causing an exception."],
    "x43": ["That is windowFixed(3); a sliding window overlaps and moves by one element.","The window size is 3, so each window holds three elements.","The window slides once more to cover 2, 3, 4.",null],
    "x44": ["scan() does not emit the initial value on its own; it emits after combining each element.","A single final result is what Gatherers.fold() gives, not scan().",null,"scan() emits running totals, not the original elements."],
    "x45": ["final stops reassigning the variable but does nothing to make ArrayList thread-safe.","volatile affects visibility of the reference, not concurrent add() calls on the list.","LinkedList is no more thread-safe than ArrayList, so the race remains.",null],
    "x46": ["Hyphens are not legal in module names, so they are replaced with dots.","The trailing version is removed from the file name before the name is derived.",null,"JARs on the module path become automatic modules; the unnamed module is for the class path."],
    "x47": ["The unnamed module does export everything, but a named module has no way to read it.",null,"The unnamed module has no name, so it cannot appear in a requires directive.","jlink links named modules into an image; it cannot fix a missing module dependency."],
    "x48": [null,"jlink builds a custom runtime image; it does not analyse a JAR's dependencies.","jshell is an interactive shell for trying Java code snippets.","jcmd sends diagnostic commands to a running JVM."],
    "x49": ["The module name comes before the slash and the class name after it.",null,"-cp puts the folder on the class path, and module/class syntax is not a class name.","Without --module the launcher looks for com.shop.App on the class path, not in the module."],
    "x50": ["A Thread object can be started only once; it cannot run its task a second time.","The second start() is detected and rejected with an exception, not ignored.","The code is valid Java; the problem is found only at run time.",null],
    "x51": [null,"There is no public VirtualThread class to instantiate; virtual threads come from Thread builders or factories.","The platform builder creates platform threads and has no virtual() method.","A fixed thread pool uses platform threads, not virtual threads."],
    "x52": ["Being synchronized in the same class is not enough; they must use the same monitor to block each other.","A static synchronized method locks only the Class object, not every instance.",null,"A class may freely declare both static and instance synchronized methods."],
    "x53": ["Unlike HashMap, ConcurrentHashMap does not allow null keys or values.","put() does not remove a key; this call fails before changing the map.",null,"The generic types accept null at compile time, so the problem appears only at run time."],
    "x54": [null,"In a deadlock the threads are blocked and waiting, not actively retrying.","Starvation is one thread being denied resources while others progress.","A race condition is a wrong result caused by unsynchronized access to shared data."],
    "x55": ["The root / is not a name element, so there are four names, not five.","getFileName() on the parent returns only its last element, docs.","The parent is /home/ann/docs, whose last element is docs, not ann.",null],
    "x56": ["Files.delete() never deletes recursively; a directory must be empty first.",null,"Files.delete() returns void and reports failure with an exception.","Nothing is deleted; the call fails as a whole."],
    "x57": ["Without REPLACE_EXISTING, move() refuses to overwrite an existing target.","move() reports failure with an exception, and ATOMIC_MOVE makes the move atomic, not overwriting.","move() never invents a new name, and NOFOLLOW_LINKS is about symbolic links.",null],
    "x58": ["An empty string means a blank line, not the end of the file.",null,"readLine() signals end of stream by its return value, not an exception.","readLine() returns a String, so it cannot return the int -1; that is read()."],
    "x59": ["Placeholders refer to arguments by index, so {1} is the second argument, Bob.","{1} has a matching argument, so it is replaced.",null,"{0} has a matching argument, Ann, so it is replaced."],
    "x60": [null,"That is the US format; the German locale swaps the grouping and decimal separators.","German formatting uses a period, not a space, for grouping.","The default number format uses grouping, so a thousands separator appears."]
  },
  levels: {
    "js1": 2, "js2": 2, "js3": 2, "js4": 3, "js5": 2, "js6": 3, "js7": 2, "js8": 1, "js9": 2, "js10": 1,
    "js11": 2, "js12": 2, "js13": 3, "js14": 1, "js15": 3, "js16": 2, "js17": 1, "js18": 1, "js19": 1, "js20": 3,
    "js21": 2, "js22": 2, "js23": 3, "js24": 2, "js25": 2, "js26": 1, "js27": 2, "js28": 2, "js29": 2, "js30": 3,
    "js31": 3, "js32": 1, "js33": 2, "js34": 1, "js35": 1, "js36": 1, "js37": 1, "js38": 2, "js39": 2, "js40": 2,
    "js41": 2, "js42": 3, "js43": 1, "js44": 1, "js45": 1, "js46": 1, "js47": 2, "js48": 2, "js49": 1, "js50": 3,
    "js51": 3, "js52": 2, "js53": 3, "js54": 2, "js55": 3, "js56": 2, "js57": 1, "js58": 2, "js59": 1, "js60": 1,
    "js61": 1, "js62": 1, "js63": 1, "js64": 1, "js65": 1, "js66": 1, "js67": 1, "js68": 2, "js69": 1, "js70": 1,
    "js71": 1, "js72": 3, "js73": 2, "js74": 1, "js75": 3, "js76": 1, "js77": 1, "js78": 2, "js79": 2, "js80": 3,
    "js81": 2, "js82": 3, "js83": 1, "js84": 2, "js85": 2, "js86": 1, "js87": 1, "js88": 1, "js89": 3, "js90": 1,
    "js91": 1, "js92": 2, "x1": 2, "x2": 1, "x3": 3, "x4": 2, "x5": 3, "x6": 2, "x7": 2, "x8": 2,
    "x9": 3, "x10": 2, "x11": 3, "x12": 2, "x13": 3, "x14": 3, "x15": 2, "x16": 2, "x17": 2, "x18": 2,
    "x19": 3, "x20": 2, "x21": 3, "x22": 3, "x23": 3, "x24": 3, "x25": 3, "x26": 2, "x27": 2, "x28": 3,
    "x29": 3, "x30": 3, "x31": 2, "x32": 2, "x33": 3, "x34": 3, "x35": 2, "x36": 1, "x37": 2, "x38": 2,
    "x39": 2, "x40": 3, "x41": 2, "x42": 2, "x43": 2, "x44": 3, "x45": 3, "x46": 3, "x47": 3, "x48": 1,
    "x49": 2, "x50": 2, "x51": 1, "x52": 3, "x53": 2, "x54": 1, "x55": 2, "x56": 2, "x57": 2, "x58": 1,
    "x59": 2, "x60": 2
  }
});
