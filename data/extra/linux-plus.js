/* More linux-plus practice questions with wrong-answer notes, and a difficulty level for every question. Merged into the free bank by tools/build.js. */
CertHub.addExtra("linux-plus", {
  questions: [
    ["x1", 0, 1, "A Linux server will act as a router, so IPv4 forwarding must be enabled and must stay enabled after every reboot. What should the admin do?", ["echo 1 > /proc/sys/net/ipv4/ip_forward", "sysctl -w net.ipv4.ip_forward=1", "Set net.ipv4.ip_forward = 1 in /etc/sysctl.d/", "Load the ip_forward module in /etc/modules-load.d/"], 2, "Settings in a file under /etc/sysctl.d/ are applied at every boot (and immediately with sysctl --system). sysctl -w is tempting because it does enable forwarding, but only in the running kernel, so it is lost at reboot.", "Domain 1: Kernel parameters"],
    ["x2", 0, 1, "A workstation must never load the nouveau graphics module, because the vendor driver is used instead. Which change keeps nouveau from loading at boot?", ["Run rmmod nouveau after each boot", "Run modprobe -r nouveau once", "Add nouveau to a file in /etc/modules-load.d/", "blacklist nouveau in /etc/modprobe.d/"], 3, "A blacklist line in a file under /etc/modprobe.d/ stops the module from being loaded automatically (rebuild the initramfs if the module is included there). modprobe -r is tempting, but it only unloads the module from the running kernel until the next boot.", "Domain 1: Kernel modules"],
    ["x3", 0, 1, "An admin needs to see exactly which kernel command-line parameters the server used for the current boot. Which command shows this?", ["cat /proc/cmdline", "cat /etc/default/grub", "cat /boot/grub2/grub.cfg", "uname -a"], 0, "/proc/cmdline shows the parameters the running kernel actually booted with. /etc/default/grub is tempting, but it holds settings for future GRUB configs and may not match what was used, for example if parameters were edited at the GRUB menu.", "Domain 1: Boot process"],
    ["x4", 0, 1, "A team wants a Linux filesystem with built-in subvolumes and copy-on-write snapshots so they can snapshot before upgrades without using LVM. Which filesystem fits?", ["ext4", "Btrfs", "XFS", "tmpfs"], 1, "Btrfs is a copy-on-write filesystem with native subvolumes and snapshots. XFS is tempting as a modern, scalable filesystem, but it has no built-in subvolume snapshots; snapshots would need LVM underneath.", "Domain 1: Filesystems"],
    ["x5", 0, 1, "An admin ran lvextend -L +20G on /dev/vg_data/lv_data but forgot the -r option. The ext4 filesystem mounted on /data still shows the old size in df. Which command fixes this?", ["xfs_growfs /data", "mkfs.ext4 /dev/vg_data/lv_data", "resize2fs /dev/vg_data/lv_data", "pvresize /dev/sdb"], 2, "resize2fs grows an ext4 filesystem to fill its now-larger logical volume, and it can do so while mounted. xfs_growfs is tempting because it also grows filesystems, but it only works on XFS, not ext4.", "Domain 1: LVM and filesystems"],
    ["x6", 0, 1, "Two new empty disks, /dev/sdb and /dev/sdc, must be mirrored with Linux software RAID so the data survives one disk failing. Which command creates the array?", ["mdadm --create /dev/md0 --level=0 --raid-devices=2 /dev/sdb /dev/sdc", "mdadm --create /dev/md0 --level=linear --raid-devices=2 /dev/sdb /dev/sdc", "mdadm --assemble /dev/md0 /dev/sdb /dev/sdc", "mdadm --create /dev/md0 --level=1 --raid-devices=2 /dev/sdb /dev/sdc"], 3, "RAID 1 mirrors data across both disks, so the array survives one disk failing. RAID 0 is tempting because it uses the same command, but it stripes data with no redundancy, so losing either disk loses everything.", "Domain 1: Software RAID"],
    ["x7", 0, 1, "The /var filesystem is filling up. The admin wants to see which top-level directory under /var uses the most space, sorted by size. Which command fits best?", ["du -sh /var/* | sort -h", "df -h /var", "ls -lS /var", "lsblk /var"], 0, "du -sh totals the space used by each directory, and sort -h orders the human-readable sizes. df -h is tempting, but it shows only the usage of the whole filesystem, not which directories use it.", "Domain 1: Filesystems"],
    ["x8", 0, 1, "A file named report.pdf will not open in a PDF viewer. The admin wants to know what kind of data the file really contains, regardless of its extension. Which command should they use?", ["stat report.pdf", "file report.pdf", "ls -l report.pdf", "touch report.pdf"], 1, "file inspects the content (magic bytes) and reports the actual data type, such as ASCII text or a gzip archive. stat is tempting because it reports details about the file, but only metadata like size, inode and timestamps, not the content type.", "Domain 1: Files and directories"],
    ["x9", 0, 1, "A deployment needs /opt/app/current to point to the directory /opt/app/releases/v2, so it can be switched to a new release later. Which command creates it?", ["ln /opt/app/releases/v2 /opt/app/current", "cp -r /opt/app/releases/v2 /opt/app/current", "ln -s /opt/app/releases/v2 /opt/app/current", "mv /opt/app/current /opt/app/releases/v2"], 2, "ln -s creates a symbolic link, which can point to a directory and be repointed later. A plain ln is tempting, but it creates a hard link, and hard links to directories are not allowed.", "Domain 1: Files and directories"],
    ["x10", 0, 1, "Each line of access.log has space-separated fields. The admin wants to print only the 1st and 3rd fields of every line. Which command does this?", ["cut -f1,3 access.log", "sed 's/ /,/g' access.log", "sort -k1,3 access.log", "awk '{print $1, $3}' access.log"], 3, "awk splits lines on whitespace by default, and print $1, $3 outputs the 1st and 3rd fields. cut -f1,3 is tempting, but cut uses tab as its default delimiter, so without -d ' ' it would not split on spaces.", "Domain 1: Shell operations"],
    ["x11", 0, 1, "An admin wants to count how many requests each client IP (field 1) made in access.log and list the busiest IPs first. Which pipeline is correct?", ["awk '{print $1}' access.log | sort | uniq -c | sort -rn", "awk '{print $1}' access.log | uniq -c | sort -rn", "awk '{print $1}' access.log | sort -u | wc -l", "awk '{print $1}' access.log | sort | uniq -d"], 0, "uniq -c only counts adjacent duplicates, so the IPs must be sorted first; sort -rn then puts the highest counts on top. Skipping the first sort is tempting, but then the same IP appearing in separate places is counted several times.", "Domain 1: Shell operations"],
    ["x12", 0, 1, "Every occurrence of http_port=80 in app.conf must be changed to http_port=8080, and the file itself must be updated. Which command does this?", ["sed 's/http_port=80/http_port=8080/g' app.conf", "sed -i 's/http_port=80/http_port=8080/g' app.conf", "grep -i 'http_port=8080' app.conf", "tr 'http_port=80' 'http_port=8080' < app.conf > app.conf"], 1, "sed -i edits the file in place, and the s///g expression replaces every match. The same sed without -i is tempting, but it only prints the changed text to the screen and leaves the file unchanged.", "Domain 1: Shell operations"],
    ["x13", 0, 1, "An admin edited a YAML file in /etc/netplan on a remote Ubuntu server. They want to apply it but have it roll back automatically if they lose connectivity and cannot confirm. Which command should they use?", ["netplan generate", "systemctl restart systemd-resolved", "netplan try", "nmcli connection reload"], 2, "netplan try applies the configuration and reverts it after a timeout unless the admin confirms. netplan generate is tempting because it processes the YAML, but it only writes backend config and does not apply it or offer a rollback.", "Domain 1: Network configuration"],
    ["x14", 0, 1, "An admin needs an archive of /var/log/app compressed with xz. Which command creates it?", ["tar -czf logs.tar.xz /var/log/app", "tar -cjf logs.tar.xz /var/log/app", "tar -xJf logs.tar.xz /var/log/app", "tar -cJf logs.tar.xz /var/log/app"], 3, "In GNU tar, -c creates an archive and -J selects xz compression. -czf is tempting because it is the most common form, but -z means gzip; naming the file .xz does not change the compression.", "Domain 1: Backup and restore"],
    ["x15", 0, 2, "Every new user account should start with a company-standard .bashrc that includes common aliases. Where should the admin place this file?", ["/etc/skel", "/etc/profile.d", "/etc/login.defs", "/root"], 0, "useradd -m copies the contents of /etc/skel into each new home directory. /etc/profile.d is tempting because it customizes shells, but its scripts run for all login shells system-wide and are not copied into new home directories.", "Domain 2: Account files"],
    ["x16", 0, 2, "An employee has left. Their account, home directory and mail spool must all be removed from the server. Which command does this?", ["usermod -L jdoe", "userdel -r jdoe", "userdel jdoe", "passwd -d jdoe"], 1, "userdel -r deletes the account together with its home directory and mail spool. Plain userdel is tempting, but it removes only the account and leaves the home directory and files behind.", "Domain 2: Local accounts"],
    ["x17", 0, 2, "A security review requires that rpcbind.service can't be started at all, whether manually or when another unit pulls it in as a dependency. Which command meets this?", ["systemctl disable rpcbind.service", "systemctl stop rpcbind.service", "systemctl mask rpcbind.service", "systemctl daemon-reload"], 2, "Masking links the unit to /dev/null so it cannot be started by anyone or anything until unmasked. disable is tempting, but it only removes boot-time start; the service can still be started manually or as a dependency.", "Domain 2: systemd units"],
    ["x18", 0, 2, "A maintenance script must run exactly once, at 23:00 tonight, and never again. Which approach fits best?", ["Add 0 23 * * * to the crontab", "Run systemctl start --at 23:00 job.service", "Run nohup /usr/local/bin/job.sh 23:00 &", "Pipe /usr/local/bin/job.sh into at 23:00"], 3, "The at command schedules a one-time job at the given time. A crontab entry is tempting because it runs at 23:00, but 0 23 * * * repeats every day rather than once.", "Domain 2: Scheduling"],
    ["x19", 0, 2, "An admin connected over SSH must start a long import job that has to keep running after they log out, and no terminal multiplexer is installed. Which command fits?", ["nohup ./import.sh &", "./import.sh &", "bg ./import.sh", "fg ./import.sh"], 0, "nohup makes the command ignore the hangup signal sent when the session ends, and & runs it in the background. Just adding & is tempting, but a plain background job can still get SIGHUP and stop when the SSH session closes.", "Domain 2: Processes and jobs"],
    ["x20", 0, 2, "An admin wants only error-level and more severe messages from the nginx unit, starting from yesterday. Which command is correct?", ["journalctl -u nginx --since yesterday | grep err", "journalctl -u nginx -p err --since yesterday", "journalctl -k -p err --since yesterday", "journalctl -u nginx -b -1 -p err"], 1, "-u picks the unit, -p err shows priority err and higher (crit, alert, emerg), and --since limits the time. Piping to grep err is tempting, but it matches text, missing critical messages that lack the word and including harmless lines that contain it.", "Domain 2: Logging"],
    ["x21", 0, 2, "An application writes to /var/log/myapp/app.log, which grows forever. It should be rotated weekly with four compressed copies kept. What should the admin do?", ["Add a weekly cron job that deletes the log file", "Set SystemMaxUse in journald.conf", "Create a file in /etc/logrotate.d/ for the log", "Add the log path to /etc/rsyslog.conf"], 2, "A logrotate config in /etc/logrotate.d/ with weekly, rotate 4 and compress handles this. journald's SystemMaxUse is tempting because it limits log size, but it applies only to the systemd journal, not to an app's own log files.", "Domain 2: Logging"],
    ["x22", 0, 2, "All syslog messages must be forwarded from a server to the central log host 10.0.0.50 over UDP port 514. Which rsyslog change does this?", ["Add *.* @@10.0.0.50:514 to the rsyslog config", "Add 10.0.0.50 to logrotate's postrotate script", "Run journalctl --forward-to 10.0.0.50:514", "Add *.* @10.0.0.50:514 to the rsyslog config"], 3, "In rsyslog, a single @ forwards over UDP and *.* selects every facility and priority. The @@ rule is tempting because it looks almost identical, but a double @@ forwards over TCP, not UDP.", "Domain 2: Logging"],
    ["x23", 0, 2, "On an Ubuntu server, an admin wants to know which installed package provided the file /usr/bin/curl. Which command answers this?", ["dpkg -S /usr/bin/curl", "dpkg -L /usr/bin/curl", "apt show /usr/bin/curl", "apt-mark showmanual curl"], 0, "dpkg -S searches installed packages for the one that owns a given file. dpkg -L is tempting because it is also a file query, but it works the other way, listing the files that belong to a named package.", "Domain 2: Package management"],
    ["x24", 0, 2, "A user needs a newer version of a desktop app than the distro repo offers. It should run sandboxed with its own bundled dependencies and without touching system libraries. What should the admin use?", ["Build it from source with make and install into /usr", "Install it as a Flatpak from a trusted remote", "Download an .rpm built for another distribution", "Run pip install for it system-wide"], 1, "Flatpak apps bundle their runtimes, run sandboxed and are installed separately from system libraries. Building from source is tempting to get the newest version, but installing into /usr can overwrite or conflict with system files and gives no sandbox.", "Domain 2: Sandboxed packages"],
    ["x25", 0, 2, "A podman container named api exits a few seconds after it starts. The admin wants to see what the application printed before it stopped. Which command should they run?", ["podman ps -a", "podman images", "podman logs api", "podman pull api"], 2, "podman logs shows the stdout and stderr the container produced, even after it has exited. podman ps -a is tempting because it shows the container exited, but it gives only status and exit code, not the application's messages.", "Domain 2: Containers"],
    ["x26", 0, 2, "An app runs in Kubernetes as pods managed by a Deployment. The pods get new IP addresses whenever they are recreated, but other apps need one stable address to reach them. What should be created?", ["A second Deployment", "A ReplicaSet", "A ConfigMap", "A Service"], 3, "A Service gives a stable virtual IP and DNS name that load-balances to the matching pods. A ReplicaSet is tempting because it manages pods, but it only keeps a number of pods running and provides no stable network address.", "Domain 2: Container orchestration"],
    ["x27", 0, 3, "The directory /srv/proj is owned by group projteam. Every new file created in it should automatically belong to group projteam, whoever creates it. Which command does this?", ["chmod g+s /srv/proj", "chmod +t /srv/proj", "chmod u+s /srv/proj", "chown :projteam /srv/proj/*"], 0, "SGID on a directory makes new files inherit the directory's group. The chown option is tempting because it sets the group, but it changes only files that exist now, not ones created later.", "Domain 3: Special permissions"],
    ["x28", 0, 3, "Web content lives in /srv/web on an SELinux-enforcing server. chcon -t httpd_sys_content_t fixed access, but a later restorecon changed the labels back and the 403 errors returned. What is the lasting fix?", ["chcon -R -t httpd_sys_content_t /srv/web", "semanage fcontext -a -t httpd_sys_content_t '/srv/web(/.*)?' then restorecon -Rv /srv/web", "setsebool -P httpd_enable_homedirs on", "setenforce 0"], 1, "semanage fcontext adds a persistent labeling rule to the policy, and restorecon then applies it, so relabels keep the right type. chcon is tempting because it works immediately, but it only changes the current label, which restorecon or a relabel reverts.", "Domain 3: SELinux"],
    ["x29", 0, 3, "On an SELinux-enforcing server, the Apache web app can't connect to a remote PostgreSQL database. ausearch shows AVC denials, and the admin finds a matching boolean. Which command allows the connection permanently?", ["setenforce 0", "chcon -t httpd_t /var/www/html", "setsebool -P httpd_can_network_connect_db on", "semanage port -a -t http_port_t -p tcp 5432"], 2, "The httpd_can_network_connect_db boolean lets httpd connect to database ports, and -P makes it persist across reboots. semanage port is tempting, but labeling 5432 as http_port_t concerns which ports httpd may listen on, not outbound database connections.", "Domain 3: SELinux"],
    ["x30", 0, 3, "With firewalld, all traffic from the management subnet 10.1.1.0/24 should be handled by the internal zone. Which firewall-cmd options bind that subnet to the zone?", ["--zone=internal --add-interface=10.1.1.0/24", "--zone=internal --add-service=10.1.1.0/24", "--zone=internal --add-port=10.1.1.0/24", "--zone=internal --add-source=10.1.1.0/24"], 3, "--add-source assigns traffic from an address or subnet to a zone, whose rules then apply. --add-interface is tempting because it also binds traffic to a zone, but it takes a network interface name like eth0, not a subnet.", "Domain 3: Firewalls"],
    ["x31", 0, 3, "All admins now log in to a server with SSH keys. Password logins over SSH must be turned off. What should be changed?", ["Set PasswordAuthentication no and reload sshd", "Set PermitRootLogin no and reload sshd", "Set PubkeyAuthentication no and reload sshd", "Delete ~/.ssh/authorized_keys and reload sshd"], 0, "PasswordAuthentication no makes sshd refuse password logins while keys keep working. PermitRootLogin no is tempting as a hardening step, but it only blocks root, and other users could still log in with passwords.", "Domain 3: SSH hardening"],
    ["x32", 0, 3, "A junior admin wants to see exactly which commands their account is allowed to run with sudo on a server. Which command shows this?", ["sudo -i", "sudo -l", "su -l", "visudo -c"], 1, "sudo -l lists the sudo rules that apply to the current user. visudo -c is tempting because it relates to sudoers, but it only checks the sudoers files for syntax errors and needs root.", "Domain 3: Privilege escalation"],
    ["x33", 0, 3, "Policy requires new passwords to be at least 14 characters and to include several character classes. Which PAM module enforces this?", ["pam_faillock", "pam_limits", "pam_pwquality", "pam_sss"], 2, "pam_pwquality checks new passwords for length, character classes and other complexity rules. pam_faillock is tempting because it is also an authentication policy, but it locks accounts after failed logins and does not check password strength.", "Domain 3: Authentication"],
    ["x34", 0, 3, "Linux servers must let users sign in with their existing directory (LDAP or Active Directory) accounts and cache credentials so logins work if the directory is briefly unreachable. Which service provides this?", ["nscd", "chronyd", "rsyslog", "SSSD"], 3, "SSSD connects Linux to LDAP, Active Directory and Kerberos identity sources and can cache credentials for offline logins. nscd is tempting because it caches name service lookups, but it does not authenticate users against a directory.", "Domain 3: Authentication"],
    ["x35", 0, 3, "An admin has downloaded an ISO, a SHA256SUMS file and its detached signature SHA256SUMS.sig, and has imported the vendor's public key. What proves that SHA256SUMS really came from the vendor?", ["gpg --verify SHA256SUMS.sig SHA256SUMS", "sha256sum -c SHA256SUMS", "md5sum SHA256SUMS", "openssl rand -hex 32"], 0, "gpg --verify checks the detached signature with the vendor's public key, proving who published the checksum file. sha256sum -c is tempting, but it only compares the ISO with the checksums; a tampered checksum file would still pass.", "Domain 3: Cryptography"],
    ["x36", 0, 3, "A security team wants to find unauthorized changes to binaries in /usr/bin by comparing their current hashes and attributes with a stored baseline database. Which tool is built for this?", ["auditd", "AIDE", "fail2ban", "SSSD"], 1, "AIDE builds a database of file hashes and attributes and reports differences on later checks. auditd is tempting because it is also a security tool, but it logs events as they happen rather than comparing files against a baseline.", "Domain 3: OS hardening"],
    ["x37", 0, 3, "Auditors want every write to /etc/sudoers and every change to its attributes recorded, along with who made it. Which command sets this up?", ["chattr +i /etc/sudoers", "aide --check", "auditctl -w /etc/sudoers -p wa -k sudoers", "getfacl /etc/sudoers"], 2, "An auditd watch with -p wa logs writes and attribute changes, and the key makes events easy to find with ausearch -k. chattr +i is tempting for protecting the file, but it blocks changes instead of recording who made them.", "Domain 3: Compliance and auditing"],
    ["x38", 0, 4, "A script runs correctly with bash deploy.sh, but ./deploy.sh fails with syntax errors on its array lines. The first line is #!/bin/sh. What fixes this?", ["Run chmod +x deploy.sh", "Add set -e near the top", "Rename the file to deploy.bash and run it again", "Change the first line to #!/bin/bash"], 3, "When run as ./deploy.sh, the shebang chooses the interpreter; /bin/sh may be a POSIX shell such as dash that lacks bash arrays. chmod +x is tempting, but the script already runs (it reaches the syntax error), so the execute bit is not the problem.", "Domain 4: Bash scripting"],
    ["x39", 0, 4, "A script runs rm $file, and it misbehaves when the filename contains spaces, trying to delete several wrong names. How should the line be written?", ["rm \"$file\"", "rm $file", "rm '$file'", "rm $(echo $file)"], 0, "Double quotes expand the variable but prevent word splitting, so a name with spaces stays one argument. Single quotes are tempting, but they stop the expansion altogether, so rm looks for a file literally named $file.", "Domain 4: Bash scripting"],
    ["x40", 0, 4, "A service script should accept start, stop or restart as its first argument and print a usage message for anything else. Which bash construct fits this most cleanly?", ["A for loop over \"$@\"", "A case statement on \"$1\"", "A while read loop", "An indexed array of arguments"], 1, "A case statement matches one value against several patterns, with *) as the catch-all for usage. A for loop is tempting because it handles arguments, but it iterates over all of them rather than choosing an action based on one.", "Domain 4: Bash control flow"],
    ["x41", 0, 4, "A script creates a temporary file in $tmp. It must be deleted however the script ends, including errors under set -e or Ctrl+C. What should be added near the top?", ["set -u to catch unset variables", "rm -f \"$tmp\" as the script's last line", "trap 'rm -f \"$tmp\"' EXIT", "exit 0 as the script's last line"], 2, "A trap on EXIT runs the cleanup command whenever the shell exits, including after errors or an interrupt. Putting rm on the last line is tempting, but that line never runs if the script stops early.", "Domain 4: Safer scripts"],
    ["x42", 0, 4, "A script must read hosts.txt one full line at a time, keeping spaces and backslashes in each line exactly as they are. Which loop is correct?", ["for line in $(cat hosts.txt); do ...; done", "cat hosts.txt | xargs -n1 echo", "for line in hosts.txt; do ...; done", "while IFS= read -r line; do ...; done < hosts.txt"], 3, "while IFS= read -r reads whole lines, keeps leading spaces and does not treat backslashes as escapes. The for over $(cat) is tempting because it is short, but it splits on every space, so a line becomes several words.", "Domain 4: Bash control flow"],
    ["x43", 0, 4, "A Python admin script needs specific library versions, and the server's system Python packages are managed by dnf and must not change. What is the best approach?", ["Create a venv and pip install inside it", "sudo pip install the libraries system-wide", "Copy the libraries into /usr/lib/python3", "Install them from a third-party dnf repo"], 0, "A virtual environment isolates the script's packages from the system Python, so dnf-managed packages stay untouched. A system-wide sudo pip install is tempting because it is quick, but it can overwrite files that dnf manages and break system tools.", "Domain 4: Python basics"],
    ["x44", 0, 4, "A Python script must store hostnames with their IP addresses and quickly look up an IP by hostname. Which built-in data type fits best?", ["A list", "A dictionary", "A tuple", "A set"], 1, "A dictionary maps keys (hostnames) to values (IPs) with fast lookup by key. A set is tempting because it also offers fast membership checks, but it stores single values and cannot map a hostname to an IP.", "Domain 4: Python basics"],
    ["x45", 0, 4, "A developer wants the commits on their own feature branch, which nobody else uses, replayed on top of the latest main so the history stays linear. Which command should they run from the feature branch?", ["git merge main", "git revert main", "git rebase main", "git reset --hard main"], 2, "git rebase main reapplies the branch's commits on top of main, producing linear history. git merge main is tempting because it also brings in main's changes, but it creates a merge commit instead of a linear history.", "Domain 4: Git"],
    ["x46", 0, 4, "An Ansible playbook deploys the nginx config from a template. nginx should restart only when the template task actually changes the file. How is this done?", ["Add a shell task that always restarts nginx", "Set gather_facts: false in the play", "Encrypt the template with ansible-vault", "Use notify to trigger a restart handler"], 3, "A task with notify triggers a handler only when the task reports changed, and handlers run once at the end of the play. Always restarting with a shell task is tempting because it guarantees the new config, but it causes needless restarts and breaks idempotence.", "Domain 4: Ansible"],
    ["x47", 0, 4, "A team follows a GitOps workflow for its infrastructure. How should an engineer change a production setting?", ["Merge the change in Git and let automation apply it", "SSH into each production host and edit the config by hand", "Email the change to the ops team to apply manually", "Edit the live resources in the cloud console"], 0, "In GitOps, Git is the source of truth and automation reconciles the live environment to match it. Editing in the cloud console is tempting because it is fast, but it creates drift that the automation may overwrite and that is not reviewed or tracked.", "Domain 4: CI/CD and GitOps"],
    ["x48", 0, 5, "A server is sluggish. vmstat 1 shows the si and so columns consistently high, while the CPU idle percentage is moderate. What does this indicate?", ["User processes are saturating every CPU core", "Memory is short, so pages are swapping", "A disk has developed failing sectors", "The NIC is dropping inbound packets"], 1, "si and so show pages swapped in and out per second; constant high values mean the system is short of RAM and swapping heavily. CPU saturation is tempting because the server is slow, but moderate idle time rules it out.", "Domain 5: Performance"],
    ["x49", 0, 5, "A user worries because free -h shows very little free memory, but buff/cache is large and the available column is high. What is the correct interpretation?", ["The server needs more RAM installed right away", "A memory leak is consuming the RAM", "Normal: cache is reclaimed when apps need it", "Swap has been disabled on the server"], 2, "Linux uses spare RAM as page cache and gives it back when applications need memory; the available column shows what apps can still use. A memory leak is tempting, but a leak would show as growing process memory and falling available memory, not a large cache.", "Domain 5: Performance"],
    ["x50", 0, 5, "An admin wants to see exactly what the DNS server 10.0.0.53 returns for app.example.com, without /etc/hosts or other local sources affecting the result. Which command fits?", ["ping app.example.com", "getent hosts app.example.com", "host -l 10.0.0.53", "dig @10.0.0.53 app.example.com"], 3, "dig @server sends the query straight to that DNS server and shows its answer. getent hosts is tempting because it resolves names, but it follows nsswitch.conf, so /etc/hosts entries can answer first.", "Domain 5: Networking"],
    ["x51", 0, 5, "Clients can't reach an HTTPS service. The admin wants to confirm whether TCP 443 packets are even arriving on eth0. Which command shows this?", ["tcpdump -i eth0 tcp port 443", "ss -tln sport = :443", "nmap -p 443 localhost", "ip link show eth0"], 0, "tcpdump captures packets on the interface, so it shows whether client traffic to port 443 arrives at all. ss is tempting because it shows the socket listening, but it cannot show whether packets are reaching the interface.", "Domain 5: Networking"],
    ["x52", 0, 5, "ss on db01 shows PostgreSQL listening on 0.0.0.0:5432, but remote clients time out. From a client, the admin wants to see whether port 5432 looks open, closed or filtered. Which command fits?", ["ss -tulpn on the client", "nmap -p 5432 db01 from the client", "ping db01 from the client", "ip route on the server to check its routes"], 1, "nmap reports a port as open, closed or filtered from the client's view, and filtered points to a firewall. ping is tempting, but ICMP succeeding does not prove TCP 5432 is reachable.", "Domain 5: Networking"],
    ["x53", 0, 5, "Users report intermittent packet loss to a remote site. The admin wants continuously updated loss and latency statistics for every hop on the path. Which tool fits?", ["ping -c 4 remote-site", "traceroute remote-site", "mtr remote-site", "dig remote-site"], 2, "mtr combines traceroute and ping, keeps probing and shows per-hop loss and latency over time. traceroute is tempting because it shows the hops, but it is a one-time snapshot that easily misses intermittent loss.", "Domain 5: Networking"],
    ["x54", 0, 5, "An admin needs to boot a server into a single-user maintenance mode that mounts local filesystems but starts no network services. Which unit should be passed with systemd.unit= at the GRUB menu?", ["emergency.target", "multi-user.target", "graphical.target", "rescue.target"], 3, "rescue.target gives a single-user root shell with local filesystems mounted and basic services running. emergency.target is tempting because it is also for repairs, but it is more minimal, with only the root filesystem mounted read-only.", "Domain 5: Boot problems"],
    ["x55", 0, 5, "An mdadm RAID 1 array on a server is reported as degraded. Which command quickly shows the array's state and which member has failed?", ["cat /proc/mdstat", "lsblk -f", "pvs", "smartctl -a /dev/md0"], 0, "/proc/mdstat shows each md array, its members and markers such as [U_] or (F) for a failed disk. smartctl is tempting because it checks disk health, but it reads SMART data from physical drives, not the md array device.", "Domain 5: Hardware and RAID"],
    ["x56", 0, 5, "User bob is locked out after too many failed logins. The server uses pam_faillock, and bob's password itself is fine. How should the admin unlock him?", ["passwd -u bob", "faillock --user bob --reset", "usermod -U bob", "chage -E -1 bob"], 1, "faillock --reset clears bob's failed-attempt record, which is what pam_faillock uses to lock him out. passwd -u is tempting because it unlocks accounts, but it removes a password lock (the ! in /etc/shadow), not the faillock tally.", "Domain 5: Security issues"],
    ["x57", 0, 5, "A server's clock is several minutes off, and timedatectl shows NTP service: inactive. Which command turns on automatic time synchronization?", ["timedatectl set-timezone UTC", "hwclock --hctosys", "timedatectl set-ntp true", "date -s now"], 2, "timedatectl set-ntp true enables the system's NTP sync service so the clock stays correct. hwclock --hctosys is tempting because it sets the system time, but it copies the hardware clock, which may be wrong too, and does not keep syncing.", "Domain 5: Time sync"],
    ["x58", 0, 5, "apt upgrade keeps reporting nginx as kept back, and apt-mark showhold lists nginx because an admin held it months ago. The team now wants nginx upgraded normally. Which command should be run first?", ["apt autoremove nginx", "dpkg --configure -a", "apt-get clean", "apt-mark unhold nginx"], 3, "apt-mark unhold removes the hold so apt can upgrade nginx again. dpkg --configure -a is tempting as a common repair command, but it only finishes configuring half-installed packages and does not affect holds.", "Domain 5: Package problems"],
    ["x59", 0, 5, "dnf install fails with a GPG check error for a package from a newly added vendor repository. What is the correct fix?", ["Import the vendor's signing key after verifying it", "Set gpgcheck=0 in the vendor's .repo file and retry", "Run dnf clean all, then retry the install", "Install the package with rpm --nosignature"], 0, "dnf needs the vendor's public key to verify package signatures; import it from a trusted source after checking its fingerprint. Setting gpgcheck=0 is tempting because the install then works, but it turns off integrity checking for every package from that repo.", "Domain 5: Package problems"],
    ["x60", 0, 5, "podman run registry.example.com/team/api:2.4 fails with a 'manifest unknown' error, while pulling api:2.3 from the same registry works. What is the most likely cause?", ["The host has no free disk space left", "The image tag was not found in the registry", "The container's port is already in use", "SELinux is blocking the container's volume mount"], 1, "manifest unknown means the registry has no image manifest for that name and tag, so the 2.4 tag likely does not exist. A port conflict is tempting because it is a common podman error, but it happens when the container starts, after the image is already pulled.", "Domain 5: Container problems"]
  ],
  whys: {
    "x1": ["Writing to /proc changes only the running kernel, so the setting is lost at reboot.", "sysctl -w also changes only the running kernel and does not persist after reboot.", null, "IP forwarding is a kernel parameter, not a module, so modules-load.d does not set it."],
    "x2": ["rmmod after boot means nouveau still loads first and must be removed by hand every time.", "modprobe -r unloads it only until the next boot; nothing stops it loading again.", "modules-load.d lists modules to load at boot, which is the opposite of what is needed.", null],
    "x3": [null, "/etc/default/grub holds settings for future configs and may not match the current boot.", "grub.cfg lists menu entries, but the parameters could have been edited at the GRUB menu for this boot.", "uname -a shows the kernel version and architecture, not the boot parameters."],
    "x4": ["ext4 has no built-in subvolumes or snapshots; it would need LVM snapshots underneath.", null, "XFS lacks native subvolume snapshots, so it would also need LVM for snapshots.", "tmpfs lives in RAM and loses its contents at reboot, so it cannot hold snapshots of the system."],
    "x5": ["xfs_growfs only works on XFS filesystems, and this one is ext4.", "mkfs creates a new empty filesystem, which would destroy the data on /data.", null, "pvresize grows a physical volume after its disk grows; the LV is already bigger, so the filesystem is what needs resizing."],
    "x6": ["RAID 0 stripes data with no redundancy, so losing either disk loses all data.", "Linear mode just joins the disks end to end and offers no redundancy.", "--assemble starts an existing array; it cannot create a new one from empty disks.", null],
    "x7": [null, "df shows usage of the whole filesystem, not which directories consume it.", "ls -lS sorts by the size of directory entries, not the total space inside each directory.", "lsblk lists block devices and partitions, not directory usage."],
    "x8": ["stat shows metadata like size, inode and timestamps, not the type of content.", null, "ls -l shows permissions, owner and size, not what kind of data is inside.", "touch only updates timestamps or creates an empty file; it reports nothing."],
    "x9": ["Without -s, ln makes a hard link, and hard links to directories are not allowed.", "cp -r makes a separate copy that will not follow later release changes.", null, "mv would move or rename the directory rather than create a link to it."],
    "x10": ["cut uses tab as its default delimiter, so without -d ' ' it will not split on spaces.", "This sed replaces spaces with commas but still prints every field.", "sort only reorders lines by key; it prints whole lines, not chosen fields.", null],
    "x11": [null, "Without sorting first, uniq -c counts only adjacent repeats, so the same IP is counted in pieces.", "sort -u | wc -l gives the number of distinct IPs, not each IP's request count.", "uniq -d prints each duplicated IP once without counts or ordering by volume."],
    "x12": ["Without -i, sed prints the edited text to the screen and leaves app.conf unchanged.", null, "grep only searches for text; it cannot change the file.", "tr maps single characters, not strings, and redirecting to the same file truncates it first."],
    "x13": ["netplan generate only writes backend config; it neither applies it nor offers a rollback.", "Restarting systemd-resolved affects DNS resolution, not netplan's interface configuration.", null, "nmcli connection reload rereads NetworkManager profiles and has no automatic rollback."],
    "x14": ["-z selects gzip; naming the file .xz does not make it xz-compressed.", "-j selects bzip2 compression, not xz.", "-x extracts an archive rather than creating one.", null],
    "x15": [null, "Scripts in /etc/profile.d run for all login shells and are not copied into new home directories.", "/etc/login.defs sets defaults like UID ranges and password aging, not starter dotfiles.", "/root is root's home directory, so its files are not given to new users."],
    "x16": ["usermod -L only locks the password; the account and files remain.", null, "Plain userdel removes the account but leaves the home directory and mail spool.", "passwd -d removes the password, which weakens the account instead of deleting it."],
    "x17": ["disable stops boot-time start, but the unit can still be started manually or as a dependency.", "stop halts it now, but it can start again at boot or when needed.", null, "daemon-reload rereads unit files and does not block any service."],
    "x18": ["This cron entry runs every day at 23:00, not just once.", "systemctl start has no --at option for scheduling a later start.", "nohup keeps a job running after logout; it does not delay it until 23:00.", null],
    "x19": [null, "A plain background job can still receive SIGHUP and stop when the SSH session closes.", "bg resumes an existing stopped job; it does not start a new command.", "fg brings a job to the foreground, where it would stop when the session ends."],
    "x20": ["grep matches text, missing critical messages without the word and catching harmless lines that contain it.", null, "-k shows kernel messages only, not the nginx unit's logs.", "-b -1 shows the previous boot only, not everything since yesterday."],
    "x21": ["Deleting the log loses all history and does not keep four compressed copies.", "SystemMaxUse limits the systemd journal only, not an app's own log files.", null, "rsyslog routes messages to files; it does not rotate or compress them."],
    "x22": ["A double @@ forwards over TCP, not UDP as required.", "logrotate's postrotate runs after rotation; it does not forward messages.", "journalctl only reads the journal; it has no option to forward to a syslog host.", null],
    "x23": [null, "dpkg -L lists the files in a named package; it does not find a file's owner.", "apt show describes a package by name and does not take a file path.", "apt-mark showmanual lists manually installed packages, not file ownership."],
    "x24": ["Installing into /usr from source can conflict with distro files and provides no sandbox.", null, "Packages built for another distro often have mismatched dependencies and may break.", "pip installs Python packages; it does not install sandboxed desktop apps."],
    "x25": ["ps -a shows that the container exited and its exit code, not what the app printed.", "podman images lists local images, not container output.", null, "pull downloads an image; it does not show the container's output."],
    "x26": ["Another Deployment creates more pods with changing IPs; it gives no stable address.", "A ReplicaSet keeps a set number of pods running but provides no network address.", "A ConfigMap stores configuration data, not network access to pods.", null],
    "x27": [null, "The sticky bit limits who can delete files; it does not set group ownership.", "SUID on a directory is ignored on Linux and does not set group ownership.", "chown changes only existing files, not files created later."],
    "x28": ["chcon changes only the current label, which restorecon or a relabel reverts.", null, "This boolean covers home directory content, not files in /srv/web.", "Disabling enforcement removes SELinux protection instead of fixing the label."],
    "x29": ["setenforce 0 turns off SELinux enforcement instead of allowing just this access.", "httpd_t is a process domain; putting it on files does not allow database connections.", null, "Labeling 5432 as http_port_t concerns ports httpd may listen on, not outbound connections."],
    "x30": ["--add-interface takes an interface name like eth0, not a subnet.", "--add-service takes a service name such as ssh, not an address range.", "--add-port takes a port and protocol such as 22/tcp, not a subnet.", null],
    "x31": [null, "PermitRootLogin no blocks only root; other users could still use passwords.", "Disabling public keys would block key logins, the opposite of the goal.", "Deleting authorized_keys breaks key logins while leaving passwords enabled."],
    "x32": ["sudo -i opens a root shell; it does not list the allowed commands.", null, "su -l switches to another user's login shell and says nothing about sudo rules.", "visudo -c only checks the sudoers files for syntax errors."],
    "x33": ["pam_faillock locks accounts after failed logins; it does not check password strength.", "pam_limits sets resource limits such as open files, not password rules.", null, "pam_sss connects PAM to SSSD for directory logins; it does not enforce complexity."],
    "x34": ["nscd caches name service lookups but does not authenticate users against a directory.", "chronyd synchronizes time; it provides no identity or login service.", "rsyslog handles log messages, not user authentication.", null],
    "x35": [null, "sha256sum -c checks the ISO against the file but cannot prove who wrote the file.", "An MD5 hash of the checksum file proves nothing about who published it.", "openssl rand generates random data and verifies nothing."],
    "x36": ["auditd logs events as they happen rather than comparing files with a hash baseline.", null, "fail2ban bans IPs after failed logins; it does not check file integrity.", "SSSD provides directory authentication, not file integrity checking."],
    "x37": ["chattr +i blocks changes instead of recording who made them.", "aide --check compares files with a baseline later; it does not log who changed them.", null, "getfacl shows the file's ACLs; it does not record any changes."],
    "x38": ["The script already runs and reaches the syntax error, so it is already executable.", "set -e stops on errors but does not add array support to /bin/sh.", "The extension is ignored; the shebang still selects /bin/sh.", null],
    "x39": [null, "Unquoted, the value is split on spaces into several arguments, which is the bug.", "Single quotes stop expansion, so rm looks for a file literally named $file.", "Command substitution without quotes still splits the result on spaces."],
    "x40": ["A for loop walks over every argument instead of picking one action from $1.", null, "while read reads input lines, not a command-line argument.", "An array stores values but does not choose an action by matching patterns."],
    "x41": ["set -u only catches unset variables; it does not clean up files.", "The last line never runs if the script exits early from an error or Ctrl+C.", null, "exit 0 at the end also never runs after an early exit, and it deletes nothing."],
    "x42": ["$(cat) output is split on every space, so each line breaks into separate words.", "xargs splits on whitespace and handles quotes and backslashes specially.", "This loops once over the literal word hosts.txt, not over the file's lines.", null],
    "x43": [null, "A system-wide pip install can overwrite files that dnf manages and break system tools.", "Copying libraries by hand into system paths bypasses package management and can break things.", "A third-party repo may not have the exact versions and still changes the system Python."],
    "x44": ["A list would need a slow search to find a hostname and does not map keys to values.", null, "A tuple is an immutable sequence without key-based lookup.", "A set stores single values and cannot map a hostname to an IP."],
    "x45": ["merge main brings in the changes but adds a merge commit, so history is not linear.", "revert creates commits that undo changes; it does not replay the branch on main.", null, "reset --hard main discards the branch's own commits."],
    "x46": ["Always restarting causes needless downtime and breaks idempotence.", "gather_facts controls fact collection and has nothing to do with restarts.", "ansible-vault encrypts secrets; it does not control when services restart.", null],
    "x47": [null, "Manual edits over SSH cause drift and skip the Git review and history.", "Emailing changes for manual work bypasses Git as the source of truth.", "Console edits create drift that the automation may overwrite and that nobody reviewed."],
    "x48": ["The CPU still has idle time, so it is not saturated.", null, "Failing sectors show up as I/O errors in dmesg, not in vmstat's swap columns.", "Network drops do not appear in vmstat's si and so columns."],
    "x49": ["High available memory means apps still have plenty of RAM, so no urgent upgrade is needed.", "A leak would show growing process memory and falling available memory, not a large cache.", null, "free shows the swap line separately; the cache numbers say nothing about swap being off."],
    "x50": ["ping uses the system resolver, including /etc/hosts, and does not show the DNS answer.", "getent follows nsswitch.conf, so /etc/hosts entries can answer first.", "host -l asks for a zone transfer listing; it does not query this name against that server.", null],
    "x51": [null, "ss shows the listening socket but cannot show whether packets reach the interface.", "Scanning localhost tests the loopback path, not traffic arriving on eth0.", "ip link shows the interface state, not the traffic on port 443."],
    "x52": ["ss on the client shows the client's own sockets, not whether the server port is reachable.", null, "A successful ping proves ICMP works, not that TCP 5432 gets through.", "Routes on the server do not show whether a firewall filters the port."],
    "x53": ["Four pings to the destination give no per-hop view and may miss intermittent loss.", "traceroute is a one-time snapshot that easily misses intermittent loss.", null, "dig tests DNS resolution, not packet loss on the path."],
    "x54": ["emergency.target is more minimal and mounts only the root filesystem, read-only.", "multi-user.target starts network services, which is not wanted here.", "graphical.target starts a full system with a desktop and network services.", null],
    "x55": [null, "lsblk -f shows filesystems and UUIDs, not the RAID array's health.", "pvs lists LVM physical volumes, not md array members.", "SMART data comes from physical drives, not the md array device."],
    "x56": ["passwd -u removes a password lock (the ! in /etc/shadow), not the faillock record.", null, "usermod -U also removes only the password lock, not the failed-attempt tally.", "chage -E -1 clears an account expiry date, which is not what locked bob."],
    "x57": ["Changing the time zone does not correct a wrong clock or turn on syncing.", "hwclock --hctosys copies the hardware clock, which may also be wrong, and does not keep syncing.", null, "date -s now just sets the time to its current wrong value."],
    "x58": ["autoremove removes unused dependencies and does not release a hold.", "dpkg --configure -a finishes half-installed packages and does not affect holds.", "apt-get clean clears the download cache only.", null],
    "x59": [null, "gpgcheck=0 turns off signature checks for every package from that repo.", "Clearing the cache does not add the missing signing key.", "Skipping signature checks installs unverified packages, which defeats their purpose."],
    "x60": ["Low disk space causes storage write errors, not a manifest unknown response from the registry.", null, "A port conflict happens when the container starts, after the image is already pulled.", "A volume mount denial happens at run time and would not stop the 2.4 image from being found."]
  },
  levels: {
    "lx1": 1, "lx2": 2, "lx3": 2, "lx4": 1, "lx5": 1, "lx6": 3, "lx7": 3, "lx8": 3, "lx9": 1, "lx10": 3,
    "lx11": 2, "lx12": 2, "lx13": 1, "lx14": 2, "lx15": 1, "lx16": 3, "lx17": 1, "lx18": 1, "lx19": 2, "lx20": 2,
    "lx21": 3, "lx22": 1, "lx23": 1, "lx24": 1, "lx25": 2, "lx26": 1, "lx27": 3, "lx28": 3, "lx29": 1, "lx30": 2,
    "lx31": 1, "lx32": 2, "lx33": 2, "lx34": 2, "lx35": 1, "lx36": 2, "lx37": 2, "lx38": 3, "lx39": 1, "lx40": 1,
    "lx41": 3, "lx42": 1, "lx43": 1, "lx44": 2, "lx45": 3, "lx46": 1, "lx47": 1, "lx48": 2, "lx49": 2, "lx50": 1,
    "lx51": 1, "lx52": 3, "lx53": 1, "lx54": 2, "lx55": 2, "lx56": 3, "lx57": 1, "lx58": 1, "lx59": 1, "lx60": 1,
    "lx61": 1, "lx62": 1, "lx63": 3, "lx64": 1, "lx65": 3, "lx66": 3, "lx67": 2, "lx68": 2, "lx69": 3, "lx70": 1,
    "lx71": 3, "lx72": 3, "lx73": 2, "lx74": 3, "lx75": 1, "lx76": 2, "lx77": 1, "lx78": 2, "lx79": 3, "lx80": 3,
    "lx81": 1, "lx82": 3, "lx83": 2, "lx84": 2, "lx85": 3, "lx86": 1, "lx87": 1, "lx88": 1, "lx89": 2, "lx90": 3,
    "lx91": 2, "x1": 2, "x2": 3, "x3": 1, "x4": 1, "x5": 2, "x6": 3, "x7": 2, "x8": 1, "x9": 2,
    "x10": 2, "x11": 3, "x12": 3, "x13": 2, "x14": 1, "x15": 2, "x16": 1, "x17": 2, "x18": 2, "x19": 2,
    "x20": 3, "x21": 2, "x22": 3, "x23": 1, "x24": 2, "x25": 1, "x26": 2, "x27": 2, "x28": 3, "x29": 3,
    "x30": 2, "x31": 2, "x32": 1, "x33": 1, "x34": 1, "x35": 3, "x36": 1, "x37": 2, "x38": 3, "x39": 2,
    "x40": 1, "x41": 3, "x42": 3, "x43": 2, "x44": 1, "x45": 2, "x46": 2, "x47": 2, "x48": 3, "x49": 3,
    "x50": 2, "x51": 2, "x52": 2, "x53": 2, "x54": 3, "x55": 1, "x56": 3, "x57": 1, "x58": 2, "x59": 3,
    "x60": 2
  }
});
