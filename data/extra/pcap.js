/* More pcap practice questions with wrong-answer notes, and a difficulty level for every question. Merged into the free bank by tools/build.js. */
CertHub.addExtra("pcap", {
  questions: [
    ["x1",0,1,"A project has a package shop containing a subpackage billing, which contains the module tax.py defining a function rate(). A script runs only `import shop.billing.tax`. Which call works?",["shop.billing.tax.rate()","tax.rate()","billing.tax.rate()","shop.rate()"],0,"A plain import of a nested module binds only the top-level package name, so the function must be fully qualified as shop.billing.tax.rate(). Writing tax.rate() is tempting but tax was never bound as a name.","Domain 1: Qualifying names in nested packages"],
    ["x2",0,1,"What is the output of the following code?\nimport math\nprint('sqrt' in dir(math), 'math' in dir(), 'pi' in dir())",["True False False","True True False","True True True","False True False"],1,"dir(math) includes sqrt, and dir() at script level includes math because import math bound it, but pi is not in the script's namespace since only the module name was imported. Answering True for pi confuses import math with from math import *.","Domain 1: dir() and namespaces"],
    ["x3",0,1,"A raffle script must pick 3 different winners from a list of 50 names so that nobody can win twice. Which call fits BEST?",["[random.choice(names) for _ in range(3)]","random.random(names, 3)","random.sample(names, 3)","random.seed(3); names[:3]"],2,"random.sample(population, k) returns k unique elements chosen without replacement, which is exactly a no-repeat draw. Calling choice() three times is tempting but can return the same name twice.","Domain 1: random module"],
    ["x4",0,1,"A setup script must branch on the operating system and needs a short name such as 'Linux', 'Windows' or 'Darwin'. Which function returns this?",["platform.machine()","platform.processor()","platform.python_implementation()","platform.system()"],3,"platform.system() returns the OS name such as 'Linux', 'Windows' or 'Darwin'. platform.machine() is the tempting distractor, but it reports the hardware architecture, not the operating system.","Domain 1: platform module"],
    ["x5",0,1,"A developer runs `python app/main.py` from their home directory. main.py imports helpers, and helpers.py sits in the app folder. Where does Python look first when resolving the import?",["The shell's current working directory (home)","The standard library directory","The directory that contains main.py (app)","The site-packages directory"],2,"When Python runs a script file, sys.path[0] is set to the directory containing that script, so app is searched first. The current working directory is the tempting answer, but that applies to the interactive prompt, not to running a script by path.","Domain 1: sys.path search order"],
    ["x6",0,1,"Module counter.py contains a single statement, `print('loaded')`. A script runs `import counter` on two consecutive lines. How many times is loaded printed?",["Twice","Never, because imports do not print","Once, and then the second import raises ImportError","Once"],3,"A module's top-level code runs only on its first import; later imports reuse the module object cached in sys.modules. Twice is tempting, but Python does not execute the file again for a repeated import.","Domain 1: Import behaviour and sys.modules"],
    ["x7",0,1,"Module util.py ends with `if __name__ == '__main__': run_tests()`. Which statement is correct?",["run_tests() runs for `python util.py` but not when util is imported","run_tests() runs when util is imported but not when run directly","run_tests() runs in both cases because the test is made at import time","run_tests() never runs unless __name__ is assigned by hand"],0,"Python sets __name__ to '__main__' only for the file being executed directly; an imported module gets its own name. Mixing up the two cases is the tempting error, since imported code sees __name__ == 'util'.","Domain 1: The __name__ == '__main__' idiom"],
    ["x8",0,2,"What is the output of the following code?\ntry:\n    [1, 2][5]\nexcept Exception:\n    print('E')\nexcept IndexError:\n    print('I')",["I","E","E then I on two lines","Nothing, because a SyntaxError is raised"],1,"Except branches are checked top to bottom and the first match wins; IndexError is a subclass of Exception, so E is printed. I is tempting because it is more specific, but order, not specificity, decides.","Domain 2: Order of except branches"],
    ["x9",0,2,"What is the output of the following code?\ndef f():\n    try:\n        raise KeyError\n    except ValueError:\n        print('V', end=' ')\n    finally:\n        print('F', end=' ')\ntry:\n    f()\nexcept KeyError:\n    print('K')",["V F K","F K","K F","F"],1,"The KeyError does not match except ValueError, so finally runs (printing F) and the exception propagates to the caller, which prints K. K F is tempting, but finally always runs before the exception leaves the function.","Domain 2: finally with unhandled exceptions"],
    ["x10",0,2,"Which of these built-in exceptions is NOT a subclass of Exception?",["KeyboardInterrupt","ZeroDivisionError","KeyError","AssertionError"],0,"KeyboardInterrupt inherits directly from BaseException, so except Exception does not catch it. AssertionError can seem special, but it is an ordinary Exception subclass.","Domain 2: Exception hierarchy"],
    ["x11",0,2,"What is the output of the following code?\ntry:\n    raise Exception('a', 2)\nexcept Exception as e:\n    print(e.args[1] * 2)",["aa","22","An IndexError is raised","4"],3,"The args attribute is a tuple of the constructor arguments, ('a', 2), so e.args[1] * 2 is 4. 22 is tempting if you assume args stores strings, but the int keeps its type.","Domain 2: except ... as e and args"],
    ["x12",0,2,"A payments library defines `class PaymentError(Exception)` and `class CardDeclined(PaymentError)`. A caller wants a specific message for declined cards and one general handler for every other payment problem. Which arrangement is correct?",["except PaymentError first, then except CardDeclined","A single except Exception branch that checks the message text","except CardDeclined first, then except PaymentError","except (PaymentError, CardDeclined) followed by an else branch"],2,"The more specific subclass must come first, because the first matching branch wins and a PaymentError branch also matches CardDeclined. Putting PaymentError first is tempting but makes the CardDeclined branch unreachable.","Domain 2: Custom exception hierarchies"],
    ["x13",0,2,"Which statement about `raise ValueError` and `raise ValueError()` is correct?",["The first form raises the class itself, which except ValueError cannot catch","Only the second form can be caught by except Exception","The first form is a SyntaxError in Python 3","Both raise a ValueError instance; the first form instantiates the class automatically"],3,"When raise is given an exception class, Python calls it with no arguments to create the instance, so the two forms behave the same, both giving an exception with empty args. The idea that the bare class cannot be caught is the tempting misconception.","Domain 2: raise with a class or an instance"],
    ["x14",0,2,"What is the output of the following code?\nimport math\ntry:\n    math.sqrt(-1)\nexcept ArithmeticError:\n    print('A')\nexcept ValueError:\n    print('V')",["A","nan","V","A then V on two lines"],2,"math.sqrt() with a negative argument raises ValueError: math domain error, and ValueError is not under ArithmeticError, so V is printed. A is tempting because it sounds mathematical, but only errors like ZeroDivisionError and OverflowError are ArithmeticErrors.","Domain 2: Built-in exception hierarchy"],
    ["x15",0,2,"What is the output of the following code?\ntry:\n    try:\n        1 / 0\n    except ZeroDivisionError:\n        raise ValueError('x')\nexcept ValueError as e:\n    print(e)",["division by zero","x","ZeroDivisionError","ValueError('x')"],1,"The inner handler raises a new ValueError('x'), which the outer handler catches; printing an exception shows its message, x. division by zero is tempting, but that belongs to the first exception, not the one caught.","Domain 2: Raising inside an except branch"],
    ["x16",0,3,"What is the output of the following code?\ns = 'abcdefgh'\nprint(s[1:7:2], s[::-3])",["bdf heb","bdf hda","ace heb","bdf hdb"],0,"s[1:7:2] takes indices 1, 3 and 5 (b, d, f), and s[::-3] starts at the last index 7 and steps back by 3 through 4 and 1 (h, e, b). ace is tempting if you forget that the start index 1 is b.","Domain 3: Slicing with steps"],
    ["x17",0,3,"What is the output of the following code?\nprint('ab' * 2 + 'c' * 0 + 'd')",["ab2d","ababd","ababcd","abcd"],1,"String replication repeats the text, so 'ab' * 2 is abab, 'c' * 0 is the empty string and adding 'd' gives ababd. ababcd is tempting but multiplying by zero produces nothing.","Domain 3: Concatenation and replication"],
    ["x18",0,3,"What is the output of the following code?\nprint(len('It\\'s\\n'))",["6","7","4","5"],3,"The literal holds I, t, an apostrophe, s and a newline, which is five characters, because each escape sequence stands for one character. 6 or 7 comes from counting the backslashes, which are not stored.","Domain 3: Escape sequences"],
    ["x19",0,3,"What is the output of the following code?\nprint('apple' < 'Apple', 'Zoo' < 'apple', 'a' < 'ab')",["False True True","True False True","False False True","True True False"],0,"Strings compare character by character by code point: 'a' (97) is greater than 'A' (65), 'Z' (90) is less than 'a' (97), and a prefix is smaller than the longer string. Treating comparison as dictionary order ignoring case is the tempting error.","Domain 3: Comparing strings"],
    ["x20",0,3,"What is the output of the following code?\nprint('\\t\\n'.isspace(), 'abc1'.islower(), ''.isalpha(), 'A1'.isupper())",["True False False True","True True True False","True True False True","False True False True"],2,"isspace() is True for tabs and newlines, islower() and isupper() look only at cased characters, and every is... test returns False for an empty string. Expecting 'abc1'.islower() to be False because of the digit is the tempting mistake.","Domain 3: Character tests"],
    ["x21",0,3,"What is the output of the following code?\ns = 'mississippi'\nprint(s.find('ss', 3), s.index('i', 5))",["2 4","5 4","5 7","2 7"],2,"The optional second argument sets where the search starts: 'ss' from index 3 is found at 5, and 'i' from index 5 is found at 7. 2 is tempting but that match lies before the start position.","Domain 3: find() and index()"],
    ["x22",0,3,"A developer needs to change the third character of `code = 'AB-12'` to an underscore, giving 'AB_12'. Which statement does it?",["code = code[:2] + '_' + code[3:]","code[2] = '_'","code.replace(2, '_')","code.insert(2, '_')"],0,"Because strings are immutable, you build a new string from slices around the position and rebind the name. code[2] = '_' is tempting because it works for lists, but it raises TypeError for a str.","Domain 3: String immutability"],
    ["x23",0,3,"What is the output of the following code?\nprint('a b c d'.split(' ', 2))",["['a', 'b']","['a b', 'c d']","['a', 'b', 'c', 'd']","['a', 'b', 'c d']"],3,"split(sep, maxsplit) splits at most maxsplit times from the left and keeps the remainder as the last item, giving three pieces. ['a', 'b'] is tempting if you read 2 as the number of items to return.","Domain 3: split()"],
    ["x24",0,3,"What is the output of the following code?\nprint(chr(97).upper() + str(ord('0')))",["a48","A48","A0","A30"],1,"chr(97) is 'a', upper() makes it 'A', and ord('0') is 48, the code point of the digit zero character. A0 is tempting but confuses the character '0' with its numeric value.","Domain 3: ord() and chr()"],
    ["x25",0,3,"A script has `word = 'dcba'` and then calls `word.sort()`. What happens?",["word becomes 'abcd'","It returns 'abcd' and word is unchanged","A TypeError is raised because strings are immutable","An AttributeError is raised because str has no sort() method"],3,"sort() is a list method, so looking it up on a str raises AttributeError; sorted(word) would return ['a', 'b', 'c', 'd']. TypeError is tempting because of immutability, but the missing attribute is found first.","Domain 3: sorted() versus list.sort()"],
    ["x26",0,3,"What is the output of the following code?\nprint(len('\\u00e9'), len('\\u00e9'.encode('utf-8')))",["2 2","1 2","1 1","2 1"],1,"A str's length counts code points, so 'é' is 1, while UTF-8 encodes U+00E9 in two bytes, so the encoded bytes have length 2. 1 1 is tempting if you assume one byte per character, which only holds for ASCII.","Domain 3: Unicode and UTF-8"],
    ["x27",0,4,"What is the output of the following code?\nclass A:\n    v = []\na, b = A(), A()\na.v = a.v + [1]\nb.v.append(2)\nprint(A.v, a.v)",["[1, 2] [1, 2]","[] [1]","[2] [1]","[2] [1, 2]"],2,"The assignment a.v = a.v + [1] creates an instance variable holding a new list, while b.v.append(2) mutates the class variable A.v through b. Expecting both to share [1, 2] is tempting but ignores that assignment creates an instance attribute.","Domain 4: Instance versus class variables"],
    ["x28",0,4,"What is the output of the following code?\nclass A:\n    def __hid(self):\n        return 'h'\n    def show(self):\n        return self.__hid()\na = A()\nprint(a.show(), hasattr(a, '_A__hid'))",["h True","h False","An AttributeError is raised","_A__hid True"],0,"Names starting with two underscores are mangled to _ClassName__name inside the class, so show() finds the method and hasattr sees _A__hid. h False is tempting if you think private members are truly hidden, but mangling only renames them.","Domain 4: Name mangling for methods"],
    ["x29",0,4,"Class A's __init__ sets `self.__x = 1`. Class B(A) does not override __init__ and defines `def get(self): return self.__x`. What happens when B().get() is called?",["AttributeError, since self.__x in B is mangled to _B__x","It returns 1","It returns None","NameError, since __x is private to A's module"],0,"Mangling uses the class where the code is written: A stored _A__x, but B's method asks for _B__x, which does not exist, so AttributeError is raised. Returning 1 is tempting because B inherits A's __init__, but the names differ.","Domain 4: Private attributes and inheritance"],
    ["x30",0,4,"What is the output of the following code?\nclass A:\n    b = 2\n    def __init__(self):\n        self.a = 1\no = A()\no.c = 3\nprint(sorted(o.__dict__))",["['a', 'b', 'c']","['a']","['a', 'c']","['b']"],2,"An object's __dict__ contains only its instance variables: a from __init__ and c added later. Including b is tempting because o.b works, but b lives in the class's __dict__.","Domain 4: The __dict__ attribute"],
    ["x31",0,4,"What is the output of the following code?\nclass A:\n    def f(self): return 'A'\nclass B(A):\n    def f(self): return 'B' + super().f()\nclass C(A):\n    def f(self): return 'C' + super().f()\nclass D(B, C):\n    def f(self): return 'D' + super().f()\nprint(D().f())",["DBA","DBCA","DCBA","DBACA"],1,"super() follows the MRO of the instance's class, D, B, C, A, object, so each f adds its letter and calls the next: DBCA. DBA is tempting if you think super() in B always means A.","Domain 4: super() and the MRO"],
    ["x32",0,4,"What is the output of the following code?\nclass A:\n    pass\nclass B(A):\n    pass\nprint(issubclass(B, B), isinstance(A(), B), isinstance(B(), object))",["False False True","True True True","False True False","True False True"],3,"A class counts as its own subclass, an instance of a parent is not an instance of the child, and everything is an instance of object. False for issubclass(B, B) is the tempting slip.","Domain 4: isinstance() and issubclass()"],
    ["x33",0,4,"What is the output of the following code?\nclass P:\n    def __init__(self, n):\n        self.n = n\n    def __str__(self):\n        return 'P' + str(self.n)\nprint(str(P(3)) + '!')",["P3!","P 3!","<__main__.P object>!","A TypeError is raised"],0,"str(obj) calls the class's __str__() method, which returns 'P3', and concatenating '!' gives P3!. The default <__main__.P object ...> text appears only when __str__ is not defined.","Domain 4: The __str__() method"],
    ["x34",0,4,"What is the output of the following code?\nclass A:\n    pass\nprint(type(A).__name__, A().__class__.__name__)",["A A","type A","class A","object A"],1,"A class is itself an object whose type is type, while an instance's __class__ is its class, A. object A is tempting because object is A's base class, but base and type are different things.","Domain 4: Introspection with type() and __class__"],
    ["x35",0,4,"What is the output of the following code?\nclass Bag:\n    def __init__(self, items=[]):\n        self.items = items\na = Bag()\nb = Bag()\na.items.append('x')\nprint(b.items)",["[]","['x', 'x']","['x']","None"],2,"Default argument values are evaluated once when the function is defined, so both Bag objects share one list and b sees 'x'. [] is tempting, and a default of None with a new list inside __init__ is the usual fix.","Domain 4: Constructors with default arguments"],
    ["x36",0,4,"A report function calls `shape.area()` on each object in a list of Circle, Square and Triangle objects, and each class defines its own area(). Which OOP concept does this rely on?",["Encapsulation","Name mangling","Composition","Polymorphism"],3,"Polymorphism lets the same method call run each class's own implementation depending on the object's type. Encapsulation is tempting because methods are involved, but it concerns hiding state, not dispatch.","Domain 4: Polymorphism"],
    ["x37",0,4,"Which term describes bundling data together with the methods that work on it, while restricting direct access to that data from outside the class?",["Inheritance","Introspection","Encapsulation","Method overriding"],2,"Encapsulation combines state and behaviour in one class and hides the state behind methods, which Python supports through private names. Inheritance is the tempting pick because it is also a core OOP idea, but it concerns reuse.","Domain 4: Core OOP concepts"],
    ["x38",0,4,"What is the output of the following code?\nclass C:\n    rate = 1\nc = C()\nC.rate = 5\nprint(c.rate, 'rate' in c.__dict__)",["1 False","5 True","1 True","5 False"],3,"The instance has no rate of its own, so lookup falls through to the class, which now holds 5, and c.__dict__ stays empty. 1 is tempting if you think the value was copied into the object when it was created.","Domain 4: Class variables"],
    ["x39",0,4,"What is the output of the following code?\nclass A:\n    def __init__(self):\n        self.x = 1\nprint(hasattr(A, 'x'), hasattr(A, '__init__'))",["False True","True True","True False","False False"],0,"Instance variables are created on objects when __init__ runs, so the class A has no x, while methods such as __init__ are class attributes. True for x is tempting because it appears in the class source.","Domain 4: hasattr() and instance variables"],
    ["x40",0,4,"What is the output of the following code?\nclass A:\n    name = 'cls'\n    def hi(self):\n        return 'hi ' + self.name\na = A()\na.name = 'obj'\nprint(A.hi(a))",["hi cls","hi obj","A TypeError is raised","hi A"],1,"A.hi(a) passes a as self, and a.name finds the instance attribute 'obj' before the class variable. hi cls is tempting because the call goes through the class, but lookup still starts at the object.","Domain 4: Methods and self"],
    ["x41",0,4,"What is the output of the following code?\nclass A:\n    pass\nclass B:\n    pass\nclass C(A, B):\n    pass\nprint(len(C.__bases__), C.__bases__[1].__name__)",["3 object","2 B","2 A","1 A"],1,"__bases__ is a tuple of the direct superclasses in the order written, (A, B), so its length is 2 and item 1 is B. Counting object is tempting, but object appears in the MRO, not in C's __bases__.","Domain 4: __bases__"],
    ["x42",0,4,"What is the output of the following code?\nclass A:\n    def __init__(self):\n        print('A', end='')\nclass B:\n    def __init__(self):\n        print('B', end='')\nclass C(A, B):\n    pass\nC()",["A","B","AB","BA"],0,"C has no __init__, so Python uses the first one in the MRO (C, A, B, object), which is A's, and A.__init__ does not call super(). AB is tempting, but Python never calls all base constructors automatically.","Domain 4: Multiple inheritance and constructors"],
    ["x43",0,4,"A reviewer sees `if result == None:` and recommends `if result is None:`. What is the reason?",["== None raises a TypeError for most objects","is compares values more strictly than ==","is None also matches empty lists and zero","is tests identity, and None is a single object"],3,"There is only one None object, so an identity test with is is exact and cannot be changed by a class's own == behaviour. Saying is compares values more strictly is tempting, but is compares identity, not value.","Domain 4: is versus =="],
    ["x44",0,4,"Module shapes.py defines class Circle. A script runs `from shapes import Circle` and then `print(Circle.__module__)`. What is printed?",["__main__","Circle","shapes","shapes.py"],2,"__module__ records the name of the module where the class was defined, shapes, no matter how it was imported. __main__ is tempting because the print happens in the script, but the class was not defined there.","Domain 4: __module__"],
    ["x45",0,4,"What is the output of the following code?\nclass A:\n    def __init__(self, n):\n        self.n = n\nclass B(A):\n    def __init__(self):\n        A.__init__(self, 7)\nprint(B().n)",["0","None","A TypeError is raised","7"],3,"Calling the superclass constructor by name with self explicitly is valid, so A.__init__ sets n to 7. A TypeError is tempting because B() is called without n, but B's own __init__ supplies it.","Domain 4: Calling a superclass constructor"],
    ["x46",0,4,"What is the output of the following code?\nclass A:\n    x = 1\nclass B(A):\n    pass\nb = B()\nB.x = 2\nprint(A.x, b.x)",["2 2","1 1","1 2","2 1"],2,"Assigning B.x creates a class variable on B that shadows A.x, so b.x finds 2 on B while A.x stays 1. 2 2 is tempting if you think the assignment changes the inherited variable in A.","Domain 4: Inherited class variables"],
    ["x47",0,4,"Class Duck(Swimmer, Flyer) inherits move() from both bases and Duck().move() uses Swimmer's version. The team must keep the base order but wants Duck objects to use Flyer's move(). Which change works?",["Call Duck.__mro__.reverse() once at start-up","Define move(self) in Duck that returns Flyer.move(self)","Assign Duck.__bases__[0] = Flyer inside __init__","Add a class variable move = 'Flyer' to Duck"],1,"Overriding move() in Duck and calling Flyer.move(self) explicitly selects Flyer's behaviour while keeping the base order. Editing __bases__ is tempting, but tuples cannot be changed item by item.","Domain 4: Method overriding and MRO"],
    ["x48",0,5,"What is the output of the following code?\nprint([c for w in ['ab', 'cd'] for c in w if c != 'c'])",["['a', 'b', 'd']","['a', 'b', 'c', 'd']","[['a', 'b'], ['d']]","['ab', 'd']"],0,"The comprehension loops over each word and then each character, keeping characters other than 'c', and produces one flat list. A nested list is tempting, but that needs a comprehension inside a comprehension.","Domain 5: List comprehensions with nested loops"],
    ["x49",0,5,"What is the output of the following code?\nprint(list(map(lambda a, b: a + b, [1, 2, 3], [10, 20])))",["[11, 22, 3]","[11, 22]","[11, 22, 33]","A TypeError is raised"],1,"With two iterables, map() calls the function with one item from each and stops at the shortest, giving [11, 22]. [11, 22, 3] is tempting, but the extra item is simply dropped.","Domain 5: map() with several iterables"],
    ["x50",0,5,"What is the output of the following code?\nprint(list(filter(None, [0, 1, '', 'a', None, 2])))",["[0, '', None]","[]","[0, 1, '', 'a', None, 2]","[1, 'a', 2]"],3,"When the function argument is None, filter() keeps the items that are truthy, so 0, '' and None are dropped. Expecting the list unchanged is tempting, but None means use each item's own truth value.","Domain 5: filter()"],
    ["x51",0,5,"`fs = [lambda: i for i in range(3)]` makes `[f() for f in fs]` return [2, 2, 2]. Which rewrite makes it return [0, 1, 2]?",["fs = [lambda i=i: i for i in range(3)]","fs = [lambda: (i) for i in range(3)]","fs = [lambda: i for i in list(range(3))]","fs = [lambda: i for i in range(3)][::-1]"],0,"A default value is evaluated when each lambda is created, so i=i captures the current value and fixes late binding. Parentheses around i are tempting but change nothing, since the name is still looked up when the lambda runs.","Domain 5: Closures and late binding"],
    ["x52",0,5,"What is the output of the following code?\ndef mult(n):\n    return lambda x: x * n\nd, t = mult(2), mult(3)\nprint(d(t(4)))",["12","8","24","14"],2,"Each call to mult() returns a closure remembering its own n: t(4) is 12 and d(12) is 24. 12 is tempting if you stop after the inner call.","Domain 5: Closures"],
    ["x53",0,5,"What is the output of the following code?\ndef countdown(n):\n    while n > 0:\n        yield n\n        n -= 1\nprint(list(countdown(3)), sum(countdown(4)))",["[3, 2, 1, 0] 10","[2, 1, 0] 6","[3, 2, 1] 10","[3, 2, 1] 6"],2,"The generator yields n and then decrements it while n > 0, giving 3, 2, 1 and, for 4, a sum of 10. Including 0 is tempting, but the loop test fails before 0 can be yielded.","Domain 5: Generators and yield"],
    ["x54",0,5,"What is the output of the following code?\ng = (x * 2 for x in range(3))\nprint(sum(g), sum(g))",["6 0","6 6","0 6","6, then StopIteration from the second sum"],0,"A generator is a one-shot iterator: the first sum consumes 0, 2 and 4, giving 6, and the second finds nothing and returns 0. 6 6 is tempting if you treat the generator like a list.","Domain 5: Generators are one-shot"],
    ["x55",0,5,"A script must create report.txt but must never overwrite an existing file; if the file exists, open() should fail. Which mode should it use?",["'w'","'a+'","'wb'","'x'"],3,"Mode 'x' is exclusive creation: it creates a new file and raises FileExistsError if the file already exists. 'w' is tempting because it creates files, but it silently truncates an existing one.","Domain 5: open() modes"],
    ["x56",0,5,"A script calls `open('cfg.txt', 'r+')`, but cfg.txt does not exist. What happens?",["An empty file is created for reading and writing","A FileNotFoundError is raised","The file is created when write() is first called","open() returns None"],1,"Modes based on 'r', including 'r+', require the file to exist, so open() raises FileNotFoundError (errno ENOENT). Creating an empty file is tempting, but that is the behaviour of 'w+' or 'a+'.","Domain 5: open() modes"],
    ["x57",0,5,"A text file notes.txt holds two lines, one and two, each ending with a newline. What does `print(open('notes.txt').readlines())` output?",["['one', 'two']","'one\\ntwo\\n'","['one\\n', 'two\\n', '']","['one\\n', 'two\\n']"],3,"readlines() returns a list with one string per line, each keeping its trailing newline. ['one', 'two'] is tempting because that is what split lines look like, but newlines are not stripped.","Domain 5: readlines()"],
    ["x58",0,5,"A script tries to open a file for writing, but its user account lacks permission to modify that file. Which errno value does the resulting OSError carry?",["errno.ENOENT","errno.EACCES","errno.EEXIST","errno.EBADF"],1,"EACCES means permission denied, which is what the OS reports when the account may not write the file. ENOENT is tempting because it is the most common open() error, but it means the file is missing.","Domain 5: errno values"],
    ["x59",0,5,"A script runs `open('out.bin', 'wb').write('hi')`. What happens?",["'hi' is written using UTF-8 automatically","A ValueError is raised as the file is not open for text","A TypeError is raised because binary mode needs bytes","'hi' is written followed by a newline"],2,"A file opened in binary mode accepts bytes-like objects, so passing a str raises TypeError. Automatic UTF-8 encoding is tempting, but that happens only in text mode.","Domain 5: Text versus binary mode"],
    ["x60",0,5,"data.txt contains the two lines abc and def, each ending with a newline. What does this code print?\nf = open('data.txt')\nf.read(2)\nprint(repr(f.readline()))",["'c\\n'","'abc\\n'","'def\\n'","'c'"],0,"read(2) moves the stream position past a and b, so readline() returns the rest of the first line, 'c\\n'. 'abc\\n' is tempting if you think readline() starts from the beginning of a line.","Domain 5: read() and readline()"]
  ],
  whys: {
    "x1": [null,"import shop.billing.tax binds only the top-level name shop, so tax on its own is a NameError.","The name billing is not bound in the script; it is only reachable as an attribute of shop.","rate() lives in the tax module, not in the shop package's own namespace, so shop.rate is an AttributeError."],
    "x2": ["dir() with no argument lists the current module's names, and import math added the name math there.",null,"import math binds only math; pi stays inside the module and is not a name in the script's namespace.","dir(math) lists every name the math module defines, and sqrt is one of them."],
    "x3": ["Each choice() call is independent, so the same name can be picked more than once.","random.random() takes no arguments and returns a float in [0.0, 1.0), so this call raises a TypeError.",null,"Seeding the generator does not shuffle the list; names[:3] always returns the first three entries."],
    "x4": ["machine() returns the hardware type, such as 'x86_64' or 'AMD64', not the operating system name.","processor() returns the processor name, which may even be an empty string, not the OS name.","python_implementation() returns the interpreter, such as 'CPython' or 'PyPy', not the OS.",null],
    "x5": ["When a script file is run, sys.path[0] is the script's own folder, not the directory the shell was in.","The standard library comes after the script's directory in sys.path, so it is not searched first.",null,"site-packages holds installed third-party packages and appears later in sys.path."],
    "x6": ["The second import finds counter in sys.modules and reuses it, so the module code does not run again.","Importing a module executes its top-level code on the first import, so print does run.","Re-importing an already loaded module is legal and silently reuses the cached module object.",null],
    "x7": [null,"It is the reverse: an imported module's __name__ is 'util', so the condition is False on import.","The condition depends on __name__, which differs between direct execution and import.","Python sets __name__ to '__main__' automatically for the script being run, so no manual assignment is needed."],
    "x8": ["The except IndexError branch is never reached because the earlier Exception branch already matches.",null,"Only the first matching except branch runs; Python never runs two branches for one exception.","An unreachable except branch is legal syntax; Python does not reject the order."],
    "x9": ["KeyError is not a ValueError, so the except ValueError branch does not run and V is never printed.",null,"finally runs before the exception leaves f(), so F comes before K.","The unhandled KeyError propagates out of f() and is caught by the caller, which then prints K."],
    "x10": [null,"ZeroDivisionError derives from ArithmeticError, which derives from Exception.","KeyError derives from LookupError, which derives from Exception.","AssertionError derives directly from Exception."],
    "x11": ["args[1] is the integer 2, not the string 'a'; 'a' is at index 0.","The value is the int 2, so multiplying gives 4 rather than repeating a string.","args holds both arguments passed to the constructor, so index 1 exists.",null],
    "x12": ["PaymentError also matches CardDeclined, so the specific branch below it would never run.","Catching Exception also swallows unrelated errors, and matching message text is fragile.",null,"A tuple gives both types one shared branch, and else runs only when no exception occurred."],
    "x13": ["Raising a class makes Python create an instance of it, which except ValueError catches normally.","Both forms raise an instance of a subclass of Exception, so except Exception catches either.","Raising an exception class without parentheses is valid Python 3 syntax.",null],
    "x14": ["math.sqrt(-1) raises ValueError (math domain error), which is not an ArithmeticError subclass.","math.sqrt does not return nan for negative input; it raises an exception.",null,"Only one except branch runs for a single exception."],
    "x15": ["That is the message of the original ZeroDivisionError, which was replaced by the new ValueError.",null,"The outer handler catches the ValueError, and print shows its message, not a class name.","print(e) uses str(), which shows only the message, not the constructor call form."],
    "x16": [null,"Stepping back by 3 from index 7 gives indices 7, 4, 1, which are h, e, b.","The slice starts at index 1 (b), not index 0, so it yields b, d, f.","Index 4 is e, not d; a step of -3 from h goes h, e, b."],
    "x17": ["Multiplying a string by an int repeats it; it does not append the number as text.",null,"'c' * 0 is the empty string, so no c appears in the result.","'ab' * 2 repeats the text to give abab, not ab."],
    "x18": ["Each escape sequence (\\' and \\n) is one character, so they are not counted as two.","Counting backslashes as separate characters gives 7, but escapes produce one character each.","The newline escape is a real character and must be counted.",null],
    "x19": [null,"Lowercase letters have higher code points than uppercase, so 'apple' is greater than 'Apple'.","'Z' is code point 90 and 'a' is 97, so 'Zoo' sorts before 'apple'.","A string that is a prefix of another is smaller, so 'a' < 'ab' is True."],
    "x20": ["islower() ignores digits and needs only the cased characters to be lowercase, so 'abc1' gives True.","An empty string returns False for isalpha(), and 'A1' is True for isupper() because its only cased letter is upper.",null,"Tab and newline are whitespace characters, so isspace() is True."],
    "x21": ["Each search has a start index: the 'ss' at 2 lies before start 3 and the 'i' at 4 lies before start 5, so both are skipped.","Searching for 'i' from index 5 skips the i at index 4 and finds the one at 7.",null,"The search for 'ss' starts at index 3, so the match at 2 is skipped and 5 is found."],
    "x22": [null,"Strings are immutable, so item assignment raises a TypeError.","replace() takes substrings, not a position, so passing the int 2 raises a TypeError.","Strings have no insert() method; that is a list method."],
    "x23": ["The second argument limits the number of splits, not the number of pieces kept; the rest is kept too.","Splitting happens from the left at the first two spaces, not into two halves.","With a maxsplit of 2 only two splits happen, so 'c d' stays together.",null],
    "x24": ["upper() is applied to chr(97), so the letter becomes uppercase A.",null,"ord('0') is the code point of the digit character, 48, not the number 0.","48 is the decimal code point of '0'; 30 is its hexadecimal form, which str() does not produce."],
    "x25": ["Strings cannot be changed in place, and there is no sort() method to do it.","No sort() method exists on str; sorted(word) is the function that returns a new list.","The failure happens at attribute lookup, before any change is attempted, so it is AttributeError.",null],
    "x26": ["The str holds one code point, so its length is 1 regardless of how it is encoded.",null,"UTF-8 needs two bytes for code point U+00E9, so the bytes object has length 2.","The character count and byte count are swapped here; the str is 1 and the bytes are 2."],
    "x27": ["a.v + [1] builds a new list and assigns it to an instance attribute, so a and the class no longer share a list.","b.v.append(2) mutates the shared class list, so A.v is [2], not empty.",null,"a.v was rebound to its own new list before the append, so it does not see the 2."],
    "x28": [null,"Name mangling renames the method to _A__hid, and that name really exists on the class.","Inside the class body self.__hid is mangled to self._A__hid, so the call succeeds.","show() returns the result of calling the method, which is the string 'h'."],
    "x29": [null,"The attribute was stored as _A__x, but code in B looks up _B__x, so it is not found.","A missing attribute raises an error; Python never substitutes None.","Mangling is per class, not per module, and the failure is an attribute lookup, not a name lookup."],
    "x30": ["b is a class variable stored in A.__dict__, not in the instance's own __dict__.","o.c = 3 adds an instance attribute after construction, and it appears in o.__dict__.",null,"The instance __dict__ holds instance variables, and b is not one of them."],
    "x31": ["super() in B follows D's MRO, where the next class after B is C, not A.",null,"The MRO lists bases left to right, so B comes before C.","Each class appears once in the MRO, so A.f runs only once."],
    "x32": ["issubclass() treats a class as a subclass of itself, so issubclass(B, B) is True.","An A object is not a B, because A is the parent, not the child.","Every class derives from object, so every instance is an object.",null],
    "x33": [null,"__str__ concatenates without a space, so there is no space between P and 3.","Because __str__ is defined, str() uses it instead of the default object description.","str() returns a string, so adding '!' is ordinary concatenation."],
    "x34": ["type(A) is the class of the class itself, which is type, not A.",null,"The metaclass of an ordinary class is named type; class is a keyword, not a type name.","A's base class is object, but its type is type."],
    "x35": ["The default list is created once, so both objects share it and b sees the appended 'x'.","Only one append happened, so the shared list contains a single 'x'.",null,"items defaults to a list, so b.items is a list rather than None."],
    "x36": ["Encapsulation is about hiding data behind methods, not about one call working on different types.","Name mangling renames double-underscore attributes and has nothing to do with dispatching calls.","Composition means an object contains other objects; it does not explain a shared method name.",null],
    "x37": ["Inheritance lets a class reuse another class's members; it does not restrict access.","Introspection is examining objects at run time, for example with hasattr() or type().",null,"Overriding replaces an inherited method in a subclass, which is unrelated to hiding data."],
    "x38": ["c has no instance attribute rate, so it reads the class variable, which is now 5.","Changing C.rate changes only the class; nothing is copied into c.__dict__.","The instance never received its own copy, so the old value 1 is not kept.",null],
    "x39": [null,"x is an instance variable that exists only after an object is created, so the class does not have it.","__init__ is defined in the class body, so the class has that attribute.","The class defines __init__ itself, so hasattr(A, '__init__') cannot be False."],
    "x40": ["self is the object a, and its instance attribute name shadows the class variable.",null,"Calling a method through the class is legal when the instance is passed explicitly as self.","self.name reads an attribute value, not the class's __name__."],
    "x41": ["__bases__ holds only the direct bases listed in the class statement, not object.",null,"Index 1 is the second base, B; A is at index 0.","C lists two bases, so the tuple has two items."],
    "x42": [null,"A comes first in the base list, so its __init__ is found first in the MRO.","Python calls only the first __init__ it finds; it does not run every base's constructor.","Bases are searched left to right and only one constructor runs."],
    "x43": ["Comparing with == None is legal and usually returns False rather than raising.","is does not compare values at all; it checks whether two names refer to the same object.","Empty lists and 0 are different objects from None, so is None is False for them.",null],
    "x44": ["__main__ is the module name of the running script, but Circle was defined in shapes.","__module__ names the module, not the class; the class name is in __name__.",null,"__module__ holds the module name without the .py file extension."],
    "x45": ["Nothing sets n to 0; the only assignment stores the value 7.","A.__init__ runs and assigns self.n = 7, so the attribute has a value.","B.__init__ takes no extra arguments and passes self and 7 to A.__init__ correctly.",null],
    "x46": ["B.x = 2 creates a new class variable on B; it does not change A.x.","b finds x on B first, and B now has its own x equal to 2.",null,"Both values are wrong: A keeps 1 and b sees B's new 2."],
    "x47": ["__mro__ is a tuple, which has no reverse() method, so this raises AttributeError.",null,"__bases__ is a tuple, so assigning to one of its items raises a TypeError.","This replaces move with a string, so Duck().move() fails because a str is not callable."],
    "x48": [null,"The if filter removes 'c', so it is not in the result.","Two for clauses in one comprehension produce a flat list, not nested lists.","The inner loop goes character by character, so 'ab' is split into 'a' and 'b'."],
    "x49": ["map() does not pass leftover items through; it stops when any iterable runs out.",null,"The second list has only two items, so there is no 30 to add to 3.","map() accepts several iterables of different lengths without error."],
    "x50": ["filter() keeps the truthy items and drops the falsy ones, not the other way round.","Some items are truthy, so the result is not empty.","Passing None does not mean no filtering; it means keep the items that are true.",null],
    "x51": [null,"Parentheses do not change when i is looked up; it is still read at call time.","Converting the range to a list does not change the late binding of i.","Reversing the list changes the order of functions, but each still returns 2."],
    "x52": ["12 is only t(4); the result is then doubled by d.","8 is only d(4); t is applied first, giving 12.",null,"The closures multiply, they do not add, so the result is not 14."],
    "x53": ["The loop stops when n reaches 0, so 0 is never yielded.","n is yielded before it is decremented, so the first value is 3.",null,"sum(countdown(4)) adds 4, 3, 2 and 1, which is 10."],
    "x54": [null,"A generator can be consumed only once, so the second sum sees no items.","The first sum consumes all values; the second sees none, not the other way round.","sum() handles the end of an iterator itself and returns 0 for an empty one."],
    "x55": ["'w' truncates an existing file, destroying its contents.","'a+' opens an existing file for appending instead of failing.","'wb' is write mode in binary and still truncates an existing file.",null],
    "x56": ["'r+' needs an existing file; 'w+' is the mode that creates one.",null,"open() checks for the file immediately; it does not wait until a write.","open() never returns None; it returns a stream or raises an exception."],
    "x57": ["readlines() keeps the newline character at the end of each line.","read() would return a single string; readlines() returns a list.","readlines() does not add an empty string at the end of the file.",null],
    "x58": ["ENOENT means the file or directory does not exist, not that access is denied.",null,"EEXIST means a file already exists, as when 'x' mode meets an existing file.","EBADF means an invalid file descriptor, such as one that was already closed."],
    "x59": ["Binary streams do not encode text; the caller must pass bytes or bytearray.","The error concerns the argument's type, so Python raises TypeError, not ValueError.",null,"write() never adds a newline, and here it fails before writing anything."],
    "x60": [null,"read(2) already consumed a and b, so readline() continues from the current position.","readline() finishes the current line first, which still has c and a newline left.","readline() keeps the newline that ends the line."]
  },
  levels: {
    "pc1": 2, "pc2": 1, "pc3": 1, "pc4": 2, "pc5": 2, "pc6": 1, "pc7": 1, "pc8": 1, "pc9": 1, "pc10": 2,
    "pc11": 3, "pc12": 2, "pc13": 2, "pc14": 1, "pc15": 2, "pc16": 3, "pc17": 2, "pc18": 1, "pc19": 2, "pc20": 1,
    "pc21": 3, "pc22": 2, "pc23": 1, "pc24": 3, "pc25": 2, "pc26": 1, "pc27": 2, "pc28": 1, "pc29": 2, "pc30": 1,
    "pc31": 3, "pc32": 2, "pc33": 2, "pc34": 3, "pc35": 1, "pc36": 2, "pc37": 2, "pc38": 1, "pc39": 1, "pc40": 2,
    "pc41": 2, "pc42": 1, "pc43": 1, "pc44": 2, "pc45": 3, "pc46": 2, "pc47": 3, "pc48": 3, "pc49": 2, "pc50": 3,
    "pc51": 1, "pc52": 2, "pc53": 3, "pc54": 1, "pc55": 1, "pc56": 3, "pc57": 1, "pc58": 3, "pc59": 2, "pc60": 2,
    "pc61": 3, "pc62": 2, "pc63": 3, "pc64": 3, "pc65": 2, "pc66": 1, "pc67": 2, "pc68": 1, "pc69": 1, "pc70": 3,
    "pc71": 2, "pc72": 3, "pc73": 2, "pc74": 1, "pc75": 1, "pc76": 1, "pc77": 3, "pc78": 1, "pc79": 1, "pc80": 3,
    "pc81": 1, "pc82": 1, "pc83": 1, "pc84": 3, "pc85": 1, "pc86": 2, "pc87": 2, "pc88": 2, "pc89": 2, "pc90": 1,
    "x1": 2, "x2": 2, "x3": 2, "x4": 1, "x5": 2, "x6": 2, "x7": 1, "x8": 2, "x9": 3, "x10": 1,
    "x11": 2, "x12": 3, "x13": 2, "x14": 3, "x15": 2, "x16": 2, "x17": 1, "x18": 2, "x19": 2, "x20": 3,
    "x21": 2, "x22": 1, "x23": 3, "x24": 2, "x25": 2, "x26": 3, "x27": 3, "x28": 2, "x29": 3, "x30": 2,
    "x31": 3, "x32": 2, "x33": 1, "x34": 2, "x35": 3, "x36": 1, "x37": 1, "x38": 2, "x39": 2, "x40": 2,
    "x41": 2, "x42": 2, "x43": 2, "x44": 2, "x45": 2, "x46": 2, "x47": 3, "x48": 2, "x49": 3, "x50": 3,
    "x51": 3, "x52": 2, "x53": 2, "x54": 3, "x55": 1, "x56": 2, "x57": 2, "x58": 1, "x59": 2, "x60": 3
  }
});
