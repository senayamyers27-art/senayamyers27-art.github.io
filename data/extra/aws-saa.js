/* More aws-saa practice questions with wrong-answer notes, and a difficulty level for every question. Merged into the free bank by tools/build.js. */
CertHub.addExtra("aws-saa", {
  questions: [
    ["x1",0,1,"In a single AWS account, an IAM user has no identity-based policy that mentions a particular S3 bucket. That bucket's policy grants the user's ARN s3:GetObject, and no policy anywhere contains a deny. What happens when the user downloads an object?",["It is denied, because the user's own identity-based policy must also contain an allow for s3:GetObject","It is denied unless the user belongs to an IAM group with an S3 policy","It is allowed, because in one account an allow in either policy type is enough","It is allowed only when the request is sent through an S3 access point"],2,"Within the same account, IAM evaluates identity-based and resource-based policies together, and an allow in either one grants access when there is no explicit deny. The need for an allow on both sides only applies to cross-account requests.","Domain 1: IAM policy evaluation logic"],
    ["x2",0,1,"An IAM role in account B has an identity policy that allows s3:GetObject on a bucket owned by account A. The bucket uses SSE-S3 encryption, yet the role gets AccessDenied. What is most likely missing?",["A bucket policy in account A granting the account B role access","A service control policy in account B that explicitly allows Amazon S3 actions","A KMS grant that lets the role use the key protecting the objects","An S3 gateway endpoint policy in account A that names the role"],0,"For cross-account access, both sides must allow the request: the caller's identity policy in account B and a resource-based policy (the bucket policy) in account A. SSE-S3 keys are managed by S3, so no KMS permission is involved, which rules out the KMS grant.","Domain 1: Cross-account resource-based policies"],
    ["x3",0,1,"A web application needs sign-up and sign-in pages, optional MFA and a managed user directory. Its Amazon API Gateway REST API must accept only requests that carry a valid token from a signed-in user. What should the architect use?",["A Cognito identity pool alone, with the API set to IAM authorization for guests","A Cognito user pool, with a Cognito user pool authorizer on the API","An IAM user for each customer, with access keys handed out at sign-up","STS GetFederationToken calls made by the browser for every visitor"],1,"A Cognito user pool is the user directory that handles sign-up, sign-in and MFA and issues JWTs, and API Gateway can validate those tokens with a user pool authorizer. An identity pool only exchanges identities for temporary AWS credentials; it is not a user directory.","Domain 1: Cognito user pools vs identity pools"],
    ["x4",0,1,"EC2 instances in private subnets with no NAT gateway must fetch secrets from AWS Secrets Manager. Traffic must stay on the AWS network. What should the architect add?",["A gateway VPC endpoint for Secrets Manager in the private route tables","A NAT instance in a public subnet with a route from the private subnets","A VPC peering connection to the VPC that hosts Secrets Manager","An interface VPC endpoint for Secrets Manager with private DNS enabled"],3,"Secrets Manager is reached privately through an interface endpoint (AWS PrivateLink), which places ENIs in the subnets; with private DNS the normal service hostname resolves to them. Gateway endpoints exist only for S3 and DynamoDB, so a gateway endpoint for Secrets Manager is not possible.","Domain 1: Interface endpoints (PrivateLink)"],
    ["x5",0,1,"Instances in private subnets reach Amazon S3 through a gateway VPC endpoint. Security wants to stop anyone copying data from those instances to S3 buckets outside the company. What control does this?",["An endpoint policy allowing access only to the company's buckets","Bucket policies on the company's buckets that require the aws:SourceVpce condition key","A security group attached to the gateway endpoint that allows only company bucket names","A network ACL rule on the private subnets that lists the approved bucket names"],0,"A VPC endpoint policy controls which resources can be reached through the endpoint, so limiting it to company buckets blocks uploads to outside buckets. Bucket policies with aws:SourceVpce restrict who can reach the company's own buckets, but do nothing to stop writes to a bucket someone else owns.","Domain 1: VPC endpoint policies"],
    ["x6",0,1,"A small company serves its website through Amazon CloudFront with DNS in Route 53 and has never bought any extra security service. What protection does it have against common network and transport layer DDoS attacks?",["None, until it subscribes to AWS Shield Advanced","AWS Shield Standard, applied automatically at no extra charge","AWS WAF managed rules, which are enabled on every distribution","Amazon GuardDuty, which blocks attack traffic at the edge"],1,"AWS Shield Standard is included automatically for all AWS customers and defends against common layer 3 and 4 attacks, notably on CloudFront and Route 53. Shield Advanced adds extras such as the response team and cost protection, but basic protection does not depend on it.","Domain 1: Shield Standard vs Shield Advanced"],
    ["x7",0,1,"A company runs its application in eu-west-1 and puts an Amazon CloudFront distribution in front of it with a custom domain name. The team imported a certificate into ACM in eu-west-1, but it does not appear when they edit the distribution. What is the cause?",["CloudFront accepts only private certificates issued by AWS Private CA","The certificate must be validated by email instead of by DNS records","CloudFront only uses ACM certificates from US East (N. Virginia)","CloudFront distributions can only use certificates stored in IAM"],2,"CloudFront is a global service that reads ACM certificates from us-east-1 only, so the certificate must be requested or imported there. The validation method (DNS or email) does not affect which Region's certificates CloudFront can list.","Domain 1: ACM certificates for CloudFront"],
    ["x8",0,1,"A bucket in account A encrypts objects with SSE-KMS using a customer managed key in account A. A role in account B is already allowed by the bucket policy, but GetObject fails with a KMS access error. What else is required?",["Copy the key material into account B and create a matching key there","Switch the bucket to the AWS managed key aws/s3 so both accounts can use it","Attach an SCP to account B that allows kms:Decrypt on all keys","Allow account B in the key policy and give the role kms:Decrypt on that key"],3,"Cross-account use of a KMS key needs both the key policy in the owning account to allow the other account and an IAM policy in account B that grants the role kms:Decrypt on the key. AWS managed keys cannot have their key policies edited, so they cannot be shared across accounts.","Domain 1: KMS key policies and cross-account use"],
    ["x9",0,1,"A company must generate and hold its own encryption keys outside AWS. It is willing to send the key with each request so S3 can encrypt and decrypt objects, but AWS must not store the key. Which option fits?",["SSE-C, server-side encryption with customer-provided keys","SSE-KMS using a KMS key with imported key material","SSE-S3, server-side encryption with Amazon S3 managed keys","SSE-KMS using the AWS managed key for Amazon S3"],0,"With SSE-C the client supplies the key on every request, S3 uses it to encrypt or decrypt and then discards it, keeping only a value to verify later requests. Imported key material is still stored and used inside AWS KMS, so it does not meet the 'AWS must not store the key' rule.","Domain 1: S3 encryption options (SSE-C)"],
    ["x10",0,1,"A company wants a guarantee that no S3 bucket in an account can become public, even if a developer later writes a bucket policy or ACL that grants public access. What should the architect enable?",["An AWS Config rule that reports buckets allowing public read access","S3 Block Public Access settings at the account level","Amazon Macie scans of every bucket in the account each day","Default encryption with SSE-KMS on every bucket in the account"],1,"Account-level S3 Block Public Access overrides bucket policies and ACLs that would grant public access, so it prevents exposure rather than detecting it. An AWS Config rule only reports a public bucket after the fact and does not stop the change.","Domain 1: S3 Block Public Access"],
    ["x11",0,1,"A versioned S3 bucket holds financial records. The company wants anyone who permanently deletes an object version or suspends versioning to have to supply an MFA code. How is this set up?",["Enable Object Lock in governance mode on the bucket","Rely on versioning, which requires MFA for version deletes by default","Enable MFA Delete with the root user's credentials via API or CLI","Enable MFA Delete in the S3 console as any user with full S3 permissions"],2,"MFA Delete requires an MFA code to permanently delete versions or change the versioning state, and it can only be enabled by the bucket owner's root user through the API or CLI. The console option and non-root administrators cannot turn it on, which is why the fourth option fails.","Domain 1: S3 versioning and MFA Delete"],
    ["x12",0,1,"A company wants continuous, automated scanning of its EC2 instances and Amazon ECR container images for known software vulnerabilities, plus reports of unintended network reachability. Which service provides this?",["Amazon GuardDuty","Amazon Macie","AWS Trusted Advisor","Amazon Inspector"],3,"Amazon Inspector scans EC2 instances, ECR images and Lambda functions for known CVEs and assesses network reachability of instances. GuardDuty detects threats from log analysis, such as malicious traffic, but it does not inventory software vulnerabilities.","Domain 1: Amazon Inspector"],
    ["x13",0,1,"A company with 50 accounts wants one place to view GuardDuty, Inspector and Macie findings from every account and to run automated checks against the AWS Foundational Security Best Practices standard. What should the architect set up?",["AWS Security Hub with a delegated administrator account for the organization","An AWS Config aggregator in the security account","Amazon Detective enabled in the management account","A CloudTrail Lake event data store for all accounts"],0,"Security Hub aggregates findings from GuardDuty, Inspector, Macie and other services across an organization and runs security standards checks. A Config aggregator collects configuration and compliance data only; it does not gather findings from those other services.","Domain 1: AWS Security Hub"],
    ["x14",0,1,"An organization must log API activity from every current and future member account into one S3 bucket in a log archive account, and member account administrators must not be able to change or delete that logging. What should the architect create?",["A separate trail in each account, created once by an administrator","An organization trail created from the management account","An AWS Config aggregator that collects every account's resource history","VPC Flow Logs in each VPC, published to the log archive account"],1,"An organization trail logs events for all member accounts, including ones added later, and member accounts can see it but cannot modify or delete it. Per-account trails can be stopped by each account's administrators and do not cover new accounts automatically.","Domain 1: CloudTrail organization trails"],
    ["x15",0,1,"An IAM role's identity policy allows s3:* and ec2:*. The role has a permissions boundary that allows s3:* and dynamodb:*. No other policies apply. Which actions can the role perform?",["S3, EC2 and DynamoDB actions, the union of both policies","S3 and EC2 actions, because the identity policy grants the permissions","Only S3 actions, where the identity policy and the boundary overlap","No actions, because a permissions boundary acts as an explicit deny"],2,"A permissions boundary sets the maximum permissions; effective permissions are the intersection of the identity policy and the boundary, which here is only S3. The boundary grants nothing by itself, so DynamoDB is not allowed even though the boundary lists it.","Domain 1: Permissions boundaries"],
    ["x16",0,1,"Application servers listen on port 8080 in private subnets behind an Application Load Balancer. They must accept traffic only from the load balancer, whose IP addresses change over time. How should the architect configure access?",["Allow the ALB subnets' CIDR ranges on port 8080 in the network ACL only","Allow the current private IP addresses of the ALB's network interfaces","Allow 0.0.0.0/0 on port 8080 and rely on the subnets being private","Allow port 8080 in the app security group with the ALB's security group as source"],3,"Referencing the ALB's security group as the source allows traffic from any interface in that group, so it keeps working as the load balancer's IPs change. Hard-coding the ALB's current IPs breaks when AWS replaces or scales the load balancer nodes.","Domain 1: Security group referencing"],
    ["x17",0,1,"An application needs dozens of configuration values, a few of which are sensitive and must be encrypted. None need automatic rotation, and the company wants the lowest-cost managed store. What should the architect use?",["Systems Manager Parameter Store standard parameters, with SecureString for sensitive values","AWS Secrets Manager for every value, including non-sensitive settings","A JSON file in S3 encrypted with SSE-S3 and downloaded at startup","Plain-text Lambda environment variables set in the function configuration"],0,"Parameter Store standard parameters carry no additional charge and SecureString encrypts sensitive values with KMS, which fits when rotation is not needed. Secrets Manager adds managed rotation but charges per secret, which is unnecessary cost for plain configuration.","Domain 1: Parameter Store vs Secrets Manager"],
    ["x18",0,1,"A company in AWS Organizations must block use of every Region except eu-west-1 and eu-central-1 in all accounts, while still letting global services such as IAM work. What is the most effective control?",["Disable all other Regions for each account in the account settings page","An SCP that denies actions when aws:RequestedRegion is not an approved Region, exempting global services","An AWS Config rule in each account that flags resources in other Regions","An IAM policy in each account that denies ec2:* outside the two Regions"],1,"An SCP with a Deny on aws:RequestedRegion applies to every principal in the member accounts, and excluding global services keeps IAM and similar services working. Default Regions cannot be switched off in account settings; only opt-in Regions can be disabled, so the first option cannot work.","Domain 1: SCP Region restriction"],
    ["x19",0,2,"A financial trading service uses a custom TCP protocol, must handle millions of requests per second with very low latency, needs one static IP address per Availability Zone, and must see the client's source IP. Which load balancer fits?",["Application Load Balancer","Classic Load Balancer","Network Load Balancer","Gateway Load Balancer"],2,"A Network Load Balancer works at layer 4, handles very high request rates at low latency, supports a static or Elastic IP per AZ and can preserve client source IPs. An Application Load Balancer handles HTTP and HTTPS at layer 7 and does not offer static IP addresses.","Domain 2: ALB vs NLB"],
    ["x20",0,2,"Workers poll an SQS standard queue, and each message takes about two minutes to process. The queue uses the default visibility timeout, and many orders are being processed twice. What should the architect change?",["Turn on long polling by setting a receive wait time","Switch to a FIFO queue to get exactly-once processing","Shorten the message retention period of the queue","Increase the queue's visibility timeout"],3,"The default visibility timeout is 30 seconds, so messages reappear while still being worked on and another worker picks them up. Setting it above the processing time fixes this. Long polling only reduces empty receives and does not stop messages becoming visible again.","Domain 2: SQS visibility timeout"],
    ["x21",0,2,"A company wants a Lambda function to run whenever any EC2 instance changes state, and it also wants to receive events from a supported SaaS partner application in the same event-routing service. What should the architect use?",["EventBridge rules that match the events and invoke the function","An SNS topic that EC2 publishes instance state changes to directly","CloudTrail Insights with a Lambda function subscribed to it","An SQS queue that the function polls for EC2 state changes"],0,"EventBridge receives AWS service events such as EC2 state-change notifications and also SaaS partner events, and rules route matching events to targets like Lambda. EC2 does not publish state changes to an SNS topic on its own; that would still need an EventBridge rule.","Domain 2: EventBridge event-driven design"],
    ["x22",0,2,"A serverless REST API built on API Gateway and Lambda is offered to external partners. One partner's bursts must not be able to overwhelm the back end for the others. What should the architect configure?",["Reserved concurrency on the Lambda function behind the API","API Gateway usage plans with API keys and throttling","A CloudFront distribution in front of the API to cache responses","An AWS WAF geographic match rule for each partner's country"],1,"Usage plans attach throttling and quota limits to each API key, so each partner is limited separately. Reserved concurrency caps the whole function, which protects the back end but lets one partner use up the shared capacity for everyone else.","Domain 2: API Gateway throttling and usage plans"],
    ["x23",0,2,"A company runs its applications on Kubernetes on premises, using Helm charts and kubectl-based tooling. It wants a managed control plane on AWS with minimal changes to its deployment process. What should the architect recommend?",["Amazon ECS with the EC2 launch type","AWS Elastic Beanstalk with Docker platforms","Amazon EKS","AWS App Runner"],2,"Amazon EKS runs a managed, conformant Kubernetes control plane, so existing Helm charts and kubectl tooling keep working. ECS is a container orchestrator with its own task definitions and API, so moving to it would mean rewriting deployments.","Domain 2: ECS vs EKS"],
    ["x24",0,2,"An Aurora MySQL cluster has a single writer instance. The company wants automatic failover inside the Region if the writer fails and wants to spread read traffic. What should the architect do?",["Create a cross-Region read replica and promote it when the writer fails","Place Amazon RDS Proxy in front of the single writer instance","Enable the RDS Multi-AZ standby option used by RDS for MySQL","Add Aurora Replicas in other AZs; use cluster and reader endpoints"],3,"Aurora Replicas share the cluster volume, serve reads through the reader endpoint and are promoted automatically if the writer fails, with the cluster endpoint following the new writer. A cross-Region replica needs manual promotion and is aimed at Regional DR, not fast in-Region failover.","Domain 2: Aurora replicas and endpoints"],
    ["x25",0,2,"An application runs in us-east-1, eu-west-1 and ap-southeast-1. Each user should be sent to whichever Region gives them the fastest response. Which Route 53 routing policy fits?",["Latency-based routing","Geolocation routing","Multivalue answer routing","Weighted routing"],0,"Latency-based routing answers with the record for the Region that has the lowest measured latency for the user's network location. Geolocation routes by where the user is, which is not always the fastest Region.","Domain 2: Route 53 routing policies"],
    ["x26",0,2,"For legal reasons, users in Germany must be served by a stack in eu-central-1, and everyone else by a stack in us-east-1. Which Route 53 configuration meets this?",["Latency-based routing records for the two Regions","Geolocation routing with Germany and default records","Weighted routing with most weight on the us-east-1 record","Failover routing with eu-central-1 as the primary record"],1,"Geolocation routing answers based on the user's location, so a Germany record plus a default record for everyone else enforces the rule. Latency-based routing could send German users to us-east-1 whenever that is measured as faster, which breaks the legal requirement.","Domain 2: Route 53 geolocation routing"],
    ["x27",0,2,"A company has six web servers without a load balancer. It wants DNS to return several server IP addresses to clients and to stop returning any server that fails a health check. Which Route 53 routing policy fits?",["Simple routing with all six IP addresses in one record","Failover routing with one primary and five secondary records","Multivalue answer routing with a health check on each record","Geoproximity routing with a bias on each server"],2,"Multivalue answer routing returns up to eight healthy records chosen at random and leaves out records whose health checks fail. Simple routing can hold several values but cannot attach health checks, so it keeps returning failed servers.","Domain 2: Route 53 multivalue answer routing"],
    ["x28",0,2,"A company runs physical and VMware servers on premises. It wants continuous block-level replication to AWS with an RPO measured in seconds, a low-cost staging area, and the ability to launch recovery instances within minutes. What should the architect use?",["AWS Backup with cross-Region copies of on-premises backups","AWS DataSync tasks scheduled every few minutes","Amazon S3 Cross-Region Replication of exported disk images","AWS Elastic Disaster Recovery"],3,"AWS Elastic Disaster Recovery continuously replicates source servers at block level into a lightweight staging area and launches full recovery instances on demand. DataSync copies files on a schedule, so it cannot give second-level RPO or launch servers.","Domain 2: AWS Elastic Disaster Recovery"],
    ["x29",0,2,"A pilot light environment in us-west-2 fails to launch its application tier during a DR test because the launch template references a custom AMI ID that exists only in us-east-1. What should the architect do?",["Copy the AMIs to us-west-2 and use the new AMI IDs there","Share the AMI with the account so it becomes visible in every Region","Make the AMI public so that every Region can launch from it","Enable EBS snapshot replication, which copies AMIs to all Regions"],0,"AMIs are Regional resources, so they must be copied to the recovery Region, where they get new IDs that the templates must use. Sharing an AMI grants other accounts access in the same Region; it does not make the AMI available in another Region.","Domain 2: AMI copies for DR"],
    ["x30",0,2,"A security team must deploy the same IAM roles and AWS Config rules to every account in an organizational unit, including accounts added to the OU later. What should the architect use?",["CloudFormation nested stacks deployed from the management account","CloudFormation StackSets with service-managed permissions and automatic deployment","CloudFormation change sets reviewed before each deployment","A separate stack created by hand in each account"],1,"StackSets with service-managed permissions deploy stacks across accounts in an OU, and automatic deployment adds the stack to accounts that join the OU later. Nested stacks only compose templates within one stack in one account and Region.","Domain 2: CloudFormation StackSets"],
    ["x31",0,2,"During a DR test, an Auto Scaling group in the recovery Region cannot launch enough instances and reports that a vCPU limit was exceeded. The same group runs fine in the primary Region. What should the architect do?",["Raise the quota in the primary Region, since quotas apply to all Regions","Use Spot Instances in the recovery Region, which are not counted in quotas","Request vCPU quota increases in the recovery Region in advance","Wait for Trusted Advisor to raise the limit automatically during failover"],2,"Most service quotas, including EC2 vCPU limits, are set per account per Region, so the recovery Region needs its own increases requested in advance. Raising the quota in the primary Region does nothing for the recovery Region.","Domain 2: Service quotas and DR planning"],
    ["x32",0,2,"A burst of events causes one Lambda function to use nearly all of the account's concurrency, and a critical payment function in the same Region starts being throttled. What should the architect do to protect the payment function?",["Increase the memory setting of the payment function","Configure provisioned concurrency on the busy function","Move the busy function into a different AWS account","Configure reserved concurrency on the payment function"],3,"Reserved concurrency sets aside part of the account's concurrency for one function, so other functions cannot consume it. Provisioned concurrency on the busy function pre-initializes environments for speed but does not stop it from taking the rest of the pool.","Domain 2: Lambda concurrency and throttling"],
    ["x33",0,2,"An application needs at least six running instances to handle peak load and must keep full capacity if any single Availability Zone fails. Which Auto Scaling group layout meets this at the lowest cost?",["Nine instances, three in each of three AZs","Six instances spread three per AZ across two AZs","Six instances spread two per AZ across three AZs","Twelve instances spread six per AZ across two AZs"],0,"With three per AZ in three AZs, losing one AZ still leaves six instances, using nine in total. Twelve across two AZs also survives an AZ loss but costs three more instances, and both six-instance layouts drop below six when an AZ fails.","Domain 2: Multi-AZ capacity planning"],
    ["x34",0,2,"A company must keep monthly backups of a DynamoDB table for seven years to meet an audit requirement. What should the architect use?",["Point-in-time recovery with its retention set to seven years","On-demand backups scheduled by an AWS Backup plan with seven-year retention","DynamoDB global tables with a replica in a second Region","DynamoDB Streams with a Lambda function that deletes old items"],1,"On-demand backups are kept until deleted, and an AWS Backup plan can take them monthly and apply a long retention rule. Point-in-time recovery only lets you restore to a point within at most the last 35 days, so it cannot hold seven years of history.","Domain 2: DynamoDB backups"],
    ["x35",0,3,"A company runs a large Cassandra cluster of dozens of nodes on EC2 and wants replicas of each data set placed on separate racks, so one rack failure affects only some nodes. The database should also know which rack each node is on. Which placement strategy fits?",["A cluster placement group in one Availability Zone","A spread placement group across the Availability Zones","A partition placement group, with replicas in different partitions","Dedicated Hosts, with one host allocated for each node"],2,"Partition placement groups put groups of instances on separate racks and expose the partition number, so topology-aware systems like Cassandra, HDFS and Kafka can place replicas in different partitions. Spread groups isolate each instance but allow only seven running instances per AZ, too few for a large cluster.","Domain 3: Placement groups"],
    ["x36",0,3,"A tightly coupled MPI weather model needs inter-node communication that bypasses the operating system network stack to get the lowest latency possible on EC2. What should the architect attach to the instances?",["An additional elastic network interface on each instance","An Elastic IP address on each instance in the cluster","Enhanced networking with ENA only, the default on current types","An Elastic Fabric Adapter (EFA)"],3,"EFA adds OS-bypass capability for HPC and machine learning workloads using MPI or NCCL, cutting latency between instances. ENA provides enhanced networking with high bandwidth, but traffic still goes through the kernel network stack.","Domain 3: Enhanced networking with ENA and EFA"],
    ["x37",0,3,"A video transcoding job needs very fast temporary scratch space for intermediate files. The files are copies of source data kept in S3, so losing them when an instance stops is acceptable. Which storage gives the highest I/O at no extra volume cost?",["Instance store volumes on an instance type that includes them","A Provisioned IOPS SSD (io2) EBS volume attached to the instance","An Amazon EFS file system in Max I/O mode shared by the instances","A General Purpose SSD (gp3) EBS volume with its IOPS raised"],0,"Instance store disks are physically attached to the host, give very high I/O and are included in the instance price, but their data is lost when the instance stops or terminates, which is acceptable here. An io2 volume is durable and fast, but it is billed separately for capacity and IOPS.","Domain 3: Instance store"],
    ["x38",0,3,"A company is moving a NAS that serves the same data to Linux clients over NFS and to Windows clients over SMB, and it relies on NetApp snapshots and SnapMirror replication. Which managed service is the best fit?",["Amazon EFS with lifecycle management enabled","Amazon FSx for NetApp ONTAP","Amazon FSx for Windows File Server","Amazon FSx for Lustre linked to S3"],1,"FSx for NetApp ONTAP supports NFS and SMB access to the same data and provides ONTAP features such as snapshots and SnapMirror. FSx for Windows File Server serves SMB for Windows workloads but not ONTAP replication or multiprotocol NFS use of the same volumes.","Domain 3: Shared file systems (FSx for NetApp ONTAP)"],
    ["x39",0,3,"An analytics application downloads multi-gigabyte objects from S3 in the same Region using a single request per object, and each download is slow. How can the architect speed up the downloads?",["Enable S3 Transfer Acceleration on the bucket","Turn on S3 Versioning for the bucket","Use parallel byte-range fetches","Upload the objects again using multipart upload"],2,"Byte-range fetches let clients download parts of an object over many parallel connections, which raises throughput for large objects. Transfer Acceleration speeds long-distance transfers through edge locations, which does little for a client in the same Region.","Domain 3: S3 byte-range fetches"],
    ["x40",0,3,"A game needs an in-memory cache that keeps sorted leaderboards, replicates data to a replica for failover, and can persist data to disk. Which service fits best?",["Amazon ElastiCache for Memcached","DynamoDB Accelerator (DAX)","Amazon CloudFront with a long TTL","Amazon ElastiCache for Redis"],3,"ElastiCache for Redis offers data structures such as sorted sets, replication with automatic failover, and persistence options. Memcached is a simple, multithreaded key-value cache with no replication or persistence, so it cannot meet those needs.","Domain 3: ElastiCache for Redis vs Memcached"],
    ["x41",0,3,"Users update their profiles and often read them again straight away. The cache in front of the database must not return a stale profile after an update. Which caching strategy fits best?",["Write-through, updating the cache every time the database is written","Lazy loading with a long TTL on every cached profile","Lazy loading alone, filling the cache only on a cache miss","Caching all profiles at startup and never expiring them"],0,"Write-through updates the cache as part of each write, so a read right after an update gets the new value. Lazy loading only fills the cache on a miss, so an existing cached entry can stay stale until it expires.","Domain 3: Lazy loading vs write-through"],
    ["x42",0,3,"A CloudFront distribution has a low cache hit ratio. The investigation shows that all query strings, cookies and headers are included in the cache key, though the origin only varies content on one query string. What should the architect do?",["Enable Origin Shield in the Region nearest to the origin","Use a cache policy whose cache key includes only that one query string","Lower the default TTL so that objects refresh more often","Put AWS Global Accelerator in front of the distribution"],1,"Including unneeded values in the cache key creates many separate cache entries for the same content; a cache policy with only the query string that matters lets requests share entries. Origin Shield adds a central caching layer that reduces origin load but does not fix the fragmented cache key.","Domain 3: CloudFront cache keys and TTLs"],
    ["x43",0,3,"A social networking app must answer queries such as 'which friends of my friends like this band' with low latency across billions of relationships. Which database fits best?",["Amazon DocumentDB","Amazon Redshift","Amazon Neptune","Amazon DynamoDB"],2,"Neptune is a managed graph database built for traversing highly connected data such as social relationships. DynamoDB can store the items, but multi-hop relationship queries would need many lookups and complex application code.","Domain 3: Choosing a database (Neptune)"],
    ["x44",0,3,"On-premises applications write files over NFS, and the company wants those files stored as objects in Amazon S3 while recently used data stays in a local cache for low-latency access. What should the architect deploy?",["AWS Storage Gateway Tape Gateway","AWS DataSync with a scheduled task","AWS Storage Gateway Volume Gateway","Amazon S3 File Gateway"],3,"S3 File Gateway presents NFS or SMB shares on premises, stores files as S3 objects and keeps a local cache of recent data. Volume Gateway exposes iSCSI block volumes, not file shares whose files appear as S3 objects.","Domain 3: Storage Gateway"],
    ["x45",0,3,"A DynamoDB table uses the current date as its partition key. Every write for the day lands on the same key and requests are throttled, even though the table's total capacity is far from used. What is the best fix?",["Spread writes by adding a calculated or random suffix to the partition key","Switch the table from provisioned capacity to on-demand capacity","Raise the provisioned write capacity of the table","Add DynamoDB Accelerator (DAX) in front of the table"],0,"Throttling while total capacity is unused points to a hot partition; a higher-cardinality key, such as the date plus a suffix, spreads writes across partitions. On-demand mode removes table-level capacity planning, but each partition still has throughput limits, so a single hot key keeps throttling.","Domain 3: DynamoDB partition key design"],
    ["x46",0,3,"An existing DynamoDB table uses customerId as its partition key. The team now needs to query orders efficiently by productId, a different attribute. What should the architect add?",["A local secondary index with productId as its sort key","A global secondary index keyed on productId","A Scan operation with a filter expression on productId","DynamoDB Accelerator (DAX) to cache the results"],1,"A global secondary index can use a different partition key and can be added to an existing table. A local secondary index must keep the table's partition key and can only be created when the table is created, so it cannot serve this need.","Domain 3: DynamoDB secondary indexes"],
    ["x47",0,3,"A company runs Apache Kafka on premises with many producer and consumer applications written against Kafka client libraries. It wants a managed service on AWS with as few code changes as possible. What should the architect use?",["Amazon Kinesis Data Streams","Amazon Data Firehose","Amazon MSK","Amazon SQS FIFO queues"],2,"Amazon MSK runs Apache Kafka as a managed service, so existing Kafka clients connect with little or no change. Kinesis Data Streams is a similar streaming service but uses its own API, so the applications would have to be rewritten.","Domain 3: Kinesis Data Streams vs Firehose vs MSK"],
    ["x48",0,3,"A data lake in S3 is queried with Athena and Redshift Spectrum. The company needs central, fine-grained permissions, such as hiding salary columns from some analysts, managed in one place. What should the architect use?",["S3 bucket policies with a separate prefix for each column","Amazon Macie sensitive data discovery jobs","AWS Glue crawlers that skip the sensitive columns","AWS Lake Formation column-level permissions"],3,"Lake Formation provides central table, column and row-level permissions that integrated services such as Athena and Redshift Spectrum enforce. Bucket policies work on objects and prefixes, so they cannot hide individual columns inside a file.","Domain 3: Analytics services (Lake Formation)"],
    ["x49",0,4,"A fault-tolerant batch workload runs on Spot Instances in an Auto Scaling group. The company wants to lower the chance of interruptions while still getting low prices. How should the group be configured?",["Use many instance types across several AZs with the price-capacity-optimized allocation strategy","Use a single instance type in one AZ with the lowest-price allocation strategy","Use the lowest-price strategy and set the Spot maximum price to the On-Demand price","Use one instance type and request replacements manually after each interruption"],0,"Diversifying across instance types and AZs with price-capacity-optimized picks the Spot pools with the most spare capacity at a low price, which reduces interruptions. The lowest-price strategy focuses on the cheapest pools, which are often the ones most likely to be interrupted.","Domain 4: Spot allocation strategies"],
    ["x50",0,4,"A web tier needs a baseline of four instances that must always be available, and extra capacity during peaks that can tolerate interruption. What is the most cost-effective Auto Scaling group setup?",["Only Spot Instances, with capacity rebalancing turned on","A mixed instances policy with an On-Demand base of four and Spot above it","Only On-Demand Instances, with a scheduled scaling action","Dedicated Hosts sized for the peak load"],1,"A mixed instances policy lets the On-Demand base cover the must-stay baseline (which can be further discounted with Savings Plans) while Spot adds cheap peak capacity. An all-Spot group risks losing the baseline instances when capacity is reclaimed.","Domain 4: Mixed-instances Auto Scaling groups"],
    ["x51",0,4,"A company runs Linux-based Java microservices on x86 EC2 instances and wants better price performance with little development effort. What should the architect recommend first?",["Move the services to Dedicated Instances","Move to larger x86 instances and consolidate the services","Test and move to AWS Graviton-based instances","Move the services to EC2 Mac instances"],2,"Graviton instances are Arm-based and usually give better price performance; Java runs on Arm JVMs, so the move mostly needs testing and new builds of native dependencies. Dedicated Instances add cost for hardware isolation and do nothing for price performance.","Domain 4: Right-sizing and Graviton"],
    ["x52",0,4,"A company will run m6i instances in us-east-1 steadily for three years and has no plans to change instance family or Region. It wants the largest discount while still being able to change instance size and operating system. Which option fits?",["A Compute Savings Plan with a three-year term","Spot Instances covered by a capacity reservation","Dedicated Hosts with a three-year reservation","An EC2 Instance Savings Plan for the m6i family in us-east-1"],3,"EC2 Instance Savings Plans commit to one instance family in one Region in exchange for a deeper discount than Compute Savings Plans, and still allow size and OS changes within that family. A Compute Savings Plan is more flexible, but that flexibility, which this company does not need, comes with a smaller discount.","Domain 4: EC2 purchase options and Savings Plans"],
    ["x53",0,4,"A research institute shares a large public data set in S3 with other organizations and wants the people who download it to pay for the requests and data transfer. What should the architect enable?",["Requester Pays on the bucket","CloudFront signed URLs for each downloader","Presigned URLs that expire after one day","S3 Transfer Acceleration on the bucket"],0,"With Requester Pays, the requester's AWS account is charged for requests and data downloaded, while the bucket owner still pays for storage. Signed or presigned URLs control who can download, but the bucket owner still pays for the transfer.","Domain 4: S3 Requester Pays"],
    ["x54",0,4,"A company with many accounts wants organization-wide dashboards of S3 storage and activity, including recommendations such as finding buckets with incomplete multipart uploads. What should the architect use?",["S3 Inventory reports on each bucket","Amazon S3 Storage Lens","AWS Cost Explorer grouped by service","CloudTrail data events for S3"],1,"S3 Storage Lens gives organization-wide usage and activity metrics with recommendations to cut costs and apply best practices. S3 Inventory lists objects in a single bucket but does not provide cross-account dashboards or recommendations.","Domain 4: S3 Storage Lens"],
    ["x55",0,4,"A team takes EBS snapshots by hand. It wants daily snapshots kept for seven days, plus monthly snapshots kept for years that are rarely restored, at the lowest cost and with the least effort. What should the architect use?",["Keep every snapshot in the standard tier and delete old ones by script","Copy each snapshot into S3 Glacier Deep Archive with a lifecycle rule","Amazon Data Lifecycle Manager policies, moving monthly snapshots to the archive tier","Detach volumes each month and keep them as unattached volumes"],2,"Data Lifecycle Manager automates creation and retention of snapshots, and EBS Snapshots Archive offers much lower storage cost for rarely restored long-term snapshots. Snapshots cannot be copied into a Glacier storage class by an S3 lifecycle rule, because they are not objects in a bucket you own.","Domain 4: EBS snapshot costs and Data Lifecycle Manager"],
    ["x56",0,4,"Cost Explorer shows EBS spending rising even though many EC2 instances were terminated months ago. What is the most likely cause, and the fix?",["Terminated instances still bill for compute; buy Reserved Instances for them","The gp2 volumes are billed at a higher rate; convert them all to gp3","Stopped instances bill for EBS; terminate every stopped instance now","Volumes left in the available state; snapshot if needed, then delete them"],3,"Volumes whose DeleteOnTermination flag was off stay behind in the available state and keep billing for storage; they should be backed up if required and deleted. Converting to gp3 lowers the rate but still pays for volumes nobody uses.","Domain 4: Unattached EBS volumes"],
    ["x57",0,4,"To save money during a month-long project pause, a team stops an Amazon RDS instance. A week later, they find it running and billing again. What explains this, and what is the better approach?",["RDS restarts a stopped instance after seven days; snapshot and delete it, then restore later","Stopped RDS instances bill fully for compute; switch the instance to Multi-AZ","Someone else must have started it; add an SCP that denies rds:StartDBInstance","RDS instances cannot be stopped at all; move the database to DynamoDB"],0,"RDS automatically starts a stopped DB instance after seven days, so for a long pause it is cheaper to take a final snapshot, delete the instance and restore when needed. Blaming another user and blocking StartDBInstance does not stop the automatic restart, which is AWS's own behavior.","Domain 4: Stopping idle databases"],
    ["x58",0,4,"Large software downloads are served straight from EC2 instances to users around the world, and internet data transfer out is the biggest line on the bill. What change most reduces cost and load on the instances?",["Put AWS Global Accelerator in front of the instances","Serve the downloads through Amazon CloudFront","Enable S3 Transfer Acceleration on the download bucket","Move the downloads to larger, network-optimized instances"],1,"CloudFront caches downloads at edge locations, data transfer from AWS origins to CloudFront is not charged, and CloudFront delivery is typically cheaper than direct internet egress. Global Accelerator improves network paths but does not cache anything and adds its own charges.","Domain 4: Data transfer costs and CloudFront"],
    ["x59",0,4,"Finance needs hourly, line-item detail of every AWS charge, including resource IDs and tags, loaded into Athena for its own reports. What should the architect set up?",["Saved reports in AWS Cost Explorer","AWS Budgets with daily email alerts","The Cost and Usage Report delivered to S3","Cost optimization checks in AWS Trusted Advisor"],2,"The Cost and Usage Report delivers the most detailed billing data, down to hourly line items with resource IDs and tags, to S3, where Athena can query it. Cost Explorer shows summarized views and charts but is not a line-item data feed.","Domain 4: Cost visibility tools"],
    ["x60",0,4,"Account A in an AWS Organization bought Reserved Instances that it now uses only partly. Account B in the same organization runs matching instances in the same Region at On-Demand rates. What happens to billing?",["Nothing, because Reserved Instances apply only in the account that bought them","Account A must formally transfer the Reserved Instances to account B","The unused Reserved Instances must be sold on the Reserved Instance Marketplace","Account B's usage gets the unused RI discount by default"],3,"With consolidated billing, Reserved Instance and Savings Plans discounts are shared across the organization's accounts by default, so unused RI hours apply to matching usage in account B. Discount sharing can be disabled, but no transfer between accounts is needed.","Domain 4: Consolidated billing and RI sharing"],
  ],
  whys: {
    "x1": ["Needing an allow on both sides applies to cross-account requests; within one account the bucket policy alone can grant access.","Group membership is just another identity-based source; it is not required when the bucket policy already allows the user.",null,"Access points are optional; a plain bucket request is evaluated against the bucket policy in the normal way."],
    "x2": [null,"SCPs never grant permissions, and the default FullAWSAccess SCP already permits S3, so an SCP is not what is missing.","SSE-S3 keys are managed entirely by S3, so no KMS key permission or grant is involved in reading these objects.","Endpoint policies only matter for traffic through that endpoint in the caller's VPC; they cannot replace the bucket owner's resource policy."],
    "x3": ["An identity pool hands out AWS credentials but has no user directory, sign-up pages or MFA of its own.",null,"Creating IAM users and long-term access keys for customers breaks least privilege and does not scale to public users.","GetFederationToken needs long-term IAM credentials to call, so browsers cannot safely use it, and it offers no sign-in."],
    "x4": ["Gateway endpoints exist only for S3 and DynamoDB; Secrets Manager is reached through an interface endpoint.","A NAT instance sends the traffic out to the public service endpoint, which is what the design is trying to avoid.","AWS services are not reached through peering into a service-owned VPC; PrivateLink interface endpoints are the mechanism.",null],
    "x5": [null,"aws:SourceVpce only restricts access to the company's own buckets; it cannot stop uploads to a bucket owned by someone else.","Gateway endpoints are route table targets and do not support security groups, and security groups cannot filter bucket names.","Network ACLs filter by IP address, port and protocol only; they have no way to match S3 bucket names."],
    "x6": ["Basic DDoS protection does not wait for Shield Advanced; Shield Standard is always on for every customer.",null,"AWS WAF is a separate, paid service that you must configure with a web ACL; it is not turned on by default.","GuardDuty detects threats by analyzing logs; it does not sit in the traffic path or block DDoS traffic."],
    "x7": ["CloudFront works with public ACM certificates; there is no rule that limits it to Private CA certificates.","The validation method has nothing to do with which Region's certificates CloudFront can see.",null,"CloudFront can use certificates in the IAM certificate store, but ACM certificates in us-east-1 are also supported and preferred."],
    "x8": ["KMS key material cannot be exported, and a different key in account B could not decrypt data encrypted under account A's key.","AWS managed keys have key policies you cannot edit, so they cannot be opened to another account.","SCPs never grant permissions, and the key policy in account A would still not trust account B.",null],
    "x9": [null,"Imported key material still lives inside AWS KMS and is used there, so AWS would be storing the key.","SSE-S3 keys are created and stored by S3, which breaks the rule that the company holds its own keys.","The AWS managed key is created and fully controlled by AWS, the opposite of what the company requires."],
    "x10": ["A Config rule detects a public bucket after the change is made; it does not prevent the policy from taking effect.",null,"Macie finds sensitive data and reports on bucket exposure, but it does not block public access settings.","Encryption at rest does not stop public reads, because S3 decrypts objects for any authorized request, including anonymous ones."],
    "x11": ["Governance-mode Object Lock protects retained versions but does not require an MFA code for deletes or versioning changes.","Versioning alone keeps old versions but lets authorized users delete them without any MFA code.",null,"MFA Delete cannot be enabled in the console, and only the root user of the bucket owner can turn it on."],
    "x12": ["GuardDuty detects threats from logs and events; it does not scan software packages for known vulnerabilities.","Macie discovers sensitive data in S3; it does not look at instances or container images at all.","Trusted Advisor checks account configuration against best practices but does not scan workloads for CVEs.",null],
    "x13": [null,"A Config aggregator collects configuration and compliance data only, not GuardDuty, Inspector or Macie findings.","Detective helps investigate the root cause of findings; it is not the central findings and standards-checking service.","CloudTrail Lake stores and queries API activity, not security findings or best-practice checks."],
    "x14": ["Per-account trails can be stopped by each account's administrators and are not created automatically for new accounts.",null,"AWS Config records resource configuration changes, not a full log of API activity across accounts.","VPC Flow Logs capture network traffic metadata, not the API calls that CloudTrail records."],
    "x15": ["Boundaries do not add permissions; the result is the overlap of the two policies, not their union.","The boundary caps what the identity policy can grant, so EC2 actions outside the boundary are not allowed.",null,"A boundary is a limit, not a deny; actions allowed by both policies, here S3, are still permitted."],
    "x16": ["Subnet CIDR rules in a NACL also admit any other resource in those subnets and leave the security group open.","ALB node IP addresses change as the load balancer scales or is replaced, so fixed IP rules break over time.","Private subnets block direct internet traffic, but 0.0.0.0/0 still admits every resource in the VPC and any peered network.",null],
    "x17": [null,"Secrets Manager charges per secret and adds rotation the application does not need, so it costs more for plain settings.","A self-managed S3 file needs custom loading code and gives no per-value access control or change history.","Plain-text environment variables leave sensitive values unencrypted in the function configuration."],
    "x18": ["Only opt-in Regions can be disabled; default Regions stay enabled, so this setting cannot block them.",null,"A Config rule only reports resources after they are created; it does not prevent the API calls.","Per-account IAM policies can be edited by account administrators, and ec2:* alone leaves every other service usable."],
    "x19": ["An ALB handles HTTP and HTTPS at layer 7 and has no static IP addresses, so it cannot serve a custom TCP protocol this way.","The Classic Load Balancer is a previous-generation service without static IPs per AZ or the NLB's performance.",null,"A Gateway Load Balancer inserts third-party virtual appliances such as firewalls into the traffic path; it does not front applications."],
    "x20": ["Long polling reduces empty receives but does not stop messages becoming visible again while still being processed.","Moving to FIFO does not fix a timeout shorter than processing time, and it changes throughput and ordering behavior.","A shorter retention period makes messages expire sooner; it does nothing about duplicate delivery during processing.",null],
    "x21": [null,"EC2 does not publish state changes straight to SNS; routing those events is EventBridge's job.","CloudTrail Insights spots unusual API call volumes; it is not an event bus for state changes or SaaS events.","EC2 does not write state changes to SQS on its own, and polling adds delay and code without routing rules."],
    "x22": ["Reserved concurrency caps the function as a whole, so one partner's burst can still use up the shared limit.",null,"Caching helps only repeated GET responses and does not limit how many requests a single partner can send.","Geographic rules block or allow countries; they do not apply fair per-client request limits."],
    "x23": ["ECS uses its own task definitions and API, so the Helm charts and kubectl tooling would have to be replaced.","Elastic Beanstalk manages application environments, not a Kubernetes API that Helm and kubectl can target.",null,"App Runner runs web services from code or images without exposing Kubernetes, so the existing tooling would not carry over."],
    "x24": ["A cross-Region replica needs manual promotion and is aimed at Regional disaster recovery, not fast in-Region failover.","RDS Proxy pools connections and speeds failover handling, but it does not create a replica to fail over to.","Aurora does not use the RDS Multi-AZ standby model; its Aurora Replicas are the failover targets.",null],
    "x25": [null,"Geolocation routes by the user's location, which does not always match the Region with the lowest latency.","Multivalue answer returns several healthy records at random; it does not consider latency.","Weighted routing splits traffic by fixed proportions, not by which Region is fastest for each user."],
    "x26": ["Latency-based routing can send German users to us-east-1 whenever it measures faster, breaking the legal rule.",null,"Weighted routing splits traffic by percentage and ignores where the user is located.","Failover routing sends everyone to the primary while it is healthy, so all users would reach eu-central-1."],
    "x27": ["Simple routing can return several values, but it cannot attach health checks, so failed servers keep being returned.","Failover routing supports one primary and one secondary record; it is not built to share load across six servers.",null,"Geoproximity routes by distance between users and resources with a bias, not to spread load across healthy servers."],
    "x28": ["AWS Backup takes periodic backups, so recovery point objectives are hours rather than seconds, with slow server rebuilds.","DataSync copies files on a schedule; it does not replicate block devices or launch recovery servers.","Exported disk images copied between buckets give neither continuous replication nor quick server launches.",null],
    "x29": [null,"Sharing an AMI grants other accounts access in the same Region; it does not make the AMI exist in another Region.","Making an AMI public exposes it to everyone but still leaves it in its original Region only.","EBS snapshots are not replicated to other Regions automatically; AMIs and snapshots must be copied explicitly."],
    "x30": ["Nested stacks reuse templates inside one stack in one account and Region; they do not deploy across accounts.",null,"Change sets preview updates to a single stack; they do not spread stacks to many accounts or future ones.","Manual stacks in each account take ongoing effort and miss accounts that join the OU later."],
    "x31": ["EC2 vCPU quotas are set per Region, so raising the primary Region's quota does not help the recovery Region.","Spot Instances have their own vCPU quotas and can also be unavailable during a large Regional event.",null,"Trusted Advisor can show quota usage but does not raise quotas; increases must be requested."],
    "x32": ["More memory speeds up each invocation but does not reserve concurrency, so the function can still be throttled.","Provisioned concurrency pre-warms environments on the busy function but does not stop it using the rest of the pool.","Moving the busy function to another account adds operational work when reserved concurrency solves it directly.",null],
    "x33": [null,"Losing one of two AZs leaves only three instances, half the capacity the application needs.","Losing one AZ leaves only four instances, below the minimum of six.","Twelve across two AZs survives an AZ loss but uses three more instances than the three-AZ layout."],
    "x34": ["Point-in-time recovery covers at most the last 35 days, so it cannot keep seven years of monthly restore points.",null,"Global tables replicate current data between Regions, so an accidental change or deletion is replicated too; they are not backups.","Streams capture item changes for 24 hours; deleting old items is the opposite of keeping long-term backups."],
    "x35": ["A cluster group packs instances close together, often on shared hardware, so one rack failure can take out many nodes.","Spread groups allow only seven running instances per AZ, too few for a large cluster of dozens of nodes.",null,"Dedicated Hosts give per-host isolation for licensing but do not expose rack placement to the database."],
    "x36": ["More ENIs add network paths but still use the kernel network stack, so they do not give OS bypass.","Elastic IP addresses give public, static addresses; they do nothing for latency between cluster nodes.","ENA gives high bandwidth but traffic still goes through the kernel, so it cannot match EFA's OS-bypass latency.",null],
    "x37": [null,"io2 is durable and fast, but it is billed separately for storage and IOPS, which the workload does not need.","EFS is a network file system with higher latency than local disks and adds storage charges.","gp3 is network-attached and billed separately, and even with higher IOPS it cannot match local instance store I/O."],
    "x38": ["EFS serves NFS only and has no SMB access or ONTAP features such as SnapMirror.",null,"FSx for Windows File Server serves SMB and has no ONTAP SnapMirror or multiprotocol NFS use of the same volumes.","FSx for Lustre is a high-performance file system for compute clusters, not a multiprotocol NAS replacement."],
    "x39": ["Transfer Acceleration helps long-distance transfers through edge locations; the client is already in the same Region.","Versioning keeps previous object versions and has no effect on download speed.",null,"Multipart upload speeds uploads; downloading each object with one request stays as slow as before."],
    "x40": ["Memcached has no replication, persistence or sorted set data type, so it cannot meet any of the three needs.","DAX caches DynamoDB reads only; it is not a general in-memory store with sorted sets.","CloudFront caches HTTP content at the edge; it cannot hold and update leaderboard data structures.",null],
    "x41": [null,"A long TTL keeps outdated entries in the cache even longer after an update, making stale reads more likely.","Lazy loading only writes to the cache on a miss, so an old cached profile stays until it expires.","Never expiring a startup snapshot guarantees stale data as soon as any profile changes."],
    "x42": ["Origin Shield reduces load on the origin but does not fix a fragmented cache key at the edge.",null,"A lower TTL makes objects expire sooner, which lowers the cache hit ratio further.","Global Accelerator improves network paths but does not cache, so it cannot raise a CloudFront hit ratio."],
    "x43": ["DocumentDB stores JSON documents; multi-hop relationship queries are not what it is designed for.","Redshift is a data warehouse for analytics, not a low-latency store for relationship traversal.",null,"DynamoDB would need many chained lookups and application code to walk friend-of-friend relationships."],
    "x44": ["Tape Gateway presents a virtual tape library for backup software, not NFS file shares.","DataSync copies data in scheduled transfers and gives applications no live NFS mount with a local cache.","Volume Gateway exposes iSCSI block volumes, so files are not stored as individual S3 objects.",null],
    "x45": [null,"On-demand mode removes table-level capacity planning, but per-partition limits still throttle a single hot key.","More table capacity does not help when every write targets one partition key.","DAX caches reads; it does not spread writes, which is where the throttling happens."],
    "x46": ["An LSI must keep the table's partition key and can only be created with the table, so it cannot be added now.",null,"A Scan reads the whole table and filters afterwards, which is slow and uses a lot of capacity.","DAX caches reads of existing access patterns; it does not add a new way to query by productId."],
    "x47": ["Kinesis Data Streams uses its own API, so every Kafka producer and consumer would need rewriting.","Firehose delivers streams to destinations; it does not speak the Kafka protocol for existing clients.",null,"SQS is a message queue with a different API and model, so the Kafka applications would need rewriting."],
    "x48": ["Bucket policies control objects and prefixes; they cannot hide individual columns inside a data file.","Macie discovers sensitive data but does not enforce column permissions for Athena or Redshift Spectrum.","Crawlers build catalog tables; leaving columns out of the catalog is not access control and hides them from everyone.",null],
    "x49": [null,"One instance type in one AZ gives a single capacity pool, raising the chance of interruptions.","A maximum price does not reduce interruptions caused by capacity being reclaimed, and lowest-price favors crowded pools.","Manual replacement adds delay and work, and a single instance type keeps interruption risk high."],
    "x50": ["An all-Spot group can lose its baseline instances when capacity is reclaimed, breaking the always-available need.",null,"All On-Demand pays full price for peak capacity that could run on cheaper Spot Instances.","Dedicated Hosts sized for peak are the most expensive choice and idle most of the time."],
    "x51": ["Dedicated Instances add cost for hardware isolation and do nothing for price performance.","Bigger x86 instances raise cost and do not improve the price-performance ratio.",null,"Mac instances are for building Apple software and are an expensive, poor fit for Linux services."],
    "x52": ["A Compute Savings Plan is more flexible but gives a smaller discount, and the company needs none of that flexibility.","Spot Instances can be interrupted and are not a commitment discount for steady three-year use.","Dedicated Hosts are for licensing or compliance needs and cost more than shared-tenancy instances with a plan.",null],
    "x53": [null,"CloudFront signed URLs control who can download, but the owner still pays for the delivery.","Presigned URLs grant temporary access but leave the bucket owner paying for requests and transfer.","Transfer Acceleration adds charges for faster transfers; it does not shift costs to downloaders."],
    "x54": ["S3 Inventory lists objects in one bucket; it has no organization-wide dashboards or recommendations.",null,"Cost Explorer shows spend by service but not storage metrics such as incomplete multipart uploads.","CloudTrail data events log individual requests and add cost; they are not a storage analytics dashboard."],
    "x55": ["Keeping long-term snapshots in the standard tier costs more, and custom scripts add ongoing work.","EBS snapshots are not objects in your bucket, so S3 lifecycle rules cannot move them to Deep Archive.",null,"Unattached volumes cost far more than snapshots and give no automated retention."],
    "x56": ["Terminated instances stop billing for compute; buying Reserved Instances for them makes no sense.","gp3 is cheaper, but converting still pays for volumes nothing uses; the orphaned volumes are the real waste.","Stopped instances do bill for their EBS volumes, but the scenario is about volumes left behind by terminated instances.",null],
    "x57": [null,"A stopped instance does not bill for instance hours, and Multi-AZ would double the cost.","The restart is AWS's own automatic behavior after seven days, so blocking StartDBInstance does not stop it.","RDS instances can be stopped temporarily; moving to DynamoDB is a redesign, not a fix."],
    "x58": ["Global Accelerator improves routing but does not cache content, and it adds its own fees.",null,"Transfer Acceleration speeds transfers to and from S3 at extra cost; it does not reduce egress from EC2.","Bigger instances raise compute cost and leave every byte still going out over the internet."],
    "x59": ["Cost Explorer shows summarized charts and views; it is not an hourly line-item data feed for Athena.","Budgets send alerts on thresholds; they do not export detailed billing records.",null,"Trusted Advisor gives optimization recommendations, not detailed billing line items."],
    "x60": ["Under consolidated billing, RI discounts are shared across accounts by default, not locked to the buyer.","No transfer is needed; the discount applies automatically to matching usage in other accounts.","Selling is unnecessary when matching usage elsewhere in the organization can use the discount.",null]
  },
  levels: {
    "saa1": 2, "saa2": 2, "saa3": 3, "saa4": 2, "saa5": 3, "saa6": 1, "saa7": 2, "saa8": 2, "saa9": 3, "saa10": 2,
    "saa11": 1, "saa12": 2, "saa13": 3, "saa14": 1, "saa15": 3, "saa16": 1, "saa17": 2, "saa18": 2, "saa19": 1, "saa20": 2,
    "saa21": 1, "saa22": 1, "saa23": 1, "saa24": 2, "saa25": 2, "saa26": 3, "saa27": 1, "saa28": 2, "saa29": 3, "saa30": 2,
    "saa31": 3, "saa32": 2, "saa33": 3, "saa34": 2, "saa35": 2, "saa36": 2, "saa37": 1, "saa38": 3, "saa39": 2, "saa40": 2,
    "saa41": 2, "saa42": 1, "saa43": 2, "saa44": 3, "saa45": 3, "saa46": 2, "saa47": 1, "saa48": 2, "saa49": 3, "saa50": 3,
    "saa51": 2, "saa52": 2, "saa53": 2, "saa54": 2, "saa55": 1, "saa56": 1, "saa57": 3, "saa58": 2, "saa59": 1, "saa60": 3,
    "saa61": 1, "saa62": 2, "saa63": 3, "saa64": 3, "saa65": 1, "saa66": 2, "saa67": 2, "saa68": 1, "saa69": 2, "saa70": 1,
    "saa71": 2, "saa72": 2, "saa73": 3, "saa74": 2, "saa75": 1, "saa76": 2, "saa77": 2, "saa78": 2, "saa79": 2, "saa80": 3,
    "saa81": 2, "saa82": 1, "saa83": 2, "saa84": 3, "saa85": 2, "saa86": 2, "saa87": 1, "saa88": 3, "saa89": 1, "saa90": 1,
    "x1": 3, "x2": 3, "x3": 2, "x4": 2, "x5": 3, "x6": 1, "x7": 1, "x8": 3, "x9": 1, "x10": 2,
    "x11": 3, "x12": 1, "x13": 1, "x14": 2, "x15": 3, "x16": 2, "x17": 2, "x18": 3, "x19": 1, "x20": 3,
    "x21": 1, "x22": 2, "x23": 1, "x24": 2, "x25": 1, "x26": 2, "x27": 2, "x28": 3, "x29": 2, "x30": 2,
    "x31": 3, "x32": 3, "x33": 3, "x34": 2, "x35": 3, "x36": 2, "x37": 2, "x38": 1, "x39": 2, "x40": 2,
    "x41": 2, "x42": 3, "x43": 1, "x44": 1, "x45": 3, "x46": 2, "x47": 1, "x48": 2, "x49": 2, "x50": 2,
    "x51": 2, "x52": 3, "x53": 1, "x54": 1, "x55": 2, "x56": 2, "x57": 3, "x58": 2, "x59": 1, "x60": 3
  }
});
