/* More terraform practice questions with wrong-answer notes, and a difficulty level for every question. Merged into the free bank by tools/build.js. */
CertHub.addExtra("terraform", {
  questions: [
    ["x1",0,1,"Long-lived web servers are patched by hand and slowly drift apart until no two are alike. Which approach, commonly paired with Terraform, avoids this?",["Build a new image and replace the servers","Log in and patch each server on a schedule","Take a snapshot of one server and restore it on failure","Store the patch list in the Terraform state"],0,"Immutable infrastructure replaces servers with new ones built from a known image instead of changing them in place, so every instance matches. Scheduled patching is tempting because it is routine, but manual changes are what cause the drift.","Domain 1: Immutable infrastructure"],
    ["x2",0,1,"A team needs to create VMs and networks, then install and configure packages inside those VMs. How are Terraform and a configuration management tool usually divided?",["Terraform installs the packages inside the VMs; the other tool creates the VMs and networks","Terraform provisions the infrastructure; the other tool configures software inside it","Terraform replaces configuration management tools entirely","Both tools must manage the same VMs' lifecycle"],1,"Terraform's strength is provisioning and managing the lifecycle of infrastructure, while tools such as Ansible configure what runs inside servers. Saying Terraform replaces them is tempting, but it is not designed for in-guest configuration.","Domain 1: Terraform vs configuration management"],
    ["x3",0,1,"An engineer changes a firewall rule directly in the cloud console during an incident. On the next terraform plan, Terraform proposes to undo the change. What is the best follow-up?",["Stop running plans for that resource","Keep making the change in the console after each apply","Put the change into the Terraform code and apply it","Delete the state file so the change is kept"],2,"With IaC the code is the source of truth, so an emergency change must be written back into the configuration and reviewed. Ignoring the resource is tempting as a quick fix, but it lets code and infrastructure silently diverge.","Domain 1: IaC as source of truth"],
    ["x4",0,1,"An auditor asks who changed a load balancer's settings last month and why. Which IaC practice answers this most easily?",["Taking weekly screenshots of the cloud console","Giving every engineer the same admin account","Running terraform fmt before each commit","Version-controlled configuration with commit history"],3,"Keeping infrastructure code in version control gives an audit trail of each change, its author and the review discussion. terraform fmt is tempting because it is a good habit, but it only formats files.","Domain 1: Advantages of IaC"],
    ["x5",0,1,"A configuration uses count = 5 for a group of identical servers. You change it to count = 3 and run apply. What does Terraform do?",["Creates three new servers alongside the five","Fails because the count cannot shrink","Destroys two servers so that three remain","Leaves all five until they are deleted by hand"],2,"Terraform is declarative: you state the desired end result and it works out the steps, here removing the two highest-indexed instances. Creating three more is tempting if you think of it as a command to run, but Terraform compares against what exists.","Domain 1: Declarative configuration"],
    ["x6",0,2,"A required_providers entry uses version = \"~> 1.2.0\". Which provider version may terraform init select?",["1.2.9","1.3.0","1.1.9","2.0.0"],0,"The pessimistic operator ~> 1.2.0 means >= 1.2.0 and < 1.3.0, so only patch releases are allowed. 1.3.0 is tempting because ~> 1.2 (two parts) would allow it, but three parts pin the minor version.","Domain 2: Version constraints"],
    ["x7",0,2,"A provider constraint is version = \">= 2.0.0, != 2.3.1, < 3.0.0\". Which version satisfies it?",["2.3.1","3.0.0","1.9.9","2.4.0"],3,"Comma-separated constraints must all be true; 2.4.0 is at least 2.0.0, is not 2.3.1 and is below 3.0.0. 2.3.1 is tempting because it sits within the range, but != removes it.","Domain 2: Version constraint operators"],
    ["x8",0,2,"What does the .terraform.lock.hcl file record?",["The exact version of every module the configuration calls","The selected provider versions and their checksums","The current resource attributes","Which user holds the state lock"],1,"The dependency lock file stores each provider's chosen version, its constraints and checksums so every run uses the same builds. Module versions are tempting to include, but Terraform does not lock modules there.","Domain 2: Dependency lock file"],
    ["x9",0,2,"Developers run terraform init on macOS and commit the lock file. The Linux CI runner then fails because the lock file has no checksums for its platform. What is the best fix?",["Add .terraform.lock.hcl to .gitignore","Run terraform providers lock with -platform for each OS and commit it","Run terraform init -upgrade on every CI run","Commit the .terraform directory with the macOS plugins"],1,"terraform providers lock -platform=... adds checksums for every listed platform, so one committed lock file works on all of them. Ignoring the lock file is tempting because the error goes away, but consistent provider versions are lost.","Domain 2: Dependency lock file across platforms"],
    ["x10",0,2,"A configuration uses resources from a partner provider published under the namespace mongodb, but has no required_providers entry for it. terraform init fails to find the provider. Why?",["Partner providers must be downloaded by hand","Only official providers can be used without HCP Terraform","The provider block is missing an alias","Without a source, Terraform assumes the hashicorp namespace"],3,"If a provider is not declared in required_providers, Terraform treats its local name as hashicorp/<name>, so providers from other namespaces need an explicit source. The alias answer is tempting because it is also provider syntax, but it has nothing to do with installation.","Domain 2: required_providers source addresses"],
    ["x11",0,2,"In the Terraform Registry, a provider is written and maintained by a third-party technology company that works with HashiCorp. Which tier is it?",["Partner","Official","Community","Archived"],0,"Partner providers are maintained by third-party companies against their own APIs as part of HashiCorp's partner program. Official is tempting, but it is reserved for HashiCorp-maintained providers.","Domain 2: Provider tiers"],
    ["x12",0,2,"A configuration declares both the docker and random providers in required_providers. What does a single terraform init do?",["Downloads only the first provider listed","Fails because one configuration allows one provider","Downloads both provider plugins","Asks you to choose which provider to install"],2,"terraform init reads all provider requirements and installs every plugin the configuration needs. Believing a configuration is limited to one provider is the tempting misconception.","Domain 2: Using several providers"],
    ["x13",0,3,"CI runs `terraform plan -out=tfplan`, and a later step runs `terraform apply -var=\"env=prod\" tfplan`. The apply fails. Why?",["Saved plans expire after one hour","apply needs -auto-approve to use a plan file","The plan file must be named terraform.tfplan","A saved plan cannot take new variable values at apply"],3,"A saved plan already contains the variable values used when it was created, so Terraform rejects a -var value that was not part of that plan at apply time. The approval flag is tempting to blame, but applying a saved plan never asks for approval.","Domain 3: Saved plans"],
    ["x14",0,3,"Which terraform apply option skips the interactive approval prompt?",["-input=false","-refresh=false","-auto-approve","-lock=false"],2,"-auto-approve applies without asking for confirmation, which is common in automation. -input=false is tempting because it also disables prompts, but not the approval step.","Domain 3: terraform apply options"],
    ["x15",0,3,"A CI job must tell three results apart: plan succeeded with no changes, plan succeeded with changes, or plan errored. Which option makes terraform plan report this through its exit code?",["-json","-detailed-exitcode","-compact-warnings","-no-color"],1,"With -detailed-exitcode, plan exits 0 for no changes, 1 for an error and 2 for a successful plan with changes. -json is tempting for automation, but it does not change what the exit code means.","Domain 3: terraform plan in automation"],
    ["x16",0,3,"terraform validate passes, but terraform apply fails because the cloud rejects an image ID that does not exist. Why did validate not catch it?",["validate does not contact provider APIs","validate only checks the formatting style","validate only runs after a plan","validate skips resources that use variables"],0,"terraform validate checks syntax and internal consistency without calling remote services, so it cannot know whether an ID really exists. Confusing it with fmt is tempting, but formatting is a separate command.","Domain 3: terraform validate"],
    ["x17",0,3,"You want to see what formatting changes terraform fmt would make, as a diff, before it rewrites files. Which option shows the differences?",["-diff","-list=false","-write=true","-json"],0,"terraform fmt -diff prints the formatting differences (add -check to avoid writing). -list=false is tempting because it controls output, but it only hides file names.","Domain 3: terraform fmt"],
    ["x18",0,3,"A provider's API starts returning rate-limit errors when Terraform creates many resources at once. Which option reduces concurrent operations during apply?",["-target on each resource in turn","-parallelism with a lower value","-refresh=false","-lock-timeout with a higher value"],1,"-parallelism sets how many resource operations Terraform runs at the same time while walking the graph. -target is tempting because it reduces the work, but it is not meant for routine use.","Domain 3: Parallelism"],
    ["x19",0,3,"Resource B references an attribute of resource A. Resource C references nothing. In what order does Terraform create them on the first apply?",["A, then B, then C, in file order","All three at once, since none use depends_on","A and C can be created in parallel; B waits for A","C first, then A, then B, alphabetically"],2,"Terraform builds a dependency graph and runs independent nodes concurrently, so A and C start together while B waits for A. Assuming no dependency without depends_on is tempting, but references create implicit dependencies.","Domain 3: Dependency graph"],
    ["x20",0,3,"Which command is equivalent to terraform destroy?",["terraform plan -destroy","terraform state rm -all","terraform apply -replace","terraform apply -destroy"],3,"terraform destroy is a convenience alias for terraform apply -destroy, which plans and then removes all managed resources. plan -destroy is tempting, but it is only a preview.","Domain 3: terraform destroy"],
    ["x21",0,3,"A plan output shows `<= data.http.config` next to a data source. What does this mean?",["The data source will be destroyed","The data source will be replaced","The data source will be read during apply","The data source is being imported into state"],2,"The <= symbol marks a read action, usually because the data source depends on values not known until apply. Replacement is tempting because it is another special symbol, but that is -/+.","Domain 3: Plan symbols"],
    ["x22",0,3,"Engineer A saves a plan. Before it is applied, engineer B applies a different change to the same state. What happens when A applies the saved plan?",["Terraform refuses because the saved plan is stale","The plan applies and overwrites B's change silently","Terraform merges both changes automatically","Terraform waits for B's lock to be released and then applies"],0,"A saved plan is tied to the state it was made from; if the state changed, Terraform reports the plan as stale and a new plan is needed. The lock answer is tempting because locking also protects state, but it does not apply here.","Domain 3: Saved plans and state changes"],
    ["x23",0,3,"What is the difference between terraform plan -refresh-only and terraform plan -refresh=false?",["They are two spellings of the same option","-refresh-only changes infrastructure; -refresh=false does not","-refresh=false updates state from reality; -refresh-only skips checking it","-refresh-only only updates state; -refresh=false skips the refresh"],3,"-refresh-only proposes only state updates to match real infrastructure, while -refresh=false plans against the existing state without querying providers. Reading them as the same option is tempting because of the similar names.","Domain 3: Planning options"],
    ["x24",0,4,"terraform.tfvars sets instance_type = \"t3.small\", and prod.auto.tfvars sets instance_type = \"t3.large\". No -var or -var-file options are used. Which value applies?",["t3.small","t3.large","The variable's default","Neither; Terraform errors on conflicting files"],1,"Terraform loads terraform.tfvars first and then *.auto.tfvars files in lexical order, so prod.auto.tfvars wins. t3.small is tempting because terraform.tfvars sounds like the main file.","Domain 4: Variable value precedence"],
    ["x25",0,4,"A run uses `-var-file=common.tfvars -var-file=prod.tfvars`, and both files set replicas. Which value is used?",["The value from common.tfvars","The value from prod.tfvars","The sum of both values","Terraform prompts for a value"],1,"When the same variable is set more than once on the command line, the last one wins, here prod.tfvars. Choosing the first file is tempting if you assume the earliest setting takes priority.","Domain 4: Variable value precedence"],
    ["x26",0,4,"Variable size has default = 10 and nullable = false. A module call passes size = null. What value does the module see?",["null","0","An error is raised","10"],3,"When nullable is false and a default exists, passing null makes Terraform use the default. null is tempting because it is what was passed, but nullable = false forbids it.","Domain 4: Input variables (nullable)"],
    ["x27",0,4,"A security group needs one ingress block for each port in var.ports, and the list changes per environment. Which feature generates these nested blocks?",["A dynamic \"ingress\" block with for_each","count on the ingress block","A for expression in a locals block","A module per port"],0,"A dynamic block with for_each generates repeated nested blocks from a collection. count is tempting because it repeats resources, but it cannot be used on nested blocks.","Domain 4: dynamic blocks"],
    ["x28",0,4,"aws_instance.web uses for_each over a map. An output written as aws_instance.web[*].id fails. Which expression returns a list of all the IDs?",["aws_instance.web.*.id[0]","aws_instance.web[\"*\"].id","values(aws_instance.web)[*].id","aws_instance.web[count.index].id"],2,"Resources with for_each are maps of instances, and splat works on lists, so values() turns the map into a list first (a for expression also works). The index answer is tempting because it looks like splat, but maps cannot be indexed by position.","Domain 4: Splat expressions and for_each"],
    ["x29",0,4,"var.tags is { env = \"dev\", team = \"web\" }. Which expression builds a map with the same keys and upper-case values?",["[ for k, v in var.tags : upper(v) ]","{ for v in var.tags : v => upper(v) }","upper(var.tags)","{ for k, v in var.tags : k => upper(v) }"],3,"A for expression in braces with key => value produces a map, and two symbols bind each key and value. The square-bracket version is tempting but it returns a list.","Domain 4: for expressions"],
    ["x30",0,4,"A monitoring resource should exist only when var.enable_monitoring is true. Which argument is the common pattern?",["depends_on = [var.enable_monitoring]","lifecycle { ignore_changes = [var.enable_monitoring] }","count = var.enable_monitoring ? 1 : 0","enabled = var.enable_monitoring"],2,"A conditional expression setting count to 1 or 0 creates or skips the resource. depends_on is tempting because it references the variable, but it only controls ordering.","Domain 4: Conditional expressions and count"],
    ["x31",0,4,"A VM should be recreated whenever a separate terraform_data resource that holds a revision number changes, even though no VM argument references it. Which lifecycle argument does this?",["create_before_destroy","replace_triggered_by","ignore_changes","prevent_destroy"],1,"replace_triggered_by replaces the resource when any referenced resource or attribute changes. create_before_destroy is tempting because it concerns replacement, but it only controls ordering.","Domain 4: lifecycle replace_triggered_by"],
    ["x32",0,4,"A production database has lifecycle { prevent_destroy = true }. A change would require replacing it. What happens at plan time?",["Terraform returns an error and the plan fails","Terraform replaces it after asking twice","Terraform skips the database and applies the rest","Terraform updates it in place instead"],0,"prevent_destroy makes any plan that would destroy the resource fail with an error. Expecting Terraform to skip it quietly is tempting, but it stops the whole plan so someone must decide.","Domain 4: lifecycle prevent_destroy"],
    ["x33",0,4,"A root module output returns var.db_password, which is marked sensitive. terraform plan errors on the output. What is the fix?",["Add sensitive = true to the output","Wrap the value in nonsensitive() by default","Move the output into a locals block","Run plan with -compact-warnings"],0,"Terraform requires outputs that use sensitive values to be marked sensitive themselves, so the value is redacted in CLI output. nonsensitive() is tempting because it makes the error go away, but it exposes the secret.","Domain 4: Sensitive outputs"],
    ["x34",0,4,"What is the key difference between a local value and an input variable?",["Locals can be set with -var on the command line","Locals cannot be set from outside the module","Locals are stored encrypted in state","Locals must be literal strings"],1,"Local values are named expressions inside a module, while input variables are the module's parameters set by callers. The -var answer is tempting, but it applies only to variables.","Domain 4: Local values"],
    ["x35",0,4,"var.zones is declared with type = set(string). The expression var.zones[0] fails. Why?",["Sets are always empty until apply","Index 0 is invalid; indexes start at 1","Sets are unordered, so their elements cannot be indexed","Sets can only hold numbers"],2,"A set has no order and no duplicates, so it cannot be indexed; convert it with tolist() or use for_each. Blaming the index start is tempting, but lists start at 0 and sets have no index at all.","Domain 4: Complex types (set)"],
    ["x36",0,4,"A configuration reads a database password from HashiCorp Vault with a data source and passes it to a resource argument. What should the team know about the value?",["It is never stored because it came from Vault","It is encrypted in state with Vault's key","It is written to the configuration files","It is written to the state file, so state must be protected"],3,"Using Vault keeps secrets out of code, but data source results and resource arguments still end up in state unless ephemeral or write-only features are used. Assuming Vault keeps it out of state is the tempting mistake.","Domain 4: Secrets with Vault"],
    ["x37",0,5,"Given: module \"vm\" { source = \"./modules/vm\"  count = 2 }. How is the first instance addressed?",["module.vm[1]","module.vm.0","module.vm[0]","module.vm[\"0\"]"],2,"Modules using count become a list of instances addressed as module.NAME[index], starting at 0. The string key is tempting because it looks like for_each addressing.","Domain 5: count on modules"],
    ["x38",0,5,"A module lives in the modules/vpc folder of a Git repository and must be pinned to tag v2.0.0. Which source is correct?",["git::ssh://git@example.com/infra.git//modules/vpc?ref=v2.0.0","git::ssh://git@example.com/infra.git/modules/vpc?ref=v2.0.0","git::ssh://git@example.com/infra.git?ref=v2.0.0 with path = modules/vpc","git::ssh://git@example.com/infra.git//modules/vpc with version = 2.0.0"],0,"A double slash separates the repository from a subdirectory, and ?ref= selects the tag. The version argument is tempting, but it only works with registry sources.","Domain 5: Module sources (Git subdirectories)"],
    ["x39",0,5,"You want to download or update modules without initializing the backend or installing providers. Which command does only that?",["terraform init -backend=false -get-plugins=false","terraform providers mirror","terraform validate","terraform get"],3,"terraform get downloads the modules referenced in the configuration into .terraform/modules. The init variant is tempting because init also installs modules, but it does more than that.","Domain 5: Installing modules"],
    ["x40",0,5,"A module is published in the private registry of HCP Terraform organization acme as module vpc for provider aws. Which source address is correct?",["acme/vpc/aws","app.terraform.io/acme/vpc/aws","registry.terraform.io/acme/vpc","git::app.terraform.io/acme/vpc"],1,"Private registry sources use hostname/organization/name/provider, and HCP Terraform's host is app.terraform.io. The short form is tempting, but without a hostname Terraform uses the public registry.","Domain 5: Private registry modules"],
    ["x41",0,5,"In the standard module structure, where should a module's input variable declarations go?",["outputs.tf","variables.tf","terraform.tfvars","README.md"],1,"The standard layout uses main.tf for resources, variables.tf for inputs and outputs.tf for outputs, plus a README. terraform.tfvars is tempting because it involves variables, but it only sets values.","Domain 5: Standard module structure"],
    ["x42",0,5,"module \"app\" takes vpc_id = module.network.vpc_id. No depends_on is written. How does Terraform order the two modules?",["It creates them in the order they appear in the files","It creates them in parallel with no ordering","It fails until depends_on is added","It builds resources that feed vpc_id before those that use it"],3,"Referencing a module output creates an implicit dependency, so the resources behind that output are handled first. Thinking depends_on is required is tempting, but it is only needed when no reference exists.","Domain 5: Module outputs and dependencies"],
    ["x43",0,5,"A shared module will be called with for_each by several teams. Why should it declare required_providers but not contain its own provider configuration block?",["A module with provider blocks cannot use for_each, count or depends_on","Provider blocks inside modules are ignored","required_providers is only allowed in child modules","Provider blocks in modules make init download plugins twice"],0,"A module that defines its own provider configuration is not compatible with count, for_each and depends_on, so reusable modules should receive providers from the caller. Saying the blocks are ignored is tempting, but they are used and cause this restriction.","Domain 5: Providers in modules"],
    ["x44",0,6,"Several CI jobs share one remote state. When a job finds the state locked, it fails immediately instead of waiting. Which option makes it wait for the lock?",["-lock=false","-parallelism=1","-lock-timeout=5m","-refresh=false"],2,"-lock-timeout tells Terraform to retry acquiring the lock for the given duration before failing. -lock=false is tempting because the error goes away, but it removes the protection.","Domain 6: State locking"],
    ["x45",0,6,"With the local backend, what is the terraform.tfstate.backup file?",["A copy of the configuration files","A lock file that blocks other runs","An encrypted copy of the current state","The previous version of the state"],3,"The local backend keeps the state from before the last write as terraform.tfstate.backup, which helps recover from mistakes. An encrypted copy is tempting to assume, but it is plain JSON.","Domain 6: Local backend"],
    ["x46",0,6,"State for a production project is stored in an S3 bucket. Which measures protect it best?",["Make the bucket public so CI can reach it","Commit a copy of the state to Git as a backup","Encryption, versioning and tightly limited access","Mark all variables sensitive so state is encrypted"],2,"State can contain secrets, so remote state should be encrypted at rest, versioned for recovery and restricted to those who need it. Marking variables sensitive is tempting, but it does not encrypt state.","Domain 6: Protecting remote state"],
    ["x47",0,6,"A configuration should name a bucket app-dev in workspace dev and app-prod in workspace prod. Which expression does this?",["\"app-${var.workspace}\"","\"app-${terraform.workspace}\"","\"app-${workspace.name}\"","\"app-${path.workspace}\""],1,"terraform.workspace returns the name of the current CLI workspace. var.workspace is tempting, but it is not built in.","Domain 6: CLI workspaces"],
    ["x48",0,6,"A team wants dev and prod isolated with different credentials and access controls, and plans to use CLI workspaces in one backend. Why is this a weak fit?",["They share one backend and its credentials, so isolation is weak","Workspaces can hold only one resource each","Workspaces cannot use different variable values for each environment","Workspaces are removed by terraform init"],0,"CLI workspaces are several states in the same backend with the same credentials, so they are not suited to strong separation; separate configurations or backends are recommended. The variable answer is tempting, but workspaces can use different values.","Domain 6: CLI workspaces and isolation"],
    ["x49",0,6,"The network team's configuration exports vpc_id as a root output in its remote state. How can the app configuration read it without copying values by hand?",["A terraform_remote_state data source","A module block pointing at the network state file","terraform state pull in the app configuration","A moved block that links the two states"],0,"terraform_remote_state reads the root module outputs of another state (an HCP Terraform outputs data source also works there). A module block is tempting, but modules are about code, not state.","Domain 6: Reading other state"],
    ["x50",0,6,"A storage bucket managed by Terraform is deleted manually in the console. What does the next terraform plan show?",["An error that state is corrupt","A plan to create the bucket again","No changes, because state still lists the bucket","A plan to remove the bucket's block from the code"],1,"During refresh Terraform sees the object is gone and, since the configuration still declares it, plans to create it. No changes is tempting if you think plan trusts state alone, but it checks reality first.","Domain 6: Resource drift"],
    ["x51",0,7,"You move a bucket resource from the root module into a new child module called logs. How do you keep Terraform from destroying and recreating the bucket?",["removed { from = aws_s3_bucket.logs }","depends_on = [aws_s3_bucket.logs] in the module block","moved { from = aws_s3_bucket.logs  to = module.logs.aws_s3_bucket.this }","Run terraform import again on the old root address after deleting the block"],2,"moved blocks can map a resource to an address inside a module, so the existing object is reused. A removed block is tempting because it also refactors, but it drops management instead of moving it.","Domain 7: Refactoring with moved blocks"],
    ["x52",0,7,"What is the main advantage of a removed block over terraform state rm?",["removed blocks always delete the real resource as well as forgetting it","state rm works only with local state","removed blocks do not need an apply","The change is written in code and shown in a plan before it happens"],3,"A removed block is declarative: it is reviewed in version control and appears in the plan, while state rm changes state immediately. Saying removed deletes the resource is tempting, but destroy = false keeps it.","Domain 7: removed blocks vs terraform state rm"],
    ["x53",0,7,"A shell script needs the value of output db_host with no quotes or JSON formatting. Which command?",["terraform output -json db_host","terraform show db_host","terraform output -raw db_host","terraform state show db_host"],2,"terraform output -raw prints a single string output exactly as-is, which is ideal for scripts. -json is tempting for automation, but it keeps the quotes.","Domain 7: terraform output"],
    ["x54",0,7,"`terraform import aws_instance.web i-0abc123` fails saying the resource address does not exist in the configuration. What is missing?",["A resource block for aws_instance.web","The -generate-config-out option on import","A data block for the instance","terraform init -upgrade"],0,"The terraform import command attaches an existing object to an address that must already be declared in the configuration. -generate-config-out is tempting, but it works with import blocks in terraform plan.","Domain 7: terraform import command"],
    ["x55",0,8,"How do HCP Terraform workspaces differ from CLI workspaces?",["They are the same as CLI workspaces with a new name","They share one state file for all environments","They can only be used with the VCS-driven workflow","Each has its own state, variables, permissions and run history"],3,"An HCP Terraform workspace is a separate unit with its own state, variables, access and runs. Treating them as renamed CLI workspaces is tempting, but CLI workspaces lack their own variables and permissions.","Domain 8: HCP Terraform workspaces"],
    ["x56",0,8,"A workspace needs AWS_ACCESS_KEY_ID set for the provider. In HCP Terraform, which variable category fits?",["Terraform variable","Environment variable","Output variable","Local value"],1,"Environment variables are set in the run environment, where providers read credentials such as AWS_ACCESS_KEY_ID. Terraform variable is tempting, but that category maps to declared input variables.","Domain 8: Variables in HCP Terraform"],
    ["x57",0,8,"A variable is marked sensitive in an HCP Terraform workspace. What does that mean for users of the web interface?",["The value is visible only to the workspace creator","The value can be set and replaced but not viewed","The value is not sent to runs","The value is stored in the Git repository"],1,"Sensitive workspace variables are write-only: they can be updated or deleted but not read back through the UI or API. Believing the creator can still see them is tempting, but no one can.","Domain 8: Sensitive variables"],
    ["x58",0,8,"A team wants HCP Terraform runs to authenticate to the cloud without storing long-lived access keys in the workspace. Which feature fits?",["A variable set holding the access keys","Run triggers","The private registry","Dynamic provider credentials"],3,"Dynamic provider credentials use workload identity tokens to get short-lived credentials for each run, so no static keys are stored. Variable sets are tempting because they share secrets, but they still hold long-lived keys.","Domain 8: Dynamic provider credentials"],
    ["x59",0,8,"A security team wants a third-party scanning service to check each plan in HCP Terraform and be able to stop the run. Which integration fits?",["Run tasks","Notifications","Run triggers","Health assessments"],0,"Run tasks call external services at set stages of a run and can be configured to block it on failure. Notifications are tempting because they also reach external tools, but they only report.","Domain 8: Run tasks"],
    ["x60",0,8,"A cloud block uses `workspaces { tags = [\"app\"] }` instead of a single name. What does this allow from the CLI?",["Running plans in every workspace that has the tag at the same time, on each command","Creating a new organization for each tag","Using any workspace with that tag, chosen with terraform workspace select","Sharing one state file across all tagged workspaces"],2,"With tags, the working directory can use any HCP Terraform workspace with those tags, and you switch between them with terraform workspace select. Running them all at once is tempting, but the CLI targets one workspace per run.","Domain 8: The cloud block"]
  ],
  whys: {
    "x1": [null,"Patching in place by hand is exactly what lets servers drift apart over time.","Snapshots help recovery but do not stop running servers from changing independently.","State records managed resource attributes; it is not a place to track or apply OS patches."],
    "x2": ["This reverses the usual roles: Terraform is built for provisioning infrastructure through APIs.",null,"Terraform can pass startup scripts, but it is not designed to manage software inside long-lived servers.","Having two tools own the same resources' lifecycle causes conflicts and drift."],
    "x3": ["Ignoring the resource leaves code and reality out of sync and hides future drift.","This repeats the drift every time and defeats IaC.",null,"Deleting state makes Terraform lose track of every resource, which is far worse than the drift."],
    "x4": ["Screenshots show a state at a moment but not who made a change or why.","A shared account hides who made each change.","fmt only standardizes code style; it records nothing about who changed what.",null],
    "x5": ["Terraform compares desired and current state; it does not add the new count on top.","count can be lowered; Terraform removes the extra instances.",null,"Terraform manages the end state, so it removes resources no longer in the configuration."],
    "x6": [null,"~> 1.2.0 only lets the last number rise, so 1.3.0 is outside the range.","Versions below 1.2.0 are excluded, because ~> 1.2.0 also means at least 1.2.0.","A new major version is well outside ~> 1.2.0."],
    "x7": ["2.3.1 is explicitly excluded by the != condition.","The < 3.0.0 condition excludes 3.0.0.","The >= 2.0.0 condition excludes anything below 2.0.0.",null],
    "x8": ["The lock file tracks providers only; module versions are not recorded in it.",null,"Resource attributes belong in the state file, not the dependency lock file.","State locks are held by the backend; the lock file is about dependencies."],
    "x9": ["Ignoring the lock file removes version pinning, so machines can drift to different provider builds.",null,"-upgrade picks newer versions each run, which defeats the purpose of locking.","The macOS plugin binaries cannot run on Linux, and .terraform should not be committed."],
    "x10": ["Partner providers are served by the Terraform Registry and installed by init like any other.","Any registry provider can be used with the plain CLI.","alias is for multiple configurations of one provider; it does not tell init where to download from.",null],
    "x11": [null,"Official providers are owned and maintained by HashiCorp itself.","Community providers are published by individuals or groups, not verified partner companies.","Archived describes a provider that is no longer maintained, not who maintains it."],
    "x12": ["init installs every provider the configuration needs, not just the first.","A configuration can use many different providers at once.",null,"init does not prompt for providers; it installs all required ones."],
    "x13": ["Terraform does not put a time limit on saved plans; staleness depends on state changes.","Applying a saved plan does not prompt for approval, so -auto-approve is not needed.","Any file name given to -out can be applied.",null],
    "x14": ["-input=false stops prompts for missing variable values, but apply still requires approval.","This skips refreshing state; it does not affect the approval prompt.",null,"This disables state locking, which is risky and unrelated to approval."],
    "x15": ["-json changes the output format, but the exit code alone does not distinguish changes from no changes.",null,"This only shortens how warnings are shown.","This removes color codes from the output; exit codes stay the same."],
    "x16": [null,"Formatting is fmt's job; validate checks syntax, types and references.","validate runs on its own and needs no plan.","validate checks every resource; it just does not check real values against the cloud."],
    "x17": [null,"-list=false only stops fmt from printing the names of changed files.","-write=true is the default and rewrites files; it does not display the changes.","terraform fmt does not offer a JSON diff output."],
    "x18": ["-target limits which resources are included, and is meant for exceptional cases, not rate limits.",null,"This skips the refresh step but does not reduce concurrent create or update calls.","This only changes how long Terraform waits for the state lock."],
    "x19": ["Terraform follows the dependency graph, not the order of blocks in files.","The reference creates an implicit dependency, so B must wait for A.",null,"Names do not decide order; only dependencies do."],
    "x20": ["plan -destroy only previews the destruction and changes nothing.","state rm only forgets resources in state; it does not delete them, and there is no -all flag.","-replace recreates a named resource; it does not destroy everything.",null],
    "x21": ["Data sources are never destroyed; destruction is shown with -.","Replacement is shown with -/+ for managed resources.",null,"Imports are reported separately in the plan, not with <=."],
    "x22": [null,"Terraform checks that state has not changed since planning, so it will not apply blindly.","Terraform does not merge plans; the old plan has to be recreated.","B's run has already finished; the problem is outdated state, not a held lock."],
    "x23": ["They do opposite things: one only refreshes, the other skips refreshing.","Neither changes infrastructure; a refresh-only apply only updates state.","This reverses them: -refresh-only is the one that updates state from the real world.",null],
    "x24": ["terraform.tfvars is loaded before any *.auto.tfvars files, so the later file overrides it.",null,"Defaults apply only when no other value is provided.","Terraform does not error; later sources override earlier ones."],
    "x25": ["Later options on the command line override earlier ones, so common.tfvars loses.",null,"Terraform never combines values; one overrides the other.","Terraform only prompts when no value is given at all."],
    "x26": ["nullable = false means the variable can never be null inside the module.","Terraform does not convert null to zero for numbers.","With a default present, Terraform uses the default instead of raising an error.",null],
    "x27": [null,"count applies to resources and modules, not to nested blocks inside a resource.","A for expression builds a value, but it cannot create nested blocks on its own.","Separate modules are heavy-handed and would not add nested blocks to one resource."],
    "x28": ["This is the old splat form with an index; it still treats the map like a list and would give one ID at most.","This looks up an instance whose key is literally *, which does not exist.",null,"count.index exists only in resources that use count, not in outputs or for_each resources."],
    "x29": ["Square brackets build a list, so the keys are lost.","With one symbol over a map it binds the value, so the keys become the old values instead of env and team.","upper works on a single string, not on a whole map.",null],
    "x30": ["depends_on sets ordering between objects; it cannot switch a resource on or off.","ignore_changes lists resource attributes to ignore, not a condition for creating the resource.",null,"enabled is not a Terraform meta-argument, so it only works if the provider defines it."],
    "x31": ["This changes the order of replacement but does not cause one.",null,"ignore_changes stops attribute changes from triggering updates, the opposite of what is needed.","prevent_destroy blocks destruction and would stop the replacement."],
    "x32": [null,"prevent_destroy does not add confirmations; it blocks the plan.","Terraform does not skip resources silently; the whole plan errors.","Terraform cannot turn a required replacement into an in-place update."],
    "x33": [null,"nonsensitive removes the protection and should only be used deliberately, not as a routine fix.","Locals are not outputs and cannot be read from outside the module.","This only changes how warnings are displayed; the error remains."],
    "x34": ["-var sets input variables only; locals are computed inside the module.",null,"Locals are not stored in state as separate values and are not encrypted.","Locals can hold any expression, including references and functions."],
    "x35": ["A set holds values as soon as they are assigned.","Terraform list indexes start at 0; the problem is that sets have no index at all.",null,"set(string) holds strings; sets can have any single element type."],
    "x36": ["Values read by data sources and used in resource arguments are recorded in state.","Terraform does not encrypt individual values in state; protection comes from the backend.","Terraform never writes values back into .tf files.",null],
    "x37": ["count indexes start at 0, so [1] is the second instance.","Dot-number syntax is not how instances are addressed.",null,"String keys are used with for_each; count uses numeric indexes."],
    "x38": [null,"A single slash is read as part of the repository URL, not a subdirectory.","module blocks have no path argument; the subdirectory goes in the source string.","version works only for registry modules; Git sources use ref."],
    "x39": ["-get-plugins is not a current init option, and init is the broader command.","This copies provider plugins to a local directory; it does not handle modules.","validate checks configuration; it does not download anything.",null],
    "x40": ["Without a hostname, Terraform looks in the public registry, not the private one.",null,"This is the public registry host and it omits the provider part of the address.","Private registry modules are not fetched through Git sources."],
    "x41": ["outputs.tf is for output values, not input variables.",null,"tfvars files assign values to variables; they do not declare them, and modules should not rely on them.","The README documents the module but does not declare anything."],
    "x42": ["File order does not determine order; references do.","The reference creates an implicit dependency, so they cannot be fully parallel.","depends_on is not required when a reference already exists.",null],
    "x43": [null,"They are not ignored; they create a separate provider configuration, which is the source of the limitation.","required_providers is used in root and child modules alike.","Plugins are downloaded once per configuration regardless of where provider blocks live."],
    "x44": ["This disables locking, which risks corrupting state when jobs overlap.","This limits concurrent resource operations within a run, not waiting for another run's lock.",null,"This skips refreshing and does not affect lock handling."],
    "x45": ["State files hold resource data, not .tf configuration.","Local locking does not use this file; it holds the previous state.","The backup is plain JSON, just like the main state file.",null],
    "x46": ["Public access would expose secrets stored in the state.","Git copies spread secrets to everyone with repository access and create conflicts.",null,"sensitive only hides values in CLI output; state still holds them in plain text."],
    "x47": ["This only works if someone declares and sets a workspace variable by hand.",null,"There is no workspace object; the built-in value is terraform.workspace.","path has module, root and cwd, but no workspace attribute."],
    "x48": [null,"Workspaces can hold any number of resources.","Different values are possible, for example with terraform.workspace or separate tfvars files.","init does not delete workspaces."],
    "x49": [null,"Modules take source code, not state files.","state pull prints the raw state of the current configuration; it is not a way to reference another project's outputs.","moved blocks refactor addresses within one configuration."],
    "x50": ["Terraform treats a missing object as drift, not corruption.",null,"Plan refreshes state first, finds the bucket missing, and plans to recreate it.","Terraform never edits your configuration files."],
    "x51": ["A removed block makes Terraform forget or destroy the object; it does not link it to its new address.","depends_on sets ordering and does not change resource addresses.",null,"Importing to the old address keeps it in the wrong place and does not match the new code."],
    "x52": ["With lifecycle destroy = false, the object is left in place; neither method must delete it.","state rm works with any backend.","A removed block takes effect on apply, after it appears in the plan.",null],
    "x53": ["-json wraps a string in quotes, which the script would have to strip.","terraform show displays state or a plan; it does not take an output name.",null,"state show takes a resource address, not an output name."],
    "x54": [null,"-generate-config-out is a plan option used with import blocks, not the import command.","A data block only reads the instance; import needs a managed resource block.","Upgrading providers does not add the missing resource block."],
    "x55": ["CLI workspaces only split state inside one backend; HCP workspaces hold much more.","Each HCP Terraform workspace has its own state.","Workspaces support VCS, CLI and API-driven workflows.",null],
    "x56": ["Terraform variables fill input variables in the configuration; the provider reads AWS_ACCESS_KEY_ID from the environment.",null,"Outputs are values a workspace exports, not inputs set for runs.","Local values are defined in configuration, not in workspace settings."],
    "x57": ["Once saved, a sensitive variable's value is not shown to anyone in the interface.",null,"Sensitive variables are still available to runs; they are just hidden from view.","Workspace variables are stored in HCP Terraform, not in your repository."],
    "x58": ["A variable set still stores long-lived keys, just in a shared place.","Run triggers start runs when another workspace changes; they do not provide credentials.","The private registry shares modules and providers, not credentials.",null],
    "x59": [null,"Notifications send run events to other systems but cannot block a run.","Run triggers queue runs in other workspaces; they do not call external checks.","Health assessments check drift and conditions on a schedule, not each plan."],
    "x60": ["The CLI works with one selected workspace at a time.","Tags filter workspaces within one organization; they do not create organizations.",null,"Each workspace keeps its own state even when grouped by tag."]
  },
  levels: {
    "tf1": 1, "tf2": 1, "tf3": 1, "tf4": 1, "tf5": 1, "tf6": 1, "tf7": 1, "tf8": 3, "tf9": 2, "tf10": 3,
    "tf11": 1, "tf12": 2, "tf13": 1, "tf14": 2, "tf15": 2, "tf16": 2, "tf17": 1, "tf18": 1, "tf19": 1, "tf20": 2,
    "tf21": 1, "tf22": 3, "tf23": 2, "tf24": 2, "tf25": 2, "tf26": 1, "tf27": 3, "tf28": 1, "tf29": 1, "tf30": 2,
    "tf31": 1, "tf32": 1, "tf33": 2, "tf34": 3, "tf35": 1, "tf36": 1, "tf37": 2, "tf38": 2, "tf39": 3, "tf40": 3,
    "tf41": 2, "tf42": 2, "tf43": 2, "tf44": 2, "tf45": 2, "tf46": 2, "tf47": 3, "tf48": 3, "tf49": 2, "tf50": 3,
    "tf51": 2, "tf52": 3, "tf53": 2, "tf54": 1, "tf55": 1, "tf56": 1, "tf57": 2, "tf58": 2, "tf59": 3, "tf60": 3,
    "tf61": 3, "tf62": 2, "tf63": 2, "tf64": 1, "tf65": 1, "tf66": 2, "tf67": 2, "tf68": 2, "tf69": 3, "tf70": 2,
    "tf71": 2, "tf72": 3, "tf73": 2, "tf74": 2, "tf75": 3, "tf76": 1, "tf77": 3, "tf78": 3, "tf79": 1, "tf80": 2,
    "tf81": 1, "tf82": 1, "tf83": 2, "tf84": 2, "tf85": 2, "tf86": 2, "tf87": 3, "tf88": 2, "tf89": 2, "tf90": 1,
    "x1": 2, "x2": 2, "x3": 2, "x4": 1, "x5": 2, "x6": 2, "x7": 2, "x8": 2, "x9": 3, "x10": 3,
    "x11": 1, "x12": 1, "x13": 3, "x14": 1, "x15": 2, "x16": 2, "x17": 1, "x18": 2, "x19": 3, "x20": 1,
    "x21": 2, "x22": 3, "x23": 3, "x24": 2, "x25": 2, "x26": 3, "x27": 2, "x28": 3, "x29": 2, "x30": 2,
    "x31": 2, "x32": 1, "x33": 3, "x34": 1, "x35": 3, "x36": 3, "x37": 1, "x38": 3, "x39": 2, "x40": 2,
    "x41": 1, "x42": 2, "x43": 3, "x44": 2, "x45": 1, "x46": 2, "x47": 1, "x48": 3, "x49": 2, "x50": 2,
    "x51": 3, "x52": 3, "x53": 1, "x54": 2, "x55": 2, "x56": 2, "x57": 1, "x58": 2, "x59": 2, "x60": 3
  }
});
