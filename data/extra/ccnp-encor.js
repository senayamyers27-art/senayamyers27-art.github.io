/* More ccnp-encor practice questions with wrong-answer notes, and a difficulty level for every question. Merged into the free bank by tools/build.js. */
CertHub.addExtra("ccnp-encor", {
  questions: [
    ["x1", 0, 1, "During bursts, a WAN router's output queue fills and tail drop hits many TCP flows at the same moment, so they all back off and ramp up together (global synchronization). Which QoS tool is designed to reduce this?", ["LLQ with a strict priority queue for all TCP traffic", "Policing TCP traffic to a fixed rate at ingress", "WRED, dropping selected packets early before the queue is full", "A larger hardware transmit ring on the interface so the output queue never fills up"], 2, "WRED randomly drops some packets (more from lower-priority markings) as the average queue depth rises, so only some TCP flows slow down at a time. LLQ is tempting, but a priority queue for all TCP would starve other traffic and does nothing about synchronized tail drop.", "Domain 1: QoS, WRED"],
    ["x2", 0, 1, "An access switch sends voice frames to a distribution switch over an 802.1Q trunk with a CoS value of 5. Where is CoS carried, and what happens to it when the packet is routed to another subnet?", ["In the 802.1Q tag's priority bits; it is lost when the Layer 2 header is rewritten", "In the IP header's ToS byte; it survives unchanged across every routed hop to the destination", "In the 802.1Q VLAN ID field; the router copies it into the next tag", "In the Ethernet frame check sequence; each hop recalculates it"], 0, "CoS is the 3-bit priority (PCP) field in the 802.1Q tag, so it exists only on tagged links and disappears when a router builds a new frame. The ToS byte option describes DSCP, which is the Layer 3 marking that does survive routing.", "Domain 1: QoS marking, CoS vs DSCP"],
    ["x3", 0, 1, "A provider must enforce a 100 Mb/s contract on traffic arriving from a customer on its edge router's inbound interface. Which tool fits, and why?", ["Shaping, which buffers the excess traffic as it arrives inbound", "Policing, which can drop or re-mark the excess as it arrives", "WRED, which re-marks the excess traffic as EF before queuing", "CBWFQ, which guarantees the customer minimum bandwidth inbound"], 1, "Policing can be applied inbound and immediately drops or re-marks traffic that exceeds the configured rate. Shaping is tempting but it works on egress queues, delaying excess traffic rather than enforcing a limit as packets arrive.", "Domain 1: QoS, policing vs shaping"],
    ["x4", 0, 1, "R1 is the HSRP active router with priority 110 and R2 has priority 100. R1 reloads, R2 takes over, and when R1 comes back it stays in standby. What is missing from R1's configuration?", ["A priority above 150, because 110 is not high enough to win", "A matching HSRP version on the two routers", "An object track on R2's uplink interface", "The standby preempt command on R1"], 3, "HSRP does not take back the active role based on priority alone; preemption must be enabled on the higher-priority router. Raising the priority is tempting, but without preempt even a much higher priority leaves R1 in standby until R2 fails.", "Domain 1: High availability, FHRP preemption"],
    ["x5", 0, 1, "In a Cisco SD-Access fabric, which node role connects the fabric to networks outside it, such as the data center, WAN or internet?", ["Border node", "Edge node", "Control plane node", "Intermediate node"], 0, "Border nodes are the fabric's gateway to external networks and hand traffic between the fabric and outside routing domains. The edge node is tempting, but it connects endpoints such as users and access points, not external networks.", "Domain 1: SD-Access fabric roles"],
    ["x6", 0, 1, "A company plans to replace its MPLS WAN with SD-WAN running over broadband internet circuits. Which is a realistic limitation to plan for?", ["SD-WAN cannot use broadband internet circuits as a transport", "Every branch must buy its internet service from the same ISP", "Internet underlays have no provider SLA, so quality still rests on them", "SD-WAN cannot encrypt the traffic that travels between branch sites over the internet"], 2, "SD-WAN can steer traffic around a bad path, but it cannot make an internet circuit meet an SLA the provider never offered. The other choices are false: SD-WAN mixes transports and ISPs freely and builds encrypted IPsec tunnels between WAN Edges.", "Domain 1: SD-WAN benefits and limitations"],
    ["x7", 0, 1, "A Layer 2 switch receives a frame in VLAN 20 whose destination MAC address it has never seen. What does it do?", ["Drops the frame after a TCAM lookup miss", "Floods it within VLAN 20 after a CAM table miss", "Punts it to the CPU for ARP resolution after a FIB miss", "Sends it only out the root port, per the STP table"], 1, "An unknown unicast destination is a CAM (MAC address) table miss, so the switch floods the frame out every port in that VLAN except the one it came in on. The ARP choice is tempting, but ARP is a Layer 3 function and a Layer 2 switch simply floods.", "Domain 1: CAM table"],
    ["x8", 0, 1, "Which statement about the RIB and the CEF FIB on an IOS router is correct?", ["The RIB is derived from the FIB after routing protocols fill it", "The FIB contains only directly connected and static routes", "The RIB stores the Layer 2 rewrite used for each packet", "The FIB is built from the RIB with recursion pre-resolved"], 3, "Routing protocols populate the RIB, and CEF builds the FIB from it with recursive next hops already resolved so each packet needs a single lookup. The Layer 2 rewrite choice is wrong because that data lives in the CEF adjacency table, not the RIB.", "Domain 1: Hardware and software switching, RIB and FIB"],
    ["x9", 0, 1, "A manufacturer's control application needs very low latency to machines on the plant floor. Leadership also wants email and CRM without buying more servers. Which deployment approach fits best?", ["Hybrid: keep the control app on site and move email and CRM to SaaS", "Public cloud for everything, since cloud hosting always lowers latency", "On premises for everything, since SaaS cannot be secured properly", "A private cloud in the provider's region for the control application"], 0, "A hybrid model keeps the latency-sensitive workload close to the machines while offloading commodity apps to SaaS. Moving everything to public cloud is tempting for cost, but a distant data center adds WAN latency the control app cannot tolerate.", "Domain 1: Cloud vs on-premises design"],
    ["x10", 0, 2, "A team wants to run dozens of instances of a small web service on one Linux host, with fast startup and little overhead. Why are containers usually chosen over virtual machines here?", ["Each container carries its own guest kernel, giving stronger isolation", "Containers share the host kernel, so they start fast and use less", "Containers need a type 1 hypervisor that replaces the host OS", "Containers can run any OS, such as Windows on a Linux kernel"], 1, "Containers package the app and its libraries but share the host OS kernel, which makes them lightweight and quick to start. The guest-kernel choice describes virtual machines, and it is the reason VMs are heavier, not lighter.", "Domain 2: Containers vs virtual machines"],
    ["x11", 0, 2, "Users can ping servers across a GRE tunnel, but large web pages and file transfers hang. The physical links have a 1500-byte MTU. What is the recommended fix on the tunnel interfaces?", ["Send GRE keepalives more often so the tunnel stays up", "Change the tunnel source to a loopback interface", "Set ip mtu 1400 and ip tcp adjust-mss 1360", "Use IPsec transport mode so no extra header is added"], 2, "GRE adds headers, so full-size packets exceed the path MTU; lowering the tunnel IP MTU and clamping TCP MSS keeps TCP segments small enough to avoid fragmentation or black-holing. Keepalives are tempting but only detect a dead tunnel; this tunnel is up and small pings succeed.", "Domain 2: GRE tunnels, MTU and MSS"],
    ["x12", 0, 2, "In an IKEv1 site-to-site IPsec VPN, how does Phase 1 differ from Phase 2?", ["Phase 1 negotiates the IPsec SAs that carry user data; Phase 2 then authenticates the peers", "Phase 1 builds the GRE tunnel, and Phase 2 adds encryption to it", "Both phases negotiate the same SA, and Phase 2 only rekeys it", "Phase 1 authenticates peers and builds the ISAKMP SA; Phase 2 sets up IPsec SAs"], 3, "Phase 1 authenticates the peers and creates a protected ISAKMP (IKE) SA, and Phase 2 runs inside it to negotiate the IPsec SAs that protect user traffic. The first option reverses the two phases, which is the classic trap.", "Domain 2: IPsec, IKE phases"],
    ["x13", 0, 2, "Two sites must run OSPF across an IPsec VPN, and the engineer wants a routable interface with no crypto ACL to maintain. What should be configured?", ["A static VTI using tunnel mode ipsec ipv4 and tunnel protection", "A crypto map on the physical interface with a crypto ACL", "IPsec transport mode through a crypto map and no tunnel interface", "An SSL VPN client session running on each of the routers"], 0, "A virtual tunnel interface is a real routed interface, so OSPF can form adjacencies over it and traffic is encrypted by routing into the tunnel rather than by matching a crypto ACL. A crypto map is tempting, but it needs a crypto ACL and does not natively carry multicast routing protocol hellos.", "Domain 2: Virtual tunnel interfaces"],
    ["x14", 0, 2, "A LISP ITR must forward traffic to an EID it has no mapping for. Where does it send its Map-Request?", ["To the map server, where ETRs look up their own registrations", "To the map resolver, which relays it toward the authoritative ETR", "To every ETR in the LISP site by broadcast", "To the proxy ETR, which holds every EID-to-RLOC mapping"], 1, "ITRs send Map-Requests to a map resolver, which forwards them through the mapping system to the map server and on to the ETR that registered the EID. The map server is tempting, but it is where ETRs register mappings, not where ITRs send queries.", "Domain 2: LISP map server and map resolver"],
    ["x15", 0, 2, "Which statement correctly describes how VXLAN carries traffic between two VTEPs?", ["It carries the frame inside GRE with a 12-bit VLAN ID", "The VTEP rewrites the source MAC and forwards at Layer 2", "It wraps the Ethernet frame in a VXLAN header, UDP and outer IP", "It encrypts the whole frame with IPsec before the underlay network sends it on"], 2, "VXLAN is MAC-in-UDP: the original frame gets a VXLAN header with a 24-bit VNI, then a UDP header (IANA port 4789) and an outer IP header between VTEP addresses. GRE with a VLAN ID is tempting but describes a different encapsulation and the old 12-bit limit VXLAN was built to escape.", "Domain 2: VXLAN encapsulation"],
    ["x16", 0, 3, "A security review requires that a user-facing switch port never become a trunk, even if the attached device sends DTP frames. Which configuration meets this?", ["switchport mode dynamic desirable", "switchport mode trunk together with switchport nonegotiate", "switchport mode dynamic auto", "switchport mode access with switchport nonegotiate"], 3, "Static access mode keeps the port from trunking, and nonegotiate stops it from sending or acting on DTP. The trunk-with-nonegotiate option is tempting because it disables DTP, but it forces the port to be a trunk, which is the opposite of the requirement.", "Domain 3: DTP and trunking"],
    ["x17", 0, 3, "To reduce the risk of double-tagging VLAN hopping across 802.1Q trunks, what is the recommended native VLAN practice?", ["Use an unused VLAN as native and assign it to no access ports", "Keep VLAN 1 as the native VLAN on every trunk and tag nothing else on it", "Enable PortFast on all trunk ports", "Allow every VLAN on every trunk"], 0, "Double tagging works because the outer tag matching the native VLAN is stripped, so a native VLAN that no user port belongs to removes the attack path. Keeping VLAN 1 as native is the default and is exactly what the attack relies on.", "Domain 3: Native VLAN"],
    ["x18", 0, 3, "SW1's ports use channel-group 5 mode auto, and SW2's ports use channel-group 5 mode auto. What is the result?", ["PAgP forms the bundle because both ends support PAgP", "No EtherChannel forms, since auto only answers and never starts", "LACP is negotiated instead, because PAgP automatically falls back to LACP when it fails", "The ports err-disable because of a mode mismatch"], 1, "PAgP auto is passive: it responds to PAgP packets but never sends them, so two auto ends never negotiate and the links stay individual. The first option is tempting, but both ends supporting PAgP is not enough without one side in desirable mode.", "Domain 3: EtherChannel, PAgP modes"],
    ["x19", 0, 3, "An alternate port on an access switch stops receiving BPDUs because of a unidirectional fiber fault. It moves to designated and forwarding, and a loop forms. Which feature would have prevented this?", ["BPDU guard", "Root guard", "Loop guard", "PortFast"], 2, "Loop guard puts a non-designated port that stops receiving BPDUs into loop-inconsistent (blocking) instead of letting it become designated. Root guard is tempting but acts when a port receives superior BPDUs, not when BPDUs stop arriving.", "Domain 3: Spanning tree, loop guard"],
    ["x20", 0, 3, "The engineer wants the core switch CORE1 to be the root bridge for VLAN 10, with CORE2 as a predictable backup root. Which configuration achieves this?", ["spanning-tree portfast default on both core switches", "spanning-tree vlan 10 cost 1 on every CORE1 uplink and a higher cost on every CORE2 uplink", "Replace CORE1 with a switch that has a lower MAC address", "spanning-tree vlan 10 priority 4096 on CORE1 and 8192 on CORE2"], 3, "The lowest bridge priority wins root, so explicit low priorities make CORE1 root and CORE2 next in line regardless of MAC addresses. Changing port cost is tempting, but cost affects which ports forward toward the root, not which switch is elected root.", "Domain 3: Spanning tree, root placement"],
    ["x21", 0, 3, "Why can RSTP bring a point-to-point link between two switches to forwarding in about a second instead of waiting on timers?", ["The proposal/agreement handshake syncs downstream ports first", "RSTP removes the blocking state, so every port starts out forwarding immediately", "RSTP shortens max age to two seconds on every link", "BPDU guard lets designated ports skip learning"], 0, "On point-to-point links, a designated port sends a proposal, the neighbor syncs (blocks its non-edge ports) and returns an agreement, so forwarding starts without timer-based listening and learning. RSTP still blocks ports (discarding state), so the second option is wrong.", "Domain 3: RSTP rapid convergence"],
    ["x22", 0, 3, "With default K values, which values does EIGRP use to calculate its composite metric?", ["Bandwidth, delay, load and reliability of each link", "Minimum bandwidth on the path and total delay", "Hop count and the slowest link bandwidth", "An interface cost based on reference bandwidth"], 1, "By default only K1 and K3 are nonzero, so the metric uses the slowest bandwidth along the path and the sum of delays. Load and reliability are part of the formula, which makes the first option tempting, but their K values are 0 by default.", "Domain 3: EIGRP metric"],
    ["x23", 0, 3, "R2 has 10.1.0.0/24 through 10.1.3.0/24 in EIGRP AS 100. The engineer enters ip summary-address eigrp 100 10.1.0.0 255.255.252.0 on R2's interface toward R1. What does R2 now do?", ["Sends R1 the summary plus all four /24 routes", "Advertises the /22 summary out every EIGRP interface on R2, not just toward R1", "Sends R1 only 10.1.0.0/22 and adds a local route to Null0", "Removes the four /24 routes from its own routing table"], 2, "Interface summarization suppresses the component routes on that interface only and creates a summary route to Null0 on R2 to prevent loops. The every-interface option is wrong because EIGRP summaries are configured per interface.", "Domain 3: EIGRP summarization"],
    ["x24", 0, 3, "Three OSPF routers on one Ethernet segment start at the same time. R1 has priority 1 and RID 1.1.1.1, R2 has priority 1 and RID 2.2.2.2, and R3 has priority 0 and RID 9.9.9.9. Which router becomes the DR?", ["R3, because it has the highest router ID", "R1, because it has the lowest router ID", "None, because one router has priority 0", "R2, the highest RID among eligible routers"], 3, "Priority 0 makes R3 ineligible, so among the tied priority-1 routers the highest router ID, R2, becomes DR and R1 becomes BDR. Picking R3 for its RID is the trap; the router ID is only a tiebreaker among eligible routers.", "Domain 3: OSPF DR election"],
    ["x25", 0, 3, "ABR R1 sits between area 0 and area 5. The engineer wants R1 to advertise 172.16.0.0/16 into area 0 instead of the individual area 5 subnets. Which command does this?", ["area 5 range 172.16.0.0 255.255.0.0 on R1", "summary-address 172.16.0.0 255.255.0.0 on R1", "ip summary-address ospf on the area 0 interface", "area 0 range 172.16.0.0 255.255.0.0 on an area 5 router"], 0, "OSPF inter-area summarization is done on the ABR with area <source-area> range. The summary-address command is tempting, but in OSPF it summarizes external routes on an ASBR, not inter-area routes.", "Domain 3: OSPF summarization"],
    ["x26", 0, 3, "An ABR connects area 0 and area 20. One specific inter-area prefix must be kept out of area 20 while all other routes still flow. What should the engineer configure?", ["distribute-list in on an internal router in area 20", "area 20 filter-list prefix with direction in on the ABR", "Make area 20 a totally stubby area", "passive-interface on the ABR's area 20 interface"], 1, "A filter-list on the ABR filters Type 3 LSAs as they are created for the area, so the prefix never enters area 20's database. A distribute-list is tempting, but it only keeps the route out of the local routing table; the LSA still floods through the area.", "Domain 3: OSPF route filtering"],
    ["x27", 0, 3, "Four OSPF routers share a broadcast segment. R3 and R4, both DROTHERs, show each other in the 2-WAY state. What does this indicate?", ["The hello and dead timers do not match", "The interface MTU differs between them", "Normal behavior; DROTHERs go FULL only with DR and BDR", "The two routers have duplicate router IDs"], 2, "On broadcast networks DROTHERs stop at 2-WAY with each other and form FULL adjacencies only with the DR and BDR. An MTU mismatch is tempting, but it causes a stall in EXSTART or EXCHANGE, not a stable 2-WAY state.", "Domain 3: OSPF neighbor states"],
    ["x28", 0, 3, "R1 in AS 65001 peers with directly connected R2. R2 is in AS 65002, but R1 was configured with neighbor 10.0.12.2 remote-as 65020. What happens to the session?", ["It reaches Established but no prefixes are exchanged", "It stays in Connect because TCP port 179 is filtered", "Routes are received but marked as invalid", "The OPEN is rejected with a bad peer AS notification"], 3, "BGP checks the AS number in the peer's OPEN message; a mismatch triggers a NOTIFICATION and the session never reaches Established. Connect is tempting, but TCP can open fine here; the failure happens at OPEN.", "Domain 3: BGP neighbor states"],
    ["x29", 0, 3, "An enterprise has links to ISP A and ISP B and wants inbound traffic from the internet to prefer ISP A. Which method is commonly used?", ["Prepend its own AS several times on routes sent to ISP B", "Set a higher local preference on routes learned from ISP A", "Set a higher weight on the ISP A neighbor", "Lower the MED on routes received from ISP B"], 0, "AS-path prepending makes the path through ISP B look longer to the rest of the internet, pulling inbound traffic toward ISP A. Local preference is tempting, but it only influences the enterprise's own outbound path choice and is not sent to eBGP peers.", "Domain 3: BGP path selection"],
    ["x30", 0, 3, "A PBR route map matches ACL GUEST and sets ip next-hop 198.51.100.1. It is applied inbound on the LAN interface. What happens to packets that do not match the route map?", ["They are dropped by an implicit deny in the route map", "They are forwarded normally using the routing table", "They are sent to the Null0 interface", "They are sent to 198.51.100.1 anyway"], 1, "In PBR, traffic that does not match a permit clause is not policy routed and falls back to normal destination-based routing. The implicit-deny option is tempting because ACLs work that way, but for PBR a deny means route normally, not drop.", "Domain 3: Policy-based routing"],
    ["x31", 0, 3, "A web server at 10.1.1.20 must always be reachable from the internet at 203.0.113.20. Which NAT configuration fits?", ["ip nat inside source list 10 interface Gi0/0 overload", "A dynamic NAT pool containing one public address", "ip nat inside source static 10.1.1.20 203.0.113.20", "ip nat outside source list 20 pool OUTSIDE"], 2, "Static NAT creates a permanent one-to-one mapping, so outside hosts can start connections to the server at any time. A one-address dynamic pool is tempting, but its mapping exists only after an inside host sends traffic, and it can be taken by other hosts.", "Domain 3: NAT, static NAT"],
    ["x32", 0, 3, "A PAT configuration looks correct, but show ip nat translations stays empty and inside hosts cannot reach the internet. What is the most likely missing piece?", ["The overload keyword on the NAT statement", "A default route on the ISP's router", "An ACL entry permitting SSH to the router", "ip nat inside and ip nat outside on the interfaces"], 3, "The router only translates traffic that flows between interfaces marked inside and outside, so with no marking nothing is translated and the table stays empty. A missing overload is tempting, but without it the first host would still get a translation.", "Domain 3: NAT troubleshooting"],
    ["x33", 0, 3, "Which capability does IGMPv3 add compared with IGMPv2?", ["Hosts can include or exclude specific sources in reports", "It removes the Leave message to cut overhead", "It replaces PIM for building multicast distribution trees between the routers", "It runs between routers instead of hosts and routers"], 0, "IGMPv3 lets a receiver say which sources it wants (or does not want) for a group, which is what makes SSM possible. Replacing PIM is wrong because IGMP runs only between hosts and their local router; PIM still builds the trees between routers.", "Domain 3: Multicast, IGMPv3"],
    ["x34", 0, 4, "A port on SW1 must be monitored by an analyzer on SW3. The switches are joined by Layer 2 trunks with no routed hop between them. Which feature fits best?", ["Local SPAN on SW1", "RSPAN, with a remote-span VLAN carried on the trunks", "ERSPAN, which any switch-to-switch mirroring requires", "Flexible NetFlow export from SW1 to SW3"], 1, "RSPAN carries mirrored traffic in a dedicated remote-span VLAN across Layer 2 trunks to a destination port on another switch. ERSPAN is tempting, but it is needed when the path crosses routed hops; it is not required for a pure Layer 2 path.", "Domain 4: SPAN and RSPAN"],
    ["x35", 0, 4, "Which SNMP notification type is acknowledged by the manager and resent by the agent if no acknowledgment arrives?", ["SNMPv1 trap", "GetBulk response", "Inform", "SNMPv2c trap"], 2, "An inform request expects a response from the manager, so the agent retransmits it if none arrives. The SNMPv2c trap is tempting because it shares the v2 PDU format, but traps are unacknowledged and can be silently lost.", "Domain 4: SNMP traps and informs"],
    ["x36", 0, 4, "A router logs %LINEPROTO-5-UPDOWN: Line protocol on Interface Gi0/1, changed state to down. What does the 5 in the message represent?", ["The syslog facility number", "The message sequence number", "The interface slot number", "The severity level, notifications"], 3, "In Cisco message format FACILITY-SEVERITY-MNEMONIC, the number is the severity, and 5 is notifications. The facility is tempting, but the facility is the text part (LINEPROTO) before the number.", "Domain 4: Syslog severity levels"],
    ["x37", 0, 4, "A ping from R1 to 10.9.9.9 succeeds, but hosts in 10.1.1.0/24 behind R1 cannot reach that address. Which test best reproduces the users' path?", ["An extended ping sourced from R1's 10.1.1.1 LAN interface", "The same ping from R1 with a larger repeat count", "A traceroute from R1 to 10.9.9.9 using its default source address for the probes", "show ip interface brief to confirm interfaces are up"], 0, "Sourcing the ping from the LAN interface tests whether the far side has a return route to 10.1.1.0/24, which a default-sourced ping never checks. A default traceroute is tempting but uses the exit interface address and hides the same return-path problem.", "Domain 4: Diagnosing with ping and traceroute"],
    ["x38", 0, 4, "Which statement about RESTCONF is correct?", ["It runs over SSH on port 830 using XML RPC operations", "It maps HTTPS methods onto YANG data, in XML or JSON", "It reads and writes SNMP MIB objects over HTTP", "It can read operational data but never change configuration"], 1, "RESTCONF exposes YANG-modeled data as HTTPS resources that clients read and change with methods such as GET, POST, PUT, PATCH and DELETE, encoded in XML or JSON. The SSH port 830 option describes NETCONF, the most common mix-up.", "Domain 4: NETCONF and RESTCONF"],
    ["x39", 0, 4, "In Flexible NetFlow, which component defines the key and non-key fields that identify and describe a flow?", ["Flow exporter", "Flow monitor", "Flow record", "Flow sampler"], 2, "The flow record lists the match (key) fields that separate flows and the collect (non-key) fields such as byte counts. The flow monitor is tempting because it ties everything together on an interface, but it references a record rather than defining the fields.", "Domain 4: Flexible NetFlow"],
    ["x40", 0, 5, "An attacker on an access VLAN sends gratuitous ARP replies claiming the default gateway's IP address. Which switch feature checks ARP packets against the DHCP snooping binding table and drops the forged ones?", ["IP Source Guard", "Port security", "Storm control", "Dynamic ARP inspection"], 3, "Dynamic ARP inspection intercepts ARP on untrusted ports and drops packets whose IP-to-MAC pairing does not match the DHCP snooping bindings. IP Source Guard is tempting because it uses the same bindings, but it filters IP traffic, not ARP messages.", "Domain 5: Layer 2 protections, DAI"],
    ["x41", 0, 5, "DHCP snooping runs on an access switch. The team must stop hosts from sending IP packets with source addresses they were not assigned. Which feature does this?", ["IP Source Guard", "Dynamic ARP inspection", "Root guard", "BPDU filter"], 0, "IP Source Guard builds per-port filters from the DHCP snooping bindings and drops IP packets whose source address does not match. Dynamic ARP inspection is tempting but validates ARP only, so spoofed IP packets would still pass.", "Domain 5: Layer 2 protections, IP Source Guard"],
    ["x42", 0, 5, "A standard ACL must stop subnet 10.1.1.0/24 from reaching the server subnet 10.3.3.0/24 while still letting it reach everything else. Where should it be applied?", ["Inbound on the interface closest to 10.1.1.0/24", "Outbound on the interface facing the server subnet", "Inbound on the router's WAN interface", "On the router's VTY lines with access-class"], 1, "A standard ACL matches only the source address, so it must sit close to the destination to avoid blocking the source's traffic to other places. Placing it near the source is tempting because that is the rule for extended ACLs, but here it would cut 10.1.1.0/24 off from everything.", "Domain 5: ACL placement"],
    ["x43", 0, 5, "An extended ACL with the single entry permit tcp any host 10.2.2.10 eq 443 is applied inbound on an interface that runs OSPF. What happens to OSPF hellos arriving on it?", ["They pass, since routing protocol packets are exempt from ACLs", "They pass, because this ACL filters only TCP traffic", "They are dropped by the implicit deny, breaking the adjacency", "They are sent to the CPU and allowed by CoPP"], 2, "An inbound interface ACL also filters packets addressed to the router, and OSPF (IP protocol 89) matches nothing but the implicit deny any. The TCP-only option is tempting, but an ACL does not ignore other protocols; anything not permitted is denied.", "Domain 5: ACLs, implicit deny"],
    ["x44", 0, 5, "A router has aaa authentication login default group radius local. The RADIUS server is reachable, but it rejects an administrator's password. What happens?", ["The router tries the local user database next", "The administrator is let in without authentication", "The router keeps retrying the RADIUS server until it accepts the administrator's password", "Login fails; local is used only if RADIUS does not respond"], 3, "Method lists move to the next method only on an error such as no response; an explicit reject is a final answer. Falling back to local is the tempting choice, but that happens only when the RADIUS server is unreachable.", "Domain 5: AAA method lists"],
    ["x45", 0, 5, "Only the management subnet 10.99.0.0/24 may open SSH sessions to a router. Which configuration enforces this most directly?", ["A standard ACL applied with access-class in on the VTY lines", "An extended ACL applied with ip access-group out on each of the router's physical interfaces", "transport input ssh on the VTY lines by itself", "login local on the VTY lines with strong passwords"], 0, "access-class on the VTY lines filters who may open management sessions, regardless of which interface the traffic arrives on. transport input ssh is tempting, but it only limits the protocol, not the source addresses allowed to connect.", "Domain 5: Device access control"],
    ["x46", 0, 5, "In Cisco TrustSec, what does a security group ACL (SGACL) match on?", ["Source and destination IP subnets", "Source and destination security group tags", "VLAN IDs on the ingress trunk", "The vendor OUI in the MAC address"], 1, "SGACLs are applied between groups: the policy is keyed on the source SGT and destination SGT, so it follows users regardless of IP address. IP subnets are tempting because traditional ACLs use them, but removing that dependency is the point of TrustSec.", "Domain 5: TrustSec SGACL"],
    ["x47", 0, 5, "An IP phone and a PC share one switch port. Each must be authenticated separately with 802.1X or MAB, the phone in the voice domain and the PC in the data domain. Which host mode fits?", ["Single-host mode", "Multi-host mode", "Multi-domain mode", "Single-host mode with PortFast"], 2, "Multi-domain authentication allows one authenticated device in the voice domain and one in the data domain on the same port. Multi-host is tempting, but it authenticates only the first device and then lets every other device through.", "Domain 5: 802.1X host modes"],
    ["x48", 0, 5, "Policy must block a file-sharing application that switches ports and hides in TLS. Which capability is best suited?", ["An extended ACL denying its well-known port numbers", "Stateful inspection that tracks TCP flags and sequences", "A stricter CoPP policy on the edge router", "NGFW application identification, independent of port"], 3, "A next-generation firewall identifies applications by behavior and signatures rather than port numbers, so it can block an app that changes ports. A port-based ACL is tempting, but the app simply moves to a port the ACL allows.", "Domain 5: Next-generation firewall"],
    ["x49", 0, 5, "A Python script with the Catalyst Center admin password hardcoded has been pushed to a shared Git repository. What is the best fix?", ["Rotate the password and load secrets from a vault or env", "Base64-encode the password inside the script", "Switch the script to plain HTTP so no certificate handling is needed and the secret is simpler", "Make the repository read-only for everyone"], 0, "The exposed password must be treated as compromised and changed, and the script should read secrets from a vault or environment variables that never go into source control. Base64 is tempting but is encoding, not encryption, and anyone can reverse it.", "Domain 5: REST API security, secret handling"],
    ["x50", 0, 5, "How does TACACS+ protect its messages compared with RADIUS?", ["Both encrypt the entire packet payload", "TACACS+ encrypts the whole body; RADIUS hides only the password", "RADIUS encrypts the whole body; TACACS+ hides only the password", "Neither protocol encrypts any part of the packet"], 1, "TACACS+ encrypts the entire packet body, while RADIUS obscures only the user password attribute and leaves the rest in clear text. The reversed option is the common mix-up.", "Domain 5: AAA, TACACS+ vs RADIUS"],
    ["x51", 0, 5, "A switch port is configured to try 802.1X first and then MAB. A laptop without a supplicant connects. What happens?", ["The port stays unauthorized with no other method tried", "The port is err-disabled because the laptop failed 802.1X, and it stays down until someone clears it", "After 802.1X times out, MAB is tried with the laptop's MAC", "The laptop is placed in the native VLAN automatically"], 2, "With 802.1X then MAB, the switch waits for EAPOL responses, and when 802.1X times out it falls back to MAB using the device MAC. The unauthorized option is tempting, but that is what would happen only if no fallback method were configured.", "Domain 5: 802.1X and MAB"],
    ["x52", 0, 6, "A script sends a RESTCONF POST that creates a new loopback interface, and the request succeeds. Which HTTP status code is expected?", ["204 No Content", "200 OK", "404 Not Found", "201 Created"], 3, "RESTCONF returns 201 Created when a POST creates a new resource. 204 is tempting because RESTCONF uses it for successful PUT or PATCH changes with no body, but POST that creates a resource returns 201.", "Domain 6: REST response codes"],
    ["x53", 0, 6, "A script gets a token from Catalyst Center successfully but receives HTTP 403 when it tries to delete a site. What does this mean?", ["The user is authenticated but not allowed to do that", "The username or password sent in the delete request was wrong or expired", "The site ID in the URL does not exist", "The controller hit an internal server error"], 0, "403 Forbidden means the server knows who you are but your role lacks permission for the action. Wrong credentials are tempting, but those return 401 Unauthorized, and the token here was issued successfully.", "Domain 6: REST response codes"],
    ["x54", 0, 6, "In Python, device = {'hostname': 'R1', 'ip': '10.1.1.1'}. What does device.get('os', 'unknown') return?", ["It raises a KeyError", "'unknown'", "None", "An empty string"], 1, "dict.get returns the default given as its second argument when the key is missing, so the result is 'unknown'. KeyError is tempting because device['os'] would raise it, but get never raises for a missing key.", "Domain 6: Python dictionaries"],
    ["x55", 0, 6, "Which Python code sends a GET with a Catalyst Center token and parses the JSON response into Python objects?", ["r = requests.get(url, headers={'X-Auth-Token': t}); d = r.text['response']", "d = requests.post(url, token=t).json()", "r = requests.get(url, headers={'X-Auth-Token': t}); d = r.json()", "r = requests.get(url, auth=t); d = json(r)"], 2, "Catalyst Center expects the token in the X-Auth-Token header, and response.json() parses the body into dicts and lists. The r.text option is tempting, but text is a plain string and cannot be indexed by a key.", "Domain 6: Python requests library"],
    ["x56", 0, 6, "In a YANG model, what distinguishes a list from a container?", ["A list can hold only one leaf node", "A container has several instances, each one selected by a key leaf value", "A list is always read-only operational data", "A list has keyed entries; a container has one instance"], 3, "A YANG list holds multiple entries, each identified by its key leaf (for example interface name), while a container simply groups nodes and appears once. The second option reverses the two, which is the usual confusion.", "Domain 6: YANG data models"],
    ["x57", 0, 6, "A team wants managed servers to run an agent that regularly pulls desired state from a central master and fixes drift on its own. Which tools match this model?", ["Puppet or Chef", "Ansible", "NETCONF", "EEM"], 0, "Puppet and Chef are agent-based and pull configuration from a central server on a schedule. Ansible is tempting because it is also a configuration tool, but it is agentless and pushes changes over SSH.", "Domain 6: Agent-based vs agentless orchestration"],
    ["x58", 0, 6, "An EEM applet must save the running configuration every day at 02:00. Which event detector should the applet use?", ["event syslog pattern", "event timer cron", "event interface", "event snmp"], 1, "The timer cron event runs the applet on a schedule written as a cron entry, such as 0 2 * * *. A syslog pattern is tempting because it is the most common EEM trigger, but it fires only when a matching message is logged.", "Domain 6: EEM applets"],
    ["x59", 0, 6, "An AI-driven assurance tool flags an unusual rise in DHCP failures at one site that static thresholds did not catch. What is the most appropriate next step?", ["Let the tool reboot the site's switches automatically", "Ignore it, since no static threshold was crossed", "Have an engineer confirm it with logs before any fix", "Retrain the model before looking at the site"], 2, "AI baselining is useful for spotting subtle anomalies, but a human should confirm the finding with supporting data before changing production. Automatic reboots are tempting for speed but skip the guardrail and could cause a wider outage.", "Domain 6: AI in network operations"],
    ["x60", 0, 6, "A script must turn a Python dictionary into a JSON string for a REST request body. Which function does this?", ["json.loads(payload)", "json.load(payload)", "str(payload)", "json.dumps(payload)"], 3, "json.dumps serializes a Python object into a JSON-formatted string. str() is tempting, but it produces Python syntax such as single quotes and True, which is not valid JSON.", "Domain 6: JSON in Python"]
  ],
  whys: {
    "x1": ["A strict priority queue for all TCP would starve other classes and does not stop synchronized tail drop.", "Policing at ingress caps a rate but does not manage queue depth or randomize drops on the congested egress.", null, "A bigger transmit ring only adds delay; when it fills, tail drop and synchronization still happen."],
    "x2": [null, "The ToS byte holds DSCP, a Layer 3 marking; CoS lives in the Layer 2 802.1Q tag.", "The VLAN ID field identifies the VLAN; priority uses the separate 3-bit PCP field.", "The FCS is an error check over the frame and carries no QoS marking."],
    "x3": ["Shaping works on output queues and delays traffic; it is not how a provider enforces an inbound limit.", null, "WRED manages queue depth by early drops; it does not enforce a rate or re-mark to EF.", "CBWFQ allocates bandwidth among classes on egress queues, not a rate cap on inbound traffic."],
    "x4": ["Priority 110 already beats 100; without preempt, no priority value makes R1 take over.", "Mismatched versions would stop the routers from seeing each other at all, not cause this behavior.", "Tracking lowers R2's priority on an uplink failure, but R1 still needs preempt to take over.", null],
    "x5": [null, "Edge nodes connect endpoints to the fabric; they are not the exit to outside networks.", "The control plane node is the LISP map server and resolver that tracks endpoints; it does not route traffic out.", "Intermediate nodes are underlay routers that just forward fabric traffic; they have no fabric edge role."],
    "x6": ["Using broadband and other internet circuits is one of SD-WAN's main selling points.", "SD-WAN is transport-independent; branches can mix any ISPs.", null, "WAN Edges build encrypted IPsec tunnels between sites by default."],
    "x7": ["TCAM holds ACL and QoS entries; a MAC miss does not cause a drop.", null, "A Layer 2 switch does not route or ARP for frames it is switching; it floods unknown unicast.", "STP decides which ports forward, but unknown unicast is flooded out all forwarding ports in the VLAN."],
    "x8": ["The direction is reversed: routing protocols feed the RIB, and the FIB is built from it.", "The FIB mirrors every usable route in the RIB, including dynamic routes.", "Layer 2 rewrite data is stored in the CEF adjacency table, not the RIB.", null],
    "x9": [null, "Cloud hosting adds distance and WAN latency for a plant-floor control app.", "SaaS can be secured; keeping everything on site ignores the no-new-servers goal.", "A provider-region private cloud is still remote, so it does not fix the latency need."],
    "x10": ["Containers do not carry their own kernel; separate kernels are what VMs have.", null, "Containers run on a normal host OS; a type 1 hypervisor is for VMs.", "Containers share the host kernel, so they cannot run a different OS kernel such as Windows on Linux."],
    "x11": ["Keepalives detect a dead tunnel; this tunnel is up and small packets already work.", "A loopback source improves resilience but does not change packet size.", null, "Transport mode still adds an ESP header and trailer, so it adds overhead rather than removing the cause of the MTU problem."],
    "x12": ["This reverses the phases; IPsec SAs for data are negotiated in Phase 2.", "IKE does not build GRE; GRE is configured separately and optionally protected by IPsec.", "Phase 1 and Phase 2 create different SAs with different jobs.", null],
    "x13": [null, "A crypto map requires a crypto ACL, which the team wants to avoid, and has no routable interface.", "Transport mode with a crypto map still needs a crypto ACL and gives no interface for OSPF.", "Routers do not use SSL VPN clients for site-to-site routing adjacencies."],
    "x14": ["ETRs register with the map server; ITRs send requests to the map resolver.", null, "LISP does not broadcast Map-Requests to ETRs.", "A proxy ETR helps non-LISP sites reach LISP sites; it is not the mapping database."],
    "x15": ["VXLAN uses UDP, not GRE, and a 24-bit VNI rather than a 12-bit VLAN ID.", "VXLAN tunnels the original frame unchanged; it does not rewrite MACs.", null, "VXLAN provides segmentation, not encryption."],
    "x16": ["Dynamic desirable actively tries to form a trunk.", "This forces the port to be a trunk, the opposite of the requirement.", "Dynamic auto becomes a trunk if the other side is desirable or trunk.", null],
    "x17": [null, "VLAN 1 as native is the default that double tagging exploits.", "PortFast is for edge ports and has nothing to do with VLAN tagging.", "Allowing all VLANs widens exposure and does not address the native VLAN."],
    "x18": ["Both ends running PAgP is not enough when neither sends PAgP packets.", null, "PAgP and LACP are separate; one does not fall back to the other.", "Auto-auto is a valid config that simply does not bundle; ports stay up individually."],
    "x19": ["BPDU guard shuts ports that receive BPDUs; here BPDUs stopped arriving.", "Root guard blocks superior BPDUs; it does not react to missing BPDUs.", null, "PortFast skips listening and learning, which would make the loop worse."],
    "x20": ["PortFast affects edge ports, not root election.", "Port cost changes path selection toward the root, not which switch is root.", "Relying on MAC address is unpredictable and not a design practice.", null],
    "x21": [null, "RSTP still has a discarding state; ports do not start forwarding.", "RSTP does not set max age to two seconds; its speed comes from the handshake.", "BPDU guard shuts ports that get BPDUs; it does not speed designated port transitions."],
    "x22": ["Load and reliability have K values of 0 by default, so they are not used.", null, "EIGRP does not use hop count in its metric.", "Interface cost from reference bandwidth is how OSPF calculates its metric."],
    "x23": ["Summarization suppresses the component /24s on that interface.", "EIGRP summaries are per interface, not global.", null, "R2 keeps its /24 routes; only what it advertises to R1 changes."],
    "x24": ["Priority 0 makes R3 ineligible no matter its router ID.", "The higher router ID wins the tie, not the lower one.", "Priority 0 removes only that router from election; the others still elect.", null],
    "x25": [null, "summary-address summarizes external routes on an ASBR.", "ip summary-address ospf is not how OSPF summarizes inter-area routes.", "The range must be configured on the ABR for the area the routes come from."],
    "x26": ["A distribute-list filters only the local routing table; the LSA still floods.", null, "Totally stubby blocks all inter-area routes, not just one prefix.", "passive-interface stops adjacencies, cutting area 20 off entirely."],
    "x27": ["Mismatched timers prevent the neighbor relationship from forming at all.", "An MTU mismatch stalls routers in EXSTART or EXCHANGE, not 2-WAY.", null, "Duplicate router IDs cause errors and flapping, not a steady 2-WAY state."],
    "x28": ["The session cannot reach Established with a wrong AS number.", "TCP is not filtered in this scenario; the problem is the AS in the OPEN.", "No routes are exchanged because the session never comes up.", null],
    "x29": [null, "Local preference affects only outbound path choice inside the AS.", "Weight is local to one router and affects only outbound choice.", "Lowering MED on received routes affects outbound choice, not inbound traffic."],
    "x30": ["A route map deny or non-match in PBR means route normally, not drop.", null, "Traffic goes to Null0 only if a set clause says so.", "Only matching traffic is sent to the set next hop."],
    "x31": ["This PAT config shares one address and does not allow unsolicited inbound connections.", "A dynamic pool mapping appears only when the inside host sends first.", null, "Outside source NAT translates outside addresses, not the inside server."],
    "x32": ["Without overload, the first host would still get a translation entry.", "The ISP's routing is not the cause of an empty local NAT table.", "SSH access to the router is unrelated to NAT translation.", null],
    "x33": [null, "IGMPv2 introduced the Leave message, and IGMPv3 keeps leave functions.", "PIM still runs between routers; IGMP is only host to router.", "IGMP runs between hosts and their local router, not between routers."],
    "x34": ["Local SPAN needs the source and destination on the same switch.", null, "ERSPAN is needed across routed hops; a Layer 2 path can use RSPAN.", "NetFlow exports flow statistics, not a copy of packets."],
    "x35": ["SNMPv1 traps are not acknowledged.", "GetBulk is a manager request, not a notification from the agent.", null, "v2c traps are also unacknowledged."],
    "x36": ["The facility is the text before the number, here LINEPROTO.", "Sequence numbers appear at the start of the line when enabled, not here.", "The interface is named in the message text, not by this number.", null],
    "x37": [null, "More repeats test the same path from the same source.", "A default-sourced traceroute uses the exit interface, missing the LAN return path.", "Interface status does not test the return route to the users' subnet."],
    "x38": ["SSH port 830 and XML RPCs describe NETCONF.", null, "RESTCONF works with YANG models, not SNMP MIBs.", "RESTCONF can change configuration with POST, PUT, PATCH and DELETE."],
    "x39": ["The exporter defines where and how records are sent.", "The monitor applies a record to an interface and holds the cache.", null, "A sampler controls which packets are sampled, not which fields are kept."],
    "x40": ["IP Source Guard filters IP packets, not ARP messages.", "Port security limits MAC addresses; it does not validate ARP contents.", "Storm control limits traffic rates, not forged ARP.", null],
    "x41": [null, "DAI validates ARP; it does not filter spoofed IP packets.", "Root guard protects root bridge placement, unrelated to IP spoofing.", "BPDU filter stops BPDUs and has nothing to do with IP addresses."],
    "x42": ["Near the source, a standard ACL would block that subnet from every destination.", null, "The WAN interface is not in the path to the server subnet here.", "access-class on VTY lines filters management logins, not transit traffic."],
    "x43": ["Inbound interface ACLs also filter routing protocol packets to the router.", "Anything not permitted, including non-TCP protocols, hits the implicit deny.", null, "The interface ACL drops the packets before CoPP would see them."],
    "x44": ["The next method is tried only on an error, not after a reject.", "A rejected login is denied; the router does not skip authentication.", "The server did answer; there is nothing to retry.", null],
    "x45": [null, "Outbound interface ACLs do not target traffic to the router's VTYs.", "transport input ssh limits protocols, not source addresses.", "login local sets the user database but does not restrict sources."],
    "x46": ["SGACLs remove the dependence on IP subnets.", null, "VLAN IDs are not the policy key in TrustSec.", "The OUI identifies a vendor, not a security group."],
    "x47": ["Single-host allows only one device on the port.", "Multi-host authenticates one device and then lets all others in.", null, "PortFast does not change how many devices authenticate."],
    "x48": ["A port-based ACL fails when the app switches ports.", "Stateful inspection tracks connections but cannot identify applications.", "CoPP protects the router's control plane, not transit traffic.", null],
    "x49": [null, "Base64 is reversible encoding, not protection.", "Plain HTTP would expose the credentials further in transit.", "Read-only access does not remove the exposed password from history."],
    "x50": ["RADIUS encrypts only the password, not the whole packet.", null, "This reverses the two protocols.", "TACACS+ encrypts its body, and RADIUS hides the password."],
    "x51": ["The configured MAB fallback is tried after the 802.1X timeout.", "Failing 802.1X does not err-disable a port.", null, "Authorization decides the VLAN; the native VLAN is not assigned automatically."],
    "x52": ["204 is typical for successful PUT or PATCH, not a POST that creates a resource.", "200 is used for successful GETs and some updates, not resource creation.", "404 means the resource was not found, but the request succeeded.", null],
    "x53": [null, "Bad credentials return 401, and the token was issued successfully.", "A missing resource returns 404, not 403.", "An internal server error returns 500."],
    "x54": ["get never raises KeyError; indexing with brackets would.", null, "get returns None only when no default is given.", "The default is the string 'unknown', not an empty string."],
    "x55": ["r.text is a string and cannot be indexed by a key.", "The token is not a requests argument, and POST is the wrong method.", null, "auth= expects credentials, and json(r) is not a valid call."],
    "x56": ["A list can hold many leaves and multiple entries.", "This describes a list, not a container.", "Lists can hold configuration data, not just operational data.", null],
    "x57": [null, "Ansible is agentless and pushes changes over SSH.", "NETCONF is a device protocol, not an agent-based orchestration tool.", "EEM is on-box automation for Cisco devices, not a central tool."],
    "x58": ["A syslog pattern fires only when a matching message is logged.", null, "event interface reacts to interface counters, not a time of day.", "event snmp watches an OID value, not a schedule."],
    "x59": ["Automatic reboots skip human review and could cause a wider outage.", "Anomaly detection exists to catch problems static thresholds miss.", null, "Retraining first delays investigation of a possibly real issue."],
    "x60": ["json.loads parses a JSON string into Python objects, the reverse.", "json.load reads JSON from a file object.", "str() makes Python syntax such as single quotes, which is not valid JSON.", null]
  },
  levels: {
    "en1": 2, "en2": 1, "en3": 1, "en4": 2, "en5": 1, "en6": 1, "en7": 1, "en8": 2, "en9": 2, "en10": 3,
    "en11": 1, "en12": 2, "en13": 2, "en14": 1, "en15": 1, "en16": 1, "en17": 2, "en18": 2, "en19": 3, "en20": 2,
    "en21": 1, "en22": 1, "en23": 2, "en24": 2, "en25": 2, "en26": 2, "en27": 2, "en28": 3, "en29": 1, "en30": 2,
    "en31": 2, "en32": 2, "en33": 1, "en34": 2, "en35": 3, "en36": 2, "en37": 3, "en38": 1, "en39": 3, "en40": 2,
    "en41": 2, "en42": 2, "en43": 1, "en44": 3, "en45": 2, "en46": 2, "en47": 2, "en48": 2, "en49": 2, "en50": 1,
    "en51": 1, "en52": 2, "en53": 2, "en54": 2, "en55": 3, "en56": 1, "en57": 2, "en58": 2, "en59": 2, "en60": 3,
    "en61": 2, "en62": 1, "en63": 1, "en64": 2, "en65": 2, "en66": 1, "en67": 2, "en68": 2, "en69": 1, "en70": 2,
    "en71": 3, "en72": 2, "en73": 1, "en74": 2, "en75": 1, "en76": 2, "en77": 1, "en78": 1, "en79": 1, "en80": 1,
    "en81": 2, "en82": 2, "en83": 1, "en84": 1, "en85": 1, "en86": 2, "en87": 2, "en88": 3, "en89": 1, "en90": 1,
    "en91": 3, "en92": 2, "en93": 3,
    "x1": 2, "x2": 2, "x3": 2, "x4": 2, "x5": 1, "x6": 2, "x7": 1, "x8": 2, "x9": 2, "x10": 1,
    "x11": 3, "x12": 2, "x13": 3, "x14": 2, "x15": 1, "x16": 3, "x17": 1, "x18": 2, "x19": 2, "x20": 2,
    "x21": 3, "x22": 1, "x23": 3, "x24": 2, "x25": 2, "x26": 3, "x27": 2, "x28": 3, "x29": 3, "x30": 2,
    "x31": 1, "x32": 2, "x33": 1, "x34": 2, "x35": 1, "x36": 1, "x37": 2, "x38": 2, "x39": 1, "x40": 1,
    "x41": 2, "x42": 2, "x43": 3, "x44": 3, "x45": 2, "x46": 1, "x47": 3, "x48": 2, "x49": 2, "x50": 1,
    "x51": 2, "x52": 3, "x53": 2, "x54": 1, "x55": 2, "x56": 2, "x57": 1, "x58": 2, "x59": 2, "x60": 1
  }
});
