/* More rhcsa practice questions with wrong-answer notes, and a difficulty level for every question. Merged into the free bank by tools/build.js. */
CertHub.addExtra("rhcsa", {
  questions: [
    ["x1", 0, 1, "An admin wants to save the output of df -h to /root/disk.txt and still see it on the screen at the same time. Which command does this?", ["df -h > /root/disk.txt", "df -h 2> /root/disk.txt", "df -h | tee /root/disk.txt", "df -h < /root/disk.txt"], 2, "tee copies its standard input to a file and to standard output, so the output is saved and displayed. Plain > is the tempting choice, but it sends everything to the file and nothing to the terminal.", "Domain 1: Redirection, pipes and tee"],
    ["x2", 0, 1, "As a normal user, you run find / -name '*.conf' and the results are buried in 'Permission denied' messages. How do you hide only the errors and keep the results on screen?", ["find / -name '*.conf' 2>/dev/null", "find / -name '*.conf' >/dev/null", "find / -name '*.conf' &>/dev/null", "find / -name '*.conf' 2>&1 | less"], 0, "2>/dev/null sends only file descriptor 2 (standard error) to /dev/null, leaving the results on standard output. Using >/dev/null is tempting, but it throws away the results and keeps the errors.", "Domain 1: Redirection"],
    ["x3", 0, 1, "You want to see only the active settings in /etc/ssh/sshd_config, skipping comment lines that start with # and blank lines. Which command does this?", ["grep -E '^#|^$' /etc/ssh/sshd_config", "grep -v '^#|^$' /etc/ssh/sshd_config", "grep -i '#' /etc/ssh/sshd_config", "grep -v -E '^#|^$' /etc/ssh/sshd_config"], 3, "-E enables extended regex so | means OR, and -v inverts the match to print lines that are neither comments (^#) nor empty (^$). Dropping -E is tempting, but then | is not an alternation operator in basic regex.", "Domain 1: grep and regular expressions"],
    ["x4", 0, 1, "Which command searches every file under /etc, including subdirectories, for lines containing PermitRootLogin?", ["grep PermitRootLogin /etc", "grep -r PermitRootLogin /etc", "find /etc -name PermitRootLogin", "grep -v PermitRootLogin /etc/*"], 1, "grep -r reads files recursively under the given directory and prints matching lines. find -name is tempting because it searches recursively too, but it matches file names rather than contents.", "Domain 1: grep"],
    ["x5", 0, 1, "You need to list the file names inside /root/logs.tar.xz without extracting anything. Which command fits?", ["tar -xJf /root/logs.tar.xz", "tar -cJf /root/logs.tar.xz", "tar -tJf /root/logs.tar.xz", "xz -l /root/logs.tar.xz"], 2, "-t lists an archive's contents, -J handles xz compression and -f names the file. xz -l is tempting because it lists something about the file, but it only shows compression details.", "Domain 1: Archiving and compression"],
    ["x6", 0, 1, "Running ln /data/report.txt /home/alice/report.txt fails with 'Invalid cross-device link'. /data and /home are separate file systems. What should the admin do?", ["Create a symbolic link with ln -s instead", "Run the same ln command with sudo", "Add -f to force ln to create the hard link", "Hard link the /data directory instead of the file"], 0, "A hard link is another name for the same inode, and inodes belong to one file system, so it cannot cross devices; a symbolic link stores a path and can. Using sudo is tempting, but root faces the same restriction.", "Domain 1: Hard links vs symbolic links"],
    ["x7", 0, 1, "User alice is in the wheel group and does not know the root password. She needs a root login shell with root's environment. Which command fits?", ["su -", "su - alice", "sudo -u alice -i", "sudo -i"], 3, "sudo -i starts a root login shell after alice enters her own password, which wheel membership allows. su - is tempting because it gives a root login shell, but it requires root's password.", "Domain 1: Switching users"],
    ["x8", 0, 2, "A .repo file must enable signature checking using the key at file:///etc/pki/rpm-gpg/RPM-GPG-KEY-redhat-release. Which pair of lines does this?", ["gpgcheck=0 and gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-redhat-release", "gpgcheck=1 and gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-redhat-release", "enabled=1 and baseurl=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-redhat-release", "gpgcheck=1 and mirrorlist=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-redhat-release"], 1, "gpgcheck=1 turns on signature verification and gpgkey tells dnf which public key to verify with. The gpgcheck=0 option is tempting because it names the right key, but it disables the check entirely.", "Domain 2: Configuring repositories"],
    ["x9", 0, 2, "Which command shows which installed package owns the file /etc/chrony.conf?", ["rpm -ql /etc/chrony.conf", "rpm -qi chrony.conf", "rpm -qf /etc/chrony.conf", "dnf search /etc/chrony.conf"], 2, "rpm -qf queries the RPM database for the package that owns a file. rpm -ql is tempting, but it works the other way round, listing files for a given package.", "Domain 2: Querying packages"],
    ["x10", 0, 2, "You must install every package in the 'Development Tools' group. Which command does this?", ["dnf group install \"Development Tools\"", "dnf install Development Tools", "dnf group list \"Development Tools\"", "rpm -i \"Development Tools\""], 0, "dnf group install installs all the mandatory and default packages of a group. dnf group list is tempting because it names the group, but it only lists groups.", "Domain 2: Package groups"],
    ["x11", 0, 2, "The Flatpak application org.gnome.Calculator must be removed from the system. Which command does this?", ["dnf remove org.gnome.Calculator", "flatpak remote-delete org.gnome.Calculator", "rpm -e org.gnome.Calculator", "flatpak uninstall org.gnome.Calculator"], 3, "flatpak uninstall removes an installed Flatpak application by its ID. dnf remove is tempting because it removes software, but Flatpak apps are managed separately from RPMs.", "Domain 2: Flatpak applications"],
    ["x12", 0, 3, "A script takes one argument, start or stop, and must run different commands for each and print usage for anything else. Which construct fits most cleanly?", ["A for loop over \"$@\"", "A case statement on \"$1\"", "A while read loop on standard input", "An until loop that tests $?"], 1, "case matches one value against patterns such as start), stop) and *), which fits simple argument handling. A for loop is tempting because it works with arguments, but it iterates rather than branching.", "Domain 3: case statements"],
    ["x13", 0, 3, "A wrapper script must pass all of its arguments to another command, keeping each argument separate and intact even if it contains spaces. What should it use?", ["$*", "\"$*\"", "\"$@\"", "$#"], 2, "Quoted \"$@\" expands to each positional parameter as its own word, preserving spaces inside arguments. \"$*\" is tempting because it is also quoted, but it merges all arguments into one.", "Domain 3: Script inputs"],
    ["x14", 0, 3, "In one line, a script must restart httpd and print 'failed' only if the restart fails. Which line does this?", ["systemctl restart httpd || echo failed", "systemctl restart httpd && echo failed", "systemctl restart httpd ; echo failed", "systemctl restart httpd | echo failed"], 0, "|| runs the second command only when the first returns a non-zero exit status. && is tempting because it also chains commands, but it runs the second command only on success.", "Domain 3: Exit status, && and ||"],
    ["x15", 0, 3, "A script must create users user1 through user5. Which loop does this?", ["for i in seq 1 5; do useradd user$i; done", "for i in $(seq 5 1); do useradd user$i; done", "while i in $(seq 1 5); do useradd user$i; done", "for i in $(seq 1 5); do useradd user$i; done"], 3, "Command substitution $(seq 1 5) produces 1 to 5, and for runs useradd once per number. Leaving out $( ) is tempting, but then seq is just a word in the list, not a command.", "Domain 3: Loops and command substitution"],
    ["x16", 0, 4, "You need to fix a broken service configuration in a minimal single-user environment where local file systems are mounted and basic system services are started. Which kernel argument do you add in GRUB?", ["systemd.unit=emergency.target", "systemd.unit=rescue.target", "rd.break", "systemd.unit=multi-user.target"], 1, "rescue.target gives a single-user root shell with local file systems mounted and basic services running. emergency.target is tempting, but it only mounts root read-only and starts far less.", "Domain 4: Booting into different targets"],
    ["x17", 0, 4, "Several processes named stress-ng are hogging the CPU. Which command sends all of them the default termination signal by name?", ["pgrep stress-ng", "kill stress-ng", "pkill stress-ng", "renice -n 19 stress-ng"], 2, "pkill matches processes by name and sends SIGTERM (15) by default. pgrep is tempting because it uses the same matching, but it only prints the PIDs.", "Domain 4: Killing processes"],
    ["x18", 0, 4, "A batch job /root/batch.sh should start with a nice value of 10 so it yields CPU to other work. Which command starts it that way?", ["nice -n 10 /root/batch.sh", "renice -n 10 /root/batch.sh", "nice -n -10 /root/batch.sh", "nohup -n 10 /root/batch.sh"], 0, "nice -n 10 launches the command with a nice value of 10, lowering its scheduling priority. renice is tempting because it sets the same value, but it only works on existing processes.", "Domain 4: nice and renice"],
    ["x19", 0, 4, "Which command shows the tuning profile that tuned is currently applying?", ["tuned-adm recommend", "tuned-adm list", "systemctl get-default", "tuned-adm active"], 3, "tuned-adm active reports the currently active tuning profile. tuned-adm recommend is tempting, but it only shows the suggested profile, not the one applied.", "Domain 4: Tuning profiles"],
    ["x20", 0, 4, "The contents of /var/www must be kept in sync with server2:/var/www over SSH, copying only changed files and preserving permissions and times. Which command fits?", ["scp /var/www server2:/var/www", "rsync -av /var/www/ server2:/var/www/", "cp -a /var/www server2:/var/www", "sftp -r /var/www server2"], 1, "rsync over SSH compares source and destination and transfers only differences, and -a preserves permissions, ownership and times. scp is tempting because it copies over SSH, but it copies everything each time.", "Domain 4: Secure file transfer"],
    ["x21", 0, 4, "You want to watch new log messages from the httpd service as they arrive. Which command does this?", ["journalctl -u httpd -b -1", "journalctl -u httpd -r", "journalctl -u httpd -f", "tail -f /var/log/httpd.service"], 2, "-f follows the journal, printing new entries for the httpd unit as they are logged. -r is tempting because it puts the newest entries first, but it exits after printing them.", "Domain 4: Reading system logs"],
    ["x22", 0, 4, "After booting with rd.break to reset the root password, you find /sysroot mounted read-only. What must you do before changing the password?", ["Run mount -o remount,rw /sysroot, then chroot /sysroot", "Run chroot /sysroot and then run passwd right away", "Run touch /.autorelabel first and then reboot", "Run setenforce 0 and then run passwd in the initramfs"], 0, "The real root is mounted read-only at /sysroot, so it must be remounted read-write and then entered with chroot before passwd can update /etc/shadow. Chrooting straight away is tempting, but writes fail on a read-only file system.", "Domain 4: Interrupting the boot process"],
    ["x23", 0, 5, "Volume group vgdata is full. The new disk /dev/vdc has already been initialized with pvcreate. Which command adds it to vgdata?", ["vgcreate vgdata /dev/vdc", "lvextend vgdata /dev/vdc", "pvcreate vgdata /dev/vdc", "vgextend vgdata /dev/vdc"], 3, "vgextend adds physical volumes to an existing volume group, increasing its free extents. vgcreate is tempting because it also takes a VG name and a PV, but it only creates new groups.", "Domain 5: Volume groups"],
    ["x24", 0, 5, "Which command creates a 2 GiB logical volume named lvdb in the volume group vgdata?", ["lvcreate -l 2G -n lvdb vgdata", "lvcreate -L 2G -n lvdb vgdata", "lvcreate -n 2G -L lvdb vgdata", "lvcreate -L 2G vgdata lvdb"], 1, "Uppercase -L sets the size with units and -n sets the LV name, followed by the volume group. Lowercase -l is tempting because it also sets size, but it counts extents.", "Domain 5: Logical volumes"],
    ["x25", 0, 5, "You created partition 1 on /dev/vdb with parted and want to flag it for use with LVM. Which command does this?", ["parted /dev/vdb mkpart lvm", "pvcreate --type lvm /dev/vdb1", "parted /dev/vdb set 1 lvm on", "mkfs.lvm /dev/vdb1"], 2, "parted's set command turns flags on for a given partition number, and the lvm flag marks it for LVM. pvcreate is tempting because it is the next LVM step, but it does not set the partition type.", "Domain 5: Partitions and partition types"],
    ["x26", 0, 5, "A server needs 1 GiB more swap without repartitioning. vgdata has free space. Which sequence adds persistent swap?", ["lvcreate -L 1G -n lvswap vgdata; mkswap /dev/vgdata/lvswap; add it to /etc/fstab; swapon -a", "lvcreate -L 1G -n lvswap vgdata; mkfs.xfs /dev/vgdata/lvswap; add it to /etc/fstab; swapon -a", "lvcreate -L 1G -n lvswap vgdata; mkswap /dev/vgdata/lvswap; swapon /dev/vgdata/lvswap only", "lvextend -L +1G /dev/vgdata/lvroot; mkswap /dev/vgdata/lvroot; swapon -a now"], 0, "Swap needs its own space, a swap signature from mkswap, an fstab entry for persistence and swapon to activate it. Activating it with swapon alone is tempting, but it does not survive a reboot.", "Domain 5: Swap"],
    ["x27", 0, 5, "Which command lists each LVM physical volume with its volume group and free space?", ["lvs", "vgs", "blkid", "pvs"], 3, "pvs shows a line per physical volume with its VG, size and free space. vgs is tempting because it shows free space, but it reports it per volume group.", "Domain 5: Physical volumes"],
    ["x28", 0, 5, "An ext4 file system has the label DOCS. Which /etc/fstab line mounts it on /docs at boot?", ["DOCS /docs ext4 defaults 0 0", "LABEL=DOCS /docs ext4 defaults 0 0", "/dev/DOCS /docs ext4 defaults 0 0", "LABEL=DOCS ext4 /docs defaults 0 0"], 1, "fstab fields are device, mount point, type, options, dump and fsck order, and a label is given as LABEL=name. A bare DOCS is tempting, but fstab reads it as a device path.", "Domain 5: Mounting by UUID or label"],
    ["x29", 0, 5, "Why should /etc/fstab refer to file systems by UUID rather than by names such as /dev/vdb1?", ["UUIDs make the file system mount faster at boot", "fstab no longer accepts /dev device names at all", "Device names can change between boots, while a UUID stays with the file system", "A UUID enables disk encryption automatically"], 2, "Kernel device names depend on detection order and can change when disks are added, but a file system's UUID does not change. The claim that device names are no longer accepted is tempting, but they still work; they are simply fragile.", "Domain 5: Persistent mounts"],
    ["x30", 0, 6, "mkfs.xfs /dev/vdb1 refuses to run because it finds an existing file system. You are sure the old data can be destroyed. Which command proceeds?", ["mkfs.xfs -f /dev/vdb1", "xfs_repair /dev/vdb1", "mkfs.xfs -L force /dev/vdb1", "xfs_growfs /dev/vdb1"], 0, "-f forces mkfs.xfs to overwrite an existing file system signature. xfs_repair is tempting because it is an XFS tool used on the device, but it repairs rather than creates.", "Domain 6: Creating file systems"],
    ["x31", 0, 6, "You ran lvextend -L +1G /dev/vgdata/lvhome without -r. The LV holds ext4 and df still shows the old size. What fixes this without data loss?", ["xfs_growfs /home", "mkfs.ext4 /dev/vgdata/lvhome", "mount -o remount /home", "resize2fs /dev/vgdata/lvhome"], 3, "resize2fs grows an ext4 file system to fill its enlarged device, and it can do so while mounted. xfs_growfs is tempting because it does the same job, but only for XFS.", "Domain 6: Extending file systems"],
    ["x32", 0, 6, "A 10 GiB logical volume holding ext4 must be reduced to 6 GiB, with its 3 GiB of data kept. Which approach is correct?", ["Run lvreduce -L 6G while mounted, then run resize2fs", "Unmount it, then run lvreduce -r -L 6G so the file system shrinks with the LV", "Run xfs_growfs with a smaller target size", "It cannot be done; back up, recreate the LV and restore"], 1, "ext4 can shrink when unmounted, and lvreduce -r resizes the file system first and then the LV. The back-up-and-recreate answer is tempting because it is the rule for XFS, but it does not apply to ext4.", "Domain 6: Shrinking ext4 vs XFS"],
    ["x33", 0, 6, "In the fstab line UUID=4f1c...e2 /data ext4 defaults 0 2, what does the final 2 mean?", ["The file system is backed up by dump twice a day", "The system retries the mount twice if it fails", "fsck checks this file system after the root file system", "This is the second file system mounted at boot"], 2, "The sixth fstab field is the fsck pass number: 1 for root, 2 for other file systems checked afterwards, 0 to skip. The dump answer is tempting because the fifth field is dump, but the 2 is in the sixth field.", "Domain 6: fstab fields"],
    ["x34", 0, 6, "The NFS export nfs1:/exports/pub must mount on demand at exactly /mnt/pub using autofs. Which line belongs in a file under /etc/auto.master.d/?", ["/- /etc/auto.direct", "/mnt/pub /etc/auto.direct", "* /etc/auto.direct", "/etc/auto.direct /-"], 0, "/- declares a direct map, whose entries give full mount paths such as /mnt/pub. Using /mnt/pub as the master entry is tempting, but it creates an indirect map with keys underneath it.", "Domain 6: Configuring autofs"],
    ["x35", 0, 6, "A user cannot open /srv/app/data/report.txt. Which command shows the owner and permissions of every directory along that path in one view?", ["ls -l /srv/app/data/report.txt", "stat /srv/app/data/report.txt", "getent passwd", "namei -l /srv/app/data/report.txt"], 3, "namei -l walks the path and prints the owner, group and mode of each component, revealing a directory missing execute permission. ls -l is tempting, but it only shows the final file.", "Domain 6: Diagnosing permission problems"],
    ["x36", 0, 7, "A job must run as root every 15 minutes from a file in /etc/cron.d/. Which line is correct for that file?", ["*/15 * * * * /usr/local/bin/poll.sh", "*/15 * * * * root /usr/local/bin/poll.sh", "15 * * * * root /usr/local/bin/poll.sh", "*/15 root * * * * /usr/local/bin/poll.sh"], 1, "System cron files such as /etc/cron.d entries have five time fields, then the user, then the command, and */15 means every 15 minutes. Leaving out the user is tempting because that is the crontab -e format.", "Domain 7: Scheduling with cron"],
    ["x37", 0, 7, "A cleanup service should run 10 minutes after every boot using a systemd timer. Which setting belongs in the [Timer] section?", ["OnCalendar=10min", "After=boot 10min", "OnBootSec=10min", "ExecStartPre=sleep 600"], 2, "OnBootSec sets a monotonic timer relative to when the machine booted. OnCalendar is tempting because it is the usual timer setting, but it is for wall-clock schedules.", "Domain 7: systemd timer units"],
    ["x38", 0, 7, "The cups service must not be startable at all, either manually or as a dependency of another unit. Which command does this?", ["systemctl mask cups", "systemctl disable cups", "systemctl stop cups", "systemctl disable --now cups"], 0, "mask links the unit to /dev/null so it cannot be started by any means until unmasked. disable is tempting, but it only affects starting at boot.", "Domain 7: Managing services"],
    ["x39", 0, 7, "After configuring chronyd, which command shows the time sources it is using and whether it is synchronized with them?", ["chronyc makestep", "hwclock --systohc", "timedatectl set-timezone UTC", "chronyc sources"], 3, "chronyc sources lists the configured servers, their state and which one is selected, with * marking the current sync source. chronyc makestep is tempting because it is a chronyc command, but it adjusts the clock rather than reporting.", "Domain 7: Time service clients"],
    ["x40", 0, 7, "Several kernels are installed. The kernel at /boot/vmlinuz-<version> must be the default for future boots. Which command does this?", ["grubby --info=ALL", "grubby --set-default /boot/vmlinuz-<version>", "grub2-mkconfig -o /boot/grub2/grub.cfg", "grubby --remove-args=default"], 1, "grubby --set-default sets the given kernel as the default boot entry. grub2-mkconfig is tempting because it is a GRUB tool, but it rebuilds the config without picking the kernel.", "Domain 7: Choosing the default kernel"],
    ["x41", 0, 7, "An RPM file at /root/tool.rpm must be installed, with its dependencies pulled automatically from the configured repositories. Which command fits?", ["rpm -ivh /root/tool.rpm", "dnf group install /root/tool.rpm", "dnf install /root/tool.rpm", "flatpak install /root/tool.rpm"], 2, "dnf install accepts a local RPM path and resolves its dependencies from enabled repositories. rpm -ivh is tempting because it installs RPM files, but it does not resolve dependencies.", "Domain 7: Installing packages from the local file system"],
    ["x42", 0, 8, "Connection ens3 must use the static IPv6 address 2001:db8::20/64 only, with no SLAAC or DHCPv6. Which command makes this persistent?", ["nmcli con mod ens3 ipv6.addresses 2001:db8::20/64 ipv6.method manual", "nmcli con mod ens3 ipv4.addresses 2001:db8::20/64 ipv4.method manual", "nmcli con mod ens3 ipv6.addresses 2001:db8::20/64 ipv6.method auto", "ip -6 addr add 2001:db8::20/64 dev ens3"], 0, "Setting ipv6.addresses with ipv6.method manual stores a static-only IPv6 configuration in the profile. Using method auto is tempting because the address is still added, but autoconfiguration stays enabled.", "Domain 8: Static IPv6 with nmcli"],
    ["x43", 0, 8, "The connection profile backup-net works when brought up by hand but is not active after a reboot. Which command fixes this?", ["nmcli con up backup-net", "systemctl enable backup-net", "nmcli dev reapply backup-net", "nmcli con mod backup-net connection.autoconnect yes"], 3, "connection.autoconnect yes tells NetworkManager to activate the profile automatically when its device is available, including at boot. nmcli con up is tempting, but it only activates it for the current boot.", "Domain 8: Autoconnect"],
    ["x44", 0, 8, "The name db1 must resolve to 10.0.0.40 locally without DNS, and you want to verify it the way applications will. What should you do?", ["Add '10.0.0.40 db1' to /etc/hosts, then run dig db1", "Add '10.0.0.40 db1' to /etc/hosts, then run getent hosts db1", "Add 'nameserver 10.0.0.40' to /etc/resolv.conf", "Run hostnamectl set-hostname db1 on this server"], 1, "/etc/hosts provides local name mappings, and getent hosts uses the same resolver order as applications. The dig option is tempting because dig is a common lookup tool, but it bypasses /etc/hosts.", "Domain 8: Hostname resolution"],
    ["x45", 0, 8, "TCP port 8443 must be allowed through firewalld both now and after reboots. Which commands do this?", ["firewall-cmd --add-port=8443/tcp", "firewall-cmd --permanent --add-service=8443", "firewall-cmd --permanent --add-port=8443/tcp; firewall-cmd --reload", "firewall-cmd --permanent --add-port=8443/tcp only"], 2, "--permanent saves the rule and --reload loads the permanent configuration into the running firewall. Adding it with --permanent alone is tempting, but it does not take effect immediately.", "Domain 8: Restricting network access"],
    ["x46", 0, 8, "Which command shows the routing table, including the default gateway?", ["ip route", "ip addr", "ip link", "hostnamectl"], 0, "ip route lists the routing table, and the 'default via' line shows the gateway. ip addr is tempting because it is the usual first network check, but it only shows addresses.", "Domain 8: Checking routes"],
    ["x47", 0, 9, "User bob's password must expire after at most 90 days, with a warning 7 days before. Which command sets this?", ["chage -m 90 -W 7 bob", "chage -M 90 -E 7 bob", "passwd -x 7 -w 90 bob", "chage -M 90 -W 7 bob"], 3, "chage -M sets the maximum password age and -W sets the warning days. Lowercase -m is tempting because it looks the same, but it sets the minimum age.", "Domain 9: Password aging"],
    ["x48", 0, 9, "A contractor's account must be blocked from all logins, including SSH key-based logins, without deleting it. Which command does this most reliably?", ["passwd -l contractor", "chage -E 0 contractor", "usermod -L contractor", "usermod -s /bin/bash contractor"], 1, "chage -E 0 expires the account, and account expiry is enforced for every login method. usermod -L is tempting because it is called locking, but it only disables the password.", "Domain 9: Locking accounts"],
    ["x49", 0, 9, "Every new user created with useradd must receive a default .vimrc in their home directory. Where should the file be placed?", ["/etc/default/useradd", "/etc/login.defs", "/etc/skel", "/etc/profile.d"], 2, "useradd copies the contents of /etc/skel into each new home directory. /etc/default/useradd is tempting because it controls new-user defaults, but it contains settings, not files.", "Domain 9: Account defaults"],
    ["x50", 0, 9, "User bob must be removed from the group developers while keeping his other group memberships. Which command does this?", ["gpasswd -d bob developers", "usermod -G developers bob", "groupdel developers", "userdel -r bob"], 0, "gpasswd -d removes one user from one group and leaves other memberships alone. usermod -G is tempting because it manages groups, but it sets the whole list rather than removing one.", "Domain 9: Group membership"],
    ["x51", 0, 9, "User backup must be able to run only /usr/bin/rsync as root through sudo, without a password. Which line belongs in a file under /etc/sudoers.d/?", ["backup ALL=(ALL) ALL", "%backup ALL=(root) NOPASSWD: /usr/bin/rsync", "backup ALL=NOPASSWD (root): /usr/bin/rsync", "backup ALL=(root) NOPASSWD: /usr/bin/rsync"], 3, "The format is user, hosts=(run-as) optional tags: command, so this grants only rsync as root without a password. The % version is tempting because it looks the same, but % makes it a group rule.", "Domain 9: Superuser access"],
    ["x52", 0, 10, "An admin ran firewall-cmd --permanent --add-service=https, but firewall-cmd --list-services does not show https. Why?", ["https is not one of firewalld's predefined services", "Permanent changes do not apply to the running firewall until a reload", "--list-services only shows the permanent configuration", "firewalld must be disabled and enabled with systemctl"], 1, "--permanent writes only to the saved configuration, while --list-services without --permanent shows runtime, so a reload is needed. The claim that list-services shows the permanent set is tempting, but it is the reverse.", "Domain 10: firewalld runtime vs permanent"],
    ["x53", 0, 10, "You tested several firewalld rules in runtime mode and they work. Which command saves all current runtime rules as the permanent configuration?", ["firewall-cmd --reload", "systemctl restart firewalld", "firewall-cmd --runtime-to-permanent", "firewall-cmd --permanent --list-all"], 2, "--runtime-to-permanent copies the running configuration into the permanent one. --reload is tempting because it is used with permanent changes, but here it would throw away the runtime-only rules.", "Domain 10: firewalld configuration"],
    ["x54", 0, 10, "A user's umask is 077. What permissions does a new regular file they create with touch receive?", ["rw------- (600)", "rwx------ (700)", "rw-r--r-- (644)", "---rwxrwx (077)"], 0, "New files start from 666 and the umask 077 removes all group and other bits, leaving 600. 700 is tempting, but that is the result for directories, which start from 777.", "Domain 10: umask"],
    ["x55", 0, 10, "An admin created index.html in /root and moved it with mv to /var/www/html. httpd returns 403, and ls -Z shows admin_home_t on the file. What fixes it properly?", ["chmod 777 /var/www/html/index.html", "setenforce 0", "semanage fcontext -a -t admin_home_t /var/www/html/index.html", "restorecon -v /var/www/html/index.html"], 3, "mv keeps the original SELinux context, and restorecon resets it to the policy default for /var/www/html (httpd_sys_content_t). setenforce 0 is tempting because the page then loads, but it disables SELinux protection instead of fixing the label.", "Domain 10: Restoring file contexts"],
    ["x56", 0, 10, "You want a plain-language explanation of recent SELinux denials along with suggested fixes. Which command provides this when setroubleshoot is installed?", ["ausearch -m AVC -ts recent", "sealert -a /var/log/audit/audit.log", "getenforce", "journalctl -k"], 1, "sealert analyzes audit log denials and explains them with suggested commands. ausearch is tempting because it finds the same denials, but it shows only raw records.", "Domain 10: Diagnosing SELinux denials"],
    ["x57", 0, 10, "You need to find SELinux booleans related to httpd, along with a short description of what each one does. Which command fits?", ["getsebool httpd", "setsebool -P httpd on", "semanage boolean -l | grep httpd", "ls -Z /etc/selinux | grep httpd"], 2, "semanage boolean -l lists every boolean with its current and default state and a description. getsebool is tempting because it reads booleans, but it needs exact names and shows no descriptions.", "Domain 10: SELinux booleans"],
    ["x58", 0, 10, "An admin ran setsebool httpd_enable_homedirs on. It worked, but after a reboot the boolean is off again. Why?", ["Without -P the change applies only to the running system", "Booleans reset whenever SELinux is in enforcing mode", "The boolean also needs restorecon on /home", "setsebool only works while SELinux is permissive"], 0, "setsebool -P writes the value to the policy so it survives reboots; without -P it lasts only until reboot. The restorecon option is tempting because home directories are involved, but labels and booleans are separate.", "Domain 10: SELinux booleans"],
    ["x59", 0, 10, "sshd must listen on TCP 2022 with SELinux enforcing. Which command handles the SELinux part?", ["semanage port -a -t http_port_t -p tcp 2022", "setsebool -P ssh_port 2022", "restorecon -Rv /etc/ssh", "semanage port -a -t ssh_port_t -p tcp 2022"], 3, "sshd may only bind to ports labeled ssh_port_t, so the new port must be added with that type. The http_port_t option is tempting because that is the classic example command, but it labels the port for httpd.", "Domain 10: SELinux port labels"],
    ["x60", 0, 10, "Direct root logins over SSH must be blocked entirely, including key-based ones, while other users still log in normally. Which sshd_config change fits?", ["PermitRootLogin prohibit-password, then reload sshd", "PermitRootLogin no, then reload sshd", "PasswordAuthentication no, then reload sshd", "AllowRoot no, then reload sshd"], 1, "PermitRootLogin no refuses every SSH login as root regardless of method. prohibit-password is tempting because it blocks root passwords, but root keys still work.", "Domain 10: Key-based SSH and sshd_config"]
  ],
  whys: {
    "x1": ["This writes the output to the file only, so nothing appears on the screen.", "2> redirects only standard error; the normal output goes to the screen and the file stays empty.", null, "< feeds the file into the command as input; it does not save any output."],
    "x2": [null, "This discards standard output, which is the list of results, while the errors stay on screen.", "&> discards both output and errors, so the results disappear too.", "2>&1 merges the errors into the output stream, so they still appear alongside the results."],
    "x3": ["Without -v this prints only the comment and blank lines, the exact opposite of what is wanted.", "Without -E, basic regex treats | literally, so the pattern matches almost nothing and nearly every line prints.", "This prints lines containing # anywhere, which are mostly the comments you want to skip.", null],
    "x4": ["Without -r, grep treats /etc as a directory it cannot read as a file and searches nothing.", null, "find -name matches file names, not the text inside files.", "-v prints lines that do not match, and the glob does not descend into subdirectories."],
    "x5": ["-x extracts the archive, which the task says not to do.", "-c creates a new archive and would overwrite the existing file.", null, "xz -l reports compression statistics for the .xz file, not the names of files in the tar archive."],
    "x6": [null, "The error is not about permissions; hard links cannot cross file systems no matter who runs ln.", "-f only removes an existing destination file; it cannot make a hard link span file systems.", "Hard links to directories are not allowed, and it would still cross file systems."],
    "x7": ["su - asks for the root password, which alice does not know.", "This starts a login shell as alice herself, not as root.", "-u alice runs the shell as alice rather than root.", null],
    "x8": ["gpgcheck=0 turns signature checking off, so the key is never used.", null, "baseurl is where packages are downloaded from; pointing it at a key file breaks the repository.", "mirrorlist names a list of mirror URLs, not the signing key."],
    "x9": ["-ql lists the files inside a named package; it expects a package name, not a file path.", "-qi shows information about a package named chrony.conf, which does not exist.", null, "dnf search matches package names and summaries, not file paths."],
    "x10": [null, "Without the group subcommand and quotes, dnf looks for two packages named Development and Tools.", "group list only displays group information; it installs nothing.", "rpm -i installs a single package file and knows nothing about dnf groups."],
    "x11": ["dnf manages RPM packages; Flatpak applications are not in the RPM database.", "remote-delete removes a configured remote repository, not an installed application.", "rpm -e erases RPM packages and cannot see Flatpak applications.", null],
    "x12": ["A for loop repeats commands for each item; it does not choose between branches by value.", null, "while read processes input lines, not the script's command-line argument.", "until repeats while a command fails; it is not a way to match an argument's value."],
    "x13": ["Unquoted $* splits arguments again on spaces, breaking arguments that contain them.", "Quoted $* joins all arguments into one single string instead of keeping them separate.", null, "$# is the number of arguments, not the arguments themselves."],
    "x14": [null, "&& runs the echo only when the restart succeeds, the opposite of what is wanted.", "; runs the echo every time, whether or not the restart failed.", "A pipe connects output to input and runs echo regardless of the exit status."],
    "x15": ["Without $( ), the loop iterates over the words seq, 1 and 5, creating userseq, user1 and user5.", "seq counts up by 1 by default, so seq 5 1 (start above end) outputs nothing and the loop never runs.", "while expects a command to test, not the in-list syntax of for, so this is a syntax error.", null],
    "x16": ["emergency.target mounts only the root file system, read-only, and starts almost nothing.", null, "rd.break stops in the initramfs before the real root is even switched to, far more minimal than needed.", "multi-user.target is a normal full boot, not a single-user maintenance environment."],
    "x17": ["pgrep only lists the matching process IDs; it does not signal them.", "kill expects process IDs or job numbers, not a process name.", null, "renice changes priority rather than ending processes, and it expects IDs, not a name."],
    "x18": [null, "renice changes an already running process by PID; it cannot start a script.", "A negative value raises the priority, the opposite of what is wanted, and needs root.", "nohup keeps a command running after logout; it does not set a nice value."],
    "x19": ["recommend shows the profile tuned suggests, which may differ from the one in use.", "list shows all available profiles, not just the active one.", "get-default shows the default systemd target, which is unrelated to tuned profiles.", null],
    "x20": ["Without -r, scp will not copy a directory, and scp always copies every file rather than only changes.", null, "cp works locally only; it would create a directory literally named server2:/var/www.", "sftp is interactive and does not take a source directory and host like this, nor sync only changes."],
    "x21": ["-b -1 shows the previous boot's messages, not new messages as they arrive.", "-r shows existing entries newest first, but it does not keep following new ones.", null, "There is no such log file; unit messages go to the journal."],
    "x22": [null, "The root file system is still read-only, so passwd cannot write the new hash to /etc/shadow.", "Relabeling is done after the password change; rebooting now leaves the password unchanged.", "In the initramfs, passwd would change the initramfs copy, not the real system's /etc/shadow."],
    "x23": ["vgcreate makes a new volume group; it fails because vgdata already exists.", "lvextend grows a logical volume, not a volume group, and needs an LV path.", "pvcreate initializes physical volumes; it does not add them to a volume group.", null],
    "x24": ["Lowercase -l expects a number of extents, not a size with a unit.", null, "The option values are swapped: -n takes the name and -L takes the size.", "Without -n, the extra word lvdb is read as a physical volume path, not a name."],
    "x25": ["mkpart creates a new partition; it does not flag an existing one, and here lvm would be read as a name.", "pvcreate has no option to set a partition flag; it initializes an LVM physical volume.", null, "There is no mkfs.lvm; LVM is not a file system."],
    "x26": [null, "A volume formatted with XFS is not swap space, so swapon rejects it.", "This activates the swap now, but without an fstab entry it is gone after reboot.", "mkswap on the root LV would destroy the root file system."],
    "x27": ["lvs lists logical volumes, not physical volumes.", "vgs summarizes volume groups, not each physical volume within them.", "blkid shows UUIDs and types of block devices, not LVM free space.", null],
    "x28": ["A bare word is treated as a device path, so the label must be written as LABEL=DOCS.", null, "Labels do not create a /dev/DOCS device node, so this path does not exist.", "The fields are in the wrong order; the mount point must come before the file system type."],
    "x29": ["Mount speed is the same; the benefit is a stable identifier, not speed.", "fstab still accepts device paths; they are just less reliable when disks change.", null, "A UUID is only an identifier and has nothing to do with encryption."],
    "x30": [null, "xfs_repair checks and repairs an XFS file system; it does not create a new one.", "-L sets the label, so this just labels the file system 'force' and still refuses.", "xfs_growfs grows a mounted XFS file system; it does not create one."],
    "x31": ["xfs_growfs only works on XFS, and this file system is ext4.", "This creates a new empty file system and destroys the data.", "Remounting does not grow the file system to fill the larger LV.", null],
    "x32": ["Shrinking the LV before the file system cuts off part of the file system and can destroy data.", null, "xfs_growfs is for XFS and can only grow, never shrink.", "That is true for XFS, but ext4 can be shrunk while unmounted."],
    "x33": ["The fifth field, not the sixth, is the dump flag, and it is 0 here.", "fstab has no retry-count field.", null, "The sixth field is fsck order, not the mount order."],
    "x34": [null, "This makes /mnt/pub an indirect mount point, so the share would appear under /mnt/pub/<key>.", "* is a wildcard key used inside map files, not a mount point in the master map.", "The fields are reversed; the mount point (/-) comes first and the map file second."],
    "x35": ["ls -l shows only the file itself, not each parent directory.", "stat gives detailed metadata for the file alone, not its parent directories.", "getent passwd lists user accounts and says nothing about file permissions.", null],
    "x36": ["Files in /etc/cron.d need a user field; this format is only right for crontab -e.", null, "15 in the minute field runs once an hour at 15 minutes past, not every 15 minutes.", "The user field must come after all five time fields, not in the middle."],
    "x37": ["OnCalendar expects a calendar event such as daily or a date pattern, not a delay after boot.", "After= orders units in the [Unit] section and does not schedule anything.", null, "This belongs to a service, delays that service each time it runs, and does not trigger it at boot."],
    "x38": [null, "disable only stops automatic start at boot; it can still be started manually or as a dependency.", "stop ends the running service now but does not prevent it starting again.", "This stops it and removes boot start, but it can still be started manually."],
    "x39": ["makestep forces an immediate clock correction; it does not report the sources.", "This copies system time to the hardware clock and does not check NTP sources.", "This changes the time zone and has nothing to do with checking synchronization.", null],
    "x40": ["--info=ALL displays boot entries; it does not change the default.", null, "This regenerates the GRUB configuration but does not choose a specific kernel as default.", "--remove-args removes kernel command-line arguments; default is not an argument to remove."],
    "x41": ["rpm installs the file but does not fetch dependencies, so it fails if any are missing.", "group install expects a group name, not a package file.", null, "Flatpak installs Flatpak applications and bundles, not RPM files."],
    "x42": [null, "IPv6 addresses go in the ipv6 settings; ipv4.addresses rejects an IPv6 address.", "ipv6.method auto keeps automatic configuration on, which the task forbids.", "ip addr changes only the running state and is lost when the connection restarts or the system reboots."],
    "x43": ["This activates the connection now only; it will still be down after the next reboot.", "Connection profiles are not systemd units, so there is nothing to enable.", "dev reapply takes a device name and reapplies settings now; it does not set autoconnect.", null],
    "x44": ["dig queries DNS servers directly and ignores /etc/hosts, so it will not show the entry.", null, "This makes 10.0.0.40 a DNS server; it does not map the name db1 to that address.", "This renames the local machine; it does not make db1 resolve to 10.0.0.40."],
    "x45": ["Without --permanent, the rule is runtime only and disappears after a reload or reboot.", "--add-service expects a service name, not a port number.", null, "Without a reload, the permanent rule is saved but not active until the next reload or reboot."],
    "x46": [null, "ip addr shows interface addresses, not routes or the gateway.", "ip link shows link-layer state of interfaces, not routes.", "hostnamectl shows and sets the hostname, not network routes."],
    "x47": ["Lowercase -m sets the minimum days between password changes, not the maximum age.", "-E sets an account expiration date, not a warning period.", "The values are swapped: this sets a 7-day maximum and a 90-day warning.", null],
    "x48": ["This locks only the password; key-based SSH logins can still succeed.", null, "Like passwd -l, this locks the password hash only and may not stop key logins.", "This sets a normal login shell, which enables logins rather than blocking them."],
    "x49": ["This file holds useradd defaults such as the shell, not files to copy into homes.", "login.defs sets account policy such as UID ranges and password aging.", null, "Scripts here run at login for everyone; files placed here are not copied into home directories."],
    "x50": [null, "-G without -a replaces all supplementary groups, making developers his only one.", "This deletes the whole group for every member.", "This deletes bob's account and home directory entirely."],
    "x51": ["This allows every command as any user and still asks for a password.", "% refers to a group named backup, not the user backup.", "The tag and run-as list are in the wrong order, so visudo rejects the syntax.", null],
    "x52": ["https is a predefined firewalld service, so the name is valid.", null, "Without --permanent, --list-services shows the runtime configuration, which is why https is missing.", "Disabling and enabling affects boot start; a reload is what applies the permanent rules."],
    "x53": ["A reload replaces runtime with the permanent config, discarding the tested rules.", "Restarting loads the permanent configuration and loses the runtime-only rules.", null, "This only displays the permanent configuration; it saves nothing."],
    "x54": [null, "700 is what a new directory gets; files start from 666, so execute is never set by default.", "644 results from a umask of 022, not 077.", "The umask removes bits; it is not the resulting permission itself."],
    "x55": ["The block comes from the SELinux type, not standard permissions, so chmod does not help.", "This turns off enforcement system-wide instead of fixing the file's context.", "This makes the wrong type the default rule, so httpd stays blocked even after relabeling.", null],
    "x56": ["ausearch shows the raw AVC records without plain-language analysis or suggested fixes.", null, "getenforce only reports the current SELinux mode.", "The kernel log does not provide analyzed SELinux denials with fix suggestions."],
    "x57": ["getsebool expects an exact boolean name; httpd alone is not a boolean.", "setsebool changes a boolean value; it does not list or describe booleans.", null, "This lists file contexts in a directory, not SELinux booleans."],
    "x58": [null, "Enforcing mode does not reset booleans; persistent values survive reboot.", "restorecon fixes file labels and has no effect on boolean values.", "setsebool works in both enforcing and permissive modes."],
    "x59": ["http_port_t is the label for web server ports; sshd is only allowed to bind ssh_port_t.", "There is no such boolean; ports are labeled with semanage port, not booleans.", "This fixes file labels in /etc/ssh but does not label the new port.", null],
    "x60": ["This still allows root to log in with a key; it only blocks password logins for root.", null, "This affects password logins for every user and still allows root key logins.", "AllowRoot is not a valid sshd_config option, so sshd rejects it."]
  },
  levels: {"rh1": 2, "rh2": 2, "rh3": 1, "rh4": 2, "rh5": 1, "rh6": 1, "rh7": 1, "rh8": 1, "rh9": 2, "rh10": 1, "rh11": 1, "rh12": 1, "rh13": 2, "rh14": 1, "rh15": 2, "rh16": 1, "rh17": 3, "rh18": 1, "rh19": 2, "rh20": 3, "rh21": 2, "rh22": 2, "rh23": 1, "rh24": 1, "rh25": 3, "rh26": 2, "rh27": 1, "rh28": 2, "rh29": 3, "rh30": 3, "rh31": 2, "rh32": 1, "rh33": 1, "rh34": 3, "rh35": 1, "rh36": 1, "rh37": 2, "rh38": 2, "rh39": 1, "rh40": 1, "rh41": 3, "rh42": 2, "rh43": 3, "rh44": 1, "rh45": 1, "rh46": 1, "rh47": 3, "rh48": 2, "rh49": 3, "rh50": 3, "rh51": 3, "rh52": 3, "rh53": 2, "rh54": 1, "rh55": 1, "rh56": 3, "rh57": 2, "rh58": 1, "rh59": 2, "rh60": 2, "rh61": 2, "rh62": 3, "rh63": 2, "rh64": 2, "rh65": 2, "rh66": 1, "rh67": 1, "rh68": 3, "rh69": 2, "rh70": 1, "rh71": 2, "rh72": 2, "rh73": 2, "rh74": 2, "rh75": 1, "rh76": 2, "rh77": 1, "rh78": 3, "rh79": 1, "rh80": 3, "rh81": 3, "rh82": 2, "rh83": 3, "rh84": 2, "rh85": 3, "rh86": 1, "rh87": 3, "rh88": 1, "rh89": 3, "rh90": 1, "x1": 1, "x2": 2, "x3": 2, "x4": 1, "x5": 1, "x6": 2, "x7": 2, "x8": 2, "x9": 1, "x10": 1, "x11": 1, "x12": 1, "x13": 3, "x14": 2, "x15": 2, "x16": 2, "x17": 1, "x18": 2, "x19": 1, "x20": 2, "x21": 2, "x22": 3, "x23": 1, "x24": 2, "x25": 2, "x26": 3, "x27": 1, "x28": 2, "x29": 2, "x30": 2, "x31": 2, "x32": 3, "x33": 1, "x34": 3, "x35": 1, "x36": 3, "x37": 2, "x38": 2, "x39": 1, "x40": 2, "x41": 2, "x42": 2, "x43": 2, "x44": 3, "x45": 2, "x46": 1, "x47": 2, "x48": 3, "x49": 1, "x50": 2, "x51": 3, "x52": 3, "x53": 2, "x54": 2, "x55": 3, "x56": 2, "x57": 2, "x58": 2, "x59": 2, "x60": 3}
});
