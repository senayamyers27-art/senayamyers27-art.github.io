/* More aws-developer practice questions with wrong-answer notes, and a difficulty level for every question. Merged into the free bank by tools/build.js. */
CertHub.addExtra("aws-developer", {
  questions: [
    ["x1",0,1,"A team must process tens of thousands of IoT events per second with AWS Step Functions. Each run finishes in well under a minute, and low cost per execution matters more than a long, exactly-once execution history. Which workflow type fits?",["Standard workflows, because they keep a full execution history for every run","Express workflows, built for high-volume, short-duration event processing","Standard workflows with a Wait state that groups events into batches first","Activity tasks polled by EC2 workers from inside a Standard workflow"],1,"Express workflows are designed for high event rates and short runs (up to five minutes) and are priced for volume. Standard workflows give exactly-once execution with long retained history, which this scenario says it does not need.","Domain 1: Step Functions Standard vs Express"],
    ["x2",0,1,"A Step Functions Task state calls a Lambda function that sometimes fails with a transient error. The workflow must retry a few times with growing delays and, if it still fails, move to a cleanup state. What should the developer add to the Task state?",["A Choice state after the task that loops back when output has an error","A Parallel state running the same task twice, keeping the branch that works","A Wait state before the task so the downstream service can recover first","A Retry field with a BackoffRate, and a Catch field routing to cleanup"],3,"Retry with IntervalSeconds, MaxAttempts and BackoffRate gives bounded exponential retries, and Catch sends any remaining failure to a recovery state. A Choice loop is the tempting DIY option but lacks backoff and cannot catch task errors.","Domain 1: Step Functions retry and catch"],
    ["x3",0,1,"A script calls DynamoDB Scan through the SDK and processes only part of a large table. The response contains a LastEvaluatedKey value. What is happening, and what should the script do?",["The 1 MB page limit was hit; call Scan again with ExclusiveStartKey","The table throttled the call and returned partial data instead of an error","Eventually consistent reads hid some items; retry with ConsistentRead","The filter removed the other items; the key marks the final match found"],0,"Scan and Query return at most 1 MB per call. LastEvaluatedKey tells the caller to request the next page by passing it as ExclusiveStartKey (or to use the SDK paginator). Throttling looks different: it raises an exception.","Domain 1: SDK pagination"],
    ["x4",0,1,"Right after calling CreateTable with the SDK, a script writes items and fails because the table is still being created. What is the cleanest fix?",["Retry each write in a tight loop every few milliseconds until it works","Switch the table to on-demand capacity so it becomes active at once","Use the SDK waiter for table existence before writing any items","Call DescribeTable once and start writing whatever status it returns"],2,"SDK waiters (such as TableExists / table_exists) poll DescribeTable until the table is ACTIVE, with delays and a maximum attempts limit. A hand-written tight loop is tempting but hammers the API and has no backoff.","Domain 1: SDK waiters"],
    ["x5",0,1,"An EC2 instance has an instance profile role, but a developer also exported AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY for an IAM user in the shell. The application's SDK calls run with the IAM user's permissions. Why?",["The instance profile is ignored whenever the instance has a public IPv4 address","IAM always prefers IAM user permissions over role permissions when both exist","The default credential chain checks environment variables before the instance role","The role credentials expired, so the SDK fell back to the environment variables"],2,"The SDK default provider chain finds explicit and environment-variable credentials before it reaches the instance metadata role, so leftover keys override the role. Removing the variables lets the instance profile be used. Expired role credentials is tempting, but the SDK renews them.","Domain 1: SDK credential provider chain"],
    ["x6",0,1,"A Lambda function downloads a 2 GB model file from S3 on each cold start and fails with 'No space left on device'. Memory and timeout are fine. What should the developer change?",["Raise the function's ephemeral storage (/tmp) setting","Raise the function timeout to the 15-minute maximum allowed","Put the model file into an environment variable","Turn on provisioned concurrency for the alias"],0,"Lambda's /tmp defaults to 512 MB and can be configured larger (up to 10,240 MB). A file bigger than the default needs more ephemeral storage. Provisioned concurrency is tempting because it reduces cold starts, but it does not change disk size.","Domain 1: Lambda ephemeral storage"],
    ["x7",0,1,"An asynchronously invoked Lambda function must send a record of every successful invocation, including the function's response, to an audit function without changing the handler code. What should be configured?",["A dead-letter queue attached to the function for async events","A CloudWatch Logs subscription on the log group","Reserved concurrency on the audit function","An on-success destination for asynchronous invocations"],3,"Lambda destinations for asynchronous invocations can send an invocation record, including the response payload, to a target on success or on failure. A DLQ is the tempting choice but only captures failed events and holds the event, not the result.","Domain 1: Lambda async invocation destinations"],
    ["x8",0,1,"A Lambda function reads a DynamoDB stream. One malformed record makes every batch fail, and Lambda keeps retrying that shard, so newer changes on the shard are stuck. Which event source mapping changes address this best?",["Raise the batch size so the bad record is mixed in with more good records","Enable bisect on error, cap retry attempts, and add an on-failure destination","Lower the function's memory so failed invocations end sooner and cost less","Change the stream view type from NEW_AND_OLD_IMAGES to KEYS_ONLY"],1,"For stream sources, BisectBatchOnFunctionError splits a failing batch to isolate the bad record, MaximumRetryAttempts (or record age) stops endless retries, and an on-failure destination keeps details of the skipped record. Bigger batches just make every batch fail.","Domain 1: Lambda event source mappings for streams"],
    ["x9",0,1,"A producer sends messages to an SQS FIFO queue and sometimes repeats a SendMessage call after a network timeout, creating duplicates. Retried messages have identical bodies. What is the simplest way to prevent the duplicates?",["Enable content-based deduplication on the queue","Give every message its own unique MessageGroupId","Enable long polling with a 20-second wait time","Set DelaySeconds to zero on every send call"],0,"With content-based deduplication, SQS FIFO derives the deduplication ID from a SHA-256 hash of the body, so identical retries inside the deduplication interval are dropped. MessageGroupId is tempting because it is FIFO-specific, but it governs ordering.","Domain 1: SQS FIFO deduplication"],
    ["x10",0,1,"A Kinesis data stream has 10 shards. One shard returns ProvisionedThroughputExceededException while the others sit idle. Every producer uses the constant string 'events' as its partition key. What should the developer do?",["Increase the stream's data retention period","Switch the consumer applications to enhanced fan-out","Use a high-cardinality partition key such as the device ID","Merge shards so that throughput is concentrated in fewer shards"],2,"The partition key is hashed to choose a shard, so one constant key sends every record to one shard. A key with many distinct values spreads writes evenly. Enhanced fan-out is tempting but addresses consumer reads, not producer hot spots.","Domain 1: Kinesis partition keys"],
    ["x11",0,1,"Five independent applications read the same Kinesis data stream and compete for each shard's shared read throughput, so their processing latency grows. What should the developer use?",["Put an SQS queue between the stream and every app","Register each app as an enhanced fan-out consumer","Raise the retention period on the Kinesis stream","Raise the batch size used on each GetRecords call"],1,"Enhanced fan-out gives every registered consumer its own dedicated read throughput per shard, pushed over HTTP/2, so consumers stop competing. Bigger GetRecords batches are tempting, but they still share one per-shard limit.","Domain 1: Kinesis enhanced fan-out"],
    ["x12",0,1,"Microservices publish events to a custom EventBridge event bus. A Lambda function must run only for events with source 'com.shop.orders' and detail-type 'OrderCancelled'. What should the developer create?",["An SNS filter policy on a topic subscribed to the default event bus","A function that receives every event and returns early for the rest","A CloudWatch Logs metric filter that looks for the event text","A rule on the custom bus whose event pattern matches source and detail-type"],3,"EventBridge rules use event patterns to match fields such as source and detail-type, then invoke only the matching targets. Filtering inside the function is the tempting shortcut, but it pays for every unneeded invocation.","Domain 1: EventBridge rules"],
    ["x13",0,1,"An order process spans payment, inventory and shipping services. The team needs one place to see each order's current step, run compensating actions when a step fails, and review execution history. Which approach fits best?",["Choreography, with each service reacting to the others' EventBridge events","Synchronous HTTP calls chained one after another from the payment service","A shared DynamoDB table that each service polls to discover status changes","Orchestration with a Step Functions state machine calling each service"],3,"Orchestration puts the flow in one place: Step Functions shows each execution's state and history, and Catch paths can run compensating steps. Choreography is a valid pattern and tempting for decoupling, but it lacks a central view of each order.","Domain 1: Orchestration vs choreography"],
    ["x14",0,1,"Placing an order must decrement stock in an Inventory table and insert a row into an Orders table. Both writes must succeed together or not at all, and stock must never go negative. What should the developer use?",["BatchWriteItem containing both requests in one call to DynamoDB","TransactWriteItems with a condition check that stock is sufficient","Two separate PutItem calls, retrying either one if it fails","A DynamoDB stream on Orders that updates the Inventory table afterwards"],1,"DynamoDB transactions apply all actions atomically across tables, and a ConditionExpression can cancel the whole transaction when stock is too low. BatchWriteItem is tempting because it groups writes, but it is not all-or-nothing.","Domain 1: DynamoDB transactions"],
    ["x15",0,1,"A developer needs an alternate sort key on an existing, populated DynamoDB table (same partition key) and requires strongly consistent queries on it. What is the constraint?",["An LSI can be added any time but only supports eventually consistent reads","A GSI allows strongly consistent reads if it reuses the table's partition key","LSIs can only be defined at table creation, so a new table is required","Neither index type may use a sort key different from the base table's"],2,"A local secondary index shares the partition key, allows strongly consistent reads, and must be created with the table. A GSI can be added later but reads from it are eventually consistent, so it cannot meet the strong-consistency requirement.","Domain 1: DynamoDB LSI vs GSI"],
    ["x16",0,1,"A user saves a profile change and immediately reloads the page, but GetItem sometimes returns the old value. Which change makes GetItem return the latest committed data?",["Set ConsistentRead to true on the GetItem request","Add a global secondary index keyed on the profile ID attribute","Enable DynamoDB Streams on the profiles table","Place DynamoDB Accelerator (DAX) in front of it"],0,"GetItem is eventually consistent by default. Setting ConsistentRead to true returns the most recent successful write. DAX is tempting as a performance fix, but its item cache serves eventually consistent results.","Domain 1: DynamoDB read consistency"],
    ["x17",0,1,"Invoice PDFs in S3 are read often for 30 days, rarely afterwards, and must be deleted after 7 years. What is the lowest-effort way to manage them?",["A nightly Lambda that copies old objects to another bucket","Lifecycle rules: transition after 30 days, expire after 7 years","Versioning with MFA Delete turned on for the invoice bucket","Event notifications that tag objects once they are 30 days old"],1,"S3 Lifecycle configurations can transition objects to lower-cost storage classes after a set age and expire them later, with no code. A scheduled Lambda job is the tempting DIY answer, but it adds code and operations for a built-in feature.","Domain 1: S3 storage classes and lifecycle"],
    ["x18",0,1,"Several teams each need their own Lambda function to react to every new object in one bucket, and more consumers will be added over time without editing the bucket's configuration each time. What design fits best?",["Add one event notification per function, all using the same prefix","Have each function list the bucket every minute for new objects","Replicate the bucket to a separate copy owned by each team","Send S3 events to EventBridge and give each consumer its own rule"],3,"With EventBridge enabled on the bucket, every object event goes to the default bus, and each team adds its own rule and target without touching the bucket. Separate S3 notifications per function is tempting but fails for overlapping event types and prefixes.","Domain 1: S3 event notifications"],
    ["x19",0,1,"An application uses lazy loading with ElastiCache for Redis. After prices change in the database, users keep seeing old prices for hours. What is the most appropriate fix?",["Give cached keys a TTL, and delete or update keys when the data is written","Move to a Memcached cluster with more nodes in it","Choose a larger node type so more keys fit in memory","Enable Multi-AZ with automatic failover on the cluster"],0,"Lazy loading only refreshes a key on a cache miss, so entries without a TTL can stay stale indefinitely. A TTL bounds staleness, and invalidating or writing through on updates removes it. Bigger nodes are tempting but do not affect freshness.","Domain 1: ElastiCache caching strategies"],
    ["x20",0,2,"A mobile app uses a Cognito user pool. Users should stay signed in for weeks without re-entering passwords, while ID and access tokens stay short-lived. What should the app do?",["Set the access token lifetime to several weeks long","Store the password on the device and sign in silently","Use the refresh token to get new ID and access tokens","Swap the ID token for an API Gateway API key"],2,"Cognito issues a refresh token alongside the ID and access tokens; the app uses it to obtain fresh short-lived tokens without prompting the user. Lengthening the access token is tempting but weakens security.","Domain 2: Cognito user pool tokens"],
    ["x21",0,2,"A company sells access to its REST API and must give each customer a monthly request quota and a throttle rate. What should the developer configure in API Gateway?",["A Cognito authorizer with a group per customer","Resource policies listing customer IP ranges","API keys associated with usage plans","Stage variables holding each customer's limits"],2,"Usage plans define throttling and quotas, and API keys identify which customer a request counts against. AWS advises not to rely on API keys alone for authorization, so pair them with an authorizer. Cognito groups are tempting but do not meter usage.","Domain 2: API keys and usage plans"],
    ["x22",0,2,"A token-based Lambda authorizer runs on every API request, adding latency and cost, even though a token's policy stays valid for several minutes. What should the developer do?",["Enable authorizer result caching with a suitable TTL","Enable API Gateway stage caching for all methods","Give the authorizer function more memory","Replace the authorizer with an API key required on each method"],0,"API Gateway can cache the policy a Lambda authorizer returns, keyed on the token (or identity sources), for a configured TTL, so repeat requests skip the authorizer. Stage caching sounds similar but applies to backend responses.","Domain 2: Lambda authorizers"],
    ["x23",0,2,"A role in account B was created for an app in account A. The app's IAM policy allows sts:AssumeRole on that role's ARN, but the call returns AccessDenied. What is most likely missing?",["The role has no session policy passed along with the AssumeRole call","Account A must first activate STS endpoints in the account's Region","The call must be made with the root user of account A to cross accounts","The role's trust policy does not name account A or its role as a principal"],3,"Cross-account role assumption needs both sides: an identity policy in A that allows sts:AssumeRole, and a trust policy on B's role that trusts A. With the caller's side already allowed, the missing trust policy is the likely cause.","Domain 2: STS AssumeRole and trust policies"],
    ["x24",0,2,"A company must keep its encryption keys outside AWS entirely but still wants Amazon S3 to perform the encryption and decryption. Which option fits?",["SSE-KMS with a customer managed key that the company controls","SSE-C, providing the key with each request over HTTPS","SSE-S3 using S3 managed keys","S3 Bucket Keys with SSE-KMS"],1,"With SSE-C the client supplies the key on every request; S3 uses it to encrypt or decrypt and does not store it. Customer managed KMS keys are tempting because the customer controls them, but they still reside in AWS.","Domain 2: S3 encryption options"],
    ["x25",0,2,"Objects in a bucket use SSE-KMS with the AWS managed aws/s3 key. The team now needs to edit the key policy and let another AWS account decrypt the data. What should they do?",["Create a customer managed key and encrypt with SSE-KMS using it","Edit the key policy of the aws/s3 AWS managed key directly","Switch the bucket's default encryption to SSE-S3","Enable S3 Bucket Keys on the aws/s3 AWS managed key"],0,"Only customer managed KMS keys let you write the key policy, grant other accounts, and control rotation. AWS managed keys such as aws/s3 are convenient but their policies are fixed, which is why editing them is not possible.","Domain 2: KMS customer managed vs AWS managed keys"],
    ["x26",0,2,"An application needs to store a 6 KB configuration document in Systems Manager Parameter Store and have it expire automatically through a parameter policy. Which option works?",["A standard-tier SecureString parameter","A standard-tier StringList parameter","An advanced-tier parameter","A standard-tier String with a label"],2,"Advanced-tier parameters allow values up to 8 KB and support parameter policies such as Expiration. Standard parameters max out at 4 KB and have no policies. SecureString is tempting because it sounds more capable, but it is a type, not a tier.","Domain 2: Parameter Store tiers"],
    ["x27",0,2,"Users get AWS credentials through a Cognito identity pool. Each user must read and write only objects under an S3 prefix that matches their own identity ID. What is the best approach?",["A separate IAM role created for every user at the moment they sign up","One authenticated role whose policy uses the cognito-identity sub variable in the ARN","A bucket policy that lists every user's identity ARN one by one","Presigned URLs for all objects, regenerated once a day for each user"],1,"IAM policy variables such as ${cognito-identity.amazonaws.com:sub} resolve per request, so one role can restrict every user to their own prefix. Per-user roles look precise but do not scale.","Domain 2: Least privilege with Cognito identity pools"],
    ["x28",0,2,"A Lambda function reads a database secret from Secrets Manager on every invocation, adding latency and API calls. The secret is rotated periodically. What is the best practice?",["Copy the secret value into an environment variable at deploy time","Bundle the secret value into the function's deployment package","Turn off rotation so the value can be cached for as long as needed","Cache the secret outside the handler and refresh it periodically"],3,"Caching the secret in the execution environment (for example with a caching client or the Parameters and Secrets Lambda extension) with a refresh interval cuts calls while still picking up rotated values. An environment variable is tempting but becomes stale.","Domain 2: Secrets Manager in Lambda"],
    ["x29",0,2,"A REST API must be reachable only from one VPC through an interface VPC endpoint for API Gateway. What should the developer configure?",["A required API key on every method of the API","A Cognito user pool authorizer on each method","A usage plan that applies a very low throttle limit to all clients","A private API with a resource policy allowing that endpoint"],3,"Private REST APIs are reachable only through interface VPC endpoints, and a resource policy using aws:SourceVpce (or aws:SourceVpc) limits access to the approved endpoint. A Cognito authorizer is tempting but controls who, not from where.","Domain 2: API Gateway resource policies"],
    ["x30",0,2,"A role's identity policy allows dynamodb:PutItem on a table, but the permissions boundary attached to the role allows only dynamodb:GetItem. What happens when the role calls PutItem?",["Allowed, because identity policies outrank permissions boundaries","Denied, because the effective permissions are the overlap of both","Allowed, but only if the table also has a resource-based policy","Denied only for items larger than the item size limit of the table"],1,"A permissions boundary caps what identity policies can grant: an action must be allowed by both. PutItem is outside the boundary, so it is implicitly denied. Thinking the identity policy wins is the common mistake.","Domain 2: IAM policy evaluation"],
    ["x31",0,2,"A game lets players try it without signing in, and these guests need limited temporary AWS credentials to write scores to a DynamoDB table. What should the developer enable?",["A Cognito user pool app client created without a client secret","One IAM user whose access keys are shared by all guest players","Guest (unauthenticated) identities in a Cognito identity pool","A resource policy on the table that allows public anonymous writes"],2,"Identity pools can issue temporary credentials to unauthenticated identities, mapped to a tightly scoped guest role. The user pool option is tempting because it is Cognito too, but user pools authenticate users rather than vend AWS credentials.","Domain 2: Cognito identity pools"],
    ["x32",0,2,"A presigned GET URL generated inside a Lambda function stops working long before the expiry time the code requested. What is the most likely reason?",["It was signed with the role's temporary credentials, which expired sooner","Presigned URLs work only for PutObject requests, never for GetObject","The bucket's default encryption is SSE-S3, which blocks presigned access","The object is stored in the S3 Standard class, which limits URL lifetime"],0,"A presigned URL is valid only while the credentials that signed it are valid. Lambda execution role credentials are temporary, so a URL can expire when they do, even if a longer expiry was requested. Blaming encryption is a tempting but wrong guess.","Domain 2: Presigned URLs"],
    ["x33",0,2,"A developer wants each KMS decrypt operation to fail unless the caller supplies the same tenant ID that was used at encryption time, and wants the tenant ID recorded in CloudTrail. What should they use?",["A data key cached separately for every tenant","An encryption context that includes the tenant ID","A separate key alias created for every tenant in the account","An unconstrained grant to each tenant's role"],1,"An encryption context is non-secret additional authenticated data: the same context must be supplied to decrypt, and it appears in CloudTrail logs. Per-tenant aliases are tempting but only name keys.","Domain 2: KMS encryption context"],
    ["x34",0,2,"An ECS task on Fargate must read an S3 bucket from application code. The task definition has a task execution role that can pull images from ECR, but the app gets AccessDenied. What should be fixed?",["Add S3 read access to the ECR repository's resource policy","Attach an EC2 instance profile to the Fargate task definition","Apply a public-read bucket policy to the bucket for the task","Add a task role with S3 permissions for the application to use"],3,"The task execution role is used by the ECS agent to pull images and send logs; the task role supplies credentials to the application code. Adding S3 access to the execution role is the tempting mistake, as the app does not use those credentials.","Domain 2: ECS task roles"],
    ["x35",0,2,"A Lambda function logs every incoming event in full, including Authorization headers that carry bearer tokens. What is the best fix?",["Stop logging raw events; log only needed fields and redact secrets","Shorten the log group's retention period to a single day","Encrypt the log group with a KMS key and keep logging the full event","Disable CloudWatch Logs for the function to hide the tokens"],0,"Sensitive values should never be written to logs; log only what troubleshooting needs and redact credentials. Encrypting the log group is tempting because it sounds secure, but authorized readers would still see tokens.","Domain 2: Keeping sensitive data out of logs"],
    ["x36",0,3,"In an AWS SAM template, which declaration tells CloudFormation to process SAM resource types such as AWS::Serverless::Function?",["AWSTemplateFormatVersion: '2010-09-09'","Resources: with a SAM Type property","Transform: AWS::Serverless-2016-10-31","Metadata: with a SAM tool section"],2,"The Transform line invokes the AWS::Serverless macro, which expands SAM resources into standard CloudFormation resources. AWSTemplateFormatVersion is tempting since it is also at the top of templates, but it does not expand anything.","Domain 3: AWS SAM template structure"],
    ["x37",0,3,"A developer ran sam deploy --guided once and wants later deployments to reuse the same stack name, Region and parameter values without prompts. Where does SAM store these choices?",["The Outputs section","buildspec.yml","samconfig.toml","appspec.yml"],2,"The guided deploy offers to save its answers to samconfig.toml, and later sam deploy runs read them. buildspec.yml is tempting because it is also a config file in the toolchain, but it belongs to CodeBuild.","Domain 3: SAM CLI configuration"],
    ["x38",0,3,"The first cdk deploy into a new account and Region fails because resources the CDK needs, such as an S3 bucket for assets, do not exist. What should be run first?",["cdk bootstrap","cdk synth --all","cdk diff","cdk init"],0,"cdk bootstrap deploys the toolkit stack (staging bucket, roles and related resources) that CDK deployments need in each account and Region. cdk synth is tempting because it is also run before deploys, but it only writes local output.","Domain 3: AWS CDK bootstrap"],
    ["x39",0,3,"Without using CodeDeploy, a developer wants the prod alias of a Lambda function to send 5% of invocations to version 8 and the rest to version 7. What should they configure?",["Two aliases that share the same name, one for each of the versions","A Lambda layer that points the function at the code of version 8","Reserved concurrency on version 8 equal to 5% of the account limit","Weighted routing on the alias, adding version 8 as an extra version"],3,"A Lambda alias can point to two versions with a routing configuration that sends a set weight to the additional version. CodeDeploy automates the same mechanism. Reserved concurrency is tempting but controls capacity, not traffic share.","Domain 3: Lambda weighted aliases"],
    ["x40",0,3,"A team wants to send 10% of a REST API stage's traffic to a new API deployment and compare its metrics before promoting it. Which API Gateway feature fits?",["A mock integration on the API","A canary release on the stage","A usage plan for the stage","An edge-optimized endpoint type"],1,"Stage canary settings route a chosen percentage of requests to a canary deployment, with separate logs and metrics, and can then be promoted. Usage plans are tempting because they also attach to stages, but they only meter usage.","Domain 3: API Gateway canary releases"],
    ["x41",0,3,"In a CodeDeploy blue/green deployment to Amazon ECS, the team wants tests to run against the replacement task set through the test listener before any production traffic is shifted. Which lifecycle hook should invoke the test function?",["AfterAllowTestTraffic","BeforeInstall","ApplicationStart","ValidateService"],0,"ECS appspec hooks are BeforeInstall, AfterInstall, AfterAllowTestTraffic, BeforeAllowTraffic and AfterAllowTraffic. AfterAllowTestTraffic runs once the test listener sends traffic to the new tasks. ValidateService is tempting from EC2 deployments but does not apply.","Domain 3: CodeDeploy hooks for ECS"],
    ["x42",0,3,"A CodeDeploy deployment for a Lambda alias must run a validation function before any traffic shifts to the new version. Which appspec hook should call it?",["AfterInstall","ApplicationStop","BeforeAllowTraffic","DownloadBundle"],2,"Lambda deployments support two hooks: BeforeAllowTraffic (before shifting) and AfterAllowTraffic (after). AfterInstall is tempting from EC2 appspec files, but Lambda deployments do not have it.","Domain 3: CodeDeploy hooks for Lambda"],
    ["x43",0,3,"An Elastic Beanstalk production app must keep full serving capacity during deployments, but the team wants lower cost than launching a complete second set of instances. Which deployment policy fits best?",["Immutable","Rolling with an additional batch","All at once","Rolling"],1,"Rolling with an additional batch first launches one extra batch, so capacity never drops, while only adding one batch of instances. Immutable is the tempting answer because it also preserves capacity, but it doubles the fleet during deployment.","Domain 3: Elastic Beanstalk deployment policies"],
    ["x44",0,3,"A team wants Elastic Beanstalk to send a percentage of client traffic to instances running the new version for an evaluation period, and to go back if the new instances are unhealthy. Which policy fits?",["Rolling","All at once","Rolling with an additional batch","Traffic splitting"],3,"Traffic-splitting deployments launch new instances and send a set percentage of traffic to them for an evaluation time; if they fail health checks, traffic returns to the old version. Rolling with an additional batch is tempting but has no canary-style split.","Domain 3: Elastic Beanstalk traffic splitting"],
    ["x45",0,3,"A CodeBuild project compiles a Java application, but the next pipeline stage receives an empty output artifact. The buildspec has install and build phases only. What is missing?",["A post_build phase that echoes a message","An env section that defines the JAVA_HOME variable","A cache section for Maven dependencies","An artifacts section listing the output files"],3,"CodeBuild only uploads files named in the buildspec artifacts section (for example target/*.jar). Without it the output artifact is empty. A post_build phase is tempting since it runs last, but it does not package output.","Domain 3: CodeBuild buildspec artifacts"],
    ["x46",0,3,"A buildspec needs a database password during integration tests, and the password must not be stored in the repository. What is the best approach?",["Write the password directly into the buildspec.yml file","Reference it in the buildspec env section from Secrets Manager","Commit it to the repository inside an encrypted zip file","Enter it as a plaintext environment variable in the console"],1,"The buildspec env section can pull values from Secrets Manager (secrets-manager) or Parameter Store (parameter-store) at build time, so nothing sensitive is committed. A plaintext console variable is tempting but exposes the value.","Domain 3: CodeBuild secrets handling"],
    ["x47",0,3,"A CloudFormation stack update fails partway through. What does CloudFormation do by default?",["Keeps the updated resources and deletes the stack","Retries the failed resource until the update succeeds","Rolls the stack back to its last known working state","Deletes the stack along with all of its resources"],2,"By default a failed stack update is rolled back, returning resources to their last stable configuration (UPDATE_ROLLBACK_COMPLETE). Deleting the stack is a tempting guess because failed creates can end in deletion, but updates roll back.","Domain 3: CloudFormation rollback"],
    ["x48",0,3,"A team published Lambda version 5 and pointed the prod alias at it. A developer then edits the code in the console without publishing. What do callers of the prod alias run?",["Version 5, since published versions cannot change","The edited code, because aliases follow $LATEST","An error until a new version has been published","Versions 5 and $LATEST alternately, request by request"],0,"Published versions are immutable snapshots, and an alias keeps pointing at the version it names. Edits affect only $LATEST until a new version is published and the alias updated. Assuming aliases track $LATEST is the common trap.","Domain 3: Lambda versions and aliases"],
    ["x49",0,3,"A pipeline deploys a serverless API to a test stage. The team wants the pipeline to stop before production if end-to-end tests against that deployed stage fail. What should they add?",["A manual approval action placed before the test stage deployment","A CodeBuild test action after the test deploy that fails on test errors","Extra unit tests in the build phase that use mocked AWS services","CloudFormation drift detection run against the test stack afterwards"],1,"A test action (for example, CodeBuild running the integration suite against the test stage URL) that fails the stage on any failing test stops the pipeline before production. Mocked unit tests are tempting but do not exercise the deployed environment.","Domain 3: Integration testing in pipelines"],
    ["x50",0,4,"A high-traffic API instrumented with X-Ray produces a large volume of traces and rising cost. The team wants a small representative sample, but every request on one critical checkout path. What should they configure?",["Disable active tracing on every function behind the API","Record all details as metadata instead of as annotations","Increase the buffer size used by the X-Ray daemon","Custom sampling rules: a low default rate plus a higher-priority checkout rule"],3,"X-Ray sampling rules match on attributes such as URL path and are evaluated by priority, so one rule can sample checkout at a high rate while the default rule samples everything else sparingly. Disabling tracing cuts cost but loses the needed data.","Domain 4: X-Ray sampling rules"],
    ["x51",0,4,"A developer wants X-Ray to show how long a function spends calling a third-party payment API compared with its own processing. What should they add?",["A subsegment around the payment API call","An annotation that stores the duration","A metric filter on the function's log group","A second X-Ray group for payment traces"],0,"Subsegments record timing for a specific downstream call or block of code within a segment, so the trace timeline shows that call separately. An annotation is tempting but only indexes values for filtering.","Domain 4: X-Ray subsegments"],
    ["x52",0,4,"An application needs a custom CloudWatch metric with 1-second granularity and an alarm that evaluates every 10 seconds. What should the developer use?",["A standard-resolution metric that is published every second","A scheduled CloudWatch Logs Insights query","A high-resolution metric with a high-resolution alarm","The latency figures on the X-Ray service map"],2,"Publishing with StorageResolution 1 creates a high-resolution metric, and alarms on it can use 10- or 30-second periods. Publishing a standard metric every second is tempting, but CloudWatch aggregates it to one minute.","Domain 4: High-resolution custom metrics"],
    ["x53",0,4,"A Lambda function writes to a legacy database that accepts only a limited number of connections. During traffic spikes the function scales out and overwhelms the database. What is the simplest control?",["Enable provisioned concurrency on the function alias","Increase the function's memory to speed each call","Set reserved concurrency to cap how many copies run","Add an X-Ray annotation to the database requests"],2,"Reserved concurrency sets a maximum number of concurrent executions for the function, protecting a fragile downstream resource. Provisioned concurrency is the tempting mix-up; it is about warm starts, not a ceiling.","Domain 4: Lambda reserved concurrency"],
    ["x54",0,4,"After a team sets a large reserved concurrency value on one Lambda function, other functions in the same account and Region start being throttled during peaks. Why?",["Reserved concurrency is taken out of the unreserved pool the other functions share","Reserved concurrency doubles the memory of every other function in the Region","Setting reserved concurrency turns off scaling for all other functions in the account","Reserved concurrency makes cold starts longer for every function in the account"],0,"Account concurrency is shared per Region. Reserving a chunk for one function removes it from the unreserved pool, so other functions can hit the lower remaining ceiling and throttle. Assuming scaling was turned off is tempting, but they are simply short of pool.","Domain 4: Lambda concurrency troubleshooting"],
    ["x55",0,4,"After fixing a bug, a developer wants to move messages from an SQS dead-letter queue back to the source queue for reprocessing with minimal code. What should they use?",["Purge the DLQ and ask the producers to resend every message","Convert the dead-letter queue into a FIFO queue","Extend the dead-letter queue's retention period","The dead-letter queue redrive to the source queue"],3,"SQS supports DLQ redrive, which moves messages from the dead-letter queue back to the source queue (or another queue) from the console or API. Extending retention is tempting as a safety step but reprocesses nothing.","Domain 4: SQS dead-letter queue redrive"],
    ["x56",0,4,"A team wants an alarm whenever the text 'PaymentDeclined' appears in application logs more than 20 times in 5 minutes. What should be created first?",["An X-Ray group with a filter expression for payment declined errors","A metric filter on the log group counting those events","A saved CloudWatch Logs Insights query for the text","A CloudTrail trail that captures application events"],1,"A metric filter turns matching log events into a CloudWatch metric, and an alarm can then fire when the count exceeds 20 in 5 minutes. A saved Logs Insights query is tempting but is an analysis tool, not a metric.","Domain 4: CloudWatch metric filters and alarms"],
    ["x57",0,4,"A new application has unpredictable DynamoDB traffic that goes from near zero to sudden large spikes, and the team does not want to manage capacity. Which option fits?",["On-demand capacity mode","Provisioned mode, low fixed capacity","Provisioned mode, scheduled scaling","DAX without any DynamoDB table"],0,"On-demand mode charges per request and adapts to traffic automatically, suiting spiky, unpredictable workloads with no capacity planning. Scheduled scaling is tempting but only helps when peaks are predictable.","Domain 4: DynamoDB capacity modes"],
    ["x58",0,4,"A Lambda function was attached to private subnets that have no NAT gateway and no VPC endpoints. Now its DynamoDB calls hang until the function logs 'Task timed out'. What is the best fix?",["Raise the timeout to 15 minutes so the calls have time to finish","Increase memory so the function gets more network bandwidth","Add a DynamoDB gateway VPC endpoint to the subnet route tables","Enable active X-Ray tracing on the function to fix connectivity"],2,"A VPC-attached function uses the VPC's routing; with no NAT or endpoint it cannot reach public service endpoints. A DynamoDB gateway endpoint adds a private route. Raising the timeout is tempting but the requests will never complete.","Domain 4: Troubleshooting Lambda timeouts in a VPC"],
    ["x59",0,4,"A developer wants to find which Lambda invocations used the most memory in the last day. Where is this data most readily available?",["CloudTrail management events recorded for the function","The REPORT log lines, queried with Logs Insights","The X-Ray service map for the application","IAM Access Analyzer findings for the role"],1,"Every invocation writes a REPORT line with duration, billed duration and max memory used, and Logs Insights exposes fields such as @maxMemoryUsed for sorting. X-Ray is tempting for performance questions but does not report memory.","Domain 4: CloudWatch Logs Insights"],
    ["x60",0,4,"A Java Lambda function has long cold starts caused by heavy initialization. The team wants to reduce them without paying to keep environments warm all the time. Which feature fits?",["Provisioned concurrency on the function alias","Reserved concurrency on the function","A larger ephemeral /tmp storage size","Lambda SnapStart on published versions"],3,"SnapStart snapshots the initialized execution environment when a version is published and resumes from it, cutting cold start time without always-on environments. Provisioned concurrency is the tempting answer but it is the pay-to-stay-warm option ruled out.","Domain 4: Lambda cold starts"]
  ],
  whys: {
    "x1": ["Standard workflows suit long-running, exactly-once orchestration; at this event rate and short duration they cost more than Express.",null,"A Wait state only pauses a run; it does not batch events and still leaves every execution as a costlier Standard run.","Activity workers add servers to manage and do nothing to address the per-execution cost of a Standard workflow."],
    "x2": ["A hand-built Choice loop reinvents Retry, has no built-in backoff, and never catches errors that stop the Task state outright.","Running the task twice in parallel doubles the side effects and gives no bounded retries or failure routing.","A fixed Wait before the first attempt does not retry anything after a failure and adds delay to every successful run.",null],
    "x3": [null,"Throttling returns a ProvisionedThroughputExceededException, not a partial page with a LastEvaluatedKey.","Consistency affects how fresh items are, not how many are returned; a LastEvaluatedKey means more pages remain.","A FilterExpression is applied after each page is read; LastEvaluatedKey signals that more of the table is left to scan."],
    "x4": ["A tight retry loop wastes calls and can hit throttling; waiters poll at sensible intervals with a limit.","Capacity mode does not skip table creation; the table still passes through CREATING before it is ACTIVE.",null,"Checking once without acting on the status does nothing to wait for ACTIVE, so writes can still fail."],
    "x5": ["A public IP has no effect on whether the SDK reads instance metadata credentials.","IAM evaluates the single principal making the call; it never merges or prefers users over roles.",null,"The SDK refreshes role credentials automatically; the env variables win because they come earlier in the chain."],
    "x6": [null,"The error is about disk space, not time; a longer timeout still leaves /tmp too small.","Environment variables are limited to a few kilobytes in total and cannot hold a large binary file.","Provisioned concurrency keeps environments warm but does not enlarge their /tmp storage."],
    "x7": ["A dead-letter queue receives only events that failed all retries, never successful results.","Log subscriptions forward log lines, not the invocation record with the returned payload, and need parsing.","Reserved concurrency limits scaling of the audit function; it does not route anything to it.",null],
    "x8": ["A bigger batch still fails as a whole whenever it contains the bad record, so the shard stays blocked.",null,"Less memory does not stop the retries; the same record keeps failing and blocking the shard.","The view type only changes what data each record carries; the malformed record would still fail processing."],
    "x9": [null,"The message group controls ordering and parallelism, not deduplication; duplicates would still be accepted.","Long polling changes how consumers receive, not whether the queue accepts a repeated send.","Delay settings only postpone delivery; they do nothing to recognize a repeated message."],
    "x10": ["Retention decides how long records are kept, not how writes are spread across shards.","Enhanced fan-out improves read throughput for consumers; the problem here is write skew from producers.",null,"Merging shards lowers total capacity and does nothing to spread one constant key across shards."],
    "x11": ["Adding queues creates extra plumbing and cost and still leaves something reading the shared shard throughput.",null,"Longer retention keeps records longer but does not give readers more throughput.","Larger batches per call still draw from the same shared per-shard read limit."],
    "x12": ["SNS filter policies apply to SNS subscriptions; the events here are on a custom EventBridge bus, and the default bus is the wrong bus.","Receiving every event works but wastes invocations and cost when EventBridge can filter before invoking.","Metric filters count log text; they do not route events to a Lambda function.",null],
    "x13": ["Choreography keeps services loosely coupled but spreads the flow across services, so there is no single view of progress or central compensation.","Chained synchronous calls couple the services tightly and give no durable history or built-in error handling.","Polling a shared table couples services through data, adds latency, and still leaves no central flow or history.",null],
    "x14": ["BatchWriteItem is not atomic: some requests can succeed while others fail, and it does not support condition expressions.",null,"Separate calls can leave one write applied and the other not if the process fails between them.","A stream update happens later and separately, so the two tables can disagree and stock checks are not enforced."],
    "x15": ["It is the reverse: an LSI supports strongly consistent reads but cannot be added to an existing table.","GSIs support only eventually consistent reads, whatever key they use.",null,"Both index types exist precisely to provide a different sort key (or key schema) from the base table."],
    "x16": [null,"GSIs are eventually consistent and would not help GetItem on the base table return fresher data.","Streams capture changes for other consumers; they do not change what a read returns.","DAX caches eventually consistent reads, so it can serve stale data rather than fixing it."],
    "x17": ["Custom copy code must be written, run and monitored, while lifecycle rules do this natively.",null,"Versioning and MFA Delete protect against deletion; they neither move data to cheaper storage nor expire it.","Tags alone change nothing; something else would still have to move and delete the objects."],
    "x18": ["S3 does not allow overlapping notification configurations for the same event type and prefix, and each change edits the bucket.","Listing on a schedule is slow, costly at scale, and misses the point of event-driven processing.","Replication multiplies storage cost and still needs notifications on every copy.",null],
    "x19": [null,"Changing engine or node count adds capacity but keeps serving the same stale entries.","More memory just keeps stale keys around longer; freshness is the problem.","Multi-AZ improves availability, not how quickly cached values reflect database changes."],
    "x20": ["Long-lived access tokens widen the damage if one is stolen, which is exactly what short lifetimes are meant to avoid.","Storing passwords on devices is insecure and unnecessary when refresh tokens exist.",null,"API keys identify callers for usage plans; they are not user credentials or session tokens."],
    "x21": ["Cognito groups control identity and authorization, not per-customer quotas and throttles.","Resource policies allow or deny access; they have no concept of monthly quotas.",null,"Stage variables pass configuration to integrations; API Gateway does not enforce limits from them."],
    "x22": [null,"Stage caching caches integration responses, not authorizer decisions, and could serve one user's data to another.","More memory may shave a little time but the authorizer still runs on every request.","API keys are not an authentication mechanism and would drop the token validation entirely."],
    "x23": ["Session policies are optional and can only narrow permissions; their absence never blocks AssumeRole.","Regional STS endpoints are active by default in commercial Regions, and the error points to authorization, not an endpoint.","Using the root user is bad practice and unnecessary; a properly trusted IAM principal can assume the role.",null],
    "x24": ["Customer managed KMS keys are stored and used inside AWS KMS, which the requirement rules out.",null,"SSE-S3 keys are created and held entirely by AWS.","Bucket Keys only reduce KMS calls for SSE-KMS; the keys still live in KMS."],
    "x25": [null,"AWS managed key policies cannot be edited, so they cannot grant another account access.","SSE-S3 has no key policy at all, so there would be nothing to grant or control.","Bucket Keys reduce KMS request volume; they do not change who controls the key policy."],
    "x26": ["Standard parameters are limited to 4 KB and cannot use parameter policies, whatever their type.","StringList is still a standard parameter, so the size limit and lack of policies remain.",null,"Labels are just version aliases; they neither raise the size limit nor add expiration."],
    "x27": ["Per-user roles do not scale and quickly run into IAM quotas and management overhead.",null,"A bucket policy listing each user grows without bound and must be edited on every sign-up.","Daily presigned URLs grant broad, long-lived access and do not scope uploads to each user's own prefix."],
    "x28": ["A deploy-time copy goes stale on the next rotation and exposes the value in function configuration.","Secrets in the deployment package are exposed to anyone who can download the code and never rotate.","Disabling rotation trades away a security control to fix a performance problem that caching solves.",null],
    "x29": ["API keys are not an access control boundary and do nothing to restrict network origin.","A Cognito authorizer checks user identity but leaves the API reachable from anywhere.","Throttling limits request rate; it does not restrict where requests come from.",null],
    "x30": ["Boundaries are not outranked; they set the maximum an identity policy can grant.",null,"Nothing about the table's policy changes this; a resource policy that names the role is still capped by the role's permissions boundary.","Item size is irrelevant to IAM authorization; the action is simply not permitted."],
    "x31": ["A user pool app client handles sign-in for registered users; it does not hand out AWS credentials to guests.","Shared long-term keys embedded in a client can be extracted and abused, and cannot be scoped per player.",null,"Allowing anonymous public writes removes all control over who can change the table."],
    "x32": [null,"Presigned URLs support GET as well as PUT and other operations.","SSE-S3 is transparent to authorized requests, including presigned ones.","Storage class has no effect on how long a presigned URL is valid."],
    "x33": ["Caching data keys per tenant does not make decryption depend on presenting a tenant ID.",null,"Aliases are friendly names for keys; they do not bind ciphertext to a tenant value.","A grant without constraints gives access but does not require any matching context."],
    "x34": ["The ECR repository policy governs image access and has nothing to do with S3 permissions.","Fargate tasks have no EC2 instances to attach a profile to; tasks use task roles.","Making the bucket public exposes data to everyone instead of granting the task least privilege.",null],
    "x35": [null,"Short retention still exposes tokens to anyone reading logs during that window.","Encryption at rest protects storage, but anyone allowed to read the logs still sees the tokens.","Turning off logs removes the ability to troubleshoot rather than fixing what is being logged."],
    "x36": ["The format version identifies the template syntax but does not expand SAM resource types.","Serverless resources are declared under Resources, but they are expanded only because of the Transform.",null,"Metadata holds extra information about the template and does not trigger any processing."],
    "x37": ["Outputs return values from a deployed stack; they do not hold deployment settings.","buildspec.yml configures CodeBuild builds, not SAM CLI deployments.",null,"appspec.yml configures CodeDeploy deployments and lifecycle hooks."],
    "x38": [null,"synth only produces the template locally; it creates nothing in the account.","diff compares with a deployed stack and does not create the missing resources.","init creates a new local project skeleton, not account resources."],
    "x39": ["Alias names must be unique within a function, so two aliases cannot share a name.","Layers hold shared code or dependencies; they do not route invocations between versions.","Concurrency settings cap scaling and do not split traffic by percentage.",null],
    "x40": ["Mock integrations return fixed responses without a backend; they do not split traffic.",null,"Usage plans meter and throttle API key holders; they do not route between deployments.","The endpoint type controls how clients reach the API, not how traffic splits across deployments."],
    "x41": [null,"BeforeInstall runs before the replacement task set is created, so there is nothing to test yet.","ApplicationStart is an EC2/on-premises hook and is not available for ECS deployments.","ValidateService is an EC2/on-premises hook; ECS deployments use the traffic-based hooks instead."],
    "x42": ["AfterInstall is not one of the hooks a Lambda deployment supports.","ApplicationStop is an EC2/on-premises hook for stopping the old application.",null,"DownloadBundle is a CodeDeploy-managed EC2 event and cannot run scripts or functions."],
    "x43": ["Immutable keeps capacity but launches a full parallel set of instances, the extra cost the team wants to avoid.",null,"All at once takes every instance out of service together, so capacity drops to zero briefly.","Plain rolling takes batches out of service, so capacity is reduced during the deployment."],
    "x44": ["Rolling replaces instances batch by batch without sending a controlled share of traffic to the new version.","All at once switches every instance at once with no evaluation period.","This keeps capacity during a rolling update but does not split traffic for evaluation.",null],
    "x45": ["A post_build phase runs commands but does not by itself declare which files become the artifact.","The build ran, so the environment is fine; env variables do not publish outputs.","Caching speeds up builds but has nothing to do with which files are handed to the next stage.",null],
    "x46": ["Anything written into buildspec.yml ends up in source control, which the requirement forbids.",null,"An encrypted zip still stores the secret in the repository and needs its own key handling.","Plaintext project variables are visible to anyone who can view the build project configuration."],
    "x47": ["CloudFormation does not keep a half-updated stack and delete it; it restores the previous state.","There is no endless retry; the failed update triggers a rollback.",null,"Deleting the whole stack on a failed update would destroy working resources; that is not the default."],
    "x48": [null,"An alias points to the version it was set to; only an unqualified call or $LATEST uses the edited code.","Editing $LATEST does not break an alias that points at a published version.","Traffic is split only when a routing configuration is set on the alias."],
    "x49": ["A manual approval before deployment tests nothing and relies on a person rather than results.",null,"Unit tests with mocks run before deployment and cannot prove the deployed stage works end to end.","Drift detection finds configuration changed outside CloudFormation; it does not test API behavior."],
    "x50": ["Turning tracing off loses visibility entirely, including for the critical path.","Metadata versus annotations changes indexing, not how many traces are recorded.","Buffer size affects how the daemon batches uploads, not how many requests are sampled.",null],
    "x51": [null,"An annotation adds a searchable value but does not produce a timed block in the trace timeline.","A metric filter counts log patterns and does not appear in the trace breakdown.","Groups filter traces by expression; they do not time a section of code."],
    "x52": ["Standard-resolution metrics are stored at 1-minute granularity, however often they are published.","Logs Insights queries are for ad hoc analysis, not 10-second alarm evaluation.",null,"The service map visualizes traces; it does not provide custom metrics or alarms."],
    "x53": ["Provisioned concurrency pre-warms environments; it does not stop the function scaling beyond that number.","Faster calls do not limit how many run at once, so connections can still spike.",null,"Annotations help filter traces but do not control scaling."],
    "x54": [null,"Concurrency settings never change any function's memory.","Other functions still scale, but only within what is left of the unreserved pool.","Cold start time is unrelated; the throttling comes from reduced available concurrency."],
    "x55": ["Purging deletes the failed messages, and producers may not be able to recreate them.","A queue's type cannot be changed after creation, and doing so would not move messages.","Longer retention keeps messages but does not move them back for processing.",null],
    "x56": ["X-Ray groups filter traces, not arbitrary log text.",null,"Saved Insights queries are run on demand and do not produce a metric to alarm on by themselves.","CloudTrail records AWS API calls, not text written to application logs."],
    "x57": [null,"A low fixed provisioned value will throttle during the spikes.","Scheduled scaling needs predictable timing, which this workload lacks.","DAX is a cache that sits in front of a table; it cannot replace one."],
    "x58": ["The calls can never reach DynamoDB without a route, so a longer timeout only delays the failure.","More memory does not create a network path out of the private subnets.",null,"Tracing can show where time goes but does not provide connectivity."],
    "x59": ["Management events record API calls such as UpdateFunctionConfiguration, not per-invocation memory use.",null,"The service map shows service connections and latency, not memory used per invocation.","Access Analyzer reviews permissions and external access, not runtime metrics."],
    "x60": ["Provisioned concurrency works but keeps environments initialized continuously, which the team wants to avoid paying for.","Reserved concurrency caps scaling and has no effect on initialization time.","More /tmp space does not speed up initialization of the runtime or code.",null]
  },
  levels: {
    "dva1": 3, "dva2": 1, "dva3": 2, "dva4": 2, "dva5": 3, "dva6": 3, "dva7": 3, "dva8": 2, "dva9": 3, "dva10": 2,
    "dva11": 3, "dva12": 1, "dva13": 2, "dva14": 2, "dva15": 3, "dva16": 1, "dva17": 3, "dva18": 1, "dva19": 1, "dva20": 1,
    "dva21": 1, "dva22": 1, "dva23": 2, "dva24": 2, "dva25": 2, "dva26": 1, "dva27": 3, "dva28": 2, "dva29": 1, "dva30": 1,
    "dva31": 3, "dva32": 2, "dva33": 2, "dva34": 2, "dva35": 3, "dva36": 1, "dva37": 2, "dva38": 2, "dva39": 2, "dva40": 3,
    "dva41": 1, "dva42": 2, "dva43": 1, "dva44": 1, "dva45": 3, "dva46": 2, "dva47": 1, "dva48": 3, "dva49": 1, "dva50": 1,
    "dva51": 2, "dva52": 1, "dva53": 1, "dva54": 2, "dva55": 2, "dva56": 1, "dva57": 3, "dva58": 1, "dva59": 2, "dva60": 2,
    "dva61": 1, "dva62": 1, "dva63": 3, "dva64": 2, "dva65": 1, "dva66": 2, "dva67": 1, "dva68": 3, "dva69": 1, "dva70": 1,
    "dva71": 2, "dva72": 1, "dva73": 1, "dva74": 2, "dva75": 2, "dva76": 1, "dva77": 2, "dva78": 2, "dva79": 2, "dva80": 3,
    "dva81": 2, "dva82": 1, "dva83": 2, "dva84": 3, "dva85": 1, "dva86": 2, "dva87": 3, "dva88": 3, "dva89": 2, "x1": 2,
    "x2": 2, "x3": 2, "x4": 1, "x5": 3, "x6": 2, "x7": 2, "x8": 3, "x9": 2, "x10": 2, "x11": 2,
    "x12": 2, "x13": 3, "x14": 2, "x15": 3, "x16": 1, "x17": 1, "x18": 3, "x19": 2, "x20": 1, "x21": 1,
    "x22": 2, "x23": 2, "x24": 1, "x25": 2, "x26": 2, "x27": 3, "x28": 2, "x29": 2, "x30": 3, "x31": 2,
    "x32": 3, "x33": 3, "x34": 3, "x35": 1, "x36": 1, "x37": 1, "x38": 1, "x39": 2, "x40": 1, "x41": 3,
    "x42": 2, "x43": 3, "x44": 2, "x45": 2, "x46": 2, "x47": 1, "x48": 2, "x49": 2, "x50": 3, "x51": 2,
    "x52": 2, "x53": 2, "x54": 3, "x55": 1, "x56": 2, "x57": 1, "x58": 3, "x59": 2, "x60": 3
  }
});
