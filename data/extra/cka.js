/* More cka practice questions with wrong-answer notes, and a difficulty level for every question. Merged into the free bank by tools/build.js. */
CertHub.addExtra("cka", {
  questions: [
    ["x1",0,1,"Your team keeps a base and a prod overlay for a monitoring stack, and ./overlays/prod holds a kustomization.yaml. Which command renders the overlay and applies it to the cluster in one step?",["kubectl apply -f ./overlays/prod/kustomization.yaml","kubectl apply -k ./overlays/prod","helm install prod ./overlays/prod","kubectl create -k ./overlays/prod --dry-run=server -o yaml"],1,"kubectl apply -k (built on Kustomize) builds the kustomization in that directory and applies the result. Passing kustomization.yaml to -f is tempting, but that file is build instructions, not an API object.","Domain 1: Kustomize"],
    ["x2",0,1,"A CI pipeline uses ServiceAccount build-bot in namespace ci. Before handing it over, you want to confirm it can create Deployments there. Which command checks exactly that identity?",["kubectl auth can-i create deployments -n ci --as=build-bot","kubectl auth whoami --as=system:serviceaccount:ci:build-bot","kubectl get rolebindings -n ci -o wide --as=build-bot","kubectl auth can-i create deployments -n ci --as=system:serviceaccount:ci:build-bot"],3,"ServiceAccounts authenticate as system:serviceaccount:<namespace>:<name>, and auth can-i with --as evaluates RBAC for that identity. Using --as=build-bot is the tempting shortcut, but it checks a different, user-type subject.","Domain 1: RBAC and kubectl auth can-i"],
    ["x3",0,1,"The API server is down and you are on a kubeadm node that runs containerd. Which tool lists the Pod sandboxes and containers through the CRI, the same way kubelet sees them?",["crictl pods and crictl ps -a","docker ps -a","ctr -n default containers list","kubectl get pods -A -o wide --request-timeout=5s"],0,"crictl talks to the runtime over the CRI socket and works without the API server, which makes it the standard node-level debugging tool. ctr is tempting, but it is a low-level containerd client that needs the k8s.io namespace and does not understand Pods.","Domain 1: Extension interfaces (CRI and crictl)"],
    ["x4",0,1,"You install a CRD for Backup objects and create a ClusterRole backup-reader with get/list/watch on backups and the label rbac.authorization.k8s.io/aggregate-to-view: \"true\". What is the effect?",["It becomes the default role for new ServiceAccounts in every namespace","It only takes effect after you create a ClusterRoleBinding named view that points at it","Its rules are merged into the built-in view ClusterRole automatically","Nothing changes until you copy the rules into the view ClusterRole by hand"],2,"The built-in view, edit and admin ClusterRoles use aggregationRule label selectors, and a controller merges the rules of any ClusterRole carrying the matching label. Thinking a binding is still required is tempting, but existing bindings to view pick up the new rules immediately.","Domain 1: RBAC aggregated ClusterRoles"],
    ["x5",0,1,"You created and approved CertificateSigningRequest jane (signer kubernetes.io/kube-apiserver-client). How do you save her signed certificate to jane.crt?",["kubectl certificate approve jane --output=jane.crt","kubeadm certs renew jane > jane.crt","kubectl get csr jane -o jsonpath='{.status.certificate}' | base64 -d > jane.crt","Copy /etc/kubernetes/pki/jane.crt from the control plane node"],2,"Once approved, the signer puts the base64-encoded PEM certificate in status.certificate of the CSR, which you extract and decode. Looking in /etc/kubernetes/pki is tempting because cluster certificates live there, but user certificates are never stored on disk by Kubernetes.","Domain 1: Users via CertificateSigningRequests"],
    ["x6",0,1,"Jane now has jane.key and a signed jane.crt. What must you add to a kubeconfig so kubectl can act as her against cluster lab?",["set-credentials jane with the cert and key, then a context pairing lab with jane","Run kubectl create user jane --cert=jane.crt, which registers her in the API server","Create a ServiceAccount named jane and attach jane.crt to it as a token Secret","Copy jane.crt into /etc/kubernetes/pki so the API server trusts her from then on"],0,"kubectl config set-credentials adds the user with client-certificate and client-key, and set-context/use-context ties it to a cluster. Creating a ServiceAccount is tempting, but it is a different identity type and cannot carry her certificate.","Domain 1: kubeconfig files and users"],
    ["x7",0,1,"You are about to run kubeadm init for a cluster that will use Flannel. Which kubeadm init flag sets the Pod network range so the controller manager hands each node a podCIDR?",["--service-cidr","--apiserver-advertise-address","--control-plane-endpoint","--pod-network-cidr"],3,"--pod-network-cidr enables per-node podCIDR allocation, which plugins such as Flannel rely on, and must match the CNI configuration. --service-cidr is the tempting mix-up because it is also a CIDR, but it covers virtual Service IPs.","Domain 1: kubeadm init and CNI"],
    ["x8",0,1,"On a kubeadm control plane node you ran kubeadm certs renew all because certificates were close to expiry. What else must you do for the renewal to take effect?",["Nothing; every control plane component watches its certificate files and reloads them","Restart the control plane static Pods and recopy admin.conf where it is used","Run kubeadm upgrade apply to push the renewed certificates to every node","Approve the renewed certificates with kubectl certificate approve on each one"],1,"kubeadm certs renew rewrites the files, but the running kube-apiserver, controller manager, scheduler and etcd keep the old ones until restarted, and a kubeconfig copied from admin.conf still has the old client cert. Assuming automatic reload is the tempting mistake.","Domain 1: kubeadm certificates"],
    ["x9",0,1,"A CRD lists two versions: v1alpha1 (served: true, storage: false) and v1 (served: true, storage: true). What does storage: true on v1 mean?",["v1 is the only version clients can read and write through the API","v1 objects are saved on a PersistentVolume created by the operator","Both versions are written to etcd side by side for every object","They are stored in etcd as v1, whichever version clients use"],3,"In a CRD, served controls which versions the API exposes and exactly one version has storage: true, the form written to etcd. Reading it as the only usable version is tempting, but that is what served controls.","Domain 1: CustomResourceDefinitions"],
    ["x10",0,1,"The chart's values.yaml sets replicaCount: 1, prod.yaml sets replicaCount: 3, and you run helm upgrade --install web ./chart -f prod.yaml --set replicaCount=5. How many replicas are rendered?",["3","5","1","The command fails because replicaCount is set twice"],1,"Helm gives --set the highest precedence, then -f files, then the chart's values.yaml. The -f value of 3 is the tempting answer, but --set wins when both set the same key.","Domain 1: Helm values"],
    ["x11",0,1,"After helm upgrade of release ingress in namespace ingress broke the controller, you must return to the last working release. What is the Helm-native way?",["kubectl rollout undo deployment --all -n ingress","helm uninstall ingress, then install it again from scratch","helm history ingress -n ingress, then helm rollback ingress <revision> -n ingress","helm repo update, then helm upgrade ingress --previous"],2,"helm history lists release revisions and helm rollback redeploys a chosen one, keeping Helm's records in sync. kubectl rollout undo is tempting because it reverts Pods, but it ignores ConfigMaps and other chart objects and confuses the next Helm upgrade.","Domain 1: Helm"],
    ["x12",0,1,"A Kustomize prod overlay must run image registry.local/api at tag 2.4.0 instead of the base's tag, without editing the base manifests. Which kustomization.yaml field is meant for this?",["images, with name registry.local/api and newTag 2.4.0","configMapGenerator with a key named image","commonLabels with image: registry.local/api:2.4.0","namePrefix: v2-4-0- so the new tag is picked up by name"],0,"The images transformer rewrites the tag (or name or digest) of matching container images across all resources. commonLabels is the tempting distractor because it also applies everywhere, but it only edits metadata labels.","Domain 1: Kustomize"],
    ["x13",0,1,"A disk replacement on worker node03 is planned for later today. Right now you only need to stop new Pods from being placed there, and running Pods must stay put. Which command?",["kubectl cordon node03","kubectl drain node03 --ignore-daemonsets","kubectl taint nodes node03 maint=true:NoExecute","kubectl delete node node03"],0,"cordon marks the node unschedulable and leaves existing Pods alone; drain is cordon plus eviction. drain is tempting because it is the usual maintenance command, but it moves the Pods immediately.","Domain 1: Node maintenance"],
    ["x14",0,1,"Your team plans an external etcd cluster and wants the best fault tolerance for the number of machines it buys. Why are 3 or 5 members preferred over 4?",["etcd refuses to start a cluster that has an even number of members","With four members two leaders can be elected at the same time, causing split-brain","Four members need three for quorum, so they tolerate one failure, like three","kubeadm only lets you join odd numbers of control plane nodes"],2,"Quorum is a majority: floor(n/2)+1. Three members tolerate one failure, four still tolerate one, five tolerate two, so an even member adds cost without resilience. The idea that etcd rejects even counts is tempting but false.","Domain 1: HA control plane and etcd quorum"],
    ["x15",0,1,"kube-apiserver in a cluster runs 1.35. Under the Kubernetes version skew policy, which kubelet version is NOT allowed on a node?",["1.35","1.36","1.34","1.32"],1,"kubelet must never be newer than kube-apiserver and may be up to three minor versions older. 1.32 is tempting because it looks far behind, but three minors of skew is permitted, while any newer kubelet is not.","Domain 1: Version skew"],
    ["x16",0,2,"Deployment web is on revision 5 after several bad releases. You want to return to revision 2 specifically. Which commands do this?",["kubectl rollout restart deployment/web --revision=2","kubectl apply -f web.yaml --revision=2","kubectl scale deployment/web --replicas=2","kubectl rollout history deployment/web, then kubectl rollout undo deployment/web --to-revision=2"],3,"rollout history shows the stored revisions and rollout undo --to-revision rolls the template back to that one. rollout restart is tempting because it also triggers a rollout, but it keeps the current template.","Domain 2: Rolling updates and rollbacks"],
    ["x17",0,2,"Deployment api has replicas: 4 with strategy RollingUpdate, maxSurge: 1 and maxUnavailable: 0. How does a rollout to a new image proceed?",["All four old Pods are removed first and four new ones are created afterwards","Available Pods can drop to 3 at a time while each old Pod is replaced","Up to 5 Pods exist; an old Pod goes only after a new one is ready","The rollout stalls, since maxUnavailable: 0 leaves no room for any change"],2,"maxSurge caps extra Pods above replicas and maxUnavailable caps how many can be missing; with 1/0 the controller surges one new Pod, waits for readiness, then scales old down. Thinking the rollout stalls is tempting, but the surge makes progress possible.","Domain 2: Deployment rolling updates"],
    ["x18",0,2,"Namespace team-a has a ResourceQuota on requests.cpu and requests.memory. Many existing manifests set no requests, and those Pods are now rejected. What fixes this without editing every manifest?",["Raise the ResourceQuota's requests.cpu and requests.memory","A PriorityClass with globalDefault: true for the namespace","A HorizontalPodAutoscaler for each Deployment","A LimitRange in team-a that sets defaultRequest (and default limits) for CPU and memory"],3,"A LimitRange admission plugin fills in default requests and limits for containers that set none, so the quota can account for them. Raising the quota is tempting, but the rejection is about missing requests, not about running out of quota.","Domain 2: LimitRange and ResourceQuota"],
    ["x19",0,2,"A Pod carries the toleration key node.kubernetes.io/unreachable, operator Exists, effect NoExecute, tolerationSeconds: 300. Its node loses contact with the control plane. What happens?",["It stays bound for 300 seconds, then it is evicted","The Pod is evicted immediately because the node is unreachable","The Pod is never evicted because it tolerates the taint","The Pod is moved 300 seconds after the node comes back"],0,"When a node becomes unreachable, the node lifecycle controller adds a NoExecute taint; a toleration with tolerationSeconds keeps the Pod bound only for that long. Reading the toleration as permanent protection is the tempting error.","Domain 2: Taints and tolerations (NoExecute)"],
    ["x20",0,2,"You want the scheduler to avoid node spare1 when other nodes have room, but still use it if nothing else fits. Which taint effect does this?",["NoSchedule","PreferNoSchedule","NoExecute","SoftNoSchedule"],1,"PreferNoSchedule is a soft taint the scheduler tries to honour. NoSchedule is tempting because it sounds similar, but it blocks untolerated Pods completely.","Domain 2: Taints and tolerations"],
    ["x21",0,2,"Pod A mounts key app.conf of ConfigMap web-cfg with subPath. Pod B mounts the whole web-cfg as a volume. After you edit the ConfigMap, Pod B sees the change a little later but Pod A never does. Why?",["The ConfigMap was created with immutable: true","Files mounted with subPath do not receive ConfigMap updates","kubelet's ConfigMap sync loop is disabled on the node where Pod A runs","Pod A reads app.conf through environment variables"],1,"kubelet refreshes projected ConfigMap volumes over time, but a subPath mount is bound once and never updated, so the Pod must be restarted. Blaming env vars is tempting because they also ignore updates, but that is not how Pod A consumes it.","Domain 2: ConfigMaps"],
    ["x22",0,2,"Namespace batch has a ResourceQuota of pods: 10. You scale Deployment worker to 12 replicas while nothing else runs there. What do you observe?",["10 Pods run; the ReplicaSet logs FailedCreate for exceeded quota","12 Pods are created and 2 stay Pending with FailedScheduling events","The kubectl scale command is rejected and the Deployment keeps its old count","The two oldest Pods are evicted to make room for the new ones"],0,"ResourceQuota is an admission check: Pod creation beyond the limit is refused, and the ReplicaSet controller logs FailedCreate while it keeps retrying. Pending Pods with FailedScheduling are tempting, but that pattern belongs to node capacity, not quota.","Domain 2: ResourceQuota"],
    ["x23",0,2,"StatefulSet db has 3 replicas and the default Pod management policy. db-0 is Running but fails its readiness probe, and db-1 never appears. Why?",["StatefulSets wait until all PVCs for every replica are bound before creating any Pod","The headless Service named in serviceName has no endpoints yet","podManagementPolicy: Parallel is the default and it limits Pods to one at a time","OrderedReady creates each Pod only after the previous one is Running and Ready"],3,"With the default OrderedReady policy the controller creates db-0, waits for Running and Ready, then db-1 and so on; an unready db-0 blocks the rest. The headless Service is tempting because it is required, but it does not gate creation.","Domain 2: StatefulSets"],
    ["x24",0,2,"A batch team wants its Pods placed ahead of normal Pods in the scheduling queue, but they must never evict running Pods to make room. What should their PriorityClass set?",["A low value with globalDefault: true","A high value with preemptionPolicy: PreemptLowerPriority","A high value with preemptionPolicy: Never","A high value, plus a PodDisruptionBudget on every other workload"],2,"preemptionPolicy: Never keeps the queue priority from value but disables preemption. Adding PDBs is tempting, but preemption respects them only on a best-effort basis, so it is not a guarantee.","Domain 2: PriorityClasses and preemption"],
    ["x25",0,3,"StatefulSet db in namespace data uses serviceName db-hs, a headless Service. Which DNS name reaches only Pod db-0?",["db-0.data.svc.cluster.local","db-hs.db-0.data.svc.cluster.local","db-0.data.pod.cluster.local","db-0.db-hs.data.svc.cluster.local"],3,"A headless Service gives StatefulSet Pods stable records of the form <pod>.<service>.<namespace>.svc.<zone>. Dropping the Service part is tempting, but then the name has no record.","Domain 3: Headless Services and DNS"],
    ["x26",0,3,"Pods must reach an outside database at db.corp.example using the in-cluster name legacy-db, so the target can change later without touching the apps. Which Service type fits?",["NodePort pointing at port 5432","Headless with a selector matching app=legacy-db","ExternalName with externalName: db.corp.example","LoadBalancer with loadBalancerIP: db.corp.example"],2,"ExternalName makes CoreDNS answer the Service name with a CNAME to the outside host. A headless Service is tempting because it also skips a ClusterIP, but with a selector it only returns Pod addresses.","Domain 3: Service types (ExternalName)"],
    ["x27",0,3,"A legacy database runs on two VMs at 10.0.5.10 and 10.0.5.11, port 5432. Pods should use a normal ClusterIP Service named pgdb that load balances between them. How?",["An ExternalName Service whose externalName is 10.0.5.10,10.0.5.11","Create pgdb with no selector, plus an EndpointSlice labeled kubernetes.io/service-name: pgdb listing both IPs","A Service whose selector matches labels on the nodes hosting the VMs","A NetworkPolicy ipBlock allowing 10.0.5.0/24 on port 5432"],1,"Without a selector, Kubernetes does not manage endpoints, so you add an EndpointSlice tied to the Service by the kubernetes.io/service-name label and kube-proxy balances across it. ExternalName is tempting, but it only works as a DNS alias.","Domain 3: Services without selectors and EndpointSlices"],
    ["x28",0,3,"After applying a default-deny egress NetworkPolicy in namespace shop, apps can still reach allowed IPs by address, but every name lookup fails. What must you add?",["Egress to the CoreDNS Pods on UDP and TCP port 53","An ingress rule on the CoreDNS Pods allowing traffic from namespace shop","Restart CoreDNS so it picks up the new NetworkPolicy","Set dnsPolicy: Default on every Pod in shop"],0,"Default-deny egress blocks DNS traffic too, so policies usually add an egress rule to kube-dns on port 53 for both UDP and TCP. An ingress rule on CoreDNS is tempting, but the traffic is dropped leaving the client namespace.","Domain 3: NetworkPolicy egress"],
    ["x29",0,3,"A NetworkPolicy on db Pods has this ingress rule:\n\nfrom:\n- namespaceSelector:\n    matchLabels: {team: web}\n  podSelector:\n    matchLabels: {app: frontend}\n\nWhich sources are allowed?",["Only Pods labeled app=frontend that run in namespaces labeled team=web","All Pods in team=web namespaces, plus app=frontend Pods in the policy's namespace","Only app=frontend Pods in the policy's own namespace","Nothing, because one peer cannot combine the two selectors"],0,"Inside one from entry, namespaceSelector and podSelector are ANDed; written as two dash items they are ORed. The OR reading is the tempting trap because the YAML differs by a single dash.","Domain 3: NetworkPolicy selectors"],
    ["x30",0,3,"A cluster uses Flannel alone as its CNI. You apply a default-deny ingress NetworkPolicy, but Pods still reach each other. What is the explanation?",["The policy needs policyTypes: [Egress] as well before it applies","Existing Pods must be restarted before a new policy applies to them","NetworkPolicies filter Service traffic only, not direct Pod IPs","Flannel alone does not enforce NetworkPolicy objects"],3,"The API server stores NetworkPolicies, but the CNI enforces them; Flannel on its own provides connectivity without policy. Restarting Pods is tempting, but no restart helps if nothing enforces the rules.","Domain 3: NetworkPolicy and CNI"],
    ["x31",0,3,"An Ingress rule has path /api with pathType: Exact pointing at Service api. A request for /api/users returns 404 from the controller. Why?",["Exact matching is case-insensitive and needs a trailing slash","pathType Exact needs a rewrite-target annotation to reach Services","Exact matches only /api itself, not paths below it","Service api must be of type NodePort for Ingress to reach it"],2,"Exact requires the request path to equal the rule's path, while Prefix matches /api and anything under /api/ by path element. A rewrite annotation is tempting because it is common with NGINX, but it does not change which rule matches.","Domain 3: Ingress path types"],
    ["x32",0,3,"An Ingress in namespace shop has tls with secretName shop-tls, but the controller serves its default certificate. shop-tls exists only in namespace ingress-nginx. What is wrong?",["The Secret must be of type Opaque rather than kubernetes.io/tls","The Secret must be in the Ingress's own namespace","The backend Service must expose port 443 for TLS to work","The controller must be restarted after every new Ingress"],1,"An Ingress can only reference Secrets in its own namespace, so the controller cannot use shop-tls and falls back to its default certificate. Restarting the controller is tempting, but it would still not see the Secret.","Domain 3: Ingress TLS"],
    ["x33",0,3,"Using the Gateway API, requests with header x-canary: true must go to Service web-v2 and all others to web-v1. Where is this configured?",["On the Gateway, with a listener hostname of x-canary","In HTTPRoute rules: one with matches.headers for x-canary sending to web-v2, and a default rule to web-v1","In the GatewayClass parametersRef for the controller","As weights of 1 and 0 on the two backendRefs"],1,"HTTPRoute rules can match on headers, paths and query parameters and send each match to its own backendRefs. Weights are tempting because they also split traffic between versions, but they are random and cannot target a header.","Domain 3: Gateway API HTTPRoute matches"],
    ["x34",0,3,"On a kubeadm cluster, where do you check which proxy mode (iptables, IPVS or nftables) kube-proxy was configured with?",["The spec of Service kubernetes in the default namespace","The CNI config files in /etc/cni/net.d","The mode field of the kube-proxy ConfigMap","The Allocated resources section of kubectl describe node"],2,"kubeadm deploys kube-proxy as a DaemonSet that reads its KubeProxyConfiguration from the kube-proxy ConfigMap; an empty mode uses the platform default. The CNI directory is tempting, but kube-proxy and the CNI are configured separately.","Domain 3: kube-proxy modes"],
    ["x35",0,3,"Which Service fronts the CoreDNS Pods on a kubeadm cluster, and in which namespace?",["Service kube-dns in namespace kube-system","Service coredns in namespace default","Service kubernetes in namespace default","Service cluster-dns in namespace kube-public"],0,"For compatibility, CoreDNS is exposed through Service kube-dns in kube-system, and its ClusterIP is the address kubelet writes into Pods' resolv.conf. The name coredns is tempting because that is the Deployment's name.","Domain 3: CoreDNS"],
    ["x36",0,3,"A Pod in namespace web needs to call Service api, which lives in namespace backend. The short name api does not resolve. Which name should the app use?",["backend.api","api.svc","api.backend.pod.cluster.local","api.backend (or api.backend.svc.cluster.local)"],3,"A Pod's search path includes its own namespace, so short names only find local Services; other namespaces need <service>.<namespace>. backend.api is the tempting swap of the two labels.","Domain 3: Service DNS names"],
    ["x37",0,4,"A 3-replica Deployment spread over several nodes must write to one shared directory. Its PVC uses a block-storage class with ReadWriteOnce, and the second Pod hangs with a Multi-Attach error. What is the right fix?",["Change the PVC's access mode to ReadOnlyMany so all nodes can mount it","Set the PV's reclaim policy to Retain","Create another PVC with the same name in the same namespace","Use RWX-capable storage, such as NFS or a file-based CSI driver"],3,"ReadWriteOnce block volumes attach to one node at a time, so Pods on other nodes cannot mount them; shared writes need RWX-capable storage. ReadOnlyMany is tempting because it allows many nodes, but the Pods could no longer write.","Domain 4: Access modes"],
    ["x38",0,4,"Which PersistentVolume reclaim policy is deprecated, with dynamic provisioning recommended instead?",["Recycle","Retain","Delete","Archive"],0,"Recycle ran a basic scrub (rm -rf) and made the PV available again; it is deprecated in favour of dynamic provisioning. Retain is tempting because it is often avoided in production, but it is not deprecated.","Domain 4: Reclaim policies"],
    ["x39",0,4,"In a three-node lab cluster, a Pod using a hostPath PV at /data seems to lose its files whenever it is rescheduled. What is the cause?",["hostPath volumes are backed by tmpfs and cleared on every restart","hostPath is node-local, so another node has a different /data","The Delete reclaim policy wipes the directory each time the Pod restarts","ReadWriteOnce allows only one write before the volume becomes read-only"],1,"A hostPath PV has no node pinning by itself, so the scheduler can place the Pod anywhere and it mounts that node's /data. The reclaim policy is tempting to blame, but it only acts when the PVC is deleted.","Domain 4: hostPath and static PVs"],
    ["x40",0,4,"A cluster has a default StorageClass. You pre-created PV legacy-pv with no storageClassName and want a new PVC to bind to it rather than trigger dynamic provisioning. What must the PVC set?",["Nothing: leave storageClassName out so it matches the PV's empty class","volumeBindingMode: Immediate in the PVC spec","storageClassName: \"\" explicitly (plus matching size and access mode)","accessModes: [ReadWriteOncePod] so it can only bind statically"],2,"An explicit empty string means no class, which matches a PV without a class; omitting the field lets the DefaultStorageClass admission plugin add the default. Omitting the field is the tempting answer because it looks like no class.","Domain 4: PV and PVC binding"],
    ["x41",0,4,"StatefulSet cache should delete its PVCs automatically when the StatefulSet itself is deleted, but keep them when it is scaled down. What do you configure?",["reclaimPolicy: Delete on the StorageClass","podManagementPolicy: Parallel","persistentVolumeClaimRetentionPolicy with whenDeleted: Delete and whenScaled: Retain","An ownerReference from each Pod to its PVC"],2,"The StatefulSet PVC retention policy controls what happens to claims from volumeClaimTemplates on delete and on scale-down. StorageClass reclaimPolicy is tempting, but it acts on PVs after claims are removed, not on the claims.","Domain 4: StatefulSet volumeClaimTemplates"],
    ["x42",0,4,"A new PVC stays Pending. Its StorageClass names a CSI provisioner, and the events say it is waiting for the external provisioner to create a volume. Other StorageClasses work. What should you check?",["Whether the worker nodes still carry a storage-related NoSchedule taint","The CSI driver's controller Pods are installed and running","That the PVC uses ReadWriteOncePod, which CSI requires","That kube-proxy is running on the control plane"],1,"With CSI, an external-provisioner sidecar in the driver's controller Pod watches PVCs and creates volumes; if it is missing or failing, claims wait forever. Checking access modes is tempting, but the event points to the missing provisioner.","Domain 4: CSI drivers and dynamic provisioning"],
    ["x43",0,5,"A Pod is in ImagePullBackOff. kubectl describe shows \"pull access denied ... may require authorization\" for an image in the company's private registry. What fixes it?",["Raise the container's memory limit","Set restartPolicy: Always so kubelet keeps retrying","Run crictl rmi --prune on the node","Create a docker-registry Secret with the credentials and reference it in imagePullSecrets"],3,"The registry is refusing an anonymous pull, so the Pod needs a kubernetes.io/dockerconfigjson Secret listed in imagePullSecrets (or on its ServiceAccount). Retrying is tempting, but without credentials every retry fails the same way.","Domain 5: Troubleshooting Pods"],
    ["x44",0,5,"Pod api is in CrashLoopBackOff and its current container has not written anything yet. How do you read the output of the container instance that just crashed?",["kubectl logs api --previous","kubectl logs api -f","kubectl get events --previous","kubectl describe pod api --logs"],0,"--previous (-p) returns the logs of the last terminated container instance, which is where the crash message is. Following with -f is tempting, but it attaches to the new, empty instance.","Domain 5: Container output streams"],
    ["x45",0,5,"A container restarts repeatedly. kubectl describe shows Last State: Terminated, Reason: OOMKilled, Exit Code: 137. What is the most direct remedy?",["Raise the container's memory limit, or fix the app's memory use","Free disk space on the node because of DiskPressure","Increase the CPU limit, since CPU throttling kills the container","Lengthen the liveness probe timeout"],0,"OOMKilled means the container exceeded its memory limit (or the node ran out) and the kernel killed it, giving exit 137. The liveness probe is tempting because it also restarts containers, but its events and reason are different.","Domain 5: Troubleshooting Pods"],
    ["x46",0,5,"kubectl logs web-7d9 fails with \"a container name must be specified\". How do you read the output?",["kubectl logs deployment/web-7d9","kubectl logs web-7d9 --previous","kubectl logs web-7d9 -c <container>, or --all-containers","kubectl describe pod web-7d9 --container"],2,"The Pod has several containers, so kubectl needs -c to choose one or --all-containers for all of them. --previous is tempting after a crash, but it does not solve the ambiguity.","Domain 5: Container output streams"],
    ["x47",0,5,"Pod app shows Init:0/1 for ten minutes. Its init container wait-db loops on nslookup db until it resolves. What is the first useful command?",["kubectl logs app","kubectl rollout restart deployment/app","kubectl top pod app","kubectl logs app -c wait-db"],3,"Init containers must finish before app containers start, and their logs are read with -c <init name>. Plain kubectl logs is tempting, but it targets the main container that is still waiting.","Domain 5: Troubleshooting Pods"],
    ["x48",0,5,"Service web's EndpointSlice lists ready Pod IPs on port 80, but curl to the Service gets \"connection refused\". The containers listen on 8080. What do you change?",["Fix the Service selector so it matches the Pod labels","Set the Service's targetPort to 8080","Remove the NetworkPolicy in the namespace","Restart kube-proxy on every node"],1,"Traffic is forwarded to the endpoint port taken from targetPort, and nothing listens on 80, so the kernel refuses the connection. The selector is the tempting suspect, but endpoints would be empty if it were wrong.","Domain 5: Service troubleshooting"],
    ["x49",0,5,"A task asks for each Pod's name and the node it runs on, as two labelled columns. Which command gives that?",["kubectl get pods -o wide --columns=name,node","kubectl get pods -o custom-columns=NAME:.metadata.name,NODE:.spec.nodeName","kubectl get pods -o jsonpath='{.items[*].NAME}'","kubectl top pods --containers"],1,"custom-columns takes HEADER:jsonpath pairs, and .spec.nodeName holds the node. -o wide is tempting because it shows the node, but it prints many extra columns and takes no column selection.","Domain 5: Output handling"],
    ["x50",0,5,"On a new admin workstation, kubectl get nodes prints \"The connection to the server localhost:8080 was refused\". What does this usually mean?",["The API server crashed and has fallen back to its insecure port","etcd is down, so the API server refuses connections","kube-proxy is not running on the workstation","No kubeconfig was found, so set KUBECONFIG or copy admin.conf"],3,"When no kubeconfig is found, kubectl falls back to localhost:8080, which nothing serves. Blaming a crashed API server is tempting, but a real outage would show the configured address, usually on 6443.","Domain 5: Cluster access problems"],
    ["x51",0,5,"kubectl top nodes returns \"error: Metrics API not available\". What is the most likely cause?",["kubelet is down on every node","The kubeconfig lacks a metrics context","metrics-server is not installed or not running","kube-proxy is in IPVS mode"],2,"kubectl top reads the metrics.k8s.io API, which metrics-server provides through API aggregation. A kubelet outage is tempting, but it would show as NotReady nodes rather than a missing API.","Domain 5: Resource monitoring"],
    ["x52",0,5,"Pod db stays Pending with the event \"pod has unbound immediate PersistentVolumeClaims\". What is the next step?",["kubectl describe pvc for that claim to see why it is not bound","Remove taints from the worker nodes","Give the Pod a higher PriorityClass","Restart the kube-scheduler static Pod"],0,"With Immediate binding the claim must be bound before scheduling, so the PVC's events explain the real problem, such as no matching PV or a missing StorageClass. Removing taints is tempting for Pending Pods, but that is not what this event says.","Domain 5: Pending Pods"],
    ["x53",0,5,"kubectl rollout status deployment/web ends with \"exceeded its progress deadline\". The new ReplicaSet's Pods are in ImagePullBackOff due to a tag typo, while the old Pods still serve. What restores the last good state cleanly?",["Delete the new ReplicaSet with kubectl delete rs","Scale the Deployment to 0 and back up","kubectl rollout undo deployment/web","Delete the failing Pods so they are retried"],2,"rollout undo resets the Deployment's Pod template to the previous revision, so the controller scales the good ReplicaSet back and the bad one down. Deleting the new ReplicaSet is tempting, but the Deployment recreates it from its template.","Domain 5: Troubleshooting workloads"],
    ["x54",0,5,"You need all events related to Pod web-0 in namespace shop. Which command filters them?",["kubectl get events -n shop --field-selector involvedObject.name=web-0","kubectl get events -n shop -l pod=web-0","kubectl logs web-0 -n shop --events","kubectl get pod web-0 -n shop -o events"],0,"Events reference their subject in involvedObject, which supports field selectors. A label selector is tempting, but events do not carry the Pod's labels.","Domain 5: Events"],
    ["x55",0,5,"A Pod stays Pending with no events at all, while other new Pods in the same namespace schedule normally. What should you check first?",["Whether kube-scheduler is down","spec.schedulerName, in case it names a scheduler that is not running","Taints on the worker nodes","The namespace ResourceQuota"],1,"A Pod that asks for a non-existent scheduler is ignored by the default one, so it stays Pending with no events. A scheduler outage is tempting, but it would stop every new Pod, not just this one.","Domain 5: Pending Pods"],
    ["x56",0,5,"A new Pod shows CreateContainerConfigError, and kubectl describe says secret \"db-cred\" not found. What fixes it?",["Add imagePullSecrets for the registry","Restart kubelet on the node","Add a toleration so the Pod can move to another node","Create Secret db-cred in the Pod's namespace"],3,"kubelet cannot build the container config because a referenced Secret is missing; once it exists, kubelet retries and starts the container. imagePullSecrets is tempting because it also mentions secrets, but it only covers registry credentials.","Domain 5: Troubleshooting Pods"],
    ["x57",0,5,"A Java service needs about 90 seconds to start. Its liveness probe starts after 10 seconds and fails three times 10 seconds apart, so the container is killed before it ever finishes starting. What is the best fix?",["Replace the liveness probe with a readiness probe only","Set restartPolicy: Never on the Pod","Lower the liveness probe's periodSeconds to 1","Add a startupProbe that allows enough time to start"],3,"A startupProbe suspends liveness and readiness checks until the app has started, then liveness protects it as before. Dropping liveness for readiness is tempting and also stops the kills, but the app loses self-healing for hangs.","Domain 5: Troubleshooting workloads"],
    ["x58",0,5,"New Pods avoid worker3. kubectl describe node shows taint node.kubernetes.io/disk-pressure:NoSchedule, which nobody added. What is going on?",["Someone ran kubectl cordon worker3","It follows the node's DiskPressure condition; free space to clear it","Remove it with kubectl taint nodes worker3 node.kubernetes.io/disk-pressure-","kube-scheduler adds it after scheduling too many Pods to a node"],1,"Nodes are tainted automatically by condition (taint-nodes-by-condition), so the taint follows the DiskPressure condition. Removing the taint by hand is tempting, but it comes back until the real problem is fixed.","Domain 5: Node problems"],
    ["x59",0,5,"You need the recent logs from every Pod of Deployment web (label app=web), with each line showing which Pod wrote it. Which command?",["kubectl logs -l app=web --prefix","kubectl logs deployment/web","kubectl logs web --all-containers","kubectl get events -l app=web --sort-by=.lastTimestamp"],0,"A label selector with -l reads logs from all matching Pods, and --prefix tags each line with its source. kubectl logs deployment/web is tempting because it accepts the Deployment, but it chooses just one Pod.","Domain 5: Container output streams"],
    ["x60",0,5,"For a report you must list all PersistentVolumes ordered by capacity. Which command sorts them?",["kubectl get pv --sort-by=capacity","kubectl top pv","kubectl get pv --sort-by=.spec.capacity.storage","kubectl get pv -o jsonpath='{sort(.items[*].spec.capacity)}'"],2,"--sort-by takes a JSONPath to the field, and capacity lives at .spec.capacity.storage. Using the column name CAPACITY is tempting, but kubectl does not accept column names there.","Domain 5: Output handling"]
  ],
  whys: {
    "x1": ["A kustomization.yaml is not a Kubernetes object, so applying it with -f fails instead of building the overlay.",null,"Helm expects a chart with Chart.yaml and templates; a Kustomize directory is not a chart.","A server-side dry run only validates and prints the objects; nothing is persisted in the cluster."],
    "x2": ["--as=build-bot impersonates a user literally named build-bot, not the ServiceAccount, so the answer can be misleading.","auth whoami only reports who the request is authenticated as; it says nothing about permissions.","Listing bindings requires reading them yourself and still misses ClusterRoleBindings and aggregated rules.",null],
    "x3": [null,"containerd-based nodes normally have no Docker Engine, and Docker would not show CRI-managed containers anyway.","Kubernetes containers live in containerd's k8s.io namespace, so listing the default namespace shows nothing useful.","kubectl still needs the API server, which is down in this scenario."],
    "x4": ["ServiceAccounts get no roles automatically; labels on a ClusterRole never create bindings.","Aggregation is done by the controller that fills view's rules; no extra binding to the new role is needed.",null,"The aggregation controller copies matching rules automatically, and manual edits to view would be overwritten."],
    "x5": ["certificate approve only sets the Approved condition; it has no option to write the issued certificate to a file.","kubeadm certs renew handles the cluster's own component certificates, not user CSRs.",null,"The signer returns the certificate in the CSR object's status; nothing is written to the pki directory."],
    "x6": [null,"Kubernetes has no User objects; users exist only as names in certificates or tokens, and there is no create user command.","ServiceAccount tokens are a separate mechanism; a client certificate cannot be stored as one.","The API server trusts client certificates signed by its CA; copying her certificate there does nothing."],
    "x7": ["This sets the range for Service ClusterIPs, not the Pod addresses the CNI hands out.","This only picks the IP the local API server advertises to other members.","This sets the shared address for HA API servers and has nothing to do with Pod IPs.",null],
    "x8": ["The docs require restarting the control plane Pods after renewal; they do not all hot-reload their serving certificates.",null,"upgrade apply upgrades versions; renewal is per control plane node and nothing is pushed to workers.","kubeadm signs these with the cluster CA directly; no CSR objects are created for them."],
    "x9": ["Both versions have served: true, so clients can use either one through the API.","Custom resources are stored in etcd by the API server; no volume is involved.","Exactly one version may be the storage version; objects are stored once in that version.",null],
    "x10": ["Values from -f files override chart defaults but are themselves overridden by --set.",null,"Chart defaults have the lowest precedence and are overridden by both -f and --set.","Helm merges values in order of precedence; setting the same key in several places is normal."],
    "x11": ["This reverts Deployments behind Helm's back, so Helm's release record and other objects stay on the broken revision.","Uninstalling removes all release resources and causes an outage; rollback restores the old revision in place.",null,"There is no --previous flag, and updating the repo only refreshes the chart index."],
    "x12": [null,"A generated ConfigMap only produces data for Pods; it never changes container images.","commonLabels adds labels and selectors, it does not touch the image field.","namePrefix only renames resources; the containers keep their original images."],
    "x13": [null,"drain also evicts the running Pods, which is more than the task asks for right now.","A NoExecute taint evicts Pods that do not tolerate it, so running workloads would move.","Deleting the Node object removes it from the cluster and its Pods get cleaned up."],
    "x14": ["etcd runs with an even member count; it is just no more fault tolerant than the odd count below it.","Raft needs a majority (three of four) to elect a leader, so two simultaneous leaders cannot happen.",null,"kubeadm has no such rule; the guidance comes from quorum math, not from a tool limit."],
    "x15": ["A kubelet at the same minor version as the API server is always supported.",null,"kubelet may be older than kube-apiserver, and one minor version older is well within the policy.","The policy allows kubelet to be up to three minor versions older than kube-apiserver, so 1.32 is still supported."],
    "x16": ["rollout restart has no revision flag; it only restarts Pods on the current template.","kubectl apply does not accept a revision; it applies whatever the file contains.","This changes the replica count, not the Pod template revision.",null],
    "x17": ["That is the Recreate strategy; RollingUpdate replaces Pods gradually.","maxUnavailable: 0 forbids going below 4 available Pods during the update.",null,"maxSurge: 1 gives the room: a new Pod is created above the desired count first."],
    "x18": ["A quota on requests forces every Pod to declare them; raising the totals does not add requests to Pods that lack them.","Priority affects scheduling order and preemption, not the requests the quota needs.","An HPA scales replicas; it cannot add resource requests to rejected Pods.",null],
    "x19": [null,"The toleration exists to delay that eviction; only Pods without it are evicted at once.","tolerationSeconds limits how long the toleration holds; without it the Pod would stay forever.","Eviction is timed from when the taint is added, not from recovery."],
    "x20": ["NoSchedule is a hard rule: Pods without a toleration are never placed there.",null,"NoExecute also evicts running Pods that do not tolerate it, which is far stronger.","There is no SoftNoSchedule effect; the soft version is called PreferNoSchedule."],
    "x21": ["An immutable ConfigMap could not have been edited, and Pod B would not see a change either.",null,"Nothing points to a node problem; subPath behaviour explains the difference fully.","The scenario says Pod A mounts the key as a file, not as an env var."],
    "x22": [null,"Quota is enforced at admission, so the extra Pods are never created and never reach the scheduler.","The Deployment object itself is not counted by the pods quota, so scaling succeeds.","ResourceQuota never evicts running Pods; it only rejects new ones."],
    "x23": ["PVCs are created per Pod as it is created; db-0 already exists, so this is not the blocker.","The governing Service provides DNS; its endpoints do not gate Pod creation.","OrderedReady is the default, and Parallel would create all Pods at once.",null],
    "x24": ["globalDefault makes it apply to Pods with no class and a low value puts them behind others.","That is the default and allows evicting lower-priority Pods, which the team must avoid.",null,"PDBs guard voluntary disruptions such as drain; scheduler preemption does not guarantee to respect them."],
    "x25": ["Per-Pod names sit under the governing Service name, which is missing here.","The labels are reversed; the Pod's hostname comes before the Service name.","The pod zone uses dashed IP addresses, not Pod names, and is not how StatefulSet Pods are addressed.",null],
    "x26": ["NodePort exposes cluster Pods outward; it does not alias an outside host.","A selector-based headless Service lists cluster Pods, and there are none for an external host.",null,"LoadBalancer requests an external address for cluster Pods; it does not alias an outside DNS name."],
    "x27": ["externalName must be a DNS name, not a list of IPs, and it would not give a ClusterIP.",null,"Service selectors match Pods, never Nodes or external machines.","A NetworkPolicy only filters traffic; it creates no Service or endpoints."],
    "x28": [null,"Ingress to kube-system is not restricted here; the block is shop's own egress.","CoreDNS is healthy; the packets never leave the shop Pods.","That sends queries to the node's resolvers, which egress still blocks, and loses cluster names."],
    "x29": [null,"That is the meaning when the two selectors are separate list items (OR); here they are in one item (AND).","The namespaceSelector widens the search to team=web namespaces instead of the local one.","Combining namespaceSelector and podSelector in one peer is valid and means both must match."],
    "x30": ["A policy with Ingress in policyTypes and no rules already denies ingress on a supporting CNI.","Enforcing CNIs apply policies to running Pods without restarts.","Policies select Pods and filter by Pod traffic, whether it arrives via a Service or not.",null],
    "x31": ["Exact is case-sensitive and matches the full path exactly; slashes are not added.","No annotation is needed for a match; the path simply does not match.",null,"Ingress controllers route to ClusterIP Services normally."],
    "x32": ["TLS Secrets for Ingress use type kubernetes.io/tls with tls.crt and tls.key.",null,"TLS terminates at the controller; the backend can stay on plain HTTP.","Ingress controllers watch the API and pick up new Ingresses automatically."],
    "x33": ["Listeners define ports, protocols and hostnames; they do not match request headers.",null,"GatewayClass picks the implementation; per-request routing lives in routes.","Weights split traffic by percentage, not by header value."],
    "x34": ["That Service fronts the API server and holds no kube-proxy settings.","Those configure the CNI plugin; kube-proxy's mode is separate.",null,"This shows reserved CPU and memory, not networking settings."],
    "x35": [null,"The Deployment is named coredns, but the Service keeps the historical name kube-dns and is in kube-system.","That Service points at the API server, not at DNS.","No such Service exists; kube-public holds public cluster info, not DNS."],
    "x36": ["The order is service first, then namespace.","The svc label must follow a namespace; api.svc does not name any Service.","The pod zone is for Pod IP records, not Service names.",null],
    "x37": ["The Pods need to write, and ReadOnlyMany mounts are read-only.","Reclaim policy only matters after the claim is deleted; it has nothing to do with attaching.","Names are unique per namespace, and a second claim would be a different volume anyway.",null],
    "x38": [null,"Retain is fully supported and keeps the volume for manual recovery.","Delete is the normal default for dynamically provisioned volumes.","There is no Archive reclaim policy in Kubernetes."],
    "x39": ["hostPath mounts a real directory on the node's filesystem; it is not memory-backed by default.",null,"Reclaim policies act when the claim is deleted, not when a Pod restarts.","RWO limits attachment to one node; it does not limit the number of writes."],
    "x40": ["An omitted field gets the default class filled in by admission, so it no longer matches the classless PV.","volumeBindingMode is a StorageClass field, not a PVC field.",null,"Access mode does not decide between static and dynamic binding."],
    "x41": ["This deletes a PV once its PVC is gone, but StatefulSet PVCs are not deleted in the first place.","This only changes the order in which Pods are created and removed.",null,"Pods are recreated often; tying PVCs to them would delete data on every Pod replacement."],
    "x42": ["The event points at the provisioner, and node taints never stop a CSI provisioner from creating a volume.",null,"CSI drivers support several access modes; RWOP is not required.","kube-proxy handles Service traffic; it plays no part in provisioning."],
    "x43": ["Memory limits affect running containers; the image has not even been downloaded.","kubelet already retries with backoff; the pull will keep failing without credentials.","Pruning deletes unused local images and does nothing about registry authorization.",null],
    "x44": [null,"Following streams the current container, which has not logged anything yet.","get events has no --previous flag and events do not hold container output.","describe has no --logs option; it shows state and events, not stdout."],
    "x45": [null,"Disk pressure causes Pod evictions, not an OOMKilled container reason.","CPU limits throttle but never kill a container.","A liveness failure shows a different restart reason; OOMKilled means the kernel killed it for memory."],
    "x46": ["The name is a Pod, and the error is about picking a container, not the resource type.","--previous changes which instance is read but still needs a container name.",null,"describe has no container flag and does not print logs."],
    "x47": ["Without -c this targets the app container, which has not started yet.","Restarting creates new Pods that hit the same wait without telling you why.","Resource usage does not explain why a name lookup keeps failing.",null],
    "x48": ["Ready endpoints are listed, which proves the selector already matches.",null,"A blocking policy usually causes timeouts, and nothing points to one here.","kube-proxy is forwarding correctly; it forwards to the wrong port because of the Service spec."],
    "x49": ["There is no --columns flag; -o wide prints a fixed set of columns.",null,"Field names are lowercase paths like .metadata.name; NAME does not exist.","top shows CPU and memory usage, not node placement."],
    "x50": ["kubeadm API servers listen on 6443 and have no insecure port; localhost:8080 is kubectl's fallback.","An etcd problem shows errors from the real API server address, not localhost:8080.","kubectl talks to the API server directly and does not use kube-proxy.",null],
    "x51": ["Nodes would show NotReady; the error names the missing Metrics API.","There is no separate metrics context; top uses the normal API server connection.",null,"The proxy mode does not affect whether the Metrics API is served."],
    "x52": [null,"The message names the claim, not a taint; the scheduler cannot place the Pod until the claim binds.","Priority cannot bind a volume, and preemption would not help.","The scheduler is doing its job by reporting the unbound claim."],
    "x53": ["The Deployment still has the bad template and immediately recreates the ReplicaSet.","Scaling keeps the broken template, so all Pods would come back with the bad tag.",null,"Replacements use the same bad image tag and fail the same way."],
    "x54": [null,"Events are not labelled with the Pod name, so a label selector finds nothing.","kubectl logs has no --events flag; logs are container output.","events is not an output format."],
    "x55": ["Other new Pods schedule fine, so the default scheduler is working.",null,"The default scheduler would log a FailedScheduling event for taints.","Quota rejections stop the Pod from being created at all, so it would not exist as Pending."],
    "x56": ["The image is fine; the failure is about a missing Secret the container config needs.","kubelet reports the problem correctly and retries on its own once the Secret exists.","The Pod is already scheduled, and a missing Secret is not node-specific.",null],
    "x57": ["This avoids the kills but loses automatic restarts if the app hangs later.","Deployments require Always, and a killed container would simply stay dead.","Probing more often makes the failures pile up faster and kills the container sooner.",null],
    "x58": ["cordon sets the unschedulable taint, not a disk-pressure taint.",null,"The node lifecycle controller re-adds the taint while the condition persists.","The scheduler never taints nodes; it only reads taints."],
    "x59": [null,"This picks one Pod of the Deployment, not all of them.","web is not a Pod name, and --all-containers covers containers of one Pod.","Events describe object changes, not container output."],
    "x60": ["--sort-by takes a JSONPath expression, not a column header.","top only covers nodes and Pods.",null,"kubectl JSONPath has no sort function."]
  },
  levels: {
    "cka1": 2, "cka2": 2, "cka3": 2, "cka4": 1, "cka5": 1, "cka6": 1, "cka7": 2, "cka8": 2, "cka9": 1, "cka10": 1,
    "cka11": 1, "cka12": 2, "cka13": 1, "cka14": 1, "cka15": 1, "cka16": 2, "cka17": 3, "cka18": 3, "cka19": 1, "cka20": 2,
    "cka21": 2, "cka22": 1, "cka23": 2, "cka24": 3, "cka25": 2, "cka26": 1, "cka27": 3, "cka28": 2, "cka29": 2, "cka30": 2,
    "cka31": 3, "cka32": 1, "cka33": 3, "cka34": 2, "cka35": 2, "cka36": 1, "cka37": 2, "cka38": 1, "cka39": 1, "cka40": 3,
    "cka41": 1, "cka42": 2, "cka43": 1, "cka44": 2, "cka45": 3, "cka46": 1, "cka47": 3, "cka48": 1, "cka49": 2, "cka50": 2,
    "cka51": 2, "cka52": 2, "cka53": 2, "cka54": 3, "cka55": 2, "cka56": 1, "cka57": 3, "cka58": 1, "cka59": 2, "cka60": 2,
    "cka61": 1, "cka62": 2, "cka63": 3, "cka64": 2, "cka65": 1, "cka66": 1, "cka67": 2, "cka68": 2, "cka69": 3, "cka70": 2,
    "cka71": 3, "cka72": 3, "cka73": 1, "cka74": 1, "cka75": 2, "cka76": 2, "cka77": 2, "cka78": 2, "cka79": 2, "cka80": 2,
    "cka81": 3, "cka82": 3, "cka83": 1, "cka84": 2, "cka85": 2, "cka86": 2, "cka87": 3, "cka88": 2, "cka89": 1, "cka90": 2,
    "cka91": 2, "x1": 1, "x2": 2, "x3": 1, "x4": 3, "x5": 2, "x6": 2, "x7": 1, "x8": 3, "x9": 2,
    "x10": 2, "x11": 1, "x12": 2, "x13": 1, "x14": 3, "x15": 2, "x16": 1, "x17": 2, "x18": 3, "x19": 2,
    "x20": 1, "x21": 3, "x22": 2, "x23": 2, "x24": 3, "x25": 2, "x26": 1, "x27": 3, "x28": 2, "x29": 3,
    "x30": 2, "x31": 2, "x32": 2, "x33": 2, "x34": 1, "x35": 1, "x36": 1, "x37": 3, "x38": 1, "x39": 2,
    "x40": 3, "x41": 2, "x42": 2, "x43": 2, "x44": 1, "x45": 2, "x46": 1, "x47": 2, "x48": 2, "x49": 1,
    "x50": 2, "x51": 1, "x52": 2, "x53": 3, "x54": 1, "x55": 3, "x56": 2, "x57": 3, "x58": 2, "x59": 2,
    "x60": 1
  }
});
