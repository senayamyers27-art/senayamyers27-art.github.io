/* More pcep practice questions with wrong-answer notes, and a difficulty level for every question. Merged into the free bank by tools/build.js. */
CertHub.addExtra("pcep", {
  questions: [
    ["x1", 0, 1, "What does this code print?\nprint(-2 ** 2)", ["4", "0.25", "-4", "A SyntaxError"], 2, "** binds more tightly than unary minus, so this is -(2 ** 2), which is -4. Getting 4 assumes the minus sign is applied first, as in (-2) ** 2.", "Domain 1: Operator priority"],
    ["x2", 0, 1, "What does this code print?\nprint(int(7.9), int(-7.9))", ["7 -7", "8 -8", "7 -8", "8 -7"], 0, "int() on a float truncates toward zero, dropping the fraction, so 7.9 gives 7 and -7.9 gives -7. The tempting 7 -8 would come from rounding down (floor), which int() does not do.", "Domain 1: Type casting"],
    ["x3", 0, 1, "A program runs without any error message but prints the wrong average because it divides by the wrong count. In PCEP terms, what kind of error is this?", ["A lexical error", "A syntax error", "An indentation error", "A semantic (logic) error"], 3, "The code is valid Python and runs, but its meaning is wrong, which is a semantic or logic error. A syntax error would stop the program before it ran at all.", "Domain 1: Lexis, syntax and semantics"],
    ["x4", 0, 1, "What does this code print?\nprint(\"ab\" * 3 + \"c\")", ["ab3c", "abababc", "ababab c", "A TypeError"], 1, "* repeats a string, so \"ab\" * 3 is \"ababab\", and + joins \"c\" with no space. A TypeError would occur only when adding a string and a number.", "Domain 1: String operators"],
    ["x5", 0, 1, "What does this code print?\nx = 10\nx //= 3\nx **= 2\nprint(x)", ["3", "9", "11.1", "9.0"], 1, "10 // 3 is the integer 3, and 3 ** 2 is 9. Floor division of two ints gives an int, so the result is 9, not 9.0.", "Domain 1: Compound assignment operators"],
    ["x6", 0, 1, "What does this code print?\nprint(float(\"1e2\") + int(\"3\"))", ["1e23", "103", "A ValueError", "103.0"], 3, "float() accepts scientific notation, so \"1e2\" becomes 100.0; adding the int 3 gives the float 103.0. The result is a float, not the int 103, because one operand is a float.", "Domain 1: Scientific notation and casting"],
    ["x7", 0, 1, "A user types 20 and presses Enter. What happens?\nage = input(\"Age: \")\nprint(age + 1)", ["A TypeError is raised", "21 is printed", "201 is printed", "20 1 is printed"], 0, "input() always returns a string, and adding a str and an int raises a TypeError. Printing 21 would require converting with int(age) first.", "Domain 1: Console input"],
    ["x8", 0, 1, "What does this code print?\nprint(3 == 3.0, 10 / 5 == 2)", ["False True", "True False", "True True", "False False"], 2, "Python compares int and float by value, so 3 == 3.0 is True, and 10 / 5 is 2.0, which equals 2. Different types do not make equal numbers compare unequal.", "Domain 1: Relational operators"],
    ["x9", 0, 1, "What does this code print?\nCount = 1\ncount = 2\nprint(Count + count)", ["4", "2", "3", "A NameError"], 2, "Python names are case-sensitive, so Count and count are two different variables holding 1 and 2. Printing 4 assumes the second line overwrote the first variable.", "Domain 1: Variables and naming rules"],
    ["x10", 0, 1, "What does this code print?\nprint(1 + 2 * 3 > 6 and not False)", ["True", "False", "7", "A TypeError"], 0, "Arithmetic is evaluated first (7), then the comparison (7 > 6 is True), then not False is True, and True and True is True. The expression never prints 7 because the comparison turns it into a Boolean.", "Domain 1: Operator priority"],
    ["x11", 0, 1, "What does this code print?\nprint(5 ^ 1, 8 >> 2)", ["6 16", "4 16", "6 2", "4 2"], 3, "5 is 101 in binary, and XOR with 001 gives 100, which is 4; 8 >> 2 shifts right two places, dividing by 4 to give 2. Reading ^ as addition or >> as multiplication leads to the wrong pairs.", "Domain 1: Bitwise operators"],
    ["x12", 0, 2, "What does this code print?\nx = \"\"\nif x:\n    print(\"A\")\nelif not x:\n    print(\"B\")\nelse:\n    print(\"C\")", ["A", "B", "C", "Nothing"], 1, "An empty string is falsy, so if x fails and not x is True, printing B. The else branch runs only when every earlier condition is false.", "Domain 2: Truthy and falsy values"],
    ["x13", 0, 2, "What does this code print?\nfor i in range(1, 10):\n    if i * i > 20:\n        break\nprint(i)", ["4", "5", "9", "25"], 1, "The loop stops at the first i whose square exceeds 20: 5 * 5 = 25. After break, i keeps its value 5, not the last value in the range.", "Domain 2: The loop variable after a loop"],
    ["x14", 0, 2, "What does this code print?\ncount = 0\nfor i in range(3):\n    while True:\n        count += 1\n        break\nprint(count)", ["1", "It never stops", "0", "3"], 3, "break exits only the innermost loop, the while True, after one pass, so each of the three for iterations adds 1. It does not loop forever because the break runs every time.", "Domain 2: break in nested loops"],
    ["x15", 0, 2, "What does this code print?\nfor n in [2, 4, 6]:\n    if n % 2:\n        break\nelse:\n    print(\"all even\")", ["all even", "Nothing", "2 4 6", "all even, three times"], 0, "Each n % 2 is 0, which is falsy, so break never runs and the else clause runs once after the loop. The else belongs to the for loop, not the if, so it does not run per item.", "Domain 2: for-else"],
    ["x16", 0, 2, "What does this code print?\nfor i in range(10, 0, -3):\n    print(i, end=\" \")", ["10 7 4", "9 6 3", "10 7 4 1", "10 7 4 1 -2"], 2, "The range starts at 10 and steps by -3 while the value stays above the stop value 0: 10, 7, 4, 1. The stop value is excluded, so -2 is never produced.", "Domain 2: range() with a negative step"],
    ["x17", 0, 2, "What does this code print?\nprint(0 or \"x\", 5 and 0)", ["False 0", "True False", "x 0", "0 5"], 2, "or returns the first truthy operand (or the last one), giving \"x\"; and returns the first falsy operand (or the last one), giving 0. They return operands, not forced True or False values.", "Domain 2: Truthy values with and/or"],
    ["x18", 0, 2, "What does this code print?\nn = 0\nwhile n > 0:\n    n -= 1\nelse:\n    print(\"else\", n)", ["else 0", "Nothing", "else -1", "An infinite loop"], 0, "The condition is false at the start, so the body never runs, but the else clause still runs because no break occurred. n is unchanged at 0.", "Domain 2: while-else"],
    ["x19", 0, 2, "What does this code print?\nc = 0\nfor i in range(4):\n    for j in range(4):\n        if j == i:\n            continue\n        c += 1\nprint(c)", ["16", "4", "8", "12"], 3, "There are 16 (i, j) pairs, and continue skips the 4 where j equals i, leaving 12. Continue skips only the rest of one inner iteration, not the whole inner loop.", "Domain 2: Nested loops and continue"],
    ["x20", 0, 2, "Which call produces the values 5, 4, 3, 2, 1 in that order?", ["range(5, 1, -1)", "range(5, 0, -1)", "range(5, 0)", "range(1, 5, -1)"], 1, "The stop value is excluded, so counting down to 1 needs stop 0 with step -1. range(5, 1, -1) stops before 1 and produces only 5, 4, 3, 2.", "Domain 2: range() with a negative step"],
    ["x21", 0, 2, "What does this code print?\na = True\nb = False\nif not a or b:\n    print(\"X\")\nelif a and not b:\n    print(\"Y\")\nelse:\n    print(\"Z\")", ["X", "Y", "Z", "X, then Y"], 1, "not a or b is False or False, so the first branch is skipped; a and not b is True and True, so Y prints. Only one branch of an if-elif-else chain can run.", "Domain 2: Multiple conditions"],
    ["x22", 0, 2, "What does this code print?\nn = 1234\nd = 0\nwhile n > 0:\n    n //= 10\n    d += 1\nprint(d)", ["3", "1234", "5", "4"], 3, "Each pass removes one digit with // 10: 123, 12, 1, 0, so the loop runs four times. Counting 3 misses the final pass that turns 1 into 0.", "Domain 2: while loops"],
    ["x23", 0, 2, "What does this code print?\nout = \"\"\nfor ch in \"banana\":\n    if ch == \"a\":\n        continue\n    out += ch\nprint(out)", ["bnn", "aaa", "banana", "b"], 0, "continue skips each \"a\" and the other letters are added in order, giving bnn. Only break would end the loop early and leave just b.", "Domain 2: Iterating over strings with continue"],
    ["x24", 0, 2, "What does this code print?\nfor i in range(2):\n    for j in range(2):\n        break\nelse:\n    print(\"outer else\")", ["Nothing", "outer else, twice", "outer else", "A SyntaxError"], 2, "The break exits only the inner loop, so the outer loop finishes normally and its else runs once. The else is attached to the outer for loop, so it cannot run twice.", "Domain 2: for-else with nested loops"],
    ["x25", 0, 2, "What does this code print?\nfor i in range(3):\n    if i == 1:\n        pass\n    print(i, end=\"\")", ["02", "0", "012", "12"], 2, "pass does nothing, so every value is printed and the output is 012. Only continue would skip the print for i equal to 1.", "Domain 2: The pass instruction"],
    ["x26", 0, 2, "A countdown is meant to stop at 0. What happens when this code runs?\nn = 3\nwhile n != 0:\n    print(n)\n    n -= 2", ["It loops forever, since n skips past 0", "It prints 3 and 1, then stops", "It prints 3, 1 and -1, then stops", "It prints nothing at all"], 0, "n goes 3, 1, -1, -3 and never equals 0, so the != condition is always true and the loop never ends. Stopping after 1 would need a condition such as n > 0.", "Domain 2: Avoiding infinite loops"],
    ["x27", 0, 2, "How many times does this loop body run?\nfor i in range(-3, 3):\n    print(i)", ["3", "5", "0", "6"], 3, "range(-3, 3) produces -3, -2, -1, 0, 1, 2, which is six values. A start below zero is valid, so the range is not empty.", "Domain 2: for loops over range()"],
    ["x28", 0, 2, "What does this code print?\nx = 15\nif x > 10:\n    if x > 20:\n        print(\"big\")\nprint(\"done\")", ["big, then done", "done", "big", "Nothing"], 1, "The outer condition is true but the inner one is false, so big is skipped; done is not indented under either if, so it always prints.", "Domain 2: Nested conditionals and indentation"],
    ["x29", 0, 3, "What does this code print?\nlst = [3, 1, 2]\nresult = lst.sort()\nprint(result)", ["[1, 2, 3]", "None", "[3, 1, 2]", "An AttributeError"], 1, "sort() sorts the list in place and returns None, so result is None. sorted(lst) is the function that returns a new sorted list.", "Domain 3: List methods"],
    ["x30", 0, 3, "What does this code print?\nm = [[0] * 2] * 2\nm[0][0] = 5\nprint(m)", ["[[5, 0], [0, 0]]", "[[5, 5], [0, 0]]", "[5, 0, 0, 0]", "[[5, 0], [5, 0]]"], 3, "Multiplying the outer list copies the reference, so both rows are the same inner list, and changing one changes both. Independent rows would need a comprehension such as [[0] * 2 for _ in range(2)].", "Domain 3: Nested lists and aliasing"],
    ["x31", 0, 3, "What does this code print?\ns = \"abcdef\"\nprint(s[-3:], s[:-3])", ["def abc", "abc def", "fed abc", "def cba"], 0, "s[-3:] takes the last three characters, and s[:-3] takes everything except the last three. Neither slice reverses the string; that needs a negative step.", "Domain 3: String slicing"],
    ["x32", 0, 3, "What does this code print?\nd = {\"a\": 1, \"b\": 2}\nprint(\"1\" in d.values(), 2 in d.values())", ["True True", "True False", "False True", "False False"], 2, "The values are the integers 1 and 2, so the string \"1\" is not among them but the integer 2 is. Python does not treat \"1\" and 1 as equal.", "Domain 3: Dictionary values()"],
    ["x33", 0, 3, "What does this code print?\nprint(len((\"ab\")), len((\"ab\",)))", ["1 1", "2 2", "2 1", "1 2"], 2, "Parentheses alone do not make a tuple, so (\"ab\") is just the string of length 2; the trailing comma makes (\"ab\",) a one-item tuple. The comma, not the brackets, builds the tuple.", "Domain 3: One-item tuples"],
    ["x34", 0, 3, "What does this code print?\nprint(\"  Hi There \".strip().lower().replace(\" \", \"_\"))", ["hi_there", "__hi_there_", "hi there", "Hi_There"], 0, "strip() removes the outer spaces first, lower() makes it lowercase, and replace() swaps the single inner space for an underscore. Because stripping happens first, no extra underscores appear.", "Domain 3: String methods"],
    ["x35", 0, 3, "What does this code print?\nprint(\"banana\".find(\"n\"), \"banana\".count(\"an\"), \"banana\".find(\"z\"))", ["3 2 0", "2 3 -1", "2 2 0", "2 2 -1"], 3, "The first n is at index 2, \"an\" occurs twice, and find() returns -1 when nothing is found. It does not return 0, which would be a valid index.", "Domain 3: String methods"],
    ["x36", 0, 3, "What does this code print?\nprint([c.upper() for c in \"a1b2\" if c not in \"12\"])", ["['A', '1', 'B', '2']", "['A', 'B']", "['a', 'b']", "AB"], 1, "The condition keeps only characters that are not digits 1 or 2, and upper() converts them, producing a list. The result is a list, not the joined string AB.", "Domain 3: List comprehensions with conditions"],
    ["x37", 0, 3, "What does this code print?\nd = {\"b\": 2, \"a\": 1}\nfor k in sorted(d.keys()):\n    print(k, end=\"\")", ["ba", "ab", "12", "a1b2"], 1, "sorted() returns the keys in alphabetical order, and only the keys are printed. Printing ba would follow insertion order, which sorted() overrides.", "Domain 3: Iterating dictionaries"],
    ["x38", 0, 3, "What does this code print?\na = [1, 2, 3]\nb = a\na = a + [4]\nprint(b)", ["[1, 2, 3, 4]", "[4]", "None", "[1, 2, 3]"], 3, "a + [4] builds a new list and rebinds a to it, while b still refers to the original list. Using a.append(4) would have changed the shared list and affected b.", "Domain 3: Copying vs aliasing lists"],
    ["x39", 0, 3, "What does this code print?\nt = (10, 20, 30, 40)\nprint(t[1:3], t[-1])", ["(20, 30) 40", "[20, 30] 40", "(20, 30, 40) 40", "(10, 20) 30"], 0, "Slicing a tuple returns a tuple of items at indexes 1 and 2, and t[-1] is the last item. A slice of a tuple is a tuple, not a list.", "Domain 3: Tuple indexing and slicing"],
    ["x40", 0, 3, "What does this code print?\ncounts = {}\nfor ch in \"abca\":\n    if ch in counts:\n        counts[ch] += 1\n    else:\n        counts[ch] = 1\nprint(counts[\"a\"], len(counts))", ["1 4", "2 4", "2 3", "1 3"], 2, "The letter a appears twice, and the dictionary holds three distinct keys: a, b and c. len() counts keys, not the four characters processed.", "Domain 3: Building dictionaries"],
    ["x41", 0, 3, "What does this code print?\ns = \"cat\"\nt = s.upper()\nprint(s, t)", ["CAT CAT", "cat cat", "cat CAT", "An error, since strings are immutable"], 2, "Strings are immutable, so upper() returns a new string and leaves s unchanged. Calling a method that returns a new string is allowed; only item assignment raises an error.", "Domain 3: String immutability"],
    ["x42", 0, 3, "What does this code print?\nprint(len(\"a\\tb\\\\\"))", ["4", "5", "6", "3"], 0, "\\t is one tab character and \\\\ is one backslash, so the string is a, tab, b, backslash: four characters. Counting each escape as two characters gives 6.", "Domain 3: Escape sequences"],
    ["x43", 0, 3, "What does this code print?\nm = [[1, 2], [3, 4]]\nprint(3 in m, [3, 4] in m)", ["True True", "True False", "False False", "False True"], 3, "in checks only the top-level items of m, which are the two inner lists, so 3 is not found but [3, 4] is. in does not search inside nested lists.", "Domain 3: in with nested lists"],
    ["x44", 0, 4, "What does this code print?\ndef a():\n    return int(\"x\")\ndef b():\n    try:\n        return a()\n    except ValueError:\n        return -1\nprint(b())", ["x", "-1", "None", "A ValueError stops the program"], 1, "The ValueError raised in a() propagates up to b(), where the except branch catches it and returns -1. The program does not crash because the caller handles it.", "Domain 4: Propagating exceptions"],
    ["x45", 0, 4, "What does this code print?\ndef f(a, b=2, c=3):\n    return a + b * c\nprint(f(1, c=10))", ["13", "21", "31", "A TypeError"], 1, "a is 1, b keeps its default 2, and c is 10, so the result is 1 + 2 * 10 = 21. 31 comes from adding before multiplying, which ignores operator priority.", "Domain 4: Default and keyword arguments"],
    ["x46", 0, 4, "What does this code print?\ndef f(x):\n    if x:\n        return\n    return 0\nprint(f(1), f(0))", ["1 0", "0 0", "None None", "None 0"], 3, "A bare return returns None, so f(1) is None, while f(0) skips the if and returns 0. A bare return does not hand back the argument.", "Domain 4: return and None"],
    ["x47", 0, 4, "What happens when this code runs?\ndef f(a):\n    return a\nf(1, 2)", ["A TypeError is raised", "It returns 1", "It returns (1, 2)", "A SyntaxError is raised"], 0, "Calling a function with more positional arguments than it has parameters raises a TypeError at call time. The code is syntactically valid, so it is not a SyntaxError.", "Domain 4: Positional arguments"],
    ["x48", 0, 4, "What does this code print?\ntry:\n    [1, 2, 3].index(5)\nexcept IndexError:\n    print(\"I\")\nexcept ValueError:\n    print(\"V\")\nexcept:\n    print(\"other\")", ["I", "other", "V", "Nothing, since index() returns -1"], 2, "list.index() raises ValueError when the value is missing, so the ValueError branch runs. IndexError is for a bad position, not a missing value.", "Domain 4: Common built-in exceptions"],
    ["x49", 0, 4, "Which built-in exception is a subclass of LookupError?", ["ZeroDivisionError", "ValueError", "KeyError", "TypeError"], 2, "KeyError and IndexError both derive from LookupError. ZeroDivisionError derives from ArithmeticError instead.", "Domain 4: The exception hierarchy"],
    ["x50", 0, 4, "ZeroDivisionError derives directly from which exception class?", ["ArithmeticError", "LookupError", "ValueError", "KeyboardInterrupt"], 0, "ZeroDivisionError is a subclass of ArithmeticError, so except ArithmeticError also catches it. ValueError is a sibling class under Exception, not its parent.", "Domain 4: The exception hierarchy"],
    ["x51", 0, 4, "What happens when this code runs, if value was never defined?\ntry:\n    print(value)\nexcept (TypeError, ValueError):\n    print(\"bad value\")", ["bad value is printed", "None is printed", "Nothing is printed and the program continues", "The program stops with a NameError"], 3, "Using an undefined name raises NameError, which is not in the handled tuple, so it is not caught and the program stops. bad value would print only for TypeError or ValueError.", "Domain 4: try-except with several exceptions"],
    ["x52", 0, 4, "What does this recursive function print?\ndef s(n):\n    if n < 10:\n        return n\n    return n % 10 + s(n // 10)\nprint(s(472))", ["472", "13", "4", "11"], 1, "Each call adds the last digit and recurses on the rest: 2 + 7 + 4 = 13, with the base case returning the single digit 4. Stopping at the base case value alone gives 4.", "Domain 4: Recursion and base cases"],
    ["x53", 0, 4, "What does this code print?\ndef f(x):\n    x = x * 2\n    return x\nx = 3\nprint(f(x), x)", ["6 6", "6 3", "3 3", "3 6"], 1, "The parameter x is local to f, so doubling it does not change the global x, which stays 3. Only a global statement would link the two names.", "Domain 4: Name scopes and shadowing"],
    ["x54", 0, 4, "What does this code print?\ndef setup():\n    global limit\n    limit = 5\nsetup()\nprint(limit)", ["A NameError", "None", "0", "5"], 3, "The global statement makes the assignment create limit in the module scope, so it exists after setup() runs. Without global the name would be local and printing it would raise NameError.", "Domain 4: The global keyword"],
    ["x55", 0, 4, "What does this code print?\ndef f():\n    for i in range(3):\n        return i\nprint(f())", ["0", "[0, 1, 2]", "2", "None"], 0, "return ends the function immediately on the first pass, returning 0. Collecting all values would need a list or yield, not return inside the loop.", "Domain 4: return"],
    ["x56", 0, 4, "What does this code print?\ndef a():\n    return b() + 1\ndef b():\n    return 1\nprint(a())", ["A NameError, since b is defined after a", "1", "2", "None"], 2, "Names in a function body are looked up when the function runs, and b is defined before a() is called, so the result is 2. Defining b after a is fine as long as both exist before the call.", "Domain 4: Defining and invoking functions"],
    ["x57", 0, 4, "A helper parse_age(text) returns int(text). Three callers need different reactions to bad input: one re-prompts, one logs and skips, one uses a default. Where should the ValueError be handled?", ["Inside parse_age, always returning 0 for bad input", "Nowhere, because ValueError cannot be caught", "In each caller, since each needs a different reaction", "With a bare except around the whole program"], 2, "Letting the exception propagate lets each caller choose its own response. Handling it inside the helper forces one reaction on everyone and hides the error with a fake 0.", "Domain 4: Deciding where to handle exceptions"],
    ["x58", 0, 4, "Which class is the base of all built-in exceptions, including SystemExit and KeyboardInterrupt?", ["BaseException", "Exception", "ArithmeticError", "RuntimeError"], 0, "BaseException is the root of the hierarchy; SystemExit and KeyboardInterrupt derive from it directly. Exception sits below BaseException and does not include those two.", "Domain 4: The exception hierarchy"],
    ["x59", 0, 4, "What happens when this code runs?\ndef f(a, b):\n    return a - b\nprint(f(1, a=2))", ["It prints 1, with a=2 and b=1", "A SyntaxError is raised", "It prints -1, with a=1 and b=2", "A TypeError is raised"], 3, "The positional 1 already fills a, so a=2 gives a a second value and Python raises TypeError. The call is syntactically legal because the keyword comes after the positional argument.", "Domain 4: Mixed argument passing"],
    ["x60", 0, 4, "What does this code print?\ntotal = 0\nfor s in [\"4\", \"x\", \"6\"]:\n    try:\n        total += int(s)\n    except ValueError:\n        continue\nprint(total)", ["4", "10", "A ValueError", "46"], 1, "int(\"x\") raises ValueError, which is caught, and continue moves on, so 4 and 6 are added to give 10. The loop does not stop at the bad item because the exception is handled.", "Domain 4: try-except in a loop"]
  ],
  whys: {
    "x1": ["4 would need (-2) ** 2; without parentheses ** is applied before the unary minus.", "0.25 would be 2 ** -2; here the minus sign applies to the result, not the exponent.", null, "Unary minus before a power is valid syntax, so no SyntaxError is raised."],
    "x2": [null, "int() does not round; 7.9 becomes 7, not 8, and -7.9 becomes -7.", "-8 would come from floor rounding, but int() truncates toward zero for negative floats too.", "Neither value rounds up; int() simply drops the fractional part of each number."],
    "x3": ["A lexical error involves invalid tokens, which would stop the program before it produced any output.", "A syntax error prevents the program from running at all, but this one runs and prints a value.", "An indentation error is a kind of syntax error and would also stop the program from running.", null],
    "x4": ["* with a string and an int repeats the string; it does not paste the number 3 into the text.", null, "+ joins strings directly and adds no space between \"ababab\" and \"c\".", "String * int and string + string are both valid operations, so no TypeError is raised."],
    "x5": ["3 is the value after //= only; the next line squares it with **= 2.", null, "11.1 would need true division and a different operation; //= gives an integer result.", "Both operands stay integers after //, so ** 2 produces the int 9, not a float."],
    "x6": ["float() converts the text to a number, so the strings are not concatenated into 1e23.", "One operand is a float (100.0), so the sum is a float and prints with .0.", "float() accepts scientific notation such as \"1e2\", so no ValueError is raised.", null],
    "x7": [null, "21 would require int(age); input() returns the string \"20\", which cannot be added to 1.", "201 would require str(1) to join strings; adding a str and an int raises an error.", "+ does not insert a space, and a str plus an int fails before anything is printed."],
    "x8": ["Python compares numbers by value, so 3 and 3.0 are equal despite different types.", "10 / 5 gives 2.0, which compares equal to 2, so the second value is True.", null, "Both comparisons are between equal numeric values, so neither is False."],
    "x9": ["4 would mean both names refer to the value 2, but Count and count are different variables.", "2 would mean Count was replaced, but Python names are case-sensitive.", null, "Both names are defined before use, so no NameError occurs."],
    "x10": [null, "7 > 6 is True and not False is True, so the and expression is True, not False.", "The comparison turns 7 into a Boolean before printing, so 7 is never the result.", "Comparing ints and combining Booleans are valid, so no TypeError is raised."],
    "x11": ["6 would be 5 + 1 and 16 would be a left shift; ^ is XOR and >> shifts right.", "4 is right for XOR, but 8 >> 2 shifts right to 2, not left to 32 or 16.", "5 ^ 1 is 4 because XOR clears the matching low bit; it is not addition.", null],
    "x12": ["An empty string is falsy, so the if x branch is skipped.", null, "not x is True for an empty string, so the elif runs before else is reached.", "One branch of an if-elif-else chain always runs here because not x is True."],
    "x13": ["4 * 4 is 16, which is not greater than 20, so the loop keeps going past 4.", null, "break stops the loop at 5, so i never reaches the end of the range.", "25 is i * i, but the code prints i itself, not its square."],
    "x14": ["break runs on every pass of the for loop, not just the first, so count increases three times.", "The break ends the while True loop immediately each time, so it never runs forever.", "count += 1 runs before break on every pass, so count cannot stay at 0.", null],
    "x15": [null, "No break runs, so the loop's else clause does run and prints its message.", "The loop body never prints n; only the else clause prints anything.", "The else belongs to the for loop and runs once after it, not once per item."],
    "x16": ["10 7 4 stops too early; 1 is still greater than the stop value 0, so it is included.", "range starts at the start value 10, not 9.", null, "-2 is past the stop value 0, and the stop is never reached or crossed."],
    "x17": ["or returns an operand, not False; 0 is falsy so or moves on to return \"x\".", "and and or return one of their operands, not forced True or False values.", null, "or skips the falsy 0 and returns \"x\", and and returns the falsy 0, not 5."],
    "x18": [null, "The loop's else runs whenever the loop ends without break, even if the body never ran.", "The body never runs, so n is never decremented to -1.", "The condition is false at the start, so the loop does not run at all, let alone forever."],
    "x19": ["16 counts every pair, but continue skips the four pairs where j equals i.", "4 counts only the skipped pairs, not the pairs that reach c += 1.", "8 would mean half the pairs were skipped, but only four of sixteen are.", null],
    "x20": ["range(5, 1, -1) stops before 1, producing only 5, 4, 3, 2.", null, "Without a negative step, range(5, 0) counts upward and is empty.", "Starting at 1 with a negative step toward 5 produces an empty range."],
    "x21": ["not a or b evaluates to False or False, which is False, so X is skipped.", null, "Z runs only if every earlier condition fails, but the elif is True.", "Only one branch of an if-elif-else chain runs, so X and Y cannot both print."],
    "x22": ["3 misses the final pass that turns 1 into 0; the loop runs once per digit.", "d counts passes; the code never prints the original number.", "The loop stops when n reaches 0 after four passes, so there is no fifth pass.", null],
    "x23": [null, "The a characters are the ones skipped, so they are never added to out.", "continue skips each a, so the full word is not rebuilt.", "continue does not end the loop; only break would stop after b."],
    "x24": ["The inner break does not affect the outer loop, which completes and runs its else.", "The else belongs to the outer for loop and runs once when it finishes, not per iteration.", null, "else after a for loop is valid syntax, and break inside the inner loop is valid too."],
    "x25": ["pass does not skip anything; 1 is printed like the other values.", "pass does not stop the loop; all three values are printed.", null, "0 is printed on the first pass; nothing skips it."],
    "x26": [null, "After 1, n becomes -1, which is not 0, so the loop does not stop there.", "It does not stop after -1 either, because n keeps decreasing and never equals 0.", "n is 3 at the start, which is not 0, so the body runs and prints."],
    "x27": ["3 counts only the non-negative values; the range also includes -3, -2 and -1.", "5 miscounts; -3 through 2 inclusive is six values because the stop 3 is excluded.", "A negative start is valid and smaller than the stop, so the range is not empty.", null],
    "x28": ["big needs x > 20, which is false for 15.", null, "big does not print because the inner condition is false, and done always prints.", "done is outside both if blocks, so it prints regardless of the conditions."],
    "x29": ["sort() sorts in place and returns None; sorted() returns the new list.", null, "sort() does change lst, but result holds the return value None, not the list.", "Lists do have a sort() method, so there is no AttributeError."],
    "x30": ["Both rows are the same list object, so the change shows in both rows.", "Only index 0 of the shared inner list changes, not every item.", "The structure stays nested; multiplying the outer list does not flatten it.", null],
    "x31": [null, "s[-3:] is the end of the string and s[:-3] is the start, not the other way round.", "These slices have no negative step, so nothing is reversed.", "s[:-3] keeps the first three characters in their original order."],
    "x32": ["The string \"1\" does not equal the integer 1, so the first check is False.", "The first check is False, not True, and the second finds the integer 2.", null, "The integer 2 is one of the values, so the second check is True."],
    "x33": ["(\"ab\") is just a string of length 2, not a one-item tuple.", "The trailing comma makes a tuple with one item, so its length is 1.", null, "The lengths are reversed; the string has 2 characters and the tuple has 1 item."],
    "x34": [null, "strip() runs before replace(), so the outer spaces are gone before any underscores are added.", "replace() swaps the inner space for an underscore, so a space no longer appears.", "lower() runs before printing, so the capital letters do not survive."],
    "x35": ["The first n is at index 2 (b=0, a=1, n=2), and find() returns -1 when nothing is found.", "\"an\" appears twice in banana, not three times.", "find() returns -1 for a missing substring, since 0 would be a valid index.", null],
    "x36": ["The condition drops the digits 1 and 2, so they are not in the result.", null, "upper() is applied to each kept character, so the letters are capitals.", "A list comprehension builds a list, not a joined string."],
    "x37": ["ba is insertion order, but sorted() puts the keys in alphabetical order.", null, "The loop prints keys from keys(), not values, so 12 does not appear.", "Only k is printed, so the values never appear in the output."],
    "x38": ["a + [4] creates a new list for a; b still refers to the original unchanged list.", "Nothing reduces b to [4]; it still holds the original three items.", "b refers to a list, not the result of a method, so it is not None.", null],
    "x39": [null, "Slicing a tuple returns a tuple, shown in parentheses, not a list.", "The slice stops before index 3, so 40 is not included.", "Index 1 is 20, so the slice starts at 20, not 10, and t[-1] is 40."],
    "x40": ["a appears twice, and len() counts distinct keys (3), not characters (4).", "len() counts the three distinct keys, not the four characters processed.", null, "counts[\"a\"] is 2 because a appears twice in abca."],
    "x41": ["upper() returns a new string; s itself is unchanged and stays lowercase.", "t holds the result of upper(), so it is CAT, not cat.", null, "Calling a method that returns a new string is allowed; only item assignment is an error."],
    "x42": [null, "5 counts one of the escapes as two characters; each escape is one character.", "6 counts both escapes as two characters each, but each is a single character.", "3 drops a character; the string holds a, tab, b and a backslash."],
    "x43": ["in checks only top-level items, so 3 inside a nested list is not found.", "The values are reversed: 3 is not a top-level item, but [3, 4] is.", "[3, 4] is one of the top-level items, so the second check is True.", null],
    "x44": ["int(\"x\") never succeeds, so the text x is not returned or printed.", null, "The except branch returns -1 explicitly, so the result is not None.", "b() catches the ValueError, so it does not reach the top level and stop the program."],
    "x45": ["13 would use c = 3 and b = 10; the keyword sets c, and b keeps its default 2.", null, "31 adds before multiplying, but * has priority over +.", "Keyword arguments after positional ones are valid, so no TypeError is raised."],
    "x46": ["A bare return gives None, not the argument 1.", "f(1) takes the bare return and gives None, not 0.", "f(0) skips the if and reaches return 0, so it is not None.", null],
    "x47": [null, "Python does not ignore extra arguments; the call fails before returning.", "Extra positional arguments are not packed into a tuple without a *args parameter.", "The call is valid syntax; the mismatch is detected only when it runs."],
    "x48": ["IndexError is for a bad position; a missing value in index() raises ValueError.", "The ValueError branch matches first, so the bare except is never reached.", null, "list.index() raises ValueError; str.find() is the method that returns -1."],
    "x49": ["ZeroDivisionError derives from ArithmeticError, not LookupError.", "ValueError is a direct subclass of Exception, not of LookupError.", null, "TypeError is a direct subclass of Exception, not of LookupError."],
    "x50": [null, "LookupError is the parent of KeyError and IndexError, not of ZeroDivisionError.", "ValueError is a sibling class under Exception, not the parent of ZeroDivisionError.", "KeyboardInterrupt derives from BaseException and is unrelated to arithmetic errors."],
    "x51": ["bad value prints only for TypeError or ValueError; an undefined name raises NameError.", "An undefined name is an error, not a variable holding None.", "The NameError is not in the handled tuple, so the program does not continue.", null],
    "x52": ["472 would be returned only if the base case fired first, but 472 is not below 10.", null, "4 is only the base case value; the other digits are added as calls return.", "11 misses one digit; 2 + 7 + 4 is 13."],
    "x53": ["The global x is not changed, because the parameter x is a separate local name.", null, "f returns the doubled value 6, not the original 3.", "The values are reversed; f returns 6 and the global x stays 3."],
    "x54": ["global makes the assignment create limit at module level, so the name exists.", "limit is assigned 5, not None.", "No code sets limit to 0; it is assigned 5.", null],
    "x55": [null, "return exits on the first pass, so the values are never collected into a list.", "The loop never reaches i = 2 because return ends the function at i = 0.", "The function returns 0 explicitly, so it does not fall through to None."],
    "x56": ["b is looked up when a() runs, and by then b exists, so no NameError occurs.", "a() adds 1 to b()'s result of 1, so it returns 2.", null, "a() returns b() + 1 explicitly, so the result is not None."],
    "x57": ["Returning 0 hides bad input and forces one reaction on callers that need different ones.", "ValueError is an ordinary exception and can be caught with try-except.", null, "A bare except around everything catches too much, including exits, and removes each caller's choice."],
    "x58": [null, "Exception is below BaseException and does not include SystemExit or KeyboardInterrupt.", "ArithmeticError is a subclass of Exception for math errors only.", "RuntimeError is a subclass of Exception, far from the root of the hierarchy."],
    "x59": ["Python does not let a keyword override a positional argument; a would get two values.", "A keyword after a positional argument is legal syntax; the error happens at call time.", "a=2 is not ignored; it clashes with the positional 1 and raises an error.", null],
    "x60": ["The bad item is skipped, but the loop continues and adds 6 as well.", null, "The ValueError is caught by the except branch, so it does not stop the program.", "int() converts each string to a number, so the values are added, not joined."]
  },
  levels: {
    "pc1": 3, "pc2": 2, "pc3": 2, "pc4": 3, "pc5": 2, "pc6": 1, "pc7": 2, "pc8": 1, "pc9": 1, "pc10": 1,
    "pc11": 1, "pc12": 2, "pc13": 2, "pc14": 1, "pc15": 2, "pc16": 2, "pc17": 1, "pc18": 2, "pc19": 1, "pc20": 2,
    "pc21": 2, "pc22": 3, "pc23": 2, "pc24": 2, "pc25": 1, "pc26": 2, "pc27": 3, "pc28": 2, "pc29": 3, "pc30": 1,
    "pc31": 2, "pc32": 2, "pc33": 3, "pc34": 2, "pc35": 2, "pc36": 2, "pc37": 2, "pc38": 3, "pc39": 2, "pc40": 2,
    "pc41": 3, "pc42": 2, "pc43": 1, "pc44": 1, "pc45": 2, "pc46": 2, "pc47": 1, "pc48": 2, "pc49": 1, "pc50": 2,
    "pc51": 1, "pc52": 2, "pc53": 1, "pc54": 3, "pc55": 1, "pc56": 1, "pc57": 2, "pc58": 1, "pc59": 2, "pc60": 1,
    "pc61": 2, "pc62": 1, "pc63": 1, "pc64": 1, "pc65": 1, "pc66": 2, "pc67": 3, "pc68": 1, "pc69": 2, "pc70": 2,
    "pc71": 2, "pc72": 3, "pc73": 2, "pc74": 3, "pc75": 2, "pc76": 1, "pc77": 2, "pc78": 2, "pc79": 2, "pc80": 2,
    "pc81": 1, "pc82": 2, "pc83": 1, "pc84": 3, "pc85": 2, "pc86": 3, "pc87": 3, "pc88": 2, "pc89": 1, "pc90": 2,
    "pc91": 2, "pc92": 1, "pc93": 2, "pc94": 1, "pc95": 1, "pc96": 3, "pc97": 1, "x1": 2, "x2": 2, "x3": 1,
    "x4": 1, "x5": 2, "x6": 2, "x7": 2, "x8": 2, "x9": 1, "x10": 2, "x11": 3, "x12": 1, "x13": 2,
    "x14": 2, "x15": 2, "x16": 2, "x17": 3, "x18": 2, "x19": 2, "x20": 1, "x21": 2, "x22": 2, "x23": 1,
    "x24": 3, "x25": 2, "x26": 3, "x27": 1, "x28": 1, "x29": 2, "x30": 3, "x31": 2, "x32": 2, "x33": 2,
    "x34": 2, "x35": 2, "x36": 2, "x37": 1, "x38": 3, "x39": 1, "x40": 2, "x41": 1, "x42": 3, "x43": 2,
    "x44": 2, "x45": 2, "x46": 2, "x47": 1, "x48": 3, "x49": 1, "x50": 1, "x51": 2, "x52": 3, "x53": 2,
    "x54": 2, "x55": 2, "x56": 3, "x57": 3, "x58": 1, "x59": 3, "x60": 2
  }
});
