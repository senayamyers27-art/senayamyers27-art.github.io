/* More ai-200 practice questions with wrong-answer notes, and a difficulty level for every question. Merged into the free bank by tools/build.js. */
CertHub.addExtra("ai-200", {
  questions: [
    ["x1",0,1,"Every small code change to a Python API makes docker build reinstall all pip packages, which takes minutes. The Dockerfile runs COPY . . and then pip install -r requirements.txt. What change helps most?",["Add the --no-cache flag to docker build so every layer is rebuilt cleanly each time","Copy requirements.txt and run pip install before copying the rest of the code","Move the pip install into a CMD instruction so it runs when the container starts","Switch the base image tag from a slim variant to the full python image variant"],1,"Docker reuses a cached layer until an earlier instruction's inputs change. Copying only requirements.txt first means the pip install layer is reused unless dependencies change. --no-cache is tempting for clean builds but defeats caching entirely.","Domain 1: Dockerfile layer caching"],
    ["x2",0,1,"A developer's image accidentally contains the local .env file, the .git folder and a virtual environment, because the Dockerfile uses COPY . . for the app code. What is the simplest fix?",["Delete those files with RUN rm in a later layer","Mark the files read-only on the build machine","Push the image to a private registry instead","List those paths in a .dockerignore file"],3,"A .dockerignore file excludes paths from the build context, so they never reach COPY instructions or any layer. RUN rm is tempting, but the files persist in the lower layer where they were added.","Domain 1: .dockerignore"],
    ["x3",0,1,"A security review finds that a Python container runs its web server as root. The image is based on python:3.12-slim. How should the developer change the Dockerfile?",["Create an unprivileged user and add a USER instruction before CMD","Add EXPOSE 8000 so the app no longer needs root privileges","Run chmod 777 on the app folder so any user can execute it","Use ENTRYPOINT instead of CMD to start the web server process"],0,"Creating a dedicated user and switching to it with USER makes the process run without root privileges, limiting impact if it is compromised. EXPOSE is tempting because ports are linked to privilege in people's minds, but it is documentation only.","Domain 1: Non-root containers"],
    ["x4",0,1,"A team deploys container images by the tag 'prod' and later cannot tell which build is running or roll back reliably. What tagging practice should they adopt for deployments?",["One stable tag like 'prod' that is overwritten by each release","The 'latest' tag for every build pushed to the container registry","A unique tag per build, such as the build ID or commit SHA","A date-only tag reused for all builds that happen on the same day"],2,"Unique tags never get overwritten, so each deployment points at a known build and rollbacks just redeploy an earlier tag (or digest). Stable tags suit base images that should receive patches, not deployments that must be traceable.","Domain 1: Stable vs unique tags"],
    ["x5",0,1,"A team wants their image rebuilt in Azure and pushed to ACR automatically whenever code is committed to the main branch of their Git repository, without running their own build agents. What should they create?",["An ACR webhook that fires when an image is pushed to the registry","A Container Apps job that runs docker build on a fixed schedule","An ACR task with a source code commit trigger on the repository","A geo-replicated Premium registry with content trust turned on"],2,"ACR tasks can be triggered by source commits (and pull requests) in a Git repository, building and pushing images with no self-hosted agents. Webhooks are tempting but react to registry events after an image already exists.","Domain 1: ACR Tasks"],
    ["x6",0,1,"An App Service web app runs a custom container from ACR. The team wants the app to pull and restart automatically whenever a new image is pushed to the tag it uses. What should they enable?",["Continuous deployment for the container in Deployment Center","Always On for the web app in its general configuration","Automatic OS patching on the App Service plan's workers","Autoscale rules for the App Service plan on CPU usage"],0,"Enabling continuous deployment for a container configures a webhook so App Service pulls the updated image and restarts when the tag is pushed. Always On is tempting because it sounds like it keeps things current, but it only prevents idle unloading.","Domain 1: App Service continuous deployment"],
    ["x7",0,1,"An App Service app has a staging slot pointed at a test database and a production slot pointed at the production database, both via an app setting. After a swap, production suddenly uses the test database. What should the developer change?",["Put the database string directly in the container image of each slot","Swap back and scale the production slot out to more instances","Use a separate App Service plan for the staging deployment slot","Mark the connection setting as a deployment slot setting in both slots"],3,"Settings marked as deployment slot settings stay with the slot during a swap, so each environment keeps its own database. Without the flag, the value moves with the swapped content. A separate plan is tempting but unrelated to settings behavior.","Domain 1: Deployment slots"],
    ["x8",0,1,"A developer updates a secret value on a Container App, but the running revision keeps using the old value. What explains this, and what should they do?",["Secrets are cached by the registry; purge the image and push again","Secret changes do not create a revision; restart or deploy a new one","Secrets refresh only in single revision mode; switch the app to it first","Secret updates are applied only after the environment is recreated"],1,"Secrets are an application-scope setting: changing them does not create a revision, and existing revisions pick up the new value only after a restart or a new revision. Assuming the registry caches secrets is tempting but wrong.","Domain 1: Container Apps secrets and revisions"],
    ["x9",0,1,"A Container App in single revision mode is updated with a new image. What happens to traffic?",["A new revision is created and receives all traffic once ready","The new revision is created with 0% traffic until assigned","Both revisions split traffic evenly until one is deleted","The image is replaced in place without any new revision"],0,"In single revision mode, a revision-scope change creates a new revision that takes 100% of traffic when it is ready, and the old one is deactivated. The 0% option is tempting because it describes multiple revision mode.","Domain 1: Container Apps revision modes"],
    ["x10",0,1,"An API on Container Apps scales to zero at night, and the first morning requests are slow while a replica starts. Latency matters more than the small idle cost. What should be changed?",["Lower the HTTP rule's concurrency value","Use a larger container image for the app","Set the minimum replica count to 1","Switch the app to internal ingress mode"],2,"With minimum replicas at 1, one replica always stays running, so the first request does not wait for a cold start. Tuning the HTTP concurrency is tempting, but scaling rules only act once traffic arrives.","Domain 1: Container Apps scaling"],
    ["x11",0,1,"Each message in a Service Bus queue needs up to 30 minutes of container processing, then the container should exit. When the queue is empty, nothing should run. Which Container Apps option fits best?",["A container app with an HTTP scale rule and min 0","An event-driven job with a Service Bus scale rule","A scheduled job that runs every 30 minutes all day","A container app with minimum replicas fixed at 1"],1,"Event-driven Container Apps jobs start executions based on a KEDA scaler such as Service Bus queue length, run to completion and stop. A container app with a scale rule is tempting, but apps are for long-running services rather than run-to-completion work.","Domain 1: Container Apps jobs"],
    ["x12",0,1,"An AKS pod keeps restarting, and kubectl describe shows the last state as OOMKilled. What is the most likely cause?",["The pod's CPU request was higher than any node had","The image could not be pulled from the private container registry","The Service selector does not match the pod labels","The container used more memory than its memory limit"],3,"OOMKilled means the container exceeded its memory limit and the kernel terminated it. Raise the limit or reduce usage. An oversized CPU request is tempting since it is also a resource setting, but it keeps the pod Pending instead.","Domain 1: AKS requests and limits"],
    ["x13",0,1,"A Kubernetes Deployment needs a database password and a non-sensitive feature toggle as environment variables. Which objects should hold them?",["A ConfigMap for both values, since both are configuration","Both values written into the container image at build time","A Service object for the password and labels for the toggle","A Secret for the password and a ConfigMap for the toggle"],3,"Kubernetes Secrets are designed for sensitive values and ConfigMaps for non-confidential configuration; both can be injected as environment variables. Using a ConfigMap for everything is tempting but treats the password as plain configuration.","Domain 1: AKS ConfigMaps and Secrets"],
    ["x14",0,1,"An AKS Deployment's pods must be reachable only by other workloads inside the cluster through a stable name. Which Service type fits?",["LoadBalancer","ClusterIP","NodePort","ExternalName"],1,"ClusterIP is the default Service type: it gives a stable internal IP and DNS name reachable only inside the cluster. LoadBalancer is tempting because it is common, but it exposes the service externally.","Domain 1: AKS Services"],
    ["x15",0,2,"Two users update the same Cosmos DB item almost at once, and one change is silently lost. How should the Python code prevent this?",["Switch the account's consistency level from Session to Strong","Use upsert_item instead of replace_item for every update","Replace the item using its _etag with an if-match condition","Add a composite index on the fields being updated"],2,"Optimistic concurrency sends the _etag read earlier with an if-match condition; if another writer changed the item, the write fails with a precondition error and the app can retry. Strong consistency is tempting but does not guard writes.","Domain 2: Cosmos DB optimistic concurrency"],
    ["x16",0,2,"A query sorts Cosmos DB items with ORDER BY c.category ASC, c.price DESC and fails. What should the developer add to the indexing policy?",["A composite index on category ascending and price descending","A spatial index on the category and price properties","A vector index on the price property of each item","An excluded path for both properties used in the sort"],0,"ORDER BY on multiple properties requires a composite index whose paths and sort orders match the query. A vector index is tempting in an AI-focused container, but it only serves VectorDistance searches.","Domain 2: Cosmos DB indexing policy"],
    ["x17",0,2,"Chat session items in Cosmos DB should be deleted automatically 7 days after their last update, with no cleanup code. What should the developer configure?",["A change feed processor that deletes items older than 7 days","Time to live on the container, set to 7 days in seconds","A timer-triggered function that queries and deletes old items","An indexing policy that excludes items older than 7 days"],1,"Setting a default TTL on the container (in seconds) makes Cosmos DB delete items automatically after that time since their last write, using spare RUs. A timer function is the tempting DIY answer but adds code and RU cost.","Domain 2: Cosmos DB TTL"],
    ["x18",0,2,"An index must stay in sync with a Cosmos DB container, including removing entries when items are deleted outright (no soft-delete flag). Which change feed option supports this?",["Latest version mode with a lease","Change feed with Strong consistency","A Cosmos DB trigger with a larger batch","All versions and deletes mode"],3,"All versions and deletes mode records every change, including deletes, within the account's continuous backup retention window. Latest version mode is the default and is tempting, but it omits deletes, which is why soft-delete flags are often used with it.","Domain 2: Change feed modes"],
    ["x19",0,2,"A dashboard for a single tenant queries a large Cosmos DB container with enable_cross_partition_query=True and no partition key, and RU charges are high. tenantId is the container partition key path. What should the developer change?",["Pass the tenant's partition key value so the query hits one partition","Raise max_item_count so each page of results returns more items at once","Raise the container's provisioned RU/s to cover the extra fan-out cost","Lower the consistency level to Eventual for all of the dashboard reads"],0,"Supplying the partition key (in the filter or partition_key argument) scopes the query to one logical partition, avoiding a costly fan-out. Raising the page size is tempting because it cuts round trips, but the fan-out cost remains.","Domain 2: Cosmos DB request units"],
    ["x20",0,2,"A global app reads from Cosmos DB in several regions. It needs reads that are never more than a known number of versions or time behind writes, with lower latency than Strong. Which consistency level fits?",["Session","Consistent prefix","Bounded staleness","Eventual"],2,"Bounded staleness guarantees reads lag writes by no more than a configured number of versions or time interval, while avoiding the latency cost of Strong. Session is tempting because it is the default, but its guarantee is per session.","Domain 2: Cosmos DB consistency levels"],
    ["x21",0,2,"A RAG app stores document chunks with embeddings from one model. After the team switches query embeddings to a different embedding model, similarity results become meaningless. What is the cause?",["The vector index must be rebuilt after every new user question","Cosine distance cannot be used with vectors that were normalized","Query and stored vectors must come from the same embedding model","The chunks are too short to be compared with longer user questions"],2,"Embeddings from different models occupy different vector spaces (and may differ in dimensions), so distances between them are not comparable. Re-embed the stored chunks with the new model or keep the old one for queries. Rebuilding the index is tempting but does not fix mismatched spaces.","Domain 2: RAG embeddings"],
    ["x22",0,2,"When preparing long documents for RAG, why do teams often split text into chunks with a small overlap between neighboring chunks?",["So sentences near a boundary keep their context in at least one chunk","So the vector index uses less storage than it would if chunks had no overlap","So each chunk can use a different embedding model and be compared","So documents no longer need any metadata such as source or owner"],0,"Overlap keeps ideas that straddle a chunk boundary intact in at least one chunk, which improves retrieval. It costs a little extra storage, so the storage-saving option is a tempting but backwards reason.","Domain 2: RAG chunking"],
    ["x23",0,2,"A pgvector table has an HNSW index created with vector_l2_ops. Queries use ORDER BY embedding <=> $1 LIMIT 5 and are slow, with the plan showing a sequential scan. Why?",["HNSW indexes only work when the query also filters on a B-tree column","LIMIT 5 is too small; HNSW is used only for LIMIT values over 100","The query must use <#> because HNSW supports only inner product","The <=> cosine operator does not match the L2 operator class of the index"],3,"A pgvector index serves only the distance operator of its operator class: vector_l2_ops for <->, vector_cosine_ops for <=>, vector_ip_ops for <#>. Create a cosine index or query with <->. Blaming the LIMIT is tempting but not how the planner decides.","Domain 2: pgvector operators and indexes"],
    ["x24",0,2,"In pgvector, which operator returns the negative inner product between two vectors?",["<->","<#>","<=>","<+>"],1,"pgvector's <#> returns the negative inner product, so ORDER BY ascending still puts the most similar vectors first. <=> is the tempting choice because cosine and inner product are closely related, but it returns cosine distance.","Domain 2: pgvector distance operators"],
    ["x25",0,2,"A team must add an approximate nearest neighbor index to a pgvector table that is still empty and will fill gradually over months. Which index type is the better fit, and why?",["HNSW, because it needs no training step on existing rows","IVFFlat, because its lists are learned from an empty table","A B-tree, because it handles vector ranges most efficiently","IVFFlat with probes set to 0 so no lists need to be searched"],0,"HNSW builds its graph incrementally as rows arrive and has no training phase, so it can be created on an empty table. IVFFlat clusters existing data into lists, so it should be built after loading data. IVFFlat is tempting because it builds faster.","Domain 2: pgvector HNSW vs IVFFlat"],
    ["x26",0,2,"An app uses cache-aside with Azure Managed Redis in front of PostgreSQL. When a product is updated, users see the old version until the key expires. What should the update code do?",["Write to Redis only and let PostgreSQL catch up some time later","Turn off TTL on product keys so they are never evicted by Redis","Write to the database, then delete or update that product's key","Flush the entire cache after every single change to any product"],2,"With cache-aside, invalidating (or updating) the specific key after a database write removes stale data while keeping the rest of the cache warm. Flushing the whole cache is tempting for correctness but punishes the database.","Domain 2: Redis cache-aside invalidation"],
    ["x27",0,2,"An Azure Managed Redis instance used purely as a cache becomes full, and writes start failing with out-of-memory errors. Every key could be recomputed. Which eviction policy should be set?",["noeviction","allkeys-lru","volatile-ttl","volatile-random"],1,"allkeys-lru lets Redis evict any key, least recently used first, which suits a cache where everything can be rebuilt. The volatile policies are tempting, but they only consider keys that have an expiry set.","Domain 2: Redis eviction policies"],
    ["x28",0,2,"A semantic cache in Redis returns stored answers for new questions whose embeddings fall within a similarity threshold. Users report answers to questions they did not ask. What should be adjusted?",["Lengthen the cache TTL so entries are stored for longer","Switch the eviction policy to noeviction to keep entries","Increase the cache size so more answers can be stored","Tighten the similarity threshold so only close matches count as hits"],3,"A threshold that is too loose treats merely related questions as the same, returning their cached answers. A stricter threshold trades some hit rate for correctness. Changing TTL is tempting because it is the usual cache knob, but it affects freshness, not matching.","Domain 2: Semantic caching"],
    ["x29",0,2,"A container of product documents uses a vector policy with 1,536-dimension embeddings, and the developer wants approximate search that keeps RU cost and latency low as the collection grows. Which vector index type suits this best?",["flat on the embedding path","range on /embedding/*","spatial on the embedding path","quantizedFlat or diskANN"],3,"quantizedFlat compresses vectors for cheaper brute-force search, and diskANN is built for large-scale approximate search with low latency. flat is tempting because it gives exact results, but it becomes costly as the collection grows.","Domain 2: Cosmos DB vector indexes"],
    ["x30",0,2,"A developer calls container.upsert_item(body) with the azure-cosmos Python SDK. What does this do?",["Creates the item and fails with a conflict if the id already exists anywhere","Creates the item, or replaces it if the id already exists in that partition","Patches only the fields in the body and keeps other properties unchanged","Inserts the item into every partition so point reads can find it faster"],1,"upsert_item creates the item if it does not exist, otherwise it replaces the existing item with the same id and partition key. create_item is the tempting mix-up because it also writes new items, but it fails on conflicts.","Domain 2: Cosmos DB Python SDK"],
    ["x31",0,2,"An app already stores its user profiles and chat history in Cosmos DB for NoSQL and now needs similarity search over embeddings of those same documents, with filters on user properties. What is the most suitable approach?",["Copy all documents into PostgreSQL with pgvector nightly","Keep all embeddings only in Azure Managed Redis memory","Add a vector policy and index to the Cosmos DB container","Store embeddings in Blob Storage and scan them in code"],2,"Cosmos DB for NoSQL supports vector policies, vector indexes and VectorDistance(), so embeddings can live beside the documents and be filtered with normal WHERE clauses. pgvector is tempting and strong for relational data, but moving this NoSQL data adds sync and staleness.","Domain 2: Choosing a vector store"],
    ["x32",0,3,"Expired Service Bus messages disappear instead of showing up in the queue's dead-letter subqueue. The team wants to inspect them. What should they enable?",["Dead-lettering on message expiration for the queue","Duplicate detection with a longer detection history window","Sessions on the queue with a session per sender","Auto-forwarding from the queue to a second queue"],0,"By default, expired messages are removed. Enabling dead-lettering on message expiration moves them to the $DeadLetterQueue subqueue for inspection. Auto-forwarding is tempting because it moves messages, but it does not apply to expiry.","Domain 3: Service Bus dead-lettering"],
    ["x33",0,3,"A Python service must read messages from a Service Bus queue's dead-letter subqueue to diagnose failures. How should it open the receiver?",["Receive from the main queue with receive-and-delete mode enabled","Use get_queue_receiver with sub_queue set to the dead-letter subqueue","Create a new queue named deadletter and receive from that queue","Peek the main queue with a filter for messages with delivery count 10"],1,"In the Python SDK, get_queue_receiver(queue_name, sub_queue=ServiceBusSubQueue.DEAD_LETTER) reads the <queue>/$DeadLetterQueue subqueue. Creating a queue named deadletter is tempting but it is unrelated to the real subqueue.","Domain 3: Reading the dead-letter queue"],
    ["x34",0,3,"An order handler receives a payment-confirmed message before the matching order-created message. It wants to set the payment message aside and fetch that exact message later by its sequence number. What should it do?",["Abandon the message so it is redelivered to another receiver","Dead-letter the message and resubmit it after the order arrives","Complete the message and store a copy in a local memory list","Defer the message and later receive it by sequence number"],3,"Deferral keeps the message in the queue but hidden from normal receives; the app records the sequence number and later calls receive_deferred_messages. Abandon is tempting but just returns the message to the queue.","Domain 3: Service Bus deferral"],
    ["x35",0,3,"A reminder message must become visible to receivers exactly at 09:00 tomorrow, not before. What Service Bus feature should the sender use?",["Scheduled messages","Message time-to-live","Session state","Duplicate detection"],0,"Scheduling a message (schedule_messages or a scheduled enqueue time) keeps it invisible until the given time. TTL is tempting because it is also time-based, but it defines when a message expires.","Domain 3: Service Bus scheduled messages"],
    ["x36",0,3,"A topic subscription should receive only messages whose correlation ID equals 'eu-orders', and the team wants the most efficient filter type. What should they use?",["A SQL filter using a LIKE pattern on the label","A topic per region created by the message sender","A correlation filter on the correlation ID","A session ID filter on the subscription receiver"],2,"Correlation filters match message properties such as CorrelationId by equality and are cheaper to evaluate than SQL filters. A SQL filter is tempting because it can express the same rule, but it is less efficient.","Domain 3: Service Bus subscription filters"],
    ["x37",0,3,"An Event Grid subscription must deliver only events where the data field orderTotal is greater than 1000. What should be configured on the subscription?",["A subject begins-with filter using the value 1000","An event type filter set to orderTotal","An advanced filter using NumberGreaterThan on data.orderTotal","A dead-letter destination for smaller orders"],2,"Advanced filters can compare fields in the event, including data properties, with operators such as NumberGreaterThan. Subject filters are the tempting choice because they are the most common filter, but they only match subject text.","Domain 3: Event Grid advanced filters"],
    ["x38",0,3,"A team needs to pick services for three flows: (1) transactional order commands that must not be lost and need ordered processing per customer, (2) reacting to blob-created events across many subscribers, (3) ingesting high-volume telemetry for stream processing. Which mapping fits?",["(1) Service Bus, (2) Event Grid, (3) Event Hubs","(1) Event Grid, (2) Service Bus, (3) Event Hubs","(1) Event Hubs, (2) Event Grid, (3) Service Bus","(1) Service Bus, (2) Event Hubs, (3) Event Grid"],0,"Service Bus handles reliable enterprise messaging with sessions and dead-lettering; Event Grid routes discrete events such as BlobCreated to many subscribers; Event Hubs ingests high-throughput streams for replay. Swapping Event Grid and Service Bus is the most common mistake.","Domain 3: Choosing Service Bus, Event Grid or Event Hubs"],
    ["x39",0,3,"A function app needs a longer execution timeout and a limit on how many Service Bus messages each instance processes at once. Where are these settings configured?",["local.settings.json","function_app.py","requirements.txt","host.json"],3,"host.json holds runtime-wide settings such as functionTimeout and extension options like Service Bus concurrency. local.settings.json is tempting because it is also a settings file, but it only feeds app settings during local development.","Domain 3: Functions host.json"],
    ["x40",0,3,"A developer has a Python function app project ready locally and an existing function app in Azure named orders-func. Which Core Tools command deploys the code?",["func start --publish orders-func","func azure functionapp publish orders-func","func new --deploy orders-func","func init orders-func --worker-runtime python"],1,"func azure functionapp publish <app-name> packages the project and deploys it to the named function app. func start is tempting because it is the most used command, but it only runs the host locally.","Domain 3: Functions deployment"],
    ["x41",0,3,"A function app must use its managed identity instead of a connection string for its host storage account. Which app setting shape enables this?",["AzureWebJobsStorage__accountName set to the storage account name","AzureWebJobsStorage set to the account key taken from the portal","FUNCTIONS_WORKER_RUNTIME set to the value managedidentity","WEBSITE_RUN_FROM_PACKAGE set to the storage account's URL"],0,"Identity-based connections use settings with a double-underscore suffix, such as AzureWebJobsStorage__accountName, and the identity needs data roles on the account. Putting the key in AzureWebJobsStorage is tempting because it is the classic setup.","Domain 3: Identity-based connections"],
    ["x42",0,3,"A Python v2 function must run when an Event Grid event is delivered and read the event's data. Which declaration fits?",["A function.json file with an eventGridTrigger binding next to function_app.py","An @app.route decorator with an HTTP trigger that Event Grid polls regularly","An @app.event_grid_trigger decorator with a func.EventGridEvent parameter","An @app.timer_trigger decorator that checks the Event Grid topic every minute"],2,"In the v2 model, @app.event_grid_trigger(arg_name=...) binds an EventGridEvent whose get_json() returns the data. function.json is tempting from older samples, but v2 apps declare triggers in code.","Domain 3: Functions Python v2 triggers"],
    ["x43",0,3,"A function app on the Consumption plan has slow first requests after idle periods, and it must also reach a database through a virtual network. The team accepts paying for warm capacity. Which plan fits best?",["Consumption plan with a longer timeout","Premium plan with always-ready instances","Dedicated plan with autoscale turned off","Consumption plan with a timer keep-alive"],1,"The Premium plan keeps always-ready instances warm to avoid cold starts and supports VNet integration. A keep-alive timer is tempting as a cheap trick, but it is unreliable and does not add network access.","Domain 3: Functions hosting plans"],
    ["x44",0,3,"A Python v2 function reads a queue message and must return an HTTP response and also write a document to Cosmos DB. How should the Cosmos DB write be declared?",["As a Cosmos DB input binding decorator with the document as its value","As a host.json setting that names the database and target container","As an app setting that starts with CosmosDBOutput__ for that function","As a Cosmos DB output binding decorator, setting the output parameter"],3,"Output bindings (for example @app.cosmos_db_output) declare the target, and the function sets the output parameter's value, so no SDK code is needed. An input binding is tempting because it names the same container, but it only reads.","Domain 3: Functions output bindings"],
    ["x45",0,3,"A subscriber processes Event Grid events, and the team is worried that during a long outage, events will be dropped after retries end. Which two settings on the event subscription control this?",["Subject filters and the choice of CloudEvents schema","Webhook validation and the maximum batch size used","Advanced filters and the topic's input schema type","Retry policy (max attempts, event TTL) and a dead-letter location"],3,"Event Grid retries until the maximum delivery attempts or event time-to-live is reached; with a dead-letter storage container configured, undelivered events are kept there instead of being dropped. Webhook validation is tempting since it causes failures, but only when subscribing.","Domain 3: Event Grid delivery and dead-lettering"],
    ["x46",0,3,"A Service Bus consumer can tolerate losing a message if it crashes mid-processing, and it wants the lowest overhead per message. Which receive mode fits?",["Peek-lock","Receive-and-delete","Deferred receive","Session receive"],1,"Receive-and-delete removes the message as soon as it is delivered, avoiding the settle round-trip, but a crash loses it, which this consumer accepts. Peek-lock is the tempting default and is right when messages must not be lost.","Domain 3: Service Bus receive modes"],
    ["x47",0,4,"A container app has two user-assigned managed identities. DefaultAzureCredential authenticates as the wrong one when accessing Key Vault. What should the developer do?",["Remove the Key Vault Secrets User role from the environment's identity","Switch both identities to system-assigned identities on the same app","Pass managed_identity_client_id or set AZURE_CLIENT_ID to the right identity","Store a client secret for the right identity in the app's configuration"],2,"When several user-assigned identities are attached, specify which one to use: set AZURE_CLIENT_ID or pass managed_identity_client_id to DefaultAzureCredential. Switching to system-assigned is tempting, but only one exists per resource.","Domain 4: DefaultAzureCredential with user-assigned identities"],
    ["x48",0,4,"An app running in Azure with a managed identity unexpectedly authenticates as a service principal. The team finds AZURE_TENANT_ID, AZURE_CLIENT_ID and AZURE_CLIENT_SECRET set in its app settings from an old test. Why does this happen?",["DefaultAzureCredential tries environment credentials before managed identity","Managed identities are disabled whenever any AZURE_ prefixed setting exists","Service principals always outrank managed identities in Microsoft Entra ID","The managed identity token expired, so the SDK switched to the secret"],0,"DefaultAzureCredential tries EnvironmentCredential early, before ManagedIdentityCredential, so leftover secret variables win. Remove them to use the managed identity. Token expiry is tempting but the SDK renews tokens automatically.","Domain 4: DefaultAzureCredential chain"],
    ["x49",0,4,"A compliance team requires that deleted Key Vault secrets can never be permanently removed until the retention period ends, even by an administrator. What should be enabled?",["Soft delete with a short retention","Purge protection on the vault","RBAC permission model on the vault","A private endpoint for the vault"],1,"Purge protection prevents purging deleted vaults and objects until the retention period expires, and it cannot be turned off once enabled. Soft delete is tempting since it enables recovery, but without purge protection a deleted item can still be purged.","Domain 4: Key Vault purge protection"],
    ["x50",0,4,"A vault uses the Azure RBAC permission model. A deployment pipeline's identity must create and update secrets but should not manage keys or certificates. Which role fits?",["Key Vault Secrets User","Key Vault Administrator","Key Vault Reader","Key Vault Secrets Officer"],3,"Key Vault Secrets Officer can perform any action on secrets, but not keys or certificates. Secrets User is tempting because it is the usual app role, but it is read-only.","Domain 4: Key Vault RBAC roles"],
    ["x51",0,4,"An app reads Key Vault secret DbPassword by a reference that includes a specific version ID. After rotation creates a new version, the app still uses the old password. What should change?",["Reference the secret without a version so the latest is used","Enable soft delete so old versions are removed automatically","Change the vault to the access policy permission model","Give the app's identity the Key Vault Administrator role"],0,"A versioned reference always returns that exact version. Omitting the version resolves to the current version (the platform refreshes versionless references periodically). Adding permissions is tempting but the app can already read the pinned version.","Domain 4: Key Vault secret versions"],
    ["x52",0,4,"A Container App needs a database password from Key Vault as a secret that the app can reference in environment variables, without copying the value into the app definition. What should be configured?",["An environment variable containing the Key Vault secret value copied at deployment","A Dapr component that writes the secret value into the image during the build","A Container Apps secret that references the Key Vault secret via a managed identity","A Key Vault access policy that grants the Container Apps environment read rights"],2,"Container Apps secrets can reference a Key Vault secret URI and use a managed identity with read access, and environment variables then use secretRef. Granting the environment an access policy is tempting, but the app still needs the reference and the identity.","Domain 4: Key Vault references in Container Apps"],
    ["x53",0,4,"A release must ship a fixed, immutable set of App Configuration key-values so that the exact configuration used can be reviewed and reused later, even as keys change. What should the team use?",["A sentinel key with refresh","A label named after the release","An App Configuration snapshot","A feature flag for each key-value"],2,"Snapshots capture a point-in-time, immutable set of key-values that apps can load by name. Labels are tempting because they group keys by release, but the values under a label can still be changed.","Domain 4: App Configuration snapshots"],
    ["x54",0,4,"A team wants to turn a new checkout flow on for a subset of users and off again instantly, without redeploying the app. Which App Configuration capability fits?",["Feature flags","Snapshots","Key Vault references","Private endpoints"],0,"App Configuration feature flags, used with the feature management libraries, can turn features on or off and target subsets of users at runtime. Snapshots are tempting since they relate to releases, but they are immutable.","Domain 4: App Configuration feature flags"],
    ["x55",0,4,"A Python app using the Azure Monitor OpenTelemetry Distro must record a custom count of orders placed so it can chart and alert on it. What should the developer use?",["A log message per order and a manual count in the Azure portal","A new Application Insights resource dedicated to order data","An availability test that calls the order endpoint every minute","An OpenTelemetry meter with a counter that is incremented per order"],3,"The OpenTelemetry metrics API (get_meter, create_counter, add) records custom metrics that the distro exports to Application Insights for charts and alerts. Logging each order is tempting but does not produce a metric by itself.","Domain 4: Custom metrics with OpenTelemetry"],
    ["x56",0,4,"During a deployment, engineers want to watch request rates, failures and server health in near real time rather than waiting for normal ingestion. Which Application Insights feature fits?",["Sampling","Live metrics","Availability tests","Smart detection"],1,"Live metrics streams key metrics with about a second of latency, which is ideal during deployments. Availability tests are tempting since they also check health, but they run periodic external probes.","Domain 4: Application Insights live metrics"],
    ["x57",0,4,"Engineers want each failed request listed together with the exception type that caused it in Application Insights. Which KQL approach fits?",["Join requests with exceptions on operation_Id, then project the fields","Union requests and exceptions, then take the first 100 rows returned","Query exceptions only and summarize count() by bin(timestamp, 1h)","Query requests only, then render a timechart of the success column"],0,"Requests and exceptions from the same operation share operation_Id, so joining on it pairs each failed request with its exception. union is tempting because it combines both tables, but it does not correlate rows.","Domain 4: KQL joins"],
    ["x58",0,4,"A team wants an alert when a Container App's average CPU exceeds 80% for 10 minutes, evaluated quickly with low cost. Which alert type fits?",["A log search alert on a custom KQL query","An activity log alert for the container app","A metric alert on the CPU usage metric","An Application Insights availability test"],2,"Metric alerts evaluate platform metrics such as CPU efficiently and near real time. Log search alerts are tempting because they are flexible, but they are better for conditions that need a KQL query over logs.","Domain 4: Metric vs log search alerts"],
    ["x59",0,4,"A Container App revision fails to start, and the console logs are empty because the container never ran. Where should the developer look next?",["The ACR repository's tag history","The system logs (ContainerAppSystemLogs)","The Key Vault diagnostic audit logs","The Application Insights dependencies"],1,"System logs record platform events such as image pull failures, probe failures and scaling, which explain why a container never started. Application Insights is tempting but needs the app to be running to send telemetry.","Domain 4: Troubleshooting Container Apps"],
    ["x60",0,4,"A function app's identity was given the Cosmos DB Account Reader role in the portal's access control (IAM) blade, but data-plane reads using Entra ID fail with a forbidden error. What is missing?",["A Key Vault access policy for the Cosmos DB account keys","A higher consistency level configured on the Cosmos DB account","A private endpoint connecting the function app to Cosmos DB","A Cosmos DB built-in data role assignment, such as Data Reader"],3,"Cosmos DB data-plane access with Entra ID uses its own role definitions (Built-in Data Reader or Data Contributor) assigned through Cosmos DB SQL role assignments. Control-plane roles like Account Reader only cover management operations, which is the tempting mix-up.","Domain 4: Cosmos DB data-plane RBAC"]
  ],
  whys: {
    "x1": ["Disabling the cache makes every build slower; the goal is to let Docker reuse the dependency layer.",null,"Installing packages at startup slows every container start and makes images unreproducible.","A larger base image adds size and does nothing to preserve the pip layer between code changes."],
    "x2": ["Files deleted in a later layer still exist in the earlier layer, so secrets remain in the image.","File permissions on the host do not stop them from being sent in the build context and copied.","A private registry limits who can pull, but the secrets are still baked into the image.",null],
    "x3": [null,"EXPOSE only documents a port; it does not change which user the process runs as.","World-writable files weaken security further and do not change the running user.","ENTRYPOINT versus CMD changes how arguments are passed, not the process user."],
    "x4": ["Overwriting one tag is the current problem: the tag no longer identifies a specific build.","latest is just another mutable tag and hides which build is actually deployed.",null,"Several builds a day would share one tag, so the tag still does not identify a build."],
    "x5": ["ACR webhooks fire after registry events like pushes; they cannot start a build from a Git commit.","A scheduled job rebuilds on a timer, not per commit, and docker is not available inside it by default.",null,"Geo-replication and content trust affect distribution and signing, not automatic builds."],
    "x6": [null,"Always On keeps the app loaded but never pulls a newer image.","Platform patching updates the host, not the container image the app runs.","Autoscale adds instances; it does not detect or pull new image versions."],
    "x7": ["Baking settings into images ties images to environments and moves the setting with every swap.","Scaling out does not stop settings from moving during the next swap.","Slots can share or not share a plan; the plan has no effect on which settings swap.",null],
    "x8": ["Secrets are not stored in the image or registry, so purging images changes nothing.",null,"Revision mode controls how many revisions run; it does not refresh secrets on its own.","Recreating the environment is drastic and unnecessary; restarting the revision is enough."],
    "x9": [null,"Zero-traffic revisions are a multiple revision mode behavior, not single revision mode.","Even splitting needs multiple revision mode with explicit weights.","Changing the image is a revision-scope change, so it always creates a new revision."],
    "x10": ["A lower concurrency target scales out sooner under load but still starts from zero after idle.","A bigger image typically starts slower, making the cold start worse.",null,"Ingress visibility has no effect on whether a replica is already running."],
    "x11": ["HTTP scale rules react to HTTP traffic, not queue backlog, and apps are meant to keep running rather than exit.",null,"A schedule runs whether or not messages exist and may lag behind bursts.","A permanently running replica costs money when the queue is empty, which the requirement rules out."],
    "x12": ["An unschedulable request leaves the pod Pending; it would never start and be killed.","A pull failure shows ErrImagePull or ImagePullBackOff, not OOMKilled.","A selector mismatch stops traffic reaching the pod but does not kill the container.",null],
    "x13": ["ConfigMaps are meant for non-confidential data, so the password should not go there.","Baking values into images exposes the password and forces a rebuild for any change.","Services expose pods on the network, and labels identify objects; neither stores config.",null],
    "x14": ["LoadBalancer provisions an external IP, exposing the pods outside the cluster.",null,"NodePort opens a port on every node, which exposes the service beyond cluster workloads.","ExternalName maps to an outside DNS name; it does not front the Deployment's pods."],
    "x15": ["Stronger consistency changes what reads see, not whether a later write overwrites an earlier one.","Upsert still overwrites the whole item, so the lost-update problem remains.",null,"Indexes affect query cost, not write conflicts."],
    "x16": [null,"Spatial indexes support geospatial queries, not multi-property sorting.","Vector indexes support similarity search on embeddings, not ORDER BY on scalar values.","Excluding the paths removes index support, making the query less able to run."],
    "x17": ["The change feed reports changes; using it to delete old items is custom code and does not fire for idle items.",null,"A scheduled delete job works but is exactly the cleanup code the requirement wants to avoid.","Indexing policies cannot exclude items by age; they control which paths are indexed."],
    "x18": ["Latest version mode does not emit deletes, whatever lease setup is used.","Consistency level does not change which operations appear in the change feed.","The Functions trigger uses latest version mode, so batch size does not surface deletes.",null],
    "x19": [null,"Bigger pages mean fewer round trips, but every partition is still queried and charged.","More RU/s pays for the waste instead of removing it.","Weaker consistency can trim some read cost but leaves the cross-partition fan-out in place."],
    "x20": ["Session guarantees read-your-own-writes for one client session, not a bound on lag for all readers.","Consistent prefix preserves write order but sets no limit on how far behind reads can be.",null,"Eventual gives no ordering or staleness guarantee at all."],
    "x21": ["Indexes do not need rebuilding per query; the problem is that the vectors live in different spaces.","Normalized vectors work well with cosine distance; normalization is not the issue.",null,"Chunk length affects retrieval quality, but it does not make results meaningless across models."],
    "x22": [null,"Overlap duplicates some text, so it slightly increases storage rather than reducing it.","All chunks must share one embedding model for similarity to be meaningful.","Metadata is still needed for filtering, citations and access control."],
    "x23": ["HNSW indexes do not require an extra B-tree filter to be used.","There is no such LIMIT threshold; small LIMIT values are the typical index use case.","HNSW supports L2, cosine and inner product, each through its own operator class.",null],
    "x24": ["<-> returns Euclidean (L2) distance.",null,"<=> returns cosine distance.","<+> is the L1 (taxicab) distance operator, not inner product."],
    "x25": [null,"IVFFlat computes its lists from the rows present at build time, so building on an empty table gives poor recall.","B-tree indexes do not support vector similarity search.","Zero probes would search no lists at all; probes must be at least 1."],
    "x26": ["Writing only to Redis makes the cache the source of truth and risks losing data.","Removing TTLs makes staleness worse, since keys would never refresh on their own.",null,"Flushing everything works but causes a burst of cache misses and database load after every change."],
    "x27": ["noeviction is what causes write errors when memory is full.",null,"volatile-ttl only evicts keys with a TTL; keys without one can still fill memory.","volatile-random also ignores keys without a TTL, so writes can still fail."],
    "x28": ["A longer TTL keeps entries around longer; it does nothing about loose matches.","noeviction affects what happens when memory is full, not how matches are chosen.","More capacity stores more entries but the same loose threshold still returns wrong answers.",null],
    "x29": ["A flat index does exact brute-force comparison, so it scales poorly and is meant for small vector counts.","Range indexes are for scalar filtering and sorting, not vector similarity.","Spatial indexes support geographic data types, not embeddings.",null],
    "x30": ["That describes create_item, which raises a conflict error on an existing id.",null,"Upsert replaces the whole item; partial updates use patch operations.","Each item lives in one logical partition, determined by its partition key value."],
    "x31": ["A nightly copy adds a second database to run and leaves search results up to a day stale.","Redis as the only store for this data risks losing it and duplicates the data model.",null,"Scanning blobs in application code is slow and does not scale."],
    "x32": [null,"Duplicate detection drops repeated messages; it does not preserve expired ones.","Sessions provide ordering per session and have nothing to do with expiry handling.","Auto-forwarding moves new messages elsewhere; expired messages would still be dropped."],
    "x33": ["Receiving from the main queue only returns active messages, never dead-lettered ones.",null,"Dead-lettered messages are in a subqueue of the original queue, not a separate queue you create.","Peeking the main queue cannot see messages that have already moved to the dead-letter subqueue."],
    "x34": ["Abandoning makes it available again immediately, often looping back and raising the delivery count.","Dead-lettering signals failure and needs extra handling to resubmit; deferral is built for this case.","Completing removes it from Service Bus, so a crash would lose the in-memory copy.",null],
    "x35": [null,"TTL controls when a message expires, not when it first becomes visible.","Session state stores data for a session and does not delay delivery.","Duplicate detection drops repeat sends and has no timing effect on delivery."],
    "x36": ["SQL filters work but are more expensive to evaluate than simple equality correlation filters.","Adding topics moves routing into the sender and multiplies entities to manage.",null,"There is no session ID filter; sessions group messages for ordered processing."],
    "x37": ["Subject filters compare the subject string prefix, not numeric data values.","Event type filters match the eventType field, not values inside data.",null,"Dead-lettering stores undeliverable events; it does not filter what is delivered."],
    "x38": [null,"Event Grid is for reactive event distribution, not transactional commands needing sessions.","Event Hubs lacks per-message settlement, sessions and dead-lettering for commands, and Service Bus is not built for high-volume telemetry.","Event Hubs is not the native route for Azure resource events, and Event Grid is not a telemetry ingestion stream."],
    "x39": ["local.settings.json holds app settings for local runs only and is not deployed.","function_app.py declares functions and bindings, not host-wide runtime options.","requirements.txt lists Python packages to install.",null],
    "x40": ["func start runs the functions locally and has no publish option.",null,"func new creates a new function from a template; it does not deploy.","func init creates a new local project rather than deploying an existing one."],
    "x41": [null,"An account key is exactly the shared secret the requirement wants to remove.","FUNCTIONS_WORKER_RUNTIME selects the language worker and has no identity values.","Run from package points at a deployment package; it does not configure host storage auth."],
    "x42": ["The v2 model declares bindings with decorators; function.json belongs to the v1 model.","Event Grid pushes events; it does not poll HTTP endpoints, and the dedicated trigger handles validation for you.",null,"Timers cannot pull events from an Event Grid topic, which pushes to subscribers."],
    "x43": ["A longer timeout lets executions run longer but does nothing about cold starts or VNet access.",null,"A Dedicated plan without autoscale cannot respond to load, which is a poor fit for event-driven work.","A keep-alive timer is a fragile workaround and still leaves no VNet integration on classic Consumption."],
    "x44": ["Input bindings read data into the function; they do not write documents.","host.json holds runtime settings, not per-function bindings.","App settings supply connection values; they cannot declare a binding by themselves.",null],
    "x45": ["Filters and schema affect what is delivered and its format, not what happens after failed retries.","Validation proves endpoint ownership, and batching affects throughput, not retention of failed events.","Filters and input schema do not change how long Event Grid retries or where failures go.",null],
    "x46": ["Peek-lock needs a separate settle call per message, adding overhead the app does not need.",null,"Deferral is for setting specific messages aside, not a general low-overhead receive.","Sessions add ordered, locked processing per session, which is extra overhead here."],
    "x47": ["Removing role assignments does not tell the credential which identity to use.","An app has only one system-assigned identity, so two identities cannot both become system-assigned.",null,"A client secret reintroduces a stored credential, which managed identities are meant to avoid."],
    "x48": [null,"The identity is not disabled; the credential chain simply finds the environment credential first.","Entra ID does not rank principals; the SDK chooses which credential to use.","The SDK refreshes managed identity tokens; the switch is due to chain order."],
    "x49": ["Soft delete alone lets a privileged user purge a deleted secret before retention ends.",null,"The permission model decides who can do what, but a highly privileged user could still purge.","Private endpoints restrict network access, not deletion behavior."],
    "x50": ["Secrets User can read secret values but cannot create or update them.","Administrator covers keys and certificates as well, which is broader than required.","Reader sees vault metadata only and cannot read or write secret values.",null],
    "x51": [null,"Soft delete governs deleted objects; it does not remove or bypass old versions.","The permission model affects authorization, not which version a reference resolves to.","Broader permissions do not change the fact that the reference pins an old version."],
    "x52": ["Copying the value at deploy time stores it in the app definition and misses future rotations.","Baking secrets into images exposes them to anyone who can pull the image.",null,"Vault access alone does not make a secret available to the app; the app must define the reference."],
    "x53": ["A sentinel key triggers refresh when changed; it does not freeze the values.","Label values can still be edited afterwards, so a labeled set is not immutable.",null,"Feature flags toggle features; they do not capture a fixed set of key-values."],
    "x54": [null,"Snapshots freeze configuration; they are not for toggling features on and off.","Key Vault references point to secrets, not feature toggles.","Private endpoints secure network access and do not control app behavior."],
    "x55": ["Counting log lines by hand is not a metric and cannot drive alerts reliably.","A separate resource adds cost and still needs something to record the metric.","Availability tests probe uptime; they do not count business events.",null],
    "x56": ["Sampling reduces telemetry volume; it does not provide a real-time view.",null,"Availability tests probe endpoints from outside on a schedule.","Smart detection raises alerts about anomalies after analysis, not a live stream."],
    "x57": [null,"A union just stacks rows; it does not link each request to its exception.","Counting exceptions per hour loses the connection to individual requests.","A chart of successes shows trends but not which exception caused each failure."],
    "x58": ["Log search alerts work but run queries on a schedule and cost more for a simple platform metric.","Activity log alerts fire on management operations, not resource utilization.",null,"Availability tests check endpoint responses, not CPU."],
    "x59": ["Tag history shows which images exist, not why the platform failed to start one.",null,"Key Vault logs only help if the failure is about secrets, and they will not show platform start errors.","Dependencies are recorded by running code; a container that never started records nothing."],
    "x60": ["Key Vault policies affect key storage, and the goal is to read data with Entra ID, not keys.","Consistency level changes read guarantees, not whether a principal is authorized.","A private endpoint changes network paths; the error is an authorization failure.",null]
  },
  levels: {
    "az1": 2, "az2": 2, "az3": 1, "az4": 2, "az5": 3, "az6": 1, "az7": 2, "az8": 3, "az9": 1, "az10": 2,
    "az11": 2, "az12": 1, "az13": 1, "az14": 2, "az15": 1, "az16": 1, "az17": 1, "az18": 3, "az19": 2, "az20": 1,
    "az21": 2, "az22": 2, "az23": 3, "az24": 3, "az25": 2, "az26": 1, "az27": 1, "az28": 2, "az29": 3, "az30": 3,
    "az31": 1, "az32": 2, "az33": 2, "az34": 1, "az35": 3, "az36": 3, "az37": 3, "az38": 1, "az39": 2, "az40": 2,
    "az41": 2, "az42": 3, "az43": 2, "az44": 1, "az45": 3, "az46": 3, "az47": 1, "az48": 2, "az49": 2, "az50": 1,
    "az51": 2, "az52": 2, "az53": 1, "az54": 2, "az55": 2, "az56": 2, "az57": 2, "az58": 1, "az59": 2, "az60": 1,
    "az61": 3, "az62": 1, "az63": 3, "az64": 2, "az65": 1, "az66": 3, "az67": 3, "az68": 2, "az69": 2, "az70": 1,
    "az71": 1, "az72": 2, "az73": 2, "az74": 2, "az75": 3, "az76": 3, "az77": 2, "az78": 3, "az79": 2, "az80": 1,
    "az81": 2, "az82": 2, "az83": 1, "az84": 1, "az85": 1, "az86": 1, "az87": 1, "az88": 3, "x1": 2, "x2": 1,
    "x3": 2, "x4": 2, "x5": 2, "x6": 2, "x7": 3, "x8": 3, "x9": 2, "x10": 2, "x11": 3, "x12": 2,
    "x13": 1, "x14": 1, "x15": 2, "x16": 2, "x17": 2, "x18": 3, "x19": 2, "x20": 3, "x21": 2, "x22": 1,
    "x23": 3, "x24": 1, "x25": 3, "x26": 2, "x27": 2, "x28": 3, "x29": 2, "x30": 1, "x31": 3, "x32": 2,
    "x33": 2, "x34": 3, "x35": 1, "x36": 2, "x37": 2, "x38": 3, "x39": 2, "x40": 1, "x41": 2, "x42": 2,
    "x43": 3, "x44": 2, "x45": 3, "x46": 1, "x47": 3, "x48": 3, "x49": 2, "x50": 2, "x51": 2, "x52": 2,
    "x53": 3, "x54": 1, "x55": 2, "x56": 2, "x57": 3, "x58": 2, "x59": 2, "x60": 3
  }
});
