CertHub.addWhys("cysa-plus", {
  "cy1": ["DNS cache poisoning corrupts a resolver's cached answers so users are sent to a malicious IP; it does not generate a flood of outbound queries with long random subdomains.", null, "A DGA makes malware query many different generated domain names, not many random subdomains under one single domain.", "CDN traffic uses a modest number of predictable hostnames and would not produce hundreds of queries a minute with long random labels to one domain."],
  "cy2": [null, "A port scan touches many ports or hosts in a short burst; it does not repeat a connection to one IP at a fixed interval around the clock.", "ARP spoofing is a local-segment attack that forges MAC-to-IP mappings and would not appear as regular connections to an external IP in NetFlow.", "Backups usually run on a schedule such as nightly and move large, varying volumes of data, not tiny identical packets every 60 seconds all day."],
  "cy3": ["Perimeter firewall logs show source and destination IPs and ports, but they cannot see which process on the endpoint opened the connection.", "DHCP logs map IP addresses to MAC addresses and hostnames over time; they record nothing about processes or outbound connections.", null, "NetFlow summarizes conversations by IP, port and byte count at the network level and has no visibility into the process that created them."],
  "cy4": ["If the lockout policy had worked, the account would have locked after a few failures and there would be no successful 4624 logon from the attacker's IP.", "Nothing here shows an account gaining higher rights; the pattern is repeated failed logons followed by a success for the same account, not privilege escalation.", "Clearing the audit log generates Event ID 1102, and the failed and successful logon events are still present, so the log was not cleared.", null],
  "cy5": ["More correlation rules would create more alerts, but they would not remove the manual lookup work analysts do on each reported phishing email.", "Hiring adds cost and headcount while leaving the same slow, repetitive manual process in place; it does not improve efficiency.", null, "Longer retention keeps data around longer but does nothing to speed up the reputation and hash lookups analysts perform by hand."],
  "cy6": ["File hashes sit at the bottom of the Pyramid of Pain; changing a single byte of the malware produces a new hash and evades the detection.", "Attackers can switch IP addresses cheaply and quickly, so IP-based detections are easy to evade.", "Domains are only slightly harder to change than IPs; attackers routinely register new ones, so they are not the hardest indicator to evade.", null],
  "cy7": ["STIX is the structured language and data format for describing threat intelligence, not the transport protocol that moves it between platforms.", null, "OpenIOC is an XML format for describing indicators of compromise; it does not define an HTTPS transport for exchanging them.", "YARA is a rule language for matching patterns in files and memory, not a protocol for sharing intelligence between organizations."],
  "cy8": [null, "Waiting for SIEM alerts is reactive detection; threat hunting is proactive searching for activity that existing alerts may be missing.", "Reimaging all admin workstations is a disruptive remediation step taken without any evidence of compromise; hunting starts with investigation, not cleanup.", "A vulnerability scan finds missing patches and misconfigurations, but it will not reveal attackers abusing legitimate built-in admin tools."],
  "cy9": ["Office updates run through the Click-to-Run update service, not by having winword.exe launch PowerShell with an encoded command.", null, "A memory leak causes growing memory use or crashes; it does not make Word spawn a new PowerShell process with encoded arguments.", "A scheduled task would be launched by the Task Scheduler service, not as a child of winword.exe right after an attachment was opened."],
  "cy10": ["Nmap is a network scanner for discovering hosts, ports and services; it has no features for decoding Base64 or decompressing gzip data.", "Hashcat cracks password hashes; Base64 and gzip are reversible encodings and compression, not hashes, so there is nothing to crack.", null, "Metasploit is an exploitation framework for penetration testing, not a tool for decoding and decompressing strings found in logs."],
  "cy11": ["SPF lists which IPs may send mail for a domain, but it has no mechanism to publish a quarantine or reject policy or to check alignment with the From header.", "DKIM adds a cryptographic signature to messages, but it does not tell receivers what to do when checks fail.", null, "S/MIME encrypts and signs individual messages end to end using certificates; it is not a DNS-published domain policy for receiving servers."],
  "cy12": ["Running unknown malware on an analyst workstation with antivirus disabled risks infecting the analyst's machine and spreading into the production network.", "A hash lookup only tells you something if the exact file is already known; it shows no runtime behavior such as dropped files or contacted domains.", null, "Emailing malware to a vendor is slow, may be blocked in transit and does not give the analyst the behavioral details needed now."],
  "cy13": ["Short retention would cause older events to be missing entirely, not appear with timestamps in the wrong order.", "Hitting a SIEM license limit can drop or delay ingestion, but it does not change the timestamps recorded by the source devices.", "Verbose logging increases the amount of detail recorded but does not affect whether the source clocks agree with each other.", null],
  "cy14": [null, "VLAN segmentation separates traffic on local switches; it does not deliver cloud-based security services to remote users or combine them with SD-WAN.", "NAC checks devices before they join the local network, which does little for a mostly remote workforce and is not a cloud-delivered security stack.", "A jump box is a single hardened host for administrative access, not an architecture that combines SD-WAN with SWG, CASB and ZTNA."],
  "cy15": ["A shared admin account removes individual accountability and still leaves standing privilege; it makes the problem worse, not better.", null, "Longer passwords make credentials harder to guess, but the 40 accounts would still hold permanent domain admin rights.", "SSO makes sign-in more convenient across applications but does not reduce the amount or duration of privileged access."],
  "cy16": [null, "Denial of service overwhelms a target to make it unavailable; a single server uploading data to cloud storage is outbound transfer, not an availability attack.", "A rogue device is unauthorized hardware on the network; this is a known finance server behaving abnormally, not a new device.", "Beaconing is small, regular check-ins to a C2 server; a single 40 GB upload is a bulk transfer, which points to exfiltration."],
  "cy17": ["A Wi-Fi passphrase protects wireless access, but this device is plugged into a wired switch port in the conference room.", "A HIDS runs on individual hosts and cannot stop an unknown device from being granted access through a switch port.", "DLP watches for sensitive data leaving the organization; it does not control which devices can connect to the network.", null],
  "cy18": ["XSS injects script such as a script tag into pages viewed by other users; this payload uses quotes and an always-true OR condition aimed at a database query.", "Directory traversal uses ../ sequences to reach files outside the web root; there are none in this request.", null, "Command injection appends OS commands with characters such as ; or |, whereas this payload manipulates SQL logic."],
  "cy19": ["SSRF makes the server request a URL of the attacker's choosing, such as an internal service; this request instead tries to climb the local file system with ../.", "XXE abuses external entity declarations inside XML input; this is a simple GET parameter with no XML involved.", "CSRF tricks a logged-in user's browser into sending an unwanted request; it does not use ../ sequences to read server files.", null],
  "cy20": ["Lateral movement is moving from one compromised host to others; a startup service and Run key keep the attacker on this same host.", null, "Reconnaissance is gathering information about targets; creating startup mechanisms happens after compromise and serves a different purpose.", "Exfiltration is moving data out; services and Run keys only ensure the malware relaunches after a reboot."],
  "cy21": ["Disabling noisy data sources removes visibility and could hide real attacks along with the false positives.", "Forwarding alerts to management only spreads the noise; it does not improve alert quality or reduce the workload.", null, "Lowering thresholds makes rules fire more often, which would increase the number of false positives."],
  "cy22": [null, "Dark web forums can contain useful intelligence, but they are unvetted, risky to access and not organized around sharing among healthcare peers.", "A general antivirus vendor blog covers broad threats and is not tailored to the healthcare sector or shared by peer organizations.", "Social media OSINT is unverified and noisy, and relying on it alone gives no structured, sector-specific peer sharing."],
  "cy23": ["Script kiddies use existing public tools and lack the resources and patience for custom malware and a 14-month covert operation.", "Hacktivists usually seek publicity, such as defacements or leaks, rather than quietly stealing engineering designs for over a year.", null, "An unintentional insider causes harm by mistake; they would not deploy custom malware or run a long, stealthy theft campaign."],
  "cy24": ["Relevancy asks whether intelligence applies to your organization; the problem here is that the indicators arrive too late to be useful.", "Confidence level describes how sure the source is about an assessment; stale indicators are a delivery-speed problem, not a confidence rating.", "Attribution accuracy is about correctly identifying who is behind activity; the feed's issue is that its IPs are outdated by the time they arrive.", null],
  "cy25": [null, "/etc/shadow stores hashed account passwords, not a record of login attempts.", "/var/log/dmesg holds kernel ring buffer messages such as hardware and driver events, not SSH authentication records.", "/etc/hosts is a static hostname-to-IP mapping file and contains no log data at all."],
  "cy26": ["Legitimate svchost.exe always runs from C:\\Windows\\System32 (or SysWOW64); running from C:\\Users\\Public is not normal behavior.", "Driver updates are handled by Windows components in system folders and would not make svchost.exe run from a public user folder.", "A corrupted page file causes memory errors or crashes; it cannot relocate a system binary to a user-writable directory.", null],
  "cy27": ["Letting AI close alerts with no human review risks dismissing real incidents, because AI output can be wrong or miss context.", null, "Pasting customer records into a public chatbot can leak sensitive data to an unapproved third party and violate privacy obligations.", "Turning off logging removes accountability for what the assistant did; its actions should be logged like any other tool's."],
  "cy28": ["A non-credentialed external scan infers versions from banners and exposed services, so it misses many local patches and produces more false positives.", null, "Passive monitoring only sees what crosses the network and cannot read installed patches or local configuration settings.", "A ping sweep only discovers which hosts are alive; it does not check for patches or configuration weaknesses."],
  "cy29": [null, "Agentless scanning from headquarters can only reach devices while they are on the corporate network, which these laptops rarely are.", "External perimeter scanning looks at internet-facing systems, not employee laptops sitting on home or public networks.", "A quarterly manual review is slow, infrequent and error-prone, leaving laptops unassessed for long periods."],
  "cy30": ["Aggressive active scanning can crash fragile PLCs, and doing it during production hours risks halting the line.", "Fuzzing deliberately sends malformed input to cause failures, which is exactly the kind of disruption the plant must avoid.", null, "Running exploit modules against live PLCs could crash or alter them, creating the safety and downtime risk the team wants to avoid."],
  "cy31": ["AV:N means network, not physical (AV:P), and UI:N means no user interaction is needed, so neither physical access nor a logged-in user is required.", "AV:N means exploitable across the network, not just the local subnet (AV:A), and PR:N means no admin or any other privileges are needed.", "UI:N means no user interaction is required, so the attacker does not need anyone to click a link.", null],
  "cy32": ["Ease of patching does not outweigh risk; an isolated lab host is far less exposed than an internet-facing appliance that is actively exploited.", "Alphabetical order has nothing to do with risk; prioritization should consider exposure and known exploitation, not names.", null, "Waiting for a quarterly window leaves an internet-facing, actively exploited vulnerability open to attack for months."],
  "cy33": ["A true positive would mean the vulnerability is really present, but the backported fix means the server is actually patched.", null, "A false negative is a real vulnerability the scanner failed to report; here the scanner reported one that does not exist.", "A true negative is when the scanner correctly reports nothing; here it did raise a finding, just an incorrect one."],
  "cy34": [null, "Accepting risk without documentation leaves no record of the decision, the owner or the review date, so the risk becomes invisible.", "Excluding the device from scans hides the problem and removes visibility into new issues; it reduces no risk at all.", "Disabling logging reduces visibility into attacks against an already vulnerable device, making the situation worse."],
  "cy35": ["The CVSS base score rates a vulnerability's technical severity; it does not estimate how likely exploitation is in the next 30 days.", null, "CWE is a catalog of software weakness types such as buffer overflows, not a scoring system for exploitation probability.", "CPE is a naming scheme for identifying products and versions, not a way to score or predict exploitation."],
  "cy36": ["DAST tests a running application from the outside, so it requires executing the app rather than analyzing source code.", "Fuzzing feeds unexpected input to a running program to trigger crashes; it does not analyze source code at rest.", null, "Penetration testing attacks a running system, typically with manual techniques, rather than analyzing source code without running it."],
  "cy37": ["Moving telnet to port 2323 is security through obscurity; credentials still cross the network in cleartext.", "A login banner provides legal notice but does nothing to encrypt the credentials and commands sent over telnet.", "Blocking ICMP stops ping responses but leaves the cleartext telnet service on 23/tcp fully reachable.", null],
  "cy38": [null, "A WAF filters HTTP traffic to web applications; it does not audit cloud account settings like bucket permissions or security groups.", "A HIDS monitors activity on individual hosts and has no view of cloud control-plane configuration across accounts.", "An OT protocol scanner is built for industrial devices such as PLCs, not for cloud storage and security group settings."],
  "cy39": ["A false positive is a reported vulnerability that is not real; here the vulnerability and patch are real but deployment is delayed.", "Risk transference shifts impact to a third party, such as through insurance; delaying a patch for business reasons does not transfer risk.", "A zero-day has no patch available; here a patch is ready and is only being held back for business reasons.", null],
  "cy40": ["TLS encrypts traffic in transit, but the injected script is stored on the server and still runs in visitors' browsers over HTTPS.", "Account lockout defends against password guessing and has no effect on script stored in a comment field.", null, "Parameterized queries prevent SQL injection by separating data from SQL code; they do not stop script from executing in browsers."],
  "cy41": ["Hiding error messages makes SQL injection harder to discover, but the concatenated query is still exploitable, for example through blind techniques.", null, "HTML output encoding defends against XSS in the browser; it does not stop user input from changing a SQL statement.", "Moving the database to another server changes where it runs, but the vulnerable query construction stays the same."],
  "cy42": ["CSRF abuses a victim's browser to send authenticated requests; here the server itself fetched an internal URL on the attacker's behalf.", "XXE uses malicious XML external entities to read files or make requests; this attack simply supplied a URL, with no XML involved.", "IDOR is accessing another user's object by changing an identifier; here the server was made to reach an internal metadata service.", null],
  "cy43": [null, "A buffer overflow writes past the end of a memory buffer; changing an ID in a URL to view another record is an authorization flaw.", "A race condition depends on the timing of concurrent operations; simply editing the account ID involves no timing issue.", "Session fixation forces a victim to use a session ID the attacker knows; this user never took over anyone's session, they just changed an ID."],
  "cy44": [null, "Input masking and autocomplete are user interface features for forms; they have no effect on memory corruption exploits.", "Code signing verifies software publishers and certificate pinning protects TLS connections; neither hinders exploiting a buffer overflow in memory.", "SPF and DKIM are email authentication mechanisms and have nothing to do with operating system memory protection."],
  "cy45": ["Acceptance means acknowledging the risk and taking no further action; buying insurance is an action that shifts financial impact.", "Avoidance means stopping the risky activity altogether; the organization still faces ransomware risk after buying insurance.", "Mitigation reduces the likelihood or impact of the threat with controls such as backups or EDR; insurance does not make an attack less likely or less damaging.", null],
  "cy46": ["Stopping database scans removes visibility into vulnerabilities on critical systems instead of fixing the performance problem.", "Annual scans leave vulnerabilities undetected for up to a year, which is far too infrequent for production databases.", null, "Scanning only from outside the firewall misses most internal findings and does not address the timing of the scans."],
  "cy47": [null, "SAST analyzes your own source code for flaws, but it is not designed to inventory third-party libraries and match them to known CVEs.", "A WAF filters web traffic at runtime; it does not identify which vulnerable libraries are included in the application.", "DLP prevents sensitive data from leaving the organization and has nothing to do with software dependencies."],
  "cy48": ["The cookie's name has no effect on security; the missing Secure and HttpOnly flags are the actual weakness.", null, "Putting session IDs in the URL exposes them in logs, browser history and Referer headers, which is less secure than a cookie.", "Disabling cookies site-wide would break session management; the fix is to set the correct flags on the session cookie."],
  "cy49": ["Reconnaissance is researching targets, such as harvesting employee names; sending the attachment comes later.", "Weaponization is building the malicious payload, such as pairing an exploit with a document, before it is sent to anyone.", null, "Exploitation happens when the payload actually runs on the victim's system, after the email has been delivered and opened."],
  "cy50": ["The adversary feature describes the attacker or group itself, not the servers and domains they use.", "Capability covers the attacker's tools and techniques, such as malware and exploits, rather than the C2 servers they run on.", "The victim feature is the targeted organization, person or asset, not the attacker's command-and-control systems.", null],
  "cy51": [null, "The OWASP Testing Guide describes how to test web applications for vulnerabilities; it does not assign technique IDs to adversary behavior like credential dumping.", "The NIST CSF organizes security program outcomes into functions such as Identify and Protect; it does not catalog specific attacker techniques with IDs.", "The Cyber Kill Chain has seven broad phases with no technique IDs, so it cannot label credential dumping at the level needed to compare detection coverage."],
  "cy52": ["Containment limits the spread, and the host was already isolated; removing the malware and persistence is the next phase.", null, "Recovery restores systems to normal operation and monitors them; it follows after the threat has been removed.", "Preparation happens before an incident, building plans, tools and training, not cleaning up an active infection."],
  "cy53": ["Paying the ransom does not stop encryption already in progress and gives no guarantee of decryption; containment is still needed.", "Powering off every server company-wide causes massive disruption and destroys volatile evidence, when isolating the affected hosts would contain the spread.", "Restoring onto hosts that are still infected lets the ransomware encrypt the restored data again; contain first.", null],
  "cy54": ["A disk image is important, but disk contents survive shutdown, so it can be collected after RAM.", null, "Backup tapes are archival and among the least volatile sources, so they are collected last if at all.", "Archived logs on the SIEM are stored remotely and persist after the server is shut down."],
  "cy55": [null, "Encrypting evidence protects its confidentiality, but that is a separate control from documenting who handled it.", "The root cause belongs in the incident report or lessons-learned documentation, not in a chain of custody form.", "Customer breach notification is a legal and communications task, unrelated to tracking evidence handling."],
  "cy56": ["Matching file counts does not prove every byte is identical, and it ignores slack space, unallocated space and deleted data.", "The serial number identifies the physical drive but says nothing about whether the image contents match it.", null, "Visually comparing a few files is a small, subjective sample and cannot prove a bit-for-bit copy."],
  "cy57": ["Preparation is building capabilities before incidents; while its outputs feed back into preparation, this review of a closed incident is post-incident activity.", "Detection and analysis is identifying and scoping an active incident, not reviewing a closed one two weeks later.", "Containment limits damage during an active incident; this incident is already closed.", null],
  "cy58": [null, "Detection and analysis happens during a real incident, identifying and scoping it; a tabletop is practice done beforehand.", "Eradication removes a real threat from systems; a tabletop simulates a scenario without touching any systems.", "Recovery restores affected systems after a real incident; a tabletop exercise involves no actual restoration."],
  "cy59": ["Eradication removes the malware, which comes after you know every affected host; searching telemetry is about finding them first.", null, "The lessons-learned report is written after the incident closes, not while still searching for other infected endpoints.", "Regulator notification is decided by legal and compliance; searching for IoCs on endpoints is a technical scoping step."],
  "cy60": [null, "A retention purge deletes data on schedule, which is exactly what must be stopped when litigation is expected.", "Chain of custody documents who handled collected evidence; it does not stop routine retention policies from deleting emails and logs.", "A right-to-erasure request asks an organization to delete personal data, the opposite of preserving it."],
  "cy61": ["A new hostname does nothing to ensure the server is free of the attacker's foothold or that the entry point is closed.", "Deleting the ransom note is cosmetic; the malware or the vulnerability that let it in could still be present.", null, "Reinstalling antivirus from the same backup could reintroduce compromised files and does not address how the attacker got in."],
  "cy62": ["Containment limits the spread during an active incident; determining how the attacker got in afterward is a different activity.", "Triage assesses and prioritizes alerts or incidents early on, not the post-incident determination of the initial entry point.", null, "Threat modeling proactively identifies potential threats to a system during design; it is not an investigation of an incident that already happened."],
  "cy63": ["Installation is when malware establishes persistence on the victim host, which happens before the attacker achieves the goal.", null, "Command and control is the channel the attacker uses to operate the implant; stealing the data is the objective itself.", "Delivery is sending the weapon to the target, such as by email, at the start of the intrusion."],
  "cy64": [null, "Eradication removes the threat from the host; here the attacker's access is deliberately kept under observation.", "Recovery returns clean systems to normal operation; the host is still compromised and being watched.", "Reverse engineering analyzes malware code to understand how it works; moving the host to a monitored VLAN is not code analysis."],
  "cy65": ["If the attacker can read corporate email, urgent messages are just as visible to them, tipping them off to the response.", "A company-wide chat may also be compromised and exposes sensitive incident details to all employees.", "Waiting to communicate stalls the response; the team needs a secure channel to coordinate now.", null],
  "cy66": ["Nessus is a vulnerability scanner for live systems; it cannot parse a memory dump to list processes or injected code.", "Nmap scans networks for hosts and open ports and has no memory forensics capability.", "Burp Suite is a web application testing proxy, not a tool for analyzing Windows memory images.", null],
  "cy67": ["An acceptable use policy defines how employees may use company resources; it does not give responders step-by-step incident actions.", null, "A business impact analysis identifies critical functions and the impact of disruption, not specific response steps for phishing.", "An SLA defines service commitments between parties, such as uptime or response times, not incident handling procedures."],
  "cy68": ["Raw scanner output is far too technical and voluminous for board members to act on.", "Plugin IDs and port numbers are technical details for the remediation team, not board-level information.", null, "Command-line remediation steps are for administrators, not executives making risk and funding decisions."],
  "cy69": ["Mean time to detect measures how quickly incidents or threats are identified, not how long vulnerabilities stay open after discovery.", null, "The number of scans completed measures scanning activity, not how quickly the findings get fixed.", "The false positive rate measures scanner accuracy and says nothing about how long real vulnerabilities remain open."],
  "cy70": ["RTO is the target time to restore a system after an outage, not a measure of how long attackers go undetected.", "RPO is the maximum acceptable data loss measured in time since the last backup, unrelated to detecting compromises.", "Mean time to respond measures how long it takes to act after an incident is identified, not the time until it is identified.", null],
  "cy71": [null, "Marketing handles promotion and brand messaging, not the employment actions an insider case may require.", "Facilities manages buildings and physical space; it has no role in handling potential employee misconduct.", "Vendor sales deals with external suppliers and is not involved in an internal investigation of an employee."],
  "cy72": ["A tier 1 analyst detects and escalates incidents but lacks the legal authority and expertise to decide on regulatory notification.", null, "The help desk supports users with IT issues and has no role in interpreting breach notification laws.", "Network engineering maintains infrastructure; notification decisions are legal and compliance matters."],
  "cy73": ["Confirming details to a reporter without authorization can spread inaccurate information and create legal exposure.", "Denying an incident that may have occurred is dishonest and can badly damage trust if the breach is later confirmed.", null, "Nothing shared with a reporter is truly off the record for the organization; sharing the timeline leaks sensitive details without approval."],
  "cy74": ["The vulnerability is real, so marking it a false positive would misrepresent the risk and hide it from oversight.", "Removing the finding hides a known risk from decision-makers instead of tracking it until it is fixed.", "The CVSS score reflects the vulnerability's severity; lowering it artificially does not reduce the actual risk. Compensating controls belong in the exception record instead.", null],
  "cy75": [null, "Full packet captures are raw technical evidence that belongs in an appendix or the technical section, not the executive summary.", "Every command the responders ran is detailed technical information for the incident record, not a summary for decision-makers.", "Raw SIEM queries are technical artifacts for analysts, not something executives need to read."],
  "cy76": ["Stopping scans of new VMs hides the problem while every new build stays vulnerable.", "Raising the reporting threshold hides the finding without fixing the vulnerable build source.", null, "Patching each VM faster still leaves every new VM vulnerable at deployment; fixing the image removes the flaw at its source."],
  "cy77": ["Mean time to detect measures how fast threats are identified, not whether fixes met a required deadline.", null, "Scanner coverage measures how many assets are scanned, not whether findings were fixed within the policy window.", "The risk acceptance rate tracks how many findings were formally accepted rather than fixed, not how many missed the 15-day deadline."],
  "cy78": [null, "Incident reports should focus on fixing processes and controls; a blame-focused report discourages honest reporting.", "SIEM licensing has no requirement for incident reports to include root cause or recommendations.", "Chain of custody documentation tracks evidence handling and must be kept separately; the report does not replace it."],
  "cy79": ["Posting details on the intranet spreads sensitive information widely and could alert an insider attacker; it is not an escalation step.", "Emailing all employees may tip off the attacker and causes confusion; the IR lead must be engaged first.", "Waiting until the end of the shift delays the response to active exploitation, allowing more damage.", null],
  "cy80": ["An overall risk grade alone does not tell administrators which systems to fix or how to fix them.", null, "Stock price impact is a business concern for executives, not information administrators need to remediate findings.", "A list of every scan ever run is historical noise that does not help administrators fix the current findings."]
});
