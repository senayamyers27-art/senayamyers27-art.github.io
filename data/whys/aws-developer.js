/* "Why this answer is wrong" explanations for AWS Certified Developer – Associate (DVA-C02). */
CertHub.addWhys("aws-developer", {
  "dva1": ["A longer visibility timeout only delays the retry; the whole batch of 10 still becomes visible again when one message fails.", "FIFO with content-based deduplication prevents duplicate sends, but a failed batch is still retried as a whole without partial batch responses.", "Reserved concurrency of 1 only serializes batches; each failing batch is still returned to the queue in full.", null],
  "dva2": ["With orderDate as the partition key, one customer's orders are scattered across many partition values and cannot be fetched with a single Query.", null, "With only orderId as the key, finding one customer's orders requires a Scan, and there is no sort key to order by date.", "A status partition key groups orders by status, not by customer, and a low-cardinality key like status also creates hot partitions."],
  "dva3": ["An LSI can only be created together with the table and must reuse the table's partition key, so it cannot be added now or be keyed on email.", "Streams capture item changes for processing; they do not give you a way to query items by email.", null, "A table's primary key cannot be changed after creation; you would have to create a new table and migrate the data."],
  "dva4": [null, "GetItem on the base table supports ConsistentRead=true, so strongly consistent reads are available there.", "LSIs share the base table's partition and support strongly consistent reads when ConsistentRead=true is set.", "A Scan of the base table can also request ConsistentRead=true, so it does support strong consistency."],
  "dva5": [null, "10 RCU would cover 6 KB items only with eventually consistent reads; strongly consistent reads of 6 KB need 2 RCU each.", "15 comes from dividing 6 KB by 4 KB without rounding up; capacity is billed in whole 4 KB units, so each read costs 2 RCU.", "60 treats RCU as 1 KB units (the write unit size); read units are 4 KB, so 6 KB needs 2 RCU per read."],
  "dva6": ["30 uses 2.5 KB without rounding up; each write is billed in whole 1 KB units, so 2.5 KB costs 3 WCU.", null, "12 would only be enough for items of 1 KB or less; a 2.5 KB item needs 3 WCU per write.", "24 rounds 2.5 KB down to 2 KB; DynamoDB always rounds item size up, to 3 KB here."],
  "dva7": ["Read consistency affects how fresh a read is, not whether a later write overwrites a concurrent one; eventual reads make the problem worse.", "A GSI on a version attribute is eventually consistent and a separate read, so a race between read and write remains.", "Streams record changes after they happen; replaying them does not stop the overwrite and cannot decide which edit should win.", null],
  "dva8": ["A longer timeout lets the slow setup finish but still repeats it on every invocation, so latency stays the same.", "Copying the file to /tmp on every call is still repeated work; /tmp persists in warm environments, but only code outside the handler avoids redoing it.", null, "Async invocation hides latency from callers but the function still repeats the same setup on every run and costs the same duration."],
  "dva9": ["Lambda's maximum timeout is 15 minutes, so a 40-minute single process would be stopped partway through.", "API Gateway integrations time out after about 29 seconds by default, and Lambda behind it is still capped at 15 minutes.", "Lambda@Edge has even tighter limits (seconds, not minutes) and is meant for request/response processing at CloudFront, not batch jobs.", null],
  "dva10": [null, "More memory may help some retries succeed, but events that still fail after the retries are still discarded without a destination or DLQ.", "Provisioned concurrency reduces cold starts; it does nothing to capture events that fail processing.", "Lowering the maximum event age makes Lambda discard events sooner; without a destination or DLQ they are still lost."],
  "dva11": ["Lambda's elastic network interfaces in a VPC never receive public IP addresses, and you cannot assign one.", "A route to an internet gateway makes the subnets public, but the function's ENIs still have no public IP, so traffic cannot get out.", null, "Gateway endpoints exist only for S3 and DynamoDB; they cannot reach arbitrary third-party internet APIs."],
  "dva12": ["Environment variables are limited to 4 KB in total and are meant for configuration strings, not library code.", null, "Downloading on each cold start adds latency and an external dependency, and it still keeps a copy per function; layers solve this directly.", "Step Functions orchestrates separate tasks; a logging library must run inside each function's process, not as a workflow state."],
  "dva13": ["A single Lambda function stops after 15 minutes, so it cannot wait days, and polling in a loop wastes compute.", "SQS visibility timeout tops out at 12 hours and a queue has no workflow logic, per-step retries or approval callbacks.", "A scheduled EventBridge rule runs on a timer; it does not track workflow state or pause until a human responds.", null],
  "dva14": ["With one shared queue each message is delivered to only one of the competing consumers, so each service would miss most orders.", "Listing a bucket is slow, has no delivery tracking and forces each service to work out which objects it has already processed.", null, "Invoking services in sequence couples the producer to all three and a failure or slowdown in one blocks or breaks the rest."],
  "dva15": ["The deduplication ID suppresses repeated messages with the same ID within 5 minutes, so using the customer ID would drop that customer's later updates.", null, "DelaySeconds only postpones delivery; FIFO queues support only a queue-level delay, and it has no effect on per-customer ordering or parallelism.", "A queue per message is unmanageable and loses ordering across a customer's messages; message groups do this within one queue."],
  "dva16": [null, "Retention controls how long unconsumed messages are kept; it does not reduce empty responses from polling an empty queue.", "A visibility timeout of 0 makes in-flight messages reappear immediately and causes duplicates; it does not reduce empty receives.", "FIFO queues change ordering and deduplication; they still return empty responses under short polling."],
  "dva17": ["Long polling reduces empty receives; it does not stop a message from reappearing while it is still being processed.", "Retention decides how long unprocessed messages are kept, which is unrelated to duplicates caused by a short visibility timeout.", null, "A delivery delay only affects when a new message first becomes visible; after it is received, the 30-second visibility timeout still expires mid-processing."],
  "dva18": [null, "SQS delay queues cap at 15 minutes, so a 24-hour delay is not possible, and it would not run on a fixed clock time.", "A CloudWatch alarm reacts to metric thresholds; it is not a scheduler and cannot invoke Lambda directly at a set time.", "S3 lifecycle rules transition or expire objects; they cannot invoke Lambda or run on a schedule."],
  "dva19": ["Immediate retries in a tight loop add even more load while the service is already throttling, so failures continue.", null, "Discarding requests loses data or work; throttling is a transient error that should be retried with backoff.", "A new client does not change the service's rate limit and wastes time on setup; the fix is spacing out retries."],
  "dva20": ["SQS standard queues do not guarantee order, and a consumed message is deleted, so it cannot be replayed or shared by several apps.", "SNS pushes messages to subscribers but does not store them for replay, and standard topics do not guarantee order.", "Amazon SES is an email-sending service and has nothing to do with streaming clickstream data.", null],
  "dva21": [null, "Lazy loading fills the cache only on a read miss, so it caches only requested data and can serve stale values after writes.", "A TTL only expires entries after a set time; it does not update the cache when the database is written.", "A read replica is a database copy for scaling reads; it is not a caching strategy and can lag behind the primary."],
  "dva22": ["A nightly Scan-and-delete job works but needs code, consumes read and write capacity, and removes items up to a day late.", "A GSI only lets you query by creation date; it does not delete anything, so cleanup code would still be needed.", "DAX caches reads in front of the table; a cache lifetime does not delete items from DynamoDB itself.", null],
  "dva23": ["CloudTrail data events record who called which API, not the item's old and new attribute values, and they are not a push trigger for Lambda.", null, "A per-minute full Scan is expensive, misses intermediate changes and never sees the item's previous values.", "DynamoDB cannot publish change notifications directly to an SNS topic; item-level changes are exposed through Streams."],
  "dva24": ["A single PutObject is limited to 5 GB and a network blip still forces the whole 3 GB upload to restart, whatever the timeout.", "Transfer Acceleration can speed up the network path, but a single PUT that fails still has to start over from zero.", null, "Object Lock prevents objects from being deleted or overwritten; it does nothing to make uploads resumable."],
  "dva25": ["A larger page size only changes how many items come back per call; the Scan still reads and bills the whole table.", "A parallel Scan finishes sooner but still reads every item and uses even more capacity at once.", null, "Filters are applied after the items are read, so another FilterExpression does not reduce the capacity consumed."],
  "dva26": [null, "Global tables replicate data across Regions for availability and local writes; they do not add an in-memory cache.", "S3 is object storage with millisecond latency and would require rewriting the data-access code; it is not a DynamoDB cache.", "Streams deliver a log of item changes for processing; they do not speed up reads."],
  "dva27": ["CORS only controls which browser origins may read a response; it does not fix the malformed proxy response that API Gateway turns into a 502.", null, "A timeout would produce a 504, not a 502, and the function already returns quickly; the problem is the shape of what it returns.", "Proxy integrations pass the request and response through without mapping templates, so API Gateway still requires statusCode and body from the function."],
  "dva28": [null, "Deleting before processing means a crash mid-charge loses the message entirely, and it still does not stop a duplicate delivery from being charged.", "A zero visibility timeout makes messages reappear immediately while still being processed, which causes more duplicate charges, not fewer.", "Read consistency affects how fresh database reads are; it does not stop the same SQS message from being delivered and charged twice."],
  "dva29": ["Long-lived IAM user keys can leak and must be rotated by hand; Lambda already gets temporary credentials from its execution role.", "Opening the table to every principal in the account breaks least privilege, and the function still needs an identity to make the calls.", null, "AdministratorAccess would work but grants far more than reading one table, violating least privilege."],
  "dva30": ["Access keys stored on the instance are long-lived secrets that can leak and must be rotated manually; an instance role avoids them.", null, "Hardcoded keys end up in source control and builds, and are the most common way credentials leak.", "Making the bucket public exposes its data to anyone, and uploads would need public write access, which violates least privilege."],
  "dva31": ["S3 only accepts SigV4-signed requests with AWS credentials; a user pool access token is a JWT that S3 does not understand.", "An IAM user per mobile user does not scale (IAM has user quotas) and puts long-lived keys on devices.", null, "API keys identify clients for API Gateway usage plans; S3 does not use them and they are not AWS credentials."],
  "dva32": [null, "A Lambda authorizer could validate the JWT, but it requires writing and maintaining custom code, which the question wants to avoid.", "IAM authorization needs AWS credentials and SigV4 signing; user pool sign-in alone does not give the client IAM credentials.", "API keys only identify clients for usage plans; anyone with the key can call the API, so they do not prove a user signed in."],
  "dva33": [null, "A Cognito user pool authorizer accepts only tokens issued by a Cognito user pool, not a third party's custom format.", "A resource policy can restrict source IPs, accounts or VPC endpoints, but it cannot validate a bearer token.", "Usage plans throttle and meter clients identified by API keys; they do not validate tokens."],
  "dva34": ["Cognito authorizers validate user pool tokens for end users; a backend service with an IAM role has no user pool token.", "API keys identify clients for usage plans; they can be shared or leaked and do not prove the caller is a specific IAM role.", null, "Leaving the API unauthenticated lets anyone call it; WAF filters traffic patterns but does not authenticate an IAM role."],
  "dva35": ["Copying long-lived IAM user keys between accounts is a security risk and needs manual rotation; roles give temporary credentials.", "The root user should never be used for applications, and root users cannot be added to IAM groups in another account.", "A policy allowing any principal to write would open the table to everyone, not just account A, which breaks least privilege.", null],
  "dva36": ["A plain String parameter is not encrypted, and Parameter Store has no built-in rotation for RDS passwords.", null, "Lambda environment variables have no rotation feature; you would have to update and redeploy the value yourself.", "A file in S3 has no rotation, and the application would have to fetch and parse it; it is not a secrets store."],
  "dva37": ["Secrets Manager charges per secret per month and mainly adds rotation, which these static values do not need.", null, "Encrypting items in DynamoDB with the Encryption SDK works but adds code and table costs that Parameter Store avoids.", "KMS keys encrypt data; they do not store configuration values, and each customer managed key costs about $1 per month."],
  "dva38": ["Chunking into 4 KB calls would work but is slow, expensive and throttle-prone; envelope encryption is the intended pattern.", "The 4 KB Encrypt limit is a fixed service limit, not an adjustable quota that Support can raise.", null, "Encrypting only the object key name leaves the file contents in plaintext, which does not meet the requirement."],
  "dva39": [null, "SSE-S3 keys are fully managed by S3; there is no key policy to control and no per-use KMS entries in CloudTrail.", "With SSE-C, the customer supplies the key on each request and S3 does not store it; there is no KMS key policy or KMS key-usage log.", "TLS protects data only in transit; objects would sit unencrypted at rest and there would be no key usage to audit."],
  "dva40": ["Client-side encryption with the Encryption SDK still calls KMS for data keys unless caching is added, and it requires significant code changes.", "A key per object multiplies the number of keys and KMS calls, making throttling and cost worse.", "Disabling default encryption does not remove the SSE-KMS requirement set by the application or policy, and would weaken protection.", null],
  "dva41": ["The server-side encryption header governs encryption at rest; it says nothing about whether the request used HTTPS.", "aws:SourceIp restricts where requests come from, not whether they are encrypted in transit.", null, "s3:signatureversion checks the signing version (SigV2 or SigV4), not whether the connection uses TLS."],
  "dva42": ["A public-read ACL exposes the bucket's objects to everyone and does not grant upload permission, so the bucket would no longer be private.", "Creating IAM users and keys for end users hands out long-lived AWS credentials, which is exactly what the app must avoid.", "Signed cookies grant read access to content through CloudFront; they do not let a user upload an object to the bucket.", null],
  "dva43": ["ACM certificates are the standard way to add a custom-domain TLS certificate to CloudFront; they just have to be in us-east-1.", null, "Both ACM-issued and imported certificates work with CloudFront; importing is not required.", "CloudFront reads certificates from ACM (or IAM), not from Secrets Manager."],
  "dva44": [null, "Environment variables still hold long-lived keys that can leak through the console, logs or the function configuration.", "Encrypting the keys still leaves long-lived credentials to manage and rotate, when the execution role provides temporary ones for free.", "A config file in the package is still a hardcoded long-lived secret that anyone with access to the package can read."],
  "dva45": ["The execution role controls what the function can do, not who may invoke it; S3 needs permission in the function's resource-based policy.", null, "A bucket policy governs access to objects; the failure is that S3 is not allowed to invoke the function.", "Bucket encryption settings have nothing to do with S3's permission to invoke a Lambda function."],
  "dva46": [null, "An allow never overrides an explicit deny; the s3:* allow is cancelled for DeleteObject by the bucket policy's deny.", "Encryption status has no effect on this evaluation; the explicit deny applies to every DeleteObject call by that user.", "Policy evaluation does not depend on creation order; all applicable policies are evaluated together and explicit deny wins."],
  "dva47": ["The access token carries scopes and groups for authorizing API calls, not the profile claims meant for display.", "The refresh token is opaque and is only used to obtain new ID and access tokens.", "An STS session token belongs to temporary AWS credentials (for example from an identity pool) and holds no user profile claims.", null],
  "dva48": ["Without the key policy in account B allowing account A, the IAM policy alone cannot grant access to a key in another account.", "The key policy lets account A delegate access, but the function's role in account A still needs an IAM policy allowing kms:Decrypt.", null, "Multi-Region replica keys are created in other Regions of the same account, not in a different account."],
  "dva49": [null, "SSE-S3 encrypts only after S3 receives the data, so AWS still sees the plaintext in transit to the service.", "SSE-KMS is still server-side encryption; S3 receives the plaintext and encrypts it on arrival.", "TLS protects data in transit only; the service decrypts it on arrival and sees plaintext."],
  "dva50": ["A metric filter counts or extracts values into metrics; it does not mask the email addresses in the log events.", "Shorter retention deletes logs sooner but the emails remain visible until then.", "A subscription filter streams log events elsewhere, copying the sensitive data rather than masking it.", null],
  "dva51": ["Trusted Advisor cost checks look for idle or underused resources, not unused IAM permissions.", null, "Inspector scans EC2, ECR images and Lambda for vulnerabilities; it does not generate IAM policies.", "Config conformance packs evaluate resource configuration against rules; they do not build a policy from actual API usage."],
  "dva52": ["sam init creates a new project from a template and sam local invoke runs a function locally; neither deploys to AWS.", "cdk synth and cdk deploy belong to the AWS CDK, not to an existing SAM template.", null, "Copying files to S3 and invoking a function does not build or deploy the SAM stack at all."],
  "dva53": ["aws lambda invoke calls the function already deployed in AWS, so it cannot test code before it is deployed.", "Change sets preview CloudFormation resource changes; they do not execute function code.", null, "CodeDeploy deploys code to AWS compute targets; it does not run a function locally for testing."],
  "dva54": ["Inline ZipFile code works only for small single-file functions (up to 4 KB) and is not how local code folders are packaged.", null, "Parameter Store holds configuration values, not deployment packages, and CloudFormation cannot load function code from it.", "CloudFormation cannot read code from an EC2 instance; function code must come from S3, an image in ECR or inline."],
  "dva55": [null, "Updating $LATEST sends 100% of traffic to the new code at once with no canary period or automatic rollback.", "Weighted ALB targets could split traffic, but there is no automatic 5-minute canary step or alarm-driven rollback.", "A layer holds shared libraries; it does not control traffic between versions or roll back."],
  "dva56": ["Canary shifts one initial percentage, waits, then moves all remaining traffic at once, rather than equal steps.", "All-at-once moves 100% of traffic immediately with no increments.", "A CNAME swap is an Elastic Beanstalk blue/green technique, not a CodeDeploy traffic-shifting configuration for Lambda.", null],
  "dva57": ["All at once takes every instance out of service together, causing downtime and a slow redeploy to roll back.", null, "Rolling updates instances in batches, so capacity drops during deployment and rollback means redeploying.", "Rolling with an additional batch keeps capacity, but it updates existing instances, so rollback requires another deployment rather than terminating new ones."],
  "dva58": ["Immutable launches a whole new set of instances, which is safe but the slowest policy.", "Traffic splitting runs new instances alongside old ones for an evaluation period, which takes longer.", null, "Rolling with an additional batch launches extra instances and updates in batches, which is slower than all at once."],
  "dva59": [null, "An all-at-once deployment to the existing environment causes downtime and has no quick way back.", "Deleting the old environment removes the easy rollback path, and environments cannot simply be renamed to take over the URL.", "Setting minimum size to zero could shut down capacity; it does not move traffic to version 2."],
  "dva60": ["An automated test action runs without a person; it cannot wait for a team lead's confirmation.", "CloudWatch alarms do not pause a pipeline for human sign-off; a manual approval action is the built-in way to do that.", "A longer action timeout only lets the deployment run longer; it does not pause the pipeline for someone to approve.", null],
  "dva61": ["runtime-versions selects language runtimes during install, which happens before the code is compiled.", null, "The artifacts section lists output files to keep; it does not run commands.", "The env section defines environment variables and runs no commands."],
  "dva62": [null, "BeforeInstall runs before the new files are installed, so the application is not running yet.", "ApplicationStop runs first, to stop the old version, well before the new application starts.", "DownloadBundle is run by the CodeDeploy agent itself and cannot run user scripts."],
  "dva63": ["Storing the revision in S3 is a normal, supported source for CodeDeploy and does not stop lifecycle events from running.", null, "Tag-based deployment groups are a normal way to target EC2 instances and do not prevent lifecycle events.", "ValidateService is optional; leaving it out would not stop all lifecycle events from running."],
  "dva64": ["Separate accounts per stage would still leave each stage needing its own integration target; stage variables do this within one API.", "Duplicating functions creates code drift and ignores aliases, which exist to point to versions of one function.", null, "Usage plans throttle and meter API keys per stage; they cannot change which Lambda alias the integration calls."],
  "dva65": ["CORS controls which browser origins may call the API; it does not publish changed methods.", "Usage plans manage throttling and quotas for API keys; they do not activate method changes.", null, "Stage caching could serve old responses, but it is off by default; the missing step is deploying the changes to the stage."],
  "dva66": [null, "Layers count toward the same 250 MB unzipped limit as the function package, so splitting across layers does not help.", "Uploading via S3 raises the upload size limit, but the 250 MB unzipped limit still applies.", "Environment variables are limited to 4 KB in total and cannot hold libraries."],
  "dva67": ["Drift detection compares live resources with the last deployed template; it does not preview a new update.", "A stack policy protects resources from being updated or replaced, but it does not show what a change will do.", "Termination protection only prevents the stack from being deleted.", null],
  "dva68": ["Fn::GetAtt only reads attributes of resources in the same template; it cannot reach into another stack.", "Copying IDs into Mappings is hardcoding, and the templates break whenever the networking stack changes.", null, "DependsOn only orders resource creation within one template; it cannot reference another stack or pass values."],
  "dva69": ["cdk bootstrap provisions the staging resources (such as an S3 bucket and roles) the CDK needs in an account and Region.", "cdk deploy synthesizes and then deploys the stacks, which the question wants to avoid.", "cdk destroy deletes deployed stacks.", null],
  "dva70": ["A Lambda function still has to be written and deployed, and provisioned concurrency only cuts cold starts at extra cost.", "An ALB fixed-response rule can return static text, but it adds an hourly-billed load balancer and is slower to set up than a mock integration in the API itself.", null, "A DynamoDB table is only storage; you would still need an API and code to return the item."],
  "dva71": ["Publishing a new version for each setting change is a code deployment, with no schema validation or gradual rollout of the setting.", null, "Updating environment variables changes the configuration for all invocations at once, with no validator or alarm rollback.", "A stack policy prevents updates to protected resources; it does not roll out configuration gradually."],
  "dva72": [null, "ECR stores container images, not npm or PyPI packages, and does not proxy those public registries.", "CodeDeploy deploys applications to compute targets; it is not a package repository.", "S3 Glacier is archival storage with slow retrieval and no package-manager protocol support."],
  "dva73": ["Metadata is stored with the trace but is not indexed, so filter expressions cannot search on it.", "Environment variables configure the function and are not recorded on traces, so X-Ray cannot filter by them.", null, "The trace ID header only propagates trace context between services; you cannot add custom values to filter by."],
  "dva74": [null, "Active tracing is a Lambda setting; it does not apply to an EC2 instance.", "Traces reach X-Ray through the daemon or agent using normal outbound connectivity; no Elastic IP is required.", "CloudTrail records API activity and is unrelated to collecting X-Ray trace segments."],
  "dva75": ["A subscription filter streams log events to Kinesis; it does not create CloudWatch metrics from them.", null, "Sampling rules decide which requests X-Ray traces; they do not publish business metrics.", "CloudTrail data events log API calls for auditing and do not produce custom application metrics."],
  "dva76": ["AWS Config records resource configuration changes, not application request logs.", "Inspector scans workloads for software vulnerabilities and cannot query log events.", "Access Analyzer finds unintended resource access and generates policies; it does not search logs.", null],
  "dva77": ["High memory raises cost per millisecond but does not cause 429 throttling errors.", "An oversized package fails at deployment time, not with runtime throttling during a traffic spike.", null, "A missing execution-role policy causes AccessDenied errors from the called service, not 429 from Lambda."],
  "dva78": [null, "A longer timeout only allows the function to run longer; it does not make the CPU-bound work any faster.", "Reserved concurrency limits and guarantees the number of concurrent environments; it does not add CPU to each one.", "Layers only package shared code; the code runs the same way and gets no extra CPU."],
  "dva79": [null, "Ephemeral /tmp storage size does not affect how long a new execution environment takes to initialize.", "Reserved concurrency of 0 throttles every invocation, so the function would not run at all.", "A DLQ captures failed asynchronous events; it does nothing about cold-start latency."],
  "dva80": ["An LSI must be defined at table creation, shares the same partition key, and adds write load rather than spreading it.", null, "Strongly consistent reads affect read capacity, not write throttling on one partition key value.", "TTL deletes expired items but does not spread today's writes across partitions."],
  "dva81": ["More memory makes each invocation faster but every request still invokes Lambda and reads DynamoDB.", "On-demand capacity changes how DynamoDB is billed and scaled but does not reduce the number of backend calls.", "A DLQ captures failed async events and does not reduce requests for the same catalog.", null],
  "dva82": ["Versioning keeps previous object versions for recovery; it does nothing for download latency.", null, "DynamoDB has a 400 KB item limit and is not suited to serving images, nor would it be closer to users.", "Object Lock enforces WORM retention; it does not cache content near users."],
  "dva83": [null, "A 1-minute retention period would delete all slow or unprocessed messages, not just the malformed one.", "Turning off long polling only increases empty receives and cost; the bad message still keeps failing.", "A larger batch does not isolate the bad message and can cause more good messages to be retried alongside it."],
  "dva84": ["Records expire only after the retention period, and a shorter period would make lagging records expire and be lost.", "Lowering memory would make the function slower and increase the lag further.", null, "An empty stream would produce no iterator age growth; a rising age means records are waiting unprocessed."],
  "dva85": [null, "CloudTrail logs Lambda API calls such as function updates, not invocation errors, and emailing every call is not a threshold alert.", "An X-Ray group filters traces, but by itself it does not send an email when errors pass a threshold over 5 minutes.", "A destination for successful invocations fires on successes, not on an error count threshold."],
  "dva86": ["Longer retention keeps logs around longer but does not link log lines from different components to one request.", null, "/tmp is ephemeral storage local to one execution environment, so logs there are lost and not searchable.", "Dropping informational logs removes context and still gives no way to tie events for one request together."],
  "dva87": ["Lambda can run for 15 minutes, but the API Gateway integration still times out at 29 seconds by default and returns 504.", "More memory may speed the report up, but a 2-minute job is unlikely to fit within the 29-second integration timeout.", null, "Caching returns stored responses for repeat requests; the first request for each report still times out."],
  "dva88": ["The resource-based policy controls who can invoke the function, not what the function can access.", "Reserved concurrency limits how many copies run; it does not cause AccessDeniedException.", "CORS only governs browser cross-origin requests; it does not authorize SDK calls from Lambda.", null],
  "dva89": ["More storage adds capacity for data but does not reduce the repeated query load on the database.", null, "A subscription filter streams logs elsewhere and has no effect on database query load.", "A NAT gateway provides outbound internet access for private subnets and does not cache or reduce database queries."]
});
