/* Why each wrong option is wrong, for Google Cloud Associate Cloud Engineer questions. */
CertHub.addWhys("google-ace", {
  "ace1": [null, "A billing account only decides who pays for usage; it carries no IAM grants or organization policies for projects to inherit.", "Putting every workload in one project removes the separation between workloads and makes least privilege and quotas harder to manage.", "Labels help group costs and filter resources, but IAM and organization policies are never inherited through labels."],
  "ace2": ["Only the display name can be edited; the project ID is permanent once the project exists.", null, "The project number is assigned by Google and can't be edited either, so this gets the permanent identifiers backwards.", "Moving a project between folders changes its parent, not its ID; the ID stays fixed for the project's lifetime."],
  "ace3": ["IAM permissions don't turn an API on; the error is about the API being disabled in the project, and Owner at the organization is also far too broad.", "A second billing account is not possible (a project links to one) and wouldn't enable the API anyway.", null, "Cloud Run doesn't need a VPC to deploy, so creating a network does nothing about a disabled API."],
  "ace4": ["A custom role only limits people who are given that role; Owners and other broad roles still include the key creation permission.", "VPC Service Controls protects data from being exfiltrated across a perimeter; it doesn't stop IAM service account keys from being created.", "A sink alert only reports a key after it has already been created, so it detects instead of preventing.", null],
  "ace5": [null, "Denying instance creation blocks VMs everywhere in the folder, including in EU regions, and does nothing for other resource types.", "Budget alerts watch spending, not where resources are created, and can't block anything.", "Firewall rules filter network traffic; they have no control over which region a resource is created in."],
  "ace6": ["IAM pages grant roles to existing identities; they can't create Cloud Identity user accounts, and manual creation is exactly what must be avoided.", null, "Service accounts are identities for workloads, and sharing their keys among employees breaks accountability and security.", "Workload Identity Federation lets external workloads get Google credentials; it isn't a way to provision employee accounts from Active Directory."],
  "ace7": ["Individual grants in three projects must be edited every time someone joins or leaves, which is exactly the maintenance burden to avoid.", "A shared key hides who did what, can leak, and gives everyone the service account's access with no per-person control.", null, "Owner at the organization grants full control of every project, far beyond the BigQuery access the analysts need."],
  "ace8": ["IAM allow policies don't override each other by level; grants on the folder and the project are combined.", "Grants never cancel each other out; effective access is the union of all allow policy grants.", "There is no approval step for IAM grants; a binding takes effect as soon as it is added.", null],
  "ace9": ["Billing Account Administrator can change payment methods and settings, which the employee must not be able to do.", "Billing Account Viewer can only view costs and transactions; it can't link projects to the account.", null, "Costs Manager manages budgets and cost exports; it isn't the role meant for linking projects to the billing account."],
  "ace10": ["Uptime checks test whether an endpoint responds; they don't track spending against an amount.", null, "Quotas limit resource counts or API rates; they don't send alerts at percentages of a dollar amount.", "Organization policies restrict configurations such as locations; there is no organization policy that tracks or limits spending."],
  "ace11": [null, "Budgets never shut down resources by themselves; automatic action requires Pub/Sub notifications and your own code.", "Budgets can be scoped to selected projects, services or labels, not only the whole billing account.", "Budgets and their alerts are part of Cloud Billing for every account; they don't require a paid support plan."],
  "ace12": ["Admin Activity audit logs record who changed configuration, not how much anything cost.", "Budget notifications report threshold crossings, not the detailed per-project and per-label cost rows needed for SQL analysis.", "A PDF invoice is a monthly summary that can't be queried with SQL by day, project or label.", null],
  "ace13": [null, "Relying on remembering which tab is which is error-prone and does nothing to set project defaults in the CLI.", "--quiet only suppresses interactive prompts; it doesn't change which project a command runs against.", "Organization policies restrict resource configurations; they don't set gcloud CLI defaults on your machine."],
  "ace14": ["This revokes Application Default Credentials used by client libraries; it doesn't sign you in or set a project.", "components update upgrades the installed CLI components; it doesn't authorize an account or pick a project.", null, "config list only shows the current configuration's properties; it doesn't walk you through setup."],
  "ace15": ["There is no gcloud compute zones set command; zones are set as a configuration property.", null, "zone/default is not a valid property; the correct section and property are compute/zone.", "gcloud init is interactive setup and doesn't take a --zone flag to set the default zone this way."],
  "ace16": ["A shared VM and SSH key break accountability, expose a public IP, and are harder to secure than a per-user browser shell.", "Cloud Workstations is a paid managed development environment, and a GPU adds cost for no benefit in running CLI commands.", "Running a portable copy from a USB drive still means installing software on a locked-down laptop, which isn't allowed.", null],
  "ace17": ["Re-enabling the API doesn't change the regional CPUS quota that the error says was exceeded.", null, "Folders affect inherited policies, not per-project quotas, so moving the project changes nothing.", "Spot VMs also count against quotas (including dedicated preemptible quotas), so this claim is false."],
  "ace18": ["Every project has exactly one parent, so it can't sit in two folders at once.", "Resources such as VMs and buckets always belong to a project; they can't be created directly under the organization.", null, "Organization policies can be set on the organization, folders or projects, not only projects."],
  "ace19": [null, "Network tags are used to target firewall rules and routes; they don't appear in billing data for cost reporting.", "Separate billing accounts per app add administrative overhead and don't work when apps share projects.", "IAM roles control access and have nothing to do with how costs are attributed in billing reports."],
  "ace20": ["A single VM in one zone fails completely if that zone has an outage.", null, "A storage bucket can store data across regions but doesn't run the application itself.", "us-central1 is in the United States, far from Tokyo users, and CDN caching doesn't make a dynamic app zone-resilient in Asia."],
  "ace21": ["This lists custom roles defined in the project, not who has been granted roles.", "describe shows project metadata such as its name, number and state, not its IAM bindings.", "This lists the project's service accounts, not the members and roles in its allow policy.", null],
  "ace22": ["A MIG needs VMs to be running and managed, so it doesn't scale to zero or remove server management.", "A GKE Standard cluster keeps nodes running and requires cluster management, which the team wants to avoid.", null, "Bare Metal Solution provides dedicated physical servers for specialized workloads, the opposite of serverless."],
  "ace23": [null, "Cloud Run runs containers in a managed sandbox where you can't load kernel modules or control the host OS.", "App Engine standard runs code in language runtimes with no access to the underlying OS or kernel.", "Cloud Run functions run small pieces of code in a managed environment with no control of the OS or kernel."],
  "ace24": ["Sole-tenant nodes give you dedicated hosts for licensing or isolation and cost more than regular VMs, not less.", null, "A three-year commitment suits steady, long-running use; it doesn't fit an occasional interruptible job and still costs more than Spot pricing.", "Memory-optimized machines are for large in-memory workloads and are expensive; nothing here needs extra memory."],
  "ace25": ["The next larger predefined type means paying for vCPUs or memory the application doesn't need.", "An e2-micro is a tiny shared-core machine; scaling out many of them doesn't give one process 6 vCPUs and 20 GB of memory.", "Local SSD is block storage, not RAM, so it can't add memory to a VM.", null],
  "ace26": [null, "A standard persistent disk is HDD-backed network storage, durable but far slower than what a high-IOPS cache needs.", "Cloud Storage FUSE mounts object storage, which has much higher latency than block storage and is a poor fit for a hot cache.", "A regional persistent disk adds durability across zones and some write latency, which isn't needed for disposable scratch data."],
  "ace27": ["Local SSD data doesn't survive the VM stopping or a host failure, so it can't be moved to another zone.", null, "A daily snapshot loses every write since the last snapshot, which breaks the no-data-loss requirement.", "An IP address has nothing to do with disk replication or preserving data across zones."],
  "ace28": ["Unmanaged instance groups hold existing VMs but can't autoscale or recreate unhealthy instances automatically.", "Snapshot schedules back up disks; they don't add VMs or replace failed ones.", null, "Sole-tenant nodes provide dedicated hosts, and a startup script only runs at boot; neither adds capacity or replaces failed VMs automatically."],
  "ace29": ["Instance templates are immutable, so the machine type in an existing template can't be edited.", null, "Changing VMs by hand makes them drift from the template, and the MIG may recreate them with the old machine type.", "Recreating from the same template gives the same old machine type and causes an outage while the group is rebuilt."],
  "ace30": ["In GKE Standard you still size, manage and pay for the node pool's VMs, which the team wants to avoid.", "Plain VMs running Container-Optimized OS aren't Kubernetes and leave all node and scheduling management to you.", "Turning off the autoscaler in Standard mode means even more manual node management, not less.", null],
  "ace31": [null, "A zonal cluster has one control plane in one zone, so a bigger node pool doesn't keep it available if that zone fails.", "Node auto-repair fixes unhealthy nodes but doesn't protect the single zonal control plane from a zone outage.", "A private endpoint changes how the control plane is reached, not how many zones it runs in."],
  "ace32": ["Making every node a GPU node wastes money on workloads that don't need GPUs.", "A second cluster in another project adds management overhead when a node pool in the same cluster solves this.", null, "A DaemonSet runs one pod on every node, which doesn't target GPU machines and isn't how you schedule selected workloads."],
  "ace33": ["A ClusterIP Service is reachable only inside the cluster, whatever name you give it, so it isn't exposed to the internet.", null, "Scaling changes the number of replicas; it doesn't expose the Deployment on an external IP.", "port-forward creates a temporary tunnel from your own machine, not an internet-facing load balancer."],
  "ace34": ["Copied commands are imperative, can't show a preview of changes, and aren't reusable modules.", "Instance templates describe only VMs; they can't define projects or VPCs or preview changes across them.", "Snapshot schedules create disk backups; they don't define infrastructure at all.", null],
  "ace35": [null, "A polling VM runs and costs money all the time, adds delay, and needs code to track which files were already processed.", "A CronJob needs a GKE cluster and still polls, adding delay and code to avoid reprocessing files.", "An hourly scheduled job adds up to an hour of delay and has to work out which files are new."],
  "ace36": ["Unauthenticated invocations let anyone on the internet call the service; a secret URL is not access control.", "Maximum instances controls scaling and cost, not who is allowed to call the service.", null, "Cloud Run services get a public URL in every region, and sharing the URL carefully is still not access control."],
  "ace37": ["Cloud SQL is regional and scales vertically and with read replicas; it can't provide strongly consistent multi-region writes at global scale.", null, "Bigtable is NoSQL with no relational schema or SQL transactions across rows as the app requires.", "Firestore is a NoSQL document database, not a relational database with SQL transactions."],
  "ace38": ["Bigtable is a NoSQL wide-column store and doesn't speak PostgreSQL or support standard SQL tools.", "BigQuery is an analytics warehouse, not a transactional database for a web app.", null, "Memorystore for Redis is an in-memory cache, not a durable PostgreSQL database."],
  "ace39": [null, "Cloud SQL scales up rather than out and can't absorb millions of writes per second.", "BigQuery is built for analytical scans, not for serving low-latency lookups by key at high write rates.", "Filestore provides NFS file shares, not a database for time-series reads and writes."],
  "ace40": ["Dedicated Interconnect is a private physical connection that takes much longer to provision and doesn't run over the internet.", "Cloud NAT gives private VMs outbound internet access; it doesn't connect a data center to a VPC.", "Direct Peering connects to Google's public services such as Workspace, not privately to your VPC.", null],
  "ace41": ["Standard has no minimum duration but the highest storage price, which suits frequently accessed data, not a 7-year archive.", "Nearline suits data read about once a month and costs more to store than Archive.", "Coldline suits data read about once a quarter; Archive is cheaper for data read less than once a year.", null],
  "ace42": [null, "A scheduled script is extra code to maintain, while lifecycle rules do this natively and reliably.", "Versioning keeps old versions of objects; it doesn't change storage classes or delete objects by age.", "A retention policy prevents deletion until a time has passed; it doesn't move or delete objects automatically."],
  "ace43": ["Uniform bucket-level access changes how permissions are managed; it doesn't keep previous object versions.", null, "Requester Pays makes the reader pay for access charges; it has nothing to do with recovery.", "Autoclass changes storage classes based on access patterns; it doesn't preserve overwritten or deleted data."],
  "ace44": ["An auto mode VPC creates subnets in every region from 10.128.0.0/9, which would overlap the on-premises range.", "The default network is an auto mode VPC, so it has the same overlapping ranges and a subnet in every region.", null, "Legacy networks are deprecated, have no subnets and aren't recommended for new designs."],
  "ace45": ["Egress rules control outbound traffic; they don't allow inbound HTTP and HTTPS from users.", null, "Allowing all protocols from anywhere exposes every port on the VMs, which breaks the requirement to open nothing else.", "A deny rule blocks exactly the traffic that must be allowed."],
  "ace46": ["Peering connects VPCs that are still administered separately, so each app team would manage its own network instead of the central team.", "VPN tunnels between projects are complex, add cost and still leave every project with its own separately managed network.", "Private Service Connect exposes specific services privately; it doesn't let teams create VMs in a centrally managed subnet.", null],
  "ace47": [null, "An internal passthrough Network Load Balancer serves traffic inside a VPC and can't give a public anycast IP or URL routing.", "External passthrough Network Load Balancers are regional layer 4 balancers with no URL-based routing or Cloud CDN integration.", "An internal proxy Network Load Balancer is regional, internal and layer 4, so it isn't internet-facing and can't route by URL."],
  "ace48": ["External IPs make each VM directly reachable from the internet, which the requirement forbids.", "A load balancer handles inbound requests to backends; it doesn't give private VMs outbound internet access.", null, "Peering with another VPC doesn't provide a path to the internet for VMs without external IPs."],
  "ace49": ["Cloud NAT only handles outbound connections from private VMs; it can't carry an inbound SSH session to them.", null, "Opening port 22 to the whole internet, even temporarily, is exactly what the requirement rules out.", "Cloud Shell doesn't live in a VPC you can peer with, so peering can't give it a private path to the VM."],
  "ace50": ["An instance template stores VM settings such as machine type and image, not the current data on the disk.", "Autohealing recreates a VM that fails a health check; it doesn't keep a copy of the disk's current data.", null, "Resizing a disk adds capacity but creates no backup copy to restore from."],
  "ace51": [null, "A cron job is a script to write and maintain, which the requirement rules out, and it copies files rather than whole disks.", "Lifecycle rules manage objects already in a bucket; they don't create backups of VM disks.", "One machine image is a single backup, not daily backups with a 14-day retention."],
  "ace52": ["A snapshot schedule backs up disks over time; it isn't the usual template for creating many identical VMs.", "Local SSD is temporary scratch storage and can't capture or copy a configured boot disk.", "A reservation holds VM capacity in a zone; it contains no OS or software configuration.", null],
  "ace53": ["kubectl autoscale creates a Horizontal Pod Autoscaler that changes pod replicas, not the number of nodes.", null, "GKE manages the instance groups behind node pools, and autoscaling them directly conflicts with GKE; use the cluster autoscaler instead.", "There's no kubectl scale for nodes; nodes are VMs managed by GKE, not a scalable Kubernetes workload."],
  "ace54": ["Resizing the cluster changes the number of nodes, not how many replicas the Deployment runs.", "rollout undo reverts to an earlier revision; it doesn't change the replica count.", "set image changes the container image version, and shop:8 is just a tag, not a replica count.", null],
  "ace55": ["Deleting the Deployment removes the app entirely, causing an outage instead of a rollback.", "A cluster upgrade changes the Kubernetes version of the cluster, not the app's image.", null, "Draining nodes moves pods elsewhere but they keep running the same faulty image."],
  "ace56": ["DNS round robin can't weight traffic precisely and would require running and managing a second service.", null, "Maximum instances caps scaling for the revision; it doesn't set the share of traffic it receives.", "Deleting the old revision removes the rollback option and doesn't create a 90/10 split."],
  "ace57": ["Lowering maximum instances limits scaling and can increase queuing; it doesn't keep instances warm.", "Concurrency 1 makes each instance handle one request at a time, which causes more new instances and more cold starts.", "Region choice affects network latency to users, not container startup time.", null],
  "ace58": [null, "A Cloud Shell session is temporary and interactive, so it isn't a managed, scheduled way to move 2 TB every night.", "Transfer Appliance is a physical device for one-off offline transfers, not nightly copies from another cloud.", "BigQuery Data Transfer Service loads data into BigQuery tables, not files into Cloud Storage buckets."],
  "ace59": ["This creates a new bucket named acme-logs; it doesn't list any objects.", "This lists Compute Engine persistent disks, which are a different storage product from Cloud Storage buckets.", null, "This describes a project named acme-logs, not the contents of a bucket."],
  "ace60": ["A replica in the same zone doesn't help when that zone fails, and manual promotion isn't automatic failover.", null, "Backups require a manual restore to a new or existing instance, so they don't fail over automatically.", "Point-in-time recovery restores data to a moment in time; it doesn't keep a standby ready in another zone."],
  "ace61": ["The HA standby only takes over on failover and doesn't serve any read traffic.", "Backup retention affects recovery options, not how much query load the primary carries.", "An extra database user still runs its queries on the same primary, so the load doesn't move.", null],
  "ace62": ["LIMIT only trims the rows returned; BigQuery still scans the full columns of a normal table, so bytes processed don't drop.", null, "Exporting the table costs time and storage and doesn't tell you what the query will process.", "SELECT * scans every column, which is the most expensive way to query and doesn't estimate anything."],
  "ace63": ["Subnet ranges can't be shrunk, and a /25 would give fewer addresses, not more.", "A custom mode VPC can't be converted to auto mode, and auto mode wouldn't enlarge this subnet anyway.", null, "A new VPC doesn't add addresses to the existing subnet, and new VMs would have to be placed in a different network."],
  "ace64": [null, "The partner connects over the internet, so the VM's internal IP isn't reachable from their side.", "Cloud NAT handles outbound connections from private VMs and doesn't give the VM a fixed inbound address.", "An IP-based allowlist compares addresses, not names, so a DNS record doesn't stop the address from changing."],
  "ace65": ["CPU utilization is a metric, not a syslog entry, and a mail script behind Pub/Sub is custom work that alerting policies do natively.", "Budget alerts track spending against a budget, not VM CPU usage.", "Uptime checks test whether endpoints respond; they don't measure CPU usage over time.", null],
  "ace66": ["The Cloud Trace library records request latency spans from application code, not OS memory or disk usage.", null, "Cloud Profiler samples CPU and memory use by function in your code, not system-wide memory and disk metrics.", "Uptime checks test availability from outside and can't see inside the VM."],
  "ace67": ["A log-based metric sees only what the VMs log; if the site is down or unreachable, it may produce no logs at all.", "Trace sampling controls how requests are traced, not whether the site answers from around the world.", null, "Firewall rules logging records connections allowed or denied by rules; it doesn't test availability from external locations."],
  "ace68": [null, "Per-project sinks must be created by hand and miss any project added later.", "A dashboard displays metrics; it doesn't copy log entries anywhere.", "A log-based metric counts matching entries but doesn't store the entries in BigQuery."],
  "ace69": ["An uptime check probes endpoints from outside; it doesn't read log text.", "Longer retention keeps logs longer but doesn't turn matching entries into a chartable metric.", "A Pub/Sub subscription delivers messages; counting and charting would still require extra tooling.", null],
  "ace70": ["Deleting a VM is a configuration change, which Admin Activity logs record; Data Access logs cover reads and aren't needed here.", null, "Flow logs record network traffic samples, not API calls that deleted a VM.", "Trace spans show request latency inside applications, not who called the Compute Engine API."],
  "ace71": ["Admin Activity logs cover configuration changes, not reads of object data.", "Flow logs record network flows and don't show which identity read which object.", null, "Versioning keeps old object versions and records nothing about who read an object."],
  "ace72": [null, "Cloud Profiler shows CPU and memory usage by function, not grouped exceptions with alerts.", "Cloud Trace shows request latency across services; it doesn't group or count exceptions.", "Cloud Scheduler runs jobs on a schedule and has nothing to do with collecting errors."],
  "ace73": [null, "Owner allows deleting VMs and changing IAM, the two things the developer must not be able to do.", "Viewer is read-only, so the developer couldn't start or stop VMs.", "Compute Instance Admin also includes deleting instances, which the developer must not be able to do."],
  "ace74": ["Google recommends predefined or custom roles instead, because basic roles grant far more than most jobs need.", "Editor can't view or change IAM policies; that requires Owner or an IAM admin role.", null, "Basic roles can be granted on projects (and other levels), not only the organization."],
  "ace75": ["Key files are long-lived secrets that can leak; an attached service account gets short-lived tokens with no key to manage.", null, "Editor does include Storage access, but it also allows changes to almost everything in the project, which breaks least privilege.", "User credentials tie the application to a person and grant everything that developer can do."],
  "ace76": ["A key stored as a secret is still a long-lived key, which the security team forbids.", "API keys identify a project for some public APIs but can't authorize deployment actions as an IAM principal.", "Sharing a personal account breaks accountability and puts a user's full access in the pipeline.", null],
  "ace77": [null, "A role on the node's service account is shared by every pod on the node and relies on the node identity, which the requirement rules out.", "A key mounted as a Secret is still a long-lived key, which the requirement rules out.", "Editor on the node's service account is far too broad and still shares one identity among all pods."],
  "ace78": ["Creating and downloading a key is exactly what the requirement forbids.", "Owner is far too broad, and downloading credentials means creating a key, which isn't allowed.", null, "Viewer on the service account doesn't allow generating tokens for it, and gcloud auth login signs in as a user, not a service account."],
  "ace79": ["Compute Viewer is read-only for Compute Engine and doesn't include permission to act as a service account.", null, "Security Reviewer can view IAM policies but can't attach or act as a service account.", "Logs Writer only lets an identity write log entries; it doesn't allow using a service account."],
  "ace80": ["Cloud NAT provides outbound internet access for private VMs, not identity-based inbound access for users.", "IP allowlists don't check who the user is and break whenever someone's home address changes.", "Peering connects VPC networks, not individual users' home networks, and doesn't check identity.", null],
  "ace81": ["Metadata SSH keys stay valid until someone removes them, so disabling an account doesn't cut off access.", "The serial console is a troubleshooting tool, not an IAM-controlled way to manage everyday SSH access.", null, "Secure boot protects the boot process from tampering; it doesn't control who can SSH in."],
  "ace82": ["With default encryption Google controls the keys, so you can't rotate on your own schedule or disable them.", null, "CSEK does give control, but Google doesn't store the keys and you must send them with every request, unlike the managed option described.", "Uniform bucket-level access controls IAM permissions; it has nothing to do with encryption keys."],
  "ace83": ["Anyone who can pull the image can read the password, and changing it means rebuilding the image.", "A password in the repository is readable by everyone with repo access and stays in its history.", "allUsers makes the object readable by anyone on the internet.", null],
  "ace84": ["Fine-grained access is the mode that keeps ACLs, the opposite of what's required.", "Requester Pays changes who pays for access charges, not how permissions are controlled.", null, "Versioning keeps old object versions; it doesn't change the access control model."],
  "ace85": [null, "Making the whole bucket public exposes every object to anyone, forever, not just one file for an hour.", "Granting allUsers read access makes every object in the bucket public with no expiry.", "Sharing a service account key hands over broad, long-lived access and is a serious security risk."],
  "ace86": ["Labels are metadata for organizing and billing; they don't enforce anything.", null, "Uniform bucket-level access disables ACLs but still allows IAM grants to allUsers, and it doesn't cover new buckets.", "Versioning keeps old versions of objects and doesn't block public access."],
  "ace87": ["Cloud Trace analyzes request latency, not IAM permissions.", "Error Reporting groups application exceptions; it doesn't evaluate IAM bindings.", "The cost table shows billing data, not who has which permissions.", null],
  "ace88": ["Budget alerts track spending, not which permissions principals use.", "Flow logs record network traffic, not IAM permission usage.", null, "Uptime checks test availability of endpoints and say nothing about IAM roles."],
  "ace89": [null, "Rewriting history doesn't invalidate the key, and anyone who already copied it can still use it.", "Renaming isn't possible for a service account's email, and even a new display name leaves the key valid.", "The project's display name has nothing to do with whether the leaked key works."],
  "ace90": ["Organization Administrator can manage IAM policies across the organization, which is far more than viewing.", "Owner allows full changes to every project, not read-only review.", "Editor can modify almost every resource, so the auditor could change things.", null]
});
