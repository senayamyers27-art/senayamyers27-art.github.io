CertHub.addWhys("rhcsa", {
  "rh1": ["A single > truncates the log every run, so earlier entries are lost even though stderr is captured.", "tee without -a overwrites the file, and the pipe only carries stdout, so error messages still go to the terminal.", "Redirections are processed left to right: 2>&1 first copies stderr to the terminal, then only stdout is moved to the file.", null],
  "rh2": ["^ anchors the pattern to the start of the line, so this only matches lines beginning with nologin (none in /etc/passwd).", "-v inverts the match and prints every line that does NOT contain nologin, the opposite of what is asked.", null, "-c prints only a count of matching lines, not the lines themselves, and it doesn't anchor to the end of the line."],
  "rh3": ["-z compresses with gzip, so the file would be a .tar.gz stream despite the .bz2 name.", "-x extracts an existing archive instead of creating one; it would fail because the archive doesn't exist yet.", "-J (capital) uses xz compression, so the content would not match the .bz2 extension.", null],
  "rh4": ["autofs mounts file systems on demand; it isn't a file link and has nothing to do with a deleted file still being readable.", "A symbolic link stores the path, so once report.txt is deleted the symlink dangles and shows an error, not the old contents.", "A bind mount shows a directory tree at a second location; deleting the file in one place removes it from both views.", null],
  "rh5": ["Without -s, ln creates a hard link, not a symbolic link, and hard links can't point across file systems.", "cp -s makes symlinks while copying, and here the arguments are reversed, so it would try to use the nonexistent link as the source.", "The arguments are backwards: this would try to create a link named /opt/tools/backup.sh pointing at /usr/local/bin/backup.", null],
  "rh6": ["644 gives others read (4), but the requirement is no access at all for others.", "660 gives the group write permission too, while the group should only read.", null, "604 gives the group no access and lets others read, which is the reverse of what's needed."],
  "rh7": ["710 replaces every bit, removing the owner's read/write settings and everything the group and others had, not just adding group execute.", "g=x sets the group to execute only, removing any read or write the group already had.", "+x with no class adds execute for user, group and others (subject to umask), not just the group.", null],
  "rh8": ["whatis prints only a one-line description of a page; it doesn't open the documentation, and -f just means 'look up by name'.", "Section 8 covers administration commands such as mount; there is no fstab page there, so man 8 fstab finds nothing.", "info mount opens the mount command's documentation, not the fstab file format, and -k is not how info searches.", null],
  "rh9": ["updatedb builds the database for locate/plocate, not the whatis index that man -k searches.", "restorecon resets SELinux labels; labels are not why the man keyword index is empty.", "Reinstalling man doesn't necessarily build the index; the whatis database is generated by mandb.", null],
  "rh10": ["Plain su root starts a non-login shell that keeps most of the current user's environment, such as PATH and the working directory.", "sudo -s starts a root shell but not a login shell, so root's login environment and home directory aren't fully loaded.", null, "sudo su runs plain su, which gives a non-login shell; it would need sudo su - or sudo -i to load root's full environment."],
  "rh11": ["-i only prompts before overwriting; without -r it won't even copy a directory, and it doesn't preserve attributes.", "-r copies recursively but ownership and timestamps are reset to the copying user and current time.", "mv moves the tree instead of copying it, so /srv/site would no longer exist in its original place.", null],
  "rh12": ["mirrorlist= points to a file that lists mirrors, not directly at a repository's metadata.", "url= is not a valid key in a dnf .repo section, so dnf ignores it and the repo has no location.", null, "source= is not a .repo option; dnf would not know where the repository is."],
  "rh13": ["rpm -qf only maps a file that already exists on disk to its package, and it needs a path; it can't search for missing commands.", null, "dnf info needs a package name, and semanage is a command, not a package name, so it won't reveal the providing package.", "rpm -ql lists files of an installed package named semanage; there is no such installed package."],
  "rh14": ["rpm -qa lists installed packages matching the name; it doesn't show which files belong to the package.", "rpm -qi prints package details such as version, vendor and description, not its config files.", "dnf list shows installed and available versions of the package, not its files.", null],
  "rh15": ["flatpak install installs an application; it needs an existing remote and doesn't add Flathub as a source.", null, "dnf config-manager --add-repo adds RPM repositories for dnf; Flatpak doesn't read dnf repo files.", "remote-modify only changes a remote that already exists; it fails if Flathub was never added."],
  "rh16": ["flatpak remotes lists the configured sources (remotes), not the applications installed from them.", "flatpak search looks up applications in remote metadata; --installed is not an option for it.", null, "dnf only tracks RPM packages, so this shows at most the flatpak package itself, not Flatpak apps."],
  "rh17": ["dnf downgrade --all isn't a valid transaction revert; downgrade works on named packages and wouldn't target just last week's changes.", "rpm -e --nodeps removes packages while ignoring dependencies, which can break the system further instead of restoring old versions.", "dnf clean all only deletes cached metadata and packages; rebooting doesn't change installed versions.", null],
  "rh18": ["-d tests whether the path is a directory, so it's false for a regular config file.", "-z tests whether a string is empty; the literal path string is never empty, so this is always false and never checks the file.", "-x tests for execute permission, which a config file usually doesn't have, and it doesn't confirm it's a regular file.", null],
  "rh19": ["$@ expands to all arguments as words, not a count, so the numeric test fails or errors.", "$0 is the script's name, a string, so comparing it numerically to 0 produces an error.", "$? is the exit status of the previous command, which has nothing to do with how many arguments were passed.", null],
  "rh20": ["This loops once over the literal word /root/hosts.txt, not over the file's lines.", null, "$(ls /root/hosts.txt) just outputs the filename, so the loop runs once over the name, not the contents.", "until loops while the command fails, and > would redirect output into the file and truncate it rather than read from it."],
  "rh21": ["Assignments don't use $ on the left; $n=... expands n and tries to run the result as a command.", "Single quotes store the literal text of the command in n; nothing is executed or counted.", "Without $( ), the shell treats n=grep as a temporary variable for the command -c, which fails; n is never set to a count.", null],
  "rh22": ["Command not found gives exit status 127, not 0.", "An unreadable file makes grep exit with status 2 (error), not 0.", null, "grep exits with 1 when no line matches; 0 means at least one match."],
  "rh23": ["PATH only matters when a command is typed without a path; ./check.sh already gives the path, and the error is about permission.", null, "The interpreter is fine; changing to /bin/sh doesn't add the execute bit the kernel needs to run the file.", "restorecon fixes SELinux labels; the 'Permission denied' here comes from the missing execute bit on a script in root's home."],
  "rh24": ["single boots into rescue mode, which on RHEL still asks for the root password, so it doesn't help when the password is lost.", "init=/sbin/nologin would make the kernel run nologin as PID 1, which just exits and causes a kernel panic.", "multi-user.target is the normal non-graphical boot target, which ends at a login prompt that needs the password.", null],
  "rh25": ["fsck is not triggered by /.autorelabel; that file is only read by SELinux at boot.", null, "The password is in /etc/shadow on disk, not in the initramfs, so nothing needs rebuilding and the flag doesn't do that.", "Clearing the journal has nothing to do with the password reset; /.autorelabel triggers an SELinux relabel."],
  "rh26": [null, "--sort=pid orders processes by ID, so heavy CPU users are not listed first.", "pgrep -l cpu finds processes whose name matches 'cpu'; it doesn't measure or sort by CPU usage.", "-o PID sorts top's output by process ID rather than %CPU, so the busiest processes aren't guaranteed at the top."],
  "rh27": ["Signal 1 (SIGHUP) usually tells a daemon to reload, and it can be caught or ignored just like SIGTERM.", "Signal 15 (SIGTERM) is what plain kill already sent; the process ignored it, so sending it again won't help.", null, "Signal 18 (SIGCONT) resumes a stopped process; it doesn't terminate anything."],
  "rh28": ["-20 is the highest priority, so this would give the backup more CPU, not less (and only root can set it).", "nice starts a new command with a niceness; it can't change an existing PID and would try to run a program called 2250.", null, "SIGSTOP pauses the process entirely rather than lowering its priority, so the backup would stop making progress."],
  "rh29": [null, "tuned-adm active only reports which profile is currently in use; it changes nothing.", "systemctl set-default sets the default boot target; tuned is not a target, and this doesn't apply a tuning profile.", "tuned-adm profile has no --recommended or --apply-now options; the recommended profile comes from tuned-adm recommend."],
  "rh30": [null, "rsyslog writes text log files such as /var/log/messages, but it doesn't make the systemd journal keep past boots.", "SystemMaxUse limits how much disk the persistent journal can use; it doesn't switch storage from /run to /var/log/journal.", "logrotate rotates text log files; it has no effect on whether the journal survives a reboot."],
  "rh31": [null, "-f follows new messages live and --until 08:00 shows messages BEFORE 08:00, and there's no priority filter.", "-k limits output to kernel messages, so sshd's own log entries are excluded.", "RHEL has no /var/log/sshd by default, and grep has no --since option."],
  "rh32": ["/var/log/messages collects general system messages; authpriv messages are specifically excluded from it in rsyslog.conf.", "audit.log is written by auditd and holds audit events such as SELinux AVCs in its own format; it isn't rsyslog's authentication log.", "/var/log/boot.log holds service start-up messages from boot, not login attempts.", null],
  "rh33": [null, "cp only copies on the local system; it would treat 'server2:/tmp/' as a local path and fail.", "This runs cp on server2, copying server2's own /etc/hosts to its /tmp; the local file never leaves this machine.", "sftp takes a host to connect to and then uses interactive (or batch) put commands; it doesn't accept a local file followed by a destination like scp."],
  "rh34": ["graphical.target is the full desktop boot, which still processes fstab and hits the same error.", null, "multi-user.target is a normal full boot without a GUI, so the bad fstab entry still blocks it.", "default.target is just an alias for the normal boot target, so the same failing mount is attempted."],
  "rh35": [null, "There is no mkfs.gpt command; mkfs tools create file systems, not partition tables.", "fdisk has no -t gpt option for creating a label, and /dev/vdb1 is a partition that doesn't exist yet on a blank disk.", "pvcreate writes an LVM physical volume header and has no --gpt option; it doesn't create partition tables."],
  "rh36": ["lvcreate carves logical volumes out of an existing volume group; it can't use a raw partition directly.", "mkfs.xfs puts a file system on the partition, which LVM can't then use as a physical volume.", null, "vgextend adds an already-initialized PV to an existing VG and needs the VG name; it doesn't prepare the partition."],
  "rh37": ["-l is not the extent-size option for vgcreate (for lvcreate it means extent count), so this doesn't set 16 MiB extents.", null, "-L is an lvcreate size option; vgcreate doesn't accept it for extent size.", "vgcreate has no --extents 16 option; the extent size is set with -s (--physicalextentsize)."],
  "rh38": ["-L 40 means a size of 40 MiB (about 3 extents), not 40 extents.", "-L 640M is the right size, but it's missing -n, so lvlog is read as the VG name and the command fails.", "-s creates a snapshot volume in lvcreate, not an extent count.", null],
  "rh39": [null, "The mount point is in the device field and the UUID in the mount-point field; the first field must be the device.", "The type and mount point are swapped, so mount would try to use 'xfs' as the mount point.", "The options and type fields are swapped, so 'defaults' would be read as a file system type."],
  "rh40": ["pvs reports LVM physical volume details such as VG, size and free space, not the file system UUID and type.", "df -T shows the type only for a mounted file system, and it never shows UUIDs.", "fdisk -u sets display units; it doesn't read the file system UUID from a partition.", null],
  "rh41": ["There is no mkfs.swap (the tool is mkswap), and swap is activated with swapon, not mount.", "swapon fails before the swap signature exists, and mkswap has no -a option.", null, "mkswap needs a device and has no -a option; -p sets a priority but can't activate an unformatted partition."],
  "rh42": ["Swap is not mounted, so a directory like /swap as the mount point is wrong; use none or swap.", null, "The mount point and type fields are swapped, so the type would be read as 'none'.", "The fsck pass must be 0 for swap; 2 would ask fsck to check a device with no file system."],
  "rh43": ["mklabel gpt writes a new empty partition table, which destroys the existing partitions and their data.", null, "wipefs erases partition-table and file-system signatures, which destroys the existing layout.", "Deleting and resizing partition 2 risks its data and isn't needed when free space is available."],
  "rh44": [null, "pvdisplay shows only LVM physical volume details, not every disk, partition and mount point as a tree.", "mount -l lists mounted file systems, not unmounted disks or a device tree with sizes.", "fdisk -s prints the size of a single device in blocks; it doesn't show a tree or mount points."],
  "rh45": ["vgreduce removes a physical volume from a volume group; it doesn't delete a logical volume.", "lvreduce can't shrink a volume to 0 and wouldn't delete it; the LV would still exist.", "pvremove wipes the label from a physical volume, not a logical volume path.", null],
  "rh46": ["XFS is a Linux file system that Windows can't read without extra software.", "ext4 is a Linux file system that Windows doesn't read natively.", null, "mkswap creates swap space, which isn't a file system for storing files."],
  "rh47": [null, "Without + this sets the LV to exactly 500 MiB (possibly shrinking it), and without -r the file system isn't grown.", "xfs_growfs grows the file system to fill its device but has no way to add space to the LV, and -L isn't its size option.", "resize2fs is for ext2/3/4, not XFS, and 500M would be an absolute size."],
  "rh48": ["xfs_growfs can only grow a file system, mounted or not; XFS has no shrink operation.", null, "lvreduce -r calls fsadm, which can't shrink XFS, so the command fails (or risks data if forced without -r).", "xfs_repair checks and repairs metadata; -L zeroes the log and doesn't resize anything."],
  "rh49": ["The type is xfs, but an NFS export must use type nfs, and there's no _netdev option.", "The mount point and the export are swapped; the first field must be the NFS source.", null, "fsck pass 1 is wrong for a network file system, and without _netdev it isn't marked as needing the network."],
  "rh50": [null, "The wildcards are reversed: & must be in the location, and * must be the key.", "In an indirect map the key is relative to /rhome, not a full path, and nfs1:/home/* doesn't substitute the key; & does.", "Options must come before the location, and the location lacks & so every key would mount the whole /home."],
  "rh51": ["mount -a only processes /etc/fstab; autofs maps aren't read by it.", "exportfs -r refreshes the export table on the NFS server, not on the autofs client.", null, "Adding it to fstab creates a static mount and bypasses autofs, which the task requires to mount on demand."],
  "rh52": ["The sticky bit on a file has no effect on Linux, and it isn't needed to read a file.", "umask only sets permissions for newly created files; it doesn't block reading an existing 644 file.", null, "Ownership by root isn't required; mode 644 already grants read to everyone who can reach the file."],
  "rh53": [null, "Isolating local-fs.target stops units not required by that target, which can shut down services, and it's not the standard fstab test.", "partprobe rereads partition tables, and rebooting is exactly the risk the test is meant to avoid.", "fsck checks file system consistency; it doesn't test that fstab entries mount."],
  "rh54": ["For mkfs.ext4, -n is not the label option; -L sets the volume label.", null, "mkfs.xfs creates XFS, not ext4, even though -L would set a label.", "e2label sets a label on an existing ext file system; it has no -c option and doesn't format the device."],
  "rh55": ["Minute and hour are swapped: 2 30 means minute 2 of hour 30, which is invalid.", null, "The 1 in the day-of-month field makes it run only on the 1st of each month.", "The * minute runs every minute of the 02 hour, and 30 is in the day-of-month field, so it runs on the 30th only."],
  "rh56": ["Enabling the service ties it to boot, not to the schedule, so the timer never triggers it.", "Starting the service runs it once now; nothing schedules future runs.", null, "crontab lines run commands, not systemd timer units; the timer has to be enabled in systemd."],
  "rh57": [null, "at -f reads the job from a file, but 'daily' is not a valid at time, and at never repeats.", "crontab -e opens an editor; it takes no time or script arguments and cron schedules repeat.", "systemd-run --on-calendar needs a calendar expression; '+20' isn't valid there (--on-active=20m would be the relative form)."],
  "rh58": ["systemctl start has no --boot option; start alone runs it now but doesn't enable it at boot.", "unmask removes a mask so the unit can be started; it neither starts nor enables httpd.", "enable creates the boot-time symlinks, but httpd isn't started until the next boot.", null],
  "rh59": ["isolate switches to multi-user.target right now but doesn't change the default for the next boot.", null, "Enabling rescue.target doesn't make it the default and rescue mode isn't a normal text console.", "grubby sets the default kernel, not the systemd target, and 'multi-user' isn't a kernel."],
  "rh60": ["ntpserver isn't a chrony directive, and prefer is ntpd syntax; chronyd would reject the line.", null, "chrony directives don't use '=', so 'pool =' is a syntax error; the correct form is 'pool host iburst'.", "timeserver isn't a chrony.conf directive; chronyd ignores it or reports an error."],
  "rh61": [null, "grub2-install writes the boot loader to disk and has no --args option for kernel arguments.", "dracut builds the initramfs image; it doesn't edit kernel command-line arguments in boot entries this way.", "sysctl changes runtime kernel parameters under /proc/sys; audit=1 is a boot argument and there is no such sysctl."],
  "rh62": ["dracut -f rebuilds the initramfs, which doesn't contain GRUB settings like the menu timeout.", null, "GRUB isn't a service; there is no grub2 unit to restart, and it only reads grub.cfg at boot.", "grubby --info=ALL only displays boot entries; it doesn't regenerate grub.cfg from /etc/default/grub."],
  "rh63": [null, "dnf clean all removes cached metadata, but an unregistered system still has no entitlement to reach the CDN repos.", "EPEL is a community repository of extra packages; it doesn't give access to Red Hat's BaseOS and AppStream content.", "Turning off GPG and SSL checks weakens security and doesn't fix the missing registration the CDN requires."],
  "rh64": [null, "nmcli dev set changes device properties like autoconnect or managed; it has no ip or gw arguments.", "ip addr add changes only the running interface, is lost at reboot, and has no 'permanent' keyword.", "ipv4.method auto keeps DHCP enabled, and the gateway isn't set, so this doesn't meet the requirement."],
  "rh65": ["RHEL 10 has no legacy network service; NetworkManager manages connections.", "nmcli general reload dns only reapplies DNS settings, not the new IP address and gateway on ens3.", "The ifup scripts aren't used on RHEL 10, and --reload isn't a valid ifup option.", null],
  "rh66": ["ifcfg files in network-scripts were deprecated and are no longer supported on RHEL 10.", "/etc/network/interfaces is the Debian/Ubuntu ifupdown file; RHEL never used it.", null, "/run is a temporary file system cleared at boot, so profiles there wouldn't be persistent."],
  "rh67": ["The hostname command changes the name only in the running kernel, so it is lost at reboot.", "Appending to /etc/sysconfig/network doesn't set the hostname on modern RHEL; the persistent name lives in /etc/hostname.", "nmcli con mod needs a connection name and a property; 'hostname' isn't a valid connection, so the command fails.", null],
  "rh68": ["NetworkManager regenerates /etc/resolv.conf, so manual edits are overwritten on restart or reconnect.", null, "/etc/hosts maps host names to addresses; it can't specify which DNS server to query.", "hostnamectl manages the host name and has no set-dns subcommand."],
  "rh69": ["A rule without --permanent is lost at reload or reboot, and --runtime-to-reboot isn't a real option (the real one is --runtime-to-permanent).", "--add-port needs a port/protocol like 80/tcp, not a service name, and restarting httpd doesn't load firewall rules.", null, "firewall-cmd has no --save option, and the trusted zone isn't where the server's interface usually is."],
  "rh70": [null, "ip route show prints the routing table, not listening sockets or processes.", "nmcli dev status shows each network device's state and connection, not open ports.", "ping only tests reachability with ICMP; it tells you nothing about listening TCP ports."],
  "rh71": [null, "-g sets the primary group (which must already exist), not the UID, so the user gets an automatic UID.", "usermod changes an existing account, and -L locks the password; it doesn't create the user or set a nologin shell.", "-U creates a same-named group and takes no value, and a /dev/null home directory doesn't block interactive login."],
  "rh72": ["-G without -a replaces all of alice's supplementary groups, so she loses her other memberships.", "useradd creates new accounts; it fails because alice already exists.", "groupmod changes a group's name or GID; it has no -a option for adding members (gpasswd -a would).", null],
  "rh73": [null, "-M 0 sets the maximum password age to 0 days, a permanent policy change that makes the password expire immediately after every change.", "passwd -l locks the password, so bob can't log in with it at all, never mind change it.", "usermod -e 0 sets the account expiry date to 1 January 1970, so the account is expired and can't log in."],
  "rh74": ["/etc/default/useradd holds defaults like home base, shell and skel, not password aging.", "pwquality.conf controls password complexity (length, character classes), not maximum age.", null, "Editing /etc/shadow changes existing accounts only; new accounts get defaults from /etc/login.defs."],
  "rh75": ["groupadd has no -u option; the GID is set with -g.", "groupmod changes a group that already exists, so it fails if finance hasn't been created.", "addgroup is a Debian-style helper that isn't part of RHEL.", null],
  "rh76": ["Without %, sudoers treats admins as a user name, so group members get nothing.", null, "@ marks a netgroup in sudoers, not a local group.", "group: isn't valid sudoers syntax, and the run-as part should be (ALL)."],
  "rh77": ["-f forces removal even while the user is logged in, but without -r the home directory and mail spool stay.", "usermod -L only locks the password; the account and home directory still exist.", null, "deluser is a Debian tool that isn't on RHEL, and --home isn't how it removes home directories."],
  "rh78": ["chcon sets the label directly, but a relabel or restorecon puts back the policy default, undoing it.", "There is no httpd_read_srv boolean, and booleans don't change file labels.", null, "chmod changes standard permissions, and without a new fcontext rule restorecon only reapplies the existing default labels."],
  "rh79": [null, "getsebool shows SELinux boolean values and doesn't take a directory argument.", "sestatus reports the overall SELinux state and policy, not individual file contexts.", "id -Z shows the context of your own login session, not files."],
  "rh80": ["httpd_can_network_connect allows outgoing connections from httpd; it doesn't let httpd bind to an unlabeled port.", null, "The firewall affects whether clients can reach the port, but SELinux still blocks httpd from binding, so it won't start.", "restorecon resets file labels under /etc/httpd; the problem is the port label, not file contexts."],
  "rh81": [null, "Without -P the boolean change is lost at the next reboot, so it's not permanent.", "There is no db_port_t type, 3306 is already labeled mysqld_port_t, and the command lacks -p tcp; port labels don't grant httpd permission to connect.", "setenforce 0 switches the whole system to permissive, removing protection instead of allowing only this access."],
  "rh82": ["SELinux isn't a systemd unit, so journalctl -u selinux shows nothing useful.", null, "getenforce only prints the current mode and doesn't take -v or search logs.", "AVC denials are recorded by auditd in audit.log, not in /var/log/secure or boot.log."],
  "rh83": ["setenforce 1 only changes the running mode; the config file still says permissive, so it reverts at reboot.", null, "SELINUXTYPE picks the policy (such as targeted); 'enforcing' isn't a valid policy type.", "semanage has no enforce subcommand; the boot mode is set in /etc/selinux/config."],
  "rh84": [null, "There is no global 'permissive' boolean, and -P would make the change persist past reboot.", "getenforce only reports the mode; it doesn't take an argument or change anything.", "semanage permissive -a makes one domain permissive persistently; 'all' isn't a domain, and the change survives reboot."],
  "rh85": ["755 would come from umask 022; 027 also removes read and execute for others.", "640 is what a new file gets with umask 027; directories start from 777 so they keep execute.", null, "770 would need umask 007; 027 also removes group write."],
  "rh86": ["This copies the private key to the server, which is unsafe and doesn't add anything to authorized_keys.", null, "ssh-add loads a private key into the local ssh-agent; it doesn't install anything on server2.", "ssh-keyscan collects the server's host keys, which belong in known_hosts, not your authorized_keys."],
  "rh87": ["sshd with StrictModes only rejects group- or world-writable files, so 755/644 can work, but it is looser than needed; 700 and 600 is the expected secure setting.", "777 and 666 let anyone write to them, which is exactly what makes sshd with StrictModes refuse the key.", null, "authorized_keys doesn't need execute bits, and 711/755 are looser than needed; 700 and 600 are what sshd expects."],
  "rh88": ["PubkeyAuthentication no disables key logins, the opposite of what's wanted, and passwords still work.", "prohibit-password only affects root logins; other users can still log in with passwords.", "UsePAM no changes how sshd uses PAM and isn't supported on RHEL; it doesn't cleanly disable password logins.", null],
  "rh89": [null, "--add-interface expects an interface name like ens3, not a network address.", "--add-port expects a port/protocol like 8080/tcp, and without --permanent it isn't persistent.", "--set-default-zone takes a zone name, not a network, and doesn't bind sources to a zone."],
  "rh90": ["ls -Z shows the label of the httpd binary on disk (httpd_exec_t), not the running processes' domain.", null, "getsebool -a lists boolean values, not the context of running processes.", "seinfo queries the policy (types, roles, and so on); -p isn't a way to show running processes."]
});
