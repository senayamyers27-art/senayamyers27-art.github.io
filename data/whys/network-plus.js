CertHub.addWhys("network-plus", {
  "np1": [null, "Layer 3 decisions use logical IP addresses, which is what routers do; a switch forwarding on MAC addresses never looks at the IP header.", "Layer 4 deals with ports, segments and end-to-end delivery (TCP/UDP), not with forwarding frames by hardware address.", "Layer 1 only moves raw bits and signals; it has no concept of addresses, so it cannot make a forwarding decision based on a MAC."],
  "np2": ["TCP 23 is Telnet, which provides command-line access but sends everything, including passwords, in cleartext.", null, "TCP 3389 is RDP, a graphical remote desktop protocol mainly for Windows, not encrypted command-line access to Linux.", "TCP 445 is SMB for file and printer sharing; it does not provide an interactive administrative shell."],
  "np3": ["Port 67 is the DHCP server port; a missing DHCP rule would stop address assignment, not just name lookups while IP access still works.", "Port 123 is NTP for time synchronization; blocking it causes clock drift, not failed hostname resolution.", null, "Port 389 is LDAP for directory queries; it is not used to resolve web server hostnames to IP addresses."],
  "np4": ["64 is the total number of addresses in a /26, but the network and broadcast addresses cannot be assigned to hosts.", "30 usable hosts is what a /27 (5 host bits) provides, not a /26.", "126 usable hosts is what a /25 (7 host bits) provides, twice the size of a /26.", null],
  "np5": ["A /24 has only 254 usable addresses, which is not enough for 500 hosts.", "A /22 gives 1,022 usable addresses; it fits 500 hosts but wastes about twice as many addresses as a /23.", null, "A /25 has only 126 usable addresses, far short of the 500 hosts required."],
  "np6": [null, "172.16.45.0 is the network address of the lower /25 block (.0 to .127); .130 falls in the upper block.", "172.16.45.129 is the first usable host address in the subnet, not the network address itself.", "172.16.45.255 is the broadcast address of the .128/25 subnet, not its network address."],
  "np7": ["169.254.0.0/16 is a reserved link-local range and is never assigned as a public address by an ISP.", "The IPv4 loopback address is 127.0.0.1 (127.0.0.0/8), not a 169.254.x.x address.", "IPv4-mapped IPv6 addresses look like ::ffff:a.b.c.d; a plain 169.254 address is an IPv4 link-local address.", null],
  "np8": ["172.32.1.1 is just outside the 172.16.0.0/12 private block, which ends at 172.31.255.255, so it is a public address.", null, "192.169.1.1 is outside 192.168.0.0/16; only 192.168.x.x is private, so this is a public address.", "11.0.0.1 is outside 10.0.0.0/8; only addresses starting with 10 are private in that range."],
  "np9": [null, "IaaS provides only virtualized compute, storage and networking; the customer still manages the OS and applications.", "PaaS supplies a managed platform for developers to deploy their own code; the customer still builds and runs the application.", "On-premises hosting means the company runs its own servers, the opposite of the provider managing everything."],
  "np10": ["OM3 multimode fiber supports 10 Gbps only up to about 300 m, far short of 8 km.", null, "Cat 6a supports 10GBASE-T only up to 100 m, so it cannot span 8 km.", "RG-6 coax is used for cable TV and broadband drops; it is not an 8 km 10 Gbps Ethernet medium."],
  "np11": ["A full mesh connects every site to every other site, which multiplies the number of circuits and raises cost.", "A bus topology shares one cable segment among all nodes; it is a legacy LAN design, not a WAN branch layout.", null, "Point-to-point links between every pair of branches is effectively a full mesh, requiring many circuits rather than one per branch."],
  "np12": ["North-south traffic enters or leaves the data center, such as client requests from the internet, not server-to-server traffic inside it.", "Broadcast is a delivery method to all hosts on a segment; it does not describe the direction of application-to-database traffic.", "Out-of-band refers to a separate management path, not the normal production traffic between servers.", null],
  "np13": ["Unicast would require a separate copy of the stream for every viewer, wasting bandwidth as the audience grows.", "Broadcast sends the stream to every host on the segment, including devices that never joined the channel.", null, "Anycast delivers to the single nearest of several receivers sharing an address, not to a group of subscribers."],
  "np14": [null, "A stateless packet filter sees only addresses and ports, so it cannot tell this app from normal HTTPS on TCP 443.", "An unmanaged Layer 2 switch forwards frames by MAC address and has no policy or application inspection features.", "A hub simply repeats bits out every port at Layer 1 and cannot inspect or block any traffic."],
  "np15": ["Global unicast addresses are routable and come from 2000::/3; they are not auto-generated with an fe80 prefix.", "Unique local addresses use fc00::/7 (typically fd00::/8) and are routable within an organization, unlike fe80 addresses.", "IPv6 multicast addresses begin with ff00::/8 and identify groups, not a single interface.", null],
  "np16": ["VxLAN is an overlay that extends Layer 2 segments across a Layer 3 network, mostly in data centers; it does not steer WAN traffic by application.", null, "Spanning Tree prevents Layer 2 loops within a switched LAN; it has nothing to do with choosing WAN paths.", "PAT translates many private addresses to one public address; it does not choose between WAN links based on performance."],
  "np17": [null, "TCP retransmits lost segments and waits for acknowledgments, adding delay that hurts real-time voice more than an occasional lost packet.", "ICMP carries diagnostic and error messages like ping; it is not used to transport voice media.", "GRE is a tunneling protocol that encapsulates other packets; it is not the transport that carries RTP voice streams."],
  "np18": ["BGP is an exterior path-vector protocol used between autonomous systems, not a link-state IGP using cost.", null, "RIPv2 is a distance-vector protocol that uses hop count, not a link-state map with a cost metric.", "EIGRP is an advanced distance-vector protocol that began as Cisco-proprietary and uses a composite metric, not link-state cost."],
  "np19": ["OSPF is an interior gateway protocol for routing within one autonomous system, not for exchanging routes with ISPs.", "RIPv2 is a simple interior distance-vector protocol limited to 15 hops and is not used between ISPs' autonomous systems.", null, "A single static default route cannot exchange routes with anyone and cannot use both ISPs intelligently or fail over dynamically."],
  "np20": ["Administrative distance only matters when two routes cover the exact same prefix; here the prefixes differ, so the more specific /24 wins.", "Routers do not fall back to a default route when routes overlap; the default is used only when nothing more specific matches.", "Load balancing applies to equal routes to the same prefix; a /16 and a /24 are different prefixes, so they are not balanced.", null],
  "np21": ["Static NAT maps one private address to one dedicated public address, so it cannot put 200 hosts behind a single IP.", "DHCP relay forwards DHCP broadcasts to a server on another subnet; it does not translate addresses for internet access.", null, "Policy-based routing chooses a path based on rules like source or application; it does not translate addresses or track ports."],
  "np22": [null, "Spanning Tree prevents Layer 2 loops by blocking redundant switch links; it does not provide a shared gateway IP address.", "LACP bundles multiple physical links into one logical link; it adds bandwidth but does not let two routers share a gateway.", "Port mirroring copies traffic to a monitoring port for analysis and provides no gateway redundancy."],
  "np23": ["An access port carries only one untagged VLAN, so VLANs 20 and 30 could not cross the uplink.", "Port security limits which or how many MAC addresses a port learns; it does not let the link carry multiple VLANs.", "PoE+ delivers electrical power over the cable to devices like phones or APs; it has nothing to do with carrying VLANs.", null],
  "np24": ["LACP bundles links deliberately configured as a group; it does not detect or block an accidental loop created by a patch cable.", null, "VRRP provides a redundant default gateway for hosts; it does not stop Layer 2 loops or broadcast storms.", "802.1X authenticates devices before granting port access; it does not detect or break switching loops."],
  "np25": [null, "Spanning Tree would block the redundant links to prevent a loop rather than combining them for more bandwidth.", "802.1Q trunking tags frames so several VLANs share one link; it does not bundle multiple ports together.", "Jumbo frames raise the MTU to reduce per-frame overhead, but they do not combine ports or add link redundancy."],
  "np26": ["PoE+ supplies power to connected devices and has no effect on per-packet overhead for storage traffic.", null, "The native VLAN only decides which VLAN is untagged on a trunk; changing it does not reduce packet overhead.", "Forcing half duplex would add collisions and cut throughput, making the storage network slower, not more efficient."],
  "np27": ["Channels 1, 5 and 9 are only four channels apart, and 20 MHz channels need five channels of spacing, so they overlap.", "1, 7 and 13 is sometimes used where channel 13 is allowed, but 13 is not used for normal Wi-Fi in the United States.", null, "36, 40 and 44 are 5 GHz channels, so they cannot be assigned to 2.4 GHz radios."],
  "np28": ["802.11ac (Wi-Fi 5) operates only in the 5 GHz band and cannot use 6 GHz.", "802.11n (Wi-Fi 4) works in 2.4 and 5 GHz but predates 6 GHz support entirely.", "802.11g is a legacy 2.4 GHz standard and has no 6 GHz capability.", null],
  "np29": ["WPA3-Personal uses a single shared passphrase with SAE, so it cannot check each user's directory credentials against RADIUS.", "WEP is an obsolete, easily cracked encryption scheme that uses a shared static key, not per-user RADIUS authentication.", null, "A captive portal only shows a web login after joining an open, unencrypted network; it is not 802.1X RADIUS authentication."],
  "np30": [null, "802.3af (standard PoE) delivers only up to 15.4 W at the port, which is not enough for a 25 W camera.", "802.3u is the Fast Ethernet (100 Mbps) standard and defines no power delivery.", "802.3ab is 1000BASE-T gigabit Ethernet over copper and does not define power delivery."],
  "np31": ["Facing intakes toward the hot aisle makes servers breathe exhaust air, raising temperatures and defeating containment.", "Alternating direction mixes hot exhaust into intakes of neighboring gear, which is exactly what hot/cold aisle design avoids.", "Rack servers are built for front-to-back airflow; mounting them sideways would break the airflow path and the aisle layout.", null],
  "np32": ["Omnidirectional antennas radiate in all directions, wasting energy and picking up interference instead of focusing on the far building.", null, "Indoor dipoles are low-gain omni antennas, and widening the channel to 160 MHz reduces range and adds interference rather than helping.", "Raising power on omnidirectional antennas still spreads the signal everywhere and adds interference; it does not focus the link on the far end."],
  "np33": [null, "A logical diagram shows subnets, VLANs, IP addressing and traffic flow, not which physical port or patch panel a jack lands on.", "An SLA defines service commitments like uptime and response time between a provider and customer; it contains no cabling details.", "An acceptable use policy tells users what they may do with company systems and says nothing about cable runs or port mappings."],
  "np34": ["Applying the upgrade immediately bypasses change management, leaving no approval, scheduled window or rollback plan if it fails.", null, "Upgrading the core switch during business hours risks an outage for everyone; changes belong in an approved maintenance window.", "Users and stakeholders should be told in advance of planned changes, not only after something breaks."],
  "np35": ["An SLA uptime clause defines required availability, not what normal utilization on this link looks like.", "An acceptable use policy defines allowed user behavior and gives no measure of normal link utilization.", null, "The MAC address table maps MAC addresses to switch ports and holds no historical utilization data."],
  "np36": ["SNMPv1 authenticates with a cleartext community string and offers no encryption at all.", "SNMPv2c still relies on cleartext community strings and provides no real user authentication or encryption.", "A longer community string is still sent in cleartext in SNMPv2c, so anyone sniffing traffic can read it; it adds no encryption.", null],
  "np37": ["A GET request is a poll sent by the manager to read a value, so the manager would still wait for its next poll.", "A walk is a series of manager-initiated GETNEXT requests that read a whole branch of the MIB; it is still polling.", null, "A MIB is the database structure that defines which objects can be monitored; it is not a notification mechanism."],
  "np38": [null, "Full packet capture records complete payloads, which is exactly what the administrator wants to avoid because of storage and privacy.", "Firewall syslog covers only traffic hitting the firewall's logged rules and does not give consistent per-conversation byte counts.", "ICMP echo monitoring only checks whether hosts respond and how fast; it shows nothing about who talks to whom or on which ports."],
  "np39": ["RTO is the maximum time allowed to restore service after a disaster, not the maximum amount of data that can be lost.", "MTTR measures the average time to repair a failed component, not how much data loss is acceptable.", "MTBF measures the average time a component runs between failures and is a reliability metric, not a data-loss limit.", null],
  "np40": ["A hot site has hardware and near-real-time replicated data, so it can take over almost immediately without restoring backups.", null, "A cold site provides only space, power and basic connectivity, with no installed hardware ready to run.", "An active-active cluster runs production at both locations simultaneously, so there is nothing to restore before going live."],
  "np41": [null, "Lease time controls how long clients keep an address; it cannot help DHCP broadcasts from VLAN 20 cross the router to reach the server.", "A reservation still requires the client's DHCP request to reach the server, which is the underlying problem across VLANs.", "Spanning Tree prevents Layer 2 loops; it does not forward DHCP broadcasts between VLANs."],
  "np42": ["An A record maps a hostname to an IPv4 address, not an IPv6 address.", null, "A PTR record maps an IP address back to a hostname for reverse lookups, the opposite direction.", "A CNAME makes one hostname an alias of another name; it does not hold an IP address itself."],
  "np43": ["An MX record tells other servers where to deliver inbound mail for the domain; it is not consulted in a reverse lookup of an IP.", "An SRV record advertises the host and port for a specific service, such as SIP or LDAP; it does not provide reverse DNS.", null, "TXT records hold text such as SPF or DKIM data; they help with email validation but do not answer reverse IP lookups."],
  "np44": ["SNMP on UDP 161 is used to monitor and manage devices; it does not synchronize clocks.", "LDAP on TCP 389 is used for directory queries; Kerberos may use the directory, but LDAP does not fix clock skew.", "Syslog on UDP 514 collects log messages; it records timestamps but does not synchronize time between servers.", null],
  "np45": ["In-band SSH travels over the production network that is down, so it fails just like the original SSH session.", "A VPN into the production network still depends on that network working, so it cannot reach the switch while production is down.", null, "A jump box on the production VLAN relies on the broken production network, so it cannot reach the switch either."],
  "np46": [null, "Stateful DHCPv6 requires a DHCPv6 server to assign and track addresses, which the scenario rules out.", "APIPA is the IPv4 169.254.0.0/16 self-assignment fallback when DHCP fails; it does not create global IPv6 addresses.", "NAT64 translates between IPv6 and IPv4 addresses so IPv6-only hosts can reach IPv4 services; it does not assign host addresses."],
  "np47": ["Implicit trust grants access based on network location or identity without checks, the opposite of restricting rights to what is needed.", "Open access would give everyone broad rights, contradicting the idea of limiting help desk staff to read-only.", "Single sign-on lets users authenticate once for many systems; it is about login convenience, not limiting permissions.", null],
  "np48": ["DNS cache poisoning inserts false name-to-IP records into a resolver; it does not involve forged ARP replies.", null, "MAC flooding overflows a switch's CAM table with fake source MACs; it does not answer ARP requests for another host's IP.", "VLAN hopping uses double tagging or switch spoofing to reach another VLAN; it does not impersonate the gateway via ARP."],
  "np49": [null, "War driving is driving around to discover and map wireless networks; it does not create a fake corporate SSID.", "Bluejacking sends unsolicited messages to Bluetooth devices and has nothing to do with Wi-Fi SSIDs or password capture.", "Jamming floods the frequency with interference to deny service; it does not present a look-alike network asking for passwords."],
  "np50": ["DHCP snooping blocks rogue DHCP servers and builds a binding table; it does not limit how many MACs a port can learn.", null, "BPDU guard shuts down access ports that receive spanning tree BPDUs; it does not stop floods of fake source MACs.", "Dynamic ARP inspection validates ARP messages against DHCP bindings; it does not cap MAC table entries per port."],
  "np51": ["Port mirroring copies traffic to an analyzer port for monitoring and cannot block rogue DHCP replies.", "802.1Q trunking tags frames to carry multiple VLANs; it provides no filtering of DHCP server messages.", null, "Link aggregation bundles ports for bandwidth and redundancy and does nothing to stop an unauthorized DHCP server."],
  "np52": ["Placing a public server on the internal user VLAN exposes the LAN directly if the server is compromised.", "The native VLAN carries untagged traffic on trunks; putting a public server there mixes it with infrastructure traffic and gives no isolation.", "A guest wireless VLAN is meant for visitor internet access, not for hosting a public server reachable from the internet.", null],
  "np53": ["MAC filtering only allows listed MAC addresses, which are easily spoofed, and it does not authenticate against RADIUS.", "WPA2-Personal is a wireless mode using a shared passphrase; it does not control wired switch ports or use RADIUS.", null, "SNMPv3 secures device monitoring and management traffic; it does not authenticate devices connecting to switch ports."],
  "np54": [null, "Leaving unused ports enabled in the default VLAN lets anyone who plugs in reach the network, the problem the audit found.", "Configuring them as trunks would be worse, letting an attached device potentially reach every VLAN.", "Enabling PoE just powers attached devices and does nothing to prevent unauthorized access through the ports."],
  "np55": ["Shoulder surfing is watching someone's screen or keyboard to steal information, not following them through a door.", "Phishing uses deceptive messages, usually email, to trick users into revealing information or clicking links.", "Dumpster diving is searching trash for discarded sensitive information, not physically entering a secure area.", null],
  "np56": ["A clientless SSL VPN portal gives individual users browser-based access to specific apps; it does not connect every branch device permanently.", null, "A client-to-site VPN requires software on each device, which the requirement specifically rules out.", "Publishing RDP to the internet exposes a risky service and gives remote desktop to one host, not an encrypted network-to-network link."],
  "np57": [null, "Firewalls do not allow traffic just because no rule matches; the implicit deny at the end drops anything not permitted.", "Nothing permits TCP 25, so it is not allowed; whether denied traffic is logged depends on configuration.", "An ACL permits or denies; it does not redirect traffic from one port to another, which would require NAT or a proxy."],
  "np58": ["Establishing a theory is the second step; it comes after identifying the problem and gathering information.", null, "Reinstalling the driver jumps straight to implementing a fix before identifying the problem or testing a theory.", "Documentation is the final step of the methodology, after the problem is resolved and verified."],
  "np59": ["Documenting findings, actions and outcomes is the last step, after verifying full system functionality.", "A new theory is only needed if the original theory fails testing; here the fix has already been implemented.", null, "Escalation is an option when a theory cannot be confirmed; after implementing a fix, the next step is to verify it."],
  "np60": ["Verifying full system functionality happens after the fix is implemented, not immediately after confirming the theory.", "Documenting lessons learned is the final step, after the fix has been implemented and verified.", "Questioning users about recent changes belongs to the first step, identifying the problem, which is already done.", null],
  "np61": ["A wrong DNS server would break name resolution, but pinging remote networks by IP would still work.", "A duplex mismatch causes slowness and errors on the link, not a clean split where local hosts work and remote ones never do.", null, "Changing to /27 would put the host in the .64 to .95 range, which still does not include the .1 gateway."],
  "np62": [null, "A wrong VLAN affects which network and addresses the desktop gets, not the physical link speed that autonegotiation picks.", "DNS affects name resolution at the application level and has no influence on the Ethernet link speed.", "PoE only supplies power to powered devices; a desktop does not need it, and disabling it does not change link speed."],
  "np63": ["An incorrect default gateway breaks routing to other networks but does not corrupt frames or raise CRC errors.", "A duplicate IP address causes intermittent connectivity and ARP conflicts, not CRC errors on an interface.", "An expired DHCP lease causes loss of the IP address, not corrupted frames failing the checksum.", null],
  "np64": ["An incorrect subnet mask causes reachability problems for some destinations, not collisions on the link.", null, "DNS misconfiguration affects name resolution and cannot produce collisions at the Ethernet level.", "A port blocked by Spanning Tree forwards no user traffic at all, so it would not show late collisions from active traffic."],
  "np65": [null, "An OTDR tests fiber runs for breaks and loss by measuring reflections; it is not used to trace copper cables in a bundle.", "A loopback plug tests a port by looping its transmit back to receive; it cannot identify which cable goes to a jack.", "A Wi-Fi analyzer surveys wireless signals and channels and cannot trace wired cable runs."],
  "np66": ["Single-mode fiber on both ends is a matched, valid setup; the problem would be a mismatch, such as single-mode optics with multimode cable.", null, "VLAN membership does not affect physical link status; ports in different VLANs still show link.", "A short patch cable is not a link problem in normal switch connections; overly short runs only matter with very high-power long-range optics."],
  "np67": ["The user can already reach 8.8.8.8, so tracing the path to it adds little; the failure is name resolution, not routing.", "arp -a shows local IP-to-MAC mappings and cannot reveal why hostnames fail to resolve.", null, "iperf3 measures throughput, and bandwidth to the gateway is not the issue when IP connectivity works but names do not."],
  "np68": ["A long lease time keeps addresses assigned longer but does not by itself make the server hand out an address already used by a static device.", "Spanning Tree prevents Layer 2 loops and has nothing to do with IP address assignment or duplicates.", "A missing PTR record only affects reverse DNS lookups; it cannot cause duplicate IP address conflicts.", null],
  "np69": ["Overlapping channels cause interference and slow throughput but would not specifically cause new devices to fall back to 169.254.x.x addresses.", "A wrong default gateway would break internet access for all clients, and they would still receive DHCP addresses, not APIPA.", null, "A DNS outage stops name resolution but does not prevent DHCP from handing out addresses, so clients would not get APIPA."],
  "np70": [null, "Throughput is the actual data rate achieved; it can be fine while packet timing varies, which is the described problem.", "Attenuation is signal loss over distance on a medium; it is a physical-layer issue, not variation in packet delay.", "Bandwidth is the link's maximum capacity; the issue is inconsistent delay between packets, not insufficient capacity."],
  "np71": ["Raising transmit power on both APs increases co-channel interference and makes the contention worse.", "Using the same SSID enables roaming between APs but does nothing to fix both radios sharing channel 6.", "WEP is an insecure, obsolete encryption method and has no effect on channel contention.", null],
  "np72": ["Pinging the default gateway only tests the first hop, so it cannot show which hop further along the path adds delay.", null, "ipconfig /all shows the local interface configuration, not the latency of hops on the path to the cloud app.", "nslookup tests DNS name resolution and does not measure latency across network hops."],
  "np73": [null, "nmap scans hosts for open ports and services; it does not measure throughput between two sites.", "netstat shows local connections, listening ports and interface statistics, not achievable bandwidth across a WAN link.", "arp displays or edits the local IP-to-MAC cache and has nothing to do with measuring throughput."],
  "np74": ["A toner and probe traces physical cable runs and cannot see which TCP ports a server is listening on.", null, "A cable certifier tests cable performance against standards like Cat 6 and knows nothing about open TCP ports.", "ipconfig shows the local machine's IP configuration and cannot scan another host for listening ports."],
  "np75": ["A toner and probe traces copper cables with an electrical tone and cannot locate a break in a fiber run.", "A loopback plug verifies that a port can transmit and receive, but it cannot measure distance to a fiber break.", null, "A multimeter measures voltage, resistance and continuity on copper circuits; it cannot test glass fiber or find a break location."],
});
