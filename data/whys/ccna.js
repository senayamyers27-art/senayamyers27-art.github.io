CertHub.addWhys("ccna", {
  "cc1": [null, "A speed mismatch usually keeps the link from coming up at all; here both sides run 100 Mb/s and the link is up.", "An MTU mismatch causes drops of oversized frames (giants), not late collisions on one side and runts on the other.", "An STP-blocking port drops user frames but does not generate CRC errors, runts or late collisions."],
  "cc2": ["Crossover versus straight-through only affects pin mapping (and Auto-MDIX handles it); it does nothing about exceeding the 100 m copper limit.", null, "Gigabit over copper effectively requires full duplex, and changing duplex cannot extend the 100 m distance limit.", "Cat5e is a lower grade than Cat6 and is still limited to 100 m, so it makes the problem no better."],
  "cc3": ["A Layer 1 failure shows \"down, line protocol is down\"; \"administratively down\" means the port was shut down by configuration.", "An encapsulation mismatch typically shows \"up, line protocol is down\", not administratively down.", null, "Clearing counters only resets statistics; it does not remove the shutdown command keeping the port down."],
  "cc4": ["A type 2 (hosted) hypervisor such as VirtualBox runs on top of a general-purpose host OS, which this server does not have.", "A container runtime such as Docker shares a host OS kernel and does not run full VMs directly on hardware.", "A hardware emulator (like QEMU in pure emulation) simulates a different CPU architecture in software; ESXi is a bare-metal hypervisor.", null],
  "cc5": [null, "That describes virtual machines; containers share the host kernel and do not carry a full guest OS each.", "Containers run on a container runtime over the host kernel and need no hypervisor at all.", "Containers get networking through virtual bridges, overlays and port mappings, so they can be fully networked."],
  "cc6": ["172.20.45.64–.127 is the previous /26 block; .130 falls after .127, so it is not in this subnet.", null, "Broadcast .255 would be correct for a /24; a /26 starting at .128 ends at .191.", "172.20.45.0–.63 is the first /26 block and does not contain .130."],
  "cc7": ["A /24 (255.255.255.0) gives only 254 usable hosts, short of the 500 required.", "A /22 (255.255.252.0) gives 1022 usable hosts, which works but wastes far more addresses than a /23.", null, "A /25 (255.255.255.128) gives only 126 usable hosts."],
  "cc8": ["With a /21 the third-octet blocks are 0–7 and 8–15, so 10.10.12.30 is not in 10.10.0.0/21.", "The hosts are configured with /21, not /16; the router-free check must use the configured mask, which puts them in different subnets.", "10.10.4.0 is not a /21 boundary (blocks go in steps of 8), so PC-A's network is 10.10.0.0/21, not 10.10.4.0/21.", null],
  "cc9": [null, "A /16 mask would be inconsistent with the router's /24 LAN, and it would not make 192.168.2.1 a reachable gateway anyway.", "A wrong DNS server breaks name resolution, not pinging remote hosts by IP; the gateway is the issue.", "A duplex mismatch would cause errors and poor performance on local traffic too, which is working fine."],
  "cc10": ["The broadcast address of 10.0.0.0/26 is .63, not .62; .62 is a valid host address in the other subnet.", null, "The network address of 10.0.0.64/26 is .64, so .66 is a valid host address.", "Classless addressing lets any mask be used with any address, including a /26 on 10.x.x.x."],
  "cc11": ["The double colon can appear only once in an address; using it twice makes the length ambiguous.", "Only leading zeros may be dropped, so 00A0 becomes A0, not A.", null, "A00 adds a trailing zero rather than dropping leading zeros; 00A0 compresses to A0."],
  "cc12": ["FE80::/10 is link-local, valid only on a single link and never routed across the organization.", "FF00::/8 is the IPv6 multicast range, not unicast addressing for hosts.", "2000::/3 is global unicast space, which is routable on the internet.", null],
  "cc13": [null, "This omits flipping the 7th (U/L) bit, so the first byte stays 00 instead of becoming 02.", "FFFE must be inserted in the middle of the MAC (after the first 3 bytes), not after the fourth byte.", "The inserted value is FFFE; this splices in FEFF-style bytes (12FE:FF34), which is the wrong order."],
  "cc14": ["A Neighbor Advertisement answers address resolution (like ARP) and does not carry prefix or gateway information.", null, "DHCPv6 Advertise comes from a DHCPv6 server, and the question states no DHCPv6 server exists.", "Duplicate Address Detection uses Neighbor Solicitations to check if an address is in use; it gives no prefix or gateway."],
  "cc15": ["Channels 1, 4 and 8 are only 3–4 channels apart, so their 22 MHz-wide signals overlap and cause adjacent-channel interference.", "Channels 3, 6 and 9 are only 3 apart and overlap each other.", null, "Channels 1, 2 and 3 are adjacent and almost entirely overlap."],
  "cc16": ["2.4 GHz was already used by earlier Wi-Fi generations and is the most crowded band.", "5 GHz is used by Wi-Fi 5 and Wi-Fi 6; 6E's addition is beyond it.", "900 MHz is used by 802.11ah (Wi-Fi HaLow) for IoT, not by Wi-Fi 6E.", null],
  "cc17": [null, "With a wrong WPA2 passphrase the 4-way handshake fails and the client never gets network access, so it would not be sitting on the SSID with an APIPA address.", "A DNS failure breaks name resolution but the laptop would still have a valid DHCP address and gateway.", "Co-channel interference hurts throughput and reliability but does not produce a 169.254.x.x self-assigned address."],
  "cc18": ["Excluded addresses stop a pool from leasing specific IPs; they do not help broadcasts reach a server on another subnet.", null, "The helper address goes on the client-facing gateway interface and points to the server, not the other way around.", "no service dhcp disables DHCP server and relay functions on the router, which would make relaying impossible."],
  "cc19": ["ip helper-address relays DHCP broadcasts to a remote server; the router itself is the server here, so it does nothing for conflicts.", "lease only changes how long leases last; the pool would still hand out the printers' addresses.", null, "dns-server just tells clients which DNS server to use; it does not stop the pool from leasing the static addresses."],
  "cc20": ["arp -a only shows the ARP cache of IP-to-MAC mappings.", "nslookup queries DNS for a name; it does not show the full adapter configuration.", "tracert shows the hop-by-hop path to a destination, not local IP settings.", null],
  "cc21": [null, "Switches drop frames only for reasons like errors or filtering; an unknown unicast destination is flooded, not dropped.", "Flooding is limited to the frame's VLAN; ports in other VLANs are a separate broadcast domain.", "A switch has no concept of a default gateway port; it forwards by MAC table or floods unknown unicasts."],
  "cc22": ["vlan 30 creates the VLAN, which already exists and is active; the port still is not assigned to it.", null, "Making Gi0/6 a trunk suits a switch uplink, not a single user's PC, and would not put the PC in VLAN 30.", "An SVI for VLAN 30 provides Layer 3 routing for the VLAN; it does not change a port's VLAN membership."],
  "cc23": ["Without the add keyword this replaces the whole allowed list with only VLAN 30, cutting off VLANs 10 and 20.", "Changing the native VLAN only affects untagged traffic and would create a native VLAN mismatch; VLAN 30 would still need to be allowed.", null, "PortFast speeds up edge ports to end hosts; it does not control which VLANs a trunk carries."],
  "cc24": ["Disabling CDP only stops the switches from detecting the mismatch; untagged traffic would still leak between VLANs 1 and 99.", "Converting to access ports removes trunking entirely and would carry only one VLAN, breaking the inter-switch link.", "The allowed list does not decide which VLAN untagged frames belong to; the native VLANs would still differ.", null],
  "cc25": [null, "A trunk forms only if at least one side actively negotiates (desirable) or is set to trunk; two auto ports both wait.", "Err-disable comes from features like port security or BPDU guard, not from two dynamic auto ports.", "EtherChannel requires channel-group configuration; DTP modes only negotiate trunking."],
  "cc26": ["LACP needs at least one side in active mode to send LACPDUs; two passive sides never start negotiation.", null, "PAgP uses auto and desirable modes; passive is an LACP mode, and nothing is negotiated here anyway.", "Without negotiation no bundle forms at all; the links operate as separate ports under STP."],
  "cc27": ["A working bundle shows Po1(SU) with members flagged P; SD and I mean it is down and nothing is bundled.", "Suspended ports are flagged s, not I; I means stand-alone.", null, "Hot-standby ports are flagged H; both ports here are I (stand-alone)."],
  "cc28": ["Interface descriptions are just labels and have no effect on EtherChannel compatibility.", "EtherChannel members do not need consecutive interface numbers.", "CDP settings are not part of the EtherChannel compatibility check.", null],
  "cc29": [null, "SW1's MAC 0011.1111.1111 is higher than SW2's 000A.AAAA.AAAA, so SW1 loses the tie-breaker.", "SW3's MAC 00BB.BBBB.BBBB is the highest of the three, so it cannot win.", "Boot order does not matter; the lowest bridge ID (priority then MAC) always wins the root election."],
  "cc30": ["The local port number is not a tiebreaker at this stage; the sender bridge ID is compared first, then sender port priority and port number.", null, "STP always prefers the lowest values, never the highest MAC address.", "BPDU arrival time plays no part in root port selection."],
  "cc31": ["40960 is higher than the default 32768, so it makes SW1 less likely to become root.", "PortFast lets edge ports skip listening and learning; it has nothing to do with root bridge election.", null, "Port cost influences root port and path selection, not which switch becomes root bridge."],
  "cc32": ["That is what happens to PortFast alone when a BPDU arrives; BPDU guard is stricter and shuts the port down.", "The port is err-disabled before the new switch can take part in STP, so it cannot become root.", "Discarding until BPDUs stop is closer to root guard's root-inconsistent behavior; BPDU guard err-disables the port.", null],
  "cc33": [null, "Blocking, listening, learning and forwarding are the classic 802.1D STP states, not RSTP's.", "Rapid PVST+ has no listening state, and this list leaves out learning.", "Err-disabled is a port error condition, not an RSTP port state, and RSTP replaces blocking with discarding."],
  "cc34": ["This swaps the VLANs: the PC's data would land in VLAN 150 and the phone's voice in VLAN 10.", null, "A trunk is unnecessary for a phone plus PC and would put the untagged PC data in VLAN 150, the voice VLAN.", "An access port has only one access VLAN; the second command simply overwrites the first."],
  "cc35": ["Routers route IPv4 by default; ip routing is needed on Layer 3 switches, not here.", "Routers do not use switchport commands; trunking on a router is done with dot1Q subinterfaces.", null, "switchport commands do not exist on a router interface, and the native VLAN is set with encapsulation dot1Q 20 native if needed."],
  "cc36": ["encapsulation dot1Q is for router subinterfaces; SVIs are already tied to their VLAN.", "Host-facing ports should stay access ports; trunking them would not enable routing between VLANs.", "STP runs on Layer 2 ports, not SVIs, and the SVIs are up/up and reachable.", null],
  "cc37": [null, "CDP is Cisco-proprietary, so a third-party switch generally will not send or understand it.", "DTP negotiates trunking between Cisco switches; it does not report model or management IP.", "VTP distributes VLAN information between Cisco switches; it is not a neighbor discovery protocol."],
  "cc38": ["Trunking changes VLAN handling but does not enable PoE; the port has power inline never.", null, "PortFast speeds up STP convergence but does not provide power to the AP.", "Oper off with Admin never shows PoE is disabled by configuration; the AP is not drawing any power at all."],
  "cc39": ["GRE is a generic tunneling protocol; Cisco lightweight APs use CAPWAP to reach the WLC.", "DTP negotiates trunking on switch ports and does not tunnel AP traffic.", null, "LACP bundles Ethernet links into an EtherChannel; it has nothing to do with AP-to-controller tunnels."],
  "cc40": ["An err-disabled port does not forward traffic, so it would not keep learning twelve MAC addresses.", "Being the root port does not by itself cause many MACs; it is the downstream devices on the link that do.", "An overflowing MAC table would cause flooding across the switch, not neatly map twelve MACs to one port.", null],
  "cc41": [null, "The /16 also matches, but the router always prefers the longest matching prefix, and the /24 is longer.", "10.10.10.0/25 covers only .0–.127, so it does not match 10.10.10.200.", "The default route is used only when no more specific route matches, and the /24 matches."],
  "cc42": ["Metrics from different protocols are not comparable; the router first compares administrative distance.", null, "Load balancing only happens across equal-cost paths from the same source; different protocols are decided by AD.", "The router does not need comparable metrics; it picks the route with the lowest administrative distance."],
  "cc43": ["The administrative distance is the first number in brackets (110), not 65.", "OSPF does not use hop count; that is RIP's metric.", null, "The route's age is shown separately as 00:02:11, not inside the brackets."],
  "cc44": ["With no AD this static route defaults to AD 1 and would override the OSPF default immediately.", "AD 90 is still lower than OSPF's 110, so this static would replace the OSPF default instead of backing it up.", "Mask 255.255.255.255 makes this a host route to 0.0.0.0/32, not a default route.", null],
  "cc45": [null, "ipv6 enable only creates a link-local address on an interface; it does not turn on IPv6 forwarding.", "ip routing controls IPv4 routing; IPv6 forwarding is enabled separately.", "An ACL only filters traffic; without ipv6 unicast-routing nothing is forwarded to filter in the first place."],
  "cc46": ["IOS rejects a link-local next hop without an exit interface because FE80::2 could exist on any link.", null, "ip route is the IPv4 command and cannot take an IPv6 prefix or next hop.", "There is no link-local keyword, and the interface must come before the next-hop address."],
  "cc47": ["The OSPF process ID is locally significant and does not need to match between neighbors.", "Router IDs must be unique, so different router IDs are normal and required.", null, "Interface priority only affects the DR/BDR election; it does not prevent neighbor formation."],
  "cc48": ["Process IDs are not carried in hellos and do not need to match between routers.", "2-WAY is normal only between DROTHERs on a multi-access segment; with just two routers one becomes DR and they go FULL.", "Summarization has nothing to do with process IDs, which are local to each router.", null],
  "cc49": [null, "A physical interface address is used only if no loopback is up; loopbacks take precedence.", "1.1.1.1 is a loopback, but the highest loopback address (5.5.5.5) is chosen, not the lowest.", "0.0.0.0 is not a valid router ID; OSPF picks one from interface addresses when none is configured."],
  "cc50": ["Priority 0 makes R3 ineligible to become DR or BDR, regardless of its high router ID.", null, "R2's priority of 10 beats R1's 1, so R2 becomes DR, not BDR.", "R3 has priority 0 and can never be BDR; R1 takes that role."],
  "cc51": ["Removing the network statement stops OSPF on G0/1 entirely, so the user LAN would no longer be advertised.", "A high cost only makes paths through G0/1 less preferred; hellos would still be sent to the users.", null, "Shutting down G0/1 disconnects the users and removes the subnet from OSPF."],
  "cc52": ["10.1.0.0 0.0.255.255 matches all of 10.1.x.x, which includes G0/2 (10.1.4.1).", "10.1.2.0 0.0.0.255 matches only 10.1.2.x, so G0/1 (10.1.3.1) would be left out.", "10.1.4.0 0.0.1.255 matches 10.1.4.0–10.1.5.255, which covers G0/2 and neither of the others.", null],
  "cc53": [null, "bandwidth is in kbps, so this would mark the 1 Gbps links as 10 kbps; it skews cost but also misleads EIGRP, QoS and monitoring, and is a hack rather than the fix.", "OSPF priority only affects DR/BDR election, not path cost.", "Hello interval changes neighbor detection timing and must match neighbors; it does not affect path selection."],
  "cc54": ["R1 has preempt enabled (the P flag) and a higher priority, so R2 will not remain active once R1 returns.", null, "R2 is the standby router and takes over the virtual IP during the outage, so hosts keep their gateway.", "HSRP has only one active router per group; both never share the virtual IP as active."],
  "cc55": ["HSRP is Cisco-proprietary and its virtual IP cannot be a real interface address.", "GLBP is Cisco-proprietary and focuses on load balancing across multiple gateways.", null, "OSPF is a routing protocol, not a first hop redundancy protocol for hosts' default gateway."],
  "cc56": ["An ACL block usually shows asterisks or an administratively-prohibited reply at one hop, not the same two routers repeating.", "A DNS failure would stop traceroute before it starts or show only IPs; it would not create alternating hops.", "An MTU problem drops large packets but does not make packets bounce between two routers.", null],
  "cc57": [null, "Wildcard 0.0.0.64 ignores only the 64 bit, so it matches just 192.168.10.0 and 192.168.10.64, not the range .64–.127.", "0.0.0.127 with .64 matches .0–.127 (the .64 bit is ignored), which is twice the required range.", "ACLs use wildcard masks, not subnet masks; 255.255.255.192 as a wildcard would match almost any address."],
  "cc58": ["ACL 110 is in the extended range (100–199), and extended ACLs can match ICMP.", null, "HTTP to the server works, which shows the direction is fine for that traffic.", "Ping uses ICMP echo, not TCP port 80."],
  "cc59": ["The ACL stops at the first match, and 10.1.5.9 already matched the deny on line 1.", "Processing never reaches the third line because line 1 matches first.", null, "The implicit deny only applies when nothing matches; line 1 matches this packet."],
  "cc60": ["A standard ACL filters only by source address, so it would block all of 10.1.1.5's traffic, not just Telnet to one server.", "A standard ACL cannot match destination or port, so near the destination it would block the host from everything behind that interface.", "Applying the ACL everywhere is unnecessary and adds complexity and risk; one well-placed ACL is enough.", null],
  "cc61": [null, "The inside global is the public translated address, 203.0.113.5:1025, in the first column.", "The outside local is how the remote host appears to the inside network, 198.51.100.20:443 here.", "The outside global is the remote server's real public address, 198.51.100.20:443, in the last column."],
  "cc62": ["A static NAT maps one inside host to one address; it does not let 200 hosts share the interface address.", null, "Without overload this is dynamic NAT, which needs one public address per concurrent host, and the pool has not been defined.", "ip nat outside source translates outside addresses, not the inside hosts heading to the internet."],
  "cc63": ["G0/1 faces the LAN, so marking it outside is wrong; G0/0 is already the outside interface.", "ip helper-address relays DHCP broadcasts and has nothing to do with NAT.", null, "NAT builds translations dynamically and handles return traffic automatically; no extra ACL is needed."],
  "cc64": ["Err-disable is the shutdown violation mode (the default), not restrict.", "Silent dropping with no log is protect mode; restrict also logs, sends a trap and increments the violation counter.", "Port security does not age out the oldest MAC on violation; the new MAC is treated as a violation.", null],
  "cc65": [null, "Trusting access ports would let a rogue DHCP server on those ports hand out addresses, defeating the purpose.", "Disabling snooping restores leases but removes the protection against the rogue server.", "ip helper-address is a Layer 3 relay command and does not change DHCP snooping trust on a switch port."],
  "cc66": ["The MAC address table maps MACs to ports but does not hold the IP-to-MAC bindings DAI needs.", null, "DAI runs on the switch using its own data; it does not consult a router's ARP cache.", "The CDP table lists directly connected Cisco devices, not host IP-to-MAC bindings."],
  "cc67": ["RADIUS encrypts only the password field and combines authentication and authorization, so it cannot authorize each command separately.", "LDAP is a directory access protocol, not a device-administration AAA protocol with per-command authorization.", null, "SNMPv2c is a monitoring protocol with clear-text community strings, not an AAA protocol."],
  "cc68": ["transport input telnet allows only Telnet, the opposite of SSH-only, and password encryption does not enable SSH.", "An enable secret and console password secure local access but do not enable SSH on the VTY lines.", "ip http secure-server enables the HTTPS web interface, not SSH CLI access.", null],
  "cc69": [null, "ip access-group is an interface command and is not used under line configuration.", "An ACL on the SVI would filter all traffic through that interface, not just management sessions, and could miss other routed paths to the VTY lines.", "The console line is local physical access; SSH arrives on the VTY lines, and out would filter outbound sessions."],
  "cc70": ["The site is reachable by IP, so the gateway and routing are working.", null, "HTTPS to the IP address works, which proves TCP 443 is not blocked.", "A duplex mismatch would degrade all traffic, but browsing by IP works and nslookup specifically fails."],
  "cc71": ["TKIP is a deprecated WPA-era cipher and WPA2-Personal still uses the pre-shared-key handshake, not SAE.", "WEP is broken and can be cracked in minutes regardless of key length.", null, "WPA2-Enterprise uses 802.1X with a RADIUS server, not SAE, and is not a personal mode."],
  "cc72": ["Remote-access VPN clients require software on each laptop, which the branch wants to avoid.", "GRE provides tunneling but no encryption, so traffic over the internet would be exposed.", "A clientless SSL VPN gives individual users browser-based access, not an always-on network-to-network link.", null],
  "cc73": [null, "Warning is severity 4; the -5- in the message marks severity 5.", "Error is severity 3; this message carries -5-.", "Informational is severity 6; this message carries -5-."],
  "cc74": ["A logging level is a threshold, so it includes all more severe levels, not only level 4.", null, "Levels 5–7 are less severe than 4 and are excluded; the threshold works downward to 0.", "Sending all levels would require logging trap 7 (debugging)."],
  "cc75": ["Traps are sent unsolicited but are never acknowledged by the manager.", "Get is sent by the manager to request values from the agent.", null, "Set is sent by the manager to change values on the agent."],
  "cc76": ["authNoPriv provides authentication but no encryption.", "noAuthNoPriv provides neither authentication nor encryption.", "SNMPv2c sends community strings in clear text and has no encryption, whatever the length.", null],
  "cc77": [null, "That describes pull-based tools like Puppet or Chef; Ansible needs no agent on managed devices.", "Ruby manifests are associated with Puppet and Chef; Ansible uses YAML playbooks and runs from a control node.", "Ansible manages devices directly over SSH or APIs and does not require an SDN controller."],
  "cc78": ["A failed connection would show unreachable or failed, not ok=3.", null, "Ansible does not remember past runs; it checks each device's current state every time.", "Check mode must be requested explicitly with --check; it is not turned on automatically for a second run."],
  "cc79": ["Generative AI creates new content such as text or configurations; this system is detecting deviations, not generating anything.", "A fixed threshold alarm fires at a preset value; this platform learned each interface's baseline.", null, "Infrastructure as code manages configuration as versioned files; it does not monitor for anomalies."],
  "cc80": ["Sharing secrets with an AI tool risks leaking credentials, and the request is too vague to get a useful summary.", "This gives no context, data or output format, so the AI cannot produce a useful result.", "This is vague and not tied to the actual configuration or change ticket.", null],
  "cc81": [null, "A chatbot that only answers questions is conversational AI, not agentic, because it takes no actions.", "A fixed scheduled script follows predefined steps and does not plan or adapt like an agent.", "An ASIC is forwarding hardware, not an AI system that plans and acts."],
  "cc82": ["Northbound APIs connect the controller up to applications; controllers reach devices through southbound interfaces.", null, "The data plane forwards user traffic; controller communication happens via management and control protocols.", "STP BPDUs are used between switches for loop prevention, not for controller management."],
  "cc83": ["Device-by-device CLI is manual and lacks version-controlled files and automated pipelines.", "A cloud dashboard centralizes management but is GUI-driven rather than versioned configuration files applied by a pipeline.", null, "Out-of-band console management is a separate access path for devices, not a configuration methodology."]
});
