CertHub.addWhys("jncia-junos", {
  "jj1": ["A switch gives each port its own collision domain, so 24 connected ports cannot share a single collision domain; that would describe a hub.", null, "All ports are in one VLAN, so they form a single broadcast domain; only a hub would make them one collision domain.", "Switch ports do not split broadcast domains; that would take 24 VLANs or 24 router interfaces."],
  "jj2": [null, "A host only ARPs directly for a destination on its own subnet, and 10.2.2.20 is outside 10.1.1.0/24.", "DNS resolves names to IP addresses; the ping already has an IP, and ARP targets the next hop, not the DNS server.", "ARP requests are sent to the broadcast MAC, but the address being resolved is the gateway, not a broadcast address."],
  "jj3": ["30 is the usable count for a /27 (32 addresses), not a /26.", "64 is the total size of a /26 block; it still includes the network and broadcast addresses, which cannot be assigned.", "126 is the usable count for a /25 (128 addresses), twice the size of a /26.", null],
  "jj4": ["172.16.45.0/25 covers only .0 to .127, so it cannot contain .130.", "The .64 boundary belongs to /26 blocks; a /25 only has blocks starting at .0 and .128.", null, "Changing the third octet to 44 is wrong because a /25 only splits the last octet; the third octet stays 45."],
  "jj5": ["2001:db8::/32 is the documentation prefix, reserved for examples, not a prefix every interface uses.", null, "ff00::/8 is the IPv6 multicast range, not unicast link-local addressing.", "fc00::/7 is the unique local range, the IPv6 counterpart of private IPv4, and is not automatically on every interface."],
  "jj6": [null, "32 bits is the length of an IPv4 address, and MAC addresses do not identify networks.", "MACs are 48 bits, but the VLAN ID is a separate 12-bit field in the 802.1Q tag, not part of the MAC.", "64 bits is the length of an IPv6 interface ID (EUI-64), not an Ethernet MAC address."],
  "jj7": ["Switches do not drop unknown unicast frames; dropping would break communication until the MAC was learned.", "ARP is a Layer 3 host mechanism for mapping IPs to MACs; a switch forwarding a frame does not send ARP requests.", "A switch never sends a frame back out the port it arrived on.", null],
  "jj8": ["In-order, guaranteed delivery is a TCP feature; UDP provides neither.", "UDP does not encrypt anything; encryption for voice comes from protocols such as SRTP.", null, "UDP's header is 8 bytes, smaller than TCP's 20 bytes, so larger headers is backwards and would not help anyway."],
  "jj9": ["best-effort is the default class for ordinary user traffic with no special treatment.", null, "assured-forwarding is for user traffic that needs a guaranteed rate with controlled drop, not control protocols.", "expedited-forwarding is for low-latency, low-loss user traffic such as voice, not routing protocol traffic."],
  "jj10": [null, "A multifield classifier uses a firewall filter matching several header fields, not just the existing DSCP value.", "A rewrite rule sets markings on egress; it does not assign packets to forwarding classes on ingress.", "A scheduler map ties forwarding classes to schedulers for queuing; it does not classify packets."],
  "jj11": ["A classifier reads markings on ingress to choose a forwarding class; it does not change the DSCP value.", "A policer limits traffic rate; although it can set loss priority, it is not the component that rewrites DSCP on egress.", "A drop profile controls how packets are dropped as a queue fills; it does not change markings.", null],
  "jj12": ["Removing a group of zeros without :: is not allowed; 2001:db8:0:1 would be only four groups and a different address.", "Using :: twice is invalid because the number of zero groups each one represents would be ambiguous.", null, "This expands to a different address with the 1 in the wrong group; the original ends in ...:0001."],
  "jj13": ["A trunk carries several VLANs between switches but keeps them separate; it does not let VLAN 10 talk to VLAN 20.", null, "MAC table size limits how many addresses a switch learns; it has nothing to do with crossing VLAN boundaries.", "Spanning Tree prevents Layer 2 loops; it does not route between VLANs."],
  "jj14": [null, "The PFE forwards packets using the forwarding table; it does not run routing protocols.", "The switch fabric moves packets between line cards; it has no role in running protocols.", "A PIC provides physical ports; it does not run routing protocols or build tables."],
  "jj15": ["The RE handles only exception (host-bound) traffic; transit packets never touch its CPU.", "mgd runs the CLI and commits; it has no role in forwarding packets.", "chassisd monitors hardware and alarms; it does not forward traffic.", null],
  "jj16": ["A web session crossing the router is transit traffic forwarded entirely by the PFE.", "Matching the default route is ordinary forwarding; the PFE handles it without the RE.", null, "Switching a frame within a VLAN is data-plane forwarding done by the PFE, not exception traffic."],
  "jj17": ["rpd is the routing protocol daemon; it runs OSPF, BGP and similar protocols, not the CLI.", null, "dcd configures interfaces; it does not run the CLI or the commit process.", "chassisd monitors chassis hardware and alarms, not the CLI or commits."],
  "jj18": [null, "rpd handles routing protocols and routes, not physical interface settings like MTU.", "chassisd monitors hardware components and alarms; interface configuration belongs to dcd.", "snmpd answers SNMP polls and sends traps; it does not apply interface configuration."],
  "jj19": ["ARP only maps next-hop IPs to MACs; it cannot tell the PFE which prefixes go where.", "The PFE does not run routing protocols; OSPF and BGP run on the RE in rpd.", "The candidate config is uncommitted configuration text and is not used to forward packets.", null],
  "jj20": ["The size of the forwarding table has nothing to do with how much host-bound traffic reaches the RE.", "Transit traffic never goes to the RE, so limiting RE-bound traffic does not speed it up.", null, "Rate limiting controls volume; it does not encrypt anything, which SSH or other protocols handle."],
  "jj21": ["Routers never reset a TTL upward; an expired packet must be dropped and an ICMP time-exceeded sent.", null, "IP routers do not retransmit packets; retransmission is a TCP endpoint function.", "Mirroring to all ports is flooding behavior for unknown frames, not how a router treats an expired packet."],
  "jj22": [null, "The RE is not in the transit path at all, so separation does not make the RE forward faster.", "The PFE absolutely needs a forwarding table; it is the table it uses to forward every packet.", "Interfaces still need configuration; the control/forwarding split does not remove that."],
  "jj23": ["Junos OS Evolved keeps the same CLI as Junos OS.", "Traditional Junos OS fully supports IPv6 routing.", "Both use the same configuration syntax and commit model; that consistency is a design goal.", null],
  "jj24": ["An output filter on ge-0/0/0 inspects traffic leaving one interface, not SSH arriving at the RE from every interface.", "Filtering only the management interface leaves SSH to the router's other interface addresses unprotected.", null, "The forwarding table holds routes, not packet filters; filters are applied to interfaces."],
  "jj25": ["mgd is the management daemon behind the CLI and commits, not hardware monitoring.", null, "rpd runs routing protocols; it does not monitor fans, power or raise chassis alarms.", "dcd configures interfaces; chassis hardware monitoring is done by chassisd."],
  "jj26": [null, "Juniper's routing, switching and security families share one Junos CLI; that is its main selling point.", "EX and QFX switches run Junos OS.", "The commit model works the same on switches, routers and firewalls, not only routers."],
  "jj27": ["`show route` shows the RE's routing table, including inactive routes, not what was pushed to the PFE.", "`show interfaces terse` lists interface status and addresses, not routes.", "`show pfe statistics traffic` shows PFE packet counters, not the forwarding table contents.", null],
  "jj28": ["Operational mode ends in >, not #.", "The shell prompt looks like % (for example root@R1%), not the CLI #.", null, "Single-user recovery mode is a boot option for password recovery, not what the # CLI prompt shows."],
  "jj29": ["In configuration mode, `show` displays the configuration hierarchy, not interface status.", null, "`exit` leaves the current level or mode; it does not take a command to run.", "`op` runs an op script, not an ordinary operational command from configuration mode."],
  "jj30": [null, "`| find` starts at the first matching line and prints everything after it, including non-matching lines.", "`| count` only prints a line count, not the matching lines.", "`| except` hides lines that contain the string, the opposite of what is wanted."],
  "jj31": ["`| display xml` outputs the configuration as XML, which cannot be pasted as CLI commands.", "`| compare` shows differences between configurations, not the full config.", "`| no-more` only turns off paging; the output stays in hierarchical format.", null],
  "jj32": ["`up` moves only one level (or a given number of levels), not straight to the top.", "`exit configuration-mode` leaves configuration mode entirely instead of going to the top of the hierarchy.", null, "`quit` exits the current level (like exit), not to the top of the hierarchy."],
  "jj33": ["`configure exclusive` locks the configuration so the second admin cannot make changes at all.", null, "`configure batch` groups commits from several users into one batch; it does not isolate each user's changes.", "`edit shared` is not a Junos configuration mode command; plain `configure` is shared mode."],
  "jj34": [null, "The candidate holds uncommitted changes and does not take effect until it is committed.", "The rescue configuration is a saved known-good fallback, not what is running.", "The factory default is only active on a new or zeroized device, not normally what runs."],
  "jj35": ["Neither Tab nor Space runs a command; Enter does.", "Command history is viewed with `show cli history` or the arrow keys, not Tab.", "Leaving a level uses `exit` or `up`; Tab does not change the hierarchy position.", null],
  "jj36": ["`| find down` prints from the first match onward, not a count.", "`| last` shows the final lines of the output, not a count of down interfaces.", null, "`| except up` hides lines with \"up\" and would also drop admin-up/link-down lines; `no-more` just disables paging and gives no number."],
  "jj37": ["`help topic` gives general background on a feature, not the syntax of a specific statement.", null, "There is no `help history`; command history is shown with `show cli history`.", "`help syslog` explains system log message tags, not configuration statement syntax."],
  "jj38": [null, "NETCONF is an XML API for automation over SSH, not a browser GUI.", "Login classes define user permissions; they do not enable a web interface.", "`extension-service` enables programmable APIs such as JET, not the J-Web GUI."],
  "jj39": ["`show | display set` prints the whole candidate as set commands but does not highlight changes.", "`commit check` validates syntax and semantics but does not list what changed.", "`show system commit` lists commit history, not the differences in the current candidate.", null],
  "jj40": ["`exit` has no count argument; it leaves one level at a time.", "`top` always jumps straight to the top and takes no count.", null, "There is no `back` command in the Junos CLI."],
  "jj41": ["A host name is recommended but not mandatory; Junos commits without one.", null, "NTP is optional; the router commits without a time server.", "A loopback address is good practice but is not required for a commit."],
  "jj42": [null, "`commit check` only validates the candidate and applies nothing, so it cannot protect access once the change is applied.", "`commit and-quit` commits and leaves configuration mode with no automatic rollback.", "`commit synchronize` copies the commit to the backup RE; it offers no rollback timer."],
  "jj43": ["10 is too low; Junos keeps rollback numbers up to 49.", "20 is too low; Junos keeps rollback 0 through 49.", "32 is not the Junos limit; it keeps 50 versions (0 through 49).", null],
  "jj44": ["`rollback 0` loads the current active configuration, which still contains the bad change.", "`load factory-default` wipes the whole configuration rather than returning to the previous version.", null, "`rollback 1` only changes the candidate; without commit the bad configuration stays active."],
  "jj45": ["The VLAN ID is configured separately with vlan-id; the unit number is only a label, even if it often matches.", null, "The port number is 3, the last number before the dot.", "The PIC slot is 2, the middle number, not the number after the dot."],
  "jj46": [null, "The physical interface level holds settings like MTU and speed; IP addresses go on a logical unit.", "routing-options holds static routes and router ID, not interface addresses.", "system services enables management services such as SSH, not interface addresses."],
  "jj47": ["`load merge` combines the file with the existing candidate instead of replacing it.", "`load patch` applies a diff-style patch to the candidate, not a full replacement.", "`load merge relative` merges statements at the current hierarchy level; it still keeps the rest of the candidate.", null],
  "jj48": ["`delete` removes the statement from the configuration entirely.", "`annotate` only adds a comment; the statement stays active.", null, "`insert before` reorders terms; it does not disable anything."],
  "jj49": ["The rescue configuration is a saved fallback, not a way to share statements across interfaces.", null, "A login class controls user permissions, not interface settings.", "Routing instances separate routing tables; they do not apply MTU to interfaces."],
  "jj50": [null, "operator can view and clear or restart some things but cannot change the configuration.", "read-only can only view information and cannot configure.", "unauthorized has no permissions at all."],
  "jj51": ["`save rescue` is not a valid command; `save` writes the configuration to a named file.", "`rollback rescue` loads the rescue configuration into the candidate; it does not save one.", "`load rescue` is not how Junos saves a rescue configuration; loading brings configuration in, not out.", null],
  "jj52": ["`commit confirmed` applies the change now, with an automatic rollback if not confirmed.", "`commit comment` records a note in the commit history but still commits immediately.", null, "`commit check` only validates the candidate and applies nothing."],
  "jj53": ["lo0 is the loopback interface, a logical interface with no physical port.", null, "irb is the integrated routing and bridging interface used to route between VLANs, not a management port.", "ae10 is an aggregated Ethernet (LAG) bundle, not the out-of-band management port."],
  "jj54": [null, "There is no `comment` configuration command; comments are added with annotate.", "`describe` is not a Junos command; the description statement labels interfaces but is not a general note on any statement.", "`note` is not a Junos CLI command."],
  "jj55": ["`show log messages` shows syslog entries, not live traffic rates and errors.", "`monitor traffic interface` captures and decodes host-bound packets rather than showing live counters.", "`show interfaces terse` shows status and addresses once, with no live rates or error counters.", null],
  "jj56": ["`show ospf interface ... extensive` shows OSPF interface state and timers, not decoded packets.", "`show ospf neighbor` lists adjacency states; it does not decode hello packets.", null, "`show route` displays the routing table, not packet contents."],
  "jj57": ["/var/tmp is a temporary storage directory, commonly used for uploaded software packages, not the default log.", null, "/config/juniper.conf holds the active configuration, not logs.", "/var/db holds databases and internal state files, not the default system log."],
  "jj58": [null, "Leaving traceoptions running keeps consuming CPU and disk and can fill storage.", "Rebooting is unnecessary disruption and does not remove traceoptions from the configuration.", "Deleting /var/log/messages destroys useful history and does nothing to stop the tracing."],
  "jj59": ["`request system reboot` only restarts the device; it cannot install a package.", "`load override` loads a configuration file, not a software package.", "`request system software validate` only checks the package against the current configuration without installing it.", null],
  "jj60": ["Remote SSH requires a working login, so it cannot bypass a lost root password.", "A factory reset is unnecessary and erases the whole configuration; recovery keeps it.", null, "SNMP cannot change the root password, and an SNMP write community would not bypass authentication."],
  "jj61": ["`request system zeroize` erases all configuration and data, which is destructive rather than a safe shutdown.", null, "`restart routing` only restarts rpd; the OS keeps running.", "`clear system commit` clears a pending commit, not a shutdown."],
  "jj62": [null, "Deleting /config/juniper.conf.gz destroys the saved configuration and could leave the device unbootable to its config.", "`request system zeroize` wipes the whole device, far beyond freeing disk space.", "`clear log` truncates a named log file; it does not clean temporary files and `clear log all` is not the storage cleanup tool."],
  "jj63": ["`show system processes` lists running processes, not hardware alarms.", "`show system users` shows logged-in users.", "`show version` shows software versions and model, not active alarms.", null],
  "jj64": ["`show system uptime` shows boot, last-commit and current times, not CPU or temperature.", "`show interfaces extensive` gives detailed interface counters, not RE utilization.", null, "`show system storage partitions` shows disk partitions, not CPU, memory or temperature."],
  "jj65": ["SNMP trap groups send SNMP traps, not syslog messages.", null, "`system ntp server` configures time synchronization, not logging.", "`syslog file` writes to a local file named remote-10.0.0.50; it does not send to the server."],
  "jj66": [null, "Time zone only changes how time is displayed and takes a zone name, not a server address.", "`syslog host` sends log messages to that address; it does not synchronize the clock.", "A static route to the address only affects reachability, not time synchronization."],
  "jj67": ["`show system storage` shows disk usage, not the running software release.", "`show chassis hardware` lists hardware inventory and serial numbers, not the Junos version.", "`show system snapshot` shows backup snapshot images, which may differ from what is running.", null],
  "jj68": ["If the local outgoing interface were down, traceroute would fail at the very first hop.", "DNS only affects name resolution of hops; traceroute to an IP still gets replies from each hop.", null, "A wrong clock does not stop routers along the path from replying to traceroute probes."],
  "jj69": ["The /16 also matches, but it is less specific; longest-prefix match prefers the /24.", null, "Preference only compares routes to the same prefix; these are different prefixes, so longest match decides.", "Route age is not used to choose between different prefixes in Junos."],
  "jj70": [null, "OSPF internal has preference 10, higher (worse) than static's 5, so it loses.", "Hop count is a metric within one protocol, not how Junos chooses between protocols.", "Routes from different sources with different preferences are not load-balanced; only the best becomes active."],
  "jj71": ["20 is the eBGP administrative distance on another vendor's platform, not the Junos preference.", "110 is OSPF's administrative distance on another vendor's platform, not the Junos BGP preference.", "200 is the iBGP administrative distance on another vendor's platform, not the Junos preference.", null],
  "jj72": ["inet6.0 holds IPv6 unicast routes, not IPv4.", "inet.3 holds MPLS LSP next hops used to resolve BGP next hops, not general IPv4 unicast routes.", null, "mpls.0 holds MPLS label-switching entries, not IPv4 unicast routes."],
  "jj73": ["Hidden routes are not marked with * and appear only with `show route hidden`.", null, "Static routes show [Static/5] in the output; the * marks any active route regardless of source.", "Route age is shown as a timer next to the route, not by the asterisk."],
  "jj74": [null, "`protocols static` does not exist in Junos; static routes are under routing-options.", "Interfaces do not have a default-route statement; routes are configured under routing-options.", "There is no default-gateway statement under routing-options; a Junos default route is a static route to 0.0.0.0/0."],
  "jj75": ["Two next hops with the same preference are both usable, so the backup would not stay idle.", "A reject next hop drops traffic and sends ICMP unreachable; it cannot forward to the backup ISP.", "A discard route drops traffic silently; it cannot forward to the backup ISP.", null],
  "jj76": ["It is reject, not discard, that sends ICMP unreachable; discard drops silently.", "Both discard and reject drop the traffic; they differ only in whether ICMP is sent.", null, "Both discard and reject install a route in the routing table."],
  "jj77": ["A forwarding instance is used for filter-based forwarding and does not give a customer its own independent routing table with interfaces.", null, "A vrf instance with route targets is used for MPLS Layer 3 VPNs, which is the signaling the provider wants to avoid.", "An l2vpn instance provides Layer 2 VPN services, not separate IPv4 routing tables."],
  "jj78": [null, "Junos puts the instance name first, not after inet.", "The instance name is a prefix, not a suffix after inet.0.", "CUST-A.inet6.0 is the instance's IPv6 table, not its IPv4 table."],
  "jj79": ["10 is the preference of OSPF internal routes, not external routes.", "100 is the default preference for RIP, not OSPF external.", "170 is the default preference for BGP, not OSPF external.", null],
  "jj80": ["OSPF is an interior gateway protocol that routes within one AS.", "VRRP provides a redundant default gateway on a LAN; it does not exchange routes.", null, "LLDP discovers neighbors at Layer 2 and exchanges no routes."],
  "jj81": ["A lower preference makes a route more preferred, so it cannot be what keeps it inactive.", null, "A description is optional and has no effect on whether a route is active.", "A loopback is not needed for a static route to become active."],
  "jj82": [null, "Export policies control routes sent from the routing table into a protocol, not routes placed into the table.", "Forwarding table export policies affect load balancing to the PFE, not which protocol routes enter the routing table.", "An input filter is a firewall filter acting on packets, not on routes."],
  "jj83": ["Import policy controls routes coming into the routing table, not what OSPF advertises to neighbors.", "Preference decides the active route between sources; it does not cause OSPF to advertise a route.", "OSPF runs on its own and does not need BGP to advertise routes.", null],
  "jj84": ["Routes are not always rejected; for example, the BGP default policy accepts active BGP routes.", "Routes are not automatically accepted either; the protocol's default policy may reject them.", null, "Evaluation never loops back to the first term; it continues to the next policy or the default policy."],
  "jj85": ["prefix-length-range matches only a given range of lengths and does not include 10.0.0.0/8 plus all more specifics by itself.", null, "`longer` matches only more-specific prefixes and excludes the /8 itself.", "`upto /8` would only match the /8 itself, since the upper limit equals the prefix length."],
  "jj86": [null, "`longer` matches /17 to /32 but excludes the /16 itself and has no /24 upper limit.", "`192.168.0.0/24 exact` matches only that single /24, not the /16 or other /17-/24 routes.", "`orlonger` matches every length up to /32, including /25 and longer."],
  "jj87": ["Junos filters do not accept unmatched traffic; an explicit accept term is needed for that.", "Unmatched traffic is not sent to the RE; it is dropped at the implicit end of the filter.", "Nothing is counted for unmatched packets unless a term with a count action matched.", null],
  "jj88": ["discard drops the packet silently, with no ICMP message.", "count is a non-terminating action that only increments a counter.", null, "next term continues evaluation; it does not drop or send ICMP."],
  "jj89": ["A count-only term does not discard; Junos adds an implicit accept.", null, "A term match ends evaluation; Junos does not move on to another filter after a terminating implicit accept.", "Nothing tells Junos to reject; reject must be configured explicitly."],
  "jj90": [null, "`show policy` displays the policy's configuration; it does not test which routes it accepts.", "`commit check` validates the whole candidate configuration and takes no policy name.", "`show route export` is not the command that tests a policy against a prefix."],
  "jj91": ["A route-filter is a routing policy match condition on routes, not a per-packet source check.", "Export policies control which routes are advertised; they are not applied to interfaces to check packets.", "A discard static route drops traffic sent to a destination, not traffic from a bad source.", null],
  "jj92": ["metric is a non-terminating action, so the route is not rejected at this point.", "The route is changed (its metric is set), and it is not accepted until a later terminating action or the default policy.", null, "Only a terminating action (accept or reject) stops evaluation; a modifier does not end the chain."]
});
