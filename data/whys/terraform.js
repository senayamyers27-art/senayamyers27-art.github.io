/* Terraform Associate: why each wrong option is wrong. */
CertHub.addWhys("terraform", {
  "tf1": ["IaC changes how resources are defined, not what the cloud provider charges for them, so prices stay the same.", "Network throughput depends on instance types and network design, not on whether the environment was built by code or by hand.", null, "IaC does not automatically swap operating systems; the OS is whatever image the code specifies, and the problem here is inconsistency between builds."],
  "tf2": [null, "Writing each API call in a fixed order is the imperative style; in Terraform you declare resources and it builds the call order from the dependency graph.", "Terraform does not record or replay console clicks; it reads configuration files and calls provider APIs to reach the declared state.", "Terraform does not schedule shell scripts; that describes a scripted, imperative approach rather than declaring the desired end state."],
  "tf3": ["Terraform has no feature that translates AWS resources into Azure ones; each provider has its own resource types that you write separately.", "There is one Terraform CLI for every platform; support for each cloud comes from provider plugins, not separate editions.", "A single state file can track resources from many providers, such as AWS, Azure and Cloudflare in one configuration.", null],
  "tf4": ["Terraform only acts on differences; with no changes to code or infrastructure it has no reason to recreate anything.", null, "Terraform tracks each resource in state and knows it already exists, so a second apply does not create duplicates.", "State is not deleted on apply; Terraform reads and updates it, and with no changes it stays effectively the same."],
  "tf5": ["Editing config files inside running VMs is in-guest configuration, which is the job of configuration management tools like Ansible or Chef.", null, "Nightly OS package patching on long-lived servers is configuration management work, and it also runs against Terraform's immutable-infrastructure approach.", "Log collection and alerting is a monitoring or observability function, not something Terraform does (though it can provision the monitoring tools)."],
  "tf6": ["Broad console admin rights let people change infrastructure directly, bypassing any review of the change.", "-auto-approve skips the approval prompt, and running from laptops has no shared review step, so this removes review rather than adding it.", "Documenting after the fact means the change already happened unreviewed; the requirement is review before the change.", null],
  "tf7": [null, "Terraform has no built-in SaaS connector; SaaS services are supported through provider plugins like any other platform.", "There is no SaaS-only Terraform edition; the same CLI manages any service that has a provider.", "State records managed objects and their attributes; it cannot perform manual steps or make Terraform able to talk to a new service."],
  "tf8": ["~> 5.40 only lets the minor and patch parts increase, so it excludes the next major version 6.0.0.", "5.39.0 is lower than the 5.40 minimum that ~> 5.40 sets.", null, "4.67.0 is an earlier major version and is below the 5.40 minimum."],
  "tf9": ["The .terraform directory holds downloaded plugins and modules for one machine and should be ignored by Git; it does not pin versions for others.", "Clearing the plugin cache just forces a fresh download, which would still pick the newest matching version without a lock file.", "TF_LOG only controls log verbosity and has no effect on which provider version is chosen.", null],
  "tf10": ["terraform validate only checks configuration syntax and consistency; it never downloads or changes provider versions.", "A refresh-only apply updates state to match real infrastructure and does not touch providers or the lock file.", null, "Deleting the state file loses Terraform's record of your resources and does nothing to the lock file's provider versions."],
  "tf11": ["That describes the state file, which records resource attributes; providers are the plugins that manage those resources.", null, "A reusable folder of resources with input variables is a module, not a provider.", "The company or service that stores remote state is a backend; a provider is a plugin that talks to a platform's API."],
  "tf12": [null, "region is an ordinary provider or resource argument that expects a string; it cannot select a provider configuration.", "The provider meta-argument takes an unquoted reference in the form aws.west, not a quoted string like \"aws-west\".", "alias is set only in the provider block; a resource chooses a configuration with the provider meta-argument, not an alias argument."],
  "tf13": ["Provider source addresses default to the public Terraform Registry, not GitHub, even though many providers are developed there.", "releases.hashicorp.com hosts binary downloads, but it is not the default hostname in a provider source address.", null, "app.terraform.io is HCP Terraform's hostname, used for private registry providers only when written out explicitly."],
  "tf14": [null, "Mapping configuration to real object IDs is a core purpose of state, so it cannot be the exception.", "Tracking metadata such as dependencies (for correct destroy ordering) is one of state's documented purposes.", "Caching resource attributes to speed up plans on large infrastructures is one of state's documented purposes."],
  "tf15": ["Terraform redacts sensitive values in plan output, showing (sensitive value) instead of the password.", "terraform fmt only reformats configuration files and prints file names; it does not output variable values.", "sensitive = true only hides values in CLI output; it does not encrypt them, and they remain readable in state.", null],
  "tf16": ["Terraform never upgrades itself; you must install a CLI version that satisfies the constraint.", null, "required_version constrains the Terraform CLI itself; provider versions are set in required_providers.", "A version outside the required_version range is a hard error, not a warning, and Terraform will not continue the run."],
  "tf17": ["Partner providers are written and maintained by third-party technology companies, not by HashiCorp.", null, "Community providers are published by individuals or groups in the community, not maintained by HashiCorp.", "Archived marks providers that are no longer maintained; it is not the tier for providers HashiCorp actively owns."],
  "tf18": ["apply also needs initialized providers and a backend, so it cannot run before init either.", "terraform refresh (deprecated) also needs an initialized directory, and it does not install providers or modules.", "fmt can reformat files without init, but it does not prepare the directory, so plan would still fail.", null],
  "tf19": [null, "Write is where you author configuration; it does not compare anything against real infrastructure or show proposed actions.", "init prepares the working directory by installing providers and modules; it does not calculate any changes.", "validate only checks syntax and internal consistency and never shows what will be created, changed or destroyed."],
  "tf20": ["validate checks that the configuration is correct, not that it is formatted; -json and -no-color only change the output format.", "plan -lock=false runs a plan without state locking; it does not check formatting and is unsafe to use by default.", null, "init -reconfigure resets backend configuration and has nothing to do with file formatting."],
  "tf21": ["validate does not contact provider APIs, so it cannot test credentials or permissions; plan or apply would reveal those problems.", "validate does not query the cloud account, so it cannot tell whether resources exist.", "Comparing state with reality is what a refresh during plan (or plan -refresh-only) does; validate does not read remote state.", null],
  "tf22": ["A separate apply computes a fresh plan, which may differ from the one reviewed if code or infrastructure changed in between.", "validate produces no plan to review, and a plain apply creates a new plan at apply time.", null, "terraform refresh only updates state and is deprecated; the following apply still builds an unreviewed new plan."],
  "tf23": ["An in-place update is shown with ~; -/+ means the object cannot be updated in place and must be replaced.", null, "Data source reads are shown with <=, not -/+.", "Imports appear as an import action in the plan; -/+ shows destroy-then-create replacement."],
  "tf24": [null, "terraform taint still works in some versions but is deprecated, and it marks the resource in state without showing the change in a reviewable plan first.", "state rm makes Terraform forget the container while leaving it running; the next apply would try to create a duplicate rather than replace it.", "destroy -auto-approve removes every resource in the state, not just the one container, and it does not recreate anything."],
  "tf25": ["terraform destroy has no -check option; the way to preview a destroy is plan -destroy.", "validate has no -destroy option and never produces a plan of any kind.", null, "terraform show displays existing state or a saved plan; it has no -destroy option to build a destroy preview."],
  "tf26": [null, "fmt only reformats files; it does not download modules.", "validate itself needs modules installed and reports the same missing-module problem rather than fixing it.", "terraform output reads output values from state and cannot install modules."],
  "tf27": ["init -upgrade updates providers and modules to the newest allowed versions; it does not change how the backend is configured.", "A refresh-only apply updates state from real infrastructure; it does not switch backends.", "validate has no -backend option, and validate never configures backends.", null],
  "tf28": ["Without a saved plan, apply always shows the plan first and asks for approval unless -auto-approve is set.", null, "apply never runs terraform fmt; formatting is a separate command.", "apply only makes the changes in the plan; it does not destroy all resources first."],
  "tf29": ["+ means a resource will be created, not updated.", null, "- means a resource will be destroyed, not updated.", "<= marks a data source being read during the run, not a managed resource update."],
  "tf30": ["There is no -tfvars option on terraform plan; variable files are passed with -var-file.", "-tfvars-file is not a real option; the correct name is -var-file.", "-input controls whether Terraform prompts for missing variables (true or false); it does not load a file.", null],
  "tf31": [null, "destroy never edits .tf files; the configuration stays the same and can be applied again later.", "destroy removes every resource in the state, not only drifted ones; handling drift is done with plan or -refresh-only.", "destroy does not touch .terraform.lock.hcl; provider selections stay recorded there."],
  "tf32": ["fmt never renames resources; renaming would change addresses and needs moved blocks, not formatting.", "fmt does not rewrite deprecated arguments; it only changes whitespace and layout, so upgrades are manual.", null, "fmt does not touch provider versions; those change only with terraform init -upgrade."],
  "tf33": ["-refresh-only is the opposite: it plans only updates to state from real infrastructure, which requires checking reality.", "-lock=false only disables state locking; Terraform still refreshes against real infrastructure.", "-parallelism=1 limits concurrent operations, which slows a run down and still performs the refresh.", null],
  "tf34": ["-replace forces recreation of a resource; it does not limit the run's scope and is not meant for all normal changes.", "-refresh-only updates state from reality and makes no infrastructure changes, so it cannot apply a fix.", null, "-lock=false removes protection against concurrent writes and should never be a default; it does not limit scope either."],
  "tf35": ["Without the data. prefix, docker_image.nginx refers to a managed resource of that type, which is not declared here.", null, "Brackets with a key are for count or for_each instances; this data source has neither, and the address still needs data.TYPE.NAME.", "var. refers to input variables, not data sources, so var.docker_image.nginx.id would point to an undeclared variable."],
  "tf36": [null, "This reverses the roles: resource blocks manage objects and data blocks only read.", "Data blocks can be used in any module, including the root module.", "Resources commonly reference data sources, for example using a looked-up image ID or AMI."],
  "tf37": ["count.index starts at 0, not 1, so there is no cfg-3.txt.", "The ${count.index} template is replaced with the index number, so the literal word count never appears in a file name.", null, "count = 3 creates three instances with indexes 0 to 2, not a single file named after the count."],
  "tf38": [null, "Numeric indexes like 0 belong to count; with for_each over a set, each.key is the element itself.", "each.value is not null for sets; it equals each.key, the set element.", "each.key is the element (\"dev\"), not the literal word \"key\"."],
  "tf39": ["depends_on only changes ordering; count still addresses servers by index, so removing one still shifts the rest.", "create_before_destroy changes the order of replacement but still replaces the shifted servers.", "-target only narrows one run; the shifted indexes remain and later full applies still change those servers.", null],
  "tf40": ["The default is the lowest-precedence source and is used only if nothing else sets the variable.", null, "TF_VAR_ environment variables are low in precedence, below tfvars files and -var options.", "terraform.tfvars outranks environment variables but is overridden by -var on the command line."],
  "tf41": ["A map with a named key is not a list and cannot be converted to list(number).", null, "A single string is not a list; Terraform will not split \"80,443\" into numbers automatically.", "The strings \"http\" and \"https\" cannot be converted to numbers, so the list fails the type check."],
  "tf42": ["map(string) needs every value to be a string, but port is a number and public is a bool (Terraform would convert them and lose the types).", "list(any) is an ordered list of elements accessed by index, not a set of named attributes.", "A set holds unordered unique values of one type and has no named attributes.", null],
  "tf43": [null, "A map would need curly braces and a key => value form; square brackets produce a list.", "A for expression returns a collection, not a joined string; join() would be needed for \"AB\".", "A for expression transforms elements rather than appending new ones, so the originals are not included."],
  "tf44": ["A counted resource is a list of instances, so aws_instance.web.id without an index is an error.", "Indexes start at 0, so [3] is out of range for three instances, and it would return only one ID.", null, "The splat goes after the resource name (aws_instance.web[*]); aws_instance[*].web is not valid syntax."],
  "tf45": ["No depends_on is needed; the reference itself creates the dependency.", "References work across resources in one module and do not force them into a particular module.", "The image can be a managed resource; referencing docker_image.app refers to a resource, not a data source.", null],
  "tf46": ["prevent_destroy blocks deletion of a resource and has no effect on creation order.", "count only controls how many instances exist; it creates no dependency between the policy and the server.", null, "A data block would only read the policy, and the server still would not reference it, so no ordering is created."],
  "tf47": ["ignore_changes = all stops Terraform updating the resource, so the certificate could never be rotated, and it does not change replacement order.", null, "depends_on sets creation order between resources; it does not make the replacement certificate appear before the old one is destroyed.", "prevent_destroy would make any plan that destroys the old certificate fail, blocking the replacement."],
  "tf48": [null, "-refresh=false skips reading real values, which hides all drift and can produce wrong plans; it is not a fix for one attribute.", "Removing the resource from state means Terraform no longer manages the group at all.", "depends_on only affects ordering; it does not stop Terraform planning to reset desired_capacity."],
  "tf49": ["Provider blocks do not accept precondition blocks; custom conditions belong in resource lifecycle blocks, outputs and variables.", "A check block assertion only produces a warning, so it would not make the plan fail.", null, "sensitive = true on an output only hides its value; it does not validate var.env."],
  "tf50": [null, "A failing postcondition raises an error and stops the run, rather than warning.", "Variable validation checks input values, not a website's response, and a failure is an error that stops the plan.", "prevent_destroy blocks destroying a resource; it has nothing to do with health checks."],
  "tf51": ["The terraform block and required_providers configure versions and providers; custom conditions cannot go there.", "Provider blocks configure the provider connection and do not accept postconditions.", "locals blocks only define named values and cannot contain precondition or postcondition blocks.", null],
  "tf52": ["sensitive only hides the value in CLI output; it is still written to the plan file and state.", null, "Local values are ordinary values, so the password would still be stored wherever it is used, including state.", "Outputs are saved in state, so putting the password in one makes it more exposed, not less."],
  "tf53": ["Committing keys in terraform.tfvars puts long-lived secrets into Git history where anyone with repo access can read them.", null, "A README is a public document; secrets in it are exposed to everyone who can read the repo.", "sensitive only hides values in output; the default still sits in plain text in variables.tf in version control."],
  "tf54": ["terraform show prints state or a saved plan; it cannot evaluate an expression you type.", "terraform graph outputs a dependency graph in DOT format and does not evaluate functions.", "terraform output -json prints existing output values from state and cannot evaluate new expressions.", null],
  "tf55": [null, "Without ./ or ../, Terraform treats \"modules/network\" as a registry address, not a local path, and init fails.", "local:: is not a valid source prefix; local paths are written as ./ or ../ paths.", "file: is not a supported module source scheme; local modules use relative paths starting with ./ or ../."],
  "tf56": ["A GitHub source must be written as github.com/... or with git::; a bare NAMESPACE/NAME/PROVIDER is a registry address.", "A local folder would need a ./ or ../ prefix; without it the string is not read as a path.", null, "Modules are never loaded from state; HCP Terraform's private registry would need the app.terraform.io hostname in the source."],
  "tf57": ["Local path modules are read directly from disk and have no versions, so version cannot be used.", "Plain HTTP archive sources do not support the version argument.", "Git sources pin with ?ref= in the URL; the version argument is not allowed for them.", null],
  "tf58": ["TF_VAR_ environment variables only set root module variables, and module_name is not how child inputs are named.", "Child modules do not inherit root variables; a root variable of the same name is not passed automatically.", null, "module.child.var.name is not valid syntax; the root can only read a child's outputs, and inputs are set in the module block."],
  "tf59": ["Resources inside a child module are private to it and cannot be referenced from the root.", null, "A data source would look up the container by a separate API call, not read it through the module boundary, and is not required.", "depends_on only affects ordering and does not expose any value to the root module."],
  "tf60": [null, "The provider meta-argument takes an unquoted reference, and child resources cannot see the root's aws.west unless it is passed in.", "region is not a module block meta-argument; unless the child declares a region variable this is an error, and it does not pass a provider.", "Only default (unaliased) provider configurations are inherited automatically; aliased ones must be passed with providers."],
  "tf61": ["version only works with registry sources; Git sources reject it.", "Terraform does not select Git tags with a # fragment; a Git ref must be given with ?ref= in a git:: source.", null, "The // syntax selects a subdirectory inside the repository, not a tag."],
  "tf62": [null, "validate only checks configuration and never downloads modules.", "fmt only reformats files and never downloads modules.", "plan uses already installed modules; -refresh=false only skips refreshing state and downloads nothing."],
  "tf63": ["each.key is only available inside the module block itself; the address uses the literal key, module.bucket[\"logs\"].", "Module instances are not renamed after keys; the module name stays bucket and the key goes in brackets.", "Numeric indexes are for count; for_each instances are addressed by their string keys.", null],
  "tf64": ["Registry popularity has nothing to do with the root module; the root is your working directory's configuration.", null, "A module block calls a child module; the first one is not the root.", "The root module can contain any resources, data sources and module calls, not only provider blocks."],
  "tf65": [".terraform.lock.hcl records provider versions and checksums; it never holds resource state.", null, "HCP Terraform is used only when a cloud block or remote backend is configured; it is never picked automatically.", "Terraform keeps its own state; the cloud provider's account holds the real resources, not Terraform's state."],
  "tf66": [".terraform.lock.hcl pins provider versions; it does not stop two applies from writing state at once.", "terraform fmt only formats files and has no effect on concurrent state writes.", "Provider aliases let you use several provider configurations; they have nothing to do with protecting state.", null],
  "tf67": [null, "Deleting state makes Terraform forget every resource, so the next run would try to create duplicates.", "init -upgrade updates providers and modules and does not release a state lock.", "-lock=true is already the default; it tries to take the lock and fails again because the stale lock is still there."],
  "tf68": ["Even with a default, variables cannot be referenced in a backend block because backends are set up before variables are evaluated.", "Terraform does not defer backend settings to apply; the backend must be fully configured at init.", null, "Terraform does not silently fall back to local state; it reports an error for the invalid backend configuration."],
  "tf69": ["Backend blocks cannot contain expressions such as terraform.workspace, so it cannot be interpolated into the bucket name.", "TF_VAR_ variables set input variables, which backend blocks cannot use.", "for_each is a meta-argument for resources and modules; backend blocks do not support it.", null],
  "tf70": ["state push overwrites remote state with a local file by hand; it is a low-level recovery tool, not the normal way to move to a new backend.", "A refresh-only apply updates state from real infrastructure and does not move state between backends.", null, "Creating a workspace starts a new, empty state; it does not copy existing local state to the remote backend."],
  "tf71": ["validate checks configuration only and never looks at the real container.", null, "fmt -diff shows formatting changes to .tf files, not drift in real infrastructure.", "state rm makes Terraform forget the container instead of showing what changed, and it changes state."],
  "tf72": [null, "Destroying and rebuilding causes downtime and throws away the manual value the team decided is correct.", "Removing it from state stops Terraform managing the resource instead of recording the correct value.", "Drift does not fix itself; the next normal plan would try to revert the tag to the value in code."],
  "tf73": ["Git stores JSON files without any problem; the concern is what the state contains and how it is shared.", "Terraform updates the state file in place (keeping a backup); it does not delete and regenerate it every run.", null, "State files are usually small; size is not the reason to keep them out of Git."],
  "tf74": [null, "terraform.tfstate at the root is used only by the default workspace.", "A .tfvars file holds variable values, not state, and workspaces do not create one.", "The .terraform directory records the currently selected workspace (in an environment file); local workspace state lives in terraform.tfstate.d."],
  "tf75": ["state push uploads a full state file by hand and does not import real objects or generate configuration.", "-replace destroys and recreates a resource Terraform already manages; it cannot adopt one it does not track.", "A data block only reads the container; it never adds it to state as a managed resource.", null],
  "tf76": ["output -json prints output values, not the list of resources in state.", null, "terraform providers lists the providers the configuration requires, not resource addresses.", "terraform graph prints a DOT-format dependency graph of the configuration, not a list of state addresses."],
  "tf77": ["depends_on on local_file.a would reference a resource that no longer exists in the configuration, and it does not tell Terraform the object was renamed.", null, "fmt only reformats files; it does not change how Terraform maps the old address to the new one.", "init -upgrade updates providers and modules and does nothing about renamed resources."],
  "tf78": ["destroy -target deletes the record, which is the opposite of leaving it in place.", "Deleting the block without a removed block makes Terraform plan to destroy the record.", "prevent_destroy keeps Terraform managing the record and blocks deletion; it does not stop management.", null],
  "tf79": [null, "TF_VAR_ variables set input variables; TF_VAR_log would only set a variable named log, not logging.", "terraform plan has no -verbose flag; redirecting output captures normal output, not debug logs.", "TF_INPUT=0 disables interactive prompts and has nothing to do with logging."],
  "tf80": ["TF_LOG_CORE sets the log level for Terraform core, the part you want to leave out.", "TF_CLI_ARGS_plan adds extra arguments to terraform plan and does not control logging.", null, "TF_WORKSPACE selects the workspace to use and has nothing to do with logging."],
  "tf81": ["validate only checks configuration and prints validation results, not state or plan contents.", "fmt reformats configuration files and does not read state.", "terraform login stores an HCP Terraform API token and does not display state.", null],
  "tf82": ["VCS-driven runs start from commits and pull requests in a connected repository, not from terraform commands on a laptop.", "API-driven runs are started by tools calling the HCP Terraform API, not from local terraform commands.", null, "\"Agent-only\" is not an HCP Terraform workflow; agents are where runs execute, not a way of starting them."],
  "tf83": ["There is no hcp provider setting for organization and workspace here; connecting to HCP Terraform is done with the cloud block, not a provider.", null, "The local backend stores state on disk and has no organization setting.", "A module block calls a module; it cannot connect the configuration to HCP Terraform."],
  "tf84": [null, "Copying variables by hand into 30 workspaces is error-prone and must be repeated whenever the credentials change.", "tfvars files are committed with code, which puts credentials in the repository, and they must be maintained in every repo.", "Module outputs pass values within one configuration; they cannot share credentials across workspaces."],
  "tf85": ["Projects do not replace workspaces; each workspace in a project still keeps its own separate state.", "Projects do not store provider binaries; providers come from registries, including the private registry.", null, "terraform fmt runs locally and needs no HCP Terraform project."],
  "tf86": [null, "Run triggers start a run in one workspace after another workspace applies; they do not block anything.", "Cost estimation shows the projected cost of a plan but does not check bucket settings or block runs by itself.", "The private registry shares approved modules and providers but does not enforce rules on each run."],
  "tf87": ["Variable sets share variable values across workspaces; they do not start runs when another workspace changes.", "Health assessments check for drift on a schedule; they do not queue runs after another workspace applies.", "Team tokens authenticate API access for a team; they do not link workspaces or start runs.", null],
  "tf88": ["fmt -check checks file formatting only and never looks at real infrastructure.", null, "The private registry shares modules and providers and does not check for drift.", "Speculative plans preview pull request changes; they run on code changes, not on a schedule to detect drift."],
  "tf89": ["VCS-driven workspaces do not apply pull requests; applies happen only after changes are merged into the tracked branch.", null, "Opening a pull request never deletes workspace state.", "HCP Terraform does not lock the repository; it only reports the plan result back on the pull request."],
  "tf90": ["terraform init has no -token option; credentials are stored with terraform login or configured in the CLI config file.", "workspace select switches to a different workspace and does not authenticate.", "providers lock updates the dependency lock file with provider checksums and does not authenticate to HCP Terraform.", null],
});
