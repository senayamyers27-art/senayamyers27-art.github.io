CertHub.addWhys("java-se", {
  "js1": ["This assumes s.concat(\" SE\") changed s, but String is immutable and the returned string was never assigned.", null, "Besides the discarded concat() result, concat adds no space before 25; string concatenation with + inserts nothing extra.", "Every line is legal: concat() can be called and its result ignored, and String + int is ordinary concatenation."],
  "js2": ["x ends at 7, not 6, because both x++ and ++x increment it; and y is 5 + 7, not 11.", "x is 7, but y is 12: ++x increments x from 6 to 7 before yielding 7, so 5 + 7 = 12.", "y is 12, but x was incremented twice (once by x++ and once by ++x), so it is 7, not 6.", null],
  "js3": [null, "The dash would be first only with insert(0, \"-\"); insert(1, ...) places it after the first character.", "This puts the dash at index 2; insert(1, \"-\") inserts after just one character of \"edcba\".", "This ignores reverse(), which changes the StringBuilder in place, and puts the dash at the end instead of index 1."],
  "js4": ["A trailing backslash is a line-continuation escape, so no newline is inserted between the two lines.", "The four leading spaces are incidental indentation shared by all content lines, so the compiler strips them.", null, "The backslash is not kept as a character; at the end of a line it is an escape that joins the lines."],
  "js5": ["This would be adding 31 days (plusDays(31) lands on 2024-03-02); plusMonths adjusts the day instead of overflowing.", null, "plusMonths never rolls over into the next month; it clamps the day to the last valid day of February.", "plusMonths does not throw for a day that does not exist in the target month; it resolves to the month's last valid day."],
  "js6": [null, "plusHours(24) adds exact elapsed time, so on a 23- or 25-hour DST day the result lands at 10:00 or 08:00 local time.", "A Duration is exact machine time, so adding 24 hours shifts the local time by an hour across a DST transition.", "86,400 seconds is exactly 24 hours of elapsed time, which drifts off 09:00 local time when the day is 23 or 25 hours long."],
  "js7": ["Compound assignment like b += 5 includes an implicit cast back to byte, so line 2 compiles.", null, "Assigning a byte to a short is a widening primitive conversion, which needs no cast.", "Line 3 fails: b * 2 is promoted to int and cannot be assigned to a byte without an explicit cast."],
  "js8": ["128 is outside the default Integer cache of -128 to 127, so c and d are distinct objects and == is false.", "127 is inside the Integer cache, so a and b refer to the same cached object and == is true.", "This reverses the caching behavior: the small value 127 is cached, and 128 is not.", null],
  "js9": ["Math.round does not round half away from zero; floor(-2.5 + 0.5) is -2, not -3.", "Both halves are wrong: -2.5 rounds up to -2, and 2.5 rounds up to 3, since Math.round is floor(x + 0.5).", "2.5 + 0.5 is 3.0, whose floor is 3, so Math.round(2.5) is 3, not 2 (Java does not use banker's rounding here).", null],
  "js10": ["abc would need a String on the left; two chars added together are promoted to int before \"c\" is reached.", null, "Neither char becomes a code number followed by a letter; 'a' + 'b' is computed first as 97 + 98.", "Adding two chars is legal numeric promotion to int, and int + String is legal concatenation."],
  "js11": ["substring(1, 3) excludes index 3, so it gives \"el\", and the first 'l' is at index 2, not 3.", "The end index of substring is exclusive, so substring(1, 3) is \"el\", not \"ell\".", "\"el\" is right, but indexOf('l') returns the first occurrence, which is index 2, not 3.", null],
  "js12": ["42 matches the guarded case first because 42 > 40, so the unguarded Integer case is never reached for it.", "default is only used when no earlier pattern matches, and 42 is an Integer that matches the first case.", "A guarded pattern before an unguarded one of the same type is legal ordering, so there is no dominance error.", null],
  "js13": [null, "The compiler rejects dominated case labels outright; it does not compile a switch with an unreachable case.", "Dominance is a compile-time error, not a warning, so the code never compiles.", "MatchException is a runtime error for unmatched values in some exhaustive switches; here the code fails at compile time first."],
  "js14": ["Without a break after case 2, execution falls through into case 3 and also prints C.", "The break at the end of case 3 stops execution, so default is never reached.", null, "Execution jumps straight to the matching label case 2; case 1's code before it is not run."],
  "js15": ["When i is 2, break outer ends both loops before anything prints, so no 2x pairs appear.", null, "continue outer runs when j == 2, before the print, so pairs ending in 2 never print.", "continue outer only skips to the next i; it does not end the outer loop, so the i = 1 pairs are printed too."],
  "js16": ["A switch expression over an enum is exhaustive when all constants are covered, so no default is required.", null, "Switch expressions support enums without a default when every constant has a case; there is no such restriction.", "Arrow-form switch expressions never fall through; an uncovered new constant makes the switch non-exhaustive and it no longer compiles."],
  "js17": ["A do-while runs its body before checking the condition, so i is incremented to 11 at least once.", null, "Nothing sets i to 5; the condition i < 5 is only a test and it is false after the first pass.", "After one pass i is 11 and i < 5 is false, so the loop ends immediately."],
  "js18": ["The block calls toUpperCase() before yielding, so the result is uppercase.", "yield is exactly how a block body in a switch expression returns a value, so it compiles.", "3 is not listed in case 1, 7, so the default block handles it.", null],
  "js19": ["Records have no setters and only a canonical constructor by default; a no-arg constructor exists only if the record has no components or you declare one that delegates.", "A record's generated equals() compares component values, not object identity.", "Records implicitly extend java.lang.Record and are final, so they cannot extend another class.", null],
  "js20": ["get() is overridden and dispatched on the runtime type B, so it returns B's field value \"B\", not \"A\".", "a.name is resolved from the reference type A, because fields are hidden, not overridden, so it gives \"A\".", "This swaps the rules: the field access uses the compile-time type (A) and the method call uses the runtime type (B).", null],
  "js21": [null, "Static methods are hidden, not overridden, so the runtime type C does not affect which id() runs.", "Calling a static method through an instance reference is legal Java; at most it produces a compiler warning.", "Only one method is called; hiding does not chain the parent and child versions."],
  "js22": ["Statements before super(...) in the prologue run first, so \"check \" prints before the Base constructor.", "Java 25 flexible constructor bodies (JEP 513) allow statements before super(...) as long as they don't use this.", null, "The argument passed is v * 2, so Base receives 6, not 3."],
  "js23": ["Declaring local variables to compute the super argument is a main use of the constructor prologue and is allowed.", "Validating arguments and throwing before super(...) is allowed because it does not touch the object being built.", null, "Java 25 specifically allows assigning fields that have no initializer before super(...), so this is permitted."],
  "js24": ["Records can implement interfaces, including sealed ones, and are implicitly final, which satisfies the sealed rules.", null, "Simple names are fine in a permits clause when the types are in scope (same file or package).", "A sealed interface can permit any number of subtypes listed in its permits clause."],
  "js25": [null, "Java does not supply a default value for an unmatched case; a pattern switch with no default must be exhaustive or fail to compile.", "No runtime fallback happens because the non-exhaustive switch is caught by the compiler first.", "Changing the permits list is legal on its own; the error appears in the switch that no longer covers every subtype."],
  "js26": [null, "The override calls both X.super.hi() and Y.super.hi() and concatenates them, so it is not just X.", "Z resolves the default-method conflict by overriding hi(), and Interface.super.method() is valid syntax for this.", "The concatenation order is X first, then Y, so the result is XY."],
  "js27": [null, "Flow scoping puts s in scope after the if, because the method returns whenever o is not a String.", "s is \"text\", whose length is 4; nothing resets it to empty.", "instanceof checks the type before binding, so no unchecked cast happens and no ClassCastException can occur."],
  "js28": ["This uses M's field value 2 instead of its ordinal 1; ordinal() is the zero-based position.", "13 would require string concatenation, but both operands are ints, so + is numeric addition.", null, "This would be ordinal 0 plus 3; M is the second constant, so its ordinal is 1."],
  "js29": ["The static initializer runs only once, when the class is first initialized, not per object.", null, "The static block runs before the first instance initializer, so S comes first.", "Instance initializers run before the constructor body, so I prints before C, and S appears only once."],
  "js30": ["Boxing to Integer is only considered when no widening match exists, and int widens to long.", "Object also needs boxing first, and widening to long is chosen before any boxing.", "The phases of overload resolution pick a single most-specific method, widening to long, so there is no ambiguity.", null],
  "js31": ["public final stops reassignment but not mutation; callers can still change the List's contents.", "Having private setters and a default constructor doesn't stop mutation of the list and leaves fields assignable inside the class.", null, "volatile controls cross-thread visibility, not mutability; the list can still be modified."],
  "js32": ["new Outer.Inner() only works for a static nested class; an inner class needs an enclosing instance.", "The outer must be an instance, not the class name; Outer.new Inner() is not valid syntax.", "An inner class is not created by passing the outer object as a constructor argument; that syntax does not compile.", null],
  "js33": ["obj1 lost its only reference when a was reassigned to obj2, so one object is eligible.", null, "obj2 is still referenced by a, so it is not eligible; setting b to null did not remove all references.", "Eligibility depends only on reachability, not on whether the garbage collector has run."],
  "js34": ["Abstract classes can declare constructors, which subclass constructors call through super(...).", "An abstract class can mix abstract and concrete methods, and can even have no abstract methods.", "Java allows only single class inheritance, abstract or not; a class extends at most one class.", null],
  "js35": [null, "Records use square brackets in toString, not parentheses (the parenthesized form is a Lombok style).", "Records override toString, so the Object-style class@hash form is never used.", "Curly-brace key=value output is Map.toString; a record's toString starts with its name and uses brackets."],
  "js36": ["private limits access to the declaring class, hiding it from the package and subclasses.", "Package access excludes subclasses in other packages, which this requirement needs.", null, "public would also expose the method to unrelated classes in other packages, which is not wanted."],
  "js37": ["null has no type the compiler can infer, so var n = null is rejected.", "An array initializer needs an explicit array type such as int[]; var cannot infer it from braces alone.", "var requires an initializer in the same statement to infer the type, so a bare declaration fails.", null],
  "js38": [null, "Interface methods cannot be protected; they can only be public or private.", "A public static method is visible to every class, so it does not hide the helper from implementers.", "Interface methods cannot be final, and a default method would still be visible and overridable."],
  "js39": ["println cannot print 1 until f() returns, and finally runs before the return completes, so F comes first.", "finally always runs, so F is printed as well.", "A try with only a finally block and a return is legal Java.", null],
  "js40": ["The FileReader(String) constructor declares the checked FileNotFoundException.", null, "A multi-catch does not need a finally block; finally is always optional when a catch exists.", "A multi-catch parameter is already implicitly final, so no explicit final is needed."],
  "js41": ["Resources are closed in reverse order of declaration, so B closes before A.", "Resources are closed right after the body, before finally runs, not after it.", "Resources are closed after the body runs, not before it.", null],
  "js42": ["The exception from the body stays primary; the close() exception is not wrapped around it as a cause.", "Java delivers one exception to the caller; the second is attached to the first rather than thrown separately.", null, "try-with-resources keeps the first exception and adds the close() failure as suppressed, so nothing is lost."],
  "js43": ["Exception is broader than IOException, and an override cannot throw broader checked exceptions.", "Throwable is broader than IOException, so this override would widen the checked exceptions.", "SQLException is a new checked exception not covered by IOException, which overrides may not add.", null],
  "js44": ["read() on a Reader does declare IOException, so that is not the problem.", null, "A try block can have many catch blocks; the problem is their order.", "Exception is a checked exception class and can certainly be caught."],
  "js45": [null, "Subclasses of RuntimeException are unchecked, so the compiler would not force callers to handle it.", "Error is for serious JVM problems that applications are not expected to handle, and it is unchecked.", "Throwable is a class, not an interface, so it cannot be implemented."],
  "js46": ["List.of returns an unmodifiable list, so set cannot change it.", null, "Index 0 exists in a three-element list, so the index is not the problem; the list rejects all changes.", "set is declared in the List interface, so it compiles; the failure happens only at runtime."],
  "js47": ["The list from Arrays.asList is backed by the array, so set(0, \"z\") writes through to arr[0].", "Nothing sets the element to null; the list write replaces \"a\" with \"z\" in the array.", "Arrays.asList supports set because it does not change the size; only add and remove throw.", null],
  "js48": ["TreeSet sorts its elements and does not keep insertion order.", "This is reverse order; TreeSet uses ascending natural order unless given another Comparator.", "Natural String order compares character codes, so uppercase 'A' sorts before lowercase letters.", null],
  "js49": ["merge adds to the existing value with Integer::sum rather than replacing it with 1.", "5 is the number of words in total; only three of them are \"a\".", "\"a\" is in the map, so get returns its count, not null.", null],
  "js50": ["pop removes from the front, where push put 2, not the 3 that offer added at the back.", "The front element is 2 (the last push) and peekLast is 3, so 1 3 has both ends wrong.", "peekLast looks at the tail, where offer(3) added 3, not 1.", null],
  "js51": [null, "remove(1) with an int argument uses the index overload, removing 20, not the value 1.", "remove(1) removes the element at index 1 (20), and the second call removes 10, so 10 does not remain.", "Only two of the three elements are removed, so 30 remains."],
  "js52": ["reversed() sorts by length descending and adds no alphabetical tie-break.", "Natural order sorts alphabetically first, so length would only break ties between equal strings.", null, "Sorting by toString is alphabetical order first, so length barely matters."],
  "js53": [null, "With ? extends Number the list may really be a List<Integer>, so adding a Double is not type-safe and is rejected.", "The list may be a List<Double>, so even an Integer cannot be added; only null can be added.", "Object is not a Number, and nothing except null can be added to a ? extends list."],
  "js54": ["reversed().getFirst() is c and getFirst() is z, so the order printed is c then z.", "addFirst(\"z\") put z at the front, so the list's first element is z, not a.", null, "The reversed view's first element is the last element c, not a."],
  "js55": ["Streams process elements lazily one at a time, and findFirst stops once 2 passes the filter, so 3 and 4 never reach peek.", null, "1 fails the filter, so the stream moves on and peeks 2 before findFirst can succeed.", "1 is peeked before the filter rejects it, so 1 is printed too."],
  "js56": ["No terminal operation is called, so peek never runs and a and b are not printed.", "Nothing runs without a terminal operation, and even if it did, peek sits before map and would print lowercase a and b.", null, "An unused stream pipeline compiles; nothing forces a terminal operation."],
  "js57": ["1 is the count for lengths 1 and 3; two strings (aa and cc) have length 2.", "The list of strings is what groupingBy gives without a downstream collector; counting() gives a Long count instead.", "4 is the total number of strings, not the number with length 2.", null],
  "js58": ["partitioningBy always contains both the true and false keys, even when a partition is empty.", null, "The map is never empty; it always has true and false keys.", "An empty partition maps to an empty list, not null."],
  "js59": ["This ignores the elements; the identity is only the starting value, and 1 through 4 are added to it.", null, "reduce with an identity argument returns T directly, not an Optional.", "This is both wrapped in Optional and ignores the elements, and the identity overload returns a plain value."],
  "js60": ["14 is the sum of squares from 1 to 3, which range(1, 4) would give; rangeClosed includes 4.", null, "10 is 1 + 2 + 3 + 4 without squaring; map squares each value first.", "55 is the sum of squares up to 5, but the range stops at 4."],
  "js61": [null, "A stream cannot be reused after a terminal operation, so it cannot return the count again.", "The stream does not reset to empty; using it again throws an exception.", "The compiler does not track stream use, so the code compiles and fails only at runtime."],
  "js62": [null, "Function<Integer, Integer> takes one argument, but the lambda has two parameters.", "Supplier takes no arguments, so a two-parameter lambda cannot target it.", "UnaryOperator takes one argument, which does not match a two-parameter lambda."],
  "js63": ["forEach takes a Consumer, and a lambda is a valid Consumer.", null, "Changing to Integer would not help; count++ still reassigns a captured local variable.", "An unused lambda parameter is legal Java and causes no compile error."],
  "js64": ["This nested result would come from map, but flatMap flattens the inner lists into one stream.", "flatMap(List::stream) is a valid method reference returning a Stream, so it compiles.", "map(i -> i * 2) doubles each element, so the values are not left as 1, 2, 3.", null],
  "js65": [null, "The prefix [ and suffix ] are included because the three-argument joining was used.", "The prefix and suffix wrap the whole result once, not each element.", "The delimiter is exactly \",\" with no space; [a, b, c] is how List.toString formats."],
  "js66": ["windowFixed keeps the final shorter window, so 5 is not dropped.", "Overlapping windows are produced by windowSliding, not windowFixed.", null, "windowFixed produces lists, not sums; summing would need another step such as map."],
  "js67": ["An empty Optional does not produce the text null; orElse supplies \"none\".", "ofNullable accepts null and returns an empty Optional; only Optional.of(null) throws.", "orElse unwraps the Optional, so the printed value is the fallback string, not Optional.empty.", null],
  "js68": ["toMap without a merge function never picks a winner for a duplicate key; it throws.", "toMap does not keep the first value for a duplicate key unless a merge function says so.", null, "The code is type-correct: charAt returns char, which boxes to Character."],
  "js69": [null, "requires names a module you depend on, not a package, and grants no reflective access.", "exports allows compile-time use of public types, which is unwanted here, and does not allow deep reflection on private fields.", "uses declares that a module consumes a service interface; it has nothing to do with reflection."],
  "js70": [null, "exports takes a package in the current module, not the name of another module.", "requires static is an optional compile-time dependency and does not pass readability on to app.", "opens takes a package and controls reflection; it cannot pass a module dependency to other modules."],
  "js71": ["uses belongs in the consumer module that loads implementations with ServiceLoader, not in the provider.", null, "A qualified export shares a package with a named module; it does not register a service implementation.", "requires service is not a real module directive."],
  "js72": ["java.desktop also exports java.awt.List, so the simple name List is ambiguous and not resolved to java.util.", "Nothing gives java.awt.List priority; both on-demand module imports are equal, so the name is ambiguous.", null, "import module declarations work in ordinary class-path code without module-info.java."],
  "js73": [null, "Java 25 allows an instance main method with no parameters and no public static modifiers.", "A compact source file declares an implicit class, so no explicit class declaration is needed.", "Compact source files automatically import module java.base, which includes java.util.List."],
  "js74": [null, "jdeps analyzes dependencies and can list the modules an app needs, but it does not build a runtime image.", "jar packages class files into archives; it cannot create a Java runtime.", "javadoc generates API documentation and has nothing to do with runtimes."],
  "js75": ["A core-sized pool gives only a handful of threads, so most tasks queue while threads sit blocked on the network.", "Parallel streams use the common ForkJoinPool, sized to the CPU cores, and suit CPU-bound work, not blocking I/O.", null, "Platform threads are costly OS threads, and creating 50,000 of them can exhaust memory or OS limits."],
  "js76": ["volatile makes writes visible to other threads but does not make the read-modify-write of ++ atomic.", "yield is just a scheduling hint and does not stop two threads from interleaving an increment.", null, "Thread priority affects scheduling only and gives no atomicity or mutual exclusion."],
  "js77": ["Runnable.run() returns void and cannot throw checked exceptions.", "Supplier.get() cannot throw checked exceptions, and ExecutorService.submit does not take a Supplier.", "Thread is a thread object to run, not a task type that returns a value.", null],
  "js78": ["ArrayList iterators are fail-fast and throw ConcurrentModificationException when another thread changes the list.", "LinkedList is not thread-safe, and its iterators are fail-fast just like ArrayList's.", null, "An unmodifiable wrapper rejects every change, but the listener list still needs occasional updates."],
  "js79": ["The name worker would appear only if start() ran the lambda on thread t.", null, "Thread-0 is the default name of a new thread, which is not the thread running the code; also t was renamed.", "run() is a normal method call, so the lambda does run and print."],
  "js80": [null, "Unlike ThreadLocal, a scoped value is bound only for the duration of run and unbound afterwards.", "ScopedValue has no set method; a binding is immutable for its scope, and you rebind with a nested where().", "Outside a binding, get() throws NoSuchElementException; it does not return null."],
  "js81": [null, "ExecutorService has implemented AutoCloseable since Java 19, so it is valid in try-with-resources.", "Interrupting running tasks is what shutdownNow() does; close() waits for them.", "close() shuts the executor down, so its threads end once the submitted tasks finish."],
  "js82": [null, "Sleeping only changes timing; each thread can still end up waiting forever for the other's lock.", "volatile controls visibility of fields and has nothing to do with lock acquisition.", "Priority is only a scheduling hint and cannot break a cycle of threads waiting for each other's locks."],
  "js83": ["The .. segment removes b rather than adding a level, so the result is not under /a/b.", null, "The .. only goes up one level, from /a/b to /a, not to the root.", "This is the result of resolve alone; normalize removes the b/.. part."],
  "js84": ["z/w would be relative to /x; starting from /x/y, you first need .. to go up one level.", "relativize returns a relative path, not the absolute target.", null, "Only one .. is needed, because /x/y and /x/z/w share the /x parent."],
  "js85": ["readString would load the whole file into memory and skips the real issue: an unclosed Files.lines stream holds a file handle.", null, "System.gc() does not reliably close file handles; resources must be closed explicitly.", "A parallel stream does not close the file either, and it adds overhead."],
  "js86": ["transient fields are skipped during serialization, so the original value is not restored.", "Deserialization gives a transient reference field its default value, null, not an empty string.", null, "Java serialization does not encrypt anything; transient fields are not written at all."],
  "js87": ["Files.list returns only the direct children of one directory and does not recurse.", null, "Files.lines reads the lines of one file, not the entries of a directory tree.", "Files.readAllLines reads all lines of one file into a List; it does not list files."],
  "js88": ["Creating all missing parents is what Files.createDirectories does, not createDirectory.", null, "The absolute path is used as given; it is not re-rooted in the current directory.", "Files.createDirectory reports failure by throwing an exception, not by returning a boolean (it returns the Path)."],
  "js89": ["The base bundle is only the parent used for keys missing from Messages_fr, not the first file searched.", "The default locale is used only if no bundle is found for fr_CA or fr, and Messages_fr exists.", null, "Fallback finds Messages_fr and the base bundle, so the lookup does not fail."],
  "js90": ["The US currency instance uses the $ symbol before the number, not the ISO code after it.", "The currency format adds grouping separators and always shows two decimal places for dollars.", "The default Locale.US currency format shows the symbol $, not the ISO code USD.", null],
  "js91": [null, "MMM is a text month abbreviation; numeric 09 would need the pattern MM.", "The full month name needs MMMM; MMM gives the short form.", "The pattern puts the day first, so the month cannot appear first."],
  "js92": ["Compact formatting defaults to zero fraction digits, so 1.2M would need setMaximumFractionDigits(1).", "The spelled-out form comes from Style.LONG; SHORT uses the M suffix.", null, "Compact format picks the largest fitting unit, so millions use M rather than K with grouping."]
});
