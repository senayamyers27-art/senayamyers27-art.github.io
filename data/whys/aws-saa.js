/* Wrong-answer explanations for AWS Certified Solutions Architect - Associate (SAA-C03). */
CertHub.addWhys("aws-saa", {
  "saa1": [null, "Access keys in a config file are exactly the long-term credentials the security team banned, and they can leak through backups or source control.", "S3 ACLs grant access to AWS accounts or predefined groups, not to IP addresses, and ACLs are disabled by default on new buckets.", "User data still delivers long-term IAM user keys, and anyone who can view the instance's user data can read them."],
  "saa2": ["Order does not matter in IAM evaluation: every applicable policy is gathered, and an explicit deny anywhere, including an SCP, wins.", null, "A bucket policy allow cannot override an SCP deny; SCPs cap what any principal in the member account can do.", "SCPs apply to every principal in a member account, not only the root user, so the developer is denied too."],
  "saa3": ["Boundaries must be attached to every principal one by one, and account administrators could remove or change them.", "Log file integrity validation only detects altered or deleted log files after the fact; it does not stop anyone from stopping the trail.", "A Config rule reports that logging stopped but does not prevent the action, so a gap in logging would still occur.", null],
  "saa4": ["Separate IAM users in 25 accounts multiply long-term credentials and bypass the corporate IdP the employees already use.", "Shared users break accountability because actions can no longer be tied to one person, and they still bypass the IdP.", null, "Cognito user pools are for application end users, and copying employee passwords into another store defeats federation."],
  "saa5": ["Keys compiled into an app can be extracted by anyone who downloads it, giving them long-term access to the bucket.", "Public write access lets anyone on the internet upload content, and renaming objects afterwards does nothing to stop abuse.", "IAM users are meant for a small number of workforce identities, not millions of consumers, and each would need long-term keys.", null],
  "saa6": ["This describes network ACLs, not security groups: security groups are stateful and evaluate all their rules together.", "Network ACLs are stateless, so return traffic needs its own explicit rule, often for the ephemeral port range.", null, "Security groups attach to network interfaces, not subnets, and they support only allow rules."],
  "saa7": ["Security groups only support allow rules, so there is no way to add a deny for a specific range.", "Removing the internet gateway route would cut off every internet user, not just the scanning range.", null, "Placement groups control physical placement of instances and have no effect on which IP addresses can reach them."],
  "saa8": ["An Elastic IP in a subnet with an internet gateway route makes the server reachable from the internet, which the requirement forbids.", "A default route to the internet gateway turns the private subnets into public ones, allowing inbound connections to instances with public IPs.", null, "Interface endpoints reach AWS services and PrivateLink endpoint services, not arbitrary internet patch repositories."],
  "saa9": ["A bastion open to 0.0.0.0/0 on port 22 exposes SSH to the whole internet and still has to be patched and managed.", "An internet-facing NLB forwarding port 22 publishes SSH to the internet, which is the opposite of no open inbound ports.", "Site-to-Site VPN connects networks with a customer gateway device, not individual laptops, and it would not log shell sessions.", null],
  "saa10": ["A second NAT gateway improves availability, but traffic still goes through NAT, which the policy forbids.", "Transfer Acceleration routes traffic through public CloudFront edge locations to speed long-distance transfers; it does not keep traffic private.", "An internet gateway is the path to the public internet, and private subnets would need NAT or public IPs to use it anyway.", null],
  "saa11": ["SQS and SNS are reached privately with interface endpoints (PrivateLink), not gateway endpoints.", null, "KMS and Secrets Manager only offer interface endpoints, which have hourly and per-GB charges.", "ECR and STS are also interface endpoint services; gateway endpoints exist only for S3 and DynamoDB."],
  "saa12": [null, "The website endpoint is a public HTTP endpoint that requires public read access, so users could reach the bucket directly.", "Random key names are obscurity, not access control; anyone who learns a key can still read the public object.", "Transfer Acceleration speeds up long-distance transfers to S3 and does nothing to restrict who can read the bucket."],
  "saa13": ["Shield Standard protects against network and transport layer DDoS attacks; it does not inspect HTTP requests for SQL injection.", "GuardDuty detects threats and produces findings, but it does not filter HTTP traffic or block IPs at the ALB by itself.", "Security groups filter only by protocol, port and source; they cannot read HTTP request contents such as SQL keywords.", null],
  "saa14": ["Shield Standard is free and automatic but includes neither Shield Response Team access nor DDoS cost protection.", "Firewall Manager centrally manages WAF, Shield and security group policies across accounts; it is not the DDoS protection tier itself.", "Inspector scans workloads for software vulnerabilities and has nothing to do with DDoS mitigation.", null],
  "saa15": ["With SSE-S3, S3 manages the keys, so the company has no key policy to control and cannot disable the key.", "SSE-C keys are supplied by the client on each request and are not managed in KMS, so there is no key policy or KMS usage log.", "The aws/s3 AWS managed key logs usage, but its key policy cannot be edited and it cannot be disabled by the customer.", null],
  "saa16": ["KMS encrypts at most 4 KB directly, so services do not send whole objects to KMS; they encrypt locally with a data key.", "Envelope encryption is about wrapping a data key with a KMS key, not about encrypting data twice with two different algorithms.", "KMS keys never leave KMS unencrypted; only the encrypted data key is stored with the data.", null],
  "saa17": ["Default encryption protects data at rest; it does not stop clients from sending requests over plain HTTP.", null, "Block Public Access prevents public grants but says nothing about whether authorized requests use TLS.", "Object Lock prevents versions from being deleted or overwritten; it does not reject non-TLS requests."],
  "saa18": [null, "A String parameter is not encrypted, and manual monthly updates are neither automatic nor reliable rotation.", "User data and environment variables are readable by anyone who can describe the instance, and nothing rotates the password.", "KMS key rotation changes the key material protecting the storage volume; the database password stays the same."],
  "saa19": ["This reverses the pricing: Parameter Store standard parameters have no extra charge, while Secrets Manager charges per secret.", null, "Secrets Manager always encrypts secrets with KMS, and Parameter Store also uses KMS for SecureString values.", "Both services are read through AWS APIs, so EC2 instances, containers and Lambda functions can all use them."],
  "saa20": [null, "Governance mode can be bypassed by users with the s3:BypassGovernanceRetention permission, so it does not stop everyone.", "Versioning keeps old versions, but users with permission can still delete specific versions, and a lifecycle rule does not lock anything.", "A bucket policy can be edited or removed by an administrator or the root user, so it is not an immutable control."],
  "saa21": [null, "A public object is readable by anyone who finds it during that hour, and someone has to remember to reverse it.", "An IAM user issues AWS credentials to the partner, which the requirement excludes, and it remains active until someone deletes it.", "Turning off Block Public Access on a copy widens exposure and still does not give the partner a way to authenticate."],
  "saa22": ["Audit Manager collects evidence and maps controls for compliance audits; it does not scan S3 objects for personal data.", "Inspector scans EC2, ECR images and Lambda for software vulnerabilities, not the contents of S3 objects.", null, "Detective helps investigate the root cause of security findings; it does not discover sensitive data."],
  "saa23": [null, "Security Hub aggregates and scores findings from GuardDuty and others but does not analyze the raw logs itself.", "Config records resource configuration history and compliance, not network or DNS activity that would reveal crypto-mining.", "Trusted Advisor checks accounts against best practices such as open ports and idle resources; it is not a threat detector."],
  "saa24": ["Inspector network reachability findings are not a configuration history, and quarterly scans are not continuous notification.", null, "Macie discovers sensitive data in S3 buckets and knows nothing about security group rules.", "Application logs do not record security group changes, and manual queries are neither continuous nor a configuration history."],
  "saa25": ["Copying IAM user access keys shares long-term credentials across accounts that can leak and must be rotated by hand.", "Sharing root credentials gives unlimited access to account A and violates every AWS security best practice.", null, "VPC peering provides a network path, but DynamoDB access is authorized by IAM, and the table is reached through a public or endpoint API anyway."],
  "saa26": ["A bucket policy controls access to that bucket's objects; it cannot limit the permissions of IAM roles developers create.", "A Lambda resource-based policy controls who can invoke the function, not what the function's execution role is allowed to do.", null, "A weekly clean-up is detective and leaves days in which an overly powerful role can be used."],
  "saa27": [null, "Browsers do not trust self-signed certificates, and IAM certificate storage does not renew anything automatically.", "Certificates on each instance must be renewed by hand, which is error-prone, and the ALB terminates TLS anyway.", "An ALB does not read certificates from S3, and storing a private key in a bucket is a needless exposure risk."],
  "saa28": ["A larger instance is still one instance in one AZ, so a single failure or AZ outage still takes the application down.", "A cluster placement group packs instances together in one AZ, which increases correlated risk, and snapshots only help after an outage.", null, "Mirroring two volumes on one instance in one AZ does nothing if the instance or the AZ fails."],
  "saa29": ["More instances add capacity, but the broken instances remain in service because nothing marks them unhealthy.", "An NLB does not fix the problem; the Auto Scaling group still ignores load balancer health unless ELB health checks are enabled.", "The cooldown period affects how often scaling policies act, not whether unhealthy instances are detected and replaced.", null],
  "saa30": ["More front-end instances retrying orders only add load to the overwhelmed back end and can still lose orders.", null, "Synchronous writes keep the tiers tightly coupled, so bursts still overload the database and processing path.", "One larger instance raises the ceiling a little but remains a bottleneck and a single point of failure."],
  "saa31": ["Standard queues deliver at least once with best-effort ordering, so duplicates and reordering can happen.", "SNS standard topics do not guarantee ordering or exactly-once delivery, and a subscription per customer does not scale.", null, "Kinesis orders records per shard, but creating a shard per customer is wasteful and consumers must still handle duplicate records."],
  "saa32": ["A zero visibility timeout makes messages reappear immediately, so they are processed repeatedly and duplicates increase.", null, "Long polling reduces empty receive responses and cost; it does nothing about messages that keep failing.", "A 60-second retention period deletes good messages too if they are not processed quickly, silently losing orders."],
  "saa33": ["With one shared queue each message is delivered to only one consumer, so each system would see only some of the orders.", "Sequential API calls couple the web tier to all three systems, so one system being down makes orders fail or go missing.", "Hourly listing of a bucket adds delay and custom tracking of which orders each system has already processed.", null],
  "saa34": ["A single Lambda invocation can run for at most 15 minutes, far short of a five-day approval wait.", "SQS delay queues can postpone delivery by at most 15 minutes, and they provide no retries, branching or workflow state.", null, "A cron job on one instance is a single point of failure and leaves you to build retries and state tracking yourself."],
  "saa35": ["With the EC2 launch type the team still provisions, patches and scales the container instances in the Auto Scaling group.", "Self-managed node groups are EC2 instances the team must patch, upgrade and scale.", "Running Docker directly on EC2 leaves the team responsible for the hosts and for orchestration.", null],
  "saa36": ["A read replica uses asynchronous replication, needs manual promotion, and has its own endpoint, so the connection string would change.", "Restoring from snapshots means downtime and data loss since the last snapshot, and the restored instance gets a new endpoint.", "A larger instance with faster storage is still a single instance in one AZ, so an AZ failure still causes an outage.", null],
  "saa37": ["Synchronous replication for automatic failover describes the Multi-AZ standby, not a read replica, which replicates asynchronously.", "Automated backups can be replicated to another Region, but that is a backup feature, not the purpose of read replicas.", null, "Encryption is set with a KMS key on the instance; replicas are not an encryption mechanism."],
  "saa38": [null, "A Multi-AZ DB cluster protects against AZ failure within one Region; it gives no local reads or failover on other continents.", "Nightly snapshots mean up to a day of data loss and hours to restore, far from a one-second RPO and one-minute failover.", "DynamoDB global tables are a NoSQL key-value store, so a MySQL-compatible application would need to be rewritten."],
  "saa39": [null, "Point-in-time recovery restores a table to an earlier moment; it does not create a writable copy in another Region.", "DAX caches reads for a table in one Region; it does not replicate writes between Regions.", "Items copied into S3 are not a writable DynamoDB table, so players in the other Region would get no low-latency writes."],
  "saa40": ["Streams hold item-level changes for 24 hours for consumers to process, but they provide no restore operation.", "Time to Live deletes items after an expiry timestamp; it cannot bring back overwritten data.", null, "Auto scaling adjusts provisioned read and write capacity and has nothing to do with data recovery."],
  "saa41": ["A 50/50 weighted split sends half of all users to the secondary Region all the time, not only when the primary fails.", "Latency-based routing sends each user to the lowest-latency Region, so both Regions serve traffic continuously.", "Geolocation routing chooses by user location, so it would split users between Regions rather than keeping them on the primary.", null],
  "saa42": ["Failover routing is active-passive and sends all traffic to one record unless its health check fails; it cannot split by percentage.", "Multivalue answer returns up to eight healthy records at random, with no control over the traffic percentage.", null, "Geoproximity routes by the geographic distance between users and resources, adjusted with a bias, not by fixed percentages."],
  "saa43": ["Pilot light keeps a database replicating in another Region all the time, which costs more than this tolerance requires.", null, "Warm standby runs a scaled-down copy of the whole stack continuously, which is far more than a one-day RTO needs.", "Active-active runs full production in two Regions, the most expensive option, for a system that can be down a day."],
  "saa44": ["Backup and restore needs infrastructure rebuilt and data restored, typically taking hours, which misses a 15-minute RTO.", "Pilot light keeps only the data layer live, so servers must be launched and scaled first, which is usually slower than warm standby.", null, "Active-active runs full-size capacity in a second Region, which the company explicitly does not want to pay for."],
  "saa45": ["Restoring from snapshots describes backup and restore; in pilot light the Aurora replica already holds current data.", null, "Full-size servers already serving traffic describes active-active or hot standby, not pilot light, where no servers run.", "Snapshots stored only in the failed Region cannot be copied during its outage, and pilot light relies on the live replica instead."],
  "saa46": ["Custom Lambda code in 12 accounts must be written, secured and maintained, and it would have to handle every service's API.", "Data Lifecycle Manager only manages EBS snapshots and EBS-backed AMIs, so RDS, DynamoDB and EFS would not be covered.", null, "Cross-Region Replication copies S3 objects only; it cannot back up EBS, RDS, DynamoDB or EFS."],
  "saa47": ["Transfer Acceleration speeds up long-distance client transfers and is not a prerequisite for replication.", "Object Lock can be used with replication but is not required; versioning is the prerequisite.", null, "Requester Pays changes who pays for requests and transfer; it is unrelated to setting up replication."],
  "saa48": ["A second Direct Connect link is the most resilient option but costs much more, and at the same location it shares that location as a failure point.", null, "VPC peering connects two VPCs; it cannot connect an on-premises data center to AWS.", "CloudFront is a content delivery network for serving content to users; it is not a private network path to a data center."],
  "saa49": ["VPC peering is never transitive and has no option to turn transitive routing on, so a full mesh would still be needed.", "A NAT gateway translates outbound traffic from private subnets to the internet; it does not route between VPCs and data centers.", "An ALB distributes HTTP requests to targets; it does not provide network routing between VPCs and on-premises networks.", null],
  "saa50": ["A runbook still depends on people clicking through consoles correctly under pressure, which is slow and error-prone.", "Snapshots cover disk data only; the network, load balancers and Auto Scaling groups would still be built by hand.", null, "A higher support plan gives faster help from AWS, but engineers would still rebuild the environment by hand."],
  "saa51": ["A spread group puts each instance on separate hardware, and spreading across AZs adds latency between the nodes.", "A partition group separates instances into different racks for fault isolation, which adds distance instead of minimizing latency.", null, "Splitting the cluster across Regions adds far more latency, and tightly coupled nodes need to be close together."],
  "saa52": ["A cluster group packs instances close together, possibly on shared racks, so one failure can take out several nodes.", null, "A warm pool keeps pre-initialized instances ready to speed up scale-out; it does not control hardware placement.", "One bare metal instance puts the whole quorum on a single host, the largest possible single point of failure."],
  "saa53": [null, "A gp3 volume's maximum IOPS is well below 150,000, so one gp3 volume cannot meet the requirement.", "gp2 ties IOPS to size and tops out far below 150,000 IOPS per volume, with less consistent latency.", "sc1 is a cold HDD for infrequently accessed sequential data and delivers very low random IOPS."],
  "saa54": [null, "io1 charges for provisioned IOPS that this sequential job does not need, making it far more expensive per GB.", "FSx for NetApp ONTAP is a managed multi-protocol file system, not a low-cost block volume for a single job.", "Instance store data is lost when the instance stops or terminates, and it only exists on some instance types."],
  "saa55": [null, "EBS Multi-Attach works only for io1 and io2 volumes within a single AZ and needs a cluster-aware file system.", "Each instance store is local to one host and is not shared, and its data is lost when instances stop or terminate.", "A Lustre scratch file system is a fixed-size, single-AZ file system for temporary HPC data, not a shared web content store."],
  "saa56": ["EFS is an NFS file system for Linux clients; it does not provide SMB shares, NTFS ACLs or Active Directory integration.", null, "FSx for Lustre is a high-performance parallel file system for Linux HPC and ML, not a Windows SMB file server.", "S3 is object storage accessed over HTTPS; Access Points simplify access policies but do not provide SMB or NTFS permissions."],
  "saa57": ["EFS One Zone is a lower-cost general-purpose NFS file system with much lower peak throughput than Lustre.", "S3 is object storage, not a POSIX file system, so mounting it as a drive gives limited semantics and performance for training.", null, "Separate gp3 volumes are not shared, and each would need its own copy of the 200 TB data set."],
  "saa58": [null, "CloudFront mainly accelerates downloads of cached content; uploads through it are not the standard fix for slow S3 uploads.", "One Zone-IA is a storage class that changes cost and resilience, not network transfer speed.", "A Network Load Balancer cannot be placed in front of an S3 bucket; S3 is reached through its own endpoints."],
  "saa59": [null, "AWS recommends multipart upload from about 100 MB, but it is only required above the 5 GB single PUT limit.", "Multipart upload works with or without Transfer Acceleration; the two features are independent.", "Parts can be as small as 5 MB (except the last part); there is no 1 GB minimum."],
  "saa60": ["The standby of a classic Multi-AZ instance does not serve reads, so it cannot take half of the queries.", "More storage can add baseline IOPS, but the bottleneck is CPU from repeated queries, not disk I/O.", "A larger instance raises cost and headroom but still runs the same repeated queries thousands of times a minute.", null],
  "saa61": [null, "Streams capture a time-ordered log of item changes for consumers; they do not speed up reads.", "A GSI adds another query pattern with its own key, but reads still take DynamoDB's usual single-digit milliseconds.", "Memcached would need custom cache-aside code in the application and is not API-compatible with DynamoDB."],
  "saa62": ["More instances in the same Region add capacity but do not shorten the network distance to readers far away.", "A larger origin instance speeds up processing slightly but leaves the long round trips to distant readers unchanged.", null, "A weighted record for one load balancer still sends everyone to the same Region, so distant readers stay slow."],
  "saa63": ["CloudFront serves HTTP and HTTPS content and does not proxy arbitrary UDP game traffic or offer two fixed IPs.", "DNS-based failover depends on resolvers and clients honoring the TTL, and it does not give clients static IP addresses to hard-code.", "ALBs are regional HTTP and HTTPS load balancers; they do not handle UDP and their IP addresses are not static.", null],
  "saa64": ["Read replicas spread read queries but do not pool connections, and write connections would still exhaust the primary.", "Storing connection strings in a queue does nothing to reduce the number of open database connections.", "Connection limits depend on the instance's memory and engine settings, not on the size of the storage volume.", null],
  "saa65": ["RDS for MySQL is a row-based OLTP database that is not built for petabyte-scale analytic joins.", "DynamoDB is a key-value store with no joins and no support for complex analytic SQL across petabytes.", "ElastiCache is an in-memory cache for fast lookups, not a place to store and analyze petabytes of history.", null],
  "saa66": ["Nightly imports into RDS add a server to manage, delay the data by a day, and cost more for occasional queries.", null, "An always-running EMR cluster means managing and paying for servers even when no one is querying.", "A provisioned Redshift cluster requires loading data and paying for the cluster all the time, which is overkill for occasional queries."],
  "saa67": [null, "An SQS message is consumed by one consumer and deleted after processing, so three apps cannot each read every event or replay it.", "Firehose delivers data to destinations such as S3 and does not keep records for multiple consumers to replay.", "SNS pushes each message to subscribers once and does not store messages, so there is nothing to reprocess later."],
  "saa68": ["Data Streams needs consumer code, and running it on EC2 means managing servers, which the requirement rules out.", null, "SQS with Lambda requires custom code to batch messages into files and convert them to Parquet.", "DataSync copies files between storage systems on a schedule; it does not ingest streaming records."],
  "saa69": ["A reactive CPU policy only adds instances after load rises, and with slow boots users are already affected.", null, "The health check grace period delays health checks on new instances; it does not add capacity before the peak.", "Manual daily changes are error-prone and depend on someone remembering, when a scheduled action does it automatically."],
  "saa70": ["Scheduled scaling changes capacity at set times; it does not respond to a metric at all.", "Simple scaling applies one fixed adjustment per alarm and then waits for a cooldown, rather than holding a metric at a target.", "Manual scaling means a person changes the desired capacity; nothing keeps the metric near a value automatically.", null],
  "saa71": ["DataSync still uses the same 100 Mbps link, and 80 TB would take more than 70 days even at full speed.", "Transfer Acceleration speeds up long-distance routing but cannot exceed the 100 Mbps local link, so it is still far too slow.", null, "A VPN runs over the same internet link, adds encryption overhead, and does not increase the available bandwidth."],
  "saa72": ["DMS alone does not convert Oracle-specific schema objects and code to PostgreSQL, and a weekend full load means long downtime.", null, "Data Pump files are an Oracle-specific format that Aurora PostgreSQL cannot import.", "Copying raw Oracle data files to EFS converts nothing; PostgreSQL cannot read Oracle data files."],
  "saa73": ["EC2 Instance Savings Plans are tied to one instance family in one Region and do not cover Fargate or Lambda.", "Standard RIs are tied to an instance type or family in one Region and do not apply to Fargate or Lambda.", "Capacity Reservations guarantee capacity in an AZ but give no discount on their own and bill whether used or not.", null],
  "saa74": ["On-Demand has no interruptions, but its price is much higher than Spot for work that can tolerate interruption.", "Dedicated Instances add a premium for single-tenant hardware, which a render job does not need.", null, "Standard RIs require a one- or three-year commitment to steady usage, which does not fit flexible batch jobs."],
  "saa75": [null, "EC2 gives two minutes of warning, not 30 seconds, through instance metadata and EventBridge.", "There is no 15-minute rebalance window; a rebalance recommendation is an early signal with no guaranteed time.", "EC2 does not give a one-hour termination notice for Spot Instances; the warning is two minutes."],
  "saa76": [null, "Dedicated Instances run on single-tenant hardware but do not expose sockets and cores or let you control placement on the host.", "Spot Instances are an interruptible pricing model and give no visibility of the physical server's sockets and cores.", "Compute Savings Plans are a pricing commitment, not a way to see or control the underlying physical server."],
  "saa77": [null, "Reserved Instances bill for every hour of the term, so reserving for 24/7 when instances run about 50 hours a week wastes money.", "Dedicated Hosts add cost for whole physical servers, which a development environment does not need.", "A Savings Plan sized for 24 hours a day commits to spend for hours when the instances are not needed."],
  "saa78": ["Standard-IA charges per-GB retrieval fees and suits data known to be rarely read, not unknown patterns.", null, "One Zone-IA also charges retrieval fees and stores data in one AZ, and it does not adapt to changing access.", "Glacier Flexible Retrieval requires restores that take minutes to hours, which does not fit data that may be read at any time."],
  "saa79": ["A custom Lambda copy job costs more to build and run, and copying into a new bucket does not delete or tier anything by itself.", null, "Versioning keeps old versions, which increases storage; it does not move objects to cheaper classes.", "Transfer Acceleration speeds up long-distance transfers; it does not compress objects or change storage class."],
  "saa80": ["S3 Standard costs more to store than the infrequent access classes, which is wasteful for rarely read images.", null, "Deep Archive retrievals take hours, so images could not load in milliseconds.", "Standard-IA stores data across multiple AZs, which costs more than One Zone-IA and is unnecessary for recreatable data."],
  "saa81": ["sc1 is a cold HDD with very low IOPS, so it cannot deliver the performance these volumes were sized for.", null, "Deleting volumes and restoring them only at night takes the workload offline and creates downtime.", "Instance store data is lost when instances stop or terminate, and moving to it requires migrating to new instances."],
  "saa82": [null, "Glacier Instant Retrieval offers millisecond access at a higher storage price, which is unnecessary when 48 hours is acceptable.", "Glacier Flexible Retrieval costs more to store than Deep Archive, and its faster retrievals are not needed here.", "Intelligent-Tiering adds monitoring fees and targets changing access patterns, not known long-term archives."],
  "saa83": [null, "Provisioning for the peak means paying for thousands of unused capacity units on the many near-idle days.", "Reserved capacity is a commitment to steady provisioned throughput, which this spiky, mostly idle table does not have.", "DAX caches reads only, so it does not help the write-heavy ticket sales, and it adds a cluster that runs all the time."],
  "saa84": ["Expedited retrievals from Glacier Flexible Retrieval typically take 1 to 5 minutes, not milliseconds, and cost extra.", "Deep Archive standard retrievals take up to 12 hours, far too slow for a doctor waiting on an image.", "S3 Standard costs more to store rarely accessed data, and Storage Lens only reports on usage without reducing cost.", null],
  "saa85": ["A three-year reservation commits to a fixed instance size for years, the opposite of paying only for what is used.", null, "A Multi-AZ DB cluster with Provisioned IOPS adds standby instances and I/O costs that an intermittent test database does not need.", "A self-managed server on a Dedicated Host is more expensive and adds patching work, and it still has a fixed size."],
  "saa86": ["A NAT instance still processes all the traffic, adds management work, and can become a bottleneck or single point of failure.", null, "More NAT gateways add hourly charges and still charge per GB processed for the DynamoDB traffic.", "DAX caches reads in the VPC, but writes and cache misses still reach DynamoDB through the NAT gateway."],
  "saa87": [null, "Data out from EC2 to the internet is charged per GB and is often the largest transfer cost.", "Transfer between Regions is charged per GB, for example for cross-Region replication.", "Traffic between Availability Zones is charged per GB in each direction, even over private IP addresses."],
  "saa88": ["A monthly report arrives after the month is over, so it cannot warn the team when the forecast first exceeds the limit.", "CPU utilization alarms measure performance, not spending, so they cannot track a cost threshold.", "The Cost and Usage Report is detailed billing data delivered to S3; it has no built-in alerting on forecasted spend.", null],
  "saa89": [null, "VPCs are network boundaries; costs are not split by VPC, and many costs such as S3 are not tied to a VPC at all.", "Detailed monitoring gives more frequent performance metrics and adds cost; it does not attribute spend to projects.", "CloudTrail data events log API calls for auditing; they do not break down billing by project."],
  "saa90": ["Trusted Advisor security checks look at issues such as open ports and MFA, not instance sizing based on usage.", null, "Inspector scans for software vulnerabilities and network exposure; it does not recommend instance sizes.", "Config conformance packs evaluate configuration compliance against rules; they do not analyze utilization to right-size instances."]
});
