CertHub.addWhys("ckad", {
  "ck1": ["A Deployment keeps a set number of replicas running forever, so a finished migration container would just be restarted again and again.", "A DaemonSet runs one long-lived Pod per node; it has no notion of running a task once to completion.", null, "A ReplicaSet maintains a fixed count of running Pods and restarts them when they exit, so it never treats a run as finished."],
  "ck2": [null, "A CronJob runs Jobs on a schedule that terminate; it does not keep an agent running on every node.", "A StatefulSet gives replicas stable names and storage but places them wherever the scheduler chooses, not one per node.", "A Deployment spreads a fixed replica count across nodes with no guarantee of one per node, and it does not grow when nodes are added."],
  "ck3": ["A Job runs Pods to completion; parallelism only controls concurrency and gives neither stable names nor per-replica claims.", "Deployment Pods get random name suffixes, and a single shared RWX claim is the opposite of one PVC per replica.", "A DaemonSet runs one Pod per node, and hostPath ties data to a node's filesystem rather than giving each replica its own claim.", null],
  "ck4": ["This puts 2 in the minute field and 30 in the hour field, which is invalid because hours only go from 0 to 23.", null, "This sets minute and hour to every value and puts 2 and 30 in the day-of-month and month fields, which is wrong and 30 is an invalid month.", "This runs at minute 0 of hours 2 and 30; hour 30 is invalid, and even 02:00 is not 02:30."],
  "ck5": ["Replace cancels the running Job and starts the new one, so runs still interrupt each other instead of being prevented from starting.", "This only controls how many finished successful Jobs are kept; it has no effect on whether runs overlap.", "startingDeadlineSeconds limits how late a missed run may start; it does not check whether a previous Job is still active.", null],
  "ck6": ["kubectl run creates a standalone Pod from an image and knows nothing about the CronJob's jobTemplate.", "rollout restart works on Deployments, StatefulSets and DaemonSets; CronJobs have no rollouts to restart.", null, "Setting suspend to false only re-enables future scheduled runs; nothing runs until the next schedule time."],
  "ck7": ["Parallelism 2 caps concurrent Pods, so six cannot start at once, and completions 6 means six successes are required, not two.", null, "Neither field is a retry count; retries are controlled by backoffLimit, and six successful Pods are required.", "Parallelism 2 allows two Pods at a time rather than strictly one after another, and nothing repeats each Pod twice."],
  "ck8": [null, "Omitting restartPolicy defaults it to Always, which Jobs reject, so a Job template must set Never or OnFailure explicitly.", "restartPolicy: Always on an init container defines a native sidecar, which does not change the Pod-level rule that Job Pods cannot use Always.", "restartPolicy is a Pod-level field; the per-container field only exists for init containers acting as sidecars, and it is not the reason for the rejection."],
  "ck9": [null, "args does not touch the ENTRYPOINT, so python is still the executable and worker.py is passed to it.", "args replaces CMD rather than appending to it, so app.py is dropped.", "This is what runs with no overrides; setting args replaces the CMD app.py with worker.py."],
  "ck10": ["go.mod is tiny and needed for the build; ignoring it breaks the build and does nothing about the toolchain in the image.", "Chaining RUN commands reduces layer count a little, but the compiler and source are still shipped in the final image.", "A memory limit controls runtime resource use and has no effect on how large the image is on disk.", null],
  "ck11": ["docker create makes a container, not an image from a build context, and docker export flattens a container's filesystem rather than saving a tagged image.", null, "docker build has no --name flag for tagging (that is -t), and docker push uploads to a registry rather than writing a tar file.", "docker commit snapshots a running container, not a directory, and docker load reads a tar into the image store instead of writing one."],
  "ck12": ["A sidecar runs alongside the app container, so a loop there does not delay the app from starting.", "postStart runs concurrently with the container's entrypoint, so the app can start before DNS is ready.", null, "Services do not have probes; probes belong to containers, and a readiness probe never blocks another container from starting."],
  "ck13": ["The kubelet ignores labels when deciding container behavior, so this is just an ordinary app container with no startup ordering.", null, "Ephemeral containers are temporary debugging containers added to a running Pod with kubectl debug, not part of normal startup.", "shareProcessNamespace lets containers see each other's processes; it does not make a container start first or act as a sidecar."],
  "ck14": ["hostPath mounts a directory from the node, which ties the Pod to that node and leaves data behind after the Pod is gone.", "A PVC gives durable storage that outlives the Pod, which is more setup than needed for throwaway scratch files.", "A configMap volume is populated from a ConfigMap and is read-only, so containers cannot use it to exchange scratch files.", null],
  "ck15": [null, "medium: Memory means tmpfs in RAM, not node disk, and emptyDir contents are deleted when the Pod is removed.", "emptyDir is not a claim and does not involve a StorageClass; sizeLimit just caps how much the volume may hold.", "An emptyDir belongs to one Pod and is shared only by that Pod's containers, not by other Pods on the node."],
  "ck16": ["RWO restricts mounting to one node, not one Pod, and says nothing about the whole cluster; ReadWriteOncePod is the single-Pod mode.", "Access modes describe how many nodes can mount a volume; none of them makes data write-once then read-only.", null, "That describes ReadWriteMany; ReadWriteOnce limits read-write mounting to a single node."],
  "ck17": ["The new Pod would reference the same unbound claim and stay Pending, because no StorageClass exists to provision a volume.", "Readiness probes only matter after a container runs; they cannot help a Pod that is not scheduled because of an unbound claim.", null, "The problem is that no volume is provisioned for the claim, not node placement; a nodeSelector cannot create the missing StorageClass."],
  "ck18": [null, "Kubernetes never creates a default volume for an unmatched mount name; the Pod is rejected during validation.", "The kubelet does not guess which volume you meant; names must match exactly or the API server rejects the spec.", "The Pod never gets created, because the API server validates volumeMount names before the Pod is stored."],
  "ck19": ["kubectl replace needs a full manifest file with -f; it does not take a Deployment name and image as arguments.", "There is no rollout update subcommand, and --image is not a rollout flag.", "kubectl edit has no --image flag, and changing a Pod does not change the Deployment's template, so the ReplicaSet would replace it.", null],
  "ck20": ["rollout history with --revision only prints the details of that revision; it does not roll anything back.", null, "rollout restart re-creates Pods with the current template and has no --revision flag.", "kubectl apply needs a manifest file and has no --revision option for returning to an earlier Deployment revision."],
  "ck21": ["maxUnavailable 0 means the count never drops below the desired 4, so 3 is impossible during the rollout.", "4 is the desired count; maxSurge 1 lets the controller create one extra Pod above it.", "maxSurge 1 allows only one extra Pod, not a full second set of 4.", null],
  "ck22": ["A rolling update with surge always runs old and new Pods side by side, which the schema lock forbids.", "maxUnavailable 0 keeps old Pods running until new ones are ready, so versions overlap for the whole rollout.", null, "A canary deliberately runs the new version alongside the old one, which is exactly what the lock does not allow."],
  "ck23": ["Scaling blue to zero before traffic is switched creates an outage window and is not the one-step cutover blue/green relies on.", null, "rollout undo changes web-blue's template back to an older revision; it does not move traffic to web-green.", "Relabeling blue's Pods as green would make the old version receive traffic meant for green, which defeats the switch."],
  "ck24": [null, "Blue/green points the Service at one version at a time; here both versions match the selector and receive traffic together.", "Recreate terminates all old Pods before starting new ones, so the two versions would not be running side by side.", "These are Deployments, not a StatefulSet, and partition updates apply to StatefulSet rolling updates only."],
  "ck25": [null, "Scaling only changes the replica count of the current ReplicaSet; the Pod template is unchanged, so no new revision is created.", "Annotations on the Deployment's own metadata are outside spec.template and do not trigger a rollout.", "revisionHistoryLimit controls how many old ReplicaSets are kept; changing it does not alter the Pod template."],
  "ck26": ["Scaling to zero causes downtime and does not stop each template change from triggering its own rollout.", "rollout restart triggers another rollout immediately, which is the opposite of batching changes.", "Deleting the Deployment takes the app down completely and throws away its revision history.", null],
  "ck27": ["The argument order is wrong: helm install takes the release name first and the chart second, and the missing namespace is not created.", null, "helm create scaffolds a new local chart directory; it does not install a chart from a repository.", "helm upgrade takes <release> <chart> and has no --name flag in Helm 3; it also needs an existing release unless --install is used."],
  "ck28": ["helm get values reads the user-supplied values of an installed release, not the defaults of a chart.", "helm list shows installed releases and has no --values flag for listing chart settings.", null, "helm status reports the state of an installed release, not a chart's configurable values."],
  "ck29": ["There is no helm undo command; rolling back is done with helm rollback.", null, "helm upgrade has no --revision flag; it installs new chart or values and always creates a new revision.", "helm history lists a release's revisions but does not change anything."],
  "ck30": ["helm search repo looks for charts in configured repositories, not installed releases in the cluster.", "Helm releases are not a Kubernetes resource type that kubectl get can list by default.", "helm status reports on one named release and has no --all flag for listing every release.", null],
  "ck31": [null, "--dry-run=false just performs a normal install, which is exactly what you wanted to avoid, and no release name or chart is given.", "helm package bundles a chart directory into a .tgz archive; it does not render templates with values.", "helm pull downloads a chart from a repository and has no --render option."],
  "ck32": ["With -f, kubectl sends the Kustomization file itself, which is not a resource the API server accepts, instead of building the overlay.", "kubectl create -r is not a valid way to build a kustomization; -R is recursive for -f, and no build step happens.", null, "kubectl kustomize only builds and prints YAML; it has no apply subcommand."],
  "ck33": ["commonAnnotations adds annotations and replicas changes replica counts; neither renames resources or changes image tags.", "nameSuffix appends to names rather than prepending, and patches is a heavier way to change images than the images field.", null, "generatorOptions affects ConfigMap and Secret generators, such as hash suffixes, and does not rename resources."],
  "ck34": [null, "kubectl diff compares against the live cluster rather than just printing YAML, and with -f it would not build the kustomization.", "kubectl get -k reads the named objects back from the cluster; it does not print the locally rendered overlay.", "kubectl explain shows field documentation for resource types and does not accept a kustomization directory."],
  "ck35": ["rollout history lists past revisions and returns immediately rather than waiting for the current rollout.", "--watch-only streams changes to the Deployment object but never exits on success or failure, so it is not a proper wait.", "Pods are never complete in a Deployment, and kubectl wait also needs a target; this does not track the rollout.", null],
  "ck36": ["The rule is enforced by the API server, so kubectl edit is rejected in exactly the same way as kubectl apply.", null, "You cannot fix it by editing ReplicaSets; the Deployment controller owns them and the Deployment's selector remains immutable.", "Selectors can match on multiple labels; the number of labels has nothing to do with this rejection."],
  "ck37": ["networking.k8s.io/v1beta1 for Ingress was also removed in 1.22, so the manifest would still fail.", "ingress.k8s.io is not a real API group; Ingress lives in networking.k8s.io.", "apps/v1 serves Deployments, StatefulSets, DaemonSets and ReplicaSets, not Ingress.", null],
  "ck38": ["cluster-info prints the control plane and core service addresses, not a list of resource kinds.", "kubectl version reports client and server versions only.", null, "config view shows kubeconfig clusters, users and contexts, not the API resources the server offers."],
  "ck39": ["There is no batch/v2 API version; CronJob is served only in batch/v1.", null, "apps/v1 serves workload controllers like Deployments, not CronJobs, which are in the batch group.", "cronjob/v1 is not an API group; apiVersion takes the group (batch) and version (v1)."],
  "ck40": [null, "Restarts are triggered by a failing liveness probe; readiness failures never restart the container.", "Probes never evict or reschedule Pods; eviction comes from node pressure or preemption.", "Deployments do not roll back automatically on probe failures; a rollback requires kubectl rollout undo."],
  "ck41": [null, "Removing liveness stops the startup kills but also loses automatic recovery from deadlocks after the app is running.", "timeoutSeconds is how long one check waits for a response, so 180 seconds would make every later check painfully slow instead of covering startup.", "restartPolicy Never is not allowed for Deployment Pods, and it would stop all recovery rather than protecting the startup period."],
  "ck42": ["failureThreshold 3 means one failure is tolerated; the kubelet waits for three consecutive failures.", "Any status outside 200-399 or a timeout counts, not only 5xx, and the failures must be consecutive rather than within a minute.", "The threshold is 3 consecutive failures, not 10; 10 is the periodSeconds value.", null],
  "ck43": ["httpGet sends an HTTP request, which a non-HTTP server like PostgreSQL cannot answer properly, so the probe would fail.", null, "curl speaks HTTP, so against a non-HTTP port it errors, and the image may not even include curl.", "A grpc probe needs the app to implement the gRPC health checking protocol, which a plain TCP service does not."],
  "ck44": ["describe does not have a logs subcommand; it shows object details and events, not container output.", "--since takes a duration like 5m, not prev, and it filters current logs rather than returning the crashed instance's output.", null, "There is no kubectl get logs resource, and -p=false would explicitly ask for the current, not previous, container."],
  "ck45": ["kubectl logs takes a single resource and a label with -l, not a positional app=web, and -A is not used here.", null, "Logs are not an API resource, so kubectl get logs does not exist.", "describe shows Pod details and events, not log streams, and it has no --follow flag."],
  "ck46": ["The read-only port is a legacy kubelet endpoint; enabling it does not provide the metrics.k8s.io API kubectl top needs.", "kube-state-metrics exposes object state for Prometheus scraping, not the resource Metrics API that kubectl top reads.", "Prometheus Operator CRDs manage Prometheus deployments; they do not serve metrics.k8s.io.", null],
  "ck47": [null, "Exceeding a memory limit shows as OOMKilled after the container has started, which needs the image to have been pulled.", "A failing readiness probe leaves a running container not Ready; it has nothing to do with pulling the image.", "Insufficient node capacity leaves the Pod Pending with FailedScheduling, before any image pull is attempted."],
  "ck48": ["A liveness failure restarts the container with a probe-related event, not an OOMKilled reason.", "Node disk pressure leads to eviction with a reason like Evicted, not OOMKilled with exit code 137.", null, "A missing entrypoint produces a start error such as exit code 127 or a ContainerCannotRun message, not OOMKilled."],
  "ck49": ["kubectl attach connects to the existing process's stdio and has no --image flag to bring in tools.", "kubectl cp relies on tar inside the container, which a distroless image lacks, and copying binaries in changes the container.", null, "kubectl run creates a separate Pod; even with hostPID it does not join the target Pod's network namespace or container process namespace cleanly."],
  "ck50": [null, "kubectl describe has no --order flag, and describing events does not list them sorted by time.", "Events are API objects, not container logs, so kubectl logs cannot read them.", "-o wide adds columns but does not sort, and --watch-only=false just disables a watch; the output still isn't ordered by time."],
  "ck51": ["APIService objects register aggregated API servers, and most Operators add kinds through CRDs, not APIServices.", "There is no built-in operators resource; it would only exist if some CRD (such as OLM's) defined it.", "describe nodes shows node capacity, conditions and Pods, not which resource kinds exist.", null],
  "ck52": ["An Operator is software, not an RBAC role; it needs RBAC permissions but is not itself a set of permissions.", null, "A mutating webhook changes objects at admission time; it does not continuously reconcile an application's state.", "A Helm chart is a packaging format that may install an Operator, but it is not a controller running in the cluster."],
  "ck53": ["The server cannot check permissions before it knows who the caller is, so authentication must come first.", "Admission runs last on requests that were already authenticated and authorized, not first.", "Authorization must happen before admission; admission controllers only see requests that RBAC already allowed.", null],
  "ck54": ["Authentication succeeded because the user was identified; the error comes from a quota check, not identity.", "RBAC allowed the create, so authorization passed; ResourceQuota is not an RBAC decision.", null, "The scheduler only sees Pods that were stored; a quota rejection happens before the Pod object exists."],
  "ck55": ["A ClusterRoleBinding grants the permissions in every namespace, which is broader than just dev.", null, "A ClusterRoleBinding cannot reference a Role; it can only bind a ClusterRole, cluster-wide.", "Binding the view ClusterRole cluster-wide grants read access to many resources in every namespace, far beyond Pods in dev."],
  "ck56": [null, "auth whoami only reports your own identity and has no --sa flag to test another subject's permissions.", "kubectl get has no --can-i flag; RoleBinding output does not evaluate whether an action is allowed.", "describe sa shows the ServiceAccount's tokens and secrets, and there is no --permissions flag."],
  "ck57": [null, "A RoleBinding links subjects to a Role; it does not accept --verb or --resource to define permissions.", "--role is not a flag of create clusterrole, and a ClusterRole is cluster-scoped rather than the namespaced Role asked for.", "kubectl apply takes manifests with -f, and there is no --pods=read shorthand for permissions."],
  "ck58": ["Memory is not compressible, so the kubelet cannot throttle it; only CPU limits are enforced by throttling.", "Limits are enforced at runtime by the kernel cgroup; requests only affect scheduling.", "The scheduler never moves running Pods; placement happens once, and exceeding a limit leads to a kill, not a move.", null],
  "ck59": ["Being killed with 137 happens for exceeding a memory limit; CPU overuse is throttled instead.", null, "Eviction comes from node pressure such as low memory or disk, not from a container hitting its own CPU limit.", "CPU being compressible is why it gets throttled rather than killed, but the limit is still enforced, so two cores are not allowed."],
  "ck60": ["BestEffort is for Pods where no container sets any requests or limits.", "Burstable applies when at least one request or limit is set but they do not all match; here they match exactly.", null, "Critical is not a QoS class; the three classes are Guaranteed, Burstable and BestEffort."],
  "ck61": ["A PriorityClass affects scheduling order and preemption; it adds no requests, so the quota still rejects the Pod.", null, "An HPA changes replica counts based on metrics and cannot add missing requests to a Pod spec.", "A PodDisruptionBudget limits voluntary evictions; it has no effect on quota admission."],
  "ck62": ["kubectl apply needs a manifest via -f and cannot build a ConfigMap from positional key=value and file arguments.", "This creates a Secret, not a ConfigMap, even though the flags are right.", "create configmap does not have --env or --file flags; the flags are --from-literal, --from-file and --from-env-file.", null],
  "ck63": [null, "configMapKeyRef works but needs a separate env entry for every key, which is more YAML than envFrom.", "A configMap volume exposes keys as files in a directory, not as environment variables.", "There is no spec.configMapName field in a Pod spec, so the API would reject or ignore it."],
  "ck64": ["Environment variables are read only when the container starts, so they do not change in a running Pod.", "It is the reverse: env vars stay fixed until restart, while mounted files are refreshed by the kubelet.", null, "A node reboot is not needed; mounted files refresh after the kubelet sync period, and env vars update after a Pod restart."],
  "ck65": ["kubectl does not encrypt Secret values client-side; the data field is just base64 text.", "Secret values must be readable by the Pods that use them, so they are encoded, not hashed.", null, "Secrets are stored in etcd like other objects; they are only kept in tmpfs on nodes when mounted into Pods."],
  "ck66": [null, "The tls type requires --cert and --key files and does not accept --from-literal.", "This would create a ConfigMap, and create configmap has no --secret flag.", "create secret needs a subtype like generic, and values are passed with --from-literal, not as positional arguments."],
  "ck67": ["Kubernetes never substitutes an empty string for a missing key unless optional: true is set, in which case the variable is simply not set.", "Without optional: true the missing key is an error; the container does not start while ignoring the variable.", "Kubernetes never guesses another key; references must name an existing key exactly.", null],
  "ck68": ["The ServiceAccount is set in spec, not metadata, so metadata.serviceAccount has no effect.", null, "ServiceAccounts are chosen per Pod, not per container, so there is no container-level serviceAccount field.", "securityContext controls UIDs, capabilities and similar settings; it has no serviceAccount field."],
  "ck69": ["Deleting the default ServiceAccount breaks Pod creation in the namespace and it is recreated automatically anyway.", "RoleBindings control what the token may do; removing one still mounts the token into the Pod.", "readOnlyRootFilesystem blocks writes to the container filesystem but does not stop the token volume being mounted.", null],
  "ck70": ["privileged: false does not prevent running as root, and runAsGroup: 0 sets the primary group to root.", "fsGroup sets volume group ownership, not the process UID, and allowing privilege escalation weakens security.", null, "runAsUser: 0 is root, which is exactly what must be avoided; a read-only filesystem does not change the UID."],
  "ck71": ["privileged: true grants nearly all host capabilities and device access, far more than changing interfaces needs.", null, "Running as root with hostNetwork gives access to the node's network stack, which is broader than the container needs.", "allowPrivilegeEscalation: true only permits setuid-style escalation; it does not grant NET_ADMIN and weakens security."],
  "ck72": [null, "Logging a warning is the warn mode behavior; enforce mode rejects the Pod.", "Pod Security Admission only validates; it never mutates Pods to make them comply.", "Pod Security Admission does not influence scheduling or taints; it acts at admission time before the Pod is stored."],
  "ck73": [null, "create service needs a type subcommand like clusterip, ports are given with --tcp=80:8080, and it would not copy the Deployment's selector.", "This exposes a Pod named web rather than the Deployment, and --node-port is not the flag for a ClusterIP Service.", "port-forward opens a temporary tunnel from your machine and creates no Service object."],
  "ck74": ["ClusterIP is the default and normal Service type; it does not prevent endpoints from being created.", "NetworkPolicies filter traffic but do not affect which Pods appear in a Service's endpoints.", "Resource limits are unrelated to endpoint selection, which is based on labels and readiness.", null],
  "ck75": ["This name points at a Service called api in namespace shop, not the one in backend.", null, "The order is wrong and svc is missing; the pattern is service, then namespace, then svc and the cluster domain.", "The namespace and svc parts are swapped and the cluster domain is wrong, so this name does not resolve."],
  "ck76": ["Port 80 is the Service port on the ClusterIP; it is not opened on the node's IP.", "30080 is opened on node IPs, and the ClusterIP is only reachable from inside the cluster anyway.", null, "Pods listen on the targetPort 8080; nothing listens on 30080 at the Pod IP, and Pod IPs are usually not reachable from outside."],
  "ck77": ["ExternalName returns a CNAME to an outside host and has no connection to the StatefulSet's Pods.", null, "sessionAffinity pins a client to one Pod behind a normal virtual IP; it creates no per-Pod DNS records.", "externalTrafficPolicy: Local controls node-local routing for external traffic, not per-Pod DNS names."],
  "ck78": ["A ClusterIP without a selector needs manual endpoints with IP addresses; it cannot map to a DNS hostname.", "A LoadBalancer exposes in-cluster Pods to outside clients, the opposite direction of this requirement.", "NodePort exposes in-cluster Pods on node ports; it does not alias an external hostname.", null],
  "ck79": [null, "With Ingress in policyTypes and an empty ingress list, nothing is allowed in; allow-all needs a rule with an empty entry.", "Egress is not in policyTypes, so outgoing traffic is not restricted by this policy.", "An empty rule list is meaningful: it selects all Pods and allows no ingress, which is the default-deny pattern."],
  "ck80": ["kube-proxy implements Service routing, not NetworkPolicy, so restarting it changes nothing.", "NetworkPolicies apply to Pod traffic in general, regardless of Service type.", null, "Policies apply to matching Pods in existing namespaces as soon as an enforcing CNI sees them."],
  "ck81": ["That would be the OR behavior of two separate from elements; in one element the selectors are ANDed.", "The namespaceSelector widens the scope to team=web namespaces instead of limiting it to the backend namespace.", null, "The selectors restrict traffic to a specific set of Pods; nothing here allows every namespace."],
  "ck82": [null, "Name resolution does not go through the API server, so allowing 6443 does not fix DNS lookups.", "The problem is outgoing DNS traffic, not incoming traffic, and kube-proxy's health port is unrelated to DNS.", "The kubelet port is used for logs and exec from the API server; Pods do not resolve names through it."],
  "ck83": ["ipBlock 0.0.0.0/0 allows traffic from any IP, not just frontend Pods.", "to peers are used in egress rules; an ingress rule needs from.", "namespaceSelector matches labels on namespaces, not on Pods, so it would not select app=frontend Pods.", null],
  "ck84": ["NodePort Services in kube-system are not required; without a controller nothing implements the Ingress.", null, "Backends can be ordinary ClusterIP Services; LoadBalancer is not needed behind an Ingress.", "A NetworkPolicy would not give the Ingress an address; the missing address means no controller has picked it up."],
  "ck85": ["Prefix matching works on whole path segments, so /apis is a different segment and does not match /api.", "Path matching is case-sensitive, so /API does not match /api.", "The prefix must match from the start of the path; /v1/api starts with /v1.", null],
  "ck86": ["create service has no ingress subtype, and Services cannot do host-based HTTP routing.", "kubectl expose creates Services, not Ingress objects.", null, "kubectl apply needs a manifest file and does not accept a rule as positional arguments."],
  "ck87": ["The Ingress tls section references Secrets only; ConfigMaps are not used for certificates or keys.", null, "ServiceAccount tokens authenticate to the Kubernetes API and have nothing to do with serving HTTPS.", "Ingress controllers do not read certificates from volumes; the tls section names a Secret."],
  "ck88": [null, "Switching to NodePort exposes the database on every node, which is exactly what should be avoided.", "kubectl proxy exposes the Kubernetes API on a local port, not a raw TCP connection to the db Service.", "kubectl run creates a new Pod and does not create a tunnel to your laptop."],
  "ck89": [null, "Services work at Layer 4; a port name like http does not force any protocol-aware routing.", "Node ports are set by nodePort; targetPort refers to the Pod side, not the node.", "Services do not reference Ingress rules; targetPort names a container port."],
  "ck90": ["A wrong selector would leave endpoints empty or pointing to other Pods; here the endpoints are the correct Pods.", "DNS is working, since the Service resolves and has endpoints; the timeout is because of the wrong port.", "Pods failing readiness would not be listed as ready endpoints, but here the correct Pods are listed.", null],
});
