CertHub.addWhys("ai-200", {
  "az1": [null, "docker build runs on a local Docker engine, which the developers do not have, and it does not push the image to ACR.", "az acr import copies an image that already exists in another registry; it does not build anything from source.", "az containerapp up builds and deploys a Container App in one step; it is not the registry build step the pipeline needs, and internal ingress is irrelevant."],
  "az2": ["Container Apps scale rules change replica counts based on load or events; there is no rule on image age and scaling never rebuilds images.", null, "A retention policy deletes untagged manifests after a number of days; it cleans up storage but never triggers a rebuild.", "Geo-replication copies the registry's content to other regions for faster pulls; it does not watch base images or rebuild."],
  "az3": ["Basic is the entry tier for development with lower storage and throughput; it supports neither private endpoints nor geo-replication.", "Standard adds storage and throughput over Basic but still lacks private endpoints and geo-replication.", null, "The admin user is an authentication option available on every tier; it has nothing to do with private endpoints or replication."],
  "az4": ["The admin user is one shared, full-rights username and password; storing it as a secret still leaves a credential to protect and rotate.", "ACR repositories cannot be made public for an IP range, and anonymous pull would drop authentication rather than use an identity.", "A service principal secret in an environment variable is still a stored password that can leak and must be rotated.", null],
  "az5": ["latest is a movable tag that any later push can repoint, which is exactly the risk described.", null, "A repository name alone resolves to the latest tag by default, so it gives no guarantee about which image runs.", "Adding the registry name only makes the reference fully qualified; the tag part can still be moved to a new image."],
  "az6": [null, "WEBSITE_RUN_FROM_PACKAGE mounts a zip package as the site content; it is for code deployments, not for container port mapping.", "SCM_DO_BUILD_DURING_DEPLOYMENT tells the Kudu/Oryx build to run on code deploy; it does not tell App Service which port the container uses.", "FUNCTIONS_WORKER_RUNTIME selects the language worker for Azure Functions and has no effect on a custom container web app's port."],
  "az7": ["Single revision mode moves all traffic to the newest revision once it is ready, so a 90/10 split is not possible.", "A KEDA HTTP scale rule changes the number of replicas based on concurrent requests; it does not divide users between versions.", null, "Two environments would be two separate network boundaries; Container Apps does not share one ingress endpoint across environments for weighted splitting."],
  "az8": ["An HTTP rule reacts to incoming requests, which a queue worker does not receive, and min replicas 1 prevents scaling to zero.", null, "CPU and memory scale rules cannot scale an app to zero, because they need a running replica to measure usage.", "A fixed count of 5 replicas never reacts to the backlog and keeps billing even when the queue is empty."],
  "az9": ["Disabling ingress removes the HTTP endpoint entirely, so other apps could not call the API over HTTP; Dapr is optional and not required for this.", "External ingress is still published to the internet; IP restrictions only filter callers and do not make the app internal.", null, "External ingress with a custom domain on 443 is explicitly internet-facing, the opposite of the requirement."],
  "az10": [null, "A Container App with min replicas 1 keeps a replica running (and billed) all day even when there is no work.", "WebJobs run inside an App Service plan that is billed continuously, and they are not the Container Apps feature for run-to-completion tasks.", "A revision with 0% traffic still exists as a running app revision; traffic weight does not schedule work or stop billing."],
  "az11": ["A ConfigMap only passes configuration to pods; the registry URL is already in the image name and the problem is missing pull permission.", "Memory limits affect running containers, but ErrImagePull happens before the container starts, when the image cannot be pulled.", "The Service type controls how traffic reaches pods; it has no effect on whether nodes can pull the image.", null],
  "az12": [null, "containers is a list of container definitions in the pod template; there is no count field and it does not control how many pods run.", "Labels are key/value metadata used for selection; a label named replicas has no effect on the pod count.", "maxSurge limits how many extra pods can be created during a rolling update; it does not set the steady-state count."],
  "az13": ["Files deleted in a later layer still exist in the earlier layers, so the image does not get smaller.", null, "Changing the tag to slim only changes the base image; the compilers installed during the build would still end up in the image.", "Geo-replication copies the image to more regions; it does not change the image size and adds storage."],
  "az14": ["Container Apps secrets are app-level settings injected at runtime; they are never baked into the container image.", "Secrets can be plain values or Key Vault references resolved with a managed identity, so they are not limited to portal-typed text.", "Secrets are scoped to a single Container App and are not shared automatically with other apps in the environment.", null],
  "az15": [null, "A second App Service plan is just more compute; it has no built-in staging or swap between versions.", "Scaling out adds instances of the same version; it does not let you stage and swap a new image.", "An ACR webhook can trigger continuous deployment, but on the production slot it would push the new image live without a staged test and swap."],
  "az16": ["Many apps can share one environment; putting each app in its own environment is optional, not required.", "The underlying Kubernetes is managed by Microsoft and hidden; you cannot use kubectl against a Container Apps environment.", null, "Environments host both Container Apps and Container Apps jobs; they are not limited to jobs."],
  "az17": ["Geo-replication adds copies of every image to more regions, which increases storage instead of reducing it.", null, "Content trust signs images so clients can verify them; it does not delete old versions or reduce storage.", "A new registry per release multiplies registries to manage and still keeps every old image unless something deletes them."],
  "az18": [null, "Killing a container over its memory usage is the effect of limits, not requests; CPU over a limit is throttled rather than killed.", "Public IPs come from Services such as LoadBalancer, not from resource requests on the pod.", "Image pull behavior is controlled by imagePullPolicy and node caching, not by resource requests."],
  "az19": ["A second environment would be a separate deployment with its own network, not a test URL for one revision of the same app.", "A KEDA rule with min replicas 0 only controls scaling; it gives the revision no separate URL.", "In single revision mode the new revision receives all traffic as soon as it is ready, which is the opposite of the goal.", null],
  "az20": ["The user a container runs as has no effect on how quickly the image layers are built.", "ACR accepts images regardless of the user they run as; running as non-root is a runtime hardening practice, not a push requirement.", null, "It is the opposite: binding to ports below 1024 normally needs root or an extra capability, so non-root apps usually listen on a high port."],
  "az21": ["Separate Basic registries need custom sync scripts, have different names per region and can drift out of date.", null, "ACR cannot be placed behind a CDN for image pulls, and Standard has no geo-replication.", "ACR tokens are scoped credentials for repository access; they do not change where images are served from."],
  "az22": ["A query for one item still goes through the query engine and costs more RUs than a point read, even with the partition key.", "A cross-partition query with ORDER BY fans out to every partition and is one of the most expensive ways to fetch a single item.", null, "The change feed lists changes over time; it is not a lookup mechanism and reading it to find one item wastes RUs."],
  "az23": [null, "/country has very few distinct values, so a few large countries would create hot partitions.", "A date key sends all of today's writes to the same partition, creating a hot partition for writes.", "/type has only a handful of values, so partitions would be huge and uneven and tenant queries would fan out."],
  "az24": ["Strong consistency changes read guarantees and raises read cost; it does nothing to reduce indexing cost on writes.", null, "Every container must have a partition key; removing it is not possible and would not reduce indexing cost.", "Storing the embedding as a string would break vector search and still gets indexed by the range index."],
  "az25": [null, "Equality compares the whole array for an exact match; it does not measure similarity, and Cosmos DB NoSQL uses TOP rather than LIMIT.", "ARRAY_CONTAINS checks whether an array contains a value; it does not rank items by vector similarity.", "ORDER BY on the embedding array sorts by array values, not by distance to the query vector @v."],
  "az26": ["Consistency is an account-level setting (optionally relaxed per request), not part of the container vector policy.", "Throughput is provisioned on the database or container separately and is not part of the vector policy.", "TTL is set on the container or per item for the whole document; vectors have no separate TTL in the policy.", null],
  "az27": ["flat performs exact brute-force search and is limited to 505 dimensions, so it suits small sets rather than large collections.", "A range index supports equality and range filters on scalar values; it is not a vector index.", null, "A spatial index supports geospatial queries on GeoJSON points and polygons, not vector similarity search."],
  "az28": ["A timer that queries everything each minute is polling, adds latency and burns RUs scanning unchanged items.", null, "Stored procedures run only when a client calls them; they cannot react automatically to new or changed items.", "Consistency and read-after-write affect what a client reads, not whether anything runs when data changes."],
  "az29": ["Instances of one processor read the same monitored container; splitting by database is not how work is shared.", "Consistency level affects read guarantees; it does not coordinate which consumer handles which partition range.", "Random filtering would drop or duplicate changes and gives no checkpointing across restarts.", null],
  "az30": [null, "Consistency level does not control what the change feed contains; latest version mode omits deletes at every level.", "Re-reading from the beginning still shows only current items, so deleted items remain invisible and RU cost rises.", "Stored procedures do not run on deletes made through other paths, and raising an error would block deletes rather than capture them."],
  "az31": ["Eventual is the weakest level and does not guarantee a client sees its own writes.", null, "Consistent prefix guarantees ordering of writes but not read-your-own-writes, and it is not the default.", "Bounded staleness is not the default and costs more for reads than Session; it bounds lag by time or versions."],
  "az32": ["An f-string builds the SQL from user input, which allows query injection and prevents plan reuse.", "The partition key routes the query to a partition; putting the user's category there is not how you pass a filter value.", null, "Manual escaping is error-prone and still concatenates user input into the query text."],
  "az33": ["The service tier does not decide which extensions are allowed; the allow-list parameter does.", "pgvector is a server-side extension; installing a Python client package does not make CREATE EXTENSION work on the server.", "pgBouncer is a connection pooler; enabling it has nothing to do with extension allow-listing.", null],
  "az34": [null, "<-> is Euclidean (L2) distance, not cosine distance.", "<#> returns the negative inner product, not cosine distance.", "@@ is the PostgreSQL full-text search match operator and does not work on vectors."],
  "az35": ["B-tree indexes support ordering and equality on scalar values; they cannot accelerate vector similarity search.", null, "IVFFlat built on an empty table has no data to form its lists, so recall is poor; it should be built after loading.", "A GIN index on the text column helps full-text or JSON search, not nearest neighbor search on embeddings."],
  "az36": ["ivfflat.probes applies only to IVFFlat indexes, and lowering it would reduce recall rather than raise it.", "Changing the dimensions would require re-embedding every row with a different model and does not tune the index.", null, "m = 2 builds a very sparse graph, which lowers recall; it is also a build-time setting, not a query-time one."],
  "az37": ["Instructions in the prompt are not a security boundary; other customers' data would already have been retrieved and exposed to the model.", "Using a separate embedding model per customer is unnecessary and does not filter rows in the database.", "Sorting by customer_id only reorders rows; it does not exclude other customers' chunks from the results.", null],
  "az38": [null, "One embedding for the whole library blurs all content into one vector, making precise retrieval impossible.", "Storing model answers to whole documents is not ingestion for retrieval and skips chunking and embeddings.", "User questions are embedded at query time; storing them in the table does not build the searchable corpus."],
  "az39": ["Write-behind caches database writes and flushes them later; it adds risk and does not address read latency here.", null, "With no TTL and Redis as the only store, price changes are never refreshed and data can be lost.", "Using Redis as the primary database with persistence off means a restart loses the catalog."],
  "az40": ["Expiring session tokens manages logins; it does nothing to reuse answers for similar questions.", "Pub/Sub delivers messages to subscribers in real time; it stores nothing and cannot match questions by meaning.", null, "Question length says nothing about meaning, so sorting by length cannot find semantically similar questions."],
  "az41": [null, "allkeys-random evicts any key at random, including keys without a TTL that must be kept.", "noeviction evicts nothing and returns errors on writes when memory is full.", "allkeys-lfu can evict any key, including those without a TTL, and uses frequency rather than recency."],
  "az42": ["Redis is a fast cache but is not a good fit as the only store for relational data needing joins and foreign keys.", "Copying relational data into Cosmos DB adds a sync pipeline and loses SQL joins across the existing tables.", "Blob index tags are simple key/value filters on blobs; Blob Storage has no vector search or relational joins.", null],
  "az43": ["A missing partition key would cause a different error at write time; 429 means the request rate was too high.", null, "Consistency has nothing to do with 429s; Strong would actually increase RU cost for reads.", "Items over 2 MB fail with a 413 (request entity too large), not a 429."],
  "az44": ["Consistency affects read cost and guarantees; it does not reduce index maintenance on writes.", "Composite indexes add more index entries on every write, raising write RU cost.", "Indexing every path by default is what makes writes expensive here.", null],
  "az45": ["Millisecond lookups are exactly what an in-memory store like Redis is good at, so this is a good fit, not a poor one.", "Using Redis as a cache of vectors backed by another durable store is a recommended pattern.", null, "Data that may expire after an hour suits Redis TTLs well, so this is a good fit."],
  "az46": [null, "hnsw.ef_search only affects HNSW indexes, and lowering it would reduce recall anyway.", "@@ is the full-text search operator and cannot compare vectors; switching operators would break the query.", "One list and zero probes would make the index useless; probes must be at least 1 and more probes, not fewer, raise recall."],
  "az47": ["Competing receivers on one queue split the messages, so each order would reach only one of the three services.", null, "Basic tier has no topics and auto-forwarding chains entities; the sender would have to send to three queues itself.", "Sessions group related messages for ordered processing by one receiver; they do not copy a message to several services."],
  "az48": ["Abandoning returns the message to the queue, so a message that can never succeed is retried until the max delivery count wastes processing.", "Completing removes the message with no record, so the bad data is lost and cannot be inspected.", null, "Letting the lock expire just redelivers the message and increases the delivery count, delaying the inevitable."],
  "az49": ["Duplicate detection drops repeated MessageIds within a time window; it does not order messages per customer.", "Scheduled messages set a future enqueue time; they do not guarantee per-customer ordering.", "Auto-forwarding moves messages from one entity to another; it has no ordering or grouping semantics.", null],
  "az50": [null, "3 is a common custom setting, but the default max delivery count is 10.", "5 is not the default; Service Bus uses 10 unless you change it.", "30 is Event Grid's default max delivery attempts, not Service Bus's max delivery count."],
  "az51": ["Sessions group and order messages by SessionId; a resent message would still be delivered twice.", null, "Lock duration affects how long a receiver holds a message; it does nothing about duplicate sends.", "Receive-and-delete changes how messages are consumed and can lose messages; it does not detect duplicates."],
  "az52": ["A separate topic forces the sender to choose where to publish, instead of letting the subscription filter messages.", "Sessions provide ordered grouping by SessionId; they do not filter on application properties.", null, "Auto-forwarding moves messages between entities; it does not select messages by property."],
  "az53": ["Basic supports queues only; it has no topics or subscriptions.", "Premium supports topics but costs more; it is not the minimum tier that does.", null, "Partitioning is a scale feature and does not add topics to the Basic tier."],
  "az54": ["A timer that lists blobs is polling; it adds delay and cost as the container grows.", "Reading storage logs through a queue is an indirect, delayed approach and not the recommended trigger.", "Relying on the upload page misses blobs created by any other path and puts trust in client-side code.", null],
  "az55": [null, "A dead-letter destination stores events that could not be delivered; it does not filter what is sent.", "The retry policy controls delivery attempts after failure and does not filter events by type or name.", "The event schema only changes the envelope format; it does not filter events."],
  "az56": ["A shorter time-to-live makes Event Grid give up sooner, which would lose events faster.", null, "A second topic does not help because the undelivered events on the original subscription would still be dropped.", "The schema only changes the event format; neither schema keeps undelivered events."],
  "az57": ["Event Grid retries many times on a backoff schedule, not once; events are dropped or dead-lettered only when limits are reached.", null, "Event Grid does not store failed events in Service Bus; it dead-letters to a Blob Storage container if configured.", "Event Grid uses exponential backoff, not a fixed one-second retry forever, and gives up at max attempts or TTL."],
  "az58": ["Event Grid pushes discrete events and has no retention or replay by offset for high-volume streams.", "Service Bus queues are for business messages; they cannot sustain millions of events per second or let several consumers replay the stream.", null, "Queue Storage is a simple queue with competing consumers and no replay, far below the needed throughput."],
  "az59": ["This is five-field cron without seconds; Functions NCRONTAB needs six fields, so it is rejected.", null, "This fires at second 15 of every minute, which is once a minute, not every 15 minutes.", "*/15 in the hour field fires at hours 0 and 15, not every 15 minutes."],
  "az60": ["function.json files belong to the Python v1 programming model, not v2.", "host.json holds app-wide runtime and extension settings, not per-function triggers and bindings.", "local.settings.json stores app settings for local runs, not trigger and binding definitions.", null],
  "az61": [null, "A connection string with SharedAccessKey is key-based authentication, which is what the team wants to avoid.", "AzureWebJobsServiceBus = managedidentity is not a valid setting shape; identity-based connections use the __fullyQualifiedNamespace suffix.", "A client secret is a stored credential; identity-based connections with a managed identity need none."],
  "az62": ["anonymous lets anyone call the function without any key.", "admin requires the master key, which the requirement explicitly excludes.", null, "system is a key type used by extensions such as webhooks, not the auth level for callers of an HTTP trigger."],
  "az63": [null, "Dedicated (App Service) plans run instances continuously and do not scale to zero.", "Premium always keeps at least one ready instance, so it cannot scale to zero.", "Container Apps is not a Functions hosting plan here, and min replicas 1 keeps a replica running."],
  "az64": ["A timer trigger runs on a schedule rather than when an order arrives, and an input binding reads rather than writes.", null, "An HTTP trigger starts on web requests, and a Service Bus output binding sends messages rather than writing a Cosmos DB document.", "A Cosmos DB trigger fires on Cosmos DB changes, not on queue messages, and there is no Service Bus queue input binding."],
  "az65": ["host.json holds runtime configuration that is deployed with the app; it is not for connection secrets.", "requirements.txt lists Python packages to install, not app settings.", null, "Hard-coding connection details in function_app.py puts secrets in source control and needs code changes per environment."],
  "az66": ["Basic tier has the same lock behavior and removes features; it does not fix lost locks.", "Duplicate detection filters duplicate sends by MessageId; it does not stop redelivery after a lost lock.", "A max delivery count of 1 would dead-letter messages after one lost lock instead of processing them.", null],
  "az67": ["Event Grid supports CloudEvents for webhooks, so the schema choice alone does not put a subscription in a failed state.", null, "Subject filters are optional; a subscription with no filter receives all events and is valid.", "Dead-lettering is optional and affects only undelivered events, not subscription creation."],
  "az68": ["ClientSecretCredential needs a stored client secret, which defeats managed identity and must be rotated.", null, "UsernamePasswordCredential uses a user's password, does not work with MFA and is not for service workloads.", "AzureNamedKeyCredential wraps a shared account key for some services; it does not use Entra ID identities."],
  "az69": ["A system-assigned identity exists only after its app is created, so it cannot be granted access before deployment and is not shared.", "A storage account key is unrelated to Key Vault access and is a shared secret, not an identity.", null, "Access policies have no All Users option, and granting everyone access would violate least privilege."],
  "az70": [null, "Key Vault Administrator grants full data-plane control over keys, secrets and certificates, far beyond reading secrets.", "Contributor is a management-plane role; in RBAC mode it does not grant reading secret values.", "Secrets Officer can create, update and delete secrets, which is more than reading them."],
  "az71": ["Access policies control who can access the vault; they cannot restore a deleted secret.", "A private endpoint limits network access and has nothing to do with recovering deleted data.", "A rotation policy creates new key versions on a schedule; it applies to keys and cannot undelete a secret.", null],
  "az72": [null, "keyvault:// is not a recognized reference syntax in App Service app settings.", "$(KeyVault.DbPassword) looks like pipeline variable syntax; App Service does not resolve it.", "A bare version ID does not tell App Service which vault and secret to read, and it is not the reference syntax."],
  "az73": ["Availability tests check whether web endpoints respond; they do not know when secrets will expire.", null, "The Cosmos DB change feed tracks item changes in Cosmos DB, not Key Vault secret expiry.", "Calling the portal from a timer is not an API pattern, and polling is not the event-driven approach Key Vault offers."],
  "az74": ["Key Vault is for secrets; one vault per setting is costly and it has no labels or feature flags.", "Deployment slots stage app versions and have sticky settings, but they do not share settings or feature flags across apps.", null, "Values baked in at build time need a rebuild and redeploy to change, which is what the team wants to avoid."],
  "az75": ["Labels select environment-specific values; a missing label would not cause a Key Vault access failure.", "Soft delete protects against deletion; it does not grant any read access.", "A sentinel key triggers refresh; it has nothing to do with resolving a Key Vault reference.", null],
  "az76": ["Polling every key every second wastes requests, can hit throttling and can load a half-updated set.", null, "Restarting on every change causes downtime and still loads settings one change at a time.", "A single JSON secret in Key Vault read only at startup gives no dynamic refresh and puts non-secret settings in a vault."],
  "az77": ["The Log Analytics agent collects host and VM data, not in-app OpenTelemetry traces, and is retired.", "Writing to stdout gives unstructured logs without traces, metrics or dependency correlation.", "Instrumentation-key-only ingestion is retired in favor of connection strings, and the HTTP data collector API is for custom logs, not OpenTelemetry.", null],
  "az78": [null, "Separate resources with no link and no shared trace context cannot show the calls as one transaction.", "Sampling at 0% would keep no telemetry at all, so there would be nothing to correlate.", "Manually writing request IDs into logs is fragile and does not create spans or the transaction view."],
  "az79": ["top 5 by timestamp returns the five latest failed rows, not counts per 5-minute bucket, and has no time range.", null, "This returns five exception timestamps with no counting, bucketing or failed-request filter.", "It groups by success rather than time buckets, and after summarize the timestamp column no longer exists to filter."],
  "az80": ["requests records incoming calls to the app, not outbound calls it makes.", "traces holds log messages written by the app, not timed records of outbound calls.", null, "pageViews records browser page loads from client-side telemetry."],
  "az81": [null, "A CPU metric alert fires on resource usage, not on the number of errors counted by a log query.", "Availability tests probe a URL from outside; they do not count errors in the logs.", "A diagnostic setting routes logs to a destination; it does not evaluate thresholds or send email."],
  "az82": ["The ACR task log shows image build output, not what the running container prints at startup.", "The Key Vault audit log records vault access; it would show the app's own crash output only indirectly at best.", "Event Grid metrics show event delivery counts and failures, unrelated to container restarts.", null],
  "az83": ["Removing the connection string stops all telemetry, losing monitoring entirely.", null, "Logging request bodies adds far more data and may capture sensitive information.", "More Application Insights resources split the data but do not reduce total ingestion."],
  "az84": ["Owner is a management-plane role with full control; it is far beyond sending messages.", "Data Owner allows send, receive and manage on the data plane, more than the task needs.", null, "Contributor manages resources in the whole resource group and does not follow least privilege for sending."],
  "az85": ["Secrets can be up to 25 KB and are not limited to 32 bytes.", "Private key material never leaves the vault in plain text; apps call the vault to use the key.", "Secrets are stored encrypted but are not always hardware-protected; HSM protection is a feature of keys (Premium or Managed HSM).", null],
  "az86": [null, "Attaching one identity to many resources describes user-assigned identities; a system-assigned identity belongs to one resource.", "Managed identities need no client secret; Azure manages their credentials.", "Azure rotates managed identity credentials automatically; developers never rotate them with scripts."],
  "az87": ["A slot swap changes which version is live; it does not show error output while reproducing the issue.", null, "ACR webhooks notify on image push or delete events; they do not show application logs.", "Cosmos DB metrics show database throughput and errors, not the web app's own 500 errors and output."],
  "az88": ["Consistency level affects read guarantees and has nothing to do with authentication methods.", "A private endpoint restricts the network path but still allows account keys to be used.", null, "An App Configuration feature flag toggles app behavior; it cannot change how Cosmos DB authenticates requests."],
});
