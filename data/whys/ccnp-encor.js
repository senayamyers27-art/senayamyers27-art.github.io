CertHub.addWhys("ccnp-encor", {
  "en1": [null, "A three-tier design adds a separate core layer, which only pays off when many distribution blocks must be interconnected, so it adds cost and hops here.", "Spine-leaf is a data center fabric design, and access switches are leaves, not spines; it is overkill for one small campus building.", "A flat network with one VLAN everywhere creates a huge broadcast and spanning tree failure domain and gives up the Layer 3 boundary."],
  "en2": ["Spines do not connect to each other in a Clos fabric, and each leaf connects to every spine, not just one.", "A ring of leaves would make hop counts vary between leaf pairs; spines are the interconnect for all leaves, not WAN-only devices.", null, "Leaves do not link to each other in a spine-leaf fabric, and every spine (not one) connects to every leaf."],
  "en3": ["SSO is active/standby: only the active supervisor runs the control plane, so throughput is not doubled.", null, "SSO avoids a reboot altogether by switching to an already synchronized standby; it is not about faster image loading.", "HSRP is a first hop redundancy protocol between separate devices and is unrelated to supervisor redundancy inside one chassis."],
  "en4": ["This swaps the roles: WAN Edges peer with the Controllers over OMP but do not form the control plane themselves, and Controllers never forward user traffic.", "The Manager is the management plane and forwards no user traffic, and templates are pushed by the Manager, not the Validator.", "Controllers do not forward user traffic, and the SD-WAN overlay routing protocol is OMP, not BGP run by the Manager.", null],
  "en5": ["The Manager is the management plane for templates and monitoring; a new edge learns how to reach it from the Validator first.", "The Controllers handle OMP routing and policy, but a new edge must be authenticated and pointed to them by the Validator before it can join them.", "Catalyst Center is the campus controller and has no role in onboarding SD-WAN edge routers.", null],
  "en6": ["VXLAN is the SD-Access data plane encapsulation, and the fabric does not flood endpoint locations to every edge.", null, "OSPF or IS-IS may run in the underlay, but SD-Access does not track endpoints by advertising host routes as external LSAs.", "Cisco TrustSec SGTs form the policy plane; they carry group identity, not endpoint location."],
  "en7": [null, "Mapping endpoint identifiers to locators is the job of LISP, the SD-Access control plane.", "Tagging traffic with scalable group identifiers is the TrustSec policy plane, although the SGT does ride inside the VXLAN header.", "Catalyst Center provisions the fabric through its own management plane; VXLAN is a packet encapsulation, not a configuration mechanism."],
  "en8": ["SD-Access still requires a routed underlay (typically IS-IS built by LAN automation) beneath the overlay.", "Per-switch hand-written ACLs are the traditional model that SD-Access replaces with centrally defined group policy.", null, "SD-Access avoids campus-wide Layer 2 and spanning tree by using a routed underlay with a VXLAN overlay."],
  "en9": ["WRED drops packets early to manage TCP congestion; dropping voice packets would harm call quality rather than prioritize it.", null, "Policing only limits or drops excess traffic; it does not give the voice queue priority access to the link.", "Shaping works on egress traffic and delays packets, and delay is exactly what voice cannot tolerate."],
  "en10": ["Policing on the customer side still drops the bursts, just earlier; it does not smooth traffic to the contracted rate.", "Remarking everything as scavenger would lower the priority of all traffic and does nothing to keep it under 50 Mb/s.", "The customer cannot configure the provider's interface, and WRED manages queue depth, not conformance to a rate.", null],
  "en11": ["The RIB holds routes from the routing protocols; CEF builds the FIB from it but the RIB carries no Layer 2 rewrite data.", "The CAM table is for Layer 2 MAC-to-port switching decisions, not for building the rewrite header of a routed packet.", null, "TCAM holds ACL, QoS and prefix match entries for lookups, not the next-hop MAC rewrite information."],
  "en12": [null, "Exact 48-bit MAC matches are what the binary CAM does; TCAM exists precisely to match masked, non-exact entries.", "TCAM is specialized hardware memory with a fixed, limited size, which is why TCAM exhaustion is a real design concern.", "TCAM lookups happen in hardware for traffic that is not punted; punted packets are handled in software by the CPU instead."],
  "en13": [null, "Punted packets are not dropped; they are sent to the CPU for handling, and CEF is still enabled for other traffic.", "Punted packets are slower, not faster, because the CPU processes them in software instead of the fast path.", "Punted traffic goes to the active route processor's CPU, not to a standby supervisor, which does not forward traffic."],
  "en14": ["AF41 (34) is the usual marking for interactive video, not voice bearer traffic.", "CS3 (24) is commonly used for call signaling such as SIP or SCCP, not for the voice media stream itself.", null, "AF11 (10) is a low-priority assured forwarding class often used for bulk data, not real-time voice."],
  "en15": ["A type 2 hypervisor like VirtualBox runs on top of a general-purpose host operating system, which the scenario rules out.", null, "Docker containers share a host OS kernel and are not managed by vCenter as full virtual machines.", "A Kubernetes worker node runs containers on a host operating system, which contradicts having no OS underneath."],
  "en16": ["Routing between VRFs is a Layer 3 function; a standard vSwitch is a Layer 2 device with no routing tables.", "A vSwitch does not encrypt traffic between VMs; that would require a separate security product or IPsec in the guests.", "vSphere standard switches do not run spanning tree; they avoid loops by never forwarding frames from one uplink to another.", null],
  "en17": ["Multiple OSPF processes still install their routes into the same global routing table, where the overlapping 10.1.1.0/24 prefixes would collide.", "Private VLANs isolate hosts within one Layer 2 VLAN; they do not give two customers separate routing tables for overlapping subnets.", "PBR can steer traffic by source, but both customers' subnets are identical, so PBR cannot tell them apart or keep the routes separate.", null],
  "en18": ["The address does not carry over; IOS strips the IP configuration when the interface is moved into a VRF.", null, "The change takes effect immediately without a reload, and the interface is placed in the VRF right away.", "The command is accepted on an up interface; IOS just warns that the IP address has been removed."],
  "en19": [null, "An MTU mismatch causes fragmentation or drops of large packets, not recursive routing messages and tunnel flapping.", "Keepalive and OSPF hello timers are independent, and a timer difference does not create a recursive route to the tunnel destination.", "GRE works without IPsec; IPsec is only needed for encryption, not to keep the tunnel up."],
  "en20": ["GRE provides no encryption at all; it is IPsec that encrypts, while GRE is what carries the multicast.", "IPsec can carry unicast IP traffic on its own; its limitation with crypto maps is multicast and routing protocol traffic.", null, "GRE has no key exchange; IKE is still used to negotiate IPsec keys when GRE is protected with IPsec."],
  "en21": ["Transport mode keeps the original IP header and protects only the payload, so it does not add a new IP header.", null, "Aggressive mode is an IKEv1 phase 1 exchange for negotiating the IKE SA, not an IPsec encapsulation mode.", "Main mode is an IKEv1 phase 1 exchange that protects identities; it does not define how packets are encapsulated."],
  "en22": ["An EID is the endpoint's IP identity, not a router loopback, and an RLOC is a routable router address, not a MAC address.", "It is the reverse: RLOCs are underlay addresses and EIDs live in the overlay.", "VNIs belong to VXLAN and route distinguishers to MPLS VPNs; neither is a LISP EID or RLOC.", null],
  "en23": ["A 12-bit ID is the 802.1Q VLAN field; the VXLAN VNI is 24 bits, which is exactly how it escapes the ~4,000 limit.", "The VNI is its own 24-bit header field; the UDP destination port is fixed at 4789 and does not identify segments.", null, "A 20-bit label is MPLS, not VXLAN; the VNI is 24 bits, giving about 16 million segments."],
  "en24": [null, "Supporting DTP is not enough; at least one side must be desirable or trunk to actively request trunking.", "Dynamic auto on both ends is not a conflict and does not err-disable anything; the link simply stays access.", "No trunk forms at all, so there is no native VLAN negotiation; the link operates as an access port."],
  "en25": [null, "DTP modes on access ports in VLAN 30 do not affect whether the trunk between the switches carries that VLAN.", "VTP domain name mismatches affect VLAN database propagation, but the VLAN already shows as active on both switches.", "The EtherChannel hash only picks which member link a flow uses; it cannot block one VLAN from crossing the trunk."],
  "en26": ["Only one side needs to be LACP active; active with passive forms a bundle, while passive with passive does not.", "Passive is an LACP mode; PAgP uses desirable and auto.", null, "Active/passive is a valid LACP combination, so nothing is misconfigured and no ports are err-disabled."],
  "en27": ["Hashing decides which member carries a flow but does not suspend any port; a suspended port carries no traffic at all.", null, "EtherChannel has no primary member, and ports with different speeds are suspended rather than promoted.", "Shutting the port channel interface would take every member down, not suspend a single port."],
  "en28": ["EtherChannel does not use per-packet round-robin, and all bundled links actively forward traffic.", "LACP can bundle up to 8 active links (with up to 8 more in hot standby), not just one.", "EtherChannel never splits a single flow across links because that would cause packet reordering; this is normal behavior.", null],
  "en29": ["A backup port backs up a designated port on the same shared segment; it does not provide a path to the root.", "A designated port forwards toward a segment away from the root; it is not a backup for the root port.", "An edge port connects to end hosts and is not a path to the root bridge.", null],
  "en30": ["Rapid PVST+ runs one instance per VLAN, which is 200 instances and the opposite of what the team wants.", null, "PVST+ is still one instance per VLAN, and PortFast belongs on edge ports, not trunks.", "802.1D runs a single common tree, which reduces instances but cannot provide two different forwarding topologies for load sharing."],
  "en31": [null, "Bridge priority influences root bridge election within an instance but has nothing to do with region membership.", "Native and allowed VLANs affect what the trunk carries, not whether two MST switches consider themselves in the same region.", "VTP settings manage VLAN database propagation; MST regions are defined only by name, revision and the VLAN-to-instance map."],
  "en32": ["PortFast only skips listening and learning; it never shuts a port, and it does not count MAC addresses (that is port security).", "Root guard puts a port into root-inconsistent state rather than err-disabling it, and it is not the feature enabled on this port.", null, "Loop guard acts when BPDUs stop arriving on a non-designated port; here BPDUs started arriving on an edge port."],
  "en33": ["BPDU filter stops sending and processing BPDUs, which can create loops; it does not block a port that hears a superior BPDU.", null, "PortFast only speeds an access port to forwarding and offers no protection against a new switch claiming root.", "UplinkFast speeds recovery of an access switch's uplinks after a root port failure; it does not protect root placement."],
  "en34": ["This reverses the protocols: EIGRP uses DUAL and OSPF is the link-state protocol that runs SPF.", "EIGRP is not link-state, and it is EIGRP (with variance), not OSPF, that supports unequal-cost load balancing.", "Only EIGRP uses DUAL, and it is OSPF, not EIGRP, that requires a backbone area 0.", null],
  "en35": ["Variance is an EIGRP feature, not OSPF-only, and with variance 2 the R3 path qualifies for installation.", "The lower reported distance does not make R3 the only path; R2 has the best feasible distance and stays the successor.", null, "EIGRP unequal-cost load balancing shares traffic in proportion to the metrics, not equally."],
  "en36": [null, "Auto-summary summarizes at classful boundaries and does not stop the hub from sending queries to spokes.", "Split horizon prevents advertising a route back out the interface it was learned on; it does not limit the query scope.", "Variance enables unequal-cost load balancing and has no effect on which neighbors are queried."],
  "en37": [null, "Duplicate router IDs cause different problems, and routers are expected to have different IDs anyway, so 'mismatched' IDs are normal.", "The OSPF process ID is locally significant and does not need to match between neighbors.", "Reference bandwidth changes computed costs but is not exchanged in hellos or DBDs, so it cannot block adjacency."],
  "en38": ["A stub area blocks Type 5 LSAs but does not allow an ASBR inside the area to redistribute routes.", "A totally stubby area blocks Type 3, 4 and 5 LSAs and also does not permit an internal ASBR.", null, "The backbone area 0 carries Type 5 LSAs normally and cannot be configured as a stub or NSSA."],
  "en39": ["An NSSA still carries inter-area Type 3 routes and allows Type 7, and every router in the area must agree on the area type or adjacencies fail.", null, "The area range command summarizes on an ABR; on internal routers it does nothing, and it would not replace routes with a default.", "summary-address is used on an ASBR to summarize external routes, not to make an area receive only a default route."],
  "en40": ["Priority 255 on both sides still runs a DR/BDR election; it only influences who wins it.", "Interface cost affects path selection but does not change the network type or remove the DR election.", "passive-interface stops sending hellos, so no adjacency would form at all.", null],
  "en41": ["TCP port 179 is BGP; OSPFv3 runs directly over IPv6 as protocol 89, just as OSPFv2 does over IPv4.", "OSPFv3 keeps the same network types as OSPFv2, and point-to-point links do not elect a DR.", "OSPFv3 still uses a 32-bit router ID and forms neighbors over IPv6 link-local addresses, not IPv4 addresses.", null],
  "en42": ["A working session is in the Established state; Active means the TCP session is not up.", null, "Received but not installed prefixes relate to an Established session and best-path or RIB issues, not the Active state.", "An administratively shut neighbor shows Idle (Admin), not Active."],
  "en43": [null, "AS path length is checked only after weight, local preference and locally originated routes.", "MED is compared late, after weight, local preference, locally originated, AS path and origin.", "Local preference comes second, and the highest value is preferred, not the lowest."],
  "en44": ["Weight is Cisco-proprietary and local to one router; it is never sent to iBGP peers.", "MED is sent to a neighboring AS to influence inbound traffic; it does not steer this enterprise's outbound choice.", null, "AS path prepending lengthens paths advertised to a neighbor to influence inbound traffic, not outbound."],
  "en45": ["A higher-AD static default is a floating backup that applies to all traffic, not just the guest subnet.", null, "Route maps for PBR are applied inbound on the ingress interface; an outbound route map on the WAN interface does not do policy routing.", "OSPF cost changes affect all traffic following the route, not just packets sourced from the guest subnet."],
  "en46": ["VRRP is an open standard, and unlike HSRP it may use a real interface address as the virtual IP.", "VRRP enables preemption by default; it is HSRP that has preemption disabled by default.", "VRRP uses master and backup roles; active and standby are HSRP terms.", null],
  "en47": ["Static NAT is one-to-one; you cannot map many inside hosts to the same single address this way.", "Dynamic NAT without overload lets only one inside host use 203.0.113.10 at a time.", null, "NAT64 translates between IPv6 and IPv4; it does not let many IPv4 hosts share one public IPv4 address."],
  "en48": [null, "Stratum 1 is a server directly attached to a reference clock such as GPS, not a client of a stratum 2 server.", "A client never reports the same stratum as its source; it is always one higher.", "Stratum 16 means unsynchronized, but the router is synchronized to its server."],
  "en49": [null, "A few milliseconds of drift in log timestamps is well within what NTP delivers, so PTP is unnecessary.", "Laptop clock sync over the internet is a classic NTP use case and cannot benefit from PTP hardware timestamping.", "Certificate validity checks need only coarse accuracy, which NTP easily provides."],
  "en50": ["PIM runs between multicast routers to build distribution trees, not between a host and its router.", "MSDP shares source information between RPs in different PIM domains; hosts never use it.", null, "IGMP snooping is a switch feature that listens to IGMP; the host itself sends IGMP, not snooping."],
  "en51": ["Flood and prune is PIM dense mode behavior; sparse mode sends traffic only where receivers explicitly join.", null, "Multicast group addresses are not handed out by the RP; applications or administrators choose them.", "The RP forwards multicast down a shared tree; it does not convert multicast into unicast streams."],
  "en52": ["The IGMP querier election picks which router queries hosts on a LAN; it does not drop multicast data packets.", "TTL scoping can limit how far multicast travels, but the drop here is due to the arrival interface, not the TTL.", "The PIM DR election selects which router sends joins or registers on a LAN; it does not validate the incoming interface.", null],
  "en53": ["IGMPv2 joins are (*,G) and cannot name a source, so any-source multicast still needs an RP.", "PIM dense mode floods and prunes, and IGMPv1 cannot specify a source either.", "Bidirectional PIM still relies on an RP address (even a phantom one) as the root of the shared tree.", null],
  "en54": ["debug all on a busy production router can flood the CPU and crash or hang it before you ever filter the log.", null, "Unfiltered debug ip packet to the console is one of the riskiest debugs, since console logging is slow and every packet generates output.", "Reloading a production router causes an outage and does not limit the debug to one host."],
  "en55": [null, "A loop would show repeating hops or TTL expirations, and later hops responding shows the destination path works.", "If hop 4 dropped all traffic toward the destination, the probes to later hops would also fail.", "Traceroute keeps increasing the TTL for each hop, and later hops responded, so the TTL clearly reached past hop 4."],
  "en56": ["SNMPv2c community strings are sent in clear text and provide no encryption, read-only or not.", "SNMPv1 also sends the community string in clear text; its length adds no encryption.", null, "Using traps changes the direction of messages but not the security model; v2c still has no authentication or encryption."],
  "en57": ["The trap level is a threshold, so more severe messages (0-3) are sent too, not only level 4.", null, "Levels 5-7 are less severe than warnings and are excluded; the range goes toward lower numbers.", "Levels 5-7 (notifications, informational, debugging) are exactly the messages filtered out by a warnings threshold."],
  "en58": ["SPAN copies raw packets to a local analyzer and does not summarize per-application usage on the router.", "IP SLA probes measure reachability, latency and jitter, not which hosts and applications use bandwidth.", "SNMP interface counters only give total bytes and packets per interface, with no per-flow or per-application detail.", null],
  "en59": ["Local SPAN can only copy to a destination port on the same switch, not to a device three routed hops away.", "RSPAN uses a dedicated VLAN, so it only works across Layer 2 trunks; it cannot cross routed hops.", null, "Port security limits MAC addresses on a port and does not mirror traffic anywhere."],
  "en60": [null, "A floating static route only takes over once the primary route is removed; it cannot detect the DNS server failure itself.", "SNMP traps only notify people, and a link-down trap would not even fire because the link stays up.", "NetFlow exports traffic statistics for analysis and has no effect on the routing table."],
  "en61": [null, "Plug and Play automates day-0 onboarding of new devices and does not analyze user experience.", "SWIM manages and upgrades software images; it does not correlate client or application health.", "The template editor pushes configuration and provides no historical troubleshooting data."],
  "en62": ["RESTCONF uses HTTPS (TCP 443) with JSON or XML, not SSH on port 830.", "SNMPv3 uses MIBs over UDP 161, not YANG models over SSH, and is rarely used for configuration changes.", null, "gNMI runs over gRPC on HTTP/2, not HTTP/1.1 or SSH port 830."],
  "en63": ["Plain login checks only the single password configured on the line, not the username database.", null, "transport input none blocks all remote protocols on the VTY lines, so nobody could log in remotely at all.", "exec-timeout 0 0 only disables the idle timeout, and the enable secret protects privileged mode, not the VTY login method."],
  "en64": ["It is TACACS+, not RADIUS, that encrypts the whole packet body; RADIUS encrypts only the password field.", "RADIUS runs over UDP, not TCP, and it combines authentication and authorization, so it cannot authorize individual commands.", "TACACS+ is the right protocol, but the reason is wrong: it separates authentication and authorization rather than combining them.", null],
  "en65": ["Listing TACACS+ first does not deny the login; an unreachable server returns an error, so the router moves on to the next method.", "The method list ends with local, not none, so the router still requires a local username and password.", "RADIUS is not in this method list at all; only tacacs+ and then local are configured.", null],
  "en66": ["MAB uses the device's MAC address as its identity and involves no user credentials or supplicant.", null, "WebAuth collects credentials or terms acceptance through a browser portal, not through a supplicant.", "Port security with sticky MAC just learns and limits MAC addresses locally; it never contacts a RADIUS server."],
  "en67": [null, "EAP-TLS runs inside 802.1X and requires a supplicant to present the certificate, which the printer does not have.", "WebAuth needs a person using a browser to log in, which a printer cannot do.", "Port security limits MAC addresses locally but cannot have ISE assign the device to a VLAN."],
  "en68": ["permit ip any any matches everything first, so the Telnet deny below it is never reached.", "The source and destination are reversed: this matches traffic from the server to the client subnet, not the Telnet sessions from the clients.", null, "This denies all IP traffic from the subnet to the server, not just Telnet, and the second line would only permit Telnet elsewhere."],
  "en69": ["Near the destination is the guidance for standard ACLs; an extended ACL there lets unwanted traffic cross the network before being dropped.", null, "A loopback interface does not carry transit traffic, so an ACL there would not filter traffic between networks.", "An ACL on VTY lines (access-class) only controls remote management access to the device, not general traffic."],
  "en70": ["uRPF drops packets with spoofed source addresses; it does not rate-limit legitimate-looking ICMP and SSH aimed at the router.", "An outbound interface ACL filters traffic leaving the interface, and traffic to the router itself does not pass through it outbound.", "Storm control limits broadcast, multicast or unknown unicast floods on switch ports, not unicast traffic addressed to a router.", null],
  "en71": ["One low rate for everything can drop routing protocol updates and cause adjacencies to flap.", "Dropping all ICMP and SNMP breaks legitimate monitoring and troubleshooting; CoPP should rate-limit them instead.", null, "CoPP is applied to the control-plane interface; per-interface policies do not see all traffic punted to the CPU."],
  "en72": [null, "Plain HTTP exposes the credentials in transit, and a password stored in the script can leak through source control.", "HTTPS encrypts the URL on the wire, but query strings are still written to server and proxy logs and browser history.", "Restricting HTTP to a management VLAN still sends credentials unencrypted to anyone who can capture that VLAN."],
  "en73": [null, "Filtering only on ports without state is a basic packet filter, which is less capable than even a traditional stateful firewall.", "An NGFW can route, but it does not replace the routers in an enterprise network.", "An NGFW inspects up to the application layer; a device limited to Layer 2 frames would be less capable, not more."],
  "en74": ["A stateful firewall inspects traffic at the time it passes and cannot find or quarantine files already sitting on endpoints.", "An IPsec VPN concentrator only encrypts tunnels and has no visibility into files on endpoints.", null, "802.1X controls who connects to a port and does not track or quarantine files."],
  "en75": ["TrustSec does not create a VLAN per user; its point is group policy that is independent of VLANs and IP addresses.", null, "TrustSec does not use IPsec; its link encryption option is MACsec, and enforcement is by SGACLs, not encryption.", "Port security limits MAC addresses per port and is not how TrustSec classifies or enforces policy."],
  "en76": ["IPsec tunnel mode encrypts at Layer 3 between IP peers, not hop by hop on an Ethernet link between switches.", "TLS 1.3 protects individual application sessions above Layer 4, not all frames on a link.", "SSH version 2 encrypts management sessions to a device, not the user traffic crossing the switch link.", null],
  "en77": ["DAI validates ARP packets and depends on DHCP snooping's bindings, but it does not drop rogue DHCP offers.", "IP source guard filters spoofed source IPs using the binding table and does not block DHCP server messages.", "Storm control limits broadcast rates and would not stop a rogue server's low-volume DHCP offers.", null],
  "en78": ["802.1X with PEAP needs a supplicant on the device, which this scenario explicitly rules out.", null, "MAB authenticates by MAC address with no user interaction, so there is no browser page to accept a policy.", "MACsec encrypts Ethernet frames on a link and does not present an acceptable use policy to users."],
  "en79": [null, "Allowing both would be transport input ssh telnet (or all); transport input ssh permits only SSH.", "transport input applies to VTY lines only and has no effect on the console port.", "transport input controls remote login protocols; it has nothing to do with SNMP versions."],
  "en80": ["The ports are swapped: TCP 49 is TACACS+ and UDP 1812 is RADIUS.", "TACACS+ uses TCP, not UDP, and it is RADIUS that encrypts only the password while TACACS+ encrypts the whole body.", null, "RADIUS uses UDP, not TCP, and it encrypts only the password; TACACS+ is the one that encrypts the whole payload."],
  "en81": ["Python dictionaries from json.loads do not support attribute access, and status is a string value, not a method to call.", null, "A dictionary is indexed by key, not by position, so data[0] raises a KeyError.", "The keys are in the wrong order: status is nested inside interface, not the other way around."],
  "en82": ["JSON requires double quotes, and True with a capital T is Python syntax; JSON uses lowercase true.", "Parentheses are a Python tuple; JSON arrays must use square brackets.", "JSON object keys must be double-quoted strings; unquoted keys are JavaScript object syntax, not valid JSON.", null],
  "en83": ["The transport and port come from the protocol, such as NETCONF over SSH 830 or RESTCONF over HTTPS, not from YANG.", "Encryption is provided by SSH or TLS underneath NETCONF or RESTCONF; YANG only describes data.", null, "YANG is language-neutral; a Python library such as ncclient or requests is chosen separately by the script author."],
  "en84": [null, "A successful creation returns 201 Created, not 401.", "A path that does not exist returns 404 Not Found, not 401.", "An internal server error is reported as 500, a server-side 5xx code, not the client-side 401."],
  "en85": [null, "POST creates a new resource or triggers an action, so it is not the read-only choice.", "PUT replaces a resource with the data sent, which changes the configuration.", "DELETE removes a resource, which is the opposite of a read-only request."],
  "en86": ["Without a token in the X-Auth-Token header, a GET to the device list is rejected with 401 Unauthorized.", "Port 830 is NETCONF over SSH; the Catalyst Center Intent API is a REST API over HTTPS.", null, "ISE MAC registration is for network access control of endpoints, not for authenticating API calls to Catalyst Center."],
  "en87": ["The Validator handles device authentication and onboarding and does not provide a central inventory and health API.", null, "Querying each WAN Edge separately over NETCONF does not scale and lacks the fabric-wide view the Manager provides.", "The Controllers run OMP and policy; an SNMP MIB there would not give the full per-branch inventory and tunnel health view."],
  "en88": ["Ansible from a cron job on a server is off-box and runs on a schedule, not immediately when the config changes.", "NetFlow exports traffic flow records and has no way to export or back up configurations.", "IP SLA measures reachability to a target; tracking the TFTP server does not copy any configuration.", null],
  "en89": ["Puppet traditionally uses an agent on each node pulling from a Puppet server, and its manifests are not YAML playbooks.", "Chef uses agents (chef-client) and Ruby-based recipes and cookbooks, not agentless YAML playbooks.", "SaltStack with minions is agent-based; the minion is the agent running on each managed node.", null],
  "en90": ["This is reversed: agentless tools use SSH or APIs, while agent-based tools run a local daemon.", null, "Agentless tools such as Ansible are widely used to manage network devices.", "Agent-based tools like Puppet and Chef typically rely on a central server that agents pull configuration from."],
  "en91": [null, "Looping over JSON keys inspects the response body; it does not repeat the request based on its status code.", "A single if runs once and cannot retry; it also does nothing specific for a 503 response.", "KeyError is raised for missing dictionary keys, not for HTTP status codes like 503."],
  "en92": ["AI analytics depends on telemetry; without data there is nothing to baseline or analyze.", "No analytics tool can guarantee changes will not cause outages; AI helps detect and explain problems, not prevent all of them.", null, "Catalyst Center AI analytics works on assurance data and does not replace routing protocols for path selection."],
  "en93": ["Full admin access removes the safety net; a wrong suggestion could take down the network with nothing to stop it.", null, "Disabling logging removes the audit trail needed to review and roll back the assistant's changes.", "Skipping change windows puts unreviewed changes into production at any time, increasing the risk of outages."]
});
