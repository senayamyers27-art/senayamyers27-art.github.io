CertHub.addWhys("cloud-plus", {
  "cp1": [null, "With IaaS, even from a hardened image, the customer still owns guest OS patching and the runtime, which is exactly what the team wants to avoid.", "SaaS delivers a finished application built by the vendor, so the team could not deploy or control its own code.", "Colocation only rents rack space, power and cooling; the company would manage the hardware, OS and runtime itself."],
  "cp2": ["The provider maintains the hypervisor and physical hosts, but in IaaS it has no responsibility for patching the guest OS inside customer VMs.", "There is no such split in IaaS; the provider does not apply any patches, critical or otherwise, to a customer's guest OS.", null, "The image publisher only supplies the starting image; once the VM is launched, keeping its OS patched is the customer's duty."],
  "cp3": ["A community cloud is infrastructure shared by several organizations with common requirements, not one hospital bridging its own data center and a public cloud.", "A private cloud alone would not let the website burst into public cloud capacity during peaks.", "Multicloud means using several public cloud or SaaS vendors; the scenario combines an on-premises data center with one public cloud.", null],
  "cp4": ["Separate private clouds would give each agency its own isolated environment, so nothing would be shared among them.", "A public cloud is open to any customer, but the agencies want an environment restricted to organizations like themselves.", "Hybrid cloud describes connecting private or on-premises infrastructure with public cloud, not several organizations sharing one environment.", null],
  "cp5": [null, "Separate hosts in a single availability zone still sit in one data center, so a facility failure would take everything down.", "Regions on different continents add so much latency that synchronous database replication becomes impractical.", "Redundant power supplies protect one server, but a single host in a single data center is still a single point of failure."],
  "cp6": ["Spot instances run on shared multitenant hardware and can be reclaimed; a private subnet only changes network placement, not physical isolation.", "Burstable instances are cheap shared-tenancy VMs that earn CPU credits; they still share physical servers with other customers.", null, "A larger instance size is still a VM on shared hardware, so it does not guarantee a physical server used only by you."],
  "cp7": ["Warm standby keeps a scaled-down but running copy of the whole stack, including app servers, not just replicated data with images waiting to launch.", "Backup and restore keeps only backups and rebuilds everything after a disaster; there is no continuously replicated live database.", "Multisite active-active runs full production in both locations serving traffic at the same time, far more than idle images.", null],
  "cp8": ["A standard block volume attaches to one instance at a time and has no shared file system semantics for twenty servers writing concurrently.", null, "Archive object storage is for rarely accessed data, has slow retrieval and is not mounted as a standard file system.", "Instance store is local, temporary disk on each host, so every server would see its own separate copy and data is lost when the instance stops."],
  "cp9": ["Block storage is provisioned as disk volumes attached to servers; it is not served over HTTPS to apps and does not scale to billions of objects cheaply.", null, "File storage exposes a mounted file system over NFS or SMB for servers, not an HTTP API with near-unlimited, pay-per-GB capacity.", "Instance store volumes are temporary local disks tied to a single VM, so they lose data and cannot scale or serve content directly."],
  "cp10": [null, "Mounting a bucket with FUSE translates file operations into HTTP object calls, giving high and unpredictable latency unsuitable for a transactional database.", "A cold file share tier is optimized for low storage cost on infrequently accessed files, not guaranteed low-latency IOPS.", "Archive storage must be restored before it can be read, and even expedited retrieval takes minutes; it cannot serve live database I/O."],
  "cp11": [null, "Provisioned IOPS block volumes are premium, performance-priced storage, the opposite of what rarely read logs need.", "The standard object tier is priced for frequent access, so seven years of rarely read logs would cost much more than an archive tier.", "A premium file share uses high-performance storage billed at a high rate, which wastes money on data that is almost never read."],
  "cp12": ["A reserved VM bills around the clock even when idle, and polling every minute adds delay and wasted work.", null, "An always-on three-node container cluster costs money when there are no uploads, which the team wants to avoid.", "A nightly batch means thumbnails appear hours late, and the job still does not respond to each upload as it happens."],
  "cp13": [null, "That describes virtual machines; containers do not include a full guest OS or a hypervisor, they share the host kernel.", "Containers run widely on cloud infrastructure, including managed container and Kubernetes services.", "It is the reverse: VMs use hardware virtualization and offer stronger isolation, while containers only use OS-level isolation."],
  "cp14": ["Splitting a monolith usually adds network calls, because in-process function calls become calls between services.", "Microservices typically give each service its own data store; a single shared schema is a monolith trait that couples services together.", null, "Debugging across many services is harder, since a request crosses several processes and needs tracing to follow."],
  "cp15": [null, "A public IP on each instance makes them directly reachable from the internet, which violates the no-inbound requirement.", "Routing a private subnet straight to an internet gateway effectively makes it public and still needs public IPs, exposing the instances.", "Peering connects two private networks to each other; it gives no path to the internet for downloading updates."],
  "cp16": ["An IPsec VPN encrypts traffic but still travels over the public internet, so latency and bandwidth vary.", null, "A client VPN connects individual devices, still rides the internet and is not built for bulk site-to-cloud transfers.", "TLS encrypts data in transit, but a public endpoint still uses the internet with its variable performance."],
  "cp17": ["A layer 4 load balancer routes on IP address and port only and cannot see the HTTP URL path.", "DNS round robin just hands out different IP addresses per lookup; DNS never sees the URL path of a request.", null, "A NAT gateway translates outbound private addresses for internet access; it does not distribute or route inbound HTTP requests."],
  "cp18": ["A bigger, compute-optimized instance does not shorten the physical distance between Asia and Europe, which causes the latency.", "A database read replica helps with query load, but the slowness is static files crossing continents, not the database.", null, "The load balancer idle timeout only controls how long idle connections stay open; it does not speed up delivery to distant users."],
  "cp19": [null, "On-demand pricing is the most expensive option for a constant 24/7 workload because it carries no commitment discount.", "Spot capacity can be reclaimed at short notice, which is unacceptable for an always-on ERP system.", "Rewriting an ERP as functions is a huge, risky project, and steady 24/7 load is often more expensive per invocation than reserved VMs."],
  "cp20": [null, "Dedicated hosts are among the most expensive options and address licensing or isolation, not lowest cost for interruptible jobs.", "Three-year reservations suit steady always-on usage; they commit spend for years for work that can simply use cheap spare capacity.", "On-demand instances cost full price, and running them in two regions adds cost without benefit for restartable batch jobs."],
  "cp21": [null, "Chargeback actually bills departments, moving money between cost centers, which finance explicitly does not want.", "One shared account with no tags makes it impossible to attribute spend to departments at all.", "Reserved instances lower costs for steady workloads but do nothing to report spend back to departments."],
  "cp22": ["A canary sends a small percentage of real traffic to the new version first, rather than switching everything between two full environments.", "A rolling deployment updates instances in batches within one environment instead of flipping traffic to a separate full copy.", null, "An in-place upgrade changes the existing servers directly, so there is no idle second environment to switch back to."],
  "cp23": ["Blue-green switches all traffic from one full environment to the other at once, not a gradual 5% start.", null, "A big bang release pushes the new version to everyone at the same moment, with no small initial slice.", "The recreate strategy stops the old version completely and then starts the new one, causing downtime and no gradual exposure."],
  "cp24": ["Blue-green requires a second full environment, which the scenario explicitly rules out.", "Terminating all instances drops capacity to zero and causes an outage until the new ones are ready.", "A canary in a separate region needs extra infrastructure there and does not describe how to update the existing twelve servers.", null],
  "cp25": ["Refactoring into microservices requires major code changes and takes far longer than four months for 200 VMs.", null, "Repurchasing replaces applications with SaaS products; it cannot quickly cover 200 varied VMs and changes the software.", "Retiring means decommissioning systems that are no longer needed; the company still needs these workloads running."],
  "cp26": ["Rehosting would move the SQL Server VM as-is to a cloud VM, not switch to a managed database service.", null, "Retain means leaving the workload where it is for now, but the database is actually being moved.", "Refactoring redesigns the application architecture itself, while here only the connection string changes."],
  "cp27": ["Replatforming moves the same application with small optimizations; here the company drops its software for a different product.", "Retain means keeping the application where it is, but the on-premises CRM is being shut down.", "Rehosting lifts the same application onto cloud VMs unchanged, not replacing it with a subscription service.", null],
  "cp28": [null, "A VPN adds encryption on top of the same internet link; it cannot increase the available bandwidth.", "Compression may shrink the data somewhat, but already-compressed archives gain little and months of transfer would remain on a busy link.", "Parallel streams cannot exceed the 200 Mbps link capacity, and they would saturate a link that is already busy."],
  "cp29": [null, "A dependency shows the database is in use, which argues for keeping it, not retiring it.", "SaaS is a repurchase option for replacing software; a dependency between servers says nothing about rehosting as SaaS.", "Nightly calls between two servers reveal an application dependency, not a shortage of IP addresses in the subnets."],
  "cp30": ["A cooldown period only controls how long autoscaling waits between actions; it does not make each server boot faster.", null, "Manual configuration is slow and error-prone and would make servers less consistent, the opposite of the goal.", "A larger instance may install packages slightly faster but still runs the same 40-minute boot-time setup."],
  "cp31": [null, "Compute-optimized instances have a high vCPU-to-RAM ratio, adding CPU the cache does not need while still running short of memory.", "Storage-optimized instances focus on fast local disk throughput, not on the large RAM an in-memory cache requires.", "GPU-accelerated instances add graphics or ML accelerators, which an in-memory cache cannot use, at a high price."],
  "cp32": ["Pre-zeroing space is a thick-provisioning (eager-zeroed) behavior; thin volumes allocate on write rather than zeroing up front.", "Thin provisioning is a space allocation method and has no effect on whether encryption at rest can be used.", "Thin-provisioned volumes can be snapshotted normally; snapshots are not the risk created by overcommitting the pool.", null],
  "cp33": ["In many managed database services the multi-AZ standby is passive and only takes over on failover, so it cannot serve the reporting queries.", "Backup retention controls how long backups are kept; it does nothing to reduce query load on the primary.", "An archive tier is for rarely accessed data and would make an active database far slower, not relieve it.", null],
  "cp34": ["IaC makes environments consistent, but code and infrastructure still need testing; templates can contain bugs too.", "IaC does not by itself lower spending; it can just as easily deploy expensive resources consistently.", null, "Consistently deployed environments still need monitoring to detect failures, performance issues and security events."],
  "cp35": ["Configuration drift is the problem of servers slowly diverging from their intended state, which this practice is designed to prevent.", "In-place upgrading modifies running servers, which is exactly what the policy forbids.", null, "Vertical scaling means resizing a server to more or less CPU and memory, unrelated to how changes are applied."],
  "cp36": ["A load test measures how the system behaves under heavy traffic, not a quick check of key functions after a release.", null, "A penetration test is a security assessment that tries to exploit vulnerabilities, not a post-deploy functionality check.", "User acceptance testing is business users confirming requirements before release, not an automated script run just after deployment."],
  "cp37": [null, "A single VM running Docker has no cross-node scheduling, no zone spread and becomes a single point of failure.", "Serverless functions run short event-driven code; they do not orchestrate long-running container replicas across nodes.", "A file share can store files but provides no scheduling, self-healing or replica management for containers."],
  "cp38": [null, "Deleting source backups removes the ability to roll back if the new environment turns out to have problems.", "Disabling monitoring right after cutover hides exactly the problems the team most needs to catch.", "Removing DNS records for the new environment would cut users off from the system that was just migrated."],
  "cp39": ["Resizing an instance usually requires a stop and restart, and vertical scaling has an upper limit, so it is not the zero-downtime way to add capacity.", "Archive storage is for rarely read data; a web tier cannot run from it, and it would add no compute capacity.", null, "A longer DNS TTL only affects how long resolvers cache records; it adds no capacity to the web tier."],
  "cp40": ["Horizontal scaling adds more servers, but this application cannot run on more than one server.", "A CDN offloads static content delivery, but it will not relieve a CPU-bound application server.", "Read replicas are a database feature for offloading reads; they do not apply to a CPU-bound application tier.", null],
  "cp41": ["Removing the health check stops the load balancer from detecting failed servers; it does not add capacity earlier.", "A longer cooldown makes autoscaling wait even longer between actions, so capacity arrives later still.", "Raising the scale-out threshold means the policy reacts even later to the Monday surge.", null],
  "cp42": ["Host CPU metrics show resource usage per machine, but cannot attribute latency of one request to a specific service hop.", null, "An archived daily log is not correlated per request and is hard to use for tracking latency across six services.", "An uptime check only confirms the home page responds; it says nothing about which checkout service is slow."],
  "cp43": [null, "Sending every alert to the whole team spreads the same noise to more people and worsens alert fatigue.", "Disabling CPU alerting entirely hides real, sustained CPU problems along with the noise.", "Lowering the threshold would fire even more alerts, making the noise problem worse."],
  "cp44": ["Local logs stay on each VM and disappear when an instance is deleted, and they are not searchable in one place.", "Dashboard screenshots capture pictures of metrics, not searchable log data, and cannot meet a retention requirement.", null, "Bigger boot disks only store more logs locally; they are still lost when the VM is deleted and not centrally searchable."],
  "cp45": ["Each differential already contains all changes since Sunday, so Monday's and Tuesday's are redundant; applying every nightly backup describes incremental restores.", "A differential only contains changes since the last full, so it cannot restore the server without Sunday's full backup.", null, "Older full backups are unnecessary; only the most recent full plus the latest differential are needed."],
  "cp46": ["More snapshots in the same account are just as exposed; an attacker with account access can delete them all again.", null, "Encryption protects confidentiality, but an attacker with account access can still delete encrypted snapshots.", "Keeping a single snapshot reduces recovery options and does nothing to stop an attacker deleting it."],
  "cp47": [null, "Running backups less often increases potential data loss and still does not prove the backups can be restored.", "Longer retention just keeps more backups that may be unusable; it does not verify recoverability.", "Moving backups to an archive tier lowers cost but slows restores and still does not prove the data is recoverable."],
  "cp48": ["Patching everything at once during peak hours risks a full outage and exposes all users if the patch fails.", "Critical patches fix vulnerabilities on all servers, not just those currently showing errors; unpatched servers stay exposed.", "Waiting for the next major release leaves a known critical vulnerability open for a long time.", null],
  "cp49": ["Under shared responsibility the provider forces the upgrade, but testing application compatibility and choosing the timing are the customer's job.", "Disabling backups does not block the upgrade and removes the ability to recover if something goes wrong.", null, "Instance size has no bearing on engine version support, so a larger instance would still be upgraded."],
  "cp50": ["The server is heavily underused, so moving to a larger size would increase waste and cost.", "Adding a second identical server doubles cost for capacity that is already mostly unused; this is not a redundancy question.", "Reserving the current oversized instance for three years locks in the waste instead of eliminating it.", null],
  "cp51": ["About 4.3 minutes per month corresponds to 99.99% availability, not 99.9%.", "About 7 hours per month corresponds to roughly 99% availability, ten times more downtime than 99.9% allows.", "About 1 minute per month is closer to 99.998% availability, far stricter than a 99.9% SLA.", null],
  "cp52": ["A larger standard-tier bucket keeps everything at the most expensive tier and never deletes data after seven years.", "Manual monthly moves and deletions are error-prone and labor-intensive when a lifecycle policy can automate both.", "Versioning with no expiration keeps every object version forever, increasing storage and never enforcing the seven-year limit.", null],
  "cp53": ["Host disk metrics show server resource usage, not whether users can actually sign in from different continents.", "A monthly penetration test looks for security weaknesses; it does not continuously check that sign-in works.", null, "Keeping logs longer helps investigate after the fact but does not proactively detect a broken sign-in page."],
  "cp54": [null, "Committing a key to any repository, even a private one, spreads a long-lived secret to everyone with repo access and its history.", "An environment variable still holds a long-lived key on the VM that can leak, and manual rotation is error-prone.", "A public bucket is readable by anyone on the internet; obscurity is not access control."],
  "cp55": [null, "Sharing the root password destroys accountability and exposes the most powerful credential to everyone.", "The root user generally cannot be deleted, and some account tasks still require it.", "Root access keys give unrestricted programmatic access and are a major risk; automation should use scoped roles."],
  "cp56": ["Separate cloud accounts synced by script still exist independently, so disabling someone in the directory may not remove cloud access.", null, "A shared admin account breaks accountability and cannot be tied to individual employee status in the directory.", "Access keys are long-lived programmatic credentials that are not removed when the directory account is disabled."],
  "cp57": ["Provider-managed default keys are controlled and rotated by the provider, so the company cannot set key policy or disable them itself.", null, "TLS protects data in transit between endpoints, not the keys encrypting data at rest.", "Hashing is one-way and cannot be reversed to read data, so it is not encryption with controllable keys."],
  "cp58": ["Security groups are stateful and automatically allow return traffic, so they do not need an outbound rule for responses.", null, "HTTPS on port 443 is perfectly normal behind a load balancer; switching to port 80 would not fix a blocked return path.", "Network ACLs filter at the IP and port level and have no effect on whether TLS certificates can be used."],
  "cp59": ["Denying port 443 from unknown sources would block legitimate customers too, and a NACL cannot inspect the request content.", "A NAT gateway provides outbound internet access for private instances and does not inspect inbound HTTP requests.", null, "A larger load balancer adds capacity but passes malicious requests through unchanged."],
  "cp60": [null, "Hard-coding the password in an image exposes it to anyone who can pull or inspect the image and requires rebuilding to rotate.", "User data is not hidden: it is retrievable from the instance metadata service and through the console or API, so secrets there are easily read.", "Base64 is only encoding, trivially reversed, so the password would be exposed in the repository again."],
  "cp61": ["Relying on developers' memory is not an enforceable control; one mistake could place data outside the EU.", "TLS encrypts data in transit but does nothing to control which region the data is stored in.", null, "A global CDN caches content at edge locations worldwide, which could store EU personal data outside the EU."],
  "cp62": ["Quarterly scans in production find vulnerable images only after they are already running, which is too late.", null, "Laptop-only scanning is optional and inconsistent, and images built in the pipeline would bypass it.", "Scanning after an incident is reactive; the vulnerable image has already reached production."],
  "cp63": ["A CMDB records assets and their relationships but does not continuously evaluate cloud settings for misconfigurations.", "Endpoint antivirus scans files and processes on hosts and cannot see account-level settings like public buckets or disabled logging.", null, "A network tap copies packets for analysis; it cannot detect configuration issues such as disabled logging."],
  "cp64": ["A flat subnet with one broad security group lets any compromised workload reach every other workload.", null, "Public subnets expose servers to the internet and do nothing to restrict traffic between workloads.", "Perimeter-only filtering trusts everything inside, so an attacker who gets in can move laterally to any port."],
  "cp65": ["Terminating the instance destroys memory and possibly disk evidence needed for the investigation.", "Rebooting clears volatile memory, wiping evidence such as running processes and network connections.", "Deleting logs destroys evidence and hampers the investigation; saving storage is irrelevant during an incident.", null],
  "cp66": ["CDN cache statistics show content delivery hit rates, not which identity called a delete operation.", "CPU metrics show resource usage and cannot identify who deleted the database or from where.", null, "Boot diagnostics show a VM's console output and startup state, not control-plane API calls against the database."],
  "cp67": [null, "Autoscaling adjusts capacity to load; it does not inspect file content or stop sensitive data from being shared.", "A read replica copies a database to serve read queries and has nothing to do with inspecting uploaded files.", "Reserved capacity is a pricing commitment and provides no data protection or content inspection."],
  "cp68": ["An SLA defines the provider's availability and support commitments, not secure configuration settings for an image.", null, "A pricing calculator estimates costs and contains no security hardening guidance.", "The OSI model is a conceptual networking layer model, not a configuration baseline for hardening an OS."],
  "cp69": [null, "Hashing is one-way and cannot be mapped back to the original card number, whereas here a vault can recover it.", "Compression reduces data size and is fully reversible by anyone, so it provides no protection for card data.", "A salted hash is still a one-way function; the salt only defeats precomputed lookups and does not allow recovering the value from a vault."],
  "cp70": ["A CDN serves content over the public internet, and allowing all networks leaves the public endpoint wide open.", "A public IP on each VM sends traffic out over the internet, the opposite of the requirement.", "An internet gateway routes traffic to the public internet; more bandwidth does not keep it private.", null],
  "cp71": ["Committing directly to main skips review and automated checks, so bad changes land immediately.", "A shared network drive has no version history, review or merge process and invites overwriting each other's work.", "Emailing ZIP files is manual, has no automated testing and makes merging conflicting changes error-prone.", null],
  "cp72": ["Configuration drift is when systems diverge from their intended state; it is a problem, not a build practice.", "Blue-green is a release strategy that switches traffic between environments, not an automated build-and-test process.", null, "Chaos engineering deliberately injects failures into running systems to test resilience; it does not compile and test commits."],
  "cp73": ["Continuous deployment pushes every passing change to production automatically with no manual approval gate.", null, "The pipeline automates build, test and staging deployment; only the production release has a manual approval, so it is not manual deployment.", "Continuous integration only covers building and testing code; this pipeline also deploys to staging and gates production."],
  "cp74": ["Puppet normally uses an agent on each managed node and its own declarative language rather than YAML playbooks.", "Chef normally uses an agent (chef-client) on each node and Ruby-based recipes rather than YAML playbooks.", "CloudFormation provisions AWS resources from templates; it does not configure the software on existing Linux servers over SSH.", null],
  "cp75": ["Imperative scripts list the exact steps to run in order, rather than declaring an end state the tool reconciles.", null, "Manual provisioning means an engineer creates resources by hand in a console, not through a file the tool evaluates.", "Ad hoc CLI commands perform individual actions one at a time and do not compute differences from a desired state."],
  "cp76": [null, "A cron job that checks every minute is still polling on a schedule, not triggering on the push event itself.", "A larger build agent makes builds run faster but does not change what starts the pipeline.", "SNMP traps are network device notifications for monitoring, not a way for a code repository to trigger a pipeline."],
  "cp77": ["GET retrieves a resource and should not create or change anything on the server.", "HEAD is like GET but returns only headers; it reads metadata and creates nothing.", "DELETE removes an existing resource rather than creating a new one.", null],
  "cp78": ["The 'latest' tag is mutable and moves to new builds, so different environments can pull different images.", null, "Rebuilding per environment is the very cause of the bug, since each build can pull different package versions.", "Installing the latest packages at container start makes every environment potentially different and unpredictable."],
  "cp79": ["Comments are ignored by YAML parsers, so having many of them cannot cause a parse error.", "Double quotes are valid YAML string syntax and do not break parsing.", null, "Lowercase keys are completely valid in YAML; key case does not cause parse errors."],
  "cp80": ["Restarting servers is acting before understanding the problem, may hide the cause and can disrupt users.", "Documenting the fix is the final step of the methodology, after the problem has been resolved.", "Implementing a larger instance assumes a cause and a solution before the problem has been identified.", null],
  "cp81": ["The load balancer's TLS certificate affects inbound HTTPS to the application, not outbound traffic from a private VM.", "Disk IOPS affects storage performance, not whether the VM can route packets to the internet.", "CDN cache settings affect content delivery to users and play no part in a VM's outbound routing.", null],
  "cp82": [null, "High memory use would cause slowness or errors, but the database is healthy and timeouts suggest packets are being dropped.", "PostgreSQL on 5432 uses its own wire protocol, not HTTP, so HTTP methods have nothing to do with the connection.", "Object storage bucket permissions do not affect TCP connections from the app tier to the database."],
  "cp83": ["Instance size does not cause health check failures when the app itself responds correctly on port 8080.", null, "A maximum size below desired capacity would cause a configuration error or cap the group, not repeatedly mark instances unhealthy.", "Extra memory does not make an instance fail a health check; it only affects cost."],
  "cp84": ["Retrying immediately in a tight loop sends even more requests and makes the rate limiting worse.", "Plain HTTP removes encryption and does not change the provider's rate limits, and many APIs reject it outright.", null, "Rate limits apply regardless of permissions; administrator rights would only break least privilege without stopping 429s."],
  "cp85": ["Quotas apply to the resources in the account and region, not to the IaC language, so any tool would hit the same limit.", null, "Deleting the virtual network is unrelated to vCPU quotas and would break other resources.", "Volume encryption has no effect on the regional vCPU quota for an instance family."],
  "cp86": ["The archive tier affects retrieval time and cost, but an allowed user would not get a 403 just because of the tier.", null, "Desktop screen resolution has nothing to do with how cloud authorization policies are evaluated.", "Versioning keeps prior object versions and does not deny access to users whose policy allows reads."],
  "cp87": ["A different key size on the certificate would not send users to the old servers; DNS decides which address they reach.", "An undersized load balancer would cause slowness or errors, not route users to the old servers.", null, "Server memory does not affect which IP address users' resolvers return for the domain."],
  "cp88": ["IAM users do not incur charges, so their number cannot double the bill.", "Security group rules are free; permissive rules are a security risk but do not create storage or IP charges.", "Tracing may add some cost, but the report points to unattached volumes and IP addresses, not tracing data.", null],
  "cp89": ["Container image size affects pull times and storage, not TLS handshakes or browser certificate warnings.", "High CPU would cause slowness or timeouts, not certificate warnings in browsers.", "Database backup settings have no role in the TLS handshake between clients and the API.", null],
  "cp90": ["The number of branches in a repository does not affect whether the pipeline can authenticate to the deployment target.", "YAML comments are ignored by the parser and cannot cause an authentication error.", null, "Plenty of free disk space on the build agent cannot cause an authentication failure; only a lack of space would cause errors."],
});
