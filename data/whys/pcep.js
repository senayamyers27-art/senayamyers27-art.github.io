/* "Why this answer is wrong" explanations for PCEP (PCEP-30-02) questions. */
CertHub.addWhys("pcep", {
  "pc1": ["This treats -7 // 2 as truncation toward zero; floor division rounds down, so the result is -4, not -3.", "These are true-division results; // is floor division and returns an int (3 and -4) for int operands.", "7 % 2 is 1, not -1; the remainder takes the sign of the divisor, which is positive here.", null],
  "pc2": [null, "64 is (2 ** 3) ** 2, which reads ** left to right; ** is right-associative, so the exponent 3 ** 2 is evaluated first.", "36 would be (2 * 3) ** 2 or 6 squared; nothing in the expression multiplies 2 by 3.", "12 comes from treating ** like multiplication (2 * 3 * 2); ** is exponentiation."],
  "pc3": ["44 reads 0o17 as decimal 17 and 0b11 as decimal 11; they are octal 15 and binary 3.", "40 misreads one of the literals; 0o17 is 15, 0x10 is 16 and 0b11 is 3, which add to 34, not 40.", null, "28 would require 0x10 to be 10; hexadecimal 10 is 16, not decimal 10."],
  "pc4": ["13 applies x * 2 first and then adds 3; a compound assignment evaluates the whole right-hand side (2 + 3) before multiplying.", null, "10 ignores the + 3 entirely and computes only x * 2.", "28 does not correspond to any grouping of 5, 2 and 3 here; the right side is 5, so x becomes 5 * 5."],
  "pc5": ["The int 2 would need floor division; / always returns a float, so the result prints as 2.0.", "0.125 comes from computing 1 / (2 * 4); * and / have equal priority and are evaluated left to right, so it is (1 / 2) * 4.", "0 treats / as integer division as in some other languages; in Python 3, 1 / 2 is 0.5, not 0.", null],
  "pc6": ["This output uses the default space separator; sep=\"-\" replaces the spaces between arguments with hyphens.", "sep is placed only between arguments, not after the last one, so there is no hyphen before the !.", "sep is never printed before the first argument; it goes only between values.", null],
  "pc7": ["10 would need the input converted with int(); input() returns the string '5', and '5' * 2 repeats it.", null, "String repetition joins copies with no separator; print would only add a space between separate arguments.", "Multiplying a string by an int is valid (repetition); a TypeError would come from str + int, not str * int."],
  "pc8": ["A leading underscore is allowed in identifiers; _total2 is legal (it is conventionally a private name).", "Digits are allowed anywhere except the first character, so total_2nd is legal.", "Identifiers are case-sensitive and may start with a capital letter; Total is legal, just unusual for a variable under PEP 8.", null],
  "pc9": ["print is a built-in function, not a keyword; it can be shadowed by assignment, although you should not do it.", "input is a built-in function name, not a reserved keyword, so assigning to it is legal (but bad practice).", "null is not a Python keyword or built-in at all; it is an ordinary legal identifier. Python uses None instead.", null],
  "pc10": [null, "Producing a stand-alone machine-code file once describes compilation, not interpretation.", "Compilers check the full syntax of the program, not just indentation; this description matches neither approach.", "Python is interpreted and still reports syntax errors before running any line of the file."],
  "pc11": ["bool(\"0\") is True because any non-empty string is truthy; the string's content is not examined.", null, "bool(0.0) is False because zero of any numeric type is falsy.", "This swaps both results: 0.0 is falsy and the non-empty string \"0\" is truthy."],
  "pc12": ["123.5 would be string concatenation; the strings are converted to numbers first, so + adds 12 and 3.5.", null, "Adding an int to a float produces a float, so the result is 15.5, not a truncated 15.", "There is no TypeError because both operands are already numbers after int() and float() convert them."],
  "pc13": ["6 & 3 is 2 (110 & 011 = 010), not 1; the other two values are right.", "This swaps the OR and XOR results; 6 | 3 is 7 and 6 ^ 3 is 5.", null, "6 & 3 is 2; AND keeps only bits set in both operands, and only the middle bit is shared."],
  "pc14": ["~ is bitwise NOT, which gives -x - 1, so ~5 is -6, not simple negation to -5.", "4 is not ~5; bitwise NOT on Python ints gives -x - 1, which is -6.", "1 << 3 multiplies 1 by 2 ** 3 = 8; 6 would be adding 3 twice or a different operation.", null],
  "pc15": [null, "The comparison would be True only with exact decimal arithmetic; binary floats make 0.1 + 0.2 equal 0.30000000000000004.", "== returns a Boolean, not a number, so the line can only print True or False.", "Comparing floats never raises ValueError; the expression simply evaluates to False."],
  "pc16": [null, "72 comes from computing (10 - 2) * 3 ** 2; subtraction has lower priority than * and **.", "-26 does not match any valid grouping; the correct steps are 3 ** 2 = 9, 2 * 9 = 18, 10 - 18.", "64 comes from squaring (10 - 2); ** binds only to its immediate operands, 3 ** 2, and * is applied before -."],
  "pc17": ["Scientific notation always creates a float, so the value prints as 1000.0, not the int 1000.", "print shows the value, not the source literal; 1e3 is displayed in normal float form as 1000.0.", "0.001 would be 1e-3; the positive exponent in 1e3 means 1 times 10 to the power 3.", null],
  "pc18": ["x > 3 is also true, but the elif is only tested when the if condition is false; x > 5 already matched.", "else runs only when every earlier condition is false, and x > 5 is true for 7.", null, "An if-elif-else chain runs at most one branch, so B is never printed after A."],
  "pc19": ["11 is past the stop value 10; range never produces a number at or beyond its stop.", null, "range starts at its first argument, 2, not at the step value 3.", "The step is 3, not 2, so the values go 2, 5, 8 rather than counting by twos."],
  "pc20": ["The loop stops before reaching the stop value 0, so -1 (which is past 0) is never produced.", "The step is -2, so every other number is skipped; this output uses a step of -1.", "A negative step counts down from the start value 5, so the order is 5, 3, 1, not ascending.", null],
  "pc21": ["i is incremented to 3 before the condition i < 3 becomes false, so the else sees 3, not 2.", null, "A while loop's else runs when the condition becomes false without a break, so it does print.", "The loop stops as soon as i reaches 3; it never increments i a fourth time."],
  "pc22": ["The loop ends with break, so the for loop's else clause is skipped and no break is never printed.", "The loop exits at n == 3 via break, so n never reaches its last range value 4.", "break skips the else clause, and n stays at 3 rather than reaching 4.", null],
  "pc23": ["6 is the sum of the even numbers 2 and 4; continue skips the evens, so they are the ones not added.", "15 is the sum of all numbers 1 to 5; the continue prevents the even numbers from being added.", null, "4 does not correspond to any subset the loop adds; the odd values 1, 3 and 5 are all added."],
  "pc24": ["9 assumes the inner loop always runs three times; range(i) runs 0, 1 and 2 times as i changes.", "6 would be the result of range(i + 1) or summing 1 + 2 + 3; the inner loop runs 0 + 1 + 2 times.", null, "2 counts only the final outer pass; the inner loop also runs once when i is 1."],
  "pc25": [null, "1 is the value just before the last subtraction; the loop subtracts 3 again before testing n < 0.", "Subtracting 3 repeatedly from 10 gives 7, 4, 1, -2; -1 is never reached.", "10 is not a multiple of 3, so subtracting 3 never lands on 0."],
  "pc26": ["range(0) produces no values, so the loop body that prints x never runs.", "The else clause still runs because the loop ended without break, even though it had no iterations.", null, "The body never executes for an empty range, so x is not printed before empty."],
  "pc27": ["A comment is ignored by the interpreter and does not count as a statement, so the block would still be empty (IndentationError).", null, "continue is only valid inside a loop; at module level it is a SyntaxError.", "break is only valid inside a loop; using it in a top-level if block is a SyntaxError."],
  "pc28": ["An elif does not require an else; a chain can end with elif or with else, and here it already has an else.", "score is assigned the int 85, not a string, so the comparison is numeric.", null, "else is tested last and runs only if every condition above it is false; branches are checked top to bottom."],
  "pc29": ["x increases by 4 each pass (4, 8, 12), so it never equals exactly 10.", "8 is still less than 10, so the loop runs one more pass and adds 4 again.", null, "Adding 4 from 0 only produces multiples of 4, so 9 is impossible."],
  "pc30": ["ch * 2 repeats each character, not the whole string; the loop handles one character at a time.", "end=\"\" prints nothing between items and ch * 2 adds no space, so there are no spaces.", "Each character is multiplied by 2, so every letter appears twice, not once.", null],
  "pc31": [null, "X prints only if b < 5, but b is 10, so the nested else runs instead.", "Z belongs to the outer else, which is skipped because a > 3 is true.", "Only one branch of each if-else runs, and the outer else is skipped, so Z is never printed."],
  "pc32": ["2 misses the value 9, which is below the stop value 10 and is therefore included.", "range(1, 10, 4) gives 1, 5 and 9; a fourth value, 13, would exceed the stop.", null, "9 counts every number from 1 to 9, ignoring the step of 4."],
  "pc33": ["The loop exits with break, so the while loop's else clause is skipped and else is not printed.", "break stops the loop at i == 2, so i never counts all the way down to 0.", "The loop never reaches 0 and ends with break, so neither else nor 0 is printed.", null],
  "pc34": ["range(3) produces 0, 1, 2; the loop variable keeps the last value produced, not the stop value 3.", null, "The loop variable is updated on every pass, so it ends at the last value 2, not the first.", "A for loop's variable stays defined after the loop ends, so print(i) works."],
  "pc35": ["print() works inside any block, including while loops.", "An else clause runs after a loop ends; it cannot make a loop stop.", null, "Changing < to <= would make the loop run even longer; the real problem is that count never changes."],
  "pc36": [null, "range(3) caps the attempts at exactly three, but the number of attempts is unknown in advance.", "A single if-else checks once and cannot repeat the prompt.", "Looping over the PIN string visits its characters; it does not repeat the prompt until valid input arrives."],
  "pc37": ["range(10, 0) with the default step +1 is empty; counting down from 10 needs a negative step.", "print(c) is outside the loop, but the loop body never runs, so the printed count is 0, not 1.", null, "No values are produced because the start 10 is already past the stop 0 for a positive step."],
  "pc38": ["break exits only the inner loop, so the outer loop still runs all three passes.", null, "9 assumes the inner loop runs fully; break stops it at j == 1 every time.", "6 assumes two values print per pass; break happens before print when j is 1, so only j = 0 prints."],
  "pc39": [null, "x < 4 is False when x is 4, so the and expression is False and in is not printed.", "else runs only if every earlier condition is false, but x == 4 makes the elif true.", "At most one branch of an if-elif-else chain runs, and the first condition is False anyway."],
  "pc40": ["20 forgets that 10 is included; the stop is 11, so 0, 2, 4, 6, 8 and 10 are all added.", "25 is the sum of the odd numbers 1 to 9; this range starts at 0 and steps by 2 through the evens.", null, "55 is the sum of all numbers 0 to 10; the step of 2 skips the odd ones."],
  "pc41": ["pass does nothing, so placing it after a loop cannot detect whether a match was found.", null, "continue only skips to the next iteration; on its own it gives no signal that the search found nothing.", "An if before the loop runs before any items are checked, so it cannot know the search result."],
  "pc42": ["The loop stops when n becomes 0 because 0 is falsy, so 0 is never printed.", "n is printed before it is decremented, so the first value shown is 3.", null, "A non-zero int such as 3 is truthy, so the loop does start."],
  "pc43": ["The code never runs; = inside an if condition is a syntax error found during parsing.", "The file fails to parse, so it cannot run silently and print nothing.", null, "x is defined, and the problem is detected at parse time as a SyntaxError, not at run time as a NameError."],
  "pc44": [null, "continue skips the print when i is 3, so 3 is not in the output.", "continue does not end the loop the way break would; it keeps going after 3.", "i is incremented before print, so the first value printed is 1."],
  "pc45": ["The stop index 4 is excluded, so nums[1:4] ends at index 3 and does not include 5.", null, "Indexes start at 0, so nums[1:4] starts at the second item (2), and nums[-2] is 4, not 3.", "nums[-2] is the second item from the end (4); -1 would be the last item, 5."],
  "pc46": ["b = a copies only the reference, so the append through b also changes the list a refers to.", "append adds to the existing list; it does not replace the contents with just the new item.", "append adds the item at the end, not the front; inserting at the front would need insert(0, 4).", null],
  "pc47": ["99 would print only if b were an alias of a; a[:] creates a separate copy.", null, "a[0] is a single item, not the whole list, and a was not changed.", "Index 0 exists in both lists, so no IndexError occurs."],
  "pc48": [null, "insert(1, 9) places 9 at index 1, not index 0; this output would need insert(0, 9).", "9 goes before the item currently at index 1 (the value 1), not after it.", "Adding at the end is what append does; insert puts the item at the given index."],
  "pc49": [null, "sorted() returns a new list and leaves lst unchanged; only lst.sort() sorts in place.", "sorted() returns the sorted list; it is lst.sort() that returns None.", "This reverses the roles: sorted() returns the sorted copy and lst stays in its original order."],
  "pc50": [null, "The slice 1:3 covers indexes 1 and 2, so both 20 and 30 are removed, not just 20.", "Index 0 is outside the slice 1:3, so 10 is kept along with 40.", "This removes 30 but keeps 20; the slice starts at index 1, which is 20, so 20 is deleted too."],
  "pc51": ["These are the squares of the even numbers; x % 2 is truthy (1) only for odd x.", "The comprehension outputs x * x, not x, so the odd values are squared.", "25 would need x = 5, but range(5) stops at 4.", null],
  "pc52": ["5 is m[1][1] alone; the expression adds m[1][0] (4) and m[0][2] (3).", "6 would come from m[1][2] or mixing up rows and columns; m[1][0] is 4 and m[0][2] is 3.", null, "9 would come from adding 6 and 3 (for example using m[1][2]); the first index selects the row, the second the column."],
  "pc53": [null, "Tuples are immutable, so their items can never be reassigned.", "Index 0 is valid for a three-item tuple; the problem is immutability, not the index.", "Python does not ignore invalid operations silently; item assignment on a tuple raises an exception."],
  "pc54": ["(5) is just the int 5 in parentheses, so t * 2 is the number 10, not a tuple.", "(5,) is a one-item tuple, so u * 2 repeats it to (5, 5) instead of multiplying.", "t is an int, so the result is 10 not (10,); multiplying a tuple repeats it, it does not multiply its items.", null],
  "pc55": ["d[\"a\"] = 10 replaces the old value, so d[\"a\"] is 10 afterwards, not 1.", "Reassigning an existing key does not add a new entry, so there are 3 keys, not 4.", null, "d[\"c\"] = 3 adds a new key, so the length grows from 2 to 3."],
  "pc56": [null, "Square-bracket lookup does not return None for a missing key; d.get(\"y\") would.", "IndexError is for sequence positions such as list indexes; dictionaries raise KeyError.", "Dictionaries never substitute an empty string for a missing key."],
  "pc57": ["The loop prints both k and v, so each value appears after its key.", "items() yields (key, value) in that order, so the key comes first.", null, "The pair is unpacked into k and v, and print shows them separated by a space, not as a tuple."],
  "pc58": ["1 is a value, not a key, and in on a dictionary checks keys only, so 1 in d is False.", "This is reversed: \"a\" is a key (True), while 1 is only a value (False).", "\"a\" is a key of d, so \"a\" in d is True.", null],
  "pc59": [null, "del d[\"a\"] removes key a, so it cannot remain in the dictionary.", "del removes the key entirely; it does not set its value to None.", "Only the named key is deleted; b and its value remain."],
  "pc60": ["Strings are immutable, so s[0] = \"H\" cannot change the string in place.", "Item assignment is not allowed on strings, and even if it were it would not replace the whole string with H.", "Index 0 exists in \"hello\"; the error is about immutability, not a bad index.", null],
  "pc61": ["s[1:3] stops before index 3, so it gives yt, not yth.", null, "s[::-1] with a step of -1 reverses the string rather than returning it unchanged.", "s[1:3] starts at index 1 (y), not index 0 (P)."],
  "pc62": [null, "With an explicit separator, split() keeps empty fields; only split() with no argument drops empties.", "The string does contain the separator, so it is split into several items, not returned whole.", "The separator itself is removed and never appears as an item in the result."],
  "pc63": ["join puts the separator only between items, never at the start or end.", null, "The separator \"-\" is inserted between items; xyz would be \"\".join(...).", "join returns a single string, not the list itself."],
  "pc64": ["The backslash is an escape character and is not stored, so the string has 4 characters, not 5.", null, "Only the four characters I, t, ' and s are stored; neither the backslash nor quotes add length.", "\\' is a valid escape sequence inside single quotes, so there is no SyntaxError."],
  "pc65": ["\"Z\" < \"a\" is True because uppercase letters have lower code points (Z is 90, a is 97).", "\"abc\" < \"abd\" is True because the first differing characters compare c < d.", "Both comparisons are True: c comes before d, and Z's code point is below a's.", null],
  "pc66": ["The line break inside a triple-quoted string is stored as a newline character, which counts toward the length.", "The newline is a single character (\\n), not two, so the length is 3.", null, "The string holds a, a newline and b; len counts all of them, not just lines."],
  "pc67": ["remove() deletes only the first matching value, so the second 2 stays in the list.", null, "remove(2) does delete a 2, so the list cannot still be [1, 2, 3].", "After removal the list is [1, 3, 2], where 2 is at index 2, not index 1."],
  "pc68": ["A list is mutable, so other code could change its items by accident.", null, "A string is immutable but stores text, so the numbers would have to be parsed out each time.", "Dictionaries are mutable; their values can be reassigned just like list items."],
  "pc69": ["\"x\" is a substring of \"xyz\", so \"x\" not in \"xyz\" is False.", "3 is an element of [1, 2, 3], so the first test is True.", "Both results are reversed: 3 is in the list (True), and x is in xyz, so not in is False.", null],
  "pc70": ["f(3, 4) passes 4 for b, which overrides the default, so the second result is 12, not 6.", "f(3) uses the default b = 2, so it returns 6, not 3.", "Omitting an argument that has a default is allowed, so f(3) raises no TypeError.", null],
  "pc71": [null, "x holds the return value of f(), which is None, and print(x) prints it after hi.", "Calling f() runs its body, so hi is printed before None.", "hi is printed only once, when f() is called; print(x) prints the return value, None."],
  "pc72": ["0 would come from a = 1, b = 3, c = 2; keyword arguments match by name, so b is 2 and c is 3.", null, "4 does not follow from 1 - 2 + 3; it treats the subtraction as addition somewhere.", "Mixing one positional argument with keyword arguments in any order is valid, so no TypeError occurs."],
  "pc73": ["The call is never executed because a positional argument after a keyword argument is rejected at parse time.", "The code cannot run at all, so no subtraction result is possible; the function adds anyway.", "The problem is detected by the parser as invalid call syntax, not as an undefined name at run time.", null],
  "pc74": ["Python never gives b an implicit None default; the header itself is invalid.", "The error occurs when the def line is parsed, before any call, and it is a SyntaxError, not a TypeError.", null, "The definition is rejected outright, so the function cannot be called with keywords either."],
  "pc75": ["Assigning x inside f creates a local variable, so the global x stays 10.", "f() returns its local x, which is 5, not the global 10.", "The order is reversed: f() returns 5 and the global x is 10.", null],
  "pc76": [null, "The global declaration makes x += 1 update the module-level x, so it becomes 2.", "x is defined at module level and declared global, so the name is found.", "UnboundLocalError happens without global; here global x makes x refer to the module variable."],
  "pc77": ["x += 1 makes x local to f for the whole function, so it cannot read the global 1 and add to it.", "The function fails on x += 1 before reaching print, so nothing is printed.", null, "The code is syntactically valid; the problem appears only at run time when the local x is read."],
  "pc78": ["10 is 4 + 3 + 2 + 1; the function multiplies, it does not add.", null, "12 is 4 * 3 and stops too early; the recursion continues down to f(1).", "4 would be returned only if the recursion stopped immediately; f(4) keeps multiplying down to the base case."],
  "pc79": ["Python enforces a recursion depth limit, so endless recursion stops with an error rather than running forever.", "Nothing limits it to 10 calls, and it never reaches a point where it returns None.", null, "Calling a function from itself is valid syntax, so defining it raises no error."],
  "pc80": ["The function receives the same list object, so its appends are visible to the caller.", "add() is called twice, and each call appends another 1.", null, "print(nums) prints the list, which is not None; only add()'s return value is None."],
  "pc81": ["The print shows b first, so the order is 2 1, not 1 2.", null, "The tuple is unpacked into a and b, so print receives two separate values, not a tuple.", "The tuple has exactly two items for two names, so unpacking succeeds without ValueError."],
  "pc82": ["After the first yield the generator resumes and yields 2 as well, so the loop runs twice.", null, "The for loop receives every yielded value in order, starting with 1.", "The loop prints the yielded values, not the generator's return value, and yield does not produce None here."],
  "pc83": ["ZeroDivisionError matches the first branch, so the ArithmeticError branch is never tested.", "Only the first matching except branch runs, so arith is not printed after zero.", "The exception is caught by the first except branch, so the program does not crash.", null],
  "pc84": ["The ArithmeticError branch comes first and already matches ZeroDivisionError, so zero is never reached.", null, "Only one except branch runs per exception, so both messages cannot appear.", "ArithmeticError catches ZeroDivisionError, so the exception is handled rather than crashing the program."],
  "pc85": ["int(\"abc\") receives the right type (a string), so it raises ValueError, not TypeError.", null, "Only the first matching except branch runs, and TypeError is never raised.", "The ValueError is caught by the first branch, so the program does not crash."],
  "pc86": ["IndexError is a subclass of LookupError, so the earlier LookupError branch catches it first.", "Only one except branch runs, and the first one matches.", "The IndexError is caught by the LookupError branch, so nothing crashes.", null],
  "pc87": ["ValueError is a subclass of Exception, so except Exception catches it.", null, "KeyError inherits from LookupError, which inherits from Exception, so it is caught.", "ZeroDivisionError inherits from ArithmeticError and Exception, so it is caught."],
  "pc88": ["The exception propagates from f to g, where the except branch handles it, so the program does not crash.", "g() is called as a statement and its return value is not printed, so None does not appear.", null, "The except branch in g prints caught in g, and g's return value is never printed."],
  "pc89": ["\"a\" + 1 raises TypeError instead of converting 1 to text; str(1) would be needed for a1.", null, "A bare except catches every exception, so the TypeError is handled.", "The print inside try never completes, so a1 is never produced."],
  "pc90": [null, "Returning \"pos\" requires x > 0, but -1 is negative, so that return never runs.", "A function that ends without return gives back None, not 0.", "Reaching the end of a function without return is allowed; it silently returns None."],
  "pc91": ["Parameters are the names in the definition (w and h), not the values passed in the call.", null, "Keywords are reserved words such as def and if; keyword arguments would be written w=3.", "3 and 4 are literals in the call, but the term for values passed to a function is arguments."],
  "pc92": [null, "The def runs after the call, so sq does not exist yet when print(sq(3)) executes.", "The code is syntactically valid; the error happens at run time when the undefined name is used.", "The call fails before sq can return anything, so None is not printed."],
  "pc93": [null, "The KeyError matches one of the names in the except tuple, so it is handled.", "The expression's result is never printed; the except branch prints lookup.", "The missing key name is not printed; only the except branch's message appears."],
  "pc94": [null, "KeyboardInterrupt is raised when the user presses Ctrl+C, not by exit().", "StopIteration signals that an iterator has no more items; it is unrelated to exiting.", "exit() raises SystemExit, a BaseException subclass, not the general RuntimeError."],
  "pc95": ["f(2, \"ab\") overrides the default, so the second result is abab, not ***.", "s * n repeats the string n times; it does not join the number and the string.", "Omitting s is allowed because it has a default, and str * int is valid repetition.", null],
  "pc96": [null, "f reads x when it is called, not when it is defined; by then x is 5.", "Reading a global inside a function needs no declaration, so no NameError occurs.", "The function doubles x (5 * 2); it does not add 3 and 5."],
  "pc97": [null, "Splitting code into functions adds small call overhead and does not guarantee faster execution.", "Variables assigned inside functions are local by default; decomposition does not make them global.", "Functions do not prevent exceptions; errors can still occur and propagate through them."],
});
