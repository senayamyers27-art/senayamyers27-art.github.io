/* AWS Certified Developer – Associate DVA-C02 — generated plan (no hand-written weeks). */
CertHub.register({
  id: "aws-developer",
  vendor: "Amazon Web Services",
  name: "AWS Certified Developer – Associate",
  short: "Developer Associate",
  exam: "DVA-C02",
  blurb: "For developers with about a year of hands-on AWS experience: building, securing, deploying and troubleshooting serverless and cloud-native applications with Lambda, DynamoDB, API Gateway, SQS/SNS, Cognito, KMS and the AWS CI/CD tools.",
  status: "check",
  statusNote: "DVA-C02 domain weights (32/26/24/18) are the ones AWS publishes in its official DVA-C02 exam guide on docs.aws.amazon.com, but the AWS pages could not be opened during this check on Sept 24, 2026, so they were confirmed only through search results that quote that guide. Recheck them, and the exam length (65 questions, 130 minutes) and passing score (720), against the official exam guide. AWS has announced DVA-C03: registration opens Oct 27, 2026 (its exam guide is published then), DVA-C02's last test day is Nov 30, 2026, and DVA-C03 is available from Dec 1, 2026. This plan covers DVA-C02.",
  lastVerified: "2026-09-24",
  notices: [
    { from: "2026-09-24", until: "2026-11-30", text: "DVA-C02 retires after Nov 30, 2026. DVA-C03 (adds AI-assisted development and AI security) starts Dec 1, 2026, with registration and the new exam guide from Oct 27, 2026. This plan covers DVA-C02." },
    { from: "2026-12-01", until: "2027-06-30", text: "This plan covers DVA-C02, which retired on Nov 30, 2026. Check the DVA-C03 exam guide on the AWS site before you book." }
  ],
  examInfo: { questions: "65 (50 scored, 15 unscored; multiple choice and multiple response)", minutes: 130, pass: "720 on a 100–1000 scale" },
  examSim: { questions: 65, minutes: 130 },
  sources: [
    { label: "AWS Certified Developer – Associate exam page", url: "https://aws.amazon.com/certification/certified-developer-associate/" },
    { label: "DVA-C02 exam guide (AWS documentation)", url: "https://docs.aws.amazon.com/aws-certification/latest/developer-associate-02/developer-associate-02.html" },
    { label: "DVA-C02 exam guide (PDF)", url: "https://docs.aws.amazon.com/pdfs/aws-certification/latest/developer-associate-02/developer-associate-02.pdf" }
  ],
  planWeeks: 10,
  hoursPerWeek: "6–8",

  domains: [
    {
      id: 1,
      name: "Development with AWS Services",
      w: 32,
      topics: [
        "Architectural patterns: event-driven, microservices, fan-out, choreography vs orchestration, loosely coupled and stateless designs",
        "Resilient code: retries with exponential backoff and jitter, idempotency, timeouts, handling partial failures and dead-letter queues",
        "Messaging and streaming: SQS standard vs FIFO (message groups, deduplication, visibility timeout, long polling), SNS fan-out, EventBridge rules and Scheduler, Kinesis Data Streams",
        "AWS Step Functions: Standard vs Express workflows, retry/catch, task tokens for callbacks",
        "Calling AWS services with the SDKs and CLI: credential provider chain, pagination, waiters, error handling",
        "Lambda configuration: memory, timeout (15-minute max), ephemeral /tmp storage, environment variables, layers, concurrency, VPC access",
        "Lambda invocation models: synchronous, asynchronous (retries, destinations, DLQs) and event source mappings (SQS, Kinesis, DynamoDB Streams, partial batch responses)",
        "Lambda coding practices: initializing SDK clients and connections outside the handler, reading events, returning API Gateway proxy responses",
        "DynamoDB: partition and sort keys, Query vs Scan, LSI vs GSI, RCU/WCU math, consistency models, condition expressions, TTL, Streams, DAX",
        "Amazon S3 from code: multipart upload, storage classes and lifecycle, event notifications",
        "Caching strategies with ElastiCache: lazy loading, write-through, TTLs; choosing between SQL, NoSQL and in-memory stores"
      ],
      notes: ["Task statements 1.1–1.3 (DVA-C02 exam guide)"],
      labs: [
        "Free Tier: write a Python or Node.js Lambda function triggered by an SQS queue that returns batchItemFailures for bad messages, attach a dead-letter queue with maxReceiveCount 3, and watch a poison message land there. Lambda and SQS stay free at this scale; delete the queue and function when done.",
        "Free Tier: create an on-demand DynamoDB table (customerId + orderDate), load 20 items with the SDK, then compare a Query with a Scan plus filter and note ConsumedCapacity for each. Add a GSI and a TTL attribute. Keep data tiny and delete the table afterwards.",
        "Free Tier: build an SNS topic with two SQS queue subscriptions and an EventBridge Scheduler rule that invokes a Lambda function every 5 minutes; confirm both queues get every message, then disable the schedule so it stops running."
      ]
    },
    {
      id: 2,
      name: "Security",
      w: 26,
      topics: [
        "IAM for applications: execution roles, instance profiles, ECS task roles, least-privilege policies, policy evaluation (explicit deny wins)",
        "Resource-based policies: Lambda permissions for S3/SNS/API Gateway, S3 bucket policies, KMS key policies",
        "Cross-account access with STS AssumeRole and trust policies; temporary credentials",
        "Amazon Cognito: user pools (sign-up, sign-in, ID/access/refresh tokens) vs identity pools (temporary AWS credentials)",
        "API Gateway authorization: IAM (SigV4), Cognito user pool authorizers, Lambda authorizers, API keys and usage plans",
        "Encryption at rest: SSE-S3, SSE-KMS, SSE-C, S3 Bucket Keys, client-side encryption with the AWS Encryption SDK",
        "AWS KMS: customer managed vs AWS managed keys, envelope encryption with GenerateDataKey, 4 KB Encrypt limit, cross-account key use",
        "Encryption in transit: TLS, ACM certificates (us-east-1 for CloudFront), enforcing aws:SecureTransport",
        "Secrets and configuration: Secrets Manager (rotation) vs Systems Manager Parameter Store (SecureString, tiers)",
        "Keeping sensitive data out of code and logs: default credential chain, no hardcoded keys, CloudWatch Logs data protection masking",
        "Presigned URLs for temporary S3 access; IAM Access Analyzer for least privilege"
      ],
      notes: ["Task statements 2.1–2.3 (DVA-C02 exam guide)"],
      labs: [
        "Free Tier: create a Cognito user pool with a test user and an API Gateway HTTP or REST API with a Cognito authorizer in front of a Lambda function; call it with and without the ID/access token and compare the responses. Delete the API and pool afterwards.",
        "Free Tier: write a script that generates a presigned PUT URL for a private S3 bucket, upload a file with curl, and then add a bucket policy that denies aws:SecureTransport=false and confirm plain HTTP is rejected.",
        "Low cost: create one KMS customer managed key and run GenerateDataKey to encrypt a local file (envelope encryption), then decrypt the stored data key. Watch costs: a customer managed key costs about $1/month, so schedule it for deletion (7-day minimum) when finished. Store a test value in Parameter Store as a SecureString (standard tier is free) instead of Secrets Manager, which bills per secret."
      ]
    },
    {
      id: 3,
      name: "Deployment",
      w: 24,
      topics: [
        "Preparing artifacts: .zip packages vs container images in ECR, Lambda layers, dependency packaging, CodeArtifact",
        "AWS SAM: template structure, sam build, sam deploy --guided, sam local invoke / start-api, samconfig.toml",
        "CloudFormation: templates, parameters, outputs and exports, Fn::ImportValue, change sets, packaging local artifacts to S3",
        "AWS CDK basics: constructs, cdk bootstrap, cdk synth, cdk deploy",
        "Lambda versions and aliases; weighted aliases; CodeDeploy canary, linear and all-at-once traffic shifting with alarm rollback",
        "API Gateway stages, stage variables, deployments, mock integrations, canary releases",
        "Elastic Beanstalk deployment policies: all at once, rolling, rolling with additional batch, immutable, traffic splitting, blue/green URL swap",
        "CodePipeline stages and actions, manual approvals; CodeBuild buildspec phases and artifacts",
        "CodeDeploy appspec.yml lifecycle hooks for EC2, Lambda and ECS; the CodeDeploy agent",
        "Testing in development environments: unit tests in CI, integration tests against deployed stages, AppConfig feature flags and gradual configuration rollout"
      ],
      notes: ["Task statements 3.1–3.4 (DVA-C02 exam guide)"],
      labs: [
        "Free Tier: install the SAM CLI and Docker, run sam init with the Hello World template, test with sam local invoke and sam local start-api, then deploy with sam deploy --guided and remove everything with sam delete.",
        "Free Tier: publish two versions of a Lambda function, create a live alias with 90/10 weighted routing, and add DeploymentPreference: Canary10Percent5Minutes in the SAM template to watch CodeDeploy shift traffic. CodeDeploy for Lambda has no extra charge.",
        "Mostly free: create a CodePipeline with a GitHub or CodeCommit-compatible source, a CodeBuild stage that runs unit tests from buildspec.yml, and a manual approval. Check pricing first: CodeBuild includes free build minutes each month and a V2 pipeline is billed per action execution minute, so delete the pipeline when done."
      ]
    },
    {
      id: 4,
      name: "Troubleshooting and Optimization",
      w: 18,
      topics: [
        "Root cause analysis with CloudWatch Logs, Logs Insights queries, metrics and dashboards",
        "Common Lambda errors: throttling (429), timeouts, AccessDenied from the execution role, malformed proxy responses (502), API Gateway 504 integration timeouts",
        "AWS X-Ray: segments, subsegments, annotations vs metadata, sampling, active tracing, the X-Ray daemon/CloudWatch agent",
        "Custom metrics: PutMetricData, CloudWatch embedded metric format, high-resolution metrics",
        "CloudWatch alarms with SNS notifications; structured logging and correlation IDs",
        "Lambda performance: memory/CPU tuning, cold starts, provisioned concurrency, reserved concurrency",
        "Stream and queue troubleshooting: Kinesis IteratorAge, parallelization factor, SQS dead-letter queues and redrive",
        "DynamoDB optimization: hot partitions, key design, on-demand vs provisioned capacity, adaptive capacity",
        "Caching for performance: API Gateway stage caching, CloudFront, ElastiCache, DAX"
      ],
      notes: ["Task statements 4.1–4.3 (DVA-C02 exam guide)"],
      labs: [
        "Free Tier: turn on active tracing for a Lambda function behind API Gateway, add an X-Ray subsegment with an annotation (for example tier=gold), and filter traces by it in the console. X-Ray includes a monthly free tier of traces.",
        "Free Tier: make a function log JSON with a requestId, deliberately throw errors, then write a CloudWatch Logs Insights query that finds the slowest and failing requests; create an alarm on Errors that emails you through SNS.",
        "Free Tier: run the open-source AWS Lambda Power Tuning tool (or invoke manually at 128, 512 and 1024 MB) against a CPU-heavy function and compare duration and cost. Avoid leaving provisioned concurrency or API Gateway caching enabled: both bill by the hour and are not in the Free Tier."
      ]
    }
  ],

  study: {
    1: [
      ["When would you choose SQS, SNS, EventBridge or Kinesis Data Streams?","SQS: buffer work for one consumer group, with retries and DLQs. SNS: push each message to many subscribers (often SQS queues for fan-out). EventBridge: route events by content pattern to many AWS targets, plus schedules. Kinesis: ordered, replayable streams read by several consumers."],
      ["Explain how Lambda handles failures for synchronous, asynchronous and event source mapping invocations.","Synchronous: the error returns to the caller, who decides whether to retry. Asynchronous: Lambda retries twice, then sends the event to an on-failure destination or DLQ if configured. Event source mappings (SQS, Kinesis, DynamoDB Streams): Lambda retries the batch; use partial batch responses, bisecting, max retry age and on-failure destinations to avoid blocking."],
      ["Calculate RCUs for 25 eventually consistent reads per second of 9 KB items.","9 KB rounds up to 12 KB, which is 3 RCUs for a strongly consistent read. Eventually consistent reads cost half, so 1.5 RCUs each. 25 × 1.5 = 37.5, rounded up to 38 RCUs."],
      ["Compare local and global secondary indexes.","LSI: same partition key, different sort key, created only with the table, shares the table's capacity, supports strong consistency, 10 GB per partition key value limit. GSI: any partition and sort key, can be added any time, has its own capacity, eventually consistent only."],
      ["Why initialize SDK clients outside the Lambda handler, and what is the catch?","The init code runs once per execution environment, so warm invocations reuse clients and connections and run faster. The catch: anything stored there is shared across invocations in that environment, so do not keep per-request data in globals."],
      ["Contrast lazy loading and write-through caching.","Lazy loading fills the cache on a miss, so only requested data is cached but it can be stale and the first read is slow. Write-through updates the cache on every write, so data is fresh but the cache fills with items that may never be read. A TTL is commonly combined with both."]
    ],
    2: [
      ["What is the difference between a Cognito user pool and an identity pool?","A user pool is a user directory that handles sign-up and sign-in and issues JWTs (ID, access, refresh). An identity pool exchanges those (or other IdP) identities for temporary AWS credentials tied to IAM roles."],
      ["Describe envelope encryption with KMS step by step.","Call GenerateDataKey to get a plaintext data key and the same key encrypted under a KMS key. Encrypt the data locally with the plaintext key, discard it from memory, and store the encrypted data key with the ciphertext. To decrypt, call KMS Decrypt on the data key, then decrypt the data locally."],
      ["When should you use Secrets Manager instead of Parameter Store?","Use Secrets Manager when you need built-in automatic rotation (for example RDS credentials), cross-account secret sharing or secret versioning labels, and accept the per-secret monthly charge. Use Parameter Store for configuration and non-rotating secrets at low or no cost."],
      ["Execution role vs resource-based policy on a Lambda function: what does each control?","The execution role controls what the function can call (DynamoDB, S3 and so on). The resource-based policy controls who can invoke the function, such as S3, SNS, API Gateway or another account."],
      ["List the API Gateway authorization options and when to use each.","IAM (SigV4): AWS principals such as other services or roles. Cognito user pool authorizer: users of a Cognito user pool with no custom code. Lambda authorizer: custom tokens or request parameters. API keys with usage plans are for metering and throttling clients, not for authorization."]
    ],
    3: [
      ["Compare the Elastic Beanstalk deployment policies.","All at once: fastest, causes downtime. Rolling: batches, reduced capacity. Rolling with additional batch: keeps full capacity by adding a batch first. Immutable: new instances in a temporary ASG, safest and quickest rollback. Traffic splitting: canary-style testing with a percentage of traffic. Blue/green: separate environment and a URL swap."],
      ["How do Lambda versions and aliases support safe deployments?","A version is an immutable snapshot of code and configuration; $LATEST is mutable. An alias is a named pointer to a version that can split traffic between two versions, which CodeDeploy uses for canary or linear shifting with alarm-based rollback."],
      ["Walk through the AWS SAM workflow from a new project to deployment.","sam init creates a project, sam build packages code and dependencies, sam local invoke or start-api tests locally in Docker, sam deploy --guided creates the stack and saves settings, sam logs and sam sync help iterate, and sam delete removes the stack."],
      ["Name the CodeDeploy lifecycle hooks for an EC2 in-place deployment in order.","ApplicationStop, (DownloadBundle), BeforeInstall, (Install), AfterInstall, ApplicationStart, ValidateService. The hooks in parentheses are run by the agent and cannot run your scripts."],
      ["What are the phases of a buildspec.yml, and what else does it define?","install, pre_build, build and post_build, each with commands. It also defines env variables (including Parameter Store and Secrets Manager references), artifacts to output, reports for test results and cache paths."]
    ],
    4: [
      ["What is the difference between X-Ray annotations and metadata?","Annotations are indexed key-value pairs (string, number, Boolean) you can search with filter expressions. Metadata can hold any object, including large ones, but is not indexed, so you cannot filter traces on it."],
      ["A Lambda function behind API Gateway returns 502 or 504 errors. How do you tell the causes apart?","502 usually means Lambda returned an invalid proxy response or crashed. 504 means the integration timed out (29 seconds by default for REST APIs). Check the function logs for errors and duration, and fix the response shape or move long work to an asynchronous pattern."],
      ["How do memory settings affect Lambda performance and cost?","CPU and network capacity scale with memory. More memory can shorten CPU-bound runs so much that cost stays flat or drops, because billing is memory × duration. Measure with tools like Lambda Power Tuning."],
      ["What causes DynamoDB throttling when total capacity looks fine, and how do you fix it?","A hot partition: too much traffic on one partition key value. Fix it with a higher-cardinality key, write sharding (random or calculated suffixes), caching reads with DAX, or on-demand capacity for spiky traffic."],
      ["How do reserved concurrency and provisioned concurrency differ?","Reserved concurrency sets aside, and caps, a number of concurrent executions for one function at no extra cost. Provisioned concurrency keeps a number of environments initialized to avoid cold starts and is billed while configured."]
    ]
  },

  questions: [
    ["dva1",0,1,"A Lambda function consumes an SQS queue in batches of 10. When one message fails, all 10 are returned to the queue and reprocessed. How can the developer retry only the failed messages?",["Raise the queue's visibility timeout to 12 hours so retries happen less often","Convert the standard queue to a FIFO queue with content-based deduplication","Set the function's reserved concurrency to 1 so batches run one at a time","Enable ReportBatchItemFailures and return the failed message IDs"],3,"With ReportBatchItemFailures the function returns batchItemFailures, so Lambda deletes the successes and only the listed messages become visible again. A longer visibility timeout just delays the same full-batch retry.","Task 1.2"],
    ["dva2",0,1,"An orders table must return all orders for one customer, newest first, using a single efficient request. Which key design fits?",["Partition key orderDate and sort key customerId","Partition key customerId and sort key orderDate","Partition key orderId with no sort key","Partition key status and sort key customerId"],1,"A Query on partition key customerId returns that customer's items, and ScanIndexForward=false sorts them by orderDate descending. Using orderId alone would force a Scan.","Task 1.3"],
    ["dva3",0,1,"A DynamoDB table already exists and holds data. The team now needs to query items by an email attribute that is not part of the primary key. What should they add?",["A local secondary index with email as its sort key","A DynamoDB Streams trigger on the table","A global secondary index with email as its partition key","A second attribute named email in the table's primary key"],2,"GSIs can be added to an existing table and can use any attribute as a new partition key. LSIs can only be defined when the table is created and must share the table's partition key.","Task 1.3"],
    ["dva4",0,1,"Which DynamoDB read operation cannot use strongly consistent reads?",["A Query against a global secondary index","A GetItem on the base table","A Query against a local secondary index","A Scan of the base table"],0,"GSIs are updated asynchronously and support only eventually consistent reads. Base-table reads and LSI queries can request ConsistentRead=true.","Task 1.3"],
    ["dva5",0,1,"An application performs 10 strongly consistent reads per second of items that are 6 KB each. How many read capacity units does the table need?",["20","10","15","60"],0,"One RCU covers one strongly consistent read per second of up to 4 KB. 6 KB rounds up to 8 KB, which is 2 RCUs per read, so 10 reads need 20. Eventually consistent reads would need 10.","Task 1.3"],
    ["dva6",0,1,"An application writes 12 items per second to DynamoDB, and each item is 2.5 KB. How many write capacity units are required?",["30","36","12","24"],1,"One WCU covers one write per second of up to 1 KB. 2.5 KB rounds up to 3 KB, so 3 WCUs per write, times 12 writes, gives 36.","Task 1.3"],
    ["dva7",0,1,"Two users edit the same DynamoDB item at the same time, and one user's change silently overwrites the other's. How should the developer prevent lost updates?",["Switch every read in the application to eventually consistent mode","Add a global secondary index on a version attribute and query it first","Turn on DynamoDB Streams and replay the lost changes from the stream","Store a version number and add a condition expression that checks it on write"],3,"Optimistic locking compares the expected version in a ConditionExpression; if another writer changed it first, the write fails with ConditionalCheckFailedException instead of overwriting.","Task 1.3"],
    ["dva8",0,1,"A Lambda function opens a new database connection and loads a large config file on every invocation, adding latency. What change helps most?",["Increase the function timeout to 15 minutes so the setup has time to finish","Copy the config file into the function's /tmp folder at the start of every call","Create the connection and load the config outside the handler for reuse","Invoke the function asynchronously so callers do not wait for the setup work"],2,"Code outside the handler runs once per execution environment and is reused by warm invocations. A longer timeout or async invocation does not remove the repeated setup work.","Task 1.2"],
    ["dva9",0,1,"A nightly batch job takes about 40 minutes to run in a single process. The team wants a serverless option with no servers to manage. Where should it run?",["A single Lambda function with the maximum timeout","An API Gateway REST API with a Lambda integration","A Lambda@Edge function on CloudFront","An Amazon ECS task on AWS Fargate"],3,"Lambda functions stop after at most 15 minutes, so a 40-minute single process does not fit. Fargate runs containers for as long as needed without managing EC2 instances.","Task 1.2"],
    ["dva10",0,1,"An S3 event invokes a Lambda function asynchronously. Events that still fail after Lambda's built-in retries are being lost. What should the developer configure?",["Configure an on-failure destination or a dead-letter queue","Increase the function's memory so the retries are more likely to succeed","Configure provisioned concurrency so every event gets a warm environment","Lower the maximum event age to 60 seconds so failures are reported sooner"],0,"For asynchronous invocations, Lambda retries twice by default and then discards the event unless an on-failure destination or DLQ is set. A shorter event age would drop events even sooner.","Task 1.2"],
    ["dva11",0,1,"A Lambda function attached to private subnets in a VPC must call a public third-party API on the internet. What is required?",["A public IP address assigned to the function's elastic network interfaces","An internet gateway route added directly to the private subnets' route table","A NAT gateway in a public subnet, routed from the private subnets","A VPC gateway endpoint that points to the third-party API's domain name"],2,"Lambda ENIs in a VPC never get public IPs, so outbound internet access goes through a NAT gateway. Gateway endpoints exist only for S3 and DynamoDB. Note that NAT gateways are billed hourly.","Task 1.2"],
    ["dva12",0,1,"Twelve Lambda functions use the same in-house logging library. How can the team share it without bundling a copy into each deployment package?",["Store the library's code in an environment variable read by each function","Publish the library as a Lambda layer and attach it to each function","Download the library into each function's /tmp folder on every cold start","Run the library as a separate state in an AWS Step Functions state machine"],1,"Layers package shared libraries or runtimes once and attach them to many functions. Environment variables hold small strings, not code.","Task 1.2"],
    ["dva13",0,1,"A developer wants a workflow that charges a card, waits (possibly for days) for a human to approve a refund, then continues, with retries on each step. Which service fits best?",["A single Lambda function that polls in a loop","An SQS standard queue with a long visibility timeout","Amazon EventBridge with a scheduled rule","AWS Step Functions with a task token callback"],3,"Step Functions Standard workflows can pause on a .waitForTaskToken task until SendTaskSuccess is called, and they provide per-state retry and catch. A polling Lambda would hit the 15-minute limit.","Task 1.1"],
    ["dva14",0,1,"When an order is placed, three independent services (billing, shipping, analytics) must each receive and process every order event. Which design is most appropriate?",["Send each order to one SQS queue that all three services poll","Write each order to an S3 bucket and have every service list the bucket","Publish to an SNS topic with one SQS queue subscribed per service","Invoke the three services one after another from the producer"],2,"SNS-to-SQS fan-out delivers a copy of each message to every subscribed queue, so each service processes all events at its own pace. With one shared queue, each message would go to only one consumer.","Task 1.1"],
    ["dva15",0,1,"An SQS FIFO queue carries updates for many customers. Updates must stay in order for each customer, but different customers should be processed in parallel. What should the producer set?",["The MessageDeduplicationId to the customer ID","The MessageGroupId to the customer ID","The DelaySeconds to a different value for each customer","A separate FIFO queue for every message"],1,"FIFO ordering is kept within a message group, and different groups can be consumed in parallel. The deduplication ID only prevents duplicates within the 5-minute window.","Task 1.1"],
    ["dva16",0,1,"A consumer polls an SQS queue in a tight loop and receives many empty responses, increasing cost. What should the developer change?",["Enable long polling with a receive wait time of up to 20 seconds","Lower the queue's message retention period to reduce the number of messages","Shorten the visibility timeout to 0 seconds so messages come back faster","Convert the queue to a FIFO queue so messages are delivered in batches"],0,"Long polling waits for messages to arrive before responding, cutting empty receives and request charges. Visibility timeout and retention do not affect empty polls.","Task 1.1"],
    ["dva17",0,1,"Messages in an SQS queue are sometimes processed twice. Processing a message takes about 90 seconds, and the queue's visibility timeout is 30 seconds. What is the most likely fix?",["Enable long polling on the queue so consumers wait for new messages","Increase the message retention period to the maximum of 14 days","Raise the visibility timeout above the typical processing time","Add a 60-second delivery delay to each message the producer sends"],2,"If processing outlasts the visibility timeout, the message becomes visible again and another consumer picks it up. Setting the timeout longer than processing time (or extending it with ChangeMessageVisibility) prevents that.","Task 1.1"],
    ["dva18",0,1,"A cleanup Lambda function must run every night at 02:00 UTC. What is the simplest way to trigger it?",["An Amazon EventBridge schedule with a cron expression","An SQS delay queue with a 24-hour delay","A CloudWatch alarm on the function's Invocations metric","An S3 lifecycle rule that invokes Lambda"],0,"EventBridge Scheduler (or a scheduled rule) invokes targets on cron or rate expressions. SQS delays are capped at 15 minutes.","Task 1.1"],
    ["dva19",0,1,"An application's AWS SDK calls occasionally fail with ThrottlingException during traffic spikes. What should the code do?",["Retry immediately in a tight loop until it succeeds","Retry with exponential backoff and jitter","Catch the error and discard the request","Create a new SDK client for each retry"],1,"AWS recommends exponential backoff with jitter so retries spread out and give the service time to recover. Immediate retries add load and make throttling worse.","Task 1.1"],
    ["dva20",0,1,"A clickstream must be processed in order per user, kept for several days so it can be replayed, and read by multiple independent applications. Which service fits?",["Amazon SQS standard queue","Amazon SNS standard topic","Amazon SES","Amazon Kinesis Data Streams"],3,"Kinesis Data Streams keeps records ordered per shard (by partition key), retains them for replay and lets several consumers read the same data. SQS messages are removed once consumed.","Task 1.1"],
    ["dva21",0,1,"Which caching strategy updates the cache every time the database is written, so reads are never stale, at the cost of caching data that may never be read?",["Write-through","Lazy loading","Time-to-live only","Read replica"],0,"Write-through writes to the cache along with the database. Lazy loading fills the cache only on a miss, so it caches only requested data but can serve stale values.","Task 1.3"],
    ["dva22",0,1,"A DynamoDB table stores web sessions that should disappear 24 hours after creation without any cleanup code. What should the developer use?",["A nightly Scan that deletes old items with BatchWriteItem calls","A global secondary index on the session creation date attribute","DynamoDB Accelerator (DAX) with a 24-hour item cache lifetime","DynamoDB TTL on an attribute that holds the expiry timestamp"],3,"TTL deletes items after the epoch time in the chosen attribute passes, at no extra write cost. A Scan-and-delete job works but consumes capacity and needs code.","Task 1.3"],
    ["dva23",0,1,"Whenever an item in a DynamoDB table changes, a Lambda function must send a notification that includes the old and new values. What should the developer configure?",["A CloudTrail trail that records DynamoDB data events for the table","DynamoDB Streams (new and old images) with a Lambda trigger","A scheduled Lambda function that scans the whole table every minute","An SNS topic subscribed directly to the table's change notifications"],1,"Streams record each item change, and the NEW_AND_OLD_IMAGES view type includes both versions; Lambda polls the stream through an event source mapping.","Task 1.3"],
    ["dva24",0,1,"An application uploads 3 GB video files to S3 and occasionally has to restart the whole upload after a network blip. What should it use?",["A single PutObject call with a longer timeout","S3 Transfer Acceleration without changing the upload method","Multipart upload, retrying only the failed parts","S3 Object Lock in governance mode"],2,"Multipart upload sends parts in parallel and lets a failed part be retried alone; AWS recommends it above about 100 MB and it is required above 5 GB. Transfer Acceleration can speed the path but still restarts a single PUT.","Task 1.1"],
    ["dva25",0,1,"A report runs a Scan with a FilterExpression on customerId, which is the table's partition key, and it is slow and expensive. What is the better approach?",["Increase the page size of the Scan","Use a parallel Scan with more segments","Use a Query with a key condition on customerId","Add a FilterExpression on another attribute as well"],2,"A Scan reads every item and filters afterward, consuming capacity for the whole table. A Query on the partition key reads only the matching items.","Task 1.3"],
    ["dva26",0,1,"A read-heavy DynamoDB application needs microsecond read latency for repeated reads of the same items, without changing its data-access code much. What should the developer add?",["DynamoDB Accelerator (DAX)","A DynamoDB global table","An S3 bucket in front of the table","DynamoDB Streams"],0,"DAX is an API-compatible in-memory cache for DynamoDB that serves repeat reads in microseconds. Global tables replicate across Regions for availability, not caching.","Task 1.3"],
    ["dva27",0,1,"An API Gateway REST API with Lambda proxy integration returns 502 Bad Gateway. The function returns the plain string \"OK\". What is wrong?",["Increase the page size (Limit) used by the Scan requests","Use a parallel Scan that splits the table into more segments","Add a second FilterExpression on another attribute to narrow the results","Use a Query with a key condition on customerId"],3,"With proxy integration, API Gateway expects a JSON object containing statusCode and body (body as a string). Any other shape is a malformed Lambda response, which becomes a 502.","Task 1.1"],
    ["dva28",0,1,"An SQS standard queue can deliver the same message more than once. How should the consumer that charges customers be designed?",["Add a usage plan and require an API key on the method","Return an object with statusCode, headers and a string body","Lower the function timeout so it finishes before the 29-second limit","Turn on stage caching so API Gateway can reuse earlier responses"],1,"Standard queues guarantee at-least-once delivery, so consumers must tolerate duplicates, for example with a conditional write on a request ID. Deleting first risks losing the message if processing fails.","Task 1.1"],
    ["dva29",0,2,"A Lambda function needs to read items from one DynamoDB table. How should it get permissions?",["Switch the consumer's database reads to eventually consistent mode","Set the visibility timeout to 0 seconds so duplicates are returned at once","Delete each message as soon as it is received, before processing it","Make it idempotent by storing each request ID and skipping repeats"],3,"Lambda assumes its execution role and gets temporary credentials automatically; scoping the policy to the table follows least privilege. Long-lived IAM user keys are a risk and unnecessary.","Task 2.1"],
    ["dva30",0,2,"An application on an EC2 instance must upload files to an S3 bucket. What is the recommended way to give it credentials?",["Save an IAM user's access keys in ~/.aws/credentials on the instance","Attach an IAM role to the instance through an instance profile","Hardcode the access keys in the application's source code","Make the bucket public so no credentials are needed"],1,"An instance profile delivers temporary, automatically rotated role credentials through the instance metadata service, and the SDK finds them without code changes.","Task 2.1"],
    ["dva31",0,2,"A mobile app's users sign in with a Cognito user pool. The app then needs temporary AWS credentials to upload photos directly to S3. What provides those credentials?",["The user pool access token, sent to S3 in place of a SigV4 signature","A separate IAM user with its own access keys for every mobile user","A Cognito identity pool that trades the user pool token for role credentials","An API Gateway API key that the app includes in its S3 upload requests"],2,"User pools authenticate users and issue JWTs; identity pools federate those identities into temporary AWS credentials for an IAM role. S3 does not accept user pool tokens directly.","Task 2.1"],
    ["dva32",0,2,"A REST API in API Gateway must accept only requests from users signed in to an existing Cognito user pool, with the least custom code. What should the developer configure?",["A Cognito user pool authorizer on the API methods","A Lambda authorizer that validates the JWT","IAM authorization with SigV4 signing","An API key required on every method"],0,"A Cognito user pool authorizer validates the user pool token without custom code. A Lambda authorizer would also work but requires writing and maintaining validation logic.","Task 2.1"],
    ["dva33",0,2,"An API must validate bearer tokens issued by a third-party identity provider that uses a custom, non-standard token format. Which API Gateway feature fits?",["A Lambda authorizer","A Cognito user pool authorizer","A resource policy that limits source IPs","A usage plan with throttling"],0,"A Lambda authorizer runs your code to validate any token format and returns an IAM policy. Cognito authorizers only accept Cognito user pool tokens.","Task 2.1"],
    ["dva34",0,2,"A backend service running with an IAM role must call an API Gateway API, and only that role should be allowed. Which authorization type should the API use?",["A Cognito user pool authorizer that checks the caller's ID token","An API key sent in the x-api-key header of every request","IAM authorization, with callers signing requests using SigV4","No authorization on the API, with an AWS WAF rule on the stage"],2,"IAM authorization checks the SigV4 signature against IAM policies, so access can be limited to a specific role. API keys identify clients for usage plans and are not an authorization mechanism.","Task 2.1"],
    ["dva35",0,2,"An application in account A must write to a DynamoDB table in account B. What is the recommended approach?",["An IAM user in account B whose access keys are copied into account A","Account A's root user added to an administrators group in account B","A table resource policy in account B that allows any principal to write","A role in account B that trusts account A, assumed with sts:AssumeRole"],3,"Cross-account access uses a role whose trust policy names account A; the caller also needs sts:AssumeRole permission and receives temporary credentials. Sharing long-lived keys is not recommended.","Task 2.1"],
    ["dva36",0,2,"An application uses an Amazon RDS database password that must be rotated automatically every 30 days with minimal custom work. Where should the password be stored?",["Systems Manager Parameter Store as a String parameter","AWS Secrets Manager with rotation enabled","An encrypted environment variable on the Lambda function","A text file in a private S3 bucket"],1,"Secrets Manager has built-in rotation for RDS credentials. Parameter Store can hold SecureStrings but has no built-in rotation, so you would build it yourself.","Task 2.3"],
    ["dva37",0,2,"A team needs to store feature settings and a few encrypted API endpoints that never rotate, at the lowest cost. What should they use?",["AWS Secrets Manager, with a separate secret created for every single value","Parameter Store standard parameters, with SecureString for sensitive values","Amazon DynamoDB items encrypted on the client with the Encryption SDK","A separate AWS KMS customer managed key that stores each setting value"],1,"Standard Parameter Store parameters have no extra storage charge and SecureString encrypts with KMS. Secrets Manager bills per secret per month and mainly adds rotation, which is not needed here.","Task 2.3"],
    ["dva38",0,2,"An application must encrypt 50 MB files with a KMS key. Calling the KMS Encrypt API fails because of the payload size. What should the developer do?",["Split the file into 4 KB chunks and call the KMS Encrypt API for each chunk","Ask AWS Support to raise the Encrypt payload limit for the account","Use GenerateDataKey and encrypt the file locally with the data key","Store the file unencrypted and use KMS to encrypt only the S3 object key"],2,"KMS Encrypt accepts at most 4 KB. Envelope encryption uses a data key to encrypt the data locally and keeps only the wrapped key, which KMS can decrypt later.","Task 2.2"],
    ["dva39",0,2,"A compliance team wants every use of the key that encrypts S3 objects to appear in CloudTrail, and wants to control the key policy themselves. Which encryption option fits?",["SSE-KMS with a customer managed key","SSE-S3 with S3 managed keys","SSE-C with keys supplied on every request","No server-side encryption, relying on TLS"],0,"SSE-KMS logs KMS API calls in CloudTrail, and a customer managed key lets the team write the key policy. SSE-S3 keys are managed entirely by S3 and have no key policy you control.","Task 2.2"],
    ["dva40",0,2,"An application writes thousands of objects per second to a bucket that uses SSE-KMS and starts receiving KMS throttling errors. What reduces KMS requests with the least change?",["Switch to client-side encryption with the Encryption SDK","Create a new KMS key for each object","Disable default encryption on the bucket","Enable S3 Bucket Keys for the bucket"],3,"An S3 Bucket Key creates a short-lived bucket-level key that S3 uses to derive data keys, cutting calls to KMS and their cost.","Task 2.2"],
    ["dva41",0,2,"A bucket policy must reject any request that is not sent over HTTPS. Which condition should a Deny statement use?",["s3:x-amz-server-side-encryption equal to AES256","aws:SourceIp not in the corporate range","aws:SecureTransport equal to false","s3:signatureversion equal to AWS"],2,"Denying requests where aws:SecureTransport is false enforces encryption in transit. The server-side encryption header controls encryption at rest, not transport.","Task 2.2"],
    ["dva42",0,2,"A web app lets users upload a profile photo directly to a private S3 bucket without giving them AWS credentials. What should the backend generate?",["Enable S3 Bucket Keys on the bucket","Switch to client-side encryption with the AWS Encryption SDK","Create a separate KMS key for each object that is uploaded","Remove default encryption from the bucket during peak hours"],0,"A presigned URL carries the signer's permission for one operation on one object until it expires, so the bucket stays private.","Task 2.2"],
    ["dva43",0,2,"A developer is attaching an ACM certificate to a CloudFront distribution for a custom domain, but the certificate does not appear in the list. What is the most likely reason?",["ACM certificates cannot be used with CloudFront","The certificate was requested in a Region other than US East (N. Virginia)","The certificate must be imported, not requested","CloudFront needs the certificate stored in Secrets Manager"],1,"CloudFront only uses ACM certificates from us-east-1. For a Regional API Gateway endpoint or ALB, the certificate must be in the same Region as the resource.","Task 2.2"],
    ["dva44",0,2,"A developer's script works locally because it reads access keys hardcoded in the source file. The script will run on Lambda next. What is the best practice?",["A public-read ACL on the bucket so that anyone can write to it","An IAM user with access keys for every user of the application","A CloudFront distribution with no origin access control configured","A presigned PutObject URL that expires after a few minutes"],3,"The SDK's default provider chain finds role credentials automatically on Lambda, EC2 and ECS. Environment variable keys are still long-lived secrets that can leak.","Task 2.3"],
    ["dva45",0,2,"S3 must invoke a Lambda function when objects are created, but the event notification fails to save with a permissions error. What is missing?",["ACM certificates cannot be attached to CloudFront distributions","The certificate has to be imported into ACM rather than requested","The certificate was requested in a Region other than us-east-1","CloudFront expects the certificate to be stored in Secrets Manager"],2,"Services that push events to Lambda need permission in the function's resource-based policy (lambda add-permission). The execution role controls what the function itself can do.","Task 2.1"],
    ["dva46",0,2,"A user has an IAM policy allowing s3:* on a bucket, and the bucket policy explicitly denies s3:DeleteObject for that user. What happens when the user tries to delete an object?",["The request is denied, because an explicit deny overrides any allow","The request succeeds, because the identity policy allows s3:*","The request succeeds only if the object is unencrypted","The result depends on which policy was created most recently"],0,"In IAM policy evaluation, an explicit deny anywhere wins over any allow. Order of creation is irrelevant.","Task 2.1"],
    ["dva47",0,2,"After signing in to a Cognito user pool, which token contains claims about the user such as email and name for the app to display?",["The access token","The refresh token","The session token from STS","The ID token"],3,"The ID token carries identity claims about the user. The access token carries scopes and groups for authorizing API calls, and the refresh token gets new tokens.","Task 2.1"],
    ["dva48",0,2,"A Lambda function in account A must decrypt data with a customer managed KMS key in account B. What permissions are needed?",["The request succeeds because the identity policy allows all s3 actions","The request is denied because an explicit deny overrides any allow","The request succeeds only if the object is not encrypted with KMS","The result depends on which of the two policies was created last"],1,"Cross-account KMS access needs both sides: the key policy grants the other account, and an IAM policy in that account grants the principal. Either alone is not enough.","Task 2.2"],
    ["dva49",0,2,"Data must be encrypted inside the application before it is sent to any AWS service, so AWS never sees plaintext. What should the developer use?",["Client-side encryption with the AWS Encryption SDK","SSE-S3 default encryption configured on each bucket","SSE-KMS server-side encryption with an AWS managed key","TLS 1.2 or later enforced on every service connection"],0,"Client-side encryption encrypts data before it leaves the application. Server-side options encrypt after the service receives the data, and TLS protects data only in transit.","Task 2.2"],
    ["dva50",0,2,"Application logs in CloudWatch Logs sometimes contain customer email addresses. The team wants those values masked automatically. What should they use?",["A CloudWatch metric filter","A shorter log retention period","A subscription filter to Kinesis","A CloudWatch Logs data protection policy"],3,"Data protection policies detect and mask sensitive data such as email addresses in log events. Metric filters only count patterns, and shorter retention does not hide the data.","Task 2.3"],
    ["dva51",0,2,"A developer wants to replace a broad policy on a role with one that grants only the actions the application actually used in the last 90 days. Which tool helps generate it?",["AWS Trusted Advisor cost optimization checks","IAM Access Analyzer policy generation","Amazon Inspector vulnerability findings for the role","AWS Config conformance packs applied to the account"],1,"IAM Access Analyzer can review CloudTrail logs for a role and generate a policy with only the used actions. Inspector scans workloads for vulnerabilities, not permissions.","Task 2.1"],
    ["dva52",0,3,"A developer has an AWS SAM template and wants to build the code and deploy it for the first time, answering prompts for the stack name and Region. Which commands should be run?",["sam init, then sam local invoke","cdk synth, then cdk deploy","sam build, then sam deploy --guided","aws s3 cp, then aws lambda invoke"],2,"sam build prepares the artifacts and sam deploy --guided asks for settings and saves them to samconfig.toml for later runs. sam local invoke only runs a function locally.","Task 3.1"],
    ["dva53",0,3,"A developer wants to run a Lambda function locally with a sample event before deploying it. Which tool supports this?",["aws lambda invoke against the $LATEST version","CloudFormation change sets","sam local invoke with an event file (using Docker)","CodeDeploy in-place deployment"],2,"SAM CLI emulates the Lambda runtime in a Docker container, so the function can be tested locally with a JSON event. aws lambda invoke calls the deployed function in AWS.","Task 3.2"],
    ["dva54",0,3,"A CloudFormation template references local Lambda code folders. Before deploying with the AWS CLI, what must happen to that code?",["Paste the code inline into the ZipFile property of every function resource","Run aws cloudformation package to upload it to S3 and rewrite the paths","Store the code in Parameter Store and reference each parameter by name","Copy the code to an EC2 instance that CloudFormation reads it from"],1,"The package command zips local artifacts, uploads them to an S3 bucket and outputs a template with S3 locations for deploy to use.","Task 3.1"],
    ["dva55",0,3,"A team deploys a new Lambda version and wants 10% of traffic to go to it for 5 minutes, then all traffic if no alarms fire, with automatic rollback. What should they use?",["CodeDeploy with Canary10Percent5Minutes on an alias","An all-at-once update of the function's $LATEST version","Two separate functions behind an ALB with equal target weights","A new Lambda layer that holds the new code for the function"],0,"CodeDeploy shifts alias weights between versions following the canary configuration and rolls back if CloudWatch alarms go off. Updating $LATEST changes all traffic at once.","Task 3.4"],
    ["dva56",0,3,"Which CodeDeploy traffic-shifting option for Lambda moves traffic in equal increments at fixed intervals, such as 10% every minute?",["Canary","All-at-once","Blue/green with CNAME swap","Linear"],3,"Linear shifts equal percentages at regular intervals until 100%. Canary shifts one first percentage, waits, then moves the rest.","Task 3.4"],
    ["dva57",0,3,"An Elastic Beanstalk production app must keep full capacity during deployments, and a failed deployment must roll back quickly by terminating the new instances. Which deployment policy fits?",["All at once","Immutable","Rolling","Rolling with an additional batch"],1,"Immutable deployments launch new instances in a temporary Auto Scaling group, so the old ones keep serving; rollback just terminates the new group. Rolling reduces capacity during the update.","Task 3.4"],
    ["dva58",0,3,"A developer wants the fastest possible Elastic Beanstalk deployment for a development environment and can accept brief downtime. Which policy should be used?",["Immutable","Traffic splitting","All at once","Rolling with an additional batch"],2,"All at once updates every instance at the same time, which is fastest but causes an outage while instances restart.","Task 3.4"],
    ["dva59",0,3,"A team runs version 2 in a separate Elastic Beanstalk environment and wants to cut over with almost no downtime and an easy way back. What should they do?",["Swap the environment URLs (CNAME swap) between the two environments","Deploy version 2 to the existing environment with the all-at-once policy","Delete the old environment and then rename the new one","Change the Auto Scaling group's minimum size to zero"],0,"A blue/green swap exchanges the CNAMEs so traffic moves to the new environment; swapping back restores the old one.","Task 3.4"],
    ["dva60",0,3,"A pipeline in CodePipeline must pause after deploying to staging until a team lead confirms the release. What should be added?",["A separate AWS account for each of the two stages","Two different Lambda functions deployed with identical code","Separate usage plans and API keys attached to each stage","Stage variables used in the Lambda integration ARN"],3,"Manual approval actions stop the pipeline until someone approves or rejects, optionally sending an SNS notification.","Task 3.4"],
    ["dva61",0,3,"In a CodeBuild buildspec.yml, where should unit tests that must run after the code compiles be placed?",["In the install phase runtime-versions section","In the build or post_build phase commands","In the artifacts files section","In the env variables section"],1,"Phases run in order: install, pre_build, build, post_build. Tests after compilation belong in build or post_build; artifacts only lists output files.","Task 3.3"],
    ["dva62",0,3,"A CodeDeploy deployment to EC2 must run integration tests after the application has started. Which appspec.yml hook should run the test script?",["ValidateService","BeforeInstall","ApplicationStop","DownloadBundle"],0,"ValidateService is the last hook of an EC2/on-premises deployment and runs after ApplicationStart, making it the place to verify the service. DownloadBundle is reserved for the agent and cannot run scripts.","Task 3.3"],
    ["dva63",0,3,"A CodeDeploy deployment to a new set of EC2 instances fails immediately with no lifecycle events recorded. What is the most likely cause?",["Use Fn::GetAtt with the other stack's name to read its resources","Copy the subnet IDs into each template's Mappings section by hand","Add a DependsOn attribute that points at the networking stack","Export the values as outputs and read them with Fn::ImportValue"],3,"On EC2 the CodeDeploy agent carries out deployments; without it, no lifecycle events run. Bundles in S3 and tag-based groups are normal.","Task 3.4"],
    ["dva64",0,3,"An API Gateway API has dev and prod stages, and each stage must call a different alias of the same Lambda function. What should the developer use?",["A separate AWS account for each stage","Two different Lambda functions with identical code","Stage variables referenced in the Lambda integration ARN","Usage plans attached to each stage"],2,"The integration can reference ${stageVariables.alias}, so each stage calls its own alias. Remember to allow API Gateway to invoke each alias.","Task 3.4"],
    ["dva65",0,3,"A developer changed several REST API methods in the API Gateway console, but clients still see the old behavior. What step was missed?",["Enabling CORS on the API","Creating a new usage plan","Deploying the API to the stage","Turning on stage caching"],2,"REST API changes take effect only after a deployment to a stage creates a new snapshot.","Task 3.4"],
    ["dva66",0,3,"A Lambda function's dependencies add up to 1.5 GB, which is too large for a .zip deployment package. How can it still run on Lambda?",["Package it as a container image in Amazon ECR","Split the dependencies across six Lambda layers","Upload the .zip to S3 instead of directly","Store the libraries in environment variables"],0,"Container images up to 10 GB are supported. .zip packages, including layers, are limited to 250 MB unzipped, so extra layers do not help.","Task 3.1"],
    ["dva67",0,3,"Before updating a production CloudFormation stack, a developer wants to see exactly which resources will be modified or replaced. What should they use?",["Stack drift detection","A stack policy","Termination protection","A change set"],3,"A change set lists the proposed changes, including replacements, before you execute it. Drift detection compares current resources to the last deployed template instead.","Task 3.4"],
    ["dva68",0,3,"A networking stack creates a VPC, and application stacks must reference its subnet IDs without hardcoding them. What should the developer use?",["A new Lambda function version published for each setting change","AWS AppConfig with a deployment strategy and validators","Environment variables updated on the function with the AWS CLI","A CloudFormation stack policy that protects the configuration"],1,"Cross-stack references export an output from one stack and import it by name in another. Fn::GetAtt only works within one template.","Task 3.1"],
    ["dva69",0,3,"A team defines infrastructure in TypeScript with the AWS CDK. Which command produces the CloudFormation template without deploying it?",["cdk bootstrap","cdk deploy","cdk destroy","cdk synth"],3,"cdk synth synthesizes the app into CloudFormation templates. cdk bootstrap creates resources the CDK needs in an account and Region.","Task 3.1"],
    ["dva70",0,3,"A front-end team needs an API endpoint that returns a fixed sample JSON response so they can build the UI before the backend exists. What is the quickest option?",["A Lambda function with provisioned concurrency","An Application Load Balancer with fixed targets","An API Gateway mock integration","A DynamoDB table with a sample item"],2,"Mock integrations return a response defined in API Gateway without calling any backend, which suits early testing.","Task 3.2"],
    ["dva71",0,3,"A team wants to roll out a configuration change to their application gradually, validate it against a schema, and roll back automatically if an alarm fires, without redeploying code. What should they use?",["A new Lambda version for each setting change","AWS AppConfig with a deployment strategy and validators","Environment variables updated with the CLI","A CloudFormation stack policy"],1,"AppConfig deploys configuration and feature flags with gradual strategies, validators and CloudWatch alarm rollback. Changing environment variables redeploys the function configuration all at once.","Task 3.4"],
    ["dva72",0,3,"Several teams want to host private npm and PyPI packages and proxy the public registries for their builds. Which service fits?",["AWS CodeArtifact","Amazon ECR","AWS CodeDeploy","Amazon S3 Glacier"],0,"CodeArtifact is a managed artifact repository for npm, PyPI, Maven and more, with upstream connections to public registries. ECR stores container images.","Task 3.1"],
    ["dva73",0,4,"A developer uses X-Ray and wants to filter traces by customer tier (for example, tier = gold). Where should that value be recorded?",["As metadata on the segment","In the function's environment variables","As an annotation on the segment or subsegment","In the trace ID header"],2,"Annotations are indexed key-value pairs you can use in filter expressions. Metadata is stored with the trace but is not indexed for search.","Task 4.2"],
    ["dva74",0,4,"An application on EC2 is instrumented with the X-Ray SDK, but no traces appear in X-Ray. What is the most likely cause?",["The X-Ray daemon or agent is not running, or the role cannot write traces","Active tracing is not turned on for the instance in the Lambda console","The instance has no Elastic IP address, so traces cannot leave it","CloudTrail is disabled in the Region, so trace data is not collected"],0,"On EC2 the SDK sends segments over UDP to a local daemon or agent, which uploads them using the instance role. Lambda's active tracing setting does not apply to EC2.","Task 4.1"],
    ["dva75",0,4,"A Lambda function must publish custom business metrics to CloudWatch without making synchronous PutMetricData calls on every request. What should it use?",["A CloudWatch Logs subscription to Kinesis","The CloudWatch embedded metric format in its log output","An X-Ray sampling rule","CloudTrail data events"],1,"The embedded metric format writes structured JSON log lines that CloudWatch turns into metrics asynchronously, avoiding extra API calls and latency.","Task 4.2"],
    ["dva76",0,4,"During an incident, a developer needs to find the 20 slowest requests from the last hour across many log streams. Which tool fits best?",["AWS Config","Amazon Inspector","IAM Access Analyzer","CloudWatch Logs Insights"],3,"Logs Insights runs queries (filter, stats, sort) across log groups, which suits finding the slowest requests. The other tools are for configuration, vulnerabilities and permissions.","Task 4.1"],
    ["dva77",0,4,"During a traffic spike, synchronous calls to a Lambda function fail with TooManyRequestsException (HTTP 429). What is the most likely cause?",["The function's memory is set too high","The deployment package is larger than 50 MB","The function has hit its concurrency limit","The function's execution role is missing a policy"],2,"429 throttling means no concurrency is available, either the account limit or the function's reserved concurrency. Request a quota increase or adjust reserved concurrency; a missing policy would cause AccessDenied instead.","Task 4.1"],
    ["dva78",0,4,"A CPU-heavy Lambda function takes 9 seconds at 512 MB of memory. What change is most likely to make it faster?",["The function's memory setting is higher than the traffic needs","The deployment package is larger than the 50 MB upload limit","The function's execution role is missing a DynamoDB read policy","The function has reached its concurrency limit"],3,"Lambda allocates CPU in proportion to memory, so more memory often shortens CPU-bound runs and can even lower cost. A longer timeout does not add speed.","Task 4.3"],
    ["dva79",0,4,"A latency-sensitive API backed by Lambda sees slow responses after quiet periods because new execution environments must start. What removes most of this delay?",["Provisioned concurrency on the function's alias","A larger /tmp storage setting","Reserved concurrency set to 0","A dead-letter queue"],0,"Provisioned concurrency keeps initialized environments ready, avoiding cold starts. Reserved concurrency of 0 would stop the function from running at all. Provisioned concurrency is billed while it is configured.","Task 4.3"],
    ["dva80",0,4,"A DynamoDB table using the date as its partition key throttles writes even though total consumed capacity is well below the table's limit. What is the likely cause and fix?",["The table needs a local secondary index on the date attribute","A hot partition; use a partition key with many distinct values","Strongly consistent reads are enabled on the table's queries","The TTL attribute is missing, so old items are never removed"],1,"All of today's writes land on one partition key value, concentrating traffic. A high-cardinality key (or adding a random suffix) spreads writes across partitions.","Task 4.3"],
    ["dva81",0,4,"A REST API returns the same product catalog to thousands of clients, and each request invokes Lambda and reads DynamoDB. How can the developer reduce backend calls with minimal code?",["Increase the Lambda function's memory","Switch DynamoDB to on-demand capacity","Add a dead-letter queue to the function","Enable API Gateway stage caching with a suitable TTL"],3,"Stage caching returns cached responses for the TTL without invoking the backend. Note that the cache is billed by the hour and is not part of the Free Tier.","Task 4.3"],
    ["dva82",0,4,"Users around the world download static images from an S3 bucket and see high latency. What is the best improvement?",["Enable S3 versioning","Serve the bucket through a CloudFront distribution","Move the images to DynamoDB","Enable S3 Object Lock"],1,"CloudFront caches content at edge locations close to users. Versioning and Object Lock protect data but do not reduce latency.","Task 4.3"],
    ["dva83",0,4,"A malformed message in an SQS queue keeps failing and being retried, blocking attention from other work. How should the developer isolate such messages?",["Configure a dead-letter queue with a maxReceiveCount","Shorten the queue's message retention period to 1 minute","Turn off long polling so consumers poll more frequently","Increase the batch size to 10 so bad messages are diluted"],0,"A redrive policy moves a message to the DLQ after it has been received maxReceiveCount times, so it can be inspected without endless retries.","Task 4.1"],
    ["dva84",0,4,"A Lambda function reading a Kinesis data stream shows a steadily rising IteratorAge metric. What does this indicate, and what helps?",["Records are expiring early; reduce the retention period","The function is over-provisioned; lower its memory","The function is falling behind; raise the parallelization factor or add shards","The stream is empty; add a producer"],2,"A growing iterator age means records wait longer before processing. Processing more batches in parallel per shard, adding shards or speeding up the code reduces it.","Task 4.1"],
    ["dva85",0,4,"A developer wants an email when a function's error count exceeds 5 in 5 minutes. What should they configure?",["A CloudWatch alarm on the Errors metric that notifies an SNS email topic","A CloudTrail trail that sends an email for every Lambda API call","An X-Ray group with a filter expression that matches failed traces","A Lambda destination that fires for every successful invocation"],0,"CloudWatch alarms watch a metric against a threshold and can send notifications through SNS.","Task 4.2"],
    ["dva86",0,4,"Requests pass through API Gateway, two Lambda functions and SQS. When a user reports an error, engineers cannot connect the log lines from each component. What logging practice helps most?",["Longer log retention settings on every component's log group","Structured JSON logs carrying a correlation ID for each request","Writing every component's logs to its local /tmp folder instead","Logging only errors and dropping all informational log messages"],1,"A shared correlation (or trace) ID in structured logs lets you search every component for one request, for example with Logs Insights.","Task 4.2"],
    ["dva87",0,4,"An API Gateway REST API calls a Lambda function that sometimes needs 2 minutes to generate a report, and clients get 504 errors. What is the best design change?",["Raise the Lambda timeout to 15 minutes","Increase the Lambda memory to the maximum","Return a job ID right away and process the report asynchronously","Enable caching on the API stage"],2,"REST API integrations time out at 29 seconds by default, so a 504 occurs no matter how long Lambda may run. An asynchronous job pattern avoids holding the request open.","Task 4.3"],
    ["dva88",0,4,"A Lambda function fails with AccessDeniedException when calling s3:GetObject on a bucket in the same account. The bucket has no policy. Where should the developer look first?",["The function's resource-based policy","The function's reserved concurrency","The bucket's CORS configuration","The function's execution role policy"],3,"Calls the function makes are authorized by its execution role. The resource-based policy controls who can invoke the function, and CORS only affects browsers.","Task 4.1"],
    ["dva89",0,4,"A read-heavy app sends the same expensive SQL queries to an RDS database many times per second, and the results change rarely. What should the developer add?",["A larger RDS instance storage volume","An Amazon ElastiCache cache using lazy loading with a TTL","A CloudWatch Logs subscription filter","A second NAT gateway"],1,"Caching query results in ElastiCache cuts repeated database reads, and a TTL limits how stale results can get.","Task 4.3"]
  ]
});
