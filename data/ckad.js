/* Certified Kubernetes Application Developer (CKAD), curriculum v1.35 — generated plan (no hand-written weeks). */
CertHub.register({
  id: "ckad",
  vendor: "The Linux Foundation / CNCF",
  name: "Certified Kubernetes Application Developer",
  short: "CKAD",
  exam: "CKAD (Kubernetes v1.35 curriculum)",
  blurb: "Hands-on Kubernetes exam for developers who design, build, configure, deploy, expose and troubleshoot cloud native applications on a cluster.",
  status: "verified",
  statusNote: "Domains and weights read on Sept 24, 2026 in the CNCF CKAD Curriculum v1.35 PDF (github.com/cncf/curriculum): Application Design and Build 20%, Application Deployment 20%, Application Observability and Maintenance 15%, Application Environment, Configuration and Security 25%, Services and Networking 20%. The Linux Foundation training site could not be opened from here, so exam length (2 hours, 15–20 tasks) and the 66% pass mark come from the Linux Foundation FAQ as quoted in search results; confirm them on the exam page before booking.",
  lastVerified: "2026-09-24",
  notices: [],
  examInfo: {
    questions: "About 15–20 performance-based tasks in a live Kubernetes command line",
    minutes: 120,
    pass: "66%",
    extra: "The real exam has no multiple-choice questions: you solve tasks with kubectl, YAML, Helm and Kustomize on real clusters, with the Kubernetes docs open. The practice exam on this site is a 60-question, 90-minute knowledge check of the same objectives; pair it with timed hands-on practice in kind or minikube (and the killer.sh simulator sessions included with registration)."
  },
  examSim: { questions: 60, minutes: 90 },
  sources: [
    { label: "CNCF CKAD Curriculum v1.35 (PDF, GitHub)", url: "https://github.com/cncf/curriculum/blob/master/CKAD_Curriculum_v1.35.pdf" },
    { label: "Linux Foundation CKAD exam page", url: "https://training.linuxfoundation.org/certification/certified-kubernetes-application-developer-ckad/" },
    { label: "Linux Foundation CKA/CKAD/CKS FAQ", url: "https://docs.linuxfoundation.org/tc-docs/certification/faq-cka-ckad-cks" }
  ],
  planWeeks: 8,
  hoursPerWeek: "6–8",

  domains: [
    {
      id: 1,
      name: "Application Design and Build",
      w: 20,
      topics: [
        "Writing Dockerfiles: base images, layers, multi-stage builds, ENTRYPOINT vs CMD",
        "Building, tagging, saving and loading images with docker or podman (build, tag, save, load)",
        "How Pod `command` and `args` override the image ENTRYPOINT and CMD",
        "Choosing a workload: Deployment, StatefulSet, DaemonSet, Job, CronJob, bare Pod",
        "Jobs: completions, parallelism, backoffLimit, activeDeadlineSeconds, restartPolicy Never/OnFailure",
        "CronJobs: schedule syntax, concurrencyPolicy, history limits, running a job manually from a CronJob",
        "Multi-container patterns: init containers, sidecars (including native sidecars with restartPolicy: Always), adapter and ambassador",
        "Ephemeral volumes: emptyDir (including medium: Memory), configMap/secret/projected volumes",
        "Persistent storage: PersistentVolume, PersistentVolumeClaim, StorageClass, access modes, dynamic provisioning",
        "Mounting volumes with volumeMounts, mountPath, subPath and readOnly"
      ],
      notes: ["Curriculum: define, build and modify container images; choose workload resources; multi-container Pod patterns; persistent and ephemeral volumes"],
      labs: [
        "Write a two-stage Dockerfile for a small Go or Python app, build it with `docker build -t demo:1.0 .`, load it into kind with `kind load docker-image demo:1.0` and run it as a Pod.",
        "In kind or minikube, create a Job with `completions: 6` and `parallelism: 2`, watch it with `kubectl get pods -w`, then turn it into a CronJob and trigger it with `kubectl create job manual --from=cronjob/<name>`.",
        "Build a Pod with an init container that writes a file into an emptyDir and a sidecar that tails a log file the main container writes; then add a PVC (minikube's default StorageClass) and prove the data survives Pod deletion."
      ]
    },
    {
      id: 2,
      name: "Application Deployment",
      w: 20,
      topics: [
        "Deployments and ReplicaSets: how the Pod template, labels and selectors fit together",
        "Rolling updates: maxSurge, maxUnavailable, minReadySeconds; the Recreate strategy",
        "Kubectl rollout status, history, undo (--to-revision), pause and resume; kubectl set image and scale",
        "Blue/green deployments by switching a Service selector between two Deployments",
        "Canary releases with two Deployments behind one Service, weighted by replica count",
        "Helm basics: repositories, charts, releases; helm repo add/update, search, install, upgrade, rollback, uninstall, list",
        "Helm values: helm show values, --set and -f values.yaml, helm template, namespaces with -n and --create-namespace",
        "Kustomize: kustomization.yaml, resources, namePrefix, namespace, commonLabels/labels, images, patches, configMapGenerator",
        "Applying overlays with kubectl apply -k and previewing with kubectl kustomize"
      ],
      notes: ["Curriculum: deployment strategies (blue/green, canary); Deployments and rolling updates; Helm; Kustomize"],
      labs: [
        "Create a Deployment with `kubectl create deployment web --image=nginx:1.26 --replicas=4`, update it with `kubectl set image`, watch `kubectl rollout status`, then roll back with `kubectl rollout undo` and read `kubectl rollout history`.",
        "Build a blue/green setup: deployments web-blue and web-green with label `version`, one Service; switch traffic with `kubectl patch svc` and check with `curl` from a busybox Pod. Then turn it into a 3:1 canary.",
        "Install a public Helm chart into a new namespace, change a value with `helm upgrade --set`, roll back with `helm rollback`; then make a Kustomize base plus a `prod` overlay that changes replicas and the image tag."
      ]
    },
    {
      id: 3,
      name: "Application Observability and Maintenance",
      w: 15,
      topics: [
        "API deprecations and removals: finding current apiVersions with kubectl api-resources, api-versions and explain",
        "Updating manifests to supported API groups (networking.k8s.io/v1 Ingress, batch/v1 CronJob, autoscaling/v2 HPA)",
        "Liveness, readiness and startup probes; httpGet, tcpSocket, exec and grpc handlers; timing fields",
        "What each probe failure does: restart the container vs remove the Pod from Service endpoints",
        "Kubectl get, describe, get events, top pod/node (metrics-server) for monitoring",
        "Container logs: kubectl logs with -c, -f, --previous, --tail, --since, -l and deploy/<name>",
        "Reading Pod status: Pending, ImagePullBackOff, CrashLoopBackOff, OOMKilled, Completed, exit codes",
        "Debugging: kubectl exec, kubectl debug (ephemeral containers and Pod copies), port-forward, temporary busybox Pods",
        "Output tricks for fast troubleshooting: -o wide, -o yaml, jsonpath, --show-labels, --sort-by"
      ],
      notes: ["Curriculum: API deprecations; probes and health checks; built-in CLI monitoring tools; container logs; debugging"],
      labs: [
        "Add liveness, readiness and startup probes to an nginx Deployment in kind; break the readiness path and watch the Pod leave `kubectl get endpointslices`, then break liveness and watch the restart count rise.",
        "Install metrics-server in minikube (`minikube addons enable metrics-server`), run a CPU-burning Pod and compare `kubectl top pod` with its requests and limits; then cause an OOMKilled with a low memory limit and read it in `kubectl describe`.",
        "Deploy three deliberately broken Pods (bad image tag, crashing command, missing ConfigMap) and fix each using only `kubectl describe`, `kubectl logs --previous`, `kubectl get events` and `kubectl debug`."
      ]
    },
    {
      id: 4,
      name: "Application Environment, Configuration and Security",
      w: 25,
      topics: [
        "Extending Kubernetes: CustomResourceDefinitions, custom resources and Operators; discovering them with kubectl get crd and api-resources",
        "Request flow: authentication, authorization (RBAC) and admission control (mutating and validating)",
        "RBAC objects: Role, ClusterRole, RoleBinding, ClusterRoleBinding; kubectl auth can-i with --as",
        "Resource requests and limits for CPU and memory; units (m, Mi, Gi); QoS classes",
        "Namespace ResourceQuota and LimitRange defaults",
        "ConfigMaps: create from literals, files and env files; consume as env, envFrom and volumes",
        "Secrets: generic, docker-registry and tls types; base64 encoding vs encryption; secretKeyRef and volume mounts",
        "ServiceAccounts: serviceAccountName, token projection, automountServiceAccountToken, kubectl create token",
        "SecurityContext at Pod and container level: runAsUser, runAsNonRoot, fsGroup, readOnlyRootFilesystem, allowPrivilegeEscalation",
        "Linux capabilities (add/drop) and Pod Security Admission levels (privileged, baseline, restricted)"
      ],
      notes: ["Curriculum: CRDs and Operators; authentication, authorization and admission; requests, limits and quotas; ConfigMaps; Secrets; ServiceAccounts; SecurityContexts and capabilities"],
      labs: [
        "In kind, create a namespace with a ResourceQuota and a LimitRange, try to create Pods with and without requests, and explain each rejection or default you see.",
        "Create a ConfigMap and a Secret from literals and a file, consume them as env vars and as a volume in one Pod, change the ConfigMap and see which view updates.",
        "Create a ServiceAccount, a Role that can only list Pods and a RoleBinding; check with `kubectl auth can-i --as=system:serviceaccount:<ns>:<sa>`, then run a Pod as non-root with a read-only root filesystem and all capabilities dropped."
      ]
    },
    {
      id: 5,
      name: "Services and Networking",
      w: 20,
      topics: [
        "Service types: ClusterIP, NodePort, LoadBalancer, ExternalName and headless (clusterIP: None)",
        "Selectors, port vs targetPort vs nodePort, named ports",
        "EndpointSlices and why an empty endpoint list means the selector or readiness is wrong",
        "Cluster DNS names: <service>.<namespace>.svc.cluster.local",
        "Kubectl expose, kubectl create service and kubectl port-forward",
        "NetworkPolicy basics: podSelector, policyTypes, ingress and egress rules, default deny",
        "NetworkPolicy peers: podSelector, namespaceSelector, ipBlock; AND vs OR rule semantics; allowing DNS on egress",
        "NetworkPolicy needs a CNI plugin that enforces it (for example Calico or Cilium)",
        "Ingress resources: ingressClassName, host and path rules, pathType Prefix vs Exact, default backend, TLS secrets",
        "Ingress controllers (for example ingress-nginx) and testing with curl and Host headers"
      ],
      notes: ["Curriculum: basic NetworkPolicies; provide and troubleshoot access via Services; Ingress rules"],
      labs: [
        "Expose a Deployment with `kubectl expose` as ClusterIP and NodePort, reach it from a busybox Pod by DNS name, then break the selector and diagnose it from the empty EndpointSlice.",
        "Start minikube with `--cni=calico`, apply a default-deny ingress policy to a namespace, then allow only Pods labeled `role=frontend` to reach the backend on port 8080 and test with `wget` from two Pods.",
        "Enable the minikube ingress addon, create an Ingress that routes /api and /web to two Services on one host, and test with `curl -H 'Host: demo.local' http://$(minikube ip)/api`."
      ]
    }
  ],

  study: {
    1: [
      ["When would you choose a Job, a CronJob, a Deployment, a StatefulSet or a DaemonSet?", "Job: run-to-completion work. CronJob: a Job on a schedule. Deployment: stateless replicas with rolling updates. StatefulSet: stable Pod names and one PVC per replica (databases, quorum systems). DaemonSet: one Pod per node (log or monitoring agents)."],
      ["How do Pod `command` and `args` relate to a Dockerfile's ENTRYPOINT and CMD?", "`command` replaces ENTRYPOINT and `args` replaces CMD. If you set only `args`, the image's ENTRYPOINT runs with your arguments; if you set `command` alone, the image's CMD is ignored."],
      ["What is the difference between an init container and a sidecar?", "Init containers run one after another to completion before app containers start (setup, waiting for dependencies). A sidecar runs alongside the app for the Pod's life (log shipping, proxies); native sidecars are init containers with restartPolicy: Always so they start first and keep running."],
      ["Compare emptyDir, hostPath and a PersistentVolumeClaim.", "emptyDir lives and dies with the Pod and is shared by its containers. hostPath mounts a node directory, tying the Pod to that node and posing a security risk. A PVC requests durable storage that outlives the Pod and is bound to a PV, often provisioned dynamically by a StorageClass."],
      ["Why use a multi-stage Dockerfile?", "Build tools and sources stay in an earlier stage; only the compiled artifact is copied into a small runtime image. The result is smaller, faster to pull and has less attack surface."]
    ],
    2: [
      ["Explain maxSurge and maxUnavailable with 4 replicas, maxSurge 1 and maxUnavailable 0.", "Kubernetes may run up to 5 Pods (4 + 1 surge) and never fewer than 4 available. It starts one new Pod, waits for it to be ready, removes an old one and repeats, so capacity never drops."],
      ["How do you do blue/green and canary with plain Kubernetes objects?", "Blue/green: two full Deployments with different version labels; the Service selector points at one and you switch it in one change. Canary: both Deployments share a label the Service selects, and the replica ratio (for example 9 to 1) sets roughly how much traffic the new version gets."],
      ["What does `kubectl rollout undo` actually do?", "It rolls the Deployment's Pod template back to an earlier ReplicaSet revision (the previous one, or --to-revision=N), which starts a normal rolling update back to that template."],
      ["Helm release vs chart vs repository?", "A chart is a package of templates and default values; a repository hosts charts; a release is one installed instance of a chart in a cluster with its own name, values and revision history."],
      ["What problem does Kustomize solve and how is it applied?", "It customizes plain YAML without templates: a base plus overlays that add prefixes, labels, namespaces, image tags and patches. Render with `kubectl kustomize <dir>` and apply with `kubectl apply -k <dir>`."]
    ],
    3: [
      ["Contrast liveness, readiness and startup probes.", "Liveness failure makes the kubelet restart the container. Readiness failure removes the Pod from Service endpoints without restarting it. A startup probe holds off the other two until the app has started, which protects slow-starting apps from early liveness restarts."],
      ["A Pod is in CrashLoopBackOff. What do you run first?", "`kubectl describe pod` for events, exit code and last state, then `kubectl logs <pod> --previous` (add -c for multi-container Pods) to read the output of the crashed instance."],
      ["How do you find the right apiVersion for a resource on the current cluster?", "`kubectl api-resources` lists kinds with their group/version; `kubectl explain <kind>` shows the preferred version and fields; `kubectl api-versions` lists all served group/versions."],
      ["What does `kubectl debug` add over `kubectl exec`?", "It can inject an ephemeral container with debugging tools into a running Pod (useful for distroless images with no shell), make a debug copy of a Pod with changes, or start a debugging Pod on a node."],
      ["What does exit code 137 with reason OOMKilled tell you?", "The container used more memory than its limit and the kernel killed it (137 = 128 + SIGKILL 9). Raise the memory limit or fix the app's memory use."]
    ],
    4: [
      ["Describe the path a kubectl request takes through the API server.", "Authentication (who are you: certificates, tokens), then authorization (may you do this: RBAC), then admission control (mutating then validating admission, such as quotas, Pod Security Admission, webhooks), then persistence to etcd."],
      ["How do requests and limits behave for CPU and memory?", "The scheduler places Pods using requests. At runtime CPU above the limit is throttled, while memory above the limit gets the container OOMKilled. Requests equal to limits for every container gives the Guaranteed QoS class."],
      ["ResourceQuota vs LimitRange?", "ResourceQuota caps totals for a namespace (for example total requests.cpu or number of Pods). LimitRange sets per-container defaults and min/max. With a CPU quota in place, Pods without requests are rejected unless a LimitRange supplies defaults."],
      ["Are Secrets encrypted?", "Not by default: values are only base64-encoded in the API. Protect them with RBAC, encryption at rest configured on the API server, and by mounting only what each Pod needs."],
      ["Why set automountServiceAccountToken: false and a dedicated ServiceAccount?", "Most apps never call the API, so not mounting a token removes a credential an attacker could steal. A dedicated ServiceAccount with a narrow Role gives apps that do need the API only the permissions they need."],
      ["Which securityContext settings form a typical hardened container?", "runAsNonRoot: true (with a non-zero runAsUser), allowPrivilegeEscalation: false, readOnlyRootFilesystem: true, capabilities drop [\"ALL\"], and seccompProfile type RuntimeDefault, which together meet the restricted Pod Security Standard."]
    ],
    5: [
      ["Explain port, targetPort and nodePort on a Service.", "port is what clients use on the Service's ClusterIP; targetPort is the container port traffic is sent to; nodePort (30000–32767 by default) opens the same port on every node for NodePort and LoadBalancer Services."],
      ["A Service exists but curl to it times out. How do you troubleshoot?", "Check `kubectl get endpointslices -l kubernetes.io/service-name=<svc>`: empty means the selector does not match Pod labels or Pods are not ready. Then verify targetPort matches the container port and that no NetworkPolicy blocks the traffic."],
      ["What does a default-deny ingress NetworkPolicy look like?", "A policy with `podSelector: {}` (all Pods in the namespace), `policyTypes: [Ingress]` and no ingress rules. Allowed traffic is then added with further policies, which are additive."],
      ["In a NetworkPolicy `from` list, what is the difference between one element with podSelector and namespaceSelector and two separate elements?", "In one element they are ANDed: Pods with that label in namespaces with that label. As two list items they are ORed: any Pod with the label in the policy's namespace, or any Pod in matching namespaces."],
      ["What do you need for an Ingress to work?", "An Ingress controller running in the cluster (such as ingress-nginx), an Ingress whose ingressClassName matches it, rules pointing at existing Services and ports, and a TLS Secret if you terminate HTTPS."]
    ]
  },

  questions: [
    /* ---------- Domain 1: Application Design and Build (18) ---------- */
    ["ck1",0,1,"A developer needs to run a database migration once and have Kubernetes retry it if the container fails, then stop. Which resource fits best?",["Deployment","DaemonSet","Job","ReplicaSet"],2,"A Job runs Pods to successful completion and retries failures up to backoffLimit. A Deployment would restart the finished container forever to keep replicas running.","Workload resources"],
    ["ck2",0,1,"A team must run a log-collection agent on every node, including nodes added later. Which workload resource should they use?",["DaemonSet","CronJob","StatefulSet","Deployment"],0,"A DaemonSet schedules one Pod per eligible node and adds Pods automatically when nodes join. A Deployment spreads replicas without guaranteeing one per node.","Workload resources"],
    ["ck3",0,1,"A clustered database needs stable Pod names (db-0, db-1) and its own PersistentVolumeClaim per replica. Which resource provides both?",["Job with parallelism set to 3","Deployment with one shared ReadWriteMany PVC","DaemonSet with a hostPath volume","StatefulSet with volumeClaimTemplates"],3,"StatefulSets give ordinal, stable identities and create a PVC per replica from volumeClaimTemplates. A Deployment's replicas are interchangeable and would share one claim.","Workload resources"],
    ["ck4",0,1,"A report CronJob must run every day at 02:30. Which schedule is correct?",["2 30 * * *","30 2 * * *","* * 2 30 *","0 2,30 * * *"],1,"Cron fields are minute, hour, day of month, month, day of week, so 30 2 * * * is 02:30 daily. \"2 30 * * *\" puts 30 in the hour field, which is invalid.","CronJobs"],
    ["ck5",0,1,"A CronJob sometimes runs longer than its interval, and overlapping runs corrupt data. Which setting prevents a new run from starting while the previous one is still active?",["concurrencyPolicy: Replace","successfulJobsHistoryLimit: 0","startingDeadlineSeconds: 0","concurrencyPolicy: Forbid"],3,"Forbid skips the new run if the previous Job is still running. Replace would kill the running Job and start a new one, which is still an interrupted run.","CronJobs"],
    ["ck6",0,1,"You want to test a CronJob named nightly right now without waiting for its schedule. Which command does this?",["kubectl run nightly --restart=OnFailure","kubectl rollout restart cronjob/nightly","kubectl create job test-run --from=cronjob/nightly","kubectl patch cronjob nightly -p '{\"spec\":{\"suspend\":false}}'"],2,"`kubectl create job <name> --from=cronjob/<cj>` creates a Job from the CronJob's jobTemplate immediately. Unsuspending only lets future scheduled runs happen.","CronJobs"],
    ["ck7",0,1,"A Job spec has `completions: 6` and `parallelism: 2`. How does it run?",["Six Pods start at once and two must succeed","Up to two Pods run at a time until six have succeeded","Two Pods run in total, each retried six times","Six Pods run one after another, each one repeated twice"],1,"completions is the number of successful Pods required and parallelism caps how many run concurrently. It is not a retry count; retries are controlled by backoffLimit.","Jobs"],
    ["ck8",0,1,"A Job Pod template sets `restartPolicy: Always` and the API server rejects it. Why?",["Jobs require restartPolicy Never or OnFailure","Jobs require the restartPolicy field to be omitted","Always is only valid for init containers in Jobs","restartPolicy must be set on each container, not the Pod"],0,"Job Pods must terminate, so only Never or OnFailure are allowed. Always is the default for Deployments, which keep containers running.","Jobs"],
    ["ck9",0,1,"An image has `ENTRYPOINT [\"python\"]` and `CMD [\"app.py\"]`. The Pod sets `args: [\"worker.py\"]` and no command. What runs?",["python worker.py","worker.py with no interpreter","python app.py worker.py","python app.py"],0,"args replaces CMD while the image ENTRYPOINT is kept, giving python worker.py. Setting command instead would have replaced ENTRYPOINT.","Container images"],
    ["ck10",0,1,"A Go service's image is 900 MB because it contains the compiler and source code. What is the best way to shrink it?",["Add a .dockerignore listing the go.mod file","Squash layers by chaining every RUN instruction with &&","Run the container with a lower memory limit","Use a multi-stage build that copies only the binary"],3,"A multi-stage build compiles in one stage and copies just the binary into a small runtime image. Chaining RUN commands trims a few layers but still ships the whole toolchain.","Container images"],
    ["ck11",0,1,"On the exam you must build an image from ./app tagged myapp:2.0 and save it as /tmp/myapp.tar. Which docker (or podman) commands do this?",["docker create myapp:2.0 ./app, then docker export myapp:2.0 > /tmp/myapp.tar","docker build -t myapp:2.0 ./app, then docker save -o /tmp/myapp.tar myapp:2.0","docker build ./app --name myapp:2.0, then docker push /tmp/myapp.tar","docker commit ./app myapp:2.0, then docker load -i /tmp/myapp.tar"],1,"build -t tags the image and save -o writes the image (all layers) to a tarball. export works on containers, not images, and load does the opposite of save.","Container images"],
    ["ck12",0,1,"A Pod must wait until a database Service resolves in DNS before the app container starts. Which design fits?",["A sidecar container that loops forever","A postStart lifecycle hook on the application container","An init container that loops until nslookup succeeds","A readiness probe on the database Service"],2,"Init containers run to completion before app containers start, so a DNS check loop there blocks startup. postStart runs alongside the entrypoint and does not delay it.","Multi-container Pods"],
    ["ck13",0,1,"In current Kubernetes, how do you declare a native sidecar that starts before the app and keeps running for the Pod's lifetime?",["Add it under containers with the label role: sidecar","Add it under initContainers with restartPolicy: Always","Add it under ephemeralContainers in the Pod spec","Set shareProcessNamespace: true on the Pod"],1,"A native sidecar is an init container with its own restartPolicy: Always, so it starts first and is not waited on to finish. Labels have no special meaning to the kubelet.","Multi-container Pods"],
    ["ck14",0,1,"Two containers in one Pod must share scratch files, and the data can be lost when the Pod is deleted. Which volume type is simplest?",["hostPath","persistentVolumeClaim","configMap","emptyDir"],3,"emptyDir is created with the Pod, shared by its containers and deleted with it. hostPath would tie the Pod to one node's filesystem for no benefit.","Volumes"],
    ["ck15",0,1,"Read this snippet: `volumes: [{name: cache, emptyDir: {medium: Memory, sizeLimit: 64Mi}}]`. What is true about the volume?",["It is backed by tmpfs and counts toward memory use","It is stored on the node's disk and survives restarts","It is a PVC that requests 64Mi from the StorageClass","It is shared across every Pod on the node"],0,"medium: Memory mounts the emptyDir as tmpfs, so its contents use RAM and count against the container's memory. It is still per-Pod and ephemeral.","Volumes"],
    ["ck16",0,1,"A PVC with `accessModes: [ReadWriteOnce]` is bound. What does ReadWriteOnce guarantee?",["Only one Pod in the cluster may ever read it","It can be written once and then becomes read-only","It can be mounted read-write by a single node","Every node can mount it read-write at once"],2,"ReadWriteOnce means read-write by a single node; several Pods on that node can still use it. ReadWriteOncePod is the mode that limits it to one Pod.","Volumes"],
    ["ck17",0,1,"A Pod stays Pending and describe shows \"pod has unbound immediate PersistentVolumeClaims\". The PVC names storageClassName: fast, which does not exist. What fixes it?",["Delete and recreate the Pod","Add a readiness probe to the Pod","Change the PVC to use an existing StorageClass","Add a nodeSelector so the Pod lands on a storage node"],2,"Without a matching StorageClass nothing provisions a volume, so the claim stays Pending. storageClassName on a PVC cannot be edited in place, so recreate the PVC with a valid class; recreating the Pod alone changes nothing.","Volumes"],
    ["ck18",0,1,"In a Pod spec, a volumeMount uses `name: data` but the volumes list only contains `name: app-data`. What happens?",["The API rejects the Pod as invalid","The mount silently uses an emptyDir","The kubelet mounts app-data because it is the only volume","The Pod starts and the mount path is left empty"],0,"Every volumeMount name must match a volume defined in the Pod, so validation fails when the Pod is created. Nothing is guessed or defaulted.","Volumes"],

    /* ---------- Domain 2: Application Deployment (18) ---------- */
    ["ck19",0,2,"Deployment web runs container web with nginx:1.25. Which command starts a rolling update to nginx:1.27?",["kubectl replace deployment web nginx:1.27","kubectl rollout update deployment/web --image=nginx:1.27","kubectl edit pod web --image=nginx:1.27","kubectl set image deployment/web web=nginx:1.27"],3,"`kubectl set image deployment/<name> <container>=<image>` changes the Pod template, which triggers a rollout. Editing a Pod does not change the Deployment and is overwritten.","Rolling updates"],
    ["ck20",0,2,"After a bad release, you need Deployment api back on revision 3 specifically. Which command?",["kubectl rollout history deployment/api --revision=3","kubectl rollout undo deployment/api --to-revision=3","kubectl rollout restart deployment/api --revision=3","kubectl apply deployment/api --revision=3"],1,"rollout undo with --to-revision rolls the template back to that revision. history with --revision only shows details of it.","Rolling updates"],
    ["ck21",0,2,"A Deployment has 4 replicas, `maxSurge: 1` and `maxUnavailable: 0`. What is the maximum number of Pods during a rollout?",["3","4","8","5"],3,"maxSurge allows one Pod above the desired 4, and maxUnavailable 0 means none are removed before replacements are ready, so the peak is 5.","Rolling updates"],
    ["ck22",0,2,"An app cannot run two versions at the same time because of a schema lock. Downtime is acceptable. Which Deployment strategy fits?",["RollingUpdate with maxSurge 25%","RollingUpdate with maxUnavailable 0","Recreate","A canary with one replica"],2,"Recreate terminates all old Pods before creating new ones, so versions never overlap. Any RollingUpdate setting runs old and new Pods together for a while.","Deployment strategies"],
    ["ck23",0,2,"You run web-blue and web-green Deployments, labeled version=blue and version=green. How do you cut all traffic over to green at once?",["Scale web-blue to zero, then wait for rollout","Point the Service selector at version=green","Run kubectl rollout undo on web-blue","Add version=green to the web-blue Pod template"],1,"In blue/green the Service selector decides which set receives traffic, and patching it switches everything in one step while blue stays ready for rollback. Scaling blue down first causes an outage window.","Deployment strategies"],
    ["ck24",0,2,"Service shop selects app=shop. Deployment shop-v1 has 9 replicas and shop-v2 has 1, both labeled app=shop. What is this setup?",["A canary sending about 10% of traffic to v2","A blue/green deployment","A Recreate rollout in progress","A StatefulSet partition update"],0,"Both versions match the Service selector, so traffic splits roughly by Pod count: 1 of 10. Blue/green would have the Service select only one version at a time.","Deployment strategies"],
    ["ck25",0,2,"Which change to a Deployment creates a new ReplicaSet revision?",["Changing an environment variable in the Pod template","Scaling replicas from 3 to 6","Adding an annotation to the Deployment's own metadata section","Changing revisionHistoryLimit"],0,"Only changes to spec.template trigger a rollout and a new ReplicaSet. Scaling changes the replica count of the existing ReplicaSet.","Deployments"],
    ["ck26",0,2,"You need to make several changes to a Deployment without triggering a rollout after each one. Which commands help?",["kubectl scale --replicas=0, then scale back","kubectl annotate deployment --overwrite, then kubectl rollout restart","kubectl delete deployment, then apply it again","kubectl rollout pause, apply changes, kubectl rollout resume"],3,"Pausing a rollout lets you batch template changes; resume starts one rollout with all of them. Scaling to zero causes downtime.","Deployments"],
    ["ck27",0,2,"You want to install the bitnami/nginx chart as a release named web in namespace frontend, which does not exist yet. Which command?",["helm install bitnami/nginx web -n frontend","helm install web bitnami/nginx -n frontend --create-namespace","helm create web bitnami/nginx --namespace frontend --create-namespace","helm upgrade bitnami/nginx --name web -n frontend"],1,"The syntax is helm install <release> <chart>, and --create-namespace makes the namespace. helm create scaffolds a new local chart instead.","Helm"],
    ["ck28",0,2,"Before installing a chart, you want to see which settings you can override. Which command lists its default values?",["helm get values <chart>","helm list --values","helm show values <chart>","helm status <chart>"],2,"helm show values prints the chart's values.yaml. helm get values reads the values of an installed release, not a chart.","Helm"],
    ["ck29",0,2,"Release api was upgraded to revision 4 and is failing. Which command returns it to revision 3?",["helm undo api --revision 3","helm rollback api 3","helm upgrade api --revision 3","helm history api 3"],1,"helm rollback <release> <revision> redeploys an earlier revision, creating a new revision 5. helm history only lists revisions.","Helm"],
    ["ck30",0,2,"You must find every Helm release in the cluster, across all namespaces. Which command?",["helm search repo --all","kubectl get helm -A","helm status --all","helm list -A"],3,"helm list -A (--all-namespaces) lists releases everywhere. helm search looks for charts in repositories, not installed releases.","Helm"],
    ["ck31",0,2,"You want to render a chart's manifests locally with your values file without installing anything. Which command?",["helm template myrel ./chart -f values.yaml","helm install --dry-run=false","helm package ./chart -f values.yaml","helm pull ./chart --render"],0,"helm template renders the templates to YAML on your machine. helm package only builds a .tgz archive of the chart.","Helm"],
    ["ck32",0,2,"A directory contains kustomization.yaml listing deployment.yaml and service.yaml. Which command applies the customized result?",["kubectl apply -f kustomization.yaml","kubectl create -r ./overlay","kubectl apply -k ./overlay","kubectl kustomize apply ./overlay"],2,"-k tells kubectl to build the kustomization in that directory and apply the output. Applying kustomization.yaml with -f would send the Kustomization file itself, which is not a cluster resource.","Kustomize"],
    ["ck33",0,2,"In a Kustomize overlay you want every resource name to start with prod- and every image nginx to use tag 1.27. Which fields do this?",["commonAnnotations and replicas","nameSuffix and patches","namePrefix and images","generatorOptions and images"],2,"namePrefix prepends to resource names and images overrides image names or tags. nameSuffix appends instead of prepending.","Kustomize"],
    ["ck34",0,2,"You want to check what an overlay will produce before applying it. Which command prints the rendered YAML?",["kubectl kustomize ./overlay","kubectl diff -f ./overlay","kubectl get -k ./overlay","kubectl explain -k ./overlay"],0,"kubectl kustomize <dir> builds and prints the output without contacting the API for changes. kubectl diff compares against the cluster and needs -k to read a kustomization.","Kustomize"],
    ["ck35",0,2,"After `kubectl apply` you want to wait until a Deployment rollout finishes or fails. Which command?",["kubectl rollout history deployment/web","kubectl get deployment web --watch-only","kubectl wait pod --for=complete","kubectl rollout status deployment/web"],3,"rollout status blocks until the rollout completes or exceeds its progress deadline. rollout history only shows past revisions.","Rolling updates"],
    ["ck36",0,2,"You try to change `spec.selector.matchLabels` on an existing apps/v1 Deployment and the API rejects it. Why?",["Selectors can only be changed with kubectl edit, not kubectl apply","The selector of an apps/v1 Deployment is immutable","Labels must be changed on the ReplicaSet first","Selectors cannot contain more than one label"],1,"In apps/v1 the Deployment selector cannot be changed after creation, so you must create a new Deployment. The editing tool makes no difference.","Deployments"],

    /* ---------- Domain 3: Application Observability and Maintenance (14) ---------- */
    ["ck37",0,3,"An old manifest uses `apiVersion: extensions/v1beta1` for an Ingress and fails on a current cluster. What should it use?",["networking.k8s.io/v1beta1","ingress.k8s.io/v1","apps/v1","networking.k8s.io/v1"],3,"Ingress graduated to networking.k8s.io/v1 and the beta versions were removed in 1.22. networking.k8s.io/v1beta1 is one of those removed versions.","API deprecations"],
    ["ck38",0,3,"Which command shows each resource kind with its API group/version and short name on the current cluster?",["kubectl cluster-info","kubectl version","kubectl api-resources","kubectl config view"],2,"api-resources lists every served kind with group/version, short name and whether it is namespaced. kubectl version only reports client and server versions.","API deprecations"],
    ["ck39",0,3,"A CronJob manifest uses `apiVersion: batch/v1beta1` and is rejected. What should the apiVersion be?",["batch/v2","batch/v1","apps/v1","cronjob/v1"],1,"CronJob is stable in batch/v1, and batch/v1beta1 was removed in Kubernetes 1.25. There is no batch/v2.","API deprecations"],
    ["ck40",0,3,"A Pod's readiness probe starts failing while its liveness probe still passes. What happens?",["The Pod is removed from the Service's endpoints","The kubelet restarts the container","The Pod is evicted to another node","The Deployment automatically rolls back to the previous revision"],0,"Readiness failure only stops traffic by removing the Pod from endpoints. Restarting the container is what a liveness failure does.","Probes"],
    ["ck41",0,3,"A Java app takes up to 3 minutes to start, and its liveness probe kills it during startup. What is the best fix?",["Add a startupProbe that covers the startup time","Remove the liveness probe entirely","Set the liveness probe's timeoutSeconds to 180 seconds","Change restartPolicy to Never"],0,"A startupProbe suspends liveness and readiness checks until it succeeds, protecting slow starts while keeping deadlock detection afterward. A 180-second timeout would make every later check extremely slow.","Probes"],
    ["ck42",0,3,"Read this probe: `livenessProbe: {httpGet: {path: /healthz, port: 8080}, periodSeconds: 10, failureThreshold: 3}`. When is the container restarted?",["After one failed check","Only if /healthz returns a 5xx code three times in a minute","After 10 failed checks","After three failed checks in a row, about 30 seconds"],3,"The kubelet probes every 10 seconds and restarts after 3 consecutive failures. Any status outside 200–399, or no response, counts as a failure.","Probes"],
    ["ck43",0,3,"A container has no HTTP endpoint but accepts TCP connections on port 5432. Which probe handler is most appropriate?",["httpGet on port 5432","tcpSocket on port 5432","exec running curl localhost:5432","grpc on port 5432"],1,"tcpSocket succeeds if a TCP connection can be opened, which suits a database port. httpGet would fail because the server does not speak HTTP.","Probes"],
    ["ck44",0,3,"Container app in Pod api keeps restarting. Which command shows the output of the previous, crashed instance?",["kubectl describe logs api -c app","kubectl logs api --since=prev","kubectl logs api -c app --previous","kubectl get logs api app -p=false"],2,"--previous (-p) returns logs from the last terminated instance of the container. Current logs may be empty right after a restart.","Container logs"],
    ["ck45",0,3,"You want live logs from all Pods labeled app=web, in every container. Which command works?",["kubectl logs deployment -A app=web","kubectl logs -l app=web --all-containers -f","kubectl get logs --selector app=web","kubectl describe pods -l app=web --follow"],1,"-l selects Pods by label, --all-containers includes each container and -f follows output. describe shows events, not log streams.","Container logs"],
    ["ck46",0,3,"`kubectl top pod` returns \"Metrics API not available\". What is missing?",["The kubelet's --read-only-port","kube-state-metrics in kube-system","A Prometheus Operator CRD","metrics-server running in the cluster"],3,"kubectl top reads the metrics.k8s.io API, which metrics-server provides. kube-state-metrics exposes object state for Prometheus, not the Metrics API.","CLI monitoring"],
    ["ck47",0,3,"A Pod shows status ImagePullBackOff. What is the most likely cause?",["Wrong image name or tag, or missing registry credentials","The container exceeded its memory limit","The readiness probe path returns 404","The node has too little free CPU and memory to schedule the Pod"],0,"ImagePullBackOff means the kubelet could not pull the image: wrong name or tag, private registry without imagePullSecrets, or network issues. A memory problem shows as OOMKilled.","Debugging"],
    ["ck48",0,3,"A container's last state shows `Reason: OOMKilled, Exit Code: 137`. What does this mean?",["The liveness probe failed three times in a row and timed out","The node ran out of disk space","The container exceeded its memory limit and was killed","The image entrypoint was not found"],2,"OOMKilled means memory use passed the limit; 137 is 128 plus SIGKILL (9). A missing entrypoint gives a start error, usually exit code 127 or 128.","Debugging"],
    ["ck49",0,3,"A distroless container has no shell, so kubectl exec fails. How can you inspect its processes and network?",["kubectl attach --image=busybox","kubectl cp busybox <pod>:/bin/sh","kubectl debug -it <pod> --image=busybox --target=<container>","kubectl run --overrides with hostPID"],2,"kubectl debug adds an ephemeral container with tools; --target shares the target container's process namespace. Copying a shell binary into a distroless image is unreliable and changes the container.","Debugging"],
    ["ck50",0,3,"You need to list events in a namespace in the order they happened. Which command?",["kubectl get events --sort-by=.metadata.creationTimestamp","kubectl describe events --order=time","kubectl logs events -n <ns>","kubectl get events -o wide --watch-only=false"],0,"--sort-by with a JSONPath to creationTimestamp orders events by time. describe has no ordering flag, and events are not logs.","CLI monitoring"],

    /* ---------- Domain 4: Application Environment, Configuration and Security (22) ---------- */
    ["ck51",0,4,"A vendor installed an Operator, and you need to see which new resource kinds it added. Which command is quickest?",["kubectl get apiservices -o yaml","kubectl get operators --all","kubectl describe nodes","kubectl get crd"],3,"CustomResourceDefinitions register new kinds, so listing CRDs shows what the Operator added. There is no built-in operators resource unless another CRD defines one.","CRDs and Operators"],
    ["ck52",0,4,"What best describes an Operator in Kubernetes?",["A cluster admin role with full RBAC rights over every namespace","A controller that manages an app via custom resources","A mutating webhook that injects sidecars","A Helm chart for installing CRDs"],1,"An Operator is a custom controller that watches custom resources and reconciles the application toward the declared state. A Helm chart may install one but is not itself a controller.","CRDs and Operators"],
    ["ck53",0,4,"In what order does the API server process a request to create a Pod?",["Authorization, authentication, admission","Admission, authentication, authorization","Authentication, admission, authorization","Authentication, authorization, admission"],3,"The server first identifies the caller, then checks permission with RBAC, then runs admission controllers before storing the object. Admission can only act on requests that were already allowed.","AuthN, AuthZ, admission"],
    ["ck54",0,4,"A Pod is rejected with a message from ResourceQuota even though RBAC allows the user to create Pods. Which stage rejected it?",["Authentication","Authorization","Admission control","The scheduler"],2,"ResourceQuota is enforced by an admission controller after authorization succeeds. The scheduler never sees a rejected Pod.","AuthN, AuthZ, admission"],
    ["ck55",0,4,"You need ServiceAccount app in namespace dev to only get, list and watch Pods in dev. Which object pair fits?",["ClusterRole and ClusterRoleBinding","Role and RoleBinding in dev","Role in dev and ClusterRoleBinding","ClusterRoleBinding to the view ClusterRole"],1,"A Role and RoleBinding grant namespaced permissions only in dev. A ClusterRoleBinding would grant access in every namespace.","RBAC"],
    ["ck56",0,4,"Which command checks whether ServiceAccount app in namespace dev may list Secrets there?",["kubectl auth can-i list secrets -n dev --as=system:serviceaccount:dev:app","kubectl auth whoami --sa app -n dev","kubectl get rolebinding app --can-i=secrets","kubectl describe sa app --permissions"],0,"auth can-i with --as impersonates the ServiceAccount using its full username. auth whoami only reports your own identity.","RBAC"],
    ["ck57",0,4,"Which command creates a Role named pod-reader with get, list and watch on Pods?",["kubectl create role pod-reader --verb=get,list,watch --resource=pods","kubectl create rolebinding pod-reader --verb=get --resource=pods","kubectl create clusterrole pod-reader --role=get,list,watch","kubectl apply role pod-reader --pods=read"],0,"create role takes --verb and --resource. A RoleBinding links a Role to subjects and does not define verbs itself.","RBAC"],
    ["ck58",0,4,"A container has `requests.memory: 256Mi` and `limits.memory: 512Mi`. It grows to 600Mi. What happens?",["The kubelet throttles it back to 512Mi","Nothing, because only requests are enforced","The scheduler moves it to a bigger node","It is OOMKilled and restarted"],3,"Memory cannot be throttled, so exceeding the limit gets the container OOMKilled. Throttling applies to CPU limits.","Requests and limits"],
    ["ck59",0,4,"A container has `limits.cpu: 500m` and tries to use two full cores. What happens?",["It is killed with exit code 137","It is throttled to about half a core","The Pod is evicted immediately","It is allowed because CPU is compressible"],1,"500m is 0.5 CPU and CFS quota throttles usage to that. Being killed happens for memory, not CPU.","Requests and limits"],
    ["ck60",0,4,"Every container in a Pod sets CPU and memory requests equal to their limits. Which QoS class does the Pod get?",["BestEffort","Burstable","Guaranteed","Critical"],2,"Requests equal to limits for CPU and memory in all containers gives Guaranteed. Burstable applies when some requests are set but do not match limits.","Requests and limits"],
    ["ck61",0,4,"Namespace team-a has a ResourceQuota on requests.cpu. A Pod without CPU requests is rejected. What prevents this without editing each manifest?",["A PriorityClass with a high value","A LimitRange with default requests","A HorizontalPodAutoscaler","A PodDisruptionBudget"],1,"A LimitRange injects default requests and limits into containers that omit them, satisfying the quota. A PriorityClass affects scheduling order, not quotas.","Quotas"],
    ["ck62",0,4,"Which command creates a ConfigMap named app-cfg with key MODE=prod and the contents of app.properties?",["kubectl apply configmap app-cfg MODE=prod app.properties","kubectl create secret generic app-cfg --from-literal=MODE=prod --from-file=app.properties","kubectl create configmap app-cfg --env=MODE=prod --file app.properties","kubectl create configmap app-cfg --from-literal=MODE=prod --from-file=app.properties"],3,"--from-literal adds key=value pairs and --from-file adds a key named after the file. create secret would make a Secret, not a ConfigMap.","ConfigMaps"],
    ["ck63",0,4,"A Pod should load every key of ConfigMap app-cfg as an environment variable. Which field does this with the least YAML?",["envFrom with configMapRef: {name: app-cfg}","env with valueFrom.configMapKeyRef for each key","volumes with configMap: {name: app-cfg}","spec.configMapName: app-cfg"],0,"envFrom imports all keys at once. configMapKeyRef works but needs one entry per key, and a volume makes files, not env vars.","ConfigMaps"],
    ["ck64",0,4,"A ConfigMap is used both as env vars and as a mounted volume (no subPath). You update the ConfigMap. What changes in running Pods?",["Both update within seconds","The env vars update right away; the mounted files stay the same","The mounted files update eventually; env vars do not","Nothing updates until the node reboots"],2,"The kubelet refreshes projected ConfigMap volumes, but env vars are read only at container start. Env vars change only after the Pod restarts.","ConfigMaps"],
    ["ck65",0,4,"A teammate says Kubernetes Secrets are secure because their values look scrambled in YAML. What is accurate?",["Values are AES-encrypted by kubectl on your machine before they are uploaded","Values are hashed and cannot be read back","Values are only base64-encoded unless encryption at rest is set up","Values are stored only in node memory"],2,"base64 is an encoding that anyone can reverse; protection comes from RBAC and API server encryption at rest. Secrets can be read back, so they are not hashed.","Secrets"],
    ["ck66",0,4,"Which command creates a Secret named db-cred with password=S3cret?",["kubectl create secret generic db-cred --from-literal=password=S3cret","kubectl create secret tls db-cred --from-literal=password=S3cret","kubectl create configmap db-cred --secret password=S3cret","kubectl create secret db-cred password=S3cret --type=opaque"],0,"generic is the subcommand for Opaque Secrets from literals or files. The tls type needs --cert and --key files.","Secrets"],
    ["ck67",0,4,"Read this snippet: `env: [{name: DB_PASS, valueFrom: {secretKeyRef: {name: db-cred, key: pwd}}}]`. The Secret has only a key named password. What happens?",["DB_PASS is set to an empty string","The Pod starts and ignores DB_PASS","Kubernetes falls back to the first key found in the Secret","The container fails to start with a config error"],3,"A secretKeyRef to a missing key causes CreateContainerConfigError unless optional: true is set. Kubernetes never guesses another key.","Secrets"],
    ["ck68",0,4,"Pod web must run as ServiceAccount web-sa, which already exists. Which Pod field sets this?",["metadata.serviceAccount: web-sa","spec.serviceAccountName: web-sa","spec.containers[].serviceAccount: web-sa","spec.securityContext.serviceAccount: web-sa"],1,"spec.serviceAccountName selects the ServiceAccount for the whole Pod. It is not a per-container or securityContext setting.","ServiceAccounts"],
    ["ck69",0,4,"An app never calls the Kubernetes API. How do you stop an API token from being mounted into its Pods?",["Delete the default ServiceAccount","Remove the Pod's RoleBinding","Set readOnlyRootFilesystem: true","Set automountServiceAccountToken: false"],3,"automountServiceAccountToken: false on the Pod or ServiceAccount skips mounting the token. Deleting the default ServiceAccount breaks Pod creation in the namespace.","ServiceAccounts"],
    ["ck70",0,4,"A container must run as UID 1000 and never as root. Which securityContext settings fit?",["privileged: false and runAsGroup: 0","fsGroup: 1000 and allowPrivilegeEscalation: true","runAsUser: 1000 and runAsNonRoot: true","runAsUser: 0 and readOnlyRootFilesystem: true"],2,"runAsUser sets the UID and runAsNonRoot makes the kubelet refuse to start the container as UID 0. privileged: false alone does not stop the process running as root.","Security contexts"],
    ["ck71",0,4,"A network tool in a container needs to change interface settings but nothing else privileged. What is the least-privilege approach?",["Set privileged: true","Drop ALL and add the NET_ADMIN capability","Run the container as root with hostNetwork","Set allowPrivilegeEscalation: true"],1,"Adding only NET_ADMIN after dropping all capabilities grants just what the tool needs. privileged: true gives nearly full host access.","Security contexts"],
    ["ck72",0,4,"Namespace apps is labeled `pod-security.kubernetes.io/enforce=restricted`. A Pod without runAsNonRoot and without dropping capabilities is submitted. What happens?",["Pod Security Admission rejects it","It runs and a warning is logged","It is mutated to comply and runs","It is scheduled only on tainted nodes"],0,"In enforce mode, Pods that violate the restricted standard are rejected at admission. Pod Security Admission validates only; it does not mutate Pods.","Admission control"],

    /* ---------- Domain 5: Services and Networking (18) ---------- */
    ["ck73",0,5,"Which command creates a ClusterIP Service for Deployment web that listens on port 80 and forwards to container port 8080?",["kubectl expose deployment web --port=80 --target-port=8080","kubectl create service web --port=8080:80","kubectl expose pod web --node-port=80","kubectl port-forward deployment/web 80:8080"],0,"expose copies the Deployment's selector and sets port and targetPort. port-forward only tunnels from your machine and creates no Service.","Services"],
    ["ck74",0,5,"A Service has no endpoints even though its Pods are Running and Ready. What is the most likely cause?",["The Service type is ClusterIP","The namespace has no NetworkPolicy allowing traffic to the Pods","The Pods have no resource limits","The Service selector does not match the Pod labels"],3,"Endpoints are built from Pods that match the selector and are ready. ClusterIP is the normal type and does not block endpoints.","Services"],
    ["ck75",0,5,"Pods in namespace shop must call Service api in namespace backend. Which DNS name always works?",["api.shop.svc.cluster.local","api.backend.svc.cluster.local","backend.api.cluster.local","api.svc.backend.local"],1,"The pattern is <service>.<namespace>.svc.<cluster-domain>. The short name api would only resolve inside backend itself.","Services"],
    ["ck76",0,5,"Read this Service: `type: NodePort`, `port: 80`, `targetPort: 8080`, `nodePort: 30080`. How does an external client reach the app?",["http://<node-ip>:80","http://<cluster-ip>:30080","http://<node-ip>:30080","http://<pod-ip>:30080"],2,"NodePort opens 30080 on every node and forwards to port 80 of the Service, then 8080 on the Pods. The ClusterIP is only reachable inside the cluster.","Services"],
    ["ck77",0,5,"A StatefulSet needs DNS records for each Pod (db-0.db, db-1.db) rather than one virtual IP. Which Service setting provides this?",["type: ExternalName","clusterIP: None (headless)","sessionAffinity: ClientIP","externalTrafficPolicy: Local"],1,"A headless Service has no virtual IP and returns per-Pod DNS records for StatefulSet members. sessionAffinity only pins clients to Pods behind a normal ClusterIP.","Services"],
    ["ck78",0,5,"Apps should call an external database as `db` inside the cluster, which maps to db.example.com. Which Service type fits?",["ClusterIP with no selector","LoadBalancer","NodePort","ExternalName"],3,"ExternalName returns a DNS CNAME to the external host. A selector-less ClusterIP needs manual endpoints with IP addresses instead.","Services"],
    ["ck79",0,5,"Read this NetworkPolicy: `podSelector: {}`, `policyTypes: [Ingress]`, and no ingress rules. What does it do?",["Denies all ingress to every Pod in the namespace","Allows all ingress to every Pod in the namespace","Denies all egress from the namespace","Has no effect because no rules are listed"],0,"An empty podSelector selects all Pods; with Ingress in policyTypes and no rules, no incoming traffic is allowed. Egress is untouched because it is not listed.","NetworkPolicy"],
    ["ck80",0,5,"You apply a valid NetworkPolicy, but traffic it should block still flows. The cluster uses a CNI with no policy support. Why?",["NetworkPolicies need a restart of kube-proxy","NetworkPolicies apply only to NodePort Services","The CNI plugin must enforce NetworkPolicies","Policies take effect only for new namespaces"],2,"The API stores NetworkPolicies, but the network plugin (such as Calico or Cilium) enforces them. kube-proxy handles Service routing, not policy.","NetworkPolicy"],
    ["ck81",0,5,"A NetworkPolicy on backend Pods has one `from` element containing both `namespaceSelector: {matchLabels: {team: web}}` and `podSelector: {matchLabels: {role: fe}}`. Who is allowed?",["Any Pod in team=web namespaces, plus any role=fe Pod","Only role=fe Pods in the backend namespace","Only role=fe Pods in namespaces labeled team=web","All Pods in every namespace"],2,"Selectors in the same from element are ANDed. Listing them as two separate elements would OR them and allow both groups.","NetworkPolicy"],
    ["ck82",0,5,"After adding an egress default-deny policy, Pods cannot resolve any Service names. What must you allow?",["Egress to kube-dns on UDP and TCP port 53","Egress to the API server on port 6443","Ingress from kube-proxy on port 10256","Egress to the node's kubelet on port 10250"],0,"DNS lookups go to the cluster DNS Pods on port 53, which the default-deny egress blocks. The API server is not involved in name resolution.","NetworkPolicy"],
    ["ck83",0,5,"Only Pods labeled app=frontend may reach Pods labeled app=api on TCP 8080. Which ingress rule belongs in a policy selecting app=api?",["from: ipBlock 0.0.0.0/0; ports: TCP 8080","to: podSelector app=frontend; ports: TCP 8080","from: namespaceSelector app=frontend; ports: 8080","from: podSelector app=frontend; ports: TCP 8080"],3,"Ingress rules use from peers, and a podSelector matches Pods in the policy's namespace. namespaceSelector would match namespace labels, not Pod labels.","NetworkPolicy"],
    ["ck84",0,5,"An Ingress is created, but `kubectl get ingress` shows no address and requests never reach the app. What is most likely missing?",["A NodePort Service in kube-system","An Ingress controller for its ingressClassName","A LoadBalancer Service type on the backend application","A NetworkPolicy allowing port 443"],1,"The Ingress resource is only configuration; a controller such as ingress-nginx must implement it. Backends can be ordinary ClusterIP Services.","Ingress"],
    ["ck85",0,5,"An Ingress path is `/api` with `pathType: Prefix`. Which request matches it?",["/apis","/API","/v1/api","/api/v1/users"],3,"Prefix matches whole path segments, so /api/v1/users matches but /apis does not. Matching is case-sensitive, so /API fails.","Ingress"],
    ["ck86",0,5,"Which command creates an Ingress named web that routes host shop.example.com, all paths, to Service web on port 80?",["kubectl create service ingress web --host=shop.example.com","kubectl expose ingress web --host=shop.example.com --port=80","kubectl create ingress web --rule=\"shop.example.com/*=web:80\"","kubectl apply ingress web shop.example.com=web:80"],2,"create ingress uses --rule=host/path=service:port, and /* makes a Prefix path. expose makes Services, not Ingresses.","Ingress"],
    ["ck87",0,5,"An Ingress must serve HTTPS for shop.example.com. What does its tls section reference?",["A ConfigMap containing the certificate","A Secret of type kubernetes.io/tls","A ServiceAccount token","A PersistentVolumeClaim with the key files"],1,"The tls section names the host and a Secret holding tls.crt and tls.key, created with kubectl create secret tls. ConfigMaps are not used for keys.","Ingress"],
    ["ck88",0,5,"You need to test Service db on port 5432 from your laptop without exposing it outside the cluster. Which command?",["kubectl port-forward svc/db 5432:5432","kubectl expose svc db --type=NodePort","kubectl proxy --port=5432","kubectl run tunnel --port=5432"],0,"port-forward tunnels a local port to the Service through the API server without changing the cluster. Switching to NodePort would expose it on every node.","Services"],
    ["ck89",0,5,"A Service defines `port: 80` and `targetPort: http`. What does targetPort: http refer to?",["A named containerPort called http in the Pod spec","The HTTP protocol, which forces Layer 7 routing","Port 80 on the node","An Ingress rule named http"],0,"targetPort can reference a containerPort by name, so Pods can use different port numbers behind the same name. Services do not do Layer 7 routing.","Services"],
    ["ck90",0,5,"curl to a Service times out. The endpoints list correct Pod IPs on port 80, but the containers listen on 8080. What is wrong?",["The Service selector is wrong","CoreDNS is not running","The Pods are failing their readiness probes and are not Ready","The Service targetPort does not match the container port"],3,"Endpoints carry the targetPort, so traffic goes to 80 where nothing listens. A wrong selector would give an empty endpoint list instead.","Services"]
  ]
});
